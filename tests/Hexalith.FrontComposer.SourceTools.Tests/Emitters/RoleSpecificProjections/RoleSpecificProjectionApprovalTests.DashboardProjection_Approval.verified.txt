=== Generated view ===
// Generated by FrontComposer source generator
// Do not edit directly

#nullable enable

using System;
using System.Collections.Generic;
using System.Globalization;
using System.Linq;
using System.Linq.Expressions;
using Fluxor;
using Microsoft.AspNetCore.Components;
using Microsoft.AspNetCore.Components.Rendering;
using Microsoft.FluentUI.AspNetCore.Components;

namespace RoleSpecific.Metrics;

/// <summary>
/// Auto-generated DataGrid view for <see cref="DashboardProjection"/>.
/// </summary>
public partial class DashboardProjectionView : ComponentBase, IDisposable
{
    [Inject]
    private IState<DashboardProjectionState> DashboardProjectionState { get; set; } = default!;

    [Inject]
    private NavigationManager Navigation { get; set; } = default!;

    /// <inheritdoc />
    protected override void OnInitialized()
    {
        DashboardProjectionState.StateChanged += OnStateChanged;
    }

    private void OnStateChanged(object? sender, EventArgs e)
        => InvokeAsync(StateHasChanged);

    /// <inheritdoc />
    public void Dispose()
    {
        DashboardProjectionState.StateChanged -= OnStateChanged;
    }

    private static string Truncate(string value, int maxLength)
        => value.Length <= maxLength ? value : value.Substring(0, maxLength - 1) + "\u2026";

    private static string HumanizeEnumLabel(string value)
    {
        if (string.IsNullOrEmpty(value))
        {
            return value;
        }

        var sb = new System.Text.StringBuilder(value.Length + 10);

        for (int i = 0; i < value.Length; i++)
        {
            char c = value[i];

            if (i == 0)
            {
                sb.Append(char.ToUpperInvariant(c));
                continue;
            }

            bool isUpper = c >= 'A' && c <= 'Z';
            bool prevIsUpper = value[i - 1] >= 'A' && value[i - 1] <= 'Z';
            bool prevIsLower = value[i - 1] >= 'a' && value[i - 1] <= 'z';
            bool prevIsDigit = value[i - 1] >= '0' && value[i - 1] <= '9';

            if (isUpper && prevIsLower)
            {
                sb.Append(' ');
                sb.Append(c);
            }
            else if (isUpper && prevIsUpper && i + 1 < value.Length)
            {
                char next = value[i + 1];
                bool nextIsLower = next >= 'a' && next <= 'z';
                if (nextIsLower && CountConsecutiveUppersBefore(value, i) + 1 >= 3)
                {
                    sb.Append(' ');
                }

                sb.Append(c);
            }
            else if (isUpper && prevIsDigit)
            {
                sb.Append(' ');
                sb.Append(c);
            }
            else
            {
                sb.Append(c);
            }
        }

        return sb.ToString();
    }

    private static int CountConsecutiveUppersBefore(string value, int index)
    {
        int count = 0;
        for (int i = index - 1; i >= 0; i--)
        {
            if (value[i] >= 'A' && value[i] <= 'Z')
            {
                count++;
            }
            else
            {
                break;
            }
        }

        return count;
    }

    /// <inheritdoc />
    protected override void BuildRenderTree(RenderTreeBuilder builder)
    {
        var state = DashboardProjectionState.Value;
        int seq = 0;

        builder.OpenComponent<Hexalith.FrontComposer.Shell.Components.Rendering.FcProjectionSubtitle>(seq++);
        builder.AddAttribute(seq++, "ProjectionType", typeof(DashboardProjection));
        builder.AddAttribute(seq++, "Role", (Hexalith.FrontComposer.Contracts.Attributes.ProjectionRole?)Hexalith.FrontComposer.Contracts.Attributes.ProjectionRole.Dashboard);
        builder.AddAttribute(seq++, "WhenState", (string?)null);
        builder.AddAttribute(seq++, "FallbackCount", state.Items?.Count ?? 0);
        builder.AddAttribute(seq++, "DistinctStatusCount", (int?)null);
        builder.AddAttribute(seq++, "EntityLabel", "Dashboard");
        builder.AddAttribute(seq++, "EntityPluralLabel", "dashboards");
        builder.AddAttribute(seq++, "IsLoading", state.IsLoading);
        builder.CloseComponent();

        if (state.IsLoading)
        {
            builder.OpenComponent<Hexalith.FrontComposer.Shell.Components.Rendering.FcProjectionLoadingSkeleton>(seq++);
            builder.AddAttribute(seq++, "ColumnCount", 3);
            builder.AddAttribute(seq++, "RowCount", 5);
            builder.AddAttribute(seq++, "Layout", Hexalith.FrontComposer.Shell.Components.Rendering.SkeletonLayout.DataGrid);
            builder.AddAttribute(seq++, "EntityLabel", "dashboards");
            builder.CloseComponent();
            return;
        }

        if (state.Items is null || state.Items.Count == 0)
        {
            builder.OpenComponent<Hexalith.FrontComposer.Shell.Components.Rendering.FcProjectionEmptyPlaceholder>(seq++);
            builder.AddAttribute(seq++, "ProjectionType", typeof(DashboardProjection));
            builder.AddAttribute(seq++, "Role", (Hexalith.FrontComposer.Contracts.Attributes.ProjectionRole?)Hexalith.FrontComposer.Contracts.Attributes.ProjectionRole.Dashboard);
            builder.CloseComponent();
            return;
        }

        seq = 100;
        builder.OpenComponent<FluentDataGrid<DashboardProjection>>(seq++);
        builder.AddAttribute(seq++, "Items", state.Items.AsQueryable());

        builder.AddAttribute(seq++, "ChildContent", (RenderFragment)((RenderTreeBuilder b) =>
        {
            int colSeq = 200;

            // Text column: Id
            b.OpenComponent<PropertyColumn<DashboardProjection, string?>>(colSeq++);
            b.AddAttribute(colSeq++, "Title", "Id");
            b.AddAttribute(colSeq++, "Property", (Expression<Func<DashboardProjection, string?>>)(x => x.Id ?? "\u2014"));
            b.CloseComponent();

            // Text column: Kpi
            b.OpenComponent<PropertyColumn<DashboardProjection, string?>>(colSeq++);
            b.AddAttribute(colSeq++, "Title", "Kpi");
            b.AddAttribute(colSeq++, "Property", (Expression<Func<DashboardProjection, string?>>)(x => x.Kpi ?? "\u2014"));
            b.CloseComponent();

            // Numeric column: Value
            b.OpenComponent<PropertyColumn<DashboardProjection, string?>>(colSeq++);
            b.AddAttribute(colSeq++, "Title", "Value");
            b.AddAttribute(colSeq++, "Property", (Expression<Func<DashboardProjection, string?>>)(x => x.Value.ToString("N2", CultureInfo.CurrentCulture)));
            b.AddAttribute(colSeq++, "Class", "fc-col-numeric");
            b.CloseComponent();
        }));

        builder.CloseComponent();
    }
}

=== Diagnostics (HFC*** only) ===
HFC1023 Info: Dashboard projection rendering is deferred to Story 6-3 - DashboardProjection falls back to Default DataGrid rendering in v1.
