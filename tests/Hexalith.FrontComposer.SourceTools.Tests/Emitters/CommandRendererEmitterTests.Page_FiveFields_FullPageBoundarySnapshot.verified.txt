// Generated by FrontComposer source generator (Story 2-2 Task 4.5)
// Do not edit directly

#nullable enable

using System;
using Fluxor;
using Hexalith.FrontComposer.Contracts.Rendering;
using Microsoft.AspNetCore.Components;
using Microsoft.AspNetCore.Components.Rendering;

namespace Demo.Domain;

/// <summary>Generated routable page composing a route header and FullPage renderer (Story 2-2 Decision D5 + D22).</summary>
[Route("/commands/Demo/DemoCommand")]
public partial class DemoCommandPage : ComponentBase
{
    [Inject] private IDispatcher Dispatcher { get; set; } = default!;
    [Inject] private NavigationManager NavigationManager { get; set; } = default!;

    protected override void OnInitialized()
    {
        // Story 2-2 Decision D10 + D30: dispatch RestoreGridStateAction; effect landing in Story 4.3.
        string? viewKey = InferReturnViewKeyFromReferrer();
        if (!string.IsNullOrWhiteSpace(viewKey))
        {
            Dispatcher.Dispatch(new global::Hexalith.FrontComposer.Shell.State.DataGridNavigation.RestoreGridStateAction(viewKey));
        }
    }

    private string? InferReturnViewKeyFromReferrer()
    {
        Uri uri = NavigationManager.ToAbsoluteUri(NavigationManager.Uri);
        if (string.IsNullOrWhiteSpace(uri.Query))
        {
            return null;
        }

        string? projectionTypeFqn = null;
        foreach (string pair in uri.Query.TrimStart('?').Split('&', StringSplitOptions.RemoveEmptyEntries))
        {
            string[] segments = pair.Split('=', 2);
            string key = Uri.UnescapeDataString(segments[0]);
            string value = segments.Length > 1 ? Uri.UnescapeDataString(segments[1]) : string.Empty;
            if (string.IsNullOrWhiteSpace(value))
            {
                continue;
            }

            if (string.Equals(key, "viewKey", StringComparison.OrdinalIgnoreCase))
            {
                return value;
            }

            if (string.Equals(key, "projectionTypeFqn", StringComparison.OrdinalIgnoreCase))
            {
                projectionTypeFqn = value;
            }
        }

        return string.IsNullOrWhiteSpace(projectionTypeFqn)
            ? null
            : "Demo:" + projectionTypeFqn;
    }

    protected override void BuildRenderTree(RenderTreeBuilder builder)
    {
        builder.OpenComponent<global::Microsoft.FluentUI.AspNetCore.Components.FluentStack>(0);
        builder.AddAttribute(1, "Orientation", global::Microsoft.FluentUI.AspNetCore.Components.Orientation.Vertical);
        builder.AddAttribute(2, "VerticalGap", "24px");
        builder.AddAttribute(3, "ChildContent", (RenderFragment)(__page =>
        {
            __page.OpenComponent<global::Hexalith.FrontComposer.Shell.Components.Layout.FcPageHeader>(0);
            __page.AddAttribute(1, "PageTitle", "Demo");
            __page.AddAttribute(2, "Heading", "Demo");
            __page.AddAttribute(3, "HeadingTabIndex", (int?)-1);
            __page.CloseComponent();
            __page.OpenComponent<Demo.Domain.DemoCommandRenderer>(4);
            __page.AddAttribute(5, "RenderMode", (CommandRenderMode?)CommandRenderMode.FullPage);
            __page.CloseComponent();
        }));
        builder.CloseComponent();
    }
}
