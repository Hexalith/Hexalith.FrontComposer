#nullable enable

namespace Hexalith.FrontComposer.SourceTools.Emitters;

using System.Text;

using Hexalith.FrontComposer.SourceTools.Transforms;

/// <summary>
/// Emits Fluxor Action records and Reducer static class source code from a FluxorModel.
/// </summary>
public static class FluxorActionsEmitter
{
    /// <summary>
    /// Generates the Action records source code.
    /// </summary>
    /// <param name="model">The Fluxor output model.</param>
    /// <returns>The generated C# source code string.</returns>
    public static string EmitActions(FluxorModel model)
    {
        StringBuilder sb = new StringBuilder(1024);

        sb.AppendLine("// Generated by FrontComposer source generator");
        sb.AppendLine("// Do not edit directly");
        sb.AppendLine();
        sb.AppendLine("#nullable enable");
        sb.AppendLine();
        sb.AppendLine("using System.Collections.Generic;");
        sb.AppendLine();

        bool hasNamespace = !string.IsNullOrEmpty(model.Namespace);
        if (hasNamespace)
        {
            sb.AppendLine("namespace " + model.Namespace + ";");
            sb.AppendLine();
        }

        // Actions (past-tense naming convention)
        sb.AppendLine("/// <summary>");
        sb.AppendLine("/// Triggers data fetch for <see cref=\"" + model.TypeName + "\"/>.");
        sb.AppendLine("/// </summary>");
        sb.AppendLine("public record " + model.TypeName + "LoadRequestedAction();");
        sb.AppendLine();

        sb.AppendLine("/// <summary>");
        sb.AppendLine("/// Data arrived for <see cref=\"" + model.TypeName + "\"/>.");
        sb.AppendLine("/// </summary>");
        sb.AppendLine("public record " + model.TypeName + "LoadedAction(IReadOnlyList<" + model.TypeName + "> Items);");
        sb.AppendLine();

        sb.AppendLine("/// <summary>");
        sb.AppendLine("/// Data fetch failed for <see cref=\"" + model.TypeName + "\"/>.");
        sb.AppendLine("/// </summary>");
        sb.AppendLine("public record " + model.TypeName + "LoadFailedAction(string Error);");

        return sb.ToString();
    }

    /// <summary>
    /// Generates the Reducer static class source code.
    /// </summary>
    /// <param name="model">The Fluxor output model.</param>
    /// <returns>The generated C# source code string.</returns>
    public static string EmitReducers(FluxorModel model)
    {
        StringBuilder sb = new StringBuilder(1024);

        sb.AppendLine("// Generated by FrontComposer source generator");
        sb.AppendLine("// Do not edit directly");
        sb.AppendLine();
        sb.AppendLine("#nullable enable");
        sb.AppendLine();

        bool hasNamespace = !string.IsNullOrEmpty(model.Namespace);
        if (hasNamespace)
        {
            sb.AppendLine("namespace " + model.Namespace + ";");
            sb.AppendLine();
        }

        sb.AppendLine("/// <summary>");
        sb.AppendLine("/// Fluxor reducers for <see cref=\"" + model.TypeName + "\"/> state transitions.");
        sb.AppendLine("/// </summary>");
        sb.AppendLine("public static class " + model.TypeName + "Reducers");
        sb.AppendLine("{");

        // LoadRequested reducer
        sb.AppendLine("    /// <summary>");
        sb.AppendLine("    /// Handles <see cref=\"" + model.TypeName + "LoadRequestedAction\"/>.");
        sb.AppendLine("    /// </summary>");
        sb.AppendLine("    [Fluxor.ReducerMethod]");
        sb.AppendLine("    public static " + model.StateName + " On" + model.TypeName + "LoadRequested(" + model.StateName + " state, " + model.TypeName + "LoadRequestedAction action)");
        sb.AppendLine("        => state with { IsLoading = true, Error = null };");
        sb.AppendLine();

        // Loaded reducer
        sb.AppendLine("    /// <summary>");
        sb.AppendLine("    /// Handles <see cref=\"" + model.TypeName + "LoadedAction\"/>.");
        sb.AppendLine("    /// </summary>");
        sb.AppendLine("    [Fluxor.ReducerMethod]");
        sb.AppendLine("    public static " + model.StateName + " On" + model.TypeName + "Loaded(" + model.StateName + " state, " + model.TypeName + "LoadedAction action)");
        sb.AppendLine("        => state with { IsLoading = false, Items = action.Items, Error = null };");
        sb.AppendLine();

        // LoadFailed reducer
        sb.AppendLine("    /// <summary>");
        sb.AppendLine("    /// Handles <see cref=\"" + model.TypeName + "LoadFailedAction\"/>.");
        sb.AppendLine("    /// </summary>");
        sb.AppendLine("    [Fluxor.ReducerMethod]");
        sb.AppendLine("    public static " + model.StateName + " On" + model.TypeName + "LoadFailed(" + model.StateName + " state, " + model.TypeName + "LoadFailedAction action)");
        sb.AppendLine("        => state with { IsLoading = false, Error = action.Error };");

        sb.AppendLine("}");

        return sb.ToString();
    }
}
