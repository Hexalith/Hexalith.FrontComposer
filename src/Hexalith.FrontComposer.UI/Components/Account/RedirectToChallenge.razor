@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
        => Nav.NavigateTo(
            $"authentication/challenge?returnUrl={Uri.EscapeDataString(new Uri(Nav.Uri).PathAndQuery)}",
            forceLoad: true);
}
