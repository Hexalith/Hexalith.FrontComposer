---
topic: GOV-1 shared-catalog compatibility, dependency provenance, and privilege-separated governed publication
updated: 2026-09-09T14:21
---

- (direction) Administrator directed this update to adopt _bmad-output/planning-artifacts/architecture/architecture-gov-1-2026-07-19/ARCHITECTURE-SPINE.md as the finalized GOV-1 architecture authority, keep AD-1 through AD-19 stable, reconcile planning sources, record unresolved owner decisions, and make no code, workflow, or GitHub-settings changes.
- (decision) The finalized GOV-1 architecture spine is an adopted companion and the detailed authority for all AD-1 through AD-19 mechanisms; the kernel and reconciled planning sources project it without renumbering, weakening, or duplicating its closed schemas.
- (capability) CAP-1 — Intent: validate shared-catalog compatibility semantically while recording a deterministic bounded committed-object dependency graph as provenance. Success: compatible pointer advances pass, incompatible catalogs fail with exact edge diagnostics, and graph identity is sealed without any commit or fingerprint allowlist.
- (capability) CAP-2 — Intent: carry one authenticated CI-tested candidate through governed release and independent verification. Success: handoff v3 and manifest v4 preserve the exact candidate, policy, quality run, evaluator closure, publication candidate, attestation or fallback, assets, and final disposition across every attempt.
- (capability) CAP-3 — Intent: separate candidate construction from protected product publication. Success: candidate-controlled code executes only in a secretless read-only builder, while a candidate-free protected publisher authenticates data, performs final attestation or fallback validation, seals and classifies manifest v4, and alone can publish.
- (capability) CAP-4 — Intent: authorize fallback only for one authenticated Release run and attempt when attestation capability is explicitly unsupported. Success: the publisher accepts only an unexpired run-bound authorization v3 matching the candidate, CI handoff, active policy, fallback digest, production approval, and Release coordinates; any drift or retry requires new authorization.
- (capability) CAP-5 — Intent: classify every governed Release attempt and preserve durable, monotonic evidence for failures and incidents. Success: handoff v3 maps every attempt to one closed disposition, ledger observations are append-only, incidents cannot be overwritten by later green observations, and incomplete publication is preserved in an immutable incident evidence Release before retry.
- (capability) CAP-6 — Intent: gate split-publication adoption on authenticated implementation and review evidence. Success: the GOV-1 split implementation gate accepts only the canonical conformance packet and approval projection defined by the spine, with every required closure row closed and no Critical or High nonconformance.
- (constraint) AD-1 through AD-19 retain their current IDs and are authoritative; summaries defer to the spine on any ambiguity.
- (constraint) GOV-1 changes no runtime or public API behavior, generated output, package inventory, dependency versions, or UX.
- (constraint) Builder results can deny early but never authorize; final publication authority resides only in pinned candidate-free protected publisher code.
- (constraint) The target contracts are hexalith.release-verification-handoff.v3, hexalith.release-evidence.v4, hexalith.publication-candidate.v1, frontcomposer.release-ledger-record.v2, and the run-bound fallback authorization v3 defined by the spine; legacy schemas are audit-only for new publication.
- (constraint) Author signing, a production PFX, RFC 3161 author timestamps, and same-job candidate build and publication are superseded; author-unsigned candidates require GitHub provenance attestation or the approved unsupported fallback and NuGet.org repository-signature verification.
- (constraint) Production releases remain halted until Product and Release owners accept AD-19 and the halt posture, a split Builds revision enters the AD-16 lineage, the incident runbook and response target are approved, and the GOV-1 split implementation gate passes.
- (constraint) HEXALITH_RELEASE_PUBLISH_ENABLED is deny-only and must be literal false during the halt; it can never authorize publication.
- (question) Product Owner and Release Owner: do you accept AD-19, the production-release halt, and the current no-exception posture recorded by PRD D-16 and G-8?
- (question) Release Owner or repository administrator: when will HEXALITH_RELEASE_PUBLISH_ENABLED be set to literal false and verified by an authenticated API observation?
- (question) Hexalith.Builds owner and Release Owner: which immutable successor revision implementing split-publication-v1 is accepted into the AD-16 lineage?
- (question) Product Owner and Release Owner: what measurable incident acknowledgement and containment target is approved, and when will docs/release-incident-response.md be approved and exercised or table-topped?
- (question) Release Owner: when will the no-bypass protected-main ruleset required for conformance approval and ledger sign-off exist and be verified?
- (note) Wrapper-only content: workflow-skill ceremony, review narration, historical validation logs, and detailed implementation chronology are not kernel content; authoritative mechanism remains in the adopted spine and audit history remains in source artifacts.
- (decision) The GOV-1 kernel adopts owner-decisions.md plus the finalized spine, parent architecture, FC-DEP-1, G2 request, GOV-1 story, and PRD as companions because each remains load-bearing to owner acceptance, upstream delivery, or implementation gating.
- (decision) OD-1 through OD-6 in owner-decisions.md explicitly track Product Owner, Release Owner, Hexalith.Builds owner, and repository-control decisions without assuming role equivalence or default acceptance.
- (event) Source reconciliation completed for architecture.md, FC-DEP-1, the GOV-1 story, the G2 request, PRD projection/addendum, epics.md, and the GOV-1 nonconformance register; no code, workflow, policy, dependency, submodule, or GitHub setting was changed.
- (capability) CAP-2 superseding clarification — Intent unchanged: carry one authenticated CI-tested candidate through governed release and independent verification. Success: handoff v3, manifest v4, and ledger v2 together preserve the candidate, policy, quality run, evaluator closure, publication candidate, attestation or fallback, assets, and final disposition without assigning ledger state to the manifest.
- (capability) CAP-5 superseding clarification — Intent unchanged: classify every governed Release attempt and preserve durable monotonic evidence. Success: handoff v3 supplies closed attempt state to the total classifier, ledger observations are append-only, incidents cannot be overwritten by later green observations, and incomplete publication is preserved in an immutable incident evidence Release before retry.
- (validation by assistant) Self-validation pass 1 — coherence PASS: six capabilities each have one intent and one success condition; constraints are design-bending; non-goals are explicit; the success signal is concrete; CAP-1 through CAP-6 are stable and unique; open Product Owner and Release Owner decisions are surfaced without invention.
- (validation by assistant) Self-validation pass 2 — preservation PASS: every load-bearing request and spine claim is present in SPEC.md or an adopted companion; AD-1 through AD-19 remain authoritative and stable; split roles, handoff v3, manifest v4, run-bound fallback, incident recovery, durable evidence, the split implementation gate, superseded-language disposition, and unresolved owner decisions are reconciled. Historical ceremony is retained only where needed to record its explicit supersession.
