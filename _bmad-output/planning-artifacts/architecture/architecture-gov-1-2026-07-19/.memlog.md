---
scope: GOV-1 shared-catalog compatibility and dependency provenance
purpose: build substrate for developers and Release Owners
altitude: epic
updated: 2026-09-09T12:31
---

- (assumption) Fast path: the architecture amendment is the only deliverable; audience is GOV-1 developers and Release Owners.
- (constraint) Brownfield sources conflict: final FC-DEP-1/PRD/epics say complete reachable graph while one proposal paragraph says root plus direct nested; full historical censuses are non-reconciled and expose legacy identities.
- (decision) GOV-1 v1 uses a bounded committed-object graph: all gitlinks at the FrontComposer root commit (depth 1) plus all gitlinks in each exact root-selected repository commit (depth 2); edges below depth 2 are outside v1. This binds graph collectors, CI, manifests, and tests and prevents incompatible traversal boundaries.
- (decision) Repository resolution is closed-world: only canonical identities declared by FrontComposer root .gitmodules may be resolved. Approved GitHub HTTPS/SSH/scp forms normalize to lowercase host/owner/repo without credentials, terminal .git, or trailing slash; unknown or unsafe identities fail closed.
- (decision) The collector reads explicit commits with Git plumbing, records each edge before visited checks, keys edges by owner_repository+owner_commit+path, and keys visited catalog work by repository+commit. It never uses ambient index/submodule HEADs, clones candidate URLs, or initializes nested submodules.
- (decision) hexalith.dependency-graph.v1 binds root repository/commit and sorted edges with owner_repository, owner_commit, path, repository, commit, depth; Builds edges also bind raw catalog SHA-256 and nullable catalog_contract_version. Strict lowercase 40-hex/64-hex and normalized ASCII POSIX paths apply.
- (decision) Edges sort by depth, owner_repository, owner_commit, path, repository, commit. Graph digest is SHA-256 over UTF-8 compact JSON with lexicographically sorted object keys and the explicit edge order, excluding graph_digest; the value domain is ASCII. This is project canonicalization v1, not RFC 8785.
- (decision) Within the depth-2 boundary, missing objects, missing/duplicate mappings, unknown identities, duplicate edges, malformed inputs, unavailable catalogs, or exceeded limits fail closed. Deeper edges are excluded by definition, not unresolved.
- (assumption) Resource ceilings for v1: 4096 total edges, 1 MiB per committed .gitmodules blob, and 4 MiB per catalog blob; exceeding a ceiling fails closed. These generous caps require confirmation during review.
- (decision) CI diffs explicit base and candidate graphs by owner_repository+path+repository and runs each affected module once from an allowlisted FrontComposer-owned command map; no candidate-supplied commands or recursive initialization.
- (decision) Semantic catalog compatibility and exact provenance remain separate. Raw catalog bytes are hashed; semantic validation operates on the same bytes; fingerprints never permit or deny compatibility. Offline manifest verification checks structure/order/digest and live verification recomputes the exact sealed graph.
- (version) Reality checked 2026-07-19: local Git 2.53.0 and Python 3.14.4; official current Git/Python docs confirm ls-tree -z/--full-tree, config --blob, explicit revision syntax, deterministic JSON controls, and SHA-256 APIs remain supported.
- (direction) Defer complete historical transitive traversal and mandatory BUILD-CAT-1 markers to separately versioned approvals with legacy identity resolution and migration evidence.
- (assumption) Correction: selecting Fast path approved drafting mode, not the bounded boundary or detailed canonicalization. AD-1, identity grammar, edge/digest recipe, resource ceilings, and exact CI mapping remain assumptions until explicit Architect and Release Owner ratification.
- (decision) All depth-1/2 edges, including self and back-reference edges, are evidence and are recorded. Owner object reads deduplicate by repository+commit; the fixed depth boundary guarantees termination and visited checks never suppress edges.
- (decision) Object acquisition is separate from collection. The collector is offline/object-only; CI may fetch exact objects from the root identity plus root-declared approved remotes into isolated temporary bare stores. Missing objects after bounded acquisition fail closed.
- (decision) The complete graph envelope is schema, root{repository,commit}, edge_count, edges, graph_digest. edge_count equals len(edges) and is included in digest material. JSON bytes use ensure_ascii=true, allow_nan=false, sort_keys=true, separators comma/colon, UTF-8, no BOM or trailing newline. The outer existing manifest seal binds the complete graph object and remains authoritative.
- (decision) Catalog blob load/parse/hash caches by Builds repository+commit; semantic evaluation runs for every selecting edge or equivalent owner contract profile; evidence retains every selector and diagnostics aggregate every owner.
- (decision) CI compares the exact merge-base commit with the exact pull-request candidate head. Added/changed depth-1 edges build candidate target modules; added/changed/removed depth-2 edges build candidate owner modules; removed depth-1 edges build FrontComposer root; modules deduplicate by canonical identity; unchanged graphs build none.
- (decision) Graph/schema rollout is atomic across collection, semantic diagnostics, preparation, sealing, offline/live verification, fallback invalidation, fixtures, Governance pins, and post-publication verification. Any graph/catalog failure yields blocked or invalid evidence and publish_authorized=false before external side effects.
- (constraint) GOV-1 changes no runtime/public API, generated output, package inventory, dependency versions, or UX. After implementation, Story 11.17d reruns its complete promotion lane on the exact candidate revision.
- (assumption) Exact inclusive ceilings proposed for review: 4096 edges, 64 MiB raw ls-tree output per owner commit, 1 MiB raw .gitmodules blob, 4 MiB raw catalog blob. Measure before decoding/parsing and fail before consuming bytes beyond the ceiling; boundary fixtures are required.
- (direction) If bounded v1 is ratified, reconcile PRD, epics, and the internally contradictory proposal from complete-reachable wording to complete-defined-v1 wording; until then canonical amendments and spine stay draft.
- (constraint) Reality check: depth-2 includes four FrontComposer self-target edges, so the trusted universe must include the explicit root identity in addition to root-declared identities.
- (decision) A FrontComposer-owned versioned dependency-graph policy binds trusted identity/path mappings, semantic contract profiles, module commands, and limits; committed base/candidate .gitmodules are graph data validated against that policy. The policy is included in release-definition and fallback fingerprints.
- (decision) The Python graph engine is the single canonical semantic-policy implementation: it reads/hashes/parses exact catalog blobs and evaluates versioned owner profiles. C# Governance invokes and asserts its machine result while retaining repository-wide MSBuild ownership checks.
- (decision) V1 supports Git SHA-1 object format only and rejects participating repositories using another object format. A future graph schema may support SHA-256 Git object IDs.
- (decision) URL grammar: HTTPS forbids userinfo, port, query, fragment; SSH/SCP requires user git, host github.com, default port, and exactly owner/repository; strip one terminal .git, lowercase ASCII, reject percent escapes, controls, extra/dot segments and duplicate normalized identities/paths.
- (decision) PR candidate is github.sha (the pull-request merge revision built by existing CI), with github.event.pull_request.base.sha as explicit base input and a recorded computed merge-base; all graph collection and affected builds use the candidate merge SHA. Push uses github.event.before to github.sha; zero/unavailable base is fail-closed full-affected.
- (constraint) Implementation-start HEAD 600f4c73 bounded census remains 8+32=40 edges and 7 Builds selectors but selects 6 distinct Builds commits, not the creation-time 5. Counts are evidence only and fixtures/story must reconcile before implementation.
- (version) The affected-module and Governance gates also bind .NET SDK 10.0.302; participating Git repositories currently report object format sha1.
- (assumption by architect) Active trust/semantic/command policy comes from the immutable base/before revision; candidate policy changes activate only for a later change, with a one-time unchanged-graph frozen bootstrap approved by Architect and Release Owner.
- (assumption by architect) Every Builds-selector owner has an explicit semantic profile and every governed target has an explicit build or evidence-only disposition; there are no implicit defaults.
- (assumption by architect) The seed module matrix builds FrontComposer, EventStore, Tenants, Commons, Builds, PolymorphicSerializations, Memories, and Parties by exact solution argv; AI.Tools is evidence-only because its seed commit has no solution.
- (decision by architect) PR graph diff records event base, computed merge-base, and CI merge SHA and fails closed unless merge-base equals event base.
- (assumption by architect) Dependency graph JSON uses a closed schema and rejects duplicate member names, missing or unknown members, boolean integers, and invalid depths before digest acceptance.
- (decision by architect) Release must receive workflow_run.head_sha explicitly, check out and propagate that exact CI-tested revision, and seal an immutable reusable Hexalith.Builds workflow identity; publication remains frozen while the current seam is non-conforming.
- (assumption by architect) GOV-1 manifest schema v2 migration is one-way: legacy manifests are audit-only, never publishable or fallback-eligible, never upgraded or resealed in place.
- (decision by architect) Semantic profiles are normative: baseline catalog structure/ownership rules plus exact FrontComposer, EventStore, Memories, and Parties consumer requirements from existing Governance; root catalog formatting remains root-only.
- (decision by architect) Affected .NET modules use exact standalone Release/NuGet argv with Configuration=Release and UseNuGetDeps=true; AI.Tools retains an explicit evidence-only disposition.
- (decision by architect) A zero or unavailable push-before revision cannot select trust and is never release-eligible; only approved-bootstrap diagnostic full-affected evaluation may run.
- (decision by architect) Release must bind the triggering successful CI run ID to an artifact whose recorded candidate equals workflow_run.head_sha.
- (decision by architect) CI-to-release handoff is dependency-release-handoff.v1: one named duplicate-free JSON artifact authenticated through GitHub Actions run/artifact metadata and sealed by raw SHA-256.
- (decision by architect) Manifest v2 places closed dependency_graph, dependency_policy, and workflow_provenance at top level; the existing outer seal covers them, and fallback digest additionally binds graph_digest and policy_sha256.
- (decision by architect) The one-time policy bootstrap is machine-addressed by Release Owner-controlled HEXALITH_DEPENDENCY_POLICY_BOOTSTRAP_SHA256 and becomes unreachable once a base policy exists; zero-before runs fail the gate.
- (decision by architect) Publishing workflow provenance records actual caller/reusable coordinates and every action source; all non-local uses are 40-hex pinned and Builds local actions come from the exact reusable workflow SHA, never @main.
- (decision by architect) Affected module builds materialize the bounded regular-file tree of the exact selected Builds commit, not catalog-only bytes; caps are 16,384 files, 16 MiB per blob, 256 MiB total, with unsafe modes/paths rejected.
- (decision by architect) Depth-1 changes are classified first and subsume descendant depth-2 churn; removals build FrontComposer and never schedule a nonexistent candidate owner.
- (decision by architect) Handoff/workflow provenance fixes JSON scalar types, action uniqueness, and ordinal action ordering; release reloads and hashes the recorded exact policy blob before acceptance.
- (decision by architect) Primary CI reusable workflow and transitive actions are immutable trusted evaluator inputs recorded in the handoff and workflow definition digest; current CI @main references are non-conforming.
- (decision by architect) CI evaluator definition_digest canonically hashes exact caller/reusable/action sources; manifest CI provenance must equal the handoff projection and bind the raw handoff JSON SHA-256, with both CI-only and combined workflow digests reverified offline.
- (event by architect) Reviewer gate passed: rubric, adversarial, and current-fit report no unresolved Critical/High finding; AD-1 and draft assumptions remain pending Architect + Release Owner ratification.
- (decision by user) Administrator explicitly ratified the complete GOV-1 focused architecture draft as Architect and Release Owner, including the depth-1/2 v1 graph boundary, canonical schemas, policy activation model, CI/release provenance, and all stated resource ceilings.
- (event by architect) Ratification received; finalize the spine and reconcile PRD, Epic, proposal, contract, canonical architecture, and GOV-1 story sources.
- (change by architect) Reconciled PRD, Epic, sprint proposal/status, FC-DEP-1, canonical architecture, GOV-1 story, and BUILD-REL-1 issue-17 request to the ratified boundary, policy-rooted evaluator authorization, bounded static action closure, both exact-candidate handoffs, manifest v2, and external completion gate.
- (event by architect) Final deterministic lint and rubric/current-fit/adversarial reviewer gates passed with zero unresolved Critical/High findings.
- (event by architect) Main advanced externally during finalization from 600f4c73 to c585073c with Builds/EventStore gitlink changes; the refreshed defined-v1 census remains 40 edges, 7 Builds selectors, and 6 distinct Builds commits, so architecture decisions are unchanged and Task 1 retains the production-start freeze.
- (event) spine finalized
- (event by architect) Update run started 2026-09-08 (Fast path, resumed from memlog) to roll the 2026-09-08 validation gate into the spine; backfilling unlogged history 2026-08-03..2026-09-07 first.
- (event by architect) Backfill 2026-08-03: commit 8a6a6cb3 amended AD-11 (Story 11.17d completed 2026-08-02 under waiver), AD-12 (policy is sole executable authority; spine does not mirror volatile rows) and AD-13 (authenticated handoff candidate is sole release authority) without bumping updated, logging here, or re-running the gate.
- (event by architect) Backfill 2026-08-04: commit 3ebbdce9 switched release.yml from workflow_run to operator workflow_dispatch from the exact current main SHA, authenticated against one completed successful push-CI run via release_contract.py select-ci; the HEXALITH_RELEASE_PUBLISH_ENABLED REL-4 freeze path was retired; parent architecture.md gained the 2026-08-04 release-integration update naming hexalith.dependency-release-source.v1 as replacing the pending AD-16 handoff.
- (event by architect) Backfill 2026-08-06: v4.1.0 (ac6a9ae8) and v4.1.1 (4a3e44f0) published under fallback approval; 4a3e44f0 made production classify-release bind the live fallback digest before comparison (approver+expiry contingency), contradicting AD-9 drift invalidation.
- (event by architect) Backfill 2026-08-08: commit 7b3d8f3b; Hexalith.Builds issue 17 reopened with the GOV-1 amendment and owner-accepted immutable revision a8a50859fa2f27f511a9470dfe1e3ae54d0ebc1a recorded (issuecomment-5226651759); evaluator_authorizations populated; ci.yml/release.yml pinned to 40-hex Builds commits; AD-13/AD-15 caller wiring landed. DW-1783 closed 2026-08-27 on it; DW-1688, contract frontmatter, sprint-status external_gate and the GOV-1 story still say closed-without-acceptance (stale).
- (event by architect) Backfill 2026-08-13..2026-08-22: execution SHA advanced by the two-phase pattern (pre-authorize next closure in policy, then pin) a8a50859 -> 99d5a46c -> 3f0e3595 -> 4eb33928 (8cb9f06a, 2026-08-22); 2026-09-05 05f77621 authorized the live release.yml closure for 4eb33928. Parent architecture.md still names 3f0e3595 as approved execution identity (stale).
- (event by architect) Backfill 2026-08-16: commit dfbe9978 split the Builds catalog gitlink (depth-1 graph identity, currently a32cb422) from the Builds CI/CD execution commit (reusable workflow identity, builds-execution-sha); equality is not required.
- (event by architect) Backfill 2026-08-30: commit df689935 (tag v4.2.0) made prepare-manifest emit hexalith.release-evidence.v3 with reduced workflow_provenance {ci:{run(7 members),evidence_sha256}, release:{caller,reusable,builds_execution_sha}, definition_digest}; v2 accepted for verify/seal/fallback only; Governance pins v3.
- (event by architect) 2026-09-08 validation gate (lint 0; rubric FAIL; current-fit PASS-with-findings; adversarial FAIL; code-drift FAIL; security PASS-with-findings): 1 Critical (A-1 fallback digest live-rebound), 17 High; report validation-report-2026-09-08.md. Graph core AD-1..AD-8, AD-10 holds; release seam AD-9, AD-11..AD-16 stale or in conflict with code.
- (version by architect) Reality check 2026-09-08: global.json .NET SDK 10.0.400 rollForward latestPatch (was 10.0.302); local Git 2.53.0 (upstream 2.55.0), Python 3.14.4 (upstream 3.14.7), plumbing/json/hashlib semantics unchanged; GitHub Actions contexts verified (github.sha PR merge commit, event.before, workflow_run.*, github/job.workflow_ref+sha; job.* is GitHub.com-only, not GHES); upload-artifact v7.0.1 pinned, artifacts immutable, names unique per run; all participating repositories report object format sha1; Git 3.0 will default new repositories to sha256.
- (constraint by architect) Validation-report correction: v4.1.0/v4.1.1 tags are dated 2026-08-06 (evidence recorded 2026-08-14); owner acceptance of a Builds revision is recorded (a8a50859, 2026-08-08), so AD-16's gate is satisfied in lineage though the pinned SHA has since advanced via pre-authorized closures.
- (decision by user) A-1 ratified: AD-9 drift-invalidation guarantee stands. approved_against_fingerprints_sha256 is an immutable Release Owner-recorded input (vars.RELEASE_ATTESTATION_FALLBACK_FINGERPRINTS_SHA256) captured outside any release run; classify recomputes the formula live and fails closed on inequality; no prepublish/release/verification step may write, default, or rebind it; a drift fixture is required. Implementation nonconformance escalated: release_evidence.py bind_live_fallback_digest and release.yml not passing the variable.
- (decision by user) T1/T2 ratified: release is operator workflow_dispatch from the exact current main SHA; dispatch SHA == live main == authenticated successful push-CI head == handoff candidate; publication jobs run under the production environment with Release Owner review; dispatching actor sealed. AD-16 becomes the timeless invariant: Builds owner-accepted revision a8a50859 (issue 17, 2026-08-08) is the accepted lineage root; later execution pins are valid only via the two-phase pre-authorized-closure pattern under delayed activation. workflow_run caller and REL-4 freeze wording retired from AD-13.
- (decision by user) A-3 ratified: a local uses under .hexalith/builds-execution/ resolves into Hexalith.Builds at the single literal 40-hex checkout ref present in the same workflow blob text (exactly one match, static, never runtime state) and that ref must equal the authorization's reusable commit / builds-execution-sha; zero or multiple matches fail closed.
- (decision by user) A-11 ratified: the gitlink path is graph evidence, not trusted policy data; identity is the trust key; policy local_path is acquisition data; a submodule move is a removed+added logical edge that schedules affected builds.
- (decision by architect) T3: AD-17 manifest v3. prepare-manifest emits only hexalith.release-evidence.v3 with closed workflow_provenance {ci:{run:{repository,workflow_path,run_id,run_attempt,event,branch,head_sha},evidence_sha256}, release:{caller,reusable,builds_execution_sha}, definition_digest=canonical({ci,release})}; ci.evidence_sha256 is the raw AD-13 handoff SHA-256 and transitively binds the CI evaluator closure; v2 manifests are verify/audit-only historical evidence, never produced, never fallback-eligible for a new publication; AD-14 retitled as the one-way migration rule.
- (decision by architect) A-5: fallback formula identifiers pinned: workflow_definition = workflow_provenance.definition_digest (combined ci+release); definition = the FALLBACK_INVALIDATION_FILES map {relative POSIX path: 64-hex sha256} plus exactly one helper_version key (sha256 of release_evidence.py version) and no other keys; lists owned by eng/release_evidence.py and pinned by a Governance fixture; eng/dependency_handoff.py and eng/workflow_source_closure.py are members of both lists.
- (decision by architect) A-2: unchanged relation defined in AD-8: two logical edges with the same (owner_repository,path,repository) key are unchanged iff every member except depth-1 owner_commit is equal (depth-2 edges compare all members); graph unchanged means every logical edge unchanged and none added/removed; reused by AD-12.
- (decision by architect) T5/A-4: post_release stage retained; reusable is JSON null for a caller whose static closure reaches no reusable workflow; closures contain only caller-reachable sources; the post-release verifier must draft and match a post_release authorization before ledger acceptance. Nonconformance escalated: release-evidence.yml checks no post_release authorization and the five policy rows name an unreachable reusable and are stale.
- (decision by architect) A-6: AD-15 evaluator is exactly the AD-13 evaluator shape {caller,reusable,actions,definition_digest}. A-8/D-8: artifact names are <base>-<run_id>-<run_attempt>, exactly one match, recorded run coordinates must equal the authenticated ones, zip archive required. A-9: assets are GitHub Release assets, unique and sorted by name, manifest.path is the sealed manifest asset name. D-9: an attempt without an authenticated CI handoff emits release-verification-handoff.deferred.json; the verifier records a failed attempt and can never green.
- (decision by architect) T6/S-1/D-4: runtime github/job.workflow_sha observation and production closure recomputation are deferred; production trust is the exact caller blob SHA-256 at the candidate commit matched to one active-policy row whose literal reusable commit and action closure are immutable by construction. Evaluator code is not delayed-activated in v1; accepted explicitly with branch protection plus required review of trust-changing files as the compensating control (Deferred, revisit trigger: a second FrontComposer-external contributor gains write).
- (decision by architect) D-6: on push to main, an AD-13 closure absent from the active policy is a gate failure, not a soft-defer; the two-phase pattern (pre-authorize, then pin) makes exit 2 unnecessary. Nonconformance escalated (DW-1795). D-10: hexalith.dependency-release-source.v1 is a CI diagnostic only, never release-eligible; prepare-manifest accepting --source-proof as candidate authority is nonconformance to remove.
- (decision by architect) S-2/S-3: AD-18 publication actor and least-privilege stage tokens: workflow-level permissions contents:read; only the publication job holds contents:write, id-token:write, attestations:write, NUGET_API_KEY under the production environment; no GOV-1 job holds actions:write; pull_request_target forbidden; graph collection and affected-module builds run secretless read-only. S-6: ledger = frontcomposer.release-ledger-record.v2 in the release-verification artifact plus the repository-tracked REL-AI-1 Release Evidence Ledger appended by the Release Owner, backed by immutable GitHub Release assets; non-compliant/partial-publish dispositions are incident states, not retryable to green.
- (decision by architect) Wording pins adopted: A-7 Builds edge iff repository == github.com/hexalith/hexalith.builds (v1 constant, equal to policy builds_identity); A-10 ordered AD-3 grammar (scheme/host case-sensitive, reject any port, strip one trailing slash then one terminal .git, lowercase owner/repository only, ssh:// form allowed, gitlink paths byte-exact); A-12 base-policy existence means a blob at the canonical path regardless of validity; A-13 seal = SHA-256 over AD-5 canonical bytes of every top-level member except seal; A-15 escape set; A-16 all-zero OID never valid; A-17 ci.run.head_sha = handoff run.candidate; A-18 edge ceiling cumulative; D-12 stage-keyed evaluator_authorizations; S-7 artifact immutability reliance; S-9 content-address trust; S-10 JS action bundle.
- (direction by architect) R-6/R-9/R-10/R-5: AD-6 reduced to the coverage invariant (roster moved to seed); bootstrap procedure replaced by one historical sentence; Story 11.17d note moved here; caps stated once in AD-7; Inherited Invariants section added citing parent architecture.md bullets; Consistency rows added for Builds catalog-vs-execution identity (R-7) and evidence envelopes (R-8); Deferred entries for Git 3.0 trigger (C-7), revocation/idempotency (S-8), SHA-1 acceptance (S-9), diagnostics retention.
- (event by architect) Story 11.17d completed 2026-08-02 under its recorded one-story GOV-1 promotion waiver; not reopened by this architecture (moved from AD-11 Rule).
- (event by architect) Update reviewer gate pass 1 (2026-09-08): lint 0; rubric PASS-with-findings (1H); current-fit PASS-with-findings (1H); adversarial FAIL (3H); code-drift PASS for spine, FAIL for implementation (17 NC items); security PASS-with-findings (1H). All prior validate findings closed except R-10 bloat and R-16 story wording. Fixes applied in one pass.
- (decision by architect) AU-2: every Governance pin named by the spine executes inside the release authority; select-ci additionally requires exactly one completed successful push run of .github/workflows/quality.yml whose head equals the dispatched SHA (Governance Gate 2b and tests/eng suites run only there today). quality.yml is a deny-only precondition, not a trust source, and stays outside the fingerprint lists.
- (decision by architect) AU-1: in a Builds blob itself (domain-ci.yml/domain-release.yml) a local uses under .hexalith/builds-execution/ resolves to the enclosing Builds commit; the single-literal-ref rule applies to non-Builds callers only. SU-2: any other step writing into that path (second checkout, other repository, mutable ref, expression, run: step) fails closed; match is over raw blob text incl. comment lines, repository:/ref:/path: within six lines (AU-16).
- (decision by architect) AU-3/AU-4/AU-5/AU-14: AD-15 handoff bumped to hexalith.release-verification-handoff.v2 adding release.publication_started; published = reusable publication job success; publication_started && !published = partial-publish-incident with GitHub/NuGet inspection; artifact holds exactly one of handoff+CI copy or .deferred.json, anything else = missing-artifact incident; the independently downloaded CI artifact is authoritative and the copy must hash-match; release_run.conclusion is a needs projection {success,failure,cancelled}, evidence not authentication, verifier records the API value too.
- (decision by architect) RU-1/SU-3/AU-9/AU-7: actors and environment approval live only in the ledger record; frontcomposer.release-ledger-record.v2 is a closed shape {schema,release_run,verification_run,candidate,disposition,published,tag,manifest_verified,published_bytes_verified,repository_signatures_verified,incident,evaluator,actors,environment_protection,publish_gate_variable}; SU-4: verifier reads environments/production read-only and records required_reviewers/prevent_self_review/can_admins_bypass/branch policy; prevent_self_review must be true once a second Release Owner exists. Sealed manifest and AD-13 handoff shapes stay closed.
- (decision by architect) RU-3: lineage predicate: a Builds commit is in the accepted lineage iff it first appears as a pre-authorized reusable.commit in a policy revision that was itself the active policy of a later change, chain rooted at the revision recording a8a50859; Git ancestry not required. AU-19: CI and release pins independent, both in lineage, builds_execution_sha = release pin. NC-16: 0a3508b3 pre-authorizations are unused lineage members pending Release Owner prune decision.
- (decision by architect) RU-4: the PR gate recomputes every added/changed evaluator row's closure from exact blobs and fails closed on actions/closure_digest difference (authoring-time enforcement); production-time recomputation stays deferred. AU-6: rows sorted by (caller.repository, caller.workflow_path, caller.blob_sha256, closure_digest), caller.blob_sha256 unique per stage. SU-9: null reusable conforming only for post_release; its execution commit binds through reached local actions' commit.
- (decision by architect) AU-12/AU-13/SU-5/SU-6: fallback definition fingerprints hash the exact blob at the candidate commit, missing blob fails closed with no sentinel; helper_version = sha256(utf8(__version__)), helper content deliberately outside the digest; approval is single-use per (candidate, run_id, run_attempt) and the Release Owner records it via release_evidence.py fallback-digest after push CI completes and before dispatch. AU-8/SU-7: caller write scopes exactly {contents,id-token,attestations,issues,pull-requests}; assertion surface = caller blobs + reusable blob at reusable.commit. AU-10: execution-SHA equality set includes checkout refs in release.yml and release-evidence.yml. AU-11: catalog identity resolved from the graph, path-literal lookups non-conforming. AU-17: seal = {algorithm,hash,sealed_at}. AU-18: asset sha256 = prepared-candidate bytes, must equal GitHub digest. AU-20: v2 accepted only by verify-manifest/audit. AU-21: raw member SHA-256 is the binding identity. AU-22: dependency_policy.commit == revisions.base exactly. AU-15: exactly one completed ci.yml push run with that head, and it succeeded. RU-6/RU-8/RU-9/CU-3/CU-4/CU-6/CU-7/SU-8/SU-10 wording pins applied.
- (decision by architect) SU-1: evaluator-code acceptance restated honestly: main is unprotected, no CODEOWNERS, a non-admin write collaborator exists; sole control today is Release Owner diff review before production approval; intended control = main ruleset without bypass + code-owner review + protection state in ledger; revisit triggers named. SU-11: network ceilings deferred to job timeout.
- (decision by architect) Reconcile: spine carries the dropped inherited requirements: Release-configuration NuGet-resolved affected builds (AD-8), ownership boundaries and BUILD-REL-1 governed reusable mode (Inherited Invariants), durable evidence = GitHub Release assets vs short-retention artifacts (AD-15), root-only BOM/CRLF working-tree gate (AD-2/AD-6), catalog path in errors, full-history merge-base checkout (AD-8), prepare->seal->offline->live->classify->publish ordering (AD-9).
- (direction by architect) R-10/RU-2 bloat: accepted as won't-fix; this spine is deliberately contract-grade (6.7k words) because the adversarial lens repeatedly shows that unnamed member sets and sort keys are exactly where two implementers diverge; member lists stay in the spine and the contract doc mirrors them.
- (change by architect) Reconciled parent architecture.md (updated 2026-09-08): release-integration note, bootstrap consumed, manifest v3 lineage + immutable fallback digest, AD-13 handoff replaces source-proof wording, quality.yml precondition, execution identity = AD-16 lineage (a8a50859 root, currently 4eb33928), production environment. Reconciled contract FC-DEP-1 (amended 2026-09-08): frontmatter accepted revision, bootstrap, decisions 13/14/15/16, consequences (freeze retired, lineage prerequisite).
- (change by architect) Wrote nonconformance-register-2026-09-08.md (NC-1..NC-27) as the implementation hand-off; companion wording drift (GOV-1 story, G2 request, sprint-status GOV-1 item, DW-1688) listed there for their owners, not edited in this run.
- (event by architect) Reviewer gate pass 2 (2026-09-08): rubric PASS-with-findings (0H, 3M fixed: Key Invariants citation, BUILD-REL-1 row sourced to G2, reported_conclusion in ledger record); adversarial FAIL (3H, all side effects of pass-1 fixes); security PASS-with-findings (1H = same quality.yml issue). All pass-1 findings confirmed closed.
- (decision by architect) AU2-1/SU2-1: quality.yml is a deny-only success precondition recorded as quality_run, outside every trust structure (no registry stage, no fingerprint list, not in AD-18 surface), may reference mutable actions; trust-bearing Governance assertions execute inside ci.yml dependency-governance (NC-28). AU2-3: injection rule applies to explicit path: values and run: text only; path-less checkouts are the caller's own candidate checkout. AU2-4/SU2-2: Builds-blob carve-out requires repository Hexalith/Hexalith.Builds + ref inputs.builds-execution-sha, and the closure asserts caller input == reusable.commit. AU2-5/AU2-7/SU2-4: publication_started/published from needs.release.result; verifier inspects GitHub/NuGet for every started attempt; publish_gate_variable in handoff v2, not true => gate-frozen. AU2-6: PR-gate recompute locates caller blobs in candidate or committed fixtures. AU2-8/SU2-3: lineage predicate verifiable from first-parent policy history, per commit; lineage admits, active rows authorize, retiring a pin removes rows. SU2-5: Release Owner review covers all main commits since the previous governed release. NC-28..NC-30 added to the register.
- (event by architect) spine amended and finalized 2026-09-08 (AD-1..AD-16 stable, AD-17 manifest v3 and AD-18 publication actor/tokens added; status final; parent architecture.md and contract FC-DEP-1 reconciled; nonconformance-register-2026-09-08.md NC-1..NC-30 handed to implementation)
- (event) Update run resumed 2026-09-09 from the finalized memlog to resolve validation-report-2026-09-09.md; deliverable remains the GOV-1 architecture spine for developers and Release Owners.
- (decision) Administrator ratified the split-phase publication privilege boundary: a secretless/read-only builder produces an authenticated run-bound package artifact; a separate production-protected publisher executes only pinned owner-controlled code, never checks out or executes candidate source, and treats verified package bytes as data. This binds AD-13, AD-17, AD-18, the external Hexalith.Builds reusable contract, and handoff/ledger verification, preventing candidate-controlled code from executing with publication credentials.
- (version) Reality check 2026-09-09 against official GitHub documentation: environment secrets are unavailable until protected-job approval; reusable workflow GITHUB_TOKEN permissions can only be maintained or reduced; workflow artifacts carry SHA-256 digests and support inter-job transfer; artifact attestations require id-token/attestations write and are beneficial only when verified. These capabilities fit the ratified secretless-builder/protected-publisher split.
- (decision) Canonical JSON is byte-for-byte Python 3.14 stdlib json.dumps(value, ensure_ascii=True, allow_nan=False, sort_keys=True, separators=(',', ':')) UTF-8 with no BOM/newline; floats are forbidden, alternate legal JSON escapes are noncanonical, and cross-language golden vectors cover quotes, backslashes, every C0 short/control escape class, DEL, BMP, and supplementary surrogate pairs. This amends AD-5 and prevents digest divergence.
- (decision) AD-13 CI/quality selection filters completed successful push runs first and requires exactly one match for the candidate head; failed/cancelled earlier attempts neither authorize nor create ambiguity, while duplicate successful reruns fail closed. quality_run is carried and authenticated in the Release-to-verifier handoff, never re-selected post-release.
- (decision) The BUILD-REL-1 successor contract is split-publication-v1: an unprotected secretless builder may execute the exact candidate and emits one run-bound publication-candidate artifact with a closed descriptor, sealed manifest, and manifest-declared files; a production-protected publisher authenticates that artifact and executes only pinned owner-controlled workflow/action/tooling, treats its contents as non-executable data, never checks out the candidate, and never restores, builds, packs, installs, or loads candidate release configuration/plugins. The exact reusable closure binds both phase roles.
- (decision) AD-15 handoff advances to hexalith.release-verification-handoff.v3: it carries the authenticated quality_run and publication_candidate artifact coordinate/digest/descriptor digest, records mode split-publication-v1, and includes a total direct-needs result projection. A governed attempt is selected by the exact active release evaluator and split reusable contract, independent of one legacy inner job name; exact builder/publisher API role names and topology are contract data, and missing/duplicate/mismatched roles fail closed.
- (decision) frontcomposer.release-ledger-record.v2 keeps its member IDs but gains closed types and the disposition vocabulary deferred-no-ci-handoff, no-releasable-commits, gate-frozen, rejected-before-publication, compliant-candidate, non-compliant, missing-artifact, partial-publish-incident. incident is null for non-incident states and exactly {kind,phase} for incident states. Records are append-only observations keyed by immutable Release-attempt and verification-run coordinates; duplicate observation keys must be byte-identical and any incident permanently dominates later green observations in REL-AI-1.
- (decision) Post-release verification may use a later active-policy-authorized post_release evaluator for historical/recovery checks, records that evaluator independently, and never projects it as release-time provenance. It executes no ambient/default-branch or released-candidate repository helper: executable logic is inline in the pre-authorized caller blob or in pinned actions in its exact closure.
- (decision) Incident containment is Release Owner-owned: before any later dispatch, set HEXALITH_RELEASE_PUBLISH_ENABLED=false, preserve immutable evidence, rotate credentials when exposure is possible, unlist rather than replace affected packages, and correct only under a new version. The dedicated runbook remains deferred but must land before split-publication-v1 integration.
- (constraint) Because this run's deliverable is spine-only, parent architecture.md, FC-DEP-1, GOV-1 story, G2 request, and nonconformance register are not edited. Their same-job BUILD-REL-1, handoff-v2, and register wording becomes explicit companion drift requiring owner reconciliation before split-publication-v1 implementation.
- (decision) hexalith.publication-candidate.v1 carries a closed release_plan: null means no releasable commit and requires null manifest/empty publish set; otherwise version, tag, release name, and a declared release-notes data path bind the publisher. This prevents the builder and publisher selecting different release identities without executing candidate release configuration in the protected job.
- (decision) AD-15 disposition mapping is ordered and total: external publication plus incomplete/unequal state => partial-publish-incident; otherwise required artifact/topology failure => missing-artifact; otherwise authorization/contract/verification failure => non-compliant; only then may exactly one non-incident outcome match. Zero or multiple matches are non-compliant.
- (decision) Ledger API-run conclusions use the current closed GitHub workflow-run vocabulary (success, failure, cancelled, action_required, neutral, skipped, stale, startup_failure, timed_out); unavailable or future values fail closed. The handoff's reported_conclusion remains the separate deterministic three-state direct-needs projection.
- (constraint) Parent reconciliation restores the full FR-24 exact-candidate evidence set: the secretless builder must run inventory, tests, package-consumer validation, checksums, SBOM, and symbol evidence against the exact packages later published; the split changes privilege placement, not evidence obligations.
- (constraint) Parent NuGet verification invariant remains binding: GitHub assets are raw-byte equal to the publication candidate; NuGet.org repository signing may add only root .signature.p7s, so verification requires a valid repository signature plus normalized ZIP-member equivalence for every other member rather than raw archive equality.
- (constraint) A successful governed GitHub Release is immutable, non-draft, and its tag resolves exactly to the AD-13 candidate; split publication does not relax the parent tag binding.
- (decision) FC-DEP-1's approved fail-closed treatment governs the deferred sentinel: deferred-no-ci-handoff remains a terminal incident-bearing disposition, not a non-incident observation. This corrects the provisional ledger mapping without requiring a new owner choice.
- (constraint) Split ownership: Hexalith.Builds owns reusable workflow/action code and minimum permission contract; FrontComposer owns the publication-candidate/manifest/evidence schemas, exact output bytes, classification, selected reusable invocation, and publication/verification operation; Release Owner retains approval, credentials, exceptions, and incident containment.
- (decision by architect) The AD-9 final authorization gate belongs exclusively to the candidate-free protected publisher. The builder emits authenticated packages, evidence, release metadata, and deterministic manifest inputs for early denial only; after artifact authentication, pinned owner-controlled publisher code obtains the required provenance attestation or validates the sealed approved-unsupported fallback, prepares and seals manifest v3, performs offline/live verification, classifies, and requires publish_authorized=true before NuGet/GitHub Release publication.
- (constraint by architect) REL-5's dated 2026-08-04 decision supersedes stale G2 author-signing/RFC 3161 requirements: candidates remain author-unsigned and NuGet.org repository-signed. Non-signing attestation/fallback controls remain mandatory; the protected publisher holds OIDC/attestation authority, binds the attestation bundle or approved-unsupported fallback into the final manifest and handoff, and post-release verification requires it for compliant-candidate.
- (constraint by architect) The split-publication-v1 Builds contract is opt-in and default-off, preserving all existing reusable-workflow callers unchanged; its candidate builder keeps checkout submodules:false and initializes only dependencies declared by FrontComposer's root .gitmodules, never nested submodules recursively.
- (decision by architect) AD-15 non-incident classifications are disjoint: gate-frozen is evaluated whenever the gate is not literal true; with gate true, no-releasable requires a successful builder descriptor with null release_plan, while rejected-before-publication requires builder/prepublication failure. publication_candidate is required after builder success and before any publisher start; otherwise it is null only for builder non-success or a valid deferred sentinel.
- (constraint by architect) The Release Owner incident runbook is docs/release-incident-response.md and must land before split-publication-v1 integration. It must require acknowledgement plus the AD-15 containment record before any retry/later dispatch and allow publish-gate re-enable only after documented cause/remediation, credential rotation when exposure is possible, external inventory, independent verification, and Release Owner approval.
- (constraint by architect) The spine-only nonconformance handoff must enumerate eight closure bundles before implementation: accepted split reusable plus delayed activation; caller switch/exact topology; removal of production from candidate jobs; publication-candidate producer/authentication/hostile fixture; handoff v3/ledger migration; candidate-free final classification/publication; pinned post-release helper; duplicate destination asset-name rejection. The register remains source-owner work and release-ineligible until these close.
- (decision by architect) Because the protected publisher now owns attestation and final AD-9 classification, publication_started no longer means that its job merely began. It is set by pinned owner-controlled publisher code immediately before the first NuGet/GitHub Release mutation, cross-checked against the authenticated publisher job result and observed external state; a protected job that fails before this marker is rejected-before-publication, while any later incomplete/unequal state is partial-publish-incident.
- (decision by architect) Manifest v3 seals quality_run as the exact AD-13 selected quality projection, but quality remains a deny-only precondition outside workflow_provenance.definition_digest and does not become a trust-source evaluator.
- (decision by architect) Manifest v3 gains an exact top-level attestation member byte-identical to the AD-15 {status,bundle,fallback_fingerprints_sha256} projection; AD-14 sealing covers it and producer/verifier/fixtures/fallback material migrate atomically.
- (decision by architect) GitHub provenance-attestation registration over already-authenticated candidate package digests is classified as evidence production, not package/Release publication. It is the sole pre-publish_authorized external evidence side effect, cannot mutate candidate bytes/tags/releases/assets, and any failure prevents publication; the parent wording must be reconciled before implementation.
- (constraint by architect) Architecture adoption does not close PRD D-16/G-8. Product Owner and Release Owner acceptance, parent/source propagation, the accepted Builds revision, implementation conformance, and the approved incident runbook/response target remain external gates. Until then HEXALITH_RELEASE_PUBLISH_ENABLED=false is the authoritative emergency stop, releases halt, and no bounded risk exception is authorized.
- (decision by architect) REL-AI-1 Release Owner sign-off is a separate append-only frontcomposer.release-ledger-signoff.v1 record bound to the byte-exact machine observation digest and an authenticated approving GitHub review; machine observations are never mutated. Missing sign-off remains visibly pending and cannot turn an incident green.
- (decision by architect) Correction to entries 127-128 after brownfield schema inspection: selected quality_run and the exact top-level attestation projection require hexalith.release-evidence.v4, not an in-place v3 mutation. Existing v3 bytes remain audit-only; new preparation emits only v4, preserving historical evidence and AD-14 one-way migration.
- (decision by architect) GitHub job permissions, environment secrets, and OIDC authority exist for the whole protected publisher job; the enforceable split invariant is that this job executes only pinned candidate-free owner code. That code must authenticate the publication artifact before first credential/token/API use, but the architecture no longer claims step-level permission elevation.
- (decision by architect) Approved-unsupported fallback is explicitly CI-evidence-bound rather than falsely single-use. A closed Release Owner authorization record permits only live GitHub-attestation capability unavailability, binds the exact candidate/CI/fingerprint digest, uses canonical issuance/expiry with a maximum 24-hour window, and cannot cover ordinary action, network, validation, or verification failure. Each Release handoff records the exact authorization hash; publication/version uniqueness prevents retry after side effects.
- (decision by architect) Attestation uses actions/attest v4.2.0 at f7c74d28b9d84cb8768d0b8ca14a4bac6ef463e6 with explicit publisher-generated subject-checksums, push-to-registry false, create-storage-record false, and show-summary false. Verification requires GitHub's OIDC issuer, FrontComposer source/caller/reusable/run/environment identity, SLSA provenance v1, and the exact sorted package subject set with no extras.
- (decision by architect) Protected publisher and post-release closures may make data-only requests only to the GitHub and NuGet endpoints named by the contract; runtime acquisition or execution of scripts, installers, packages, plugins, actions, redirects, or configuration is forbidden. Executable code must be present in the pre-authorized literal-commit closure; platform runner/runtime remains an explicit GitHub trust dependency.
- (constraint by architect) Publication archive caps: raw 512 MiB, at most 256 members, each uncompressed member 128 MiB, aggregate uncompressed 1 GiB, compression ratio at most 100:1 per member and aggregate, path depth 8, path length 240 UTF-8 bytes. Scan streaming before extraction, then materialize only declared regular members into a fresh temp directory.
- (decision by architect) All evidence integers use the interoperable JSON safe-integer domain: identifiers and run coordinates 1..9007199254740991; counts and sizes 0..9007199254740991 unless a narrower AD-7 bound applies. Boundary/overflow vectors are mandatory.
- (decision by architect) AD-15 gains a nullable prepublication_denial enum so expected gate-true builder/attestation/fallback/manifest/live/classification denials map only to rejected-before-publication; malformed/unauthenticated contract failures map to non-compliant. The same reason is copied to the ledger.
- (decision by architect) BUILD-REL-1 split-publication-v1 is exposed only as a default-off opt-in of .github/workflows/domain-release.yml; any sibling workflow may be an internal literal-pin implementation detail but is never a caller-selected reusable.
- (decision by architect) Environment approval evidence records an ordinally unique approver-login set plus approval_observed_at, the verifier's sampling time, because GitHub review history exposes no authoritative approval timestamp. Required reviewers use a tagged User/Team projection with exactly one login/slug.
- (decision by architect) Release Owner sign-off is authenticated by a GitHub APPROVED pull-request review whose exact canonical review body binds the observation key and machine-record digest; the later append-only signoff projection records repository, PR, head SHA, review ID/state/submitted_at and signer. This avoids circularly requiring the server review ID inside the reviewed commit.
- (decision by architect) Immutable GitHub Release publication sequence is create draft, upload and verify the complete collision-checked asset set, push/verify NuGet packages, then publish the draft once; successful state is non-draft and immutable. Any abandoned draft, tag, asset, or package side effect is partial-publish-incident.
- (constraint by architect) Canonical timestamps use exactly UTC second precision YYYY-MM-DDTHH:MM:SSZ. GitHub asset names use conservative 1..128 ASCII alphanumeric/dot/underscore/hyphen grammar, start/end alphanumeric, and ordinal uniqueness.
- (constraint by architect) Durable GitHub Release evidence includes the raw publication-candidate archive, its descriptor, the raw CI handoff, final manifest, and attestation bundle when attested, in addition to declared package/symbol/evidence assets; short-lived Release handoff bytes may expire after their canonical digest and ledger projection are retained.
- (decision by architect) Close the second reviewer wave by authenticating fallback authority through an exact GitHub APPROVED review and retaining its raw canonical record; preserve partial-publication evidence in an immutable, reserved incident-evidence Release before any retry; permit only GitHub platform resolution of closure-listed literal-SHA actions; forbid nested reusable workflows from defining either split job; key publication concurrency on a strict canonical NuGet version subset; use no artifact-metadata permission when storage-record creation is disabled; verify attestations through the verifier bundled in the pinned actions/attest closure; define exact ZIP ratio arithmetic, raw-archive streaming validation, and a two-PR ledger-signoff protocol. The live release variable observed true remains an external Critical halt blocker, and implementation acceptance requires a named conformance packet bound to the final spine and exact implementation commits.
- (decision by architect) Reviewer Gate pass at spine 9dc9ded5 blocks finalization. Replace the recursive same-repository fallback PR with a canonical workflow-dispatch request whose digest is the exact production deployment-review comment for the same Release run, authenticated through the run approvals API with duplicate projections collapsed. Define incident_recovery as a second, namespace-limited candidate-free protected actor rather than contradicting the sole product publisher. Permit one bounded signed-storage redirect for raw Actions artifacts; pin privileged runners to ubuntu-24.04; use exact GitHub repository casing in signed attestation fields; key ledger approval/signoff paths for multiple signers; close incident slot states and recovery retries; fully authenticate implementation-gate check/run evidence; mark Structural Seed as target state; and include all authoritative policy fields in AD-12.
- (decision by architect) Final gate rerun closes all prior critical findings but requires three last convergence fixes: bind the fallback deployment comment to request digest plus Release run ID and run attempt; state the exact equality map from every implementation-gate run/artifact/check to the claimed FrontComposer, Builds, policy, and caller identities, including a single-parent hostile fixture delta; and authenticate each reviewer result with a closed machine sidecar from an authenticated review run plus preserve the Release Owner's exact packet-PR approval through a second projection PR. Also distinguish post_release and incident_recovery pins consistently in AD-13.
- (event by architect) spine finalized — rubric, configured current-technology, configured adversarial-divergence, and supplemental security reviews pass with zero Critical/High architecture findings. Code-drift review separately records that release eligibility remains blocked by the live publish flag being true, missing split implementation/source reconciliation, and missing repository controls/conformance evidence; final architecture status does not waive those Adoption gates.
