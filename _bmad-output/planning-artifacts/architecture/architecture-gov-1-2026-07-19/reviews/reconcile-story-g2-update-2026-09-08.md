# Reconciliation — GOV-1 story, G2 request, sprint-status, DW-1688 vs. ARCHITECTURE-SPINE (2026-09-08 update)

Spine: `_bmad-output/planning-artifacts/architecture/architecture-gov-1-2026-07-19/ARCHITECTURE-SPINE.md`
(`updated: 2026-09-08`, AD-1..AD-18). Ratified decisions taken from the last ~20 `.memlog.md` entries
(A-1, T1/T2, A-3, A-11, T3/AD-17, A-5, A-2, T5/A-4, A-6/A-8/A-9/D-9, T6/S-1/D-4, D-6/D-10, S-2/S-3/S-6, wording pins).

Live facts checked read-only on 2026-09-08 (no submodule commands, no `references/` cd):

- `.github/workflows/ci.yml:25` → `domain-ci.yml@4eb33928a1d8c7775f97221cf9edc171db0cb5f8` (not `@main`).
- `.github/workflows/release.yml:7` `workflow_dispatch`; `:321` `domain-release.yml@4eb33928…`; `:329` `builds-execution-sha: 4eb33928…`; no `freeze-guard` / `HEXALITH_RELEASE_PUBLISH_ENABLED` anywhere in the file.
- `eng/dependency-graph-policy.json` `evaluator_authorizations` = ci 7 / release 7 / post_release 5 rows (not empty).
- `eng/release_evidence.py:64-65` `MANIFEST_SCHEMA = "hexalith.release-evidence.v2"`, `CURRENT_MANIFEST_SCHEMA = "hexalith.release-evidence.v3"`; `bind_live_fallback_digest=True` at `:3834` (the A-1 nonconformance the spine escalates).
- `deferred-work.md`: DW-1783 (closed 2026-08-27, accepted revision recorded), DW-1795 (exit-2 soft-defer nonconformance, open).

Line numbers below are the input file's own line numbers. Replacement sentences are written in the input's voice.

---

## Input 1 — GOV-1 story (`_bmad-output/implementation-artifacts/gov-1-validate-shared-catalog-compatibility-and-seal-dependency-provenance.md`)

### (A) Statements the spine now contradicts or supersedes — 24

| # | Line(s) | Stale statement | Replacement (story voice) | Spine authority |
|---|---|---|---|---|
| S-1 | 9 vs 23 | Frontmatter `status: done` while body reads `Status: in-progress` — the file disagrees with itself. | Pick one value and set both lines to it. If `done`: "Status: done — external AD-16 gate satisfied in lineage (issue 17 accepted revision `a8a50859…`, 2026-08-08; caller pins advanced to `4eb33928…` by the two-phase pre-authorized closure pattern); the 2026-09-08 architecture validation escalated nonconformances A-1, D-6, D-10 and T5 as separate ledger items, not as GOV-1 scope." If `in-progress`: "Status: in-progress — blocked only on the escalated nonconformances (A-1 fallback digest live-rebind, D-6 exit-2 soft-defer, D-10 `--source-proof` candidate authority, T5 missing `post_release` authorization check), not on the external Builds revision." | AD-16 Rule; memlog "Validation-report correction" + "2026-09-08 validation gate" |
| S-2 | 3 | `updated: 2026-08-04` | `updated: 2026-09-08` (the 2026-08-08 Debug Log/Completion Notes edits were never dated in frontmatter). | — |
| S-3 | 15 | `upstream_release_follow_up: Reopen BUILD-REL-1 issue 17 with the GOV-1 amendment or file a successor` | `upstream_release_follow_up: Done 2026-08-08 — issue 17 reopened with the GOV-1 amendment; owner-accepted revision a8a50859fa2f27f511a9470dfe1e3ae54d0ebc1a (issuecomment-5226651759). Later execution pins advance only via AD-12 two-phase pre-authorized closures.` | AD-16 |
| S-4 | 19 | `external_completion_gate: Qualifying immutable revision from reopened Hexalith.Builds issue 17 or successor; integration and end-to-end proof only` | `external_completion_gate: satisfied in lineage 2026-08-08 (a8a50859…); integration landed in 7b3d8f3b; current execution pin 4eb33928… authorized by the active policy` | AD-16; memlog backfill 2026-08-08/08-13..22 |
| S-5 | 56 | "…affected-module dispositions, manifest v2, authenticated CI handoff, and immutable workflow provenance." | "…affected-module dispositions, the release-evidence manifest lineage (v2 at ratification, v3 `hexalith.release-evidence.v3` since 2026-08-30 per AD-17), authenticated CI handoff, and immutable workflow provenance." | AD-14, AD-17 |
| S-6 | 68-74 | "Execution boundary corrected 2026-08-02: … remain externally blocked until the Release Owner reopens issue 17 … and records an owner-accepted immutable revision satisfying AD-13, AD-15, and AD-16." | Append: "Resolved 2026-08-08: the Release Owner reopened issue 17 with the complete amendment and recorded owner-accepted revision `a8a50859…`; FrontComposer pinned `ci.yml`/`release.yml` to 40-hex Builds commits, populated `evaluator_authorizations`, and landed the AD-13/AD-15 caller wiring (commit 7b3d8f3b). The external boundary is closed; remaining work is the nonconformance set escalated by the 2026-09-08 architecture validation." | AD-16 |
| S-7 | 86 (AC5) | "**when** manifest v2 is processed" | "**when** the current sealed manifest (`hexalith.release-evidence.v3`, AD-17; v2 accepted for verification of historical evidence only) is processed" | AD-14, AD-17 |
| S-8 | 86 (AC5, last sentence) | "…classification, incident handling, and freeze controls remain intact." | "…classification, incident handling, and the publication-actor controls (operator `workflow_dispatch` from the exact `main` SHA, `production` environment with Release Owner review, least-privilege stage tokens — AD-13/AD-18) remain intact." The REL-4 `HEXALITH_RELEASE_PUBLISH_ENABLED` freeze path was retired on 2026-08-04 and no AD carries a freeze control any more. | AD-13, AD-18; memlog T1/T2 + backfill 2026-08-04 |
| S-9 | 92-95 | "…at-most-once set of affected modules, including explicit bootstrap handling." | "…at-most-once set of affected modules, including the fail-closed zero/unavailable-base diagnostic path; no policy bootstrap mode is reachable (the one-time v1 bootstrap was consumed on 2026-07-19)." | AD-12 Activation |
| S-10 | 106 | "**Given** a publishable manifest v2" | "**Given** a publishable `hexalith.release-evidence.v3` manifest" | AD-17 |
| S-11 | 117 | "Reopen it with the complete amendment or file a successor; only reusable-workflow integration … await the accepted revision…" | "Reopened and owner-accepted 2026-08-08 at `a8a50859…`; integration, pins, and policy authorization landed in 7b3d8f3b." | AD-16 |
| S-12 | 121, 137 | "`evaluator_authorizations` … landed as a closed, empty registry" / "successful sealed emission still requires a non-empty active-policy `evaluator_authorizations.ci` entry (AD-16 external gate below)" | Add: "Populated 2026-08-08 and advanced through pre-authorized closures; the active policy now holds stage-keyed `ci`/`release`/`post_release` rows (7/7/5 at 2026-09-08). An empty registry is no longer the live state." | AD-12 Evaluator authorizations |
| S-13 | 141 | "Empty/`@main` evaluator state fails closed with exit 2 and a deferred diagnostic without failing the graph gate." | "On push to `main`, a caller closure absent from the active policy is a gate failure (exit non-zero, job red), never a soft deferral; the two-phase pre-authorize-then-pin pattern makes exit 2 unnecessary. The current exit-2 path is nonconformance DW-1795." | AD-12 (closure absent → fails the push gate); memlog D-6 |
| S-14 | 144 | Task 4 external-gate bullet: "after the Release Owner reopens issue 17 … integrate its owner-accepted 40-hex revision. Pin the primary CI reusable workflow … FrontComposer must not … use `@main`, or claim integration complete while that revision is pending." | "Done 2026-08-08 (7b3d8f3b): `ci.yml` pinned to the policy-authorized Builds commit (lineage `a8a50859` → `99d5a46c` → `3f0e3595` → `4eb33928`, each pre-authorized before pinning); the CI evaluator row's `caller.blob_sha256` matches the exact `ci.yml` blob. `@main` no longer appears in the closure." Check the box or convert it to a dated record. | AD-13 Evaluator trust; AD-16 |
| S-15 | 150 | "emit `hexalith.release-evidence.v2` with … and AD-14 `workflow_provenance` objects" | "emit only the current schema `hexalith.release-evidence.v3` with the complete AD-5 `dependency_graph`, the AD-14 `dependency_policy` projection, and the AD-17 `workflow_provenance` `{ci:{run,evidence_sha256}, release:{caller,reusable,builds_execution_sha}, definition_digest}`; v2 is verify/audit-only." | AD-14, AD-17 |
| S-16 | 151, 240 | "Implement the exact v2 fallback formula over definition, package set, graph digest, policy SHA-256, and workflow definition digest." / "V2 fallback invalidation binds…" | "Implement the AD-9 fallback formula `sha256(canonical({definition, package_set, dependency_graph, dependency_policy, workflow_definition}))` where `definition` is the `FALLBACK_INVALIDATION_FILES` map plus exactly one `helper_version` key and `workflow_definition` is the AD-17 combined `definition_digest`. `approved_against_fingerprints_sha256` is an immutable Release Owner-recorded input (`vars.RELEASE_ATTESTATION_FALLBACK_FINGERPRINTS_SHA256`); classify recomputes live and fails closed on inequality — no step may rebind it (current `bind_live_fallback_digest=True` is nonconformance A-1)." | AD-9; memlog A-1, A-5 |
| S-17 | 153 | "Reject duplicate/unknown v2 members…" | "Reject duplicate/unknown v3 members…" | AD-17 |
| S-18 | 156 | "…so the caller authenticates the triggering run ID/attempt and event head through read-only Actions APIs … Preserve the REL-4 freeze … (release eligibility remains AD-16-gated while `evaluator_authorizations` is empty / CI reusable stays `@main`)." | "…so the operator-dispatched `release.yml` (on `refs/heads/main` only) requires the dispatched `github.sha` to equal the live `main` ref and the head of exactly one completed successful push-CI run selected by `release_contract.py select-ci`, then downloads that run's `dependency-release-handoff-<run_id>-<run_attempt>` artifact and treats its recorded candidate as sole release authority. Publication jobs run under the `production` environment with Release Owner review (AD-18); the REL-4 freeze variable was retired 2026-08-04. Release eligibility is no longer gated on an empty registry or an `@main` reusable." | AD-13 Rule; AD-18; memlog T1/T2 |
| S-19 | 157 | "`release-evidence.yml` … prefers the AD-15 candidate over second-hop `head_sha`" and "require a policy-authorized post-release closure" (checked) | "`release-evidence.yml` derives the live root only from the AD-15 handoff candidate and sealed manifest and never from `workflow_run.head_sha`, `GITHUB_SHA`, a branch, or a tag lookup ('prefers' implies a fallback that AD-15 forbids). The post-release authorization check is not yet implemented: the workflow matches no `post_release` row and the five policy rows name an unreachable `reusable` (must be JSON `null`) — escalated by T5/A-4; this sub-bullet is not satisfied as written." | AD-15 Rule; AD-12 (`reusable` null); memlog T5/A-4 |
| S-20 | 158 | Task 5 external-gate bullet: "…prove both exact-candidate handoffs plus manifest-v2 verification end to end." | "Integration to the accepted lineage landed 2026-08-08; v4.2.0 (df689935, 2026-08-30) sealed a `hexalith.release-evidence.v3` manifest through the pinned reusable at an authorized execution SHA. What remains is not this gate but the escalated nonconformances (A-1, D-6, D-10, T5)." | AD-16, AD-17; memlog backfill 2026-08-30 |
| S-21 | 162, 429, 431-433 | "`Story12_4_RedPhaseDefTests.cs` pins the `hexalith.release-evidence.v2` manifest schema"; File List rows say "manifest v2 / manifest-v2 fixture" | "Governance pins `hexalith.release-evidence.v3` (since df689935); fixtures are sealed as v3 through the actual helper; v2 fixtures are retained only as historical verify/audit inputs." | AD-17 (Governance pins); memlog backfill 2026-08-30 |
| S-22 | 180, 188, 244, 252 | Dev Notes: ci.yml "Release is triggered from this workflow's conclusion. Current reusable/transitive `@main` references are mutable." / release.yml "preserving the REL-3/REL-4 publication policy" / "Preserve … REL-4's default-frozen publication gate" / "No … REL-4 freeze redesign" | "Release is operator `workflow_dispatch` from the exact current `main` SHA authenticated against one successful push-CI run; `ci.yml`/`release.yml` reference Builds by literal 40-hex commits." / "preserving REL-3 exact-artifact enforcement and the AD-18 production-environment publication policy" / "Preserve REL-3 exact-artifact enforcement and the AD-13/AD-18 dispatch-and-environment gate (REL-4 freeze retired 2026-08-04)." / "No REL-3 signing/artifact-scope redesign, AD-18 actor/token redesign, REL-5 operational authorization, or actual publication." | AD-13, AD-18 |
| S-23 | 199 | ".NET SDK 10.0.302 are available at story creation." | Keep as historical, add: "Reality check 2026-09-08: `global.json` now pins .NET SDK 10.0.400 (`rollForward: latestPatch`); the affected-module gate binds whatever `global.json` selects." | Spine Stack |
| S-24 | 254, 307, 399, 404, 407, 409, 410, 443-458 | Never-List "issue 17 closed without a qualifying revision"; Previous-Story "11.17d … remains `in-progress`"; Debug Log 2026-08-08 "External AD-16 gate unchanged (issue 17 still closed; empty `evaluator_authorizations`; CI still `@main`)"; Completion Notes repeating the same; Suggested Review Order "AD-13/AD-15 … a schema production never emits", `deferred-work.md:2051` pointer | Leave dated log lines as history but add a dated 2026-09-08 Completion Note: "Superseded: issue 17 was reopened and accepted the same day (a8a50859…, 7b3d8f3b); `evaluator_authorizations` is populated (7/7/5); `ci.yml`/`release.yml` are pinned to `4eb33928…`; Story 11.17d completed 2026-08-02 under its waiver; DW-1783 closed 2026-08-27. The 'AD-13/AD-15 never emitted' finding is closed; open items are A-1, D-6 (DW-1795), D-10, T5." Replace L254 with "BUILD-REL-1 issue 17 accepted revision `a8a50859…` on 2026-08-08; execution pins advance only by AD-12 two-phase closures. No local contingency is authorized by this story." Replace L307's first sentence with "Story 11.17d completed 2026-08-02 under its recorded one-story GOV-1 promotion waiver and is not reopened." | AD-16; memlog "Story 11.17d completed" |

Also stale but not spine-driven: L13 `baseline_commit: 3786330d…` predates 7b3d8f3b and later commits (validator note, see memory); L455 `deferred-work.md:2051` no longer points at the divergence entry (now DW-1783 at ~L8721, closed).

### (B) Quiet requirements in the story the spine dropped and should carry

| # | Story line(s) | Requirement | Spine status | Suggested carry |
|---|---|---|---|---|
| B-1 | 132, 205, 408 | Root-catalog BOM/CRLF format check (`assert_builds_checkout_format_policy`) deliberately reads the checked-out working tree, not the raw object, and is a root-only repository-format assertion, not part of every catalog's semantic contract. | AD-2 says evidence is read only from exact Git objects and never from a mutable checkout; the memlog records "root catalog formatting remains root-only" but AD-6 text does not state the format check or its working-tree exception. As written, the story's departure conflicts with AD-2. | Add one sentence to AD-6 (or AD-2 exception list): "The root-only catalog format assertion (BOM/CRLF) is a repository-format check outside the semantic profile; it may read the checked-out root working tree and is never release evidence." |
| B-2 | 82 (AC3), 133 | Fail-closed diagnostics must name the **catalog path** alongside owner repository/commit/path and selected Builds commit. | Consistency Conventions "Errors" lists owner repository/commit/path, selected repository/commit, and the precise mismatch — catalog path is missing. | Add "selected catalog path" to the Errors convention row. |
| B-3 | 145 | `quality.yml` supplemental gate must keep Gate 2b and root-only init and must fetch enough exact history that `fetch-depth: 1` is never the only source for merge-base or candidate-object proof. | The spine names `ci.yml`, `release.yml`, `release-evidence.yml` in AD-18/Structural Seed but never `quality.yml`; AD-8/AD-10 do not state the history-depth requirement for the supplemental lane. | Add `quality.yml` to the Structural Seed with "supplemental Python/Governance coverage; full history for merge-base proof; root-only init" or state the history requirement once under AD-8. |
| B-4 | 86 (AC5), 154, 244 | REL-3 pack-once, symbol checksums, immutability probe, signing/timestamp/attestation, and `prepare -> seal -> live verify -> classify` ordering in `eng/release_prepublish.py` remain intact. | Inherited Invariants cite the parent FR-24 row generically; no AD states the prepublish ordering invariant that AD-9's "before NuGet/GitHub side effects" depends on. | Add the ordering sentence to AD-9 or the Inherited Invariants table ("`release_prepublish.py` ordering prepare → seal → live verify → classify is inherited from REL-3 and unchanged"). |
| B-5 | 133, 161 | Test-fixture coverage obligations (compatible advance, multi-selector same commit, every AD-7 ceiling boundary, self/back edges, depth-2 exclusion, unsafe URL/path). | AD-7 says "boundary fixtures are required" and AD-13 lists closure fixtures; AD-3/AD-4 fixtures are implied only. | Optional: one line under AD-4 "golden ordering/self-edge/depth-exclusion fixtures pin the collector". Low priority. |

Everything else the story requires (no SHA allowlist, raw-byte hashing, closed policy as sole authority, no recursive init, static argv, zero-base fail-closed, one-way manifest migration, both handoffs, race fixture, no publish in tests) is carried by an AD — see the AC map.

### (C) Agreements (story ↔ spine)

- Depth-1/2 boundary, self/back edges recorded, deeper edges excluded by definition (L47-52, 124, 213 ↔ AD-1, AD-4, AD-7).
- Committed-object reads via `ls-tree -r -z --full-tree`, `config --blob`, `<commit>:<path>`; never ambient index/nested HEAD (L122, 209 ↔ AD-2).
- Closed-world identity from root `.gitmodules`, normalization, rejection set (L123, 211 ↔ AD-3).
- Edge identity, ordering, closed envelope, canonical bytes, "not RFC 8785" (L125-126, 214, 243 ↔ AD-4, AD-5; seal A-13).
- Raw catalog bytes hashed and parsed; cache by `(Builds repo, commit)`; every selector evaluated and reported; no fingerprint allowlist (L124, 126, 132, 134, 215 ↔ AD-6).
- AD-7 ceilings 4096 / 64 MiB / 1 MiB / 4 MiB and 16,384 / 16 MiB / 256 MiB (L127, 140 ↔ AD-7 table).
- PR/push revision model, merge-base equality, zero-base fail-closed, logical-edge diff key, depth-1 subsumption, dedupe, no-op unchanged (L138-139, 232 ↔ AD-8, AD-12).
- Policy as the sole executable authority; `builds_identity` in policy; planning prose keeps no second seed (L121, 143, 408 ↔ AD-12, Executable Policy Authority section).
- Static closure semantics and failure set (L142 ↔ AD-13 Static closure).
- Legacy manifests audit-only, never resealed/upgraded (L153 ↔ AD-14).
- AD-15 artifact under `if: always()`, original-candidate preservation across the second hop, no green-no-op, race fixture (L157, 164, 242, 298 ↔ AD-15).
- BUILD-CAT-1 upstream, marker optional until separately approved (L88, 116, 253 ↔ AD-6, Deferred).
- No `references/**` edit, no submodule/gitlink change, governance-only scope (L248-251 ↔ AD-11, AD-16).
- Story 11.17d completed under waiver and not reopened (L167 ↔ memlog).

### AC → AD coverage map

| AC | Covering ADs | Gap |
|---|---|---|
| AC1 selected-catalog semantics, no allowlist | AD-1, AD-2, AD-3, AD-4, AD-6, AD-7, AD-12 (profiles) | none |
| AC2 compatible advance passes; identity only in diff/provenance | AD-6, AD-8, AD-9 | none |
| AC3 precise fail-closed diagnostics | AD-6, AD-7, Consistency "Errors" | catalog path not in the Errors convention (B-2) |
| AC4 exact affected-module CI proof | AD-7 (materialization caps), AD-8, AD-10, AD-12, AD-18 (secretless builds) | `quality.yml` history-depth requirement absent (B-3) |
| AC5 sealed/re-verified manifest, handoffs, fail-closed verification | AD-5, AD-9, AD-13, AD-14, AD-15, AD-17, AD-18 | AC5 says "manifest v2" (spine: v3) and "freeze controls remain intact" — no AD carries a freeze control since REL-4 was retired; AC5 must be reworded (S-7, S-8) or it is an AC no AD covers. Prepublish ordering invariant only inherited (B-4). |
| AC6 explicit ownership/migration | AD-6, AD-16, Deferred (mandatory marker) | none |
| 2026-08-02 clarification 1 (deterministic bounded affected set, bootstrap) | AD-8, AD-12 | "explicit bootstrap handling" superseded: no bootstrap mode reachable (S-9) |
| Clarification 2 (evaluator authority, static closure) | AD-12, AD-13, AD-16 | none |
| Clarification 3 (both handoffs bind same candidate/policy/closure) | AD-13, AD-15, AD-17 | none |
| Clarification 4 (publishable manifest v2 seals graph/policy/handoff/provenance) | AD-9, AD-14, AD-17 | "manifest v2" → v3 (S-10) |

---

## Input 2 — G2 request (`_bmad-output/planning-artifacts/g2-hexalith-builds-inline-pre-publish-gate-request.md`)

### (A) Statements the spine now contradicts or supersedes — 11

| # | Line(s) | Stale statement | Replacement (request voice) | Spine authority |
|---|---|---|---|---|
| G-1 | 27-29 | "**Required next request:** FrontComposer integration against the accepted revision — pin … (never `@main`), populate `evaluator_authorizations` for `ci`/`release`/`post_release`, and prove sealed AD-13/AD-15 handoffs" | "**Integration status (2026-09-08):** done 2026-08-08 (FrontComposer 7b3d8f3b) — `ci.yml`/`release.yml` pinned to 40-hex Builds commits, `evaluator_authorizations` populated, AD-13/AD-15 caller wiring live. Execution pin advanced by pre-authorized closures `a8a50859` → `99d5a46c` → `3f0e3595` → `4eb33928` (2026-08-22); each pin's closure lives in the active policy before use. Open FrontComposer-side nonconformances: A-1, D-6, D-10, T5 (see validation-report-2026-09-08)." | AD-12, AD-16 |
| G-2 | 31-35 | "**Release impact:** … Reusable-workflow integration, end-to-end exact-candidate evidence proof, GOV-1 completion, release eligibility, unfreeze, and the next FrontComposer NuGet/GitHub release remain blocked until the integration checklist … succeeds." | "**Release impact:** integration landed 2026-08-08; v4.1.0/v4.1.1 (2026-08-06, fallback approval) and v4.2.0 (2026-08-30, `hexalith.release-evidence.v3`) were published through the pinned reusable. There is no freeze to lift: release is operator `workflow_dispatch` from the exact `main` SHA under the `production` environment (AD-13/AD-18). No local contingency is authorized without a new dated Architect + Release Owner decision." | AD-13, AD-16, AD-18; memlog backfills 08-04/08-06/08-30 |
| G-3 | 36-37 | "`references/Hexalith.Builds` is a shared `@main` submodule consumed by every Hexalith module" | "`references/Hexalith.Builds` is a shared submodule pinned by an exact depth-1 gitlink commit (catalog identity, currently `a32cb422…`); it is deliberately independent of the Builds execution commit (`builds-execution-sha`) and equality is not required." | AD-16; Consistency "Builds identities" |
| G-4 | 95-109 | Caller shape lists `uses: …domain-release.yml@<accepted-40-hex-revision>` with no `builds-execution-sha` input and no checkout ref | Add to the `with:` block: `builds-execution-sha: <same 40-hex as the uses ref>` and note: "the `uses:` commit, the `builds-execution-sha` input, and the single `actions/checkout` `ref:` for `.hexalith/builds-execution/` must be the same literal, and that literal must equal the active-policy `release` row's `reusable.commit`; the current literal is `4eb33928…`, not the lineage root `a8a50859…`." | AD-13 Evaluator trust; A-3 |
| G-5 | 111-113 | "uses `release-evidence.yml` only to verify downloaded NuGet/GitHub assets and update the historical ledger" | "uses `release-evidence.yml` to authenticate the Release run and its `release-verification-handoff-<run_id>-<run_attempt>` artifact, independently re-authenticate the CI handoff, match its own caller blob to a `post_release` authorization (`reusable` null), verify downloaded NuGet/GitHub assets, and emit `frontcomposer.release-ledger-record.v2`; the Release Owner appends the REL-AI-1 ledger from that record." | AD-15 (post-release paragraph, Ledger) |
| G-6 | 124-125 | "Filing the issue/story does not clear the FrontComposer release freeze; the accepted revision must be integrated and REL-3's gate must pass." | "Filing the issue does not authorize publication; publication requires the accepted lineage pinned and authorized in the active policy, REL-3's exact-artifact gate, and a Release Owner-approved `production` environment run (AD-18)." | AD-13, AD-16, AD-18 |
| G-7 | 127-158 (esp. 137-141, 148-150, 155-156) | Second required item: `domain-release.yml` must refuse to run unless the caller's `HEXALITH_RELEASE_PUBLISH_ENABLED` is exactly `true`; "the REL-4 caller-side `freeze-guard` in FrontComposer's `release.yml` enforces it immediately and remains as defense-in-depth"; "FrontComposer's stays non-`true` until REL-3 completes." | Mark the section: "**Status 2026-09-08:** FrontComposer no longer depends on this item. Its `freeze-guard` and the `HEXALITH_RELEASE_PUBLISH_ENABLED` path were retired on 2026-08-04 (3ebbdce9); FrontComposer's publication control is operator `workflow_dispatch` on `refs/heads/main` with dispatch SHA == live `main` == authenticated push-CI head == handoff candidate, publication jobs under the `production` environment with Release Owner review (AD-13/AD-18). The ecosystem-wide freeze gate remains a Hexalith.Builds-owned ask for other modules only; FrontComposer records no caller-side variable." `release.yml` contains no `freeze-guard` today, so L155-156 is factually false. | AD-13, AD-18; memlog T1/T2 ("workflow_run caller and REL-4 freeze wording retired from AD-13"), backfill 2026-08-04 |
| G-8 | 165-167 | "FrontComposer must still pin callers to that revision, authorize the live static closure, and prove sealed AD-13/AD-15 handoffs before GOV-1 external gates close." | "FrontComposer pinned callers, authorized the static closure, and wired AD-13/AD-15 on 2026-08-08 (7b3d8f3b); the external gate is satisfied in lineage. Subsequent execution pins are valid only through the AD-12 two-phase pre-authorized closure pattern." | AD-16 |
| G-9 | 174-179 | "`domain-release.yml` accepts … the authenticated CI-handoff candidate, triggering CI run ID/attempt … The event head authenticates the CI run but is not an independent candidate source." | "`domain-release.yml` receives `release-commit` = the operator-dispatched `github.sha`, which the caller has already required to equal the live `main` ref and the head of exactly one completed successful push-CI run (`release_contract.py select-ci`); the CI run ID/attempt are the authenticated coordinates from that selection, and the handoff artifact `dependency-release-handoff-<run_id>-<run_attempt>` recorded candidate must equal the dispatched SHA. There is no `workflow_run` event head in this seam." | AD-13 Rule; memlog T1/T2 |
| G-10 | 180-188 | AD-15 mirror: artifact name `release-verification-handoff` without run suffix; "Pre-manifest, failed, or partial runs … use the closed null/empty representation … rather than omitting the artifact." | Add: "The artifact is named `release-verification-handoff-<run_id>-<run_attempt>`; its normative content is `release-verification-handoff.json` plus a byte-identical copy of the authenticated CI handoff. Failed/pre-publication attempts use null fields, `published=false`, empty assets. An attempt that never obtained an authenticated CI handoff emits `release-verification-handoff.deferred.json` instead; the verifier records a failed attempt from it and can never green. `manifest.path` is the sealed manifest's GitHub Release asset name; assets are unique and sorted by `name`; the `evaluator` member is exactly the AD-13 evaluator shape." | AD-15; memlog A-6/A-8/A-9/D-9 |
| G-11 | 196-203 | "**GOV-1 completion gate:** … FrontComposer implements … manifest v2 … Reusable-workflow integration against that SHA, end-to-end exact-candidate evidence proof, story completion, release eligibility, and any REL-4 unfreeze remain the FrontComposer integration checklist." | "**GOV-1 completion gate (updated 2026-09-08):** accepted revision `a8a50859…` is the lineage root; the current authorized execution pin is `4eb33928…`. FrontComposer implements local graph diff, bounded affected-module proof, policy-authorized evaluator closure, both handoff schemas and consumers, manifest v3 (`hexalith.release-evidence.v3`, AD-17; v2 verify-only), policy governance, and hostile fixtures. Reusable-workflow integration and an end-to-end v3 release (v4.2.0) are done. What remains before a completion claim is the nonconformance set escalated by the 2026-09-08 validation: A-1 (fallback digest live-rebind), D-6 (exit-2 soft-defer, DW-1795), D-10 (`--source-proof` as candidate authority), T5 (no `post_release` authorization check; stale `reusable` in post_release rows). There is no REL-4 unfreeze step." | AD-16, AD-17; memlog 2026-09-08 gate |

Minor: L3-5 header and L162 "Added 2026-07-19 … Corrected 2026-08-02 … Reopened … 2026-08-08" should gain "Updated 2026-09-08 to AD-13 dispatch model / AD-17 manifest v3 / AD-18 actor controls."

### (B) Quiet requirements in the G2 request the spine dropped and should carry

| # | Line(s) | Requirement | Spine status | Suggested carry |
|---|---|---|---|---|
| GB-1 | 169-171 | Upstream `domain-ci.yml` "validates its actual `job.workflow_ref/job.workflow_sha`" and outputs actual reusable/action provenance for the caller. | Spine T6 defers runtime `job.workflow_sha` observation and production closure recomputation; v1 trust is the caller blob hash plus literal reusable commit, and `job.*` is GitHub.com-only. Not a contradiction of the upstream contract, but the spine does not say whether FrontComposer must consume the Builds `governed-provenance` output when present. | Keep as-is in the request; in the spine's Deferred entry add "the request (G2 L169-179) already asks Builds to emit actual coordinates; consuming them is the revisit trigger" (already partially stated: "Revisit when the Builds `governed-provenance` output is consumed"). No change strictly needed. |
| GB-2 | 68-86 | Governed-mode contract details the spine does not restate: opt-in default-off input, `workflow_call` signing secrets scoped to governed steps, RFC 3161 timestamp input, `attest-build-provenance` over the exact candidate packages, attestation-bundle path handed to `prepareCmd`, semantic-release publishing without repack, root-only non-recursive init in the reusable. | These are FR-24/REL-3 obligations of the reusable workflow, cited by AD-16 only as "issue 17 accepted lineage". AD-18 covers permissions but not the signing/attestation interleave. | Add one Inherited Invariants row: "Governed reusable mode (G2 L65-91): signing/attestation executes inside the reusable at the accepted lineage over the exact candidate packages before any publication side effect; FrontComposer's `.releaserc.json` retains `prepareCmd`/`publishCmd` ownership." |
| GB-3 | 193-194 | "The accepted revision is recorded here with the exact `domain-ci.yml`, `domain-release.yml`, and composite-action blob SHA-256 values before FrontComposer integration." | AD-16 records the revision, AD-12 rows carry blob hashes; the spine does not require the *request document* to carry the blob hashes for each later pin. | Optional: AD-16 sentence "each pre-authorized pin's blob hashes are recorded in the active policy row; the request document need only name the lineage root." Clarifies that the request is not a second registry (consistent with AD-12 "no mirroring"). |
| GB-4 | 205-211 | Bounded contingency: only by a new dated Architect + Release Owner decision with scope, approvers, expiry/reopen trigger, migration back, and equivalent proofs. | AD-16 carries "scope, expiry, migration trigger, and equivalent proofs" — "approvers recorded" is implicit. | none needed. |

### (C) Agreements (G2 ↔ spine)

- Status block L9-10, 16-26, 30: reopened 2026-08-08, accepted `a8a50859…`, acceptance comment recorded (↔ AD-16, memlog backfill 2026-08-08).
- L56-63 attestation must be minted inside the workflow that owns the candidate packages with `id-token`/`attestations: write` scoped to that job (↔ AD-18).
- L86, 120 root-only, non-recursive submodule init (↔ AD-2, AD-10).
- L119, 121 secrets scoped, minimum permissions (↔ AD-18).
- L169-173 `domain-ci.yml` governed inputs, bounded static closure, single `dependency-release-handoff` artifact `hexalith.dependency-release-handoff.v1` (↔ AD-13 Handoff).
- L176-179 every checkout/prepare/seal/live-verify/fallback/classify/publish consumes `release-commit`; never default-branch HEAD (↔ AD-13 "sole release-candidate authority").
- L180-188 verifier re-downloads both handoffs and requires matching policy/candidate projections; null/empty representation for failed attempts (↔ AD-15, except the deferred-sentinel refinement G-10).
- L189-192 every reusable/action reference literal-40-hex; local composites loaded from the exact reusable commit; full static transitive closure incl. conditional entries matches active policy and handoff/manifest projection (↔ AD-13 Evaluator trust / Static closure, A-3).
- L201-203, 205-211 FrontComposer never edits the Builds submodule; contingency only by dated Architect + Release Owner decision (↔ AD-16).

---

## sprint-status.yaml — action item GOV-1 (`_bmad-output/implementation-artifacts/sprint-status.yaml:252-276`)

| # | Line(s) | Stale statement | Replacement | Spine authority |
|---|---|---|---|---|
| SS-1 | 266-269 | `external_gate: "Hexalith.Builds issue 17 closed 2026-07-20 without the GOV-1 amendment or a qualifying revision. Reopen it or file a successor; only reusable-workflow integration, end-to-end exact-candidate evidence proof, completion, release eligibility, and unfreeze await the accepted immutable revision."` | `external_gate: "Satisfied in lineage 2026-08-08: issue 17 reopened with the GOV-1 amendment; owner-accepted revision a8a50859fa2f27f511a9470dfe1e3ae54d0ebc1a (issuecomment-5226651759). FrontComposer pinned ci.yml/release.yml and populated evaluator_authorizations in 7b3d8f3b; execution pin advanced by AD-12 two-phase closures to 4eb33928 (2026-08-22). No unfreeze step exists (REL-4 freeze retired 2026-08-04)."` | AD-16, AD-13 |
| SS-2 | 270-272 | `local_work: "unblocked 2026-08-02: … manifest v2, policy governance, and hostile fixtures"` | `local_work: "landed: exact-revision graph diff, bounded affected-module proof, evaluator authorization/closure, both handoff schemas/consumers, manifest v3 (hexalith.release-evidence.v3 since df689935 / v4.2.0; v2 verify-only), policy governance, and hostile fixtures. Open nonconformances from the 2026-09-08 architecture validation: A-1 fallback digest live-rebind, D-6 exit-2 soft-defer (DW-1795), D-10 --source-proof candidate authority, T5 post_release authorization check."` | AD-17; memlog 2026-09-08 gate |
| SS-3 | 262 | `status: open` | Agreement — remains `open` until the escalated nonconformances close; the reason text (SS-1/SS-2) is what changes. | — |

Agreements: L253-257 action text (defined depth-1/2 v1 graph, semantic profiles per selector, exact-revision diff, bounded affected-module proof, policy-authorized evaluator closure, both handoffs, sealed graph/policy/workflow provenance) ↔ AD-1, AD-6, AD-8, AD-12, AD-13, AD-15, AD-17. L273-276 waiver text ↔ memlog "Story 11.17d completed 2026-08-02 under its recorded waiver; not reopened".

---

## deferred-work.md — DW-1688 (`_bmad-output/implementation-artifacts/deferred-work.md:7903-7909`)

| # | Line(s) | Stale statement | Replacement | Spine authority |
|---|---|---|---|---|
| DW-1 | 7903, 7909 | Title asks to "reopen … then obtain the owner-accepted immutable reusable-workflow revision and exact evaluator closure"; `status: open` | Both triggers fired: reopen trigger (issue 17 reopened 2026-08-08), close trigger ("its qualifying revision is integrated, authorized, and proven end to end": pinned/authorized in 7b3d8f3b, v3 release v4.2.0 on 2026-08-30 through the pinned reusable at an authorized execution SHA). Proposed: `status: done 2026-09-08` / `resolution: already resolved: Hexalith.Builds issue 17 reopened 2026-08-08 with the GOV-1 amendment; owner-accepted revision a8a50859fa2f27f511a9470dfe1e3ae54d0ebc1a recorded (issuecomment-5226651759); FrontComposer pinned callers and populated evaluator_authorizations in 7b3d8f3b; lineage advanced by two-phase closures to 4eb33928; superseded by DW-1783 (closed 2026-08-27). Residual nonconformances tracked separately (A-1, DW-1795, D-10, T5).` | AD-16; memlog backfill 2026-08-08 ("DW-1783 closed 2026-08-27 on it; DW-1688 … still say closed-without-acceptance (stale)") |
| DW-2 | 7908 | "Issue 17 … closed on 2026-07-20 without a comment…"; "manifest-v2"; "REL-4 unfreeze"; "AD-16 still gates reusable-workflow integration, end-to-end exact-candidate proof, GOV-1 completion, release eligibility, and REL-4 unfreeze until…" | Keep the historical evidence sentence; append: "Update 2026-09-08: gate satisfied in lineage (see resolution); manifest lineage is now v3 (AD-17); the REL-4 freeze/unfreeze path was retired 2026-08-04 — AD-16 no longer gates an unfreeze." | AD-16, AD-17, AD-13 |

Agreement: DW-1688's "No FrontComposer-owned contingency is authorized without a new dated Architect + Release Owner decision" ↔ AD-16.

---

## Cross-input inconsistencies worth one owner decision

1. Story frontmatter `done` vs body `in-progress` vs sprint-status action `open` vs DW-1688 `open` vs DW-1783 `done`. The spine's position: the AD-16 external gate is satisfied in lineage; what is open is the 2026-09-08 nonconformance set. Choose one representation and apply it to all four files in the same commit.
2. Every input still says "manifest v2" (story L56/86/106/150/153/162/421-433; G2 L198; sprint-status L271; DW-1688 L7908). Code and spine are at v3 (`CURRENT_MANIFEST_SCHEMA`, AD-17). A single find-and-replace with the AD-14 "v2 is verify/audit-only" caveat resolves all of them.
3. Every input still carries REL-4 freeze / `@main` / `workflow_run` release-trigger wording (story L86/156/180/188/244/252; G2 L124-158/177; sprint-status L269; DW-1688 L7908). The spine retired all three on 2026-08-04 (T1/T2). The G2 "Second required item" section is the largest block and should be explicitly marked as no longer FrontComposer-blocking rather than deleted (it is still an ecosystem ask).
4. Story L157 claims the `post_release` authorization check is implemented; the spine (T5/A-4) records it as absent nonconformance. That checkbox should be reopened or re-cited.
