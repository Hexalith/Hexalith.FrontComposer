# Reviewer Gate — Implementation-Drift Lens (VALIDATE) — GOV-1, 2026-09-09

**Verdict: PASS-with-findings (spine) / FAIL (implementation conformance).** The current spine is a usable, load-bearing consistency contract, but its structural diagram and one seed annotation still misdescribe the brownfield tree. The implementation remains non-conforming because the production fallback path defeats AD-9's immutable approval digest and the release evidence chain does not yet enforce the AD-12/AD-13/AD-15 trust model. The existing 30-row register remains materially current: **27 rows are active implementation work items, 3 are advisory/deferred rather than conformance failures, 0 are resolved**, and this pass found **1 additional implementation gap** (`NC-31`).

Reviewed the current working-tree `ARCHITECTURE-SPINE.md` and `nonconformance-register-2026-09-08.md` against FrontComposer HEAD `053b2008`. The GOV-1 code/config surfaces have no working-tree diff; the spine and planning artifacts do. Evidence included `eng/dependency_graph.py`, `dependency_handoff.py`, `workflow_source_closure.py`, `release_evidence.py`, `release_prepublish.py`, `release_contract.py`, `release_disposition.py`, `release_compatibility.py`, `eng/dependency-graph-policy.json`, `.github/workflows/{ci,quality,release,release-evidence}.yml`, the Python/C# Governance tests, fixtures, and the exact pinned Builds `domain-release.yml` blob at `4eb33928a1d8c7775f97221cf9edc171db0cb5f8`.

## Counts

| Surface | Critical | High | Medium | Low | Total |
| --- | ---: | ---: | ---: | ---: | ---: |
| Spine defects found by this lens | 0 | 0 | 0 | 2 | 2 |
| Implementation finding clusters below | 1 | 3 | 3 | 0 | 7 |
| Register work items after reconciliation | — | — | — | — | 27 active + 3 advisory/deferred |
| New implementation work items | 0 | 0 | 1 | 0 | 1 (`NC-31`) |

The implementation cluster count is deliberately not additive with the register count: one finding below reconciles several already-registered, overlapping backlog rows.

## Findings

| ID | Severity | AD | Class | Finding | Evidence | Disposition |
| --- | --- | --- | --- | --- | --- | --- |
| ICD-01 | **Critical** | AD-9, AD-12 | Implementation; registered | Production classification overwrites the Release Owner's `approved_against_fingerprints_sha256` with the live digest before comparing, while `release.yml` does not forward the repository variable and the fingerprint helper reads working-tree bytes with a `sha256("missing")` sentinel. Drift is therefore re-authorized rather than invalidating publication. | `eng/release_evidence.py:2263-2270,2472,2728-2743,3830-3835`; `.github/workflows/release.yml:282-292`; `eng/release_prepublish.py:485-535` | **Fix implementation.** Existing `NC-1`, `NC-2`, `NC-3`, and `NC-24`; keep `fallback-digest` as an operator tool, compare the immutable input, hash exact candidate blobs, and add production-path mutation tests. |
| ICD-02 | **High** | AD-12, AD-13, AD-15 | Implementation; registered | Evaluator authorization remains soft or absent: CI turns unauthorized `create-ci` exit 2 into a green no-handoff run; Release reuses the deferred sentinel after obtaining a CI handoff; the post-release verifier neither reloads the base policy nor authorizes a `post_release` closure; and the closure engines cannot represent the required `reusable: null` shape. | `.github/workflows/ci.yml:167-205`; `.github/workflows/release.yml:542-564`; `.github/workflows/release-evidence.yml:171-180`; `eng/dependency_handoff.py:53-67,547-622,629-635,690-692`; `eng/workflow_source_closure.py:806-850,907-951,979-1057`; `eng/dependency-graph-policy.json:1725-2257` | **Fix implementation/policy.** Existing `NC-4` through `NC-9`, in the register's stated dependency order (`NC-8` before `NC-7`, then post-release wiring). |
| ICD-03 | **High** | AD-15, AD-18 | Implementation; registered | The emitted handoff is still v1 and has no `publication_started` or `publish_gate_variable`; failed/partial publication is collapsed into `published=false`; the post-release classifier can fall back to `head_sha`; and two ad-hoc ledger shapes omit evaluator, actors, environment protection, authenticated run projections, and the AD-15 verification result. A governed run can therefore be labeled `compliant-candidate` without the v2 chain the spine requires. | `eng/dependency_handoff.py:22,275-359`; `.github/workflows/release.yml:425-535`; `eng/release_disposition.py:50-115`; `.github/workflows/release-evidence.yml:209-218,585-601` | **Fix implementation.** Existing `NC-10`, `NC-12`, `NC-19`, `NC-26`, and `NC-29`; implement one closed producer/validator for the ledger and inspect GitHub/NuGet for every started attempt. |
| ICD-04 | **High** | AD-12, AD-13, AD-16, AD-18 | Implementation; registered | Release authenticates only `ci.yml`, not the required same-head `quality.yml`; trust-bearing Governance checks still execute only in `quality.yml`, outside the authorized CI closure; changed policy rows are not closure-recomputed in the PR gate; checkout-injection checks and release checkout-pin checks are incomplete; and no Governance path verifies the AD-16 first-parent lineage predicate. | `eng/release_contract.py:231-273,320-331`; `.github/workflows/release.yml:41-71`; `.github/workflows/ci.yml:42-250`; `.github/workflows/quality.yml:126-173`; `eng/workflow_source_closure.py:806-873`; `tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs:753-783,1341-1402` | **Fix implementation/tests.** Existing `NC-18`, `NC-20`–`NC-23`, `NC-28`, and `NC-30`. |
| ICD-05 | Medium | AD-13, AD-14 | Implementation; registered | `hexalith.dependency-release-source.v1` remains an alternate manifest authority, and publication-oriented commands still accept v2 manifests although the spine confines v2 to verification/audit. | `eng/release_evidence.py:1537-1556,3241-3266,3675-3679,3993-4004`; `eng/release_prepublish.py:400-411,538-548` | **Fix implementation.** Existing `NC-11` and `NC-25`; require CI handoff plus release evaluator for preparation and make migration one-way. |
| ICD-06 | Medium | AD-5, AD-12, AD-18 | Implementation; registered | Required enforcement fixtures are absent: no literal golden canonical graph/digest fixture, no member-by-member pin of both AD-12 lists, and no complete static assertions over the three caller blobs plus the exact reusable blob for trigger/permission/secret/environment/upload rules. | `tests/eng/test_dependency_graph.py:161-188`; `tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs:1107-1127,2140-2168,3501-3552`; `eng/release_evidence.py:123-168` | **Fix tests.** Existing `NC-13`–`NC-15`. |
| ICD-07 / NC-31 | **Medium** | AD-15 | Implementation; **new** | `validate_release_handoff` sorts assets by `(name, sha256, size)` and deduplicates whole rows, so two rows with the same `name` but different hash/size are accepted. The producer rejects duplicate names, but the verifier does not enforce the closed handoff's explicit unique-by-name invariant. | Spine `ARCHITECTURE-SPINE.md:434-438`; `eng/dependency_handoff.py:313-325` versus producer guard `:419-435`; no hostile duplicate-name handoff case in `tests/eng/test_dependency_handoff.py:133-174,262-359` | **Fix implementation/tests.** Validate `assets == sorted(assets, key=name)` and `len({row["name"]}) == len(assets)`; add a verifier fixture with equal names and different hashes/sizes. Add as `NC-31` to the register. |
| SCD-01 | Low | Structural diagram | Spine defect | The module diagram asserts `dependency_handoff → workflow_source_closure`, `release_prepublish → dependency_handoff`, `release_contract → release_evidence`, and `release_disposition → dependency_handoff`, but those dependencies do not exist; it omits `release_evidence → dependency_handoff`, `release_prepublish → release_contract`, and `release_prepublish → release_compatibility`. | Spine `ARCHITECTURE-SPINE.md:546-558`; `eng/dependency_handoff.py:19`; `eng/release_evidence.py:502-510`; `eng/release_prepublish.py:60-61,244-245,554`; `eng/release_contract.py:4-14`; `eng/release_disposition.py:9-16` | **Update spine diagram.** This is seed drift, not an implementation failure and not a release blocker. |
| SCD-02 | Low | Structural Seed | Spine defect | The seed says `tests/ci-governance/fixtures/` contains policy fixtures pinned by C# Governance, but that directory contains release-manifest fixtures and no policy fixture. | Spine `ARCHITECTURE-SPINE.md:626-635`; `tests/ci-governance/fixtures/release-manifest-valid.json`; `tests/ci-governance/fixtures/release-manifest-invalid.json`; C# partial list check at `CiGovernanceTests.cs:1107-1127` | **Update spine seed** after `NC-15` chooses the actual fixture location/form. |

## Register reconciliation

| Register status | IDs | Result |
| --- | --- | --- |
| Confirmed active implementation work | `NC-1`–`NC-15`, `NC-18`–`NC-26`, `NC-28`–`NC-30` | **27 open.** No corresponding code/config file differs from HEAD, and targeted source inspection still shows each condition. Several rows overlap intentionally (notably `NC-10`/`NC-12`/`NC-19`/`NC-29`). |
| Advisory policy hygiene, not a demonstrated violation | `NC-16` | The unused `0a3508b3…` authorization rows still exist, but AD-12 permits pre-authorization and AD-16's current history predicate does not require a pin to have executed. Retain as a Release Owner prune/retain decision, not in the conformance-failure count. |
| Dead-code cleanup, not observable nonconformance | `NC-17` | The candidate-policy branch remains at `.github/workflows/ci.yml:181-184`, but the handoff step is gated on graph-diff exit 0 while the zero-before diagnostic path exits nonzero. Candidate policy does not actually select an evaluator. Retain as cleanup. |
| Explicitly deferred platform control | `NC-27` | The spine itself accepts the current unprotected-main risk under **Deferred** (`ARCHITECTURE-SPINE.md:685-694`). Track as the named compensating-control follow-up, not as present implementation drift. |
| Newly missing from register | `NC-31` | Add the AD-15 duplicate-asset-name verifier gap from `ICD-07`. |

No register row is proven resolved. The register headline should therefore distinguish **27 active code/test/policy nonconformances**, **2 advisory cleanup/decision rows**, **1 deferred platform-control row**, and the new `NC-31`.

## Confirmed sound in this pass

- **AD-1–AD-8 graph core:** bounded depth-1/2 collection, exact committed-object reads, closed identity grammar, edge shape/order, duplicate-member rejection, canonical graph digest, semantic-vs-provenance separation, all eleven resource ceilings, and the PR/push revision/diff model remain implemented. The focused Python suite exercised these paths.
- **AD-13 dispatch and handoff foundation:** `release.yml` is operator `workflow_dispatch`, re-reads `main`, selects exactly one successful push `ci.yml` run, authenticates a uniquely named immutable artifact, checks its run coordinates, verifies the CI handoff live, and carries its candidate forward. The missing `quality.yml` companion check remains isolated as `NC-18`.
- **AD-16/AD-18 live pin and environment shape:** FrontComposer pins `4eb33928…` consistently in the reusable `uses`, input, and both exact Builds-action checkouts. The exact pinned Builds blob puts both write/secret-bearing jobs under `environment: ${{ inputs.environment-name }}`, and FrontComposer passes `production`; the remaining problem is enforcement/evidence coverage, not the live shape.
- **AD-17 v3 manifest projection:** production code emits and validates the stated `{ci, release, definition_digest}` projection and transitively binds the CI evaluator through the raw CI-handoff hash.
- **Focused validation:** `python3 -m unittest tests/eng/test_dependency_graph.py tests/eng/test_dependency_handoff.py tests/eng/test_workflow_source_closure.py tests/eng/test_release_contract.py tests/eng/test_release_evidence_v2.py tests/eng/test_release_prepublish.py tests/eng/test_release_disposition.py tests/eng/test_ci_governance.py` completed **190 tests, all passing**. This confirms current behavior; it does not waive the architecture gaps above.

