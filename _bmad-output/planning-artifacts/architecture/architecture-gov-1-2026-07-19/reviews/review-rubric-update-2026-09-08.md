# Reviewer Gate — Rubric Walker (UPDATE) — GOV-1 Architecture Spine

**Target:** `_bmad-output/planning-artifacts/architecture/architecture-gov-1-2026-07-19/ARCHITECTURE-SPINE.md` (status `draft`, `updated: 2026-09-08`, AD-1..AD-18)
**Review date:** 2026-09-08
**Lens:** rubric walker, UPDATE intent (rewrite after the 2026-09-08 validation gate and the ratified decisions in `.memlog.md`)
**Inputs read:** spine, `.memlog.md` (backfill 2026-08-03..09-07, validation gate, ratifications), `reviews/review-rubric-validate-2026-09-08.md` (R-1..R-16), `validation-report-2026-09-08.md`, spine template, parent `architecture.md` (L45-53, L186-196, L230-286), GOV-1 story (frontmatter + AC1-6), `eng/dependency-graph-policy.json` (shape dump), `eng/release_evidence.py` (schema constants, file lists, `canonical_sha256`, `fallback_invalidation_fingerprints`, `fallback_v2_digest`, `_validate_policy_projection`, `_validate_source_workflow_provenance`), `eng/dependency_handoff.py` (schemas, closed member sets, `draft_evaluator_for_stage`), `eng/dependency_graph.py` (`normalize_identity`), `eng/workflow_source_closure.py` (builds-execution resolution), `eng/release_contract.py` (`select-ci`), `.github/workflows/{ci,release,release-evidence}.yml`, `global.json`, `tests/eng/`, `tests/ci-governance/fixtures/`. Deterministic lint re-run: `python3 .claude/skills/bmad-architecture/scripts/lint_spine.py --workspace <run folder>` → `ok: true, total_findings: 0`.

## Verdict

**PASS WITH FINDINGS** — 0 Critical, 1 High, 4 Medium, 6 Low. The rewrite closes 14 of the 16 validate-pass findings and resolves every Critical/High of the consolidated gate (A-1, A-2, A-3, A-4/D-5, A-5, A-6, R-2, R-3, R-4, R-1, D-4/S-1) with ratified, enforceable rules; the release seam (AD-9, AD-12..AD-18) is now internally consistent with the graph core. One new internal contradiction was introduced by AD-18 (actor recording with no evidence member to carry it) and must be autofixed before `status: final`; the remaining findings are bounded wording pins, one hygiene regression (size), and parent reconciliation work that belongs to finalize.

## Findings

| ID | Severity | Section | Finding | Evidence | Disposition / one-line fix |
| --- | --- | --- | --- | --- | --- |
| RU-1 | **High** | AD-18 L426-428 vs AD-15 L357-365, AD-17 L406-416, AD-13 L298-306 | AD-18 requires that "the dispatching actor, triggering actor, and environment approval are recorded in release evidence", but every evidence envelope the spine defines is a closed, duplicate-rejecting member set with no member for any of the three: the AD-15 verification handoff (`{schema, release_run, ci_handoff, candidate, dependency_policy, release, manifest, assets, evaluator}`, `release_run` = `{repository, workflow_path, run_id, run_attempt, conclusion}`), the AD-17 `workflow_provenance` (`release` = `{caller, reusable, builds_execution_sha}`), and the AD-13 CI handoff. A builder cannot satisfy AD-18 without violating a closed shape, or will park the actors in a non-normative file where nothing verifies them. No shipped code records an actor either (`grep actor eng/*.py .github/workflows/*.yml` → 0). | Spine L426-428; L357-358; L413-414; `eng/dependency_handoff.py:283,289` closed sets. | **autofix** — name the carrier: add `actors: {dispatching, triggering, environment_approval}` (strings; `null` before approval) as a member of AD-15 `release_run` **or** of AD-17 `workflow_provenance.release`, update that AD's closed member list, and state which one AD-18 means; if it is meant to be diagnostic only, say "recorded in `frontcomposer.release-run-disposition.v2`" and drop "release evidence". |
| RU-2 | Medium | Whole spine; AD-13 (L279-332, ~54 lines), AD-12 (L235-277, ~43), AD-15 (L349-385, ~37), AD-9 (L190-212) | R-10 (bloat) is not closed and regressed: 5,458 words vs 4,453 at validate (+22%). The rewrite pinned every ratified wording pin (A-7..A-18, D-8..D-12, S-7..S-10) as spine prose, so AD-12/AD-13/AD-15 now embed full closed member lists, the checkout-injection resolution algorithm, golden-fixture inventories, and API-response failure modes that already live in `eng/dependency_handoff.py` closed sets and `tests/eng/*` fixtures. Template: "Decisions, not rationale… Carry shape in diagrams; prose only where it must." | `wc -w` = 5458; validate review header: 4,453. AD-13 "Golden fixtures cover conditional sources, checkout-injected local actions, … and every limit" (L329-331) is a test inventory, not a rule. | **discuss** — either accept this spine as deliberately contract-grade (record that decision once in the memlog and close R-10 as won't-fix), or move the member lists and fixture inventories into `_bmad-output/contracts/shared-catalog-dependency-governance-2026-07-19.md` and keep per-AD Binds/Prevents plus the invariant; do not renumber. |
| RU-3 | Medium | AD-16 L391-400 | "Lineage" is used as the acceptance predicate ("advances only through the AD-12 two-phase pattern"; "a reference outside that lineage"; "a contingency outside the accepted lineage") but is never defined as something a verifier can decide. The policy today holds `reusable.commit` values `0a3508b3`, `4eb33928`, `3f0e3595`, `99d5a46c`; nothing in the spine says what proves `0a3508b3` is "in lineage" from `a8a50859` (ancestry? first appearance in an active policy revision? owner comment?). Two verifiers could disagree on whether a Builds fork or hotfix commit is inside the lineage. | Spine L391-397; policy `evaluator_authorizations.ci[0].reusable.commit = 0a3508b3…`, `[1] = 4eb33928…`; memlog chain a8a50859 → 99d5a46c → 3f0e3595 → 4eb33928 (no ancestry rule stated). | **autofix** — define it in one sentence: "A Builds execution commit is in the accepted lineage iff it first appears as a pre-authorized `reusable.commit` in a policy revision that was itself the active policy of a later change, and that chain of policy revisions starts at the revision recording `a8a50859…`; Git ancestry is not required." (or require ancestry and say so). |
| RU-4 | Medium | AD-13 "Static closure" L318-332; AD-12 L257-268; Deferred L565-568 | AD-13 binds `eng/workflow_source_closure.py` as the closure computer and AD-12 makes a row's `actions`/`closure_digest` the trust record, but the spine names no enforcement point that checks a *policy row* against the *exact blobs* it claims to describe. Deferred pushes production recomputation out; `draft_evaluator_for_stage` only projects the matched row (`eng/dependency_handoff.py:547-593`); `tests/eng/test_dependency_handoff.py:75` recomputes `closure_digest` from the row, not from blobs. A row whose `actions` list is stale or hand-edited would pass CI, release, and post-release verification while remaining "immutable by construction". This is not a two-builder divergence today, but the AD-13 closure paragraph currently has no verifier that could ever reject anything in production. | `.github/workflows/ci.yml:185` and `release.yml:264,488` call `draft-evaluator` only; `workflow_source_closure` appears in no workflow (`grep` → 0); memlog T6/D-4 accept production deferral but say nothing about authoring-time recomputation. | **autofix** — add one sentence to AD-12 Evaluator authorizations (or AD-13): "The PR gate/Governance recomputes every candidate-policy row's closure from the exact caller/reusable/action blobs it names and fails closed on any `actions` or `closure_digest` inequality; only runtime recomputation is deferred." This is offline and cheap; it is what makes the Deferred bullet safe. |
| RU-5 | Medium | Inherited Invariants L52-53; parent `architecture.md` L191, L235-236, L251-252 | The spine declares itself authoritative and promises "the parent's FR-24 prose … is reconciled whenever an AD below changes", but the parent still contradicts three ratified ADs: L191 `manifest_schema: hexalith.release-evidence.v2` (vs AD-17 v3); L235-236 "`hexalith.dependency-release-source.v1` … deliberately replaces the pending AD-16 evaluator handoff" (vs AD-13: the handoff is the sole authority, the source proof is diagnostic-only); L251-252 "approved Builds execution identity is `3f0e3595…`" (vs live `4eb33928…` and AD-16 lineage). Checklist 7 holds for the spine (no AD weakens the five inherited rows), but the inheritance is one-way until the parent is edited. | Parent lines quoted above; spine L52-53. | **defer to finalize** — reconcile the parent in the finalize step (replace v2 with v3 lineage per AD-14/AD-17, replace the "replaces the pending AD-16 handoff" sentence with the AD-13 model, replace the literal execution SHA with "the active-policy `reusable.commit` in the AD-16 lineage"); add a memlog `(change)` entry when done. Not a spine edit. |
| RU-6 | Low | AD-12 Activation L245-248 | "at release, from the exact FrontComposer commit recorded in the authenticated handoff" is ambiguous among the handoff's `run.candidate`, `revisions.base`, and `dependency_policy.commit`. Code pins `dependency_policy.commit == revisions.base` (`dependency_handoff.py:161-162`) and AD-15 says "base/before policy blob", so the intent is clear but the AD is not self-contained. | Spine L246-247; `eng/dependency_handoff.py:160-162`. | **autofix** — "at release, from the handoff `dependency_policy.commit`, which must equal `revisions.base`." |
| RU-7 | Low | Inherited Invariants L55-61 | The parent's "Ownership boundaries" bullets (Hexalith.Builds owns the reusable-workflow contract and catalog marker; FrontComposer owns evidence/verification/publication of authorized bytes; Release Owner owns production approval, credentials, exceptions, incident response) are not inherited, yet AD-15 (ledger), AD-16, AD-18, and three Deferred bullets assign duties to those roles ad hoc. R-11 residual. | Parent L276-283; spine table has five rows, none for ownership. | **autofix** — add one row: "Ownership boundaries (Builds / FrontComposer / Release Owner) → `architecture.md` FR-24 → AD-15 ledger, AD-16 lineage, AD-18 approval, Deferred runbook items." |
| RU-8 | Low | AD-3 L89-94 | The ordered grammar strips one terminal `.git` case-sensitively and then lowercases, so `…/Repo.GIT` normalizes to the distinct identity `owner/repo.git` rather than failing or collapsing to `owner/repo`; code behaves identically (`_HTTPS_RE` `(?:\.git)?` then `.lower()`). Harmless for the closed-world map (such an identity is simply unknown → fail closed), but two implementers could reasonably differ on whether to reject it earlier. | `eng/dependency_graph.py:270-293`. | **autofix (wording pin)** — append "a normalized repository segment ending in `.git` is rejected." |
| RU-9 | Low | AD-12 Bindings L270-277; AD-18 L423; AD-15 L351 | `RELEASE_DEFINITION_FILES`/`FALLBACK_INVALIDATION_FILES` membership is stated for eight files but `.github/workflows/release-evidence.yml` and `eng/release_disposition.py` (bound by AD-15/AD-18) are neither listed nor explicitly excluded. Exclusion is defensible (post-publication never authorizes, AD-9), but a builder will "fix" it unless told. | `eng/release_evidence.py:123-160` (neither file present in either list). | **autofix** — one clause: "post-publication sources (`release-evidence.yml`, `eng/release_disposition.py`) are deliberately outside both lists because they never authorize (AD-9)." |
| RU-10 | Low | Mermaid blocks L39-48, L437-448, L531-539 | Three `flowchart` blocks parse as valid on manual read (dotted labelled edge `-. text .->`, cylinder nodes `[( )]`, self-loop `REL --> REL`, pipe labels). `lint_spine.py` does not parse mermaid and no renderer is available locally, so the pipe label `|select-ci + verify-ci --live|` (contains `--`) is unverified against a real mermaid build. | `lint_spine.py` L19 comment: mermaid only excluded from false positives. | **ignore / render-check** — render once in a mermaid-capable viewer; if the `--live` label trips the lexer, change it to `verify-ci live`. |
| RU-11 | Low | Sources L15 (GOV-1 story) | R-16 residual, out of spine scope: story frontmatter `status: done` vs body `Status: in-progress`; AC5 still says "when manifest v2 is processed" and "freeze controls remain intact" although AD-17 makes v3 the only produced schema and the REL-4 freeze was retired (memlog 2026-08-04). Checklist 6 is met on substance (AC5 → AD-9/13/14/15/17) but the cited source disagrees with the spine on two words. | Story L9, L23, L86. | **defer** — story owner aligns AC5 wording to v3/AD-17 and removes "freeze controls"; no spine edit. |

Nonconformances that are **not** spine findings (spine is ratified, implementation escalated in the memlog, listed so nobody re-opens them as spine gaps): AD-9 live rebinding of `approved_against_fingerprints_sha256` (`release_evidence.py:2737`, `release.yml` passes four of five `RELEASE_ATTESTATION_*` vars); AD-15/AD-12 `post_release` rows carry a non-null unreachable `reusable` and `release-evidence.yml` matches no `post_release` row; AD-13 `prepare-manifest` still accepts the source proof as candidate authority (`release_evidence.py:1551`); AD-18 `release.yml` prepare job at L217 carries `environment: production` with only `contents: read` (harmless) while the writing job is the reusable call at L307-335.

## Closure of R-1..R-16

| ID | Validate finding (short) | Status | Where closed / residual |
| --- | --- | --- | --- |
| R-1 | Post-final edit without `updated`/memlog/re-gate | **closed** | `updated: 2026-09-08`, `status: draft`; memlog backfill entries 2026-08-03..08-30; this gate. |
| R-2 | Spine vs parent release model; no authority statement | **closed (spine)** | AD-13 rewritten to operator `workflow_dispatch` + `select-ci` + handoff-as-sole-authority; Inherited Invariants added with authority sentence L52-53. Parent still to be reconciled → RU-5. |
| R-3 | Manifest v3 shipped, spine binds v2 | **closed** | AD-17 added (closed `workflow_provenance`, `definition_digest`, v2 disposition); AD-14 retitled as one-way migration with v1→v2→v3 lineage; AD-9 formula names `workflow_provenance.definition_digest`. |
| R-4 | AD-16/AD-13 dated status narrative | **closed** | AD-16 is the timeless lineage invariant (issue 17 / `a8a50859` as root); "current `@main`" sentences removed from AD-13; Deferred bullet collapsed. Residual: "lineage" undefined → RU-3. |
| R-5 | Sprint fact inside AD-11 Rule | **closed** | AD-11 Rule reduced to the scope invariant; 11.17d note moved to memlog (last entry). |
| R-6 | AD-6 mirrors profile roster | **closed** | AD-6 is the coverage invariant; "Profile identifiers and rows are policy seed, not spine content." |
| R-7 | Catalog gitlink vs execution commit silent | **closed** | AD-16 last sentences + Consistency row "Builds identities" (both sealed, equality not required). |
| R-8 | Sub-manifest evidence envelopes undecided | **closed** | Consistency row "Evidence envelopes" names all seven schemas (all verified present in `eng/` / workflows) with closed/dup-rejecting/raw-SHA-256 rule; Deferred "Diagnostic retention". |
| R-9 | Dead bootstrap clause | **closed** | One historical sentence L254-255; no bootstrap mode reachable. |
| R-10 | Bloat / contract-document drift | **open, regressed** | 5,458 words (+22%) → RU-2. |
| R-11 | No Inherited Invariants; `binds` lacks parent ids | **closed** | Table with five rows; `binds` carries `parent:*` slugs (parent has no AD ids — acceptable). Residual ownership row → RU-7. |
| R-12 | Stack SDK row stale | **closed** | `10.0.400` matches `global.json`; Git/Python rows carry the memlog reality-check values (not independently re-verified here; behaviour is fixture-pinned, so no divergence risk). |
| R-13 | Blanket `[ADOPTED]` | **closed (accepted)** | AD-1..AD-17 are backed by user/architect ratification entries in the memlog (2026-07-19 and 2026-09-08 A-1/T1/T2/A-3/A-11 by user; T3..S-10 by architect); AD-18 is correctly untagged as a new decision. |
| R-14 | Seed/diagram lag; no dependency-direction diagram | **closed** | Seed lists `dependency_handoff.py`, `workflow_source_closure.py`, `test_dependency_handoff.py`, `test_workflow_source_closure.py`, `test_release_evidence_v2.py` (all exist); second mermaid is the Governance(C#) → Python engine dependency-direction diagram. |
| R-15 | Deferred restates AD-16 | **closed** | Deferred no longer mentions issue 17; each bullet names what is pushed down and its trigger. |
| R-16 | GOV-1 story source inconsistent (v2, status) | **open, deferred** | Out of spine scope → RU-11. |

## Cross-AD consistency check (requested pairs)

| Pair | Result |
| --- | --- |
| AD-9 ↔ AD-17 | Consistent: AD-9 `workflow_definition` = `workflow_provenance.definition_digest` (combined ci+release); AD-17 says its digest "is the `workflow_definition` input of AD-9". Formula keys `{definition, package_set, dependency_graph, dependency_policy, workflow_definition}` match `fallback_v2_digest`; `helper_version` = sha256(version string) matches `fallback_invalidation_fingerprints`. |
| AD-9 ↔ AD-12 Bindings | Consistent: the eight named files are members of both lists in `release_evidence.py:123-160`. Residual RU-9 (two post-publication files unmentioned). |
| AD-12 ↔ AD-13 (evaluator trust, `reusable` null, two-phase) | Consistent: row matched by caller blob; `reusable` null only when the closure reaches no reusable workflow (post_release); `ci.yml`/`release.yml` pin `4eb33928…` and the policy has ci/release rows for it. Residual RU-4 (authoring-time row-vs-blob check unnamed). |
| AD-12 ↔ AD-14 (policy projection) | Consistent: `{schema, repository, path, commit, sha256}` = `_validate_policy_projection`. Residual RU-6 (which handoff commit activates at release). |
| AD-13 ↔ AD-15 ↔ AD-17 (shapes, candidate, actors) | Handoff `run` (7 members incl. `candidate`), `revisions {base, candidate, merge_base}`, AD-15 nine members with `release_run {…conclusion}`, AD-17 `ci.run` with `head_sha` = handoff `run.candidate` — all match `dependency_handoff.py` / `release_evidence.py:911-936`. **Contradiction:** AD-18 actor recording has no member in any of them → RU-1. |
| AD-13 ↔ AD-16 | Consistent: literal reusable commit == row `reusable.commit` == `builds-execution-sha` == `.hexalith/builds-execution` checkout ref; lineage root `a8a50859`. Residual RU-3. |
| AD-14 ↔ AD-17 | Consistent: v1 legacy audit-only, v2 verify-only, v3 produced; seal = canonical bytes of every top-level member except `seal` (matches `release_evidence.py:1725`). |
| Consistency table ↔ ADs | Consistent: Builds identities row = AD-16/AD-8; evidence-envelope row = AD-5 canonicalization; artifacts row = AD-13/AD-15 names (`ci.yml:215`, `release.yml:574`); errors row = AD-7; commands row = AD-8. AD-7 ceilings equal policy `resource_limits` exactly (11 values checked). |
| Deferred ↔ AD-13 / AD-12 | Consistent: AD-13 explicitly points runtime `workflow_sha` and production closure recomputation to Deferred; AD-12 last sentence points evaluator-code activation to Deferred; Deferred "Incident response" complements AD-15 incident states without redefining them. Nothing in Deferred lets two units diverge (RU-4 is a missing enforcement point, not a deferral gap). |

## Checklist Scorecard

| # | Checklist item | Result | One line |
| --- | --- | --- | --- |
| 1 | Fixes the real divergence points for GOV-1 stories and misses none | **met** | Graph boundary/identity/digest (AD-1..7), CI revision + unchanged relation (AD-8), fallback digest ownership (AD-9), acquisition (AD-10), policy activation + closed registry (AD-12), dispatch/handoff/closure (AD-13), migration + v3 (AD-14/17), verifier handoff (AD-15), Builds identities (AD-16 + Consistency), tokens/actors (AD-18). Only RU-1's carrier is unfixed. |
| 2 | Every AD's Rule is enforceable and prevents its stated divergence | **partial** | AD-1..AD-15, AD-17 enforceable and matched to code shapes; AD-18's actor clause has no carrier (RU-1); AD-16's "lineage" is not a decidable predicate (RU-3); AD-13's static-closure paragraph names no authoring-time verifier (RU-4). |
| 3 | Nothing under Deferred could let two units diverge | **met** | Six bullets each name owner and trigger; the two risky deferrals (runtime identity, evaluator-code activation) are bounded by the caller-blob trust rule and branch protection; RU-4 is fixed in an AD, not by un-deferring. |
| 4 | Named tech verified-current (note only) | **met** | Stack labelled "observed, not trust inputs"; .NET `10.0.400` verified against `global.json`; `upload-artifact` v7.0.1 verified in all three workflows; Git 2.53.0 / Python 3.14.4 local values taken from today's memlog reality check; fixture-pinned behaviour makes staleness non-divergent. |
| 5 | Ratifies rather than contradicts brownfield code (note only) | **met, with four escalated deliberate contradictions** | Every closed shape, schema name, file list, canonicalization, identity grammar, ceiling, artifact name, and subcommand named in the spine exists in `eng/`/`.github/`/`tests/eng/`; the four spine-over-code decisions (A-1 rebinding, post_release rows, source-proof authority, D-6 exit-2) are ratified and escalated in the memlog. |
| 6 | Covers the driving story's capabilities | **met** | AC1 → AD-1/2/3/4/6; AC2 → AD-6/8; AC3 → AD-6/7 + Errors row; AC4 → AD-8/10/12; AC5 → AD-9/13/14/15/17; AC6 → AD-6/16 + Deferred marker. Story wording lags (RU-11). |
| 7 | Parent inherited; no AD weakens or contradicts an inherited one | **met (spine) / pending (parent)** | Five inherited rows bind AD-1, AD-6, AD-9, AD-11, AD-13, AD-15, AD-17; no AD weakens them; authority sentence present. The parent itself is behind on v3, source-proof, and execution SHA (RU-5) and lacks the ownership row here (RU-7). |
| 8 | Every dimension the epic altitude owns is decided, deferred, or open | **partial** | Operational envelope → FR-24 (deferred); failure/incident → AD-15 + Deferred; testing → fixtures per AD; migration → AD-14/17; observability → evidence-envelope row + retention deferral; security/trust → AD-10/12/18; ownership → AD-16/18 ad hoc (RU-7); actor identity → decided in AD-18 but uncarried (RU-1). |

## Hygiene

- **Template comments / placeholders:** none (`grep '<!--\|{name}\|{date}\|TODO'` → 0). Pass.
- **AD ids:** AD-1..AD-18 ascending, no gaps, no reuse, no renumbering versus the validate baseline (AD-1..AD-16 titles unchanged except AD-14's retitle and AD-16's intent-preserving rewording). Pass.
- **Mermaid:** three `flowchart` blocks, each non-empty and syntactically plausible on manual read; not machine-rendered (RU-10).
- **Frontmatter:** `status: draft`, `created`/`updated` consistent with git and memlog; `binds` carries GOV-1 plus four parent slugs; all local `sources`/`companions` exist on disk; the five URLs are the documentation anchors cited by the memlog reality check. Pass.
- **Terse substrate:** no — 5,458 words, contract-grade prose in AD-9/12/13/15 (RU-2). AD-1..AD-8, AD-10, AD-11, AD-14, AD-16, AD-17, AD-18 are at the right grain.
- **Invariants vs seed:** correct — Stack labelled observed; profile rows and registry rows explicitly delegated to the policy; Structural Seed matches `eng/` and `tests/eng/` on disk.
- **Deterministic lint:** `lint_spine.py` → 0 findings.
