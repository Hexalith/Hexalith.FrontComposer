# Reviewer Gate — Rubric Walker (VALIDATE) — GOV-1 Architecture Spine

**Target:** `_bmad-output/planning-artifacts/architecture/architecture-gov-1-2026-07-19/ARCHITECTURE-SPINE.md` (status `final`, `updated: 2026-07-19`)  
**Review date:** 2026-09-08  
**Lens:** rubric walker, VALIDATE intent (spine already finalized; implementation landed under `eng/`, `tests/eng/`, `.github/workflows/`)  
**Inputs read:** spine, `.memlog.md`, spine template, July reviews (context only), `architecture.md` (parent, last edited 2026-09-05), contract `shared-catalog-dependency-governance-2026-07-19.md`, GOV-1 story, G2/BUILD-REL-1 request, `eng/dependency_graph.py`, `eng/dependency_handoff.py`, `eng/release_evidence.py`, `eng/dependency-graph-policy.json`, `.github/workflows/{ci,release,release-evidence}.yml`, `global.json`, git history of the spine file.

## Verdict

**FAIL** — 0 Critical, 4 High, 7 Medium, 5 Low. The graph core (AD-1–AD-10) is coherent, enforceable, and ratified by the shipped code. The failure is confined to (a) the release-integration ADs (AD-11–AD-16), which are now stale or contradict the updated parent architecture, and (b) spine metadata: the file was edited after finalization without an `updated` bump, memlog entry, or re-gate. Under this project's gate convention (zero unresolved Critical/High), the spine cannot be re-stamped `final` as-is.

## Findings

| ID | Severity | AD/Section | Finding | Evidence (quote or line ref) | Recommended disposition / one-line fix |
| --- | --- | --- | --- | --- | --- |
| R-1 | High | Frontmatter L8–L10; AD-11 L197; AD-12 L209–L211; AD-13 L246–L261; `.memlog.md` L65 | Spine was materially edited after finalization (AD-11, AD-12, AD-13 Rules rewritten) while `status: final`, `updated: '2026-07-19'` are unchanged, the memlog ends at "spine finalized", and no reviewer gate re-ran. The edit is buried in a test commit. | `git log`: `8a6a6cb3 2026-08-03 feat(tests): add comprehensive tests for GOV-1 manifest-v2 …` touches `ARCHITECTURE-SPINE.md`; diff replaces AD-11 "After GOV-1, Story 11.17d reruns…" with "Story 11.17d completed on 2026-08-02…"; AD-13 replaces the `workflow_run.head_sha` input model with "authenticated handoff candidate is the sole release-candidate authority". Memlog L65 `(event) spine finalized` is last entry. | **autofix** — set `updated: '2026-08-03'` (or today after re-gate), append memlog `(change by architect) 2026-08-03: AD-11/12/13 amended …`, and record this gate; going forward, spine edits after `final` require a memlog entry and gate re-run in the same change. |
| R-2 | High | AD-13 L244–L261, AD-14 L307–L311, AD-16; parent `architecture.md` L99–L101, L138–L139, L233–L253 | Spine contradicts its parent (canonical architecture) on the release-integration model and neither says which is authoritative. Parent (edited 2026-08-04 onward) says Release is operator `workflow_dispatch`, push CI emits `hexalith.dependency-release-source.v1` which "deliberately replaces the pending AD-16 evaluator handoff", and the manifest is `hexalith.release-evidence.v3`. Spine binds "the `workflow_run` caller" (AD-13 L244), mandates `dependency-release-handoff.v1`, and fixes manifest `v2` (AD-14 L308). Reality has both: `release.yml:7 workflow_dispatch`, `ci.yml:152 create-source` **and** `ci.yml:192 create-ci`. | Parent L235–L236: "This deliberately replaces the pending AD-16 evaluator handoff." Spine AD-13 L244: "**Binds:** the `workflow_run` caller, reusable release workflow…". `.github/workflows/release.yml:6-7`: `on: workflow_dispatch:`. | **discuss** — Architect + Release Owner decide the authoritative model, then either amend AD-13/AD-14 (dispatch trigger, source-proof + handoff coexistence, v3) or revert the parent prose; add an Inherited Invariants row pointing at the parent section so future drift is detectable. |
| R-3 | High | AD-14 L307–L331, AD-9 L170–L174 | Manifest schema v3 shipped but the spine never decides v3: AD-14 names `hexalith.release-evidence.v2` as *the* required schema and AD-9 defines only the "V2 fallback approval" digest. Code accepts both v2 and v3 for verification (`release_evidence.py:1411`), so whether v2 is still publishable or legacy-audit-only is undecided — two verifiers could diverge. | `eng/release_evidence.py:64-65`: `MANIFEST_SCHEMA = "hexalith.release-evidence.v2"` / `CURRENT_MANIFEST_SCHEMA = "hexalith.release-evidence.v3"`; commit `df689935 2026-08-30 fix(ci): project release provenance into v3 schema`; parent L247 "emits `hexalith.release-evidence.v3`". | **discuss → autofix** — add AD-17 "manifest v3": closed member set, what v3 adds over v2 (source-proof hash, `builds-execution-sha`, exact caller/reusable bytes), the v2 disposition (publishable? audit-only?), and the v3 fallback-digest recipe; retitle AD-14 as the v1→v2 one-way rule it already is. |
| R-4 | High | AD-16 L371–L382; AD-13 L275–L279; Deferred L490–L493 | Rule text is a dated status narrative that is now false, in a `final` spine. AD-16 says issue 17 "closed on 2026-07-20 … the Release Owner reopens it"; AD-13 says "The current CI `domain-ci.yml@main` … references are non-conforming". Reality: issue 17 reopened and owner-accepted 2026-08-08, `ci.yml` pinned to a 40-hex Builds commit, `evaluator_authorizations` populated. A builder reading the spine today is told the gate is still open. A Rule must be a timeless invariant, not a timestamp. | G2 request L9: "issue 17 reopened 2026-08-08 … owner-accepted immutable revision recorded"; `.github/workflows/ci.yml:25`: `uses: Hexalith/Hexalith.Builds/.github/workflows/domain-ci.yml@4eb33928a1d8c7775f97221cf9edc171db0cb5f8`; policy `evaluator_authorizations` = {ci: 7, release: 7, post_release: 5} entries. | **autofix** — rewrite AD-16 Rule as the invariant only ("no reusable-workflow integration, completion claim, release eligibility, or REL-4 unfreeze without an owner-accepted immutable Builds revision recorded in the active policy and upstream request; no FrontComposer contingency without a dated Architect + Release Owner decision"); move the issue-17 chronology to the memlog; delete the "current @main" sentences from AD-13; collapse Deferred bullet 3 to one line. |
| R-5 | Medium | AD-11 L197–L199 | The 2026-08-03 edit inserted a sprint-status fact into a Rule: "Story 11.17d completed on 2026-08-02 under its recorded one-story GOV-1 promotion waiver and is not reopened by this architecture". It is not enforceable and prevents no divergence; the Binds/Prevents (scope discipline) are fine, the Rule's second sentence is memlog material. | Spine L197–L199. | **autofix** — keep "GOV-1 changes no runtime/public API behavior, generated output, package inventory, dependency versions, or UX." Move the 11.17d note to memlog as `(event) 2026-08-02 …`. |
| R-6 | Medium | AD-6 L119–L124 vs AD-12 L209–L211 and "Executable Policy Authority" L398–L402 | Internal contradiction on what the spine mirrors. AD-6 enumerates the seed profile roster ("FrontComposer, EventStore, Memories, and Parties have named consumer profiles; Builds, Commons, PolymorphicSerializations, Tenants, and AI.Tools have an explicit `shared-catalog-baseline-v1` profile"), while L400 says "This spine intentionally does not mirror their volatile values" and AD-12 says the policy is "the sole executable authority for profile IDs/values". Policy has since added `builds_identity` and both `semantic_profiles` and `profiles` keys, so the roster is already the kind of row the spine says it won't carry. | Spine L121–L124; L398–L402; `eng/dependency-graph-policy.json` top-level keys `['schema','builds_identity','trusted_identities','semantic_profiles','profiles','module_build_registry','resource_limits','evaluator_authorizations']`. | **autofix** — reduce AD-6 to the coverage invariant (every Builds-selector owner → exactly one named profile; no implicit default; fail closed) and drop the named roster, or relocate it to Structural Seed explicitly labelled *seed at 2026-07-19*. |
| R-7 | Medium | AD-8 L158–L161, AD-13 L254–L256; Deferred (silent) | A real divergence point is silent: the Builds **catalog gitlink** (AD-8's "exact candidate Builds commit") and the Builds **CI/CD execution commit** (AD-13's "approved immutable 40-hex Hexalith.Builds commit" for the reusable workflow) were split in implementation and the parent now says they "need not equal". The spine never states they are two identities, whether both are sealed, or how policy names each. Two units (graph engine vs. evaluator closure) could disagree on which Builds commit "the" Builds identity is. | Commit `dfbe9978 2026-08-16 fix: split Builds catalog gitlink from CI/CD execution SHA`; parent L247–L248: "That catalog gitlink is an independent development dependency and need not equal the approved execution commit." Spine: no occurrence of "execution" identity. | **discuss** — add one AD or a Consistency Conventions row: "Builds catalog identity = depth-1 gitlink commit (graph); Builds execution identity = active-policy `evaluator_authorizations.*.reusable.commit`; both sealed, equality not required." |
| R-8 | Medium | AD-8 L148–L149; Consistency Conventions "Errors" L393; checklist 8 (observability/diagnostics) | Diagnostic/evidence envelopes below the manifest are undecided. Production emits `hexalith.dependency-graph-diff.v1`, `hexalith.dependency-object-acquisition.v1`, `hexalith.affected-module-build-results.v1`, `hexalith.dependency-release-source.v1`, `hexalith.release-evidence-recovery.v1`; the spine names none and gives no rule for their shape/canonicalization/retention. AD-8 only says "record all three revisions". Independent builders of CI diff vs. release verification could shape these differently. | `eng/dependency_graph.py:36, 895, 1052`; `eng/dependency_handoff.py:23`; `.github/workflows/release-evidence.yml:59`. | **discuss / defer** — either add a Consistency Conventions row "every GOV-1 evidence artifact is a closed, duplicate-rejecting, schema-named JSON object under AD-5 canonicalization with a recorded raw SHA-256" and list the schema names, or add an explicit Deferred bullet owning the diagnostics dimension. |
| R-9 | Medium | AD-12 L219–L226 | The one-time bootstrap clause (12 lines) is dead rule text that was never machine-enforced: `HEXALITH_DEPENDENCY_POLICY_BOOTSTRAP_SHA256` appears nowhere in `eng/` or `.github/`, and a base policy has existed since the policy landed, so by the AD's own words bootstrap is "permanently unavailable". Keeping a procedure that cannot run inflates the AD and misstates enforcement. | `rg HEXALITH_DEPENDENCY_POLICY_BOOTSTRAP_SHA256 --glob '!_bmad-output/**'` → 0 matches; `eng/dependency-graph-policy.json` present since 2026-07-19 (`git log`). | **autofix** — replace with one sentence: "The v1 bootstrap was consumed when the first policy landed on `<commit>`; bootstrap mode is permanently unavailable." Move the procedure to memlog. |
| R-10 | Medium | AD-12 (41 lines), AD-13 (61), AD-15 (34), AD-14 (30), AD-8 (22); whole spine 4,453 words | Bloat: the spine has become a contract document. AD-13 embeds the full handoff JSON member list (L263–L273), the closure algorithm with numeric caps (L289–L301), a golden-fixture list, and status prose; AD-8 embeds the materialization procedure that AD-7 (L136–L138) and the Policy section (L418–L424) already state. Template: "Decisions, not rationale… Carry shape in diagrams; prose only where it must." The decision in each case fits in 3–5 lines; the schemas already live in code constants, golden fixtures, and the contract doc. | Line counts per AD block (heading to next heading): AD-12 41, AD-13 61, AD-14 30, AD-15 34, AD-8 22. Three separate statements of the 16,384 / 16 MiB / 256 MiB materialization caps (L136–L138, L158–L161, L418–L424 by reference). | **autofix** — per AD keep Binds/Prevents plus the invariant (closed shape, canonical digest, who authenticates what, fail-closed); move member lists and caps to `_bmad-output/contracts/…` and the `eng/` schema constants + fixtures, referenced by name; state each numeric cap exactly once (AD-7). |
| R-11 | Medium | Frontmatter `binds: ['GOV-1']` L11; missing "Inherited Invariants" section | Checklist 7: there is a parent (`architecture.md` is canonical and lists the GOV-1 amendment at L49–L52 and the FR-24 pipeline at L206–L231), but the spine has no Inherited Invariants table and `binds` carries no parent ids (template L11: "at epic altitude, also the inherited parent AD ids"; template L24–L30). The spine re-derives parent constraints (AD-6 ≈ parent L49; AD-9 ≈ FR-24 "only pre-publication may authorize") instead of citing them, which is exactly why R-2 went unnoticed. | Template L26: "A local decision that contradicts one is a conflict to surface, not an override." Spine sections: Design Paradigm → Invariants & Rules (no Inherited Invariants). | **autofix** — add an Inherited Invariants table citing the parent bullets by their text/anchor (semantic-not-allowlist compatibility, FR-24 exact-artifact pipeline, ownership boundaries, "does not alter runtime/UX") and reference them from AD-6, AD-9, AD-11, AD-16 instead of restating. |
| R-12 | Low | Stack L437–L442 | Observed .NET SDK row is stale: spine says `10.0.302`; `global.json` pins `10.0.400` (`rollForward: latestPatch`). The Stack text already says CI consumes `global.json`, so no divergence risk; note only for checklist 4. | `global.json`: `"version": "10.0.400"`. | **autofix (trivial)** — update the row or drop the SDK row and keep only the `global.json` reference. |
| R-13 | Low | All AD headings L48–L367 | `[ADOPTED]` on all 16 ADs removes the tag's signal. The template reserves it for decisions "the user or existing reality settled"; AD-16 (an external gate that was open at authoring) and AD-13 (whose own Rule declared current reality non-conforming) were settled by neither. The 2026-07-19 administrator ratification justifies the tag on AD-1–AD-10; the release-integration ADs were design intent. | Memlog L60 ratifies "the complete GOV-1 focused architecture draft"; AD-13 L259–L261 "The current `@main` caller … are non-conforming". | **discuss / ignore** — either accept blanket ADOPTED as "ratified" semantics and say so once in the memlog, or drop the tag from AD-13/AD-16 until the seam is proven end-to-end. |
| R-14 | Low | Structural Seed L446–L466 | Seed and second diagram lag the code and miss the dependency-direction rule the template requires. Seed omits `eng/dependency_handoff.py`, `tests/eng/test_dependency_handoff.py`, `tests/eng/test_release_evidence_v2.py`; the mermaid omits `workflow_source_closure.py` and both handoffs; there is no diagram of "C# Governance → invokes Python engine" (AD-6), which the template says "IS a rule". Both mermaid blocks are syntactically valid. | Template L34: "Include a dependency-direction diagram (who may depend on whom) — it IS a rule". `ls eng/ tests/eng/`. | **autofix (minor)** — add the two files to the seed; add one small `flowchart LR` showing Governance(C#) → dependency_graph.py → release_evidence.py / dependency_handoff.py → workflow_source_closure.py. |
| R-15 | Low | Deferred L490–L493 | Deferred bullet 3 restates AD-16 nearly verbatim instead of naming what is pushed down and why it can wait. Deferred should hold decisions not taken, not duplicate an AD. | L490–L493 vs AD-16 L371–L382. | **autofix** — fold into the R-4 rewrite; keep one line: "Owner-accepted Builds revision: external; recorded in policy when accepted." |
| R-16 | Low | Sources L15 (GOV-1 story) | Declared source is internally inconsistent and still says v2: story frontmatter `status: done` (L9) vs body `Status: in-progress` (L23); AC5 (L86) requires "manifest v2". Out of the spine's edit scope, but a `final` spine citing a source in this state weakens checklist 6 evidence. | Story L9, L23, L86. | **defer** — raise to the story owner; once R-3 lands, align AC5 wording. |

## Checklist Scorecard

| # | Checklist item | Result | One line |
| --- | --- | --- | --- |
| 1 | Fixes the real divergence points for stories under GOV-1 and misses none | **partial** | Graph boundary, identity, edge/digest, fail-closed, CI revision model, policy activation are all fixed (AD-1–AD-10, AD-12); missing: manifest v3 (R-3), catalog-gitlink vs execution-commit split (R-7), sub-manifest evidence envelopes (R-8). |
| 2 | Every AD's Rule is enforceable and prevents its stated divergence | **partial** | AD-1–AD-10, AD-14, AD-15 coherent; AD-11 and AD-16 Rules contain status narrative (R-4, R-5); AD-12 bootstrap clause never enforced (R-9); AD-13 Binds a `workflow_run` caller that no longer exists (R-2). |
| 3 | Nothing under Deferred could let two units diverge | **met** | Historical traversal, marker, topology are safe to defer; the one risky dimension (R-7) is *silent*, not deferred — scored under 1 and 8. |
| 4 | Named tech verified-current (note only) | **met** | Git 2.53 / Python 3.14 stdlib APIs still valid; SDK row stale vs `global.json` 10.0.400 (R-12). |
| 5 | Ratifies rather than contradicts brownfield code (note only) | **partial** | Core engine/policy/tests match AD-1–AD-10, AD-12, AD-15; code has moved past AD-13/AD-14/AD-16 (dispatch trigger, source proof, v3, pinned CI, populated authorizations) — R-2, R-3, R-4. |
| 6 | Covers the driving story's capabilities | **met** | AC1→AD-2/3/6; AC2→AD-6/8; AC3→AD-6/7; AC4→AD-8/10/12; AC5→AD-9/13/14/15; AC6→AD-6/16 + Deferred. AC5's v2 wording shares R-3's staleness (R-16). |
| 7 | Parent constraints inherited, none weakened/contradicted | **not met** | No Inherited Invariants section, `binds` lacks parent ids (R-11); AD-13/AD-14/AD-16 now contradict the 2026-08-04+ parent release-integration text with no authority statement (R-2). |
| 8 | Every owned dimension decided, deferred, or open | **partial** | Operational envelope: deferred to FR-24 (met). Failure/incident: AD-9/AD-15 (met). Testing: fixtures/Governance pins across ADs (met). Migration/rollout: AD-9/AD-12/AD-14 (met, v3 gap R-3). Observability/diagnostics: only the Errors convention row and per-selector diagnostics (partial, R-8). Ownership/RACI: Architect and Release Owner named ad hoc in AD-12/AD-16; parent "Ownership boundaries" not inherited (partial, R-11). |

## Hygiene Notes (not scored separately)

- **Terse substrate vs prose:** no — 4,453 words; AD-12/13/14/15 read as contract specs (R-10). AD-1–AD-7, AD-10, AD-11 (minus its second sentence) are at the right grain and are a good model for the rest.
- **Invariants vs seed separation:** mostly correct; AD-6's profile roster is seed data inside an invariant (R-6); Stack correctly labelled "observed".
- **ADOPTED tags:** used uniformly; signal lost (R-13).
- **Stable AD IDs:** AD-1…AD-16 ascending, no gaps or reuse. Pass.
- **Frontmatter:** `status`/`updated` inconsistent with git history (R-1); `sources` and `companions` valid and present on disk; `binds` incomplete for epic altitude (R-11).
- **Diagrams:** two valid mermaid `flowchart` blocks; the first (depth boundary) is load-bearing; the second is a partial file-dependency sketch (R-14).
- **Template comments / placeholders:** none present. Pass.
- **Memlog:** decision history is thorough to 2026-07-19 and then stops; the 2026-08-03 spine edit, 2026-08-08 issue-17 reopening, 2026-08-16 gitlink/execution split, and 2026-08-30 manifest v3 all lack entries (R-1).
