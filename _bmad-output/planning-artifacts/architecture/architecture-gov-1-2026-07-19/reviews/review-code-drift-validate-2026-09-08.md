# Reviewer Gate — Brownfield / Implementation-Drift Lens (VALIDATE) — GOV-1 Spine vs Code, 2026-09-08

**Verdict: FAIL** — the spine (final 2026-07-19) no longer ratifies the brownfield codebase on AD-13/AD-14/AD-16 (release trigger model, manifest schema, and unfreeze gate were all superseded by the REL-5 exact-SHA dispatch decision without a spine amendment), and two AD-13/AD-15 clauses (runtime workflow-source coordinates, `post_release` self-authorization) are not honored by the shipped workflows. No Critical finding; 5 High, 7 Medium, 6 Low.

Reviewed at repo HEAD `053b2008` (clean working tree). Scope: `eng/dependency_graph.py`, `eng/dependency-graph-policy.json`, `eng/workflow_source_closure.py`, `eng/dependency_handoff.py`, `eng/release_evidence.py`, `eng/release_prepublish.py`, `eng/release_contract.py`, `eng/release_disposition.py`, `.github/workflows/{ci,release,release-evidence}.yml`, `tests/eng/*`, `tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs`, GOV-1 story, `sprint-status.yaml`, `deferred-work.md`. No file other than this review was modified; no submodule was initialized or updated.

Class legend: **(a)** code violates spine · **(b)** spine stale/over-specified, code made a legitimate call · **(c)** spine names/omits something (seed drift).

## Findings

| ID | Severity | AD / Section | Class | Finding | Evidence | Recommended disposition |
| --- | --- | --- | --- | --- | --- | --- |
| D-1 | High | AD-13 | b | Release is **not** a `workflow_run` caller receiving CI run ID + event head. It is operator `workflow_dispatch` from the exact current `refs/heads/main` SHA; the CI run is then *selected* via the Actions API (`release_contract.py select-ci` on `head_sha=github.sha`), the run-suffixed AD-13 artifact is downloaded, `verify-ci --live` runs, and `main` is re-checked before protected credentials. Functionally equivalent authentication, but the spine's trigger model, "caller passes run ID/head" wording, and the "no ambient/default branch may select the candidate" sentence are contradicted (the dispatch SHA *is* the default-branch head, subsequently authenticated). REL-5 recorded this as the "2026-08-04 supersession"; the spine was never amended. | `.github/workflows/release.yml:6-7,17,41-147,270-281`; `sprint-status.yaml:290-299,334-339`; Governance pin `CiGovernanceTests.cs:1195,1426,2146` requires `workflow_dispatch:` | **discuss** — amend AD-13 to ratify the exact-SHA dispatch model (dispatch SHA == live main == authenticated push-CI head == handoff candidate) and delete the `workflow_run`/`head_sha` input wording. |
| D-2 | High | AD-16 / Deferred | b | REL-4 publication freeze is **no longer in effect**: v4.1.0 (run 31107597159) and v4.1.1 (run 31116045352) were published 2026-08-14 using a FrontComposer-selected Builds pin (`4eb33928…`), while the spine says unfreeze/eligibility wait for an owner-accepted immutable revision from reopened issue 17 or a successor, and "no FrontComposer-owned contingency is authorized" without a dated decision. DW-1783 (accepted Builds revision) is still open. The REL-5 / sprint-change-proposal-2026-08-03 decision is the de-facto contingency but AD-16 and Deferred were not updated. | `sprint-status.yaml:342-349`; `deferred-work.md` DW-1783 (`:8721`), DW-1742 (`:8372`); `release.yml:17,321,329` | **discuss** — Architect + Release Owner either (i) record the dated contingency decision and rewrite AD-16/Deferred to "pin-and-pre-authorize in active policy" as the accepted external contract, or (ii) declare the published releases as non-governed. Spine must stop asserting a freeze that operations have lifted. |
| D-3 | High | AD-14 | b | Manifest producer emits `manifest_schema: hexalith.release-evidence.v3`, not v2. v3 `workflow_provenance.ci` is `{run{repository,workflow_path,run_id,run_attempt,event,branch,head_sha},evidence_sha256}` (no `caller/reusable/actions`), and `release` is `{caller,reusable,builds_execution_sha}` (no `actions`); `definition_digest = canonical_sha256({ci,release})` over those. v2 shape is accepted for verification only. Governance pins v3. The spine's exact AD-14 member sets, the "ci-only evaluator digest" recomputation, and the `{ci:{caller,reusable,actions},release:{…}}` digest material are therefore stale. | `eng/release_evidence.py:64-65,833-886,911-958,1411-1420,3403`; `CiGovernanceTests.cs:3570` | **discuss** — amend AD-14 to describe v3 (and mark v2 as the migration predecessor, audit/verify-only) or revert code to v2. Note the action closure is still bound transitively via `ci.evidence_sha256` → CI handoff `evaluator.actions` and `release.builds_execution_sha`; spine should say so explicitly if v3 is ratified. |
| D-4 | High | AD-13 (runtime identity, static closure) | a | No workflow records or validates actual `github.workflow_ref/workflow_sha` or `job.workflow_ref/workflow_sha`. The evaluator used for AD-13/AD-15 is **projected from the policy row** whose caller `blob_sha256` matches the live caller blob (`draft-evaluator`), not observed at runtime; `eng/workflow_source_closure.py` is never executed by `ci.yml`, `release.yml`, or `release-evidence.yml` (only its unit tests run in `quality.yml:169`). The "Governance constructs the same static transitive closure and requires it to equal one authorization" clause has no production or Governance-lane execution against the live workflows. | `rg workflow_sha .github eng` → only manifest `workflow_ref` field (`release_evidence.py:1389,3407`); `dependency_handoff.py:547-622`; `rg workflow_source_closure .github` → `quality.yml:169` only | **discuss** — either wire `workflow_source_closure.py` into `ci.yml`/`release.yml` (compute closure at candidate commit, compare to policy row, record `job.workflow_sha`) or downgrade the spine clause to "policy row pre-authorized by blob hash; runtime observation deferred" with a Deferred entry. |
| D-5 | High | AD-15 (post-release), AD-12 | a | `release-evidence.yml` never drafts/validates a `post_release` evaluator and calls `verify-release` **without `--root`**, so no active-policy evaluator authorization is checked at verification time. Independently, the HEAD `release-evidence.yml` blob (sha256 `185593f0…`) matches **none** of the five `post_release` caller rows in the active policy (`307008c0…`, `318cd253…`, `854349c0…`, `8664cf12…`, `9ca68589…`) — the rows went stale after commit `7625ccb3` and nothing failed because nothing consumes them. | `.github/workflows/release-evidence.yml:171-181` (no `--root`, no `--stage post_release`); `eng/dependency-graph-policy.json:1725-2257`; blob check `git show HEAD:.github/workflows/release-evidence.yml \| sha256sum` | **autofix** (code+policy) — add `draft-evaluator --stage post_release` + `require_evaluator_authorized` to the verifier and pre-authorize the current blob in a later policy revision; or **discuss** dropping the `post_release` stage from AD-12/AD-15 if the Release Owner accepts a two-stage model. |
| D-6 | Medium | AD-12 / AD-13 | a | `ci.yml` treats `create-ci` exit 2 (no matching active-policy authorization) as success (`created=false`) and the push run stays green with **no** AD-13 artifact; the spine says an unauthorized closure "fails before CI handoff". Release then fails closed later, but CI evidence of the failure is soft. Already tracked as DW-1795. | `.github/workflows/ci.yml:167-209`; `deferred-work.md` DW-1795 (`:8825`) | **discuss** — make exit 2 a hard failure on push once the two-phase pre-authorization pattern (DW-1796/DW-1808) is documented; or ratify the soft-defer in AD-12 with the rationale (pin bumps must not block CI). |
| D-7 | Medium | AD-12 (bootstrap) | b/c | `HEXALITH_DEPENDENCY_POLICY_BOOTSTRAP_SHA256` does not exist anywhere in code, workflows, or tests; the "one-time v1 bootstrap" never had a machine path. A base policy has existed since 2026-07-19 so bootstrap is permanently unavailable by the spine's own rule. The zero-`before` push path uses the **candidate** policy for diagnostics and flags `release_eligible=false`, which is the spine's intended outcome but not its stated mechanism (`policy_commit="$CANDIDATE"` when before is all-zero). | `rg HEXALITH_DEPENDENCY_POLICY_BOOTSTRAP_SHA256` → no hits; `ci.yml:181-184`; `eng/dependency_graph.py:826-836,1930-1950`; fixture `test_zero_push_base_forces_full_affected_and_is_never_release_eligible` | **autofix** (spine) — move the bootstrap paragraph to a historical note in Deferred; state the zero-before rule as implemented (candidate policy for diagnostics only, never release-eligible). |
| D-8 | Medium | AD-13, AD-15 | b | Artifact names are run-suffixed: `dependency-release-handoff-${run_id}-${run_attempt}` and `release-verification-handoff-${run_id}-${run_attempt}`; the spine says exactly `dependency-release-handoff` / `release-verification-handoff`. The AD-15 artifact also uploads a **directory** (release-verification-handoff.json, dependency-release-handoff.json copy, release.json, manifest.json, assets.json, release-evaluator.json, optional `.deferred.json`) rather than exactly one JSON file; the verifier tolerates this by requiring the handoff *pair*. Governance tests pin the suffixed names. | `ci.yml:215`; `release.yml:163,574-575,557`; `release-evidence.yml:125,171-172`; `CiGovernanceTests.cs:775,2151,2154,2413` | **autofix** (spine) — ratify the run-suffixed naming (it prevents cross-attempt collisions) and define the AD-15 artifact as "the handoff JSON plus the authenticated CI handoff copy; auxiliary files are non-normative". |
| D-9 | Medium | AD-15 | a | When `verify-source` fails (no authenticated AD-13 handoff), the AD-15 job uploads only `release-verification-handoff.deferred.json`; no `release-verification-handoff.json` with null version/tag/manifest and empty assets is produced. Spine: failed/pre-publication attempts "never omit the artifact" and use JSON nulls. Same when `create-release` exits 2. The verifier records this as "deferred emission" and continues on a fail-closed disposition, so it is not a silent green, but it is not the spine's shape. | `release.yml:470-481,559-564`; `release-evidence.yml:178-181` | **discuss** — either ratify the `.deferred.json` convention in AD-15 (a documented sentinel is arguably better than a null-filled handoff that cannot bind a `ci_handoff`), or emit the null handoff. |
| D-10 | Medium | AD-13, AD-9 | b/c | A second CI→Release evidence path exists that the spine does not name: `hexalith.dependency-release-source.v1` (`create-source`, artifact `dependency-release-source-*`, no `evaluator` member). `prepare_manifest` accepts **either** `--source-proof` **or** `--ci-handoff` as candidate/graph/policy authority. `release.yml` uses only the ci-handoff path today, but the alternate authority contradicts "the authenticated handoff candidate is the sole release-candidate authority". | `ci.yml:147-165`; `eng/dependency_handoff.py:23,203-272`; `eng/release_evidence.py:3241-3266`; `eng/release_prepublish.py:401-411` | **discuss** — either remove the source-proof path from `prepare_manifest` (keep it as CI diagnostics only) or add it to AD-13 as an explicitly non-release-eligible diagnostic artifact. |
| D-11 | Medium | AD-5 | a | No Git object-format check exists (`extensions.objectformat` / `rev-parse --show-object-format` are never queried); SHA-256-object repositories are rejected only implicitly by the 40-hex commit regex, and no fixture covers it. Also, no **golden digest literal** fixture pins the canonical bytes — tests only assert determinism (`envelope1 == envelope2`) and recompute digests with the same helper. | `rg -i "objectformat|object-format" eng/dependency_graph.py` → none; `tests/eng/test_dependency_graph.py:179,1078,1311,1326` | **autofix** (tests) — add an explicit object-format probe with a fail-closed diagnostic and a pinned literal digest fixture for a small synthetic graph; or relax AD-5 to "SHA-1 implied by 40-hex identity validation". |
| D-12 | Low | AD-12 | b | `evaluator_authorizations` is an object keyed by stage (`{"ci":[…],"release":[…],"post_release":[…]}`), not one flat sorted registry; each row still carries `stage` and `closure_digest`. Consumers index `policy["evaluator_authorizations"][stage]`. | `eng/dependency-graph-policy.json:346-348,974,1725`; `dependency_handoff.py:103,571` | **autofix** (spine) — ratify the stage-keyed shape. |
| D-13 | Low | Structural Seed / Capability Map | c | Seed omits GOV-1-owned units that exist: `eng/dependency_handoff.py` (both handoff contracts — the AD-13/AD-15 implementation), `eng/release_prepublish.py` (prepare/seal orchestration, env→argv plumbing), `eng/release_contract.py` (`select-ci`, `builds`, `manifest`, `publication` — the exact-SHA dispatch authentication), `eng/release_disposition.py` (post-release run classification), `eng/release_compatibility.py`; tests `test_dependency_handoff.py`, `test_release_evidence_v2.py`, `test_release_prepublish.py`, `test_release_contract.py`, `test_release_disposition.py`, `test_ci_governance.py`, and `tests/ci-governance/fixtures/*` (manifest fixtures). Capability row "CI-to-release handoff → named CI artifact + release.yml" should also name `dependency_handoff.py` and `release_contract.py`. | `ls eng tests/eng`; story File List `gov-1-…md:414-439` | **autofix** (spine) — regenerate Structural Seed and Capability Map from the code. |
| D-14 | Low | AD-8, AD-10, AD-13, AD-15 | c | Identifiers a second implementer could not infer: schemas `hexalith.dependency-graph-diff.v1`, `hexalith.dependency-object-acquisition.v1`, `hexalith.affected-module-build-results.v1`, `hexalith.release-evidence-recovery.v1`, `frontcomposer.release-run-disposition.v2`, `frontcomposer.release-ledger-record.v2`; env vars `BUILDS_EXECUTION_SHA`, `HEXALITH_BUILDS_EXECUTION_SHA`, `DEPENDENCY_RELEASE_HANDOFF`, `RELEASE_EVALUATOR`; artifacts `release-ci-handoff-*`, `release-candidate-*`, `dependency-graph-evidence-*`; the `.hexalith/builds-execution` exact-SHA Builds checkout and local action `./.hexalith/builds-execution/Github/initialize-build`; `.github/release-evidence-recovery.json`; exit-2 `.deferred.json` sentinel; policy members `builds_identity`, `module_build_registry`, `resource_limits`. | `dependency_graph.py:36,895,1052`; `release.yml:17,229-237,284-286`; `release-evidence.yml:14,47-67,112,217,600` | **autofix** (spine) — add a "Named identifiers" table or fold into the relevant ADs. |
| D-15 | Low | AD-13 (pins) | b | Pin state has improved past the spine text: `ci.yml` → `domain-ci.yml@4eb33928a1d8c7775f97221cf9edc171db0cb5f8`; `release.yml` → `domain-release.yml@4eb33928…`; Builds local actions are checked out at that SHA and invoked from the checkout (spine-conformant). Governance forbids `@main` for both. The spine's "current `@main` caller… non-conforming" sentences are stale. Residual `@main` (`initialize-build@main`, `initialize-dotnet@main`, `commitlint.yml@main`, `codeql.yml@main`, `dependency-review.yml@main`) is confined to non-release workflows (`quality.yml`, `nightly.yml`, etc.) that the spine does not bind. | `ci.yml:25`; `release.yml:229-237,321`; `CiGovernanceTests.cs:733-734,1341-1362,1390-1400` | **autofix** (spine) — replace the "current @main" prose with the pinned-state rule; optionally note the non-binding residual. |
| D-16 | Low | AD-3 | b | Grammar accepted by code is slightly wider than the spine: `ssh://git@github.com/owner/repo(.git)?/?` URL form and an optional trailing slash on all three forms. HTTPS userinfo/port/query/fragment/percent are rejected by regex/explicit check; one `.git` stripped; lowercase; control chars and `.`/`..` segments rejected — conformant. | `eng/dependency_graph.py:270-293` | **autofix** (spine) — add "ssh:// URL form" and "optional trailing slash" to AD-3. |
| D-17 | Low | AD-6 | b | `deferred-work.md` DW-1803 records that sibling catalogs are now presence-only rather than exact required-version fail-closed, and that only project-context (not planning architecture/spine) was updated. AD-6 itself does not fix version semantics, but the spine's "Executable Policy Authority" section ("required package row is … authoritative") should be read against the shipped profile semantics. Not re-verified at profile level in this pass. | `deferred-work.md:8896-8901` | **defer** — resolve with DW-1803. |
| D-18 | Low | Stack | b | Observed-version table is informational only; no CI step records Git/Python/.NET versions "in diagnostics" as the section claims. | `rg -n "python3 --version|git --version" .github/workflows/ci.yml` → none | **ignore** or trim the sentence. |

## Confirmed conformances (no finding)

- **AD-1/AD-4/AD-5 core**: `SCHEMA = "hexalith.dependency-graph.v1"`; envelope exactly `{schema, root, edge_count, edges, graph_digest}` with `root` exactly `{repository, commit}`; edge members exactly the six, Builds edges add `catalog_sha256` + nullable `catalog_contract_version` (marker regex `[A-Za-z0-9][A-Za-z0-9._-]{0,63}`); sort tuple `(depth, owner_repository, owner_commit, path, repository, commit)`; `json.dumps(ensure_ascii=True, allow_nan=False, sort_keys=True, separators=(",", ":"))` UTF-8; `object_pairs_hook` duplicate rejection; `edge_count`/`depth` bool-as-int rejection; depth ∈ {1,2} (`dependency_graph.py:34,322,410-414,456-499,1486`).
- **AD-7**: policy limits 4096 / 64 MiB / 1 MiB / 4 MiB and materialization 16,384 / 16 MiB / 256 MiB (`policy.json:333-345`); `cat-file -s` before blob reads (`:204`); `ls-tree -r -z --full-tree` under byte ceiling (`:222-225,735-745,1301-1303`); non-160000/regular-file rejection in contract-tree extraction (`:752-763`).
- **AD-8**: `PULL_REQUEST_BASE=github.event.pull_request.base.sha`, `CANDIDATE=github.sha`, `PUSH_BEFORE=github.event.before` (`ci.yml:64-83`); merge-base computed and required equal to event base (`dependency_graph.py:1963-1969`); logical diff key `(owner_repository, path, repository)` (`:509,572`); depth-1-first classification (`:674-691`).
- **AD-9**: fallback digest material is exactly `{definition, package_set, dependency_graph, dependency_policy, workflow_definition}` (`release_evidence.py:2283-2289`); `local` sentinel forbidden (`:3237-3238`).
- **AD-10**: `acquire` builds isolated bare stores from policy identities only; `submodules: false` everywhere in the GOV-1 path (`ci.yml:52-59,102-106`; `release.yml:39,131-138`).
- **AD-12 bindings**: `RELEASE_DEFINITION_FILES` and `FALLBACK_INVALIDATION_FILES` include `eng/dependency-graph-policy.json`, `.github/workflows/ci.yml`, `eng/dependency_graph.py`, `eng/dependency_handoff.py`, `eng/workflow_source_closure.py` (`release_evidence.py:123-168`); policy read from `event_base`/non-zero `before` (`ci.yml:181`; `dependency_handoff.py:161-162,529-531`); `evaluator_authorizations` rows exactly `{stage,caller,reusable,actions,closure_digest}`, caller `{repository,workflow_path,blob_sha256}`, actions sorted by `(repository,path,commit,blob_sha256)` (`dependency_handoff.py:64-108`; `workflow_source_closure.py:975-1041`). HEAD `ci.yml` and `release.yml` blobs are each authorized exactly once for Builds `4eb33928…`.
- **AD-13 handoff shape**: `dependency-release-handoff.json` with exactly `{schema, run, revisions, evaluator, dependency_policy, dependency_graph}`; `run` 7 members; `revisions {base, candidate, merge_base=null}`; `evaluator {caller, reusable, actions, definition_digest}`; run_id/attempt positive ints; event `push`, branch `main`; Release recomputes evaluator digest and raw handoff SHA-256 and verifies run/artifact via Actions API (`dependency_handoff.py:131-200`; `release.yml:78-147`).
- **AD-14 policy projection**: `dependency_policy` exactly `{schema, repository, path, commit, sha256}` with `path == eng/dependency-graph-policy.json`, `schema == hexalith.dependency-graph-policy.v1` (`dependency_handoff.py:111-121`); seal = SHA-256 over compact sorted-key JSON of all members except `seal` (`release_evidence.py:1725`); legacy audit via `--audit-legacy` (`:3142,3984`).
- **AD-15 handoff shape**: exactly `{schema, release_run, ci_handoff, candidate, dependency_policy, release, manifest, assets, evaluator}` with the specified sub-shapes, null discipline for unpublished attempts, sorted unique assets `{name,sha256,size}`; emit job is `if: always()`; verifier derives candidate from the AD-15 artifact, not second-hop `head_sha` (`dependency_handoff.py:275-401`; `release.yml:406-577`; `release-evidence.yml:117-207`).

## Per-AD conformance scorecard

| AD | Status | One-line |
| --- | --- | --- |
| AD-1 | conforms | Depth-1/2 boundary enforced; census is evidence (43 edges at `3786330d`, not a fixed count). |
| AD-2 | conforms | Exact-object reads via plumbing; no clone of candidate URLs; `submodules: false`. |
| AD-3 | conforms | Grammar implemented; slightly wider (`ssh://` form, trailing slash) — D-16. |
| AD-4 | conforms | Edge identity, member sets, ordinal sort tuple as specified. |
| AD-5 | partial | Envelope/digest/dup-rejection/type checks conform; no object-format probe or golden literal fixture — D-11. |
| AD-6 | conforms (not re-verified at profile level) | Python engine is the single semantic authority; C# consumes result; sibling presence-only drift tracked in DW-1803 — D-17. |
| AD-7 | conforms | All ceilings in policy and enforced before decode/parse; materialization caps and mode checks present. |
| AD-8 | conforms | PR/push revision model, merge-base equality, diff key, depth-1-first classification all present. |
| AD-9 | conforms | Fallback digest binds graph/policy/workflow; offline+live modes; `local` forbidden; v3 accepted alongside v2 (see AD-14). |
| AD-10 | conforms | Isolated bare-store acquisition from policy identities for both base and candidate. |
| AD-11 | conforms | No runtime/API scope observed in GOV-1 files. |
| AD-12 | partial | Policy authority, base/before rule, bindings conform; bootstrap variable never existed (D-7); exit-2 soft pass (D-6); stage-keyed registry (D-12). |
| AD-13 | diverges | Handoff schema conforms and pins are now 40-hex, but trigger is `workflow_dispatch` (D-1), runtime `workflow_sha` never recorded and closure never executed (D-4), artifact run-suffixed (D-8), alternate source-proof authority (D-10). |
| AD-14 | diverges | Production emits `hexalith.release-evidence.v3` with reduced `workflow_provenance` shape; v2 verify-only (D-3). |
| AD-15 | partial | Handoff shape/`if: always()`/original-candidate rule conform; artifact is a directory (D-8); deferred path omits null handoff (D-9); no `post_release` authorization and policy rows stale (D-5). |
| AD-16 | diverges | Releases v4.1.0/v4.1.1 published under a FrontComposer-selected pin; issue-17/successor acceptance (DW-1783) still open; spine still asserts freeze (D-2). |

## Disposition summary

- **Spine amendments required (b/c)**: D-1, D-2, D-3, D-7, D-8, D-12, D-13, D-14, D-15, D-16 — re-ratify against code; most are autofix rewrites of AD-13/AD-14/AD-16/Seed/Map.
- **Code or policy work required (a)**: D-4 (runtime closure), D-5 (post_release authorization + stale rows), D-6 (exit-2 fail-closed), D-9 (deferred-attempt handoff shape), D-11 (object-format probe, golden fixture).
- **Decisions needed from Architect + Release Owner**: whether the REL-5 exact-SHA dispatch + self-selected immutable pin is the accepted replacement for the AD-13/AD-16 `workflow_run` + owner-accepted-revision model (D-1, D-2), and whether v3 supersedes v2 as the governed manifest schema (D-3).
