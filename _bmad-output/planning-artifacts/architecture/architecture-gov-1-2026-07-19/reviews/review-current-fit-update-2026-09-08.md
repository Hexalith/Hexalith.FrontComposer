# Current-Fit Review (UPDATE) — GOV-1 Architecture Spine — 2026-09-08

**Lens (verbatim):** "Verify every committed decision was web-researched or reality-checked rather than asserted from training data: current library/framework versions, that each named technology still exists and fits, and — greenfield — the live defaults of any starter it leans on. Flag anything that could be out of date and wasn't confirmed against the web, the existing project, or the current starter."

**Spine:** `_bmad-output/planning-artifacts/architecture/architecture-gov-1-2026-07-19/ARCHITECTURE-SPINE.md` (status `draft`, updated 2026-09-08, rewritten this pass)
**Prior pass:** `reviews/review-current-fit-validate-2026-09-08.md` (C-1..C-8)
**Memlog reality-check entries:** line 19 (2026-07-19 Git/Python), line 38 (.NET 10.0.302, object format), line 75 (2026-09-08: .NET 10.0.400, Git 2.53.0/2.55.0, Python 3.14.4/3.14.7, GitHub Actions contexts incl. `job.*` GitHub.com-only, upload-artifact v7.0.1 immutability/name uniqueness, object format sha1, Git 3.0 sha256 default).
**Repository state reviewed:** HEAD `053b2008` (2026-09-08); working tree clean except untracked review artifacts. No submodule was initialized, updated, or entered; `references/Hexalith.Builds` content at other revisions was read through the GitHub API only.

## Verdict: PASS WITH FINDINGS

The Stack table is now fully reality-checked and correct (every version, tag SHA, and platform caveat re-verified against the web and the repository today). The 2026-09-08 `version` memlog entry closes the "asserted, not checked" gap the prior pass found for GitHub Actions semantics. AD-12, AD-13, AD-16, and AD-17 describe the repository as it is at HEAD, and the Builds lineage claim in AD-16 is verifiable end to end. What remains stale is one `[ADOPTED]` statement in AD-15 about the post-release verifier's self-authorization: the repository neither implements it nor authorizes the current `release-evidence.yml` blob, and the live policy rows disagree with the `reusable = null` shape the spine asserts. No Critical finding.

## Findings

| ID | Severity | Section | Finding | Evidence / what was checked | Disposition + proposed fix |
| --- | --- | --- | --- | --- | --- |
| CU-1 | High | AD-15 ("requires its own caller blob to match a `post_release` authorization … before ledger acceptance"), AD-12 evaluator authorizations | The post-release self-authorization the rule asserts as current behavior is not in the repository, and the current `release-evidence.yml` blob is not authorized. (a) `.github/workflows/release-evidence.yml` never invokes `draft-evaluator --stage post_release` or any `post_release` matching; `eng/dependency_handoff.py verify-release` calls `require_evaluator_authorized(policy, "release", …)` only (lines 358, 377); the string `post_release` appears in no workflow and in `eng/release_disposition.py` not at all. (b) The HEAD blob of `release-evidence.yml` is SHA-256 `185593f0…` (commit `7625ccb3`, 2026-08-30); the five `post_release` rows in `eng/dependency-graph-policy.json` (last changed `05f77621`, 2026-09-05) authorize `854349c0…`, `8664cf12…`, `307008c0…`, `318cd253…`, `9ca68589…` — the newest matches commit `8cb9f06a` (2026-08-22); the two later edits (`c8b31f12`, `7625ccb3`) were never authorized. If the AD-15 rule were enforced today every post-release verification would fail closed; because it is not enforced, the ledger accepts records from an unauthorized caller blob. Either way the spine's "current state" sentence is false. | `grep -rn post_release .github/workflows eng/release_disposition.py` → none; `sed -n 330,380p eng/dependency_handoff.py`; `git show <c>:.github/workflows/release-evidence.yml \| sha256sum` for the last 8 commits vs `python3 -c` dump of `evaluator_authorizations.post_release[*].caller.blob_sha256`; `git log -5 -- .github/workflows/release-evidence.yml`; `git log -1 -- eng/dependency-graph-policy.json`. | **discuss** — Architect + Release Owner choose one: (1) keep the rule and move its enforcement into scope (wire `draft-evaluator --stage post_release` + `require_evaluator_authorized(policy, "post_release", …)` into `release-evidence.yml` before the ledger record, and pre-authorize blob `185593f0…` via the AD-12 two-phase pattern); or (2) rewrite the AD-15 sentence as an intended control and add a Deferred entry "post-release caller self-authorization not enforced in v1; current `release-evidence.yml` blob unauthorized since 2026-08-30" with a revisit trigger. The policy refresh belongs in a separate earlier commit (workflow edits de-authorize the chain). |
| CU-2 | Medium | AD-15 ("`post_release` authorization (with `reusable` null)"), AD-12 ("`reusable` … JSON `null` when the caller's static closure reaches no reusable workflow") | The live policy contradicts the asserted shape: all five `post_release` rows carry a non-null `reusable` = `{github.com/hexalith/hexalith.builds, .github/workflows/domain-release.yml, <Builds commit>, <blob>}` even though `release-evidence.yml` calls no reusable workflow. This is by design of the closure tool: `collect_workflow_source_closure(require_reusable_edge=False)` visits the reusable coordinate independently ("Post-release verifiers authorize their own caller closure plus the Builds revision they check out", `eng/workflow_source_closure.py` lines 814-851), and AD-13 itself needs `reusable.commit` to resolve the `.hexalith/builds-execution` checkout ref (`release-evidence.yml` line 318-324 pins `4eb33928…`). The spine is therefore internally inconsistent (AD-13 vs AD-15) and inconsistent with the policy it says is authoritative. | Policy dump (above); `sed -n 806,870p eng/workflow_source_closure.py`; `sed -n 314,330p .github/workflows/release-evidence.yml`. | **autofix** — in AD-12 and AD-15 replace "`reusable` null" with "`reusable` is the Builds execution coordinate the caller checks out into `.hexalith/builds-execution/` (visited as an independent root, `require_reusable_edge=false`) and must equal the caller's literal checkout `ref:`"; keep `null` only as the schema's permitted value for a caller with neither a reusable call nor a Builds checkout. |
| CU-3 | Low | AD-15 ("derives the live root only from the handoff candidate and sealed manifest, never from `workflow_run.head_sha`, `GITHUB_SHA`, …") | The verifier does read the authenticated run's `head_sha` (Actions run-attempt API, `release-evidence.yml` lines 84-98) and `eng/release_disposition.py classify` sets `candidate = ad15_candidate or run["head_sha"]` (line 103), i.e. the run head becomes the candidate whenever the AD-15 handoff lacks one (deferred emission). The recovery record (`hexalith.release-evidence-recovery.v1`) also carries `expected_head_sha`. For a governed, sealed attempt the handoff candidate wins, so the protective intent holds; but the absolute "never" is not what the code does. | `sed -n 40,100p .github/workflows/release-evidence.yml`; `sed -n 95,120p eng/release_disposition.py`; `.github/release-evidence-recovery.json` exists. | **autofix** — word as "the authenticated run's `head_sha` is used only to authenticate the run coordinate and as the candidate of a *non-governed* disposition; a governed attempt's root comes from the handoff candidate/sealed manifest and must equal it". |
| CU-4 | Low | Structural Seed | The seed now lists the three files C-8 asked for (all exist), but still omits two existing test modules that implement spine rules: `tests/eng/test_release_disposition.py` (AD-15 disposition/ledger record) and `tests/eng/test_release_prepublish.py` (AD-9/AD-17 prepare/seal). Picture of the existing project is incomplete, not wrong. | `find . -path ./references -prune -o -name "test_release_*" -print`; `ls tests/eng`. | **autofix** — add the two files to the seed tree. |
| CU-5 | Low | AD-16 companions: `g2-hexalith-builds-inline-pre-publish-gate-request.md` (listed in `sources` and `companions`), `deferred-work.md` DW-1688 | The companion the spine cites for the accepted revision still states "Required next request: pin `references/Hexalith.Builds` + caller reusable refs to that 40-hex SHA" and "the next FrontComposer NuGet/GitHub release remain[s] blocked until the integration checklist … succeeds", while AD-16 (correctly, per repository and memory) makes the catalog gitlink (`a32cb422…`) independent of the execution commit (`4eb33928…`) and the lineage has advanced through five authorized Builds commits; DW-1688 is still `status: open` although its close trigger (revision integrated, authorized) is met. The spine is right; its cited source is stale. | `sed -n 1,40p _bmad-output/planning-artifacts/g2-…-request.md`; `grep -n DW-1688 -A6 _bmad-output/implementation-artifacts/deferred-work.md`; `git ls-tree HEAD references/Hexalith.Builds` → `a32cb422…`; `gh api repos/Hexalith/Hexalith.Builds/compare/a8a50859…4eb33928` → ahead 70 / behind 0 (also `3f0e3595` +29, `99d5a46c` +7, `0a3508b3` +3, gitlink `a32cb422` +166 — every pinned commit descends from the accepted revision); `gh api …/issues/17` → state `open`, updated 2026-08-08. | **autofix (companions)** — add a dated status line to the G2 request ("2026-09-08: integrated; execution lineage `a8a50859 → … → 4eb33928`; gitlink independent per AD-16") and close DW-1688 or re-scope it to the end-to-end proof; no spine change. |
| CU-6 | Low | Stack ("`job.workflow_*` contexts GitHub.com-only"), AD-18 ("relies on Actions artifact immutability") | The GitHub.com-only constraint is broader than the spine records: `actions/upload-artifact` v4+ (hence v7.0.1 and the immutability AD-18 relies on) is itself unsupported on GitHub Enterprise Server (README: v4+ "unsupported on GHES, use v3.2.2"). Both platform dependencies were verified today, but the Stack row attributes the GHES exclusion only to `job.*`. | https://raw.githubusercontent.com/actions/upload-artifact/main/README.md ("Artifacts created by upload-artifact@v4 are immutable", `overwrite` default `false`, `archive` default `true`, GHES note); https://docs.github.com/en/actions/reference/workflows-and-actions/contexts (`job.workflow_ref`/`job.workflow_sha` "(not available on GitHub Enterprise Server)"). | **autofix** — Stack row: "GitHub Actions on GitHub.com only: `actions/upload-artifact` v7.0.1 (v4+ immutable artifacts, unique names per run, `archive: true`; v4+ unsupported on GHES); `job.workflow_*` contexts (not on GHES)". |
| CU-7 | Low | Front-matter `sources` | The two web sources that actually ground the release-trigger semantics are not listed: the events reference (`GITHUB_SHA` = "last commit on the `GITHUB_REF` branch" for `workflow_dispatch`, "last commit on default branch" for `workflow_run` — the basis of AD-13's dispatch model and AD-15's refusal to use the `workflow_run` head) and the upload-artifact README (immutability/name uniqueness AD-18 relies on). The checks are recorded in memlog line 75, so this is provenance hygiene, not an unchecked decision. | https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows (table quoted above); upload-artifact README. | **autofix** — append both URLs to `sources`. |

Severity counts: Critical 0 · High 1 · Medium 1 · Low 5.

## Closure of prior findings C-1..C-8

| Prior | Status | Evidence |
| --- | --- | --- |
| C-1 (AD-16 stale external-gate narrative) | **Closed in spine; residual in companions (CU-5)** | AD-16 now names the owner-accepted revision `a8a50859…` (matches the G2 request "Accepted revision" and issue 17 acceptance comment), states lineage advances via the AD-12 two-phase pattern, and declares the gitlink independent of the execution commit. Verified: `ci.yml`/`release.yml` pin `domain-ci.yml`/`domain-release.yml@4eb33928…`, `builds-execution-sha: 4eb33928…`, both `.hexalith/builds-execution` checkouts `ref: 4eb33928…`; HEAD `ci.yml` blob `22e1674c…` and `release.yml` blob `305f56dd…` each match exactly one active `ci`/`release` row whose `reusable.commit` is `4eb33928…`; all five Builds commits in the registry descend from `a8a50859…` (GitHub compare API). DW-1688 remains `open` and the G2 doc wording is stale. |
| C-2 (AD-13 `workflow_run`/REL-4 freeze) | **Closed** | AD-13 now reads "operator-initiated `workflow_dispatch` on `refs/heads/main` only … equal the live `main` ref re-read through the API … exactly one completed successful push-CI run … `select-ci`". Matches `release.yml` lines 6-7, 47-75 and `eng/release_contract.py` (main-ref shape check lines 236-242; `total_count != len` → "ambiguous or paginated" line 250-251; "expected exactly one exact-source CI run" line 259; filter `event push / head_branch main / status completed / conclusion success` line 262). No `REL-4`/`freeze` text remains in the spine. |
| C-3 (.NET 10.0.302 stale) | **Closed** | Stack: ".NET SDK 10.0.400 (`global.json`, `rollForward: latestPatch`)" = `global.json`; local `dotnet --version` 10.0.400; dotnet.microsoft.com lists 10.0.400 as the latest SDK band for runtime 10.0.11 (2026-08-11). Memlog line 75 records it. |
| C-4 (Git/Python observed vs upstream) | **Closed** | Stack header dated 2026-09-08; rows "Git 2.53.0 local (upstream 2.55.0)", "Python 3.14.4 local (upstream 3.14.7)"; "CI invokes `python3`, not `uv`" (all three workflows call `python3` only). Web: git-scm.com shows 2.55.0 (2026-06-29); python.org shows 3.14.7 (2026-08-05) as latest. |
| C-5 (literal artifact names) | **Closed** | AD-13: `dependency-release-handoff-<run_id>-<run_attempt>` "(exactly one match, zip archive …)"; AD-15: `release-verification-handoff-<run_id>-<run_attempt>` under `if: always()`; Consistency row "`<base>-<run_id>-<run_attempt>` zip archives". Matches `ci.yml` line 215, `release.yml` lines 575-577 (`if: always()`), `release-evidence.yml` line 125; upload-artifact `archive` default `true` and none of the workflows sets it. |
| C-6 (`job.workflow_*` unrecorded / GHES) | **Closed** | Memlog line 75 `version` entry records the contexts check; AD-13 ends "`job.*` contexts are GitHub.com-only"; Stack row repeats it. Docs re-verified today: "(not available on GitHub Enterprise Server)" on both `job.workflow_ref` and `job.workflow_sha`. See CU-6 for the broader GHES caveat. |
| C-7 (Git 3.0 sha256 trigger) | **Closed** | Deferred: "Reopen trigger for Git SHA-256 object support: Git 3.0 GA or GitHub hosting sha256 repositories; SHA-1 collision risk is accepted for v1". BreakingChanges doc re-verified: default for new repositories changes to sha256 in 3.0, "no planned release date", "no plan to deprecate sha1". Local `git rev-parse --show-object-format` → `sha1`. |
| C-8 (seed omitted handoff files) | **Closed; small residual (CU-4)** | Seed lists `eng/dependency_handoff.py`, `tests/eng/test_dependency_handoff.py`, `tests/eng/test_release_evidence_v2.py`, `tests/eng/test_workflow_source_closure.py`, `eng/release_disposition.py`; all exist on disk. |

## Verified current (2026-09-08)

- **Stack — Git:** local `git version 2.53.0`; upstream 2.55.0 (git-scm.com, released 2026-06-29). Plumbing relied on (`ls-tree -r -z --full-tree`, `cat-file -s`, `config --blob`, `merge-base`) unchanged (prior pass reviewed 2.54/2.55 notes; no newer release since).
- **Stack — Python:** local 3.14.4; upstream 3.14.7 (python.org, 2026-08-05; 3.14 supported to Oct 2030). `json`/`hashlib` stdlib controls unchanged.
- **Stack — .NET SDK:** `global.json` = `10.0.400`, `rollForward: latestPatch`; local SDKs 10.0.302 + 10.0.400; 10.0.400 is the current 10.0.4xx band (runtime 10.0.11, 2026-08-11).
- **Stack — upload-artifact:** tag `v7.0.1` → commit `043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` (GitHub refs API), published 2026-04-10, and it is the latest release; every `uses: actions/upload-artifact@043fb46d…` in the three workflows matches. Artifacts immutable since v4; names unique per run; `overwrite` default `false`; `archive` default `true` (README). Also checked though not in the spine: `actions/checkout@9c091bb2…` = tag v7.0.0 (upstream latest v7.0.1, 2026-07-20 — a closure choice, not a spine claim).
- **Stack — Git object format:** all participating repositories (root and gitlinks) read as SHA-1 40-hex; local `sha1`; Git 3.0 sha256 default confirmed with no planned date.
- **GitHub Actions semantics:** `workflow_dispatch` → `GITHUB_SHA` = last commit on the dispatched branch/tag, `GITHUB_REF` = that branch (AD-13 basis; `release.yml` additionally requires `refs/heads/main`, 40-hex, checkout HEAD equality, and API `main` ref equality); `workflow_run` → `GITHUB_SHA`/`GITHUB_REF` = default branch (why AD-15 must not use them); `push` → `GITHUB_SHA` = tip pushed, payload `before` = "most recent commit on ref before the push" (AD-8/AD-12); `pull_request` → `GITHUB_SHA` = last merge commit on `refs/pull/N/merge` (AD-8). `github.workflow_ref`/`github.workflow_sha` present; `job.workflow_ref`/`job.workflow_sha` present, GitHub.com-only; Builds `domain-release.yml@4eb33928…` consumes `job.workflow_repository/sha/ref` (lines 269-270, 654-656).
- **AD-12 current state:** policy `schema` `hexalith.dependency-graph-policy.v1`; top-level members `builds_identity`, `trusted_identities`, `semantic_profiles`, `profiles`, `module_build_registry`, `resource_limits`, `evaluator_authorizations`; `resource_limits` equal the AD-7 table exactly (4096 / 64 MiB / 1 MiB / 4 MiB / 16384 / 16 MiB / 256 MiB / 16 / 256 / 1 MiB / 16 MiB); rows keyed `ci`/`release`/`post_release` with `{stage, caller, reusable, actions, closure_digest}`; the eight files AD-12 names are members of both `RELEASE_DEFINITION_FILES` and `FALLBACK_INVALIDATION_FILES` in `eng/release_evidence.py` (lines 123-160); `helper_version` is added to the fallback fingerprints (line 2269); `RELEASE_ATTESTATION_FALLBACK_FINGERPRINTS_SHA256` is the variable read (line 242).
- **AD-13 current state:** `select-ci` main-ref/pagination/exactly-one/success checks; handoff artifact `dependency-release-handoff-<run_id>-<run_attempt>` with `total_count == len(artifacts)` and exactly one non-expired match; zip must contain exactly `dependency-release-handoff.json`; `verify-ci --live` after `dependency_graph.py acquire`; `.run.candidate == .revisions.candidate == DISPATCH_SHA` and run coordinates asserted; `dependency-release-source` proof uploaded separately as diagnostic; `.hexalith/builds-execution` checkout `ref: 4eb33928…` equals `BUILDS_EXECUTION_SHA` env and `builds-execution-sha` input.
- **AD-16 current state:** accepted revision `a8a50859…` (2026-08-08, issue 17 open/reopened, acceptance comment linked in the G2 request); every registry Builds commit (`a8a50859`, `0a3508b3`, `99d5a46c`, `3f0e3595`, `4eb33928`) is a descendant of it; catalog gitlink `a32cb422…` ≠ execution `4eb33928…`, as AD-16 permits.
- **AD-17 current state:** `CURRENT_MANIFEST_SCHEMA = "hexalith.release-evidence.v3"`; `prepare-manifest` emits only it (line 3403); verifier accepts v2/v3 and audits legacy only under `--audit-legacy` (AD-14); `workflow_provenance` validated as exactly `{ci, release, definition_digest}`, `ci` = `{run, evidence_sha256}`, `run` = `{repository, workflow_path, run_id, run_attempt, event, branch, head_sha}` with push/main enforced, `release` = `{caller, reusable, builds_execution_sha}` with `reusable.commit = builds_execution_sha` (lines 752-780, 833-942).
- **AD-9 fallback digest:** `{"definition", "package_set", "dependency_graph", "dependency_policy", "workflow_definition"}` (lines 2279-2290) — identical to the spine formula.
- **AD-18 current state:** workflow-level `permissions: contents: read` (ci, release) / `actions: read` + `contents: read` (release-evidence); `prepare-candidate` has `environment: production`; the reusable `domain-release.yml@4eb33928…` places `contents: write`/`id-token: write`/`attestations: write`/`NUGET_API_KEY` jobs under `environment: ${{ inputs.environment-name }}` = `production`; the FrontComposer `production` environment has `required_reviewers` = `jpiquot` (GitHub environments API). No job holds `actions: write`.
- **Evidence envelopes named in Consistency Conventions:** every schema identifier resolves to a producer in `eng/*.py` or `release-evidence.yml` (`hexalith.dependency-graph-diff.v1`, `…object-acquisition.v1`, `…affected-module-build-results.v1`, `…dependency-release-source.v1`, `…release-evidence-recovery.v1`, `frontcomposer.release-run-disposition.v2`, `frontcomposer.release-ledger-record.v2`).
- **Deferred sentinel:** `release-verification-handoff.deferred.json` is produced by `output.with_suffix(".deferred.json")` (`eng/dependency_handoff.py` lines 506-508); `release-evidence.yml` treats a missing handoff pair as deferred and never greens it.

## Method notes

- Read-only except this file. No `git submodule` command; no `cd` into `references/`; Builds workflow content at `4eb33928…` fetched via `gh api repos/Hexalith/Hexalith.Builds/contents/...?ref=`.
- Blob hashes were computed on `git cat-file -p HEAD:<path>` bytes (the same bytes `eng/workflow_source_closure.py` hashes at line 368), not on working-tree files.
- Web sources were fetched on 2026-09-08 and are cited inline.
