# Reviewer Gate — Rubric Walker (UPDATE, pass 2) — GOV-1 Architecture Spine

**Target:** `_bmad-output/planning-artifacts/architecture/architecture-gov-1-2026-07-19/ARCHITECTURE-SPINE.md` (status `draft`, `updated: 2026-09-08`, AD-1..AD-18, 6,736 words)
**Review date:** 2026-09-08
**Lens:** rubric walker, UPDATE intent, pass 2 (closure of RU-1..RU-11 from `reviews/review-rubric-update-2026-09-08.md`, parent/contract reconciliation, cross-AD consistency, hygiene)
**Inputs read:** spine (full), `.memlog.md` (entries after the pass-1 gate event through the reconciliation and register entries), pass-1 review, `reviews/reconcile-parent-contract-update-2026-09-08.md`, `nonconformance-register-2026-09-08.md` (NC-1..NC-27 and the companion-drift section), parent `_bmad-output/planning-artifacts/architecture.md` (frontmatter, Key Invariants L37-55, Shared Catalog section L93-208, FR-24 section L209-294), contract `_bmad-output/contracts/shared-catalog-dependency-governance-2026-07-19.md` (frontmatter, decisions 13-16, Consequences), `_bmad-output/planning-artifacts/g2-hexalith-builds-inline-pre-publish-gate-request.md` L70-115, `.releaserc.json`, `.github/release-evidence-recovery.json`, `.github/workflows/release.yml` L284-285, `eng/release_prepublish.py` env names, `eng/dependency_handoff.py:22`, `.github/workflows/release-evidence.yml:217,600`. Deterministic lint reported passing by the caller; placeholder grep re-run here (0 hits). No mermaid renderer is available offline; blocks were read by hand.

## Verdict

**PASS WITH FINDINGS** — 0 Critical, 0 High, 3 Medium, 8 Low. All eleven pass-1 findings are closed, recorded as won't-fix (RU-2), or correctly deferred to their owners with the deferral recorded (RU-11). The AD-18 actor contradiction (RU-1) is resolved with a named carrier and the sealed manifest and handoff shapes stay closed. The reconciled parent no longer contradicts any AD on the three pass-1 points (manifest v3, handoff-as-authority, execution identity). What remains is citation-grade: two Inherited Invariants rows cite a parent section name that does not exist, one row cites the parent for content that only the G2 companion carries (and whose "signing" word sits against the parent's "unsigned" packages), and AD-15 repeats the RU-1 pattern once more by promising to record two conclusions into a closed shape with one conclusion member. None of these blocks `status: final` on its own; all are one-sentence autofixes plus one Release Owner word choice.

## Closure of RU-1..RU-11

| ID | Pass-1 finding (short) | Status | Closing text / evidence |
| --- | --- | --- | --- |
| RU-1 (High) | AD-18 actors recorded "in release evidence" with no member in any closed envelope | **closed** | AD-15 Ledger L444-449: "`frontcomposer.release-ledger-record.v2` … exactly `{schema, release_run, verification_run, candidate, disposition, published, tag, manifest_verified, published_bytes_verified, repository_signatures_verified, incident, evaluator, actors, environment_protection, publish_gate_variable}` … `actors` and `environment_protection` are the AD-18 records." AD-18 L513-516: "records `actors` as `{dispatching_actor, triggering_actor, environment:{name, approver, approved_at}}` from the authenticated run and deployment-review APIs." AD-17 L493-494: "Actors and approvals never enter the sealed manifest; their home is the AD-15 ledger record." Memlog: "(decision by architect) RU-1/SU-3/AU-9/AU-7: actors and environment approval live only in the ledger record … Sealed manifest and AD-13 handoff shapes stay closed." Implementation gap escalated as NC-10/NC-12. Residual: the `environment_protection` branch-policy member and the `publish_gate_variable` shape are unnamed → RU2-4 (Low). |
| RU-2 (Medium) | Bloat regressed; R-10 open | **closed as won't-fix (recorded)** | Memlog: "(direction by architect) R-10/RU-2 bloat: accepted as won't-fix; this spine is deliberately contract-grade (6.7k words) because the adversarial lens repeatedly shows that unnamed member sets and sort keys are exactly where two implementers diverge; member lists stay in the spine and the contract doc mirrors them." Not re-raised. Word count today 6,736. |
| RU-3 (Medium) | "Lineage" undefined as a decidable predicate | **closed** | AD-16 L464-469: "A Builds commit is **in the accepted lineage** iff it first appears as a pre-authorized `reusable.commit` in a policy revision that was itself the active policy of a later change, and that chain of policy revisions starts at the revision recording the owner-accepted revision `a8a50859fa2f27f511a9470dfe1e3ae54d0ebc1a` (Hexalith.Builds issue 17, accepted 2026-08-08); Git ancestry is not required." Memlog RU-3 entry matches; the unused `0a3508b3` rows are parked as NC-16 (Release Owner prune decision). |
| RU-4 (Medium) | No enforcement point checks a policy row against the blobs it names | **closed** | AD-12 L290-293: "The PR gate recomputes every added or changed row's closure from the exact caller/reusable/action blobs it names and fails closed on any `actions` or `closure_digest` difference, so a row is never trusted on its own word." Deferred L660-662 now reads "v1 trust is the exact caller blob hash, the literal reusable commit, and the PR-gate recomputation of AD-12." Memlog RU-4 entry; implementation gap escalated as NC-20 (workflows still call `draft-evaluator` only). |
| RU-5 (Medium) | Parent contradicted AD-13/AD-16/AD-17 (v2, source-proof replaces handoff, literal `3f0e3595`) | **closed** | Parent L191-193: "The manifest lineage is `hexalith.release-evidence.v1` (legacy) → `v2` → `v3`; preparation emits only `v3` … (spine AD-17)." Parent L236-240: "Push CI on `main` emits the sealed `hexalith.dependency-release-handoff.v1` artifact (spine AD-13) … It is the sole release-candidate authority. The `hexalith.dependency-release-source.v1` proof CI also emits is a diagnostic only and never authorizes preparation." Parent L256-258: "the active-policy `reusable.commit` in the AD-16 lineage rooted at the owner-accepted revision `a8a50859…` (currently `4eb33928…`)". Memlog `(change)` entry present. Residual staleness risk of the literal → RU2-10 (Low, parent-side). |
| RU-6 (Low) | Release activation commit ambiguous among handoff members | **closed** | AD-12 L268-269: "at release, from the handoff `dependency_policy.commit`, which must equal `revisions.base`." AD-13 L338-339 repeats "exactly, not merely a commit carrying an identical blob" (AU-22). |
| RU-7 (Low) | Ownership boundaries not inherited | **closed** | Inherited Invariants row 5 L63: "Ownership boundaries: Hexalith.Builds owns the reusable-workflow contract and catalog marker; FrontComposer owns evidence, verification, and publication of authorized bytes; the Release Owner owns production approval, credentials, exceptions, and incident response \| `architecture.md` FR-24 Release Evidence Architecture \| AD-15 ledger, AD-16 lineage, AD-18 approval, Deferred runbook items." Parent L284-292 carries the bullets. `binds` gained `parent:ownership-boundaries`. |
| RU-8 (Low) | `Repo.GIT` normalizes to a distinct `.git` identity | **closed** | AD-3 L98-99: "a normalized repository segment still ending in `.git` is rejected." |
| RU-9 (Low) | `release-evidence.yml` / `release_disposition.py` neither listed nor excluded from the fingerprint lists | **closed** | AD-12 Bindings L301-303: "Post-publication sources (`release-evidence.yml`, `eng/release_disposition.py`) are deliberately outside both lists because they never authorize (AD-9)." |
| RU-10 (Low) | Pipe label `--live` unverified against a mermaid lexer | **closed (label)** / **residual render check** | Third block L628: `REL -->\|select-ci, verify-ci live\| REL`. All three blocks re-read: no `--` inside any `\|…\|` label. Residual unquoted `#` in `G[C# Governance tests]` → RU2-11 (Low). |
| RU-11 (Low) | GOV-1 story wording (v2, freeze, status) | **deferred, recorded** | Register "Companion documents still carrying superseded wording (not code)": "GOV-1 story: frontmatter `status: done` vs body `in-progress`; AC5 says manifest v2 and freeze controls … See `reviews/reconcile-story-g2-update-2026-09-08.md`." Memlog: "companion wording drift (GOV-1 story, G2 request, sprint-status GOV-1 item, DW-1688) listed there for their owners, not edited in this run." Out of spine scope; no spine edit needed. |

## New Findings

| ID | Severity | Section | Finding | Fix | Disposition |
| --- | --- | --- | --- | --- | --- |
| RU2-1 | **Medium** | Inherited Invariants rows 1-2 (L59-60) | Both rows cite "`architecture.md` Cross-Cutting Invariants". No section by that name exists in the parent; the two bullets live under `## Key Invariants` (parent L37; approved GOV-1 amendment at L47-49, semantic-not-allowlist at L46), and the amendment is repeated in `## Shared Catalog Compatibility And Dependency Provenance` (L95-97). Checklist 7 asks that the table cite sections that exist; a reader following the citation finds nothing. | Replace "Cross-Cutting Invariants" with "Key Invariants" in both rows (optionally "Key Invariants; Shared Catalog Compatibility And Dependency Provenance" for row 1). | **autofix** |
| RU2-2 | **Medium** | Inherited Invariants row 6 (L64); parent FR-24 L217, L262-266 | The row "BUILD-REL-1 governed reusable mode: signing/attestation happens inside the pinned reusable over the exact candidate packages before any side effect; `.releaserc.json` `prepareCmd`/`publishCmd` are FrontComposer-owned" cites "`architecture.md` FR-24 Release Evidence Architecture", but that section contains no "BUILD-REL-1", no `.releaserc.json`, no `prepareCmd`/`publishCmd`, and no attestation text (grep → 0). The content comes from the G2 request (companion) L76-92: "`actions/attest-build-provenance` executed over those exact candidate packages … FrontComposer's `.releaserc.json` continues to own the semantic-release `prepareCmd`/`publishCmd` evidence lifecycle". Worse, the row's word "signing" sits against the parent's own FR-24 model, which checksums "the exact unsigned packages" (L217) and says the pinned reusable "publishes only manifest-authorized unsigned candidate packages and symbols" (L266), relying on NuGet.org repository signatures (L276-278). Two readers diverge on whether packages reach NuGet author-signed. The `.releaserc.json` ownership fact is true today (`prepareCmd`/`publishCmd` at `.releaserc.json:21-22`) but is not inherited from the parent. | Either (a) change the source column to the G2 request (companion) and keep the row as a companion-sourced invariant, or (b) add one sentence to the parent FR-24 section carrying it. In both cases replace "signing/attestation" with "attestation (and author signing only when the governed mode enables it, after the sealed checksums are taken)" or drop "signing", and reconcile parent L266 so "unsigned" is explicitly the checksummed/sealed state, not necessarily the published state. Release Owner picks the word. | **autofix (citation) + discuss (signing word)** |
| RU2-3 | **Medium** | AD-15 L427-429 vs Ledger L444-449 | "The post-release workflow authenticates the triggering Release run ID/attempt, repository, workflow path, and conclusion through read-only Actions APIs, records both the API conclusion and the handoff's projection", yet the closed `frontcomposer.release-ledger-record.v2` shape carries exactly one `release_run.conclusion` and no member for the second value. Same class as RU-1: a record requirement with no carrier in a closed, duplicate-rejecting shape; one builder stores the API value, another the projection, a third invents `handoff_conclusion`. NC-6 restates the requirement ("record both conclusions") without naming the member either. | Pin it in one sentence: "Ledger `release_run.conclusion` is the Actions API value; the handoff's projection is carried as `release_run.handoff_conclusion` (closed vocabulary of AD-15) …" — or state that the projection is retained only inside the verification artifact and the ledger records the API value alone. Update the closed member list accordingly. | **autofix** |
| RU2-4 | Low | AD-18 L511-516; AD-15 Ledger L447 | `environment_protection` names three members (`required_reviewers`, `prevent_self_review`, `can_admins_bypass`) and then "the deployment-branch policy" with no member name; `publish_gate_variable` (AD-12 L306-308 "their value at publication is recorded in the ledger record") has no stated shape (string value? `{name, value}`? null when unset?). Both are closed-shape members, so naming is the whole rule. | "`environment_protection` is exactly `{required_reviewers, prevent_self_review, can_admins_bypass, deployment_branch_policy}`; `publish_gate_variable` is `{name, value}` with `value` the literal repository-variable string at publication or `null` when unset." | **autofix** |
| RU2-5 | Low | AD-13 L316-318; parent L243-245; contract decision 13 | The uniqueness predicate reads as self-contradictory on first pass: "the head of exactly one completed push run of `.github/workflows/ci.yml` on `main` (whatever its conclusion) whose conclusion is `success`". The intent (AU-15: count completed runs of every conclusion, require exactly one, and it must have succeeded) is recoverable, but the parent ("selects exactly one completed successful push CI run for that same SHA") and contract decision 13 ("exactly one completed successful push run of `ci.yml` and of `quality.yml`") project the weaker reading under which a failed sibling run for the same head is ignored. | Spine: "exactly one completed push run of `ci.yml` on `main` has that head (runs of every conclusion counted), and that run concluded `success`; likewise for `quality.yml`." Parent/contract: mirror the same sentence in finalize. | **autofix (spine) / defer to finalize (parent, contract)** |
| RU2-6 | Low | Frontmatter `binds` L11 vs Inherited Invariants L59-65 | `binds` carries six `parent:*` slugs for seven Inherited rows; row 7 ("GOV-1 alters no runtime, API, package inventory, or UX", parent L294) has no slug. The task's hygiene rule is that `binds` entries match Inherited rows. | Add `parent:no-runtime-change` (or fold row 7 into `parent:fr-24-exact-artifact-pipeline` and say so in the row). | **autofix** |
| RU2-7 | Low | AD-15 L405-408 | Case (a) requires "`release-verification-handoff.json` plus a byte-identical copy of the authenticated CI handoff", and "other files are non-normative", but the copy's file name inside the artifact is never stated. The verifier must locate the copy to hash it; without a pinned name it must guess (`dependency-release-handoff.json`?) or scan, and a scan over non-normative files re-opens the "neither/both" ambiguity the sentence tries to close. | "…plus a byte-identical copy of the authenticated CI handoff named `dependency-release-handoff.json`." | **autofix** |
| RU2-8 | Low | Consistency Conventions "Builds identities" L543 | "execution identity = active-policy `reusable.commit` / `builds_execution_sha`" — the active policy holds two distinct `reusable.commit` values (the `ci` row and the `release` row, which AD-13/AD-16 say need not be equal). AD-16 pins `builds_execution_sha` to the release pin only; the Consistency row does not. | "execution identity = the active-policy `release` row `reusable.commit`, equal to `builds_execution_sha` (AD-16); the `ci` row pin is sealed through the CI handoff." | **autofix** |
| RU2-9 | Low | Contract decision 15 vs AD-15 L405-410 | Contract: "treats a missing or deferred handoff pair as an incident state". Spine: `release-verification-handoff.deferred.json` is the legitimate outcome "for an attempt that never obtained an authenticated CI handoff"; only "neither, both, or a copy whose SHA-256 differs" is the `missing-artifact` incident. The contract folds the deferral into the incident set. The spine also never names which disposition a `.deferred.json` attempt receives (the Evidence-envelopes row lists `frontcomposer.release-run-disposition.v2` but AD-15 names only the three incident states and `published`). | Contract: "treats a missing, duplicated, or hash-mismatched handoff pair as an incident state; a deferred sentinel is a recorded non-governed attempt." Spine: add the disposition name for a deferred attempt to the AD-15 Ledger paragraph. | **defer to finalize (contract) / autofix (spine, one clause)** |
| RU2-10 | Low | Parent L236, L258 | Two parent-side residuals after reconciliation, neither a contradiction: (1) L258 keeps the literal "(currently `4eb33928a1d8c7775f97221cf9edc171db0cb5f8`)" — true today (`ci.yml:25`, `release.yml:17/286/321/329`) but exactly the volatile policy-row mirroring AD-12 forbids in architecture and the pass-1 RU-5 fix asked to replace; it will drift on the next lineage advance. (2) L236 calls the CI handoff "sealed"; it has no AD-14 `seal` member, its identity is the raw SHA-256 (AD-17 `evidence_sha256`). | Drop the parenthetical (or date it "as of 2026-09-08"); change "sealed" to "authenticated". | **defer to finalize (parent)** |
| RU2-11 | Low | Mermaid block 2 L527 | The `--` concern from RU-10 is gone from every pipe label (checked all six labels across the three blocks). Remaining unverified lexer risk, still not machine-rendered: bare `#` in the unquoted node label `G[C# Governance tests]`, plus `/` and `.` in unquoted file-name labels (`E[eng/dependency_graph.py]` etc.). These usually render, but only quoting removes the doubt. | Quote: `G["C# Governance tests"]`; optionally quote the file-name labels. Render once in a mermaid-capable viewer before `status: final`. | **ignore / render-check** |

## Checklist 7 — parent versus ADs after reconciliation

| Parent statement | Spine AD | Result |
| --- | --- | --- |
| L47-49 approved GOV-1 amendment (depth 1 + 2) | AD-1 | agrees (row 1; section name wrong → RU2-1) |
| L46 semantic compatibility, no SHA allowlist | AD-6 | agrees (row 2; section name wrong → RU2-1) |
| L99-103 release-integration note 2026-09-08 (`workflow_dispatch`, one successful push-CI run, sealed handoff, 40-hex Builds refs in lineage, spine authoritative) | AD-13, AD-16 | agrees |
| L134-136 bootstrap consumed 2026-07-19, base-policy absence permanently fail-closed | AD-12 L276-278 | agrees |
| L138-139 handoff and manifest v3 record policy coordinates | AD-13, AD-14 | agrees |
| L191-198 lineage v1→v2→v3, only v3 produced, seal covers all but `seal`, fallback digest immutable and never rebound, v2 verify-only | AD-9, AD-14, AD-17 | agrees |
| L236-240 handoff is sole authority; source proof diagnostic only | AD-13 L325-331 | agrees |
| L242-248 dispatch gate: `refs/heads/main`, 40-hex, live main re-read, one successful CI run, one successful `quality.yml` run, run/attempt-named artifact, no substitute candidate | AD-13 | agrees on substance; uniqueness phrasing weaker → RU2-5 |
| L251-253 preparation recomputes live graph, real `160000` catalog gitlink, emits v3, catalog gitlink independent of execution commit | AD-16, AD-17, Consistency "Builds identities" | agrees |
| L253-256 v3 binds raw CI handoff hash, caller bytes at candidate, reusable bytes at execution commit, identical `builds-execution-sha` | AD-17 | agrees |
| L256-261 execution identity = active-policy `reusable.commit` in AD-16 lineage rooted at `a8a50859`; equality set; `production` environment (AD-18) | AD-13, AD-16, AD-18 | agrees; literal "currently …" → RU2-10 |
| L262-267 sealed to Release run/attempt, upload after production approval, reusable re-verifies, publishes unsigned candidate packages, tag resolves to dispatched SHA | AD-13, AD-15, AD-18 | agrees except the Inherited row 6 "signing" word → RU2-2 |
| L269-282 run-topology authentication, partial-publication incident, no retroactive authorization, durable Release assets vs supplemental artifacts | AD-9, AD-15 | agrees (AD-15 L450-453 carries the durable-evidence rule) |
| L284-292 ownership boundaries | Inherited row 5 | agrees; the parent lists "upstream semantic catalog-contract version/canonicalization contract" and "minimum permissions" under Builds, which the row compresses to "catalog marker" and "reusable-workflow contract" — acceptable paraphrase |
| L294 does not alter runtime, public product behavior, or UX | AD-11, Inherited row 7 | agrees; row 7 lacks a `binds` slug → RU2-6 |

No AD weakens or contradicts an inherited row. Every cited parent section exists except "Cross-Cutting Invariants" (RU2-1); the FR-24 section holds ownership boundaries and the no-runtime sentence as the task states, but not the BUILD-REL-1 row content (RU2-2).

Contract (also reconciled): frontmatter `amended: 2026-09-08`, `amendmentStatus: approved`, `upstreamReleaseFollowUp` names `a8a50859` and the AD-16 lineage; decisions 13-16 agree with AD-13/AD-14/AD-15/AD-16 (decision 13 carries the `quality.yml` precondition, the `@main` non-conformance, the AD-7 ceilings; decision 14 the v3 lineage and immutable fallback digest; decision 16 the lineage root and two-phase advance); Consequences retire the freeze ("no freeze variable exists (spine AD-13, AD-18)") and name the lineage prerequisite. Two residual wording deltas: RU2-5 (uniqueness) and RU2-9 (deferred = incident).

## Cross-AD consistency (amended text)

| Pair | Result |
| --- | --- |
| AD-9 ↔ AD-17 | Consistent: `workflow_definition` = `workflow_provenance.definition_digest` over `{ci, release}`; single-use approval per `(candidate, run_id, run_attempt)` follows from `ci.evidence_sha256`. |
| AD-9 ↔ AD-12 Bindings | Consistent: eight files in both lists; two post-publication sources explicitly excluded (RU-9 closed). |
| AD-12 ↔ AD-13 | Consistent: caller-blob row match; `reusable` null only for `post_release`; `closure_digest` over `{stage, caller, reusable, actions}` (four members) vs handoff `definition_digest` over `{caller, reusable, actions}` — different names, different material, both stated. PR-gate recomputation (AD-12) is what the AD-13 "Static closure" paragraph and the Deferred bullet now rely on; the three agree. |
| AD-12 ↔ AD-16 | Consistent: lineage predicate is stated in terms of AD-12 activation ("active policy of a later change"); two-phase pattern named in both. |
| AD-13 ↔ AD-15 ↔ AD-17 ↔ AD-18 | Consistent on actors: AD-17 excludes them from the manifest, AD-15 Ledger carries `actors`/`environment_protection`, AD-18 defines both. Gaps: second conclusion carrier (RU2-3), branch-policy and `publish_gate_variable` member names (RU2-4), CI-copy file name (RU2-7). |
| AD-13 ↔ AD-16 ↔ Consistency "Builds identities" | Consistent on substance; the Consistency row does not say which policy row's `reusable.commit` is the execution identity (RU2-8). |
| AD-15 ↔ Deferred (incident response) | Consistent: `partial-publish-incident` defined once in AD-15; Deferred adds the duplicate-version re-dispatch case and runbook items without redefining states. |
| Structural Seed identifier table ↔ AD-13/AD-12/AD-9 | Consistent: `BUILDS_EXECUTION_SHA`/`HEXALITH_BUILDS_EXECUTION_SHA`/`builds-execution-sha`/checkout `ref:` = AD-13 equality set (the `domain-release.yml@` pin, a member per AD-13, is not listed in the table row — cosmetic); `DEPENDENCY_RELEASE_HANDOFF`/`RELEASE_EVALUATOR` exist (`release.yml:284-285`, `release_prepublish.py:273-274,401-402`); `RELEASE_ATTESTATION_*` and `HEXALITH_RELEASE_PUBLISH_ENABLED` match AD-12 Bindings; `.github/release-evidence-recovery.json` exists and is read by `release-evidence.yml:14,47,59`. Seed file list unchanged from pass 1 and present on disk. |
| Deferred ↔ ADs | Consistent: each of the seven bullets names what is pushed down and a trigger; the evaluator-code bullet restates the unprotected-`main` reality (NC-27) without weakening AD-12/AD-18. Nothing deferred lets two units diverge. |

## Checklist Scorecard

| # | Checklist item | Result | One line |
| --- | --- | --- | --- |
| 1 | Fixes the real divergence points and misses none | **met** | RU-1/RU-3/RU-4 carriers and predicates are now in the spine; remaining divergence points are member-naming residue (RU2-3, RU2-4, RU2-7, RU2-8). |
| 2 | Every AD's Rule is enforceable | **met, with residue** | AD-15's "records both" clause and AD-18's two unnamed members are the only rules a builder cannot implement identically twice. |
| 3 | Nothing under Deferred could let two units diverge | **met** | Unchanged from pass 1; the RU-4 fix removed the one open enforcement gap the Deferred section depended on. |
| 4 | Named tech verified-current (note only) | **met** | Stack unchanged and dated; no new technology named. |
| 5 | Ratifies rather than contradicts brownfield code (note only) | **met** | The new spine-over-code decisions (PR-gate recomputation, ledger v2 shape, verification-handoff v2, actors) are all escalated as NC-6/NC-10/NC-12/NC-19/NC-20; `dependency_handoff.py:22` still says `release-verification-handoff.v1` and `release-evidence.yml:217,600` still emit two ad-hoc `jq` shapes, as the register records. |
| 6 | Covers the driving story's capabilities | **met** | Unchanged; story wording drift is registered for its owner (RU-11). |
| 7 | Parent inherited; no AD weakens or contradicts an inherited one | **met, with citation defects** | Parent reconciled on all three RU-5 points; no contradiction found in the FR-24 or Shared Catalog sections. Citation defects: nonexistent section name (RU2-1), companion-sourced row cited to the parent with a "signing" word the parent does not carry (RU2-2), missing `binds` slug (RU2-6). |
| 8 | Every dimension the epic altitude owns is decided, deferred, or open | **met** | Actor identity and environment protection are now decided with a carrier (AD-15/AD-18); ownership is inherited (row 5); durable evidence chain decided (AD-15). |

## Hygiene

- **Template comments / placeholders:** `grep '<!--\|{name}\|{date}\|TODO\|TBD\|FIXME'` → 0. Pass.
- **AD ids:** headings AD-1..AD-18 in order, 18 of 18, no gaps, no reuse; titles unchanged from pass 1. Pass.
- **Frontmatter:** `status: draft`, `created: 2026-07-19`, `updated: 2026-09-08`; `sources`/`companions` local paths all exist; `binds` has `GOV-1` + six `parent:*` slugs — one short of the seven Inherited rows (RU2-6).
- **Mermaid:** three ` ```mermaid ` blocks; block 1 (`flowchart LR`, dotted labelled edge `-. excluded from v1 .->`), block 2 (`flowchart LR`, cylinder `POL[(…)]`), block 3 (`flowchart TD`, self-loop `REL --> REL`, cylinder `LED[(…)]`). Six pipe labels, none containing `--`. Not machine-rendered; only residual is the unquoted `#` (RU2-11).
- **Terse substrate:** not applicable — contract-grade by recorded decision (RU-2 won't-fix).
- **Invariants vs seed:** correct; profile/registry rows delegated to the policy; Stack labelled observed.
- **Deterministic lint:** reported passing by the caller; not re-run here.
