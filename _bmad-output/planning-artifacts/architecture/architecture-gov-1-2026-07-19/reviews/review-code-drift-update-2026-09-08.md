# Reviewer Gate — Brownfield / Implementation-Drift Lens (UPDATE) — GOV-1 Spine vs Code, 2026-09-08

**Verdict: PASS-with-findings (spine) / FAIL (implementation conformance).** The rewritten spine (AD-1..AD-18, `updated: 2026-09-08`, working tree) now ratifies the brownfield release seam the VALIDATE pass found stale (dispatch model, manifest v3, run-suffixed artifacts, deferred sentinel, stage-keyed registry, Builds lineage). Fourteen of the eighteen VALIDATE findings are closed by the rewrite; four are carried as deliberate implementation nonconformances. Against HEAD `053b2008` the code violates the spine in **1 Critical, 3 High, 4 Medium, 9 Low** places; the definitive register below lists **17 nonconformance items** for the implementation backlog. No finding requires a further spine amendment before hand-off, though six Low items (class b/c) recommend wording or seed corrections.

Reviewed: spine + `.memlog.md` (working tree), prior `review-code-drift-validate-2026-09-08.md`, and at HEAD: `eng/dependency_graph.py`, `eng/dependency_handoff.py`, `eng/workflow_source_closure.py`, `eng/release_evidence.py`, `eng/release_prepublish.py`, `eng/release_contract.py`, `eng/release_disposition.py`, `eng/release_compatibility.py`, `eng/dependency-graph-policy.json`, `.github/workflows/{ci,release,release-evidence,quality}.yml`, `tests/eng/*`, `tests/ci-governance/fixtures/`, `tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs`, `deferred-work.md`. Live workflow blob SHA-256 at HEAD: `ci.yml` = `22e1674c…` (authorized `ci` row, reusable `4eb33928…`), `release.yml` = `305f56dd…` (authorized `release` row, reusable `4eb33928…`), `release-evidence.yml` = `185593f0…` (**no** `post_release` row). No file other than this review was written; no submodule command was run; `references/` was not entered.

Class legend: **(a)** code violates spine — implementation must fix · **(b)** spine stale/over-specified vs a legitimate code call · **(c)** seed/diagram/identifier drift.

## Findings

| ID | Severity | AD | Class | Finding | Evidence | Disposition |
| --- | --- | --- | --- | --- | --- | --- |
| DU-1 | **Critical** | AD-9 | a | Production classification **rebinds** `approved_against_fingerprints_sha256` to the live digest before comparison, so the drift-invalidation guarantee is void: any graph/policy/workflow-definition change is silently re-approved, and an *absent* value never fails. `classify-release` calls `classify_release_payload(..., bind_live_fallback_digest=True)`; the rebinding overwrites `fallback["approved_against_fingerprints_sha256"]` with `fallback_v2_digest(live…)`. `release.yml` forwards `STATUS/APPROVER/APPROVED_AT/EXPIRES_AT` only — `vars.RELEASE_ATTESTATION_FALLBACK_FINGERPRINTS_SHA256` is never passed, and `release_prepublish.py` never forwards it either; `__version__` 4.1.0 note documents the rebinding as intentional. This is the path that published v4.1.0/v4.1.1. | `eng/release_evidence.py:31-33,2472,2724-2743,3834,4093`; `.github/workflows/release.yml:287-290`; `eng/release_prepublish.py:485-535` | **autofix (code)** — NC-1, NC-2, NC-3. |
| DU-2 | High | AD-15, AD-12 | a | `release-evidence.yml` performs **no** `post_release` evaluator authorization and calls `verify-release` **without `--root`**, so `validate_release_handoff(root=None)` never reloads the active policy blob and never checks the `release` or `post_release` rows. The five `post_release` policy rows all carry a non-null `reusable` = `domain-release.yml` (unreachable from `release-evidence.yml`, forbidden by AD-12) and none matches the HEAD caller blob `185593f0…` (rows: `307008c0…`, `318cd253…`, `854349c0…`, `8664cf12…`, `9ca68589…`). Nothing consumes them, so the staleness is invisible. | `.github/workflows/release-evidence.yml:171-176` (no `--root`, no `draft-evaluator --stage post_release`); `eng/dependency_handoff.py:355-360` (policy reload only when `root is not None`); `eng/dependency-graph-policy.json:1725-2257` | **autofix (code+policy)** — NC-6, NC-7 (after NC-8). |
| DU-3 | High | AD-12, AD-13, AD-15 | a | Exit-2 **soft deferral** survives at two stages. (i) `ci.yml`: `create-ci` exit 2 → `created=false`, push run stays green with no AD-13 artifact (comment still says "until AD-16 populates…"); DW-1795 open. (ii) `release.yml`: `create-release` exit 2 on an *unpublished* attempt prints a summary and exits 0; `_deferred_result` writes `release-verification-handoff.deferred.json` for an unauthorized **release** evaluator — the spine reserves the sentinel for attempts that never obtained an authenticated CI handoff (`release.yml:470-481`), and says an absent closure "is never a soft deferral". | `.github/workflows/ci.yml:167-209`; `.github/workflows/release.yml:552-564`; `eng/dependency_handoff.py:503-512,741-749`; `deferred-work.md:8825-8831` | **autofix (code)** — NC-4, NC-5. |
| DU-4 | Medium | AD-18 | a | Neither the dispatching actor, the triggering actor, nor the `production` environment approval is recorded anywhere in release evidence (`rg actor .github eng` → only prose/`APPROVAL_MATRIX` strings; sealed manifest v3 and AD-15 handoff carry no actor member). Governance has no static assertion that `pull_request_target` is absent, that no GOV-1 job holds `actions: write`, or that `dependency-governance` is secretless (only `NUGET_API_KEY` presence and `environment: production` are pinned). Permission blocks themselves conform (workflow-level `contents: read` ×3; write scopes and `NUGET_API_KEY` only on the `release` job, which passes `environment-name: production`). | `.github/workflows/release.yml:13-14,217,314-335`; `ci.yml:20-21,49-50`; `release-evidence.yml:20-22`; `CiGovernanceTests.cs:1419,2163,3502-3545` | **autofix (code+tests)** — NC-12, NC-13. Architect: name *where* actor/approval is sealed (AD-17 is a closed member set). |
| DU-5 | Medium | AD-13 | a | `prepare-manifest` still accepts `--source-proof` (`hexalith.dependency-release-source.v1`) as a full candidate/graph/policy/provenance authority when no CI handoff is given; `release_prepublish.py` forwards `DEPENDENCY_RELEASE_SOURCE_PROOF` and stages `ci_handoff or source_proof`; live manifest verification branches on either schema. `release.yml` sets only `DEPENDENCY_RELEASE_HANDOFF` today, so the path is dormant, not removed. | `eng/release_evidence.py:1542-1556,3241-3266,4003`; `eng/release_prepublish.py:272,400-409,538-548` | **autofix (code)** — NC-11. |
| DU-6 | Medium | AD-15 | a | The post-release verifier (i) never **independently downloads** the CI artifact at `ci_handoff.run_id/run_attempt` — it hashes only the copy embedded in the Release artifact (`rg dependency-release-handoff- release-evidence.yml` → none); (ii) never compares `release_run.{run_id,run_attempt,conclusion}` to the API-authenticated upstream coordinates (`validate_release_handoff` checks positivity only; `verify-release` receives no expected coordinates); (iii) the ledger record binds manifest/bytes/signatures/incident but **not** `steps.ad15.outputs.verified`, so a governed attempt whose AD-15 artifact lacks the handoff pair can still be ledgered `compliant-candidate`. Diagram 3's "POST re-authenticates CI artifact" edge describes the spine, not the code. | `.github/workflows/release-evidence.yml:120-182,590-602`; `eng/dependency_handoff.py:288-296` | **autofix (code)** — NC-6, NC-9, NC-10. |
| DU-7 | High | AD-12, AD-13 | a | The engines cannot represent the ratified `reusable: null` form: `validate_evaluator` → `_source(evaluator["reusable"])` requires a closed object; `_validate_closure`/`_validate_authorization`/`_authorization_sort_key` index `reusable[...]`; `draft_evaluator_for_stage` drafts `post_release` with a `domain-release.yml` placeholder and reports `authorized_draft` from `reusable.commit`. `collect_workflow_source_closure(require_reusable_edge=False)` **adds the unreachable reusable as a root** (`visit_workflow(*reusable_key, depth=0)`), which AD-12 now forbids. | `eng/dependency_handoff.py:53-66,547-620,690-694`; `eng/workflow_source_closure.py:806-870,907-951,979-1058` | **autofix (code)** — NC-8 (prerequisite of NC-7). |
| DU-8 | Low | AD-5 | a | No golden digest **literal** fixture: `tests/eng/test_dependency_graph.py` (99 tests) contains no 64-hex literal; digests are only recomputed with the same helper. Object-format half of D-11 is closed by the spine's "40-hex validation enforces it" wording, which the code satisfies. | `rg -E '"[0-9a-f]{64}"' tests/eng/test_dependency_graph.py` → none | **autofix (tests)** — NC-14. |
| DU-9 | Low | AD-12 | a | No Governance fixture pins the eight AD-12 members of `RELEASE_DEFINITION_FILES`/`FALLBACK_INVALIDATION_FILES`; `SharedPackageCatalog_WhenChanged_…` only asserts the shared catalog's presence/absence, and `test_release_evidence_v2.py:152` iterates the list without pinning it. The lists themselves conform (all eight present in both). | `eng/release_evidence.py:123-168`; `CiGovernanceTests.cs:1107-1127` | **autofix (tests)** — NC-15. |
| DU-10 | Medium | AD-9 | a | The drift fixture exercises `fallback_complete` with a stored digest (unit level) and passes; it never runs `classify_release_payload`/`classify-release` with the production flag, so the production rebinding (DU-1) is invisible to the suite. Spine requires a fixture proving drift invalidates "an otherwise complete approval" on the path that authorizes. | `tests/eng/test_release_evidence_v2.py:752-806` | **autofix (tests)** — NC-3. |
| DU-11 | Low | Diagram 2 | c | Module-dependency diagram edges do not match imports: `H → W` is false (nothing in `eng/` or any workflow imports/executes `workflow_source_closure.py`; only `quality.yml:169` unit tests), `P → H` is false (`release_prepublish.py` invokes only `release_evidence.py` subcommands), `C → M` is false (`release_contract.py` imports nothing from `eng/`); missing `M → H` (`importlib dependency_handoff`), `P → C` (`import release_contract`), `P → release_compatibility.py`. | `eng/dependency_handoff.py:19`; `eng/release_evidence.py:496,510`; `eng/release_prepublish.py:60-61,254-711` | **autofix (spine)** — redraw: `G→E`, `H→E`, `M→E`, `M→H`, `P→M (cli)`, `P→C`, `P→K[release_compatibility]`, `E→POL`, `H→POL`, `W→(tests only)`. |
| DU-12 | Low | Seed / Map | c | Structural Seed omits `eng/release_compatibility.py` (imported by `release_prepublish.py`, release-line policy), `tests/eng/test_release_prepublish.py`, `test_release_disposition.py`, `test_ci_governance.py`; `tests/ci-governance/fixtures/` holds manifest fixtures (`release-manifest-valid/invalid.json`) but **no policy fixtures** as the comment claims; `release_disposition.py` owns `frontcomposer.release-run-disposition.v2` only — the `release-ledger-record.v2` is composed inline by `jq` in the workflow. | `ls eng tests/eng tests/ci-governance/fixtures`; `release-evidence.yml:217,600`; `eng/release_disposition.py:107` | **autofix (spine)** — seed lines. |
| DU-13 | Low | AD-16, AD-12 | b/c | Policy carries pre-authorized closures for Builds `0a3508b3…` (ci ×2, release ×2, post_release ×1; added `d3739e78`, 2026-08-12) that were never pinned — the lineage moved 3f0e3595 → 4eb33928 on 2026-08-22 instead. Not in the memlog lineage. Harmless under the two-phase rule but stale trust rows; Builds descent of `0a3508b3` not verifiable without entering the submodule. | `eng/dependency-graph-policy.json` rows with reusable commit `0a3508b3e568…`; `git log -S0a3508b3e568 -- eng/dependency-graph-policy.json` | **discuss** — NC-16 (prune or record in AD-16 lineage). |
| DU-14 | Low | AD-8, AD-12 | b | Zero-before push: `diff` exits 2 with `ok:false`, uses the candidate policy for full-affected diagnostics, `release_eligible=false` — exactly the ratified rule. Two residues: `ci.yml:181-184` `policy_commit="$CANDIDATE"` is dead (handoff step is gated on `exit-code == '0'`), and the diagnostic evidence records `revisions.event_base = "000…0"`, which the Consistency row "all-zero OID is never a valid commit in any evidence member" reads literally. | `eng/dependency_graph.py:1928-1954`; `.github/workflows/ci.yml:173,181-184` | **autofix (spine wording + code)** — NC-17; Consistency row: "never *accepted* as a commit identity; may be *recorded* as a rejected input". |
| DU-15 | Low | AD-18 | b | The `release` publication job also holds `issues: write` and `pull-requests: write` (semantic-release comments). Spine enumerates only `contents/id-token/attestations` write and `NUGET_API_KEY`; not forbidden, but "least-privilege" should either enumerate them as accepted or the caller should drop them. | `.github/workflows/release.yml:314-320` | **discuss** — spine wording. |
| DU-16 | Low | AD-10, AD-2 | b | Post-release live verification (`verify-manifest --root . --graph-root .`) and `prepare-candidate`'s `verify-ci --live` read from the root-declared checkout initialized by `./.hexalith/builds-execution/Github/initialize-build`, not from isolated bare stores acquired from policy identities as `verify-source` does. Reads remain exact-object (AD-2 holds; no nested init), but AD-10 binds "live release verification" to isolated acquisition. | `.github/workflows/release-evidence.yml:311-321,421-425`; `release.yml:230-237,253-256` | **discuss** — either acquire in both jobs or scope AD-10 to CI + `verify-source`. |
| DU-17 | Low | AD-15, Consistency | c | `frontcomposer.release-ledger-record.v2` has two shapes (`{published,disposition,status}` for non-governed; `{tag,release_run_id,…,disposition}` for governed), no producer-side schema validation, and no consumer records its raw SHA-256 — the Consistency "Evidence envelopes" row lists it as closed/duplicate-rejecting/hash-recorded. | `.github/workflows/release-evidence.yml:213-218,590-602` | **autofix (code, folded into NC-10)** or relax the row for the ledger record. |

## Nonconformance register (hand-off to implementation backlog)

Fix size: **S** ≤ ½ day, one file; **M** 1–2 days, multi-file or schema/policy change; **L** > 2 days or cross-repository.

| # | AD | File:line | What the code does | What the spine requires | Size |
| --- | --- | --- | --- | --- | --- |
| NC-1 | AD-9 | `eng/release_evidence.py:2472` (param), `:2724-2743` (rebind block), `:3834` (`bind_live_fallback_digest=True` in `classify-release`) | Overwrites `fallback["approved_against_fingerprints_sha256"]` with the live `fallback_v2_digest(...)` before `fallback_complete` compares. | Classification recomputes the live digest and **compares** it with the immutable Release Owner-recorded value; fails closed on inequality or absence; no step may write/default/rebind/refresh it. Delete the rebind block and the parameter; keep `fallback-digest` as the operator tool that prints the value to record. | M |
| NC-2 | AD-9, AD-12 | `.github/workflows/release.yml:287-290`; `eng/release_prepublish.py:485-535` (`classify` argv) | `RELEASE_ATTESTATION_FALLBACK_FINGERPRINTS_SHA256` is never forwarded to `prepare` → `classify-release` reads `""` from env (`:4093`). | Forward `vars.RELEASE_ATTESTATION_FALLBACK_FINGERPRINTS_SHA256` as an env input to the prepare step; `classify` must receive it (or the empty value must block). Record it in evidence as a `RELEASE_ATTESTATION_*` shape-validated input. | S |
| NC-3 | AD-9 | `tests/eng/test_release_evidence_v2.py:752-806` (+ new) | Drift fixture only exercises `fallback_complete` with a stored digest. | Add a fixture through `classify_release_payload` / CLI `classify-release` (the production path) that records a valid digest, then mutates graph digest, policy sha256, and `workflow_provenance.definition_digest` in turn and asserts `publish_authorized=false` with the "drifted release definition" diagnostic; plus an **absent**-value case that blocks. | S |
| NC-4 | AD-12, AD-13 | `.github/workflows/ci.yml:167-170` (comment), `:189-209` (`status -eq 2` → `created=false`, exit 0) | Treats `create-ci` exit 2 (no matching active-policy `ci` row) as success; push run stays green without an AD-13 artifact. | A closure absent from the active registry **fails the push gate**; never a soft deferral (two-phase pre-authorization makes exit 2 unnecessary). Make exit 2 `::error` + `exit 1`; drop the AD-16 comment; close DW-1795. | S |
| NC-5 | AD-12, AD-15 | `.github/workflows/release.yml:557-564`; `eng/dependency_handoff.py:741-749` | `create-release` exit 2 (unauthorized `release` evaluator) on an unpublished attempt exits 0 and leaves only `release-verification-handoff.deferred.json`. | The `.deferred.json` sentinel is reserved for an attempt that **never obtained an authenticated CI handoff** (`:470-481`). An unauthorized release evaluator is a gate failure: return 1, fail the job, still upload the artifact under `if: always()` with a null-filled handoff or an explicit failure record. | S |
| NC-6 | AD-15 | `.github/workflows/release-evidence.yml:171-176` | `verify-release --handoff … --ci-handoff …` with no `--root`, no policy reload, no evaluator authorization, no expected run coordinates. | Verifier must (i) reload the exact base/before policy blob at `dependency_policy.commit` (`--root .` after exact checkout, or an acquired store), (ii) `draft-evaluator --stage post_release --caller-commit <candidate> --caller-workflow .github/workflows/release-evidence.yml --policy-commit <dependency_policy.commit>` and `require_evaluator_authorized(policy,"post_release",…)` before ledger acceptance, (iii) require `release_run.{run_id,run_attempt,conclusion}` == upstream API values (add `--expected-run-id/--expected-run-attempt/--expected-conclusion` to `verify-release`). Runs for **every** attempt, including deferred/non-governed. | M |
| NC-7 | AD-12, AD-15 | `eng/dependency-graph-policy.json:1725-2257` (`evaluator_authorizations.post_release`, 5 rows) | Every row names `reusable = hexalith.builds/.github/workflows/domain-release.yml@<commit>` (unreachable from the caller) and a caller blob that does not equal HEAD `185593f0…`. | Exactly the rows needed: caller `{repository, workflow_path: .github/workflows/release-evidence.yml, blob_sha256: <current blob>}`, `reusable: null`, `actions` = static closure of `release-evidence.yml` at HEAD (checkout, setup-dotnet, upload-artifact, `.hexalith/builds-execution/Github/initialize-build` resolved at the literal checkout ref `4eb33928…`), `closure_digest` recomputed. Prune the five stale rows. Land under the two-phase rule (policy commit first, then any workflow edit). Depends on NC-8. | S |
| NC-8 | AD-12, AD-13 | `eng/dependency_handoff.py:53-66` (`_source`, `validate_evaluator`), `:547-620` (`draft_evaluator_for_stage`), `:690-694` (`authorized_draft`); `eng/workflow_source_closure.py:806-870` (`collect_workflow_source_closure`), `:907-951` (`_validate_closure`), `:979-1058` (`_validate_authorization`, `_authorization_sort_key`) | `reusable` must be a closed object; `require_reusable_edge=False` visits the reusable as an extra root; post_release draft uses a `domain-release.yml` zero-commit placeholder. | `reusable` is `{repository, workflow_path, commit, blob_sha256}` **or JSON `null`** when the caller's static closure reaches no reusable workflow; closures contain only caller-reachable sources; digest/sort/projection tolerate `null`; `draft-evaluator --stage post_release` drafts `reusable: null` and derives `authorized_draft` from the row match, not from `reusable.commit`. Update `test_dependency_handoff.py` / `test_workflow_source_closure.py` fixtures (null-reusable authorization, unreachable-root rejection). | M |
| NC-9 | AD-15 | `.github/workflows/release-evidence.yml:120-182` | Downloads only `release-verification-handoff-<run>-<attempt>` and hashes the CI handoff copy inside it. | Independently authenticate and download `dependency-release-handoff-<ci_handoff.run_id>-<ci_handoff.run_attempt>` from the CI run via read-only Actions APIs (exactly one non-expired match, zip with exactly one member, run metadata `push`/`main`/`ci.yml`/`success`), require its raw SHA-256 == `ci_handoff.evidence_sha256` == SHA-256 of the embedded copy; pass **that** file to `verify-release --ci-handoff`. | M |
| NC-10 | AD-15, Consistency | `.github/workflows/release-evidence.yml:213-218,590-602` | Ledger record ignores `steps.ad15.outputs.verified` and post_release authorization; two ad-hoc `jq` shapes; no schema validation or consumer hash. | `disposition == compliant-candidate` only if AD-15 handoff verified (NC-6/NC-9) **and** manifest/bytes/signatures verified and no incident; a governed attempt with a missing/deferred AD-15 pair is `non-compliant` (incident state). Emit one closed `frontcomposer.release-ledger-record.v2` shape (nullable fields for non-governed dispositions) from `eng/release_disposition.py` and record its raw SHA-256 in the verification artifact. | S |
| NC-11 | AD-13 | `eng/release_evidence.py:3241-3266` (source-proof authority branch), `:4003` (`--source-proof` arg), `:1542-1556` (verify accepts `SOURCE_PROOF_SCHEMA` for a v3 manifest); `eng/release_prepublish.py:272,400-409,538-548` | `hexalith.dependency-release-source.v1` can be the candidate/graph/policy/provenance authority when no CI handoff is given; the staged asset is misleadingly named `dependency-release-source.json` even when it holds the v1 **handoff**. | The source proof is a CI diagnostic only; never a candidate authority for manifest preparation. Remove `--source-proof`/`DEPENDENCY_RELEASE_SOURCE_PROOF` from prepare/verify (keep `create-source`/`verify-source` in `dependency_handoff.py` and `ci.yml`); require `--ci-handoff` + `--release-evaluator` unconditionally; verify branch accepts `CI_HANDOFF_SCHEMA` only. Optional: rename the staged asset to `dependency-release-handoff.json` (touches `release-evidence.yml:405` required-asset list and Governance pins). | S/M |
| NC-12 | AD-18 (+ AD-17 location) | `.github/workflows/release.yml:217-290` (prepare step env), `eng/release_evidence.py` prepare-manifest (`:3225+`), `eng/dependency_handoff.py:362-401` (`create_release_handoff`) | No actor, triggering actor, or environment-approval evidence anywhere. | Record `github.actor`, `github.triggering_actor`, and the `production` environment approval (approver login + timestamp from the run's approvals/deployment-review API) in sealed release evidence. **Architect must name the member** — AD-17's `workflow_provenance` and the AD-15 handoff are closed sets; recommended: a new sealed top-level manifest member `publication_actor {dispatching_actor, triggering_actor, environment, approved_by, approved_at}` added to AD-17 in the same atomic change as fixtures/Governance pins. | M |
| NC-13 | AD-18 | `tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs` (near `:2146-2165`, `:3502-3545`) | Static assertions cover `workflow_dispatch:`, `environment: production`, `NUGET_API_KEY`, artifact names only. | Assert from the exact workflow blobs: no `pull_request_target` in any `.github/workflows/*.yml`; workflow-level `permissions:` is `contents: read` in `ci.yml`/`release.yml`/`release-evidence.yml`; no job in those three declares `actions: write`; `dependency-governance` and `verify-source` reference no `secrets.*`; write scopes appear only on the `release` job; every `upload-artifact` step has `if-no-files-found: error` and no `continue-on-error`. | S |
| NC-14 | AD-5 | `tests/eng/test_dependency_graph.py` (new test) | Determinism asserted only by re-hashing with the same helper. | Golden literal fixture: a small synthetic graph (≥1 Builds edge with `catalog_contract_version: null`, ≥1 depth-2 edge, one non-ASCII/``/`/`-bearing path to pin the escape set) with the expected canonical bytes and `graph_digest` as 64-hex **string literals**. | S |
| NC-15 | AD-12 | `CiGovernanceTests.cs:1107-1127` or `tests/eng/test_release_evidence_v2.py:152` | Lists are not pinned member-by-member. | Pin that `eng/dependency-graph-policy.json`, `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `eng/dependency_graph.py`, `eng/dependency_handoff.py`, `eng/workflow_source_closure.py`, `eng/release_contract.py`, `eng/release_prepublish.py` are members of **both** `RELEASE_DEFINITION_FILES` and `FALLBACK_INVALIDATION_FILES`, and that `fallback_invalidation_fingerprints()` keys == that list ∪ `{helper_version}`. | S |
| NC-16 | AD-16, AD-12 | `eng/dependency-graph-policy.json` rows whose `reusable.commit == 0a3508b3e568…` (ci: `1000f6af…`, `7625a6ad…`; release: `43cfde35…`, `dd9a58eb…`; post_release: `8664cf12…`) | Pre-authorized closures for a Builds commit that was never pinned and is outside the memlog lineage (a8a50859 → 99d5a46c → 3f0e3595 → 4eb33928). | Either prune the rows (policy change, Governance fixture) or record `0a3508b3…` in the AD-16 lineage note as an accepted-but-unused pre-authorization. Decision: Architect + Release Owner. | S |
| NC-17 | AD-12 | `.github/workflows/ci.yml:181-184` | `policy_commit="$CANDIDATE"` when `EVENT_BASE` is all-zero — unreachable because the step is gated on `steps.dependency-diff.outputs.exit-code == '0'` and the zero-before `diff` exits 2. | Remove the dead branch (the candidate policy must never select the evaluator); keep the zero-before diagnostic path in `dependency_graph.py:1928-1954` as is. | S |

Ordering hint: NC-8 → NC-7 → NC-6/NC-9/NC-10 (post-release chain); NC-1 → NC-2 → NC-3 (fallback digest); NC-4/NC-5 independent; NC-12 needs the Architect's member decision before code.

## D-1..D-18 closure table

| Prior | Status | Basis at HEAD / in the rewritten spine |
| --- | --- | --- |
| D-1 | **Closed (spine)** | AD-13 now states operator `workflow_dispatch` on `refs/heads/main`, API re-read of `main`, `select-ci` exactly-one successful push-CI run — matches `release.yml:6-7,41-75` and `release_contract.py:231-272`. |
| D-2 | **Closed (spine)** | AD-16 rewritten as the timeless lineage invariant rooted at `a8a50859…`; memlog constraint records the 08-06 tags and 08-08 acceptance. Policy rows for a8a50859/99d5a46c/3f0e3595/4eb33928 exist. Residual: DU-13 stale `0a3508b3` rows. |
| D-3 | **Closed (spine)** | AD-17 ratifies v3 exactly as produced (`release_evidence.py:851-886`) and verified (`:911-958`); AD-14 retitled as the one-way migration rule. |
| D-4 | **Closed (deferred)** | Deferred entry "Runtime workflow identity and closure recomputation"; v1 trust = caller blob hash + literal reusable commit, which is what `draft_evaluator_for_stage` implements (`dependency_handoff.py:547-620`). |
| D-5 | **Carried (a)** | DU-2/DU-7 → NC-6, NC-7, NC-8. Now sharper: spine requires `reusable: null` for post_release, which the engines cannot express. |
| D-6 | **Carried (a)** | DU-3 → NC-4 (ci.yml) plus the newly identified release-stage soft-defer NC-5. |
| D-7 | **Closed (spine)** | Bootstrap reduced to one historical sentence; zero-before rule stated as implemented. Residual dead branch NC-17 (Low). |
| D-8 | **Closed (spine)** | Run-suffixed names and "normative content = handoff JSON + byte-identical CI handoff copy; other files non-normative" match `release.yml:552,572-577`, `ci.yml:215`. |
| D-9 | **Closed (spine)** | `.deferred.json` sentinel ratified for the no-authenticated-CI-handoff case (`release.yml:470-481`). Residual: code also uses it for an unauthorized release evaluator (NC-5). |
| D-10 | **Carried (a)** | DU-5 → NC-11; spine now explicitly says the source proof is diagnostic-only and never a candidate authority. |
| D-11 | **Half closed** | Object-format probe dropped by the spine ("40-hex validation enforces it") — code conforms. Golden literal fixture still absent → DU-8 / NC-14. |
| D-12 | **Closed (spine)** | Stage-keyed `evaluator_authorizations` ratified; `require_policy_authorization` enforces exact stage set, sorting, uniqueness (`workflow_source_closure.py:1060-1109`). |
| D-13 | **Mostly closed** | Seed now lists `dependency_handoff.py`, `release_prepublish.py`, `release_contract.py`, `release_disposition.py`, four test files, fixtures dir; Capability Map row names `dependency_handoff.py` + `release_contract.py`. Residual omissions DU-12. |
| D-14 | **Closed (spine)** | Named-identifier table + Consistency "Evidence envelopes" row cover all schemas/env vars/artifact names/recovery record from D-14. |
| D-15 | **Closed (spine)** | "current @main" prose gone; AD-13 states the literal 40-hex rule; live pins `4eb33928…` in `ci.yml:25`, `release.yml:17,233,321,329`. |
| D-16 | **Closed (spine)** | AD-3 lists `ssh://` form and trailing-slash strip; regexes at `dependency_graph.py:270-293` match the ordered grammar (scheme/host case-sensitive, no port, one `.git`, lowercase owner/repo only). |
| D-17 | **Closed** | DW-1803 done 2026-09-05; AD-6 now carries only the coverage invariant and "profile identifiers and rows are policy seed", so sibling presence-only semantics live in policy, not spine. |
| D-18 | **Closed (spine)** | Stack table declares versions "not trust inputs"; no CI recording is claimed. |

## Per-AD scorecard

| AD / section | Status | One-line |
| --- | --- | --- |
| AD-1 | conforms | Depth-1/2 boundary, depth-2 owner must equal a depth-1 target (`dependency_graph.py:517-527`); census is evidence. |
| AD-2 | conforms | Plumbing reads by exact commit; `submodules: false` on every GOV-1 checkout; `initialize-build` initializes root-declared only (DU-16 wording). |
| AD-3 | conforms | Ordered grammar implemented exactly; identity is the trust key; `local_path` is acquisition data (`:296-300`). |
| AD-4 | conforms | Six/eight members, identity `(owner_repository, owner_commit, path)`, ordinal sort tuple (`:503-535`). |
| AD-5 | partial | Envelope/digest/dup-rejection/type rules conform; escape set matches `json.dumps(ensure_ascii=True)`; **no golden literal fixture** (DU-8). |
| AD-6 | conforms | Python engine is the single semantic authority; `builds_identity` must be a trusted identity (`:1602`); profiles are policy seed. |
| AD-7 | conforms | All eleven ceilings present in policy `resource_limits` and enforced (`workflow_source_closure.py:22-27,335-376`; `dependency_graph.py:204-225,735-763`). |
| AD-8 | conforms | PR/push revision model, merge-base equality, logical key, unchanged relation (`_edge_change_projection` drops depth-1 `owner_commit` only, `:582-586`), depth-1-first classification. |
| AD-9 | **violates** | Formula/definition/helper_version conform (`:2255-2289`); **live rebinding + missing variable + no production drift fixture** (DU-1, DU-10). |
| AD-10 | conforms (wording) | Isolated bare stores from canonical identities in CI and `verify-source`; post-release/prepare use the root-declared checkout (DU-16). |
| AD-11 | conforms | No runtime/API scope in GOV-1 files. |
| AD-12 | **violates** | Policy authority, activation, bindings, stage-keyed registry conform; **exit-2 soft-defer** (DU-3), **null `reusable` unrepresentable / unreachable root added** (DU-7), lists unpinned (DU-9), stale `0a3508b3` rows (DU-13), dead branch (DU-14). Candidate-policy schema rejection on PRs is provided by the Governance lane (`validate` on the working tree via `quality.yml`), not by `diff`. |
| AD-13 | partial | Dispatch/authentication/handoff/download/verify-ci --live/pins/closure rules conform (`release.yml:41-147`, `release_contract.py:231-272`, `workflow_source_closure.py:424-500`); **`--source-proof` authority remains** (DU-5). |
| AD-14 | conforms | v2 verify-only, legacy audit mode, projection shape, seal rule (`release_evidence.py:1449-1454,1725`). |
| AD-15 | **violates** | Emission shape, `if: always()`, null discipline, sorted assets, candidate from handoff not `head_sha` conform (`dependency_handoff.py:275-401`, `release_disposition.py:105-112`); **no post_release authorization / policy reload / coordinate equality / independent CI download / ledger binding** (DU-2, DU-6), release-stage soft-defer (DU-3), ledger shape (DU-17). |
| AD-16 | conforms | Lineage rows a8a50859 → 99d5a46c → 3f0e3595 → 4eb33928 present; `validate_builds_identity` requires `uses:@` == `builds-execution-sha` == approved (`release_contract.py:320-331`); catalog gitlink independent (test `:150`). Stale pre-authorization DU-13. |
| AD-17 | conforms | Producer and verifier implement exactly the v3 member sets, `head_sha` = candidate, `reusable.commit == builds_execution_sha`, `definition_digest = canonical({ci,release})` (`release_evidence.py:851-958`); Governance pins v3 (`CiGovernanceTests.cs:3570`). |
| AD-18 | partial | Permission blocks, `production` environment, single upload step per name, `if-no-files-found: error`, zero/multiple-match forgery rule conform; **actor/environment approval not recorded; no static assertions for `pull_request_target`/`actions: write`** (DU-4); extra `issues`/`pull-requests: write` (DU-15). |
| Structural Seed | partial (c) | Omits `release_compatibility.py`, three test files; fixtures dir has no policy fixtures; ledger record not produced by `release_disposition.py` (DU-12). |
| Diagram 1 (paradigm) | conforms | Informational; matches the depth model. |
| Diagram 2 (modules) | drifted (c) | Three false edges, three missing (DU-11). |
| Diagram 3 (flow) | matches spine, not code | "POST re-authenticates CI artifact → CI" is required by AD-15 but unimplemented (DU-6/NC-9). |
| Named-identifier table | conforms | `BUILDS_EXECUTION_SHA`/`HEXALITH_BUILDS_EXECUTION_SHA`/`builds-execution-sha` all `4eb33928…` (`release.yml:17,286,329`); `DEPENDENCY_RELEASE_HANDOFF`/`RELEASE_EVALUATOR` (`:284-285`); `.hexalith/builds-execution/` + `Github/initialize-build` (`:230-237`); `RELEASE_ATTESTATION_*` (`:287-290`); `.github/release-evidence-recovery.json` (`release-evidence.yml:14,47-67`). |
| Capability Map | conforms | Rows resolve to existing units; "Evaluator trust/closure → `workflow_source_closure.py`" is accurate only as the policy-authoring/test tool (no production execution — consistent with the Deferred entry). |

## Confirmed conformances (no finding, verified this pass)

- AD-13 artifact authentication: `total_count == len(artifacts)`, exactly one non-expired name match, zip `namelist() == ["dependency-release-handoff.json"]`, `run.candidate == revisions.candidate == dispatch SHA`, `run_id/run_attempt` equality (`release.yml:78-144`); `select-ci` rejects malformed/paginated/duplicate responses and requires `push`/`main`/`completed`/`success`/`ci.yml` (`release_contract.py:231-272`).
- AD-13 static closure: `_BUILDS_EXECUTION_CHECKOUT_RE` requires exactly one literal `ref:` + `path: .hexalith/builds-execution` in the enclosing workflow blob; Docker, `${{ }}`, local `@ref`, anchors/aliases, cycles, and every limit fail closed (`workflow_source_closure.py:42-48,424-500,553-600,702-735`); fixtures at `test_workflow_source_closure.py:186-521`.
- AD-9 fallback digest material and `definition` map: `FALLBACK_INVALIDATION_FILES` + one `helper_version` key = `sha256(__version__)`; absent files hash the literal `missing` (64-hex shape preserved) (`release_evidence.py:2255-2289`).
- AD-12 activation: PR policy from `event_base`, push from non-zero `before`, release from the handoff's `dependency_policy.commit` (`ci.yml:64-83,181`; `release.yml:145-146,209-211`); handoff requires `dependency_policy.commit == revisions.base` (`dependency_handoff.py:162-163`).
- AD-15 emission: exactly one `release-verification-handoff-<run>-<attempt>` upload under `if: always()`, published attempts hard-fail on any deferral (`release.yml:475-479,557-560`; `test_published_create_release_hard_fails_instead_of_deferred`).
- AD-18 token hygiene: `persist-credentials: false` on every checkout; `verify-source`/`plan-release`/`dependency-governance` reference no `secrets.*`; only `release` receives `NUGET_API_KEY`.
