diff --git a/.github/workflows/release.yml b/.github/workflows/release.yml
index b224ae7..3574795 100644
--- a/.github/workflows/release.yml
+++ b/.github/workflows/release.yml
@@ -134,19 +134,45 @@ jobs:
         # post-mortem becomes impossible. Always seed the directory with bounded
         # run metadata so the forensic artifact is never empty and the `error`
         # discipline catches genuine missing-evidence regressions.
+        #
+        # CR-12-4-P234 (round-10, BH-F15/EC-3): the Seed step itself needs
+        # `if: always()`. Its prior placement after "Run All Tests" meant a
+        # `dotnet test` failure (the most common early-failure scenario) would
+        # skip Seed, leaving `release-evidence/` empty and triggering the upload
+        # step's `if-no-files-found: error` — defeating the "artifact never
+        # empty" claim. `if: always()` makes the seed bedrock unconditional.
+        #
+        # CR-12-4-P241 (round-10, BH-F5/EC-4): switched from a shell heredoc with
+        # unquoted `<<EOF` (which expanded `${...}` in arbitrary refs and produced
+        # invalid JSON for refs containing `"`) to a Python one-liner reading
+        # GHA values from env vars. JSON-safe escaping is handled by `json.dump`;
+        # special characters in refs/run-ids no longer corrupt the metadata file.
+        if: always()
+        env:
+          RUN_META_EVENT_NAME: ${{ github.event_name }}
+          RUN_META_REF: ${{ github.ref }}
+          RUN_META_RUN_ID: ${{ github.run_id }}
+          RUN_META_RUN_ATTEMPT: ${{ github.run_attempt }}
+          RUN_META_SHA: ${{ github.sha }}
         run: |
           mkdir -p release-evidence
-          cat > release-evidence/run-metadata.json <<EOF
-          {
-            "decision_contract": "frontcomposer.release-run-metadata.v1",
-            "event_name": "${{ github.event_name }}",
-            "ref": "${{ github.ref }}",
-            "run_id": "${{ github.run_id }}",
-            "run_attempt": "${{ github.run_attempt }}",
-            "sha": "${{ github.sha }}",
-            "started_at": "${RELEASE_STARTED_AT:-unknown}"
+          python3 - <<'PYEOF'
+          import json
+          import os
+
+          metadata = {
+              "decision_contract": "frontcomposer.release-run-metadata.v1",
+              "event_name": os.environ.get("RUN_META_EVENT_NAME", ""),
+              "ref": os.environ.get("RUN_META_REF", ""),
+              "run_id": os.environ.get("RUN_META_RUN_ID", ""),
+              "run_attempt": os.environ.get("RUN_META_RUN_ATTEMPT", ""),
+              "sha": os.environ.get("RUN_META_SHA", ""),
+              "started_at": os.environ.get("RELEASE_STARTED_AT", "unknown"),
           }
-          EOF
+          with open("release-evidence/run-metadata.json", "w", encoding="utf-8", newline="\n") as fh:
+              json.dump(metadata, fh, sort_keys=True, separators=(",", ":"))
+              fh.write("\n")
+          PYEOF
 
       - name: Record workflow-dispatch release inputs
         if: ${{ github.event_name == 'workflow_dispatch' }}
@@ -536,11 +562,20 @@ jobs:
         # invokes classify-release directly against the sealed manifest. No
         # `continue-on-error` is needed because publishCmd cannot reach the
         # exit-1-after-classifier path.
+        #
+        # CR-12-4-P248 (round-10, D22/BH-F6): dry-run prepareCmd intentionally signs
+        # with the production NuGet certificate so the dry-run exercises the full
+        # prepare-path fidelity (per D2: "Build, sign, SBOM, and verification
+        # exercise the same code as live publish"). `NUGET_API_KEY` is NOT passed
+        # because the push (`dotnet nuget push`) lives in publishCmd, which
+        # semantic-release --dry-run never reaches — passing the API key here
+        # widens blast radius without enabling any legitimate dry-run behavior.
+        # A dry-run that somehow reaches push code is a workflow misconfiguration
+        # we want to fail-closed via missing-auth, not silently succeed.
         if: ${{ env.RELEASE_DRY_RUN == 'true' }}
         run: npx semantic-release --dry-run
         env:
           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
-          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
           NUGET_SIGNING_CERTIFICATE_PATH: ${{ secrets.NUGET_SIGNING_CERTIFICATE_PATH }}
           NUGET_SIGNING_CERTIFICATE_PASSWORD: ${{ secrets.NUGET_SIGNING_CERTIFICATE_PASSWORD }}
           NUGET_SIGNING_TIMESTAMPER: ${{ vars.NUGET_SIGNING_TIMESTAMPER || 'http://timestamp.digicert.com' }}
@@ -619,12 +654,37 @@ jobs:
             # The step still exits 0 because there is no useful budget data to
             # compute, but the `::warning::` annotation is visible in the workflow run
             # summary so the regression is observable.
+            #
+            # CR-12-4-P239 (round-10, AA-003/AA-004): AC19 requires release-budget
+            # state to be either RECORDED or EXPLICITLY MARKED UNAVAILABLE; D17
+            # mandates a single typed classification contract. The job-summary
+            # warning alone is prose, not machine-readable evidence — a release
+            # owner reading only the typed contract cannot tell the budget step
+            # was skipped. Emit a typed `release-budget-skipped.json` marker
+            # under `release-evidence/` so the skip flows into the artifact
+            # contract and the CiGovernanceTests can assert on its shape.
+            mkdir -p release-evidence
+            python3 - <<'PYEOF'
+            import datetime as dt
+            import json
+            marker = {
+                "decision_contract": "frontcomposer.release-budget-skipped.v1",
+                "classification": "budget-unavailable",
+                "reason": "RELEASE_STARTED_AT empty; release-budget monitor cannot compute elapsed minutes",
+                "skipped_at": dt.datetime.now(dt.timezone.utc).isoformat(),
+            }
+            with open("release-evidence/release-budget-skipped.json", "w", encoding="utf-8", newline="\n") as fh:
+                json.dump(marker, fh, sort_keys=True, separators=(",", ":"))
+                fh.write("\n")
+            PYEOF
             {
               echo '## Release budget monitor skipped'
               echo
               echo '`RELEASE_STARTED_AT` was not set; the run failed before timing started or the recorder step was bypassed. Investigate before promoting this run.'
+              echo
+              echo 'Typed evidence marker: `release-evidence/release-budget-skipped.json` (decision_contract `frontcomposer.release-budget-skipped.v1`).'
             } >> "$GITHUB_STEP_SUMMARY"
-            echo '::warning::Release budget monitor skipped — RELEASE_STARTED_AT was not set.'
+            echo '::warning::Release budget monitor skipped — RELEASE_STARTED_AT was not set; see release-budget-skipped.json.'
             exit 0
           fi
           python3 eng/release_evidence.py release-budget --evidence release-evidence/release-budget-history.json --append-current --started-at "$RELEASE_STARTED_AT" --manifest release-evidence/sealed-manifest.json --output release-evidence/release-budget-summary.json --event-name "${{ github.event_name }}" --ref "${{ github.ref }}" --ref-protected "${{ github.ref_protected }}" --from-fork "${{ env.RELEASE_FROM_FORK }}" --tag "${{ github.ref_name }}" --run-id "${{ github.run_id }}" --publish-status "${{ job.status }}"
diff --git a/.releaserc.json b/.releaserc.json
index d155787..4b4cf8e 100644
--- a/.releaserc.json
+++ b/.releaserc.json
@@ -9,7 +9,7 @@
       "@semantic-release/exec",
       {
         "prepareCmd": "mkdir -p ./release-evidence ./release-evidence/attestations && dotnet build --configuration Release -p:Version=${nextRelease.version} && dotnet pack --no-build --configuration Release --include-symbols --include-source --output ./nupkgs -p:Version=${nextRelease.version} && dotnet CycloneDX Hexalith.FrontComposer.slnx -o ./release-evidence/sbom --json --disable-github-licenses && python eng/release_evidence.py inventory --root . --expected eng/release-package-inventory.json --output ./release-evidence/package-inventory.json && dotnet nuget sign ./nupkgs/*.nupkg --certificate-path \"$NUGET_SIGNING_CERTIFICATE_PATH\" --certificate-password \"$NUGET_SIGNING_CERTIFICATE_PASSWORD\" --timestamper \"$NUGET_SIGNING_TIMESTAMPER\" --output ./nupkgs-signed && dotnet nuget verify ./nupkgs-signed/*.nupkg --all --verbosity normal > ./release-evidence/signing-verification.txt && if [ \"$RELEASE_ATTESTATION_STATUS\" = \"attested\" ]; then if gh attestation verify ./nupkgs-signed/*.nupkg --repo \"$GITHUB_REPOSITORY\" --signer-workflow \"Hexalith/Hexalith.FrontComposer/.github/workflows/release.yml\" --format json > ./release-evidence/attestations/attestation-bundle.json.tmp; then mv ./release-evidence/attestations/attestation-bundle.json.tmp ./release-evidence/attestations/attestation-bundle.json; else rm -f ./release-evidence/attestations/attestation-bundle.json.tmp; exit 1; fi; fi && python eng/release_evidence.py release-budget --evidence ./release-evidence/release-budget-history.json --append-current --started-at \"$RELEASE_STARTED_AT\" --tag v${nextRelease.version} --run-id \"$GITHUB_RUN_ID\" --publish-status pre-publish --output ./release-evidence/release-budget-prepublish.json && python eng/release_evidence.py checksums --root . --pattern 'nupkgs-signed/*.nupkg' --pattern 'nupkgs/*.snupkg' --pattern 'release-evidence/sbom/**/*.json' --pattern 'release-evidence/signing-verification.txt' --pattern 'release-evidence/attestation-unavailable.md' --pattern 'release-evidence/attestations/*.json' --pattern 'release-evidence/test-results.json' --pattern 'release-evidence/release-budget-prepublish.json' --pattern 'release-evidence/concurrency-guard.json' --output ./release-evidence/checksums.json && python eng/release_evidence.py prepare-manifest --root . --inventory ./release-evidence/package-inventory.json --checksums ./release-evidence/checksums.json --signing-verification ./release-evidence/signing-verification.txt --attestation-bundle ./release-evidence/attestations/attestation-bundle.json --version ${nextRelease.version} --tag v${nextRelease.version} --benchmark-summary-hash \"$(sha256sum ./release-evidence/release-budget-prepublish.json | cut -d' ' -f1)\" --attestation-status \"$RELEASE_ATTESTATION_STATUS\" --output ./release-evidence/pre-publish-manifest.json --diagnostics-output ./release-evidence/manifest-diagnostics.json && python eng/release_evidence.py seal-manifest --manifest ./release-evidence/pre-publish-manifest.json --output ./release-evidence/sealed-manifest.json && python eng/release_evidence.py partial-publish-incident --manifest ./release-evidence/sealed-manifest.json --output ./release-evidence/partial-publish-placeholder.json --phase none --classification none --tag v${nextRelease.version} && python eng/release_evidence.py partial-publish-incident --manifest ./release-evidence/sealed-manifest.json --output ./release-evidence/partial-publish-incident.json --phase none --classification none --tag v${nextRelease.version} && printf '{\"status\":\"no-prior-release\",\"tag\":\"v${nextRelease.version}\"}\\n' > ./release-evidence/prior-release.json && python eng/release_evidence.py verify-manifest --root . --manifest ./release-evidence/sealed-manifest.json --output ./release-evidence/release-verification.json",
-        "publishCmd": "python eng/release_evidence.py verify-manifest --root . --manifest ./release-evidence/sealed-manifest.json && release_dry_run_lower=$(printf '%s' \"${RELEASE_DRY_RUN:-true}\" | tr -d '[:space:]' | tr 'A-Z' 'a-z') && { case \"$release_dry_run_lower\" in false|0|no) is_dry_run=false ;; *) is_dry_run=true ;; esac; if [ \"$is_dry_run\" = \"true\" ]; then python eng/release_evidence.py classify-release --root . --evidence-root ./release-evidence --manifest ./release-evidence/sealed-manifest.json --test-results ./release-evidence/test-results.json --output ./release-evidence/release-readiness.json --require-publishable --dry-run-clean-exit --semantic-release-state matches --concurrent-same-version \"$RELEASE_CONCURRENT_SAME_VERSION\" --event-name \"$GITHUB_EVENT_NAME\" --ref \"$GITHUB_REF\" --ref-protected \"$GITHUB_REF_PROTECTED\" --from-fork \"$RELEASE_FROM_FORK\" --dry-run true --run-attempt \"${GITHUB_RUN_ATTEMPT:-1}\" --owner-approved \"$RELEASE_OWNER_APPROVED\" --approver \"$RELEASE_APPROVER\" --approval-mechanism \"$RELEASE_APPROVAL_MECHANISM\" --attestation-status \"$RELEASE_ATTESTATION_STATUS\" --fallback-approved-at \"$RELEASE_ATTESTATION_FALLBACK_APPROVED_AT\" --fallback-approver \"$RELEASE_ATTESTATION_FALLBACK_APPROVER\" --fallback-expires-at \"$RELEASE_ATTESTATION_FALLBACK_EXPIRES_AT\" --fallback-approved-against-fingerprints-sha256 \"$RELEASE_ATTESTATION_FALLBACK_FINGERPRINTS_SHA256\" --concurrency-guard ./release-evidence/concurrency-guard.json; classifier_rc=$?; if [ $classifier_rc -ne 0 ]; then exit $classifier_rc; fi; echo 'Dry-run classification complete; halting before publish side effects. (CR-12-4-P214 round-8: defense-in-depth — primary dry-run path uses `npx semantic-release --dry-run` and never invokes this branch; this fallback exit 1 prevents @semantic-release/github + @semantic-release/git from firing if publishCmd is reached during a dry-run via a future semantic-release config change.)' >&2; exit 1; fi; } && { prior_stderr=$(mktemp 2>/dev/null || echo ./release-evidence/.prior-tag-stderr.$$); same_version_stderr=$(mktemp 2>/dev/null || echo ./release-evidence/.same-version-stderr.$$); trap 'rm -f \"$prior_stderr\" \"$same_version_stderr\"' EXIT; prior_release_response=$(gh api \"repos/${GITHUB_REPOSITORY}/releases/tags/v${nextRelease.version}\" 2>\"$prior_stderr\"); gh_rc=$?; if [ $gh_rc -eq 0 ]; then if printf '%s' \"$prior_release_response\" | python3 -c 'import json,sys; json.dump(json.load(sys.stdin), sys.stdout)' > ./release-evidence/prior-release.json.tmp 2>/dev/null; then mv ./release-evidence/prior-release.json.tmp ./release-evidence/prior-release.json; RELEASE_CONCURRENT_SAME_VERSION=true; else rm -f ./release-evidence/prior-release.json.tmp; printf '{\"status\":\"probe-malformed-response\",\"tag\":\"v${nextRelease.version}\"}\\n' > ./release-evidence/prior-release.json; echo 'CR-12-4-P212: prior-tag probe returned non-JSON body; failing closed' >&2; RELEASE_CONCURRENT_SAME_VERSION=true; fi; elif grep -q 'HTTP 404\\|Not Found' \"$prior_stderr\" 2>/dev/null; then :; else echo \"Prior-tag probe failed transient (rc=$gh_rc): $(cat \"$prior_stderr\" 2>/dev/null)\" >&2; exit 1; fi; set +e; same_version_runs=$(gh api \"repos/${GITHUB_REPOSITORY}/actions/runs?status=in_progress&per_page=100\" 2>\"$same_version_stderr\" | jq --arg run_id \"$GITHUB_RUN_ID\" \"[.workflow_runs[]? | select(.path | endswith(\\\".github/workflows/release.yml\\\")) | select(.display_title | test(\\\"(^|[^0-9.])v${nextRelease.version}([^0-9.]|$)\\\")) | select(.id != (\\$run_id | tonumber))] | length\"); same_version_rc=$?; set -e; if [ $same_version_rc -ne 0 ]; then echo \"Same-version run probe failed transient (rc=$same_version_rc): $(cat \"$same_version_stderr\" 2>/dev/null)\" >&2; RELEASE_CONCURRENT_SAME_VERSION=true; elif ! printf '%s' \"${same_version_runs:-}\" | grep -Eq '^[0-9]+$'; then echo \"Same-version run probe returned non-numeric result; failing closed.\" >&2; RELEASE_CONCURRENT_SAME_VERSION=true; elif [ \"$same_version_runs\" -gt 0 ]; then echo \"Detected $same_version_runs other in-flight release run(s) targeting v${nextRelease.version}; failing closed.\" >&2; RELEASE_CONCURRENT_SAME_VERSION=true; fi; } && python eng/release_evidence.py classify-release --root . --evidence-root ./release-evidence --manifest ./release-evidence/sealed-manifest.json --test-results ./release-evidence/test-results.json --output ./release-evidence/release-readiness.json --require-publishable --semantic-release-state matches --concurrent-same-version \"$RELEASE_CONCURRENT_SAME_VERSION\" --event-name \"$GITHUB_EVENT_NAME\" --ref \"$GITHUB_REF\" --ref-protected \"$GITHUB_REF_PROTECTED\" --from-fork \"$RELEASE_FROM_FORK\" --dry-run \"$RELEASE_DRY_RUN\" --run-attempt \"${GITHUB_RUN_ATTEMPT:-1}\" --owner-approved \"$RELEASE_OWNER_APPROVED\" --approver \"$RELEASE_APPROVER\" --approval-mechanism \"$RELEASE_APPROVAL_MECHANISM\" --attestation-status \"$RELEASE_ATTESTATION_STATUS\" --fallback-approved-at \"$RELEASE_ATTESTATION_FALLBACK_APPROVED_AT\" --fallback-approver \"$RELEASE_ATTESTATION_FALLBACK_APPROVER\" --fallback-expires-at \"$RELEASE_ATTESTATION_FALLBACK_EXPIRES_AT\" --fallback-approved-against-fingerprints-sha256 \"$RELEASE_ATTESTATION_FALLBACK_FINGERPRINTS_SHA256\" --concurrency-guard ./release-evidence/concurrency-guard.json && (dotnet nuget push ./nupkgs-signed/*.nupkg --source https://api.nuget.org/v3/index.json --api-key $NUGET_API_KEY --skip-duplicate || (python eng/release_evidence.py partial-publish-incident --manifest ./release-evidence/sealed-manifest.json --output ./release-evidence/partial-publish-incident.json --phase package-push --tag v${nextRelease.version} && exit 1)) && (dotnet nuget push ./nupkgs/*.snupkg --source https://api.nuget.org/v3/index.json --api-key $NUGET_API_KEY --skip-duplicate || (python eng/release_evidence.py partial-publish-incident --manifest ./release-evidence/sealed-manifest.json --output ./release-evidence/partial-publish-incident.json --phase symbol-push --tag v${nextRelease.version} && exit 1))"
+        "publishCmd": "python eng/release_evidence.py verify-manifest --root . --manifest ./release-evidence/sealed-manifest.json && release_dry_run_lower=$(printf '%s' \"${RELEASE_DRY_RUN:-true}\" | tr -d '[:space:]' | tr 'A-Z' 'a-z') && { case \"$release_dry_run_lower\" in false|0|no) is_dry_run=false ;; *) is_dry_run=true ;; esac; if [ \"$is_dry_run\" = \"true\" ]; then python eng/release_evidence.py classify-release --root . --evidence-root ./release-evidence --manifest ./release-evidence/sealed-manifest.json --test-results ./release-evidence/test-results.json --output ./release-evidence/release-readiness.json --require-publishable --dry-run-clean-exit --semantic-release-state matches --concurrent-same-version \"$RELEASE_CONCURRENT_SAME_VERSION\" --event-name \"$GITHUB_EVENT_NAME\" --ref \"$GITHUB_REF\" --ref-protected \"$GITHUB_REF_PROTECTED\" --from-fork \"$RELEASE_FROM_FORK\" --dry-run true --run-attempt \"${GITHUB_RUN_ATTEMPT:-1}\" --owner-approved \"$RELEASE_OWNER_APPROVED\" --approver \"$RELEASE_APPROVER\" --approval-mechanism \"$RELEASE_APPROVAL_MECHANISM\" --attestation-status \"$RELEASE_ATTESTATION_STATUS\" --fallback-approved-at \"$RELEASE_ATTESTATION_FALLBACK_APPROVED_AT\" --fallback-approver \"$RELEASE_ATTESTATION_FALLBACK_APPROVER\" --fallback-expires-at \"$RELEASE_ATTESTATION_FALLBACK_EXPIRES_AT\" --fallback-approved-against-fingerprints-sha256 \"$RELEASE_ATTESTATION_FALLBACK_FINGERPRINTS_SHA256\" --concurrency-guard ./release-evidence/concurrency-guard.json; classifier_rc=$?; if [ $classifier_rc -ne 0 ]; then exit $classifier_rc; fi; echo 'Dry-run classification complete; halting before publish side effects. (CR-12-4-P214 round-8: defense-in-depth — primary dry-run path uses `npx semantic-release --dry-run` and never invokes this branch; this fallback exit 1 prevents @semantic-release/github + @semantic-release/git from firing if publishCmd is reached during a dry-run via a future semantic-release config change.)' >&2; exit 1; fi; } && { prior_stderr=$(mktemp 2>/dev/null || echo ./release-evidence/.prior-tag-stderr.$$); same_version_stderr=$(mktemp 2>/dev/null || echo ./release-evidence/.same-version-stderr.$$); trap 'rm -f \"$prior_stderr\" \"$same_version_stderr\"' EXIT; prior_release_response=$(gh api \"repos/${GITHUB_REPOSITORY}/releases/tags/v${nextRelease.version}\" 2>\"$prior_stderr\"); gh_rc=$?; if [ $gh_rc -eq 0 ]; then if printf '%s' \"$prior_release_response\" | python3 -c 'import json,sys; json.dump(json.load(sys.stdin), sys.stdout)' > ./release-evidence/prior-release.json.tmp 2>/dev/null; then mv ./release-evidence/prior-release.json.tmp ./release-evidence/prior-release.json; RELEASE_CONCURRENT_SAME_VERSION=true; else rm -f ./release-evidence/prior-release.json.tmp; printf '{\"status\":\"probe-malformed-response\",\"tag\":\"v${nextRelease.version}\"}\\n' > ./release-evidence/prior-release.json; echo 'CR-12-4-P212: prior-tag probe returned non-JSON body; failing closed' >&2; RELEASE_CONCURRENT_SAME_VERSION=true; fi; elif grep -q 'HTTP 404\\|Not Found' \"$prior_stderr\" 2>/dev/null; then :; else echo \"Prior-tag probe failed transient (rc=$gh_rc): $(cat \"$prior_stderr\" 2>/dev/null)\" >&2; exit 1; fi; set +e; same_version_response=$(gh api \"repos/${GITHUB_REPOSITORY}/actions/runs?status=in_progress&per_page=100\" 2>\"$same_version_stderr\"); same_version_gh_rc=$?; if [ $same_version_gh_rc -eq 0 ] && printf '%s' \"$same_version_response\" | python3 -c 'import json,sys; d=json.load(sys.stdin); sys.exit(0) if isinstance(d, dict) and d.get(\"message\") and d.get(\"documentation_url\") and \"workflow_runs\" not in d else sys.exit(1)' >/dev/null 2>&1; then echo \"CR-12-4-P240: same-version probe returned GitHub rate-limit envelope (200 OK with message+documentation_url, no workflow_runs); failing closed.\" >&2; same_version_runs=\"\"; same_version_rc=2; else same_version_runs=$(printf '%s' \"$same_version_response\" | jq --arg run_id \"$GITHUB_RUN_ID\" \"[.workflow_runs[]? | select(.path | endswith(\\\".github/workflows/release.yml\\\")) | select(.display_title | test(\\\"(^|[^0-9.])v${nextRelease.version}([^0-9.]|$)\\\")) | select(.id != (\\$run_id | tonumber))] | length\"); same_version_rc=$?; fi; set -e; if [ $same_version_rc -ne 0 ]; then echo \"Same-version run probe failed transient (rc=$same_version_rc): $(cat \"$same_version_stderr\" 2>/dev/null)\" >&2; RELEASE_CONCURRENT_SAME_VERSION=true; elif ! printf '%s' \"${same_version_runs:-}\" | grep -Eq '^[0-9]+$'; then echo \"Same-version run probe returned non-numeric result; failing closed.\" >&2; RELEASE_CONCURRENT_SAME_VERSION=true; elif [ \"$same_version_runs\" -gt 0 ]; then echo \"Detected $same_version_runs other in-flight release run(s) targeting v${nextRelease.version}; failing closed.\" >&2; RELEASE_CONCURRENT_SAME_VERSION=true; fi; } && python eng/release_evidence.py classify-release --root . --evidence-root ./release-evidence --manifest ./release-evidence/sealed-manifest.json --test-results ./release-evidence/test-results.json --output ./release-evidence/release-readiness.json --require-publishable --semantic-release-state matches --concurrent-same-version \"$RELEASE_CONCURRENT_SAME_VERSION\" --event-name \"$GITHUB_EVENT_NAME\" --ref \"$GITHUB_REF\" --ref-protected \"$GITHUB_REF_PROTECTED\" --from-fork \"$RELEASE_FROM_FORK\" --dry-run \"$RELEASE_DRY_RUN\" --run-attempt \"${GITHUB_RUN_ATTEMPT:-1}\" --owner-approved \"$RELEASE_OWNER_APPROVED\" --approver \"$RELEASE_APPROVER\" --approval-mechanism \"$RELEASE_APPROVAL_MECHANISM\" --attestation-status \"$RELEASE_ATTESTATION_STATUS\" --fallback-approved-at \"$RELEASE_ATTESTATION_FALLBACK_APPROVED_AT\" --fallback-approver \"$RELEASE_ATTESTATION_FALLBACK_APPROVER\" --fallback-expires-at \"$RELEASE_ATTESTATION_FALLBACK_EXPIRES_AT\" --fallback-approved-against-fingerprints-sha256 \"$RELEASE_ATTESTATION_FALLBACK_FINGERPRINTS_SHA256\" --concurrency-guard ./release-evidence/concurrency-guard.json && (dotnet nuget push ./nupkgs-signed/*.nupkg --source https://api.nuget.org/v3/index.json --api-key $NUGET_API_KEY --skip-duplicate || (python eng/release_evidence.py partial-publish-incident --manifest ./release-evidence/sealed-manifest.json --output ./release-evidence/partial-publish-incident.json --phase package-push --tag v${nextRelease.version} && exit 1)) && (dotnet nuget push ./nupkgs/*.snupkg --source https://api.nuget.org/v3/index.json --api-key $NUGET_API_KEY --skip-duplicate || (python eng/release_evidence.py partial-publish-incident --manifest ./release-evidence/sealed-manifest.json --output ./release-evidence/partial-publish-incident.json --phase symbol-push --tag v${nextRelease.version} && exit 1))"
       }
     ],
     [
diff --git a/eng/release_evidence.py b/eng/release_evidence.py
index ff8a2d6..c384059 100644
--- a/eng/release_evidence.py
+++ b/eng/release_evidence.py
@@ -462,11 +462,22 @@ def looks_like_sha256(value: Any) -> bool:
 
 
 def parse_strict_bool(value: Any, *, field: str, default: bool = False) -> tuple[bool, str | None]:
-    """Strict security-flag boolean (true/false only, case-insensitive). Empty -> default."""
+    """Strict security-flag boolean (true/false only, case-insensitive). Empty -> default.
+
+    CR-12-4-P237 (round-10, EC-9): when the caller supplies ``None`` for a security-flag
+    field (e.g., ``checks.concurrent_same_version`` missing from a direct-evidence payload),
+    emit a typed diagnostic instead of silently returning ``default``. Direct-evidence mode
+    previously fail-opened on missing keys; CLI mode defaulted to ``"true"`` which fails
+    closed. The typed diagnostic forces the caller (``classify_release_payload``) to surface
+    the missing-flag as a blocker. Callers that legitimately want a default on ``None`` can
+    detect the diagnostic and choose to ignore it; the security-flag loop in
+    ``classify_release_payload`` treats every diagnostic as blocking, restoring symmetry
+    with the CLI-default fail-closed path.
+    """
     if isinstance(value, bool):
         return value, None
     if value is None:
-        return default, None
+        return default, f"{field} must be true or false; actual=<missing>"
     text = str(value).strip().lower()
     if text == "":
         return default, f"{field} must be true or false; actual=<empty>"
@@ -1306,12 +1317,21 @@ def fallback_invalidation_fingerprints(root: pathlib.Path) -> dict[str, str]:
     intent via the version bump. This couples AC34's "release-definition drift
     invalidates fallback" invariant to an explicit operator action, matching D16's
     "intentional version bump" contract.
+
+    CR-12-4-P245 (round-10, BH-F13): hash the version string before insertion so every
+    value in the fingerprints dict is a 64-char sha256 hex. The original P232 placed
+    the literal `"1.0.0"` alongside file-content hashes — operator forensic tools
+    auditing the digest input saw a non-hex entry and assumed corruption. The hash is
+    deterministic per version, so the consumer side computes the same value when
+    `manifest.helper_version.version` matches; a future audit tool validating
+    "every fingerprint is sha256-hex" now passes uniformly.
     """
     fingerprints: dict[str, str] = {}
     for rel in FALLBACK_INVALIDATION_FILES:
         target = root / rel
         fingerprints[rel] = sha256_file(target) if target.exists() else "missing"
-    fingerprints["helper_version"] = helper_version_record()["version"]
+    helper_version = helper_version_record()["version"]
+    fingerprints["helper_version"] = hashlib.sha256(helper_version.encode("utf-8")).hexdigest()
     return fingerprints
 
 
@@ -1364,6 +1384,17 @@ def fallback_complete(
     # silently shift expiry by a calendar day.
     if expiry <= dt.datetime.now(dt.timezone.utc):
         return False, f"fallback expired on {expiry.isoformat()}"
+    # CR-12-4-P243 (round-10, BH-F21/EC-6): the parser rejects future-skewed
+    # `approved_at` values (>5 min in future) but the original code accepted any past
+    # value — `0001-01-01T00:00:00Z` would satisfy structural completeness. Document
+    # WHEN the owner accepted the risk requires the timestamp to be (a) before
+    # `expires_at`, and (b) within the last 365 days (operationally a 1-year approval
+    # window matches the typical signing-cert lifetime).
+    if approved_at >= expiry:
+        return False, f"approved_at ({approved_at.isoformat()}) must be before expires_at ({expiry.isoformat()})"
+    approval_age = dt.datetime.now(dt.timezone.utc) - approved_at
+    if approval_age > dt.timedelta(days=365):
+        return False, f"approved_at is older than 365 days ({approved_at.isoformat()}); re-approve the fallback"
     # CR-12-4-P109 (round-5): path-check the evidence pointer. A fallback record with
     # `evidence: "release-evidence/attestation-unavailable.md"` is meaningless if the
     # file is not present — the document is the record. Phantom evidence pointers used
@@ -1411,8 +1442,18 @@ def contains_dangerous_evidence(value: Any) -> bool:
 
 
 def evidence_section(evidence: dict[str, Any], name: str, diagnostics: list[str]) -> dict[str, Any]:
-    value = evidence.get(name, {})
+    # CR-12-4-P242 (round-10, BH-F10/EC-13): distinguish missing-section, empty-dict,
+    # and malformed cases. Operator forensic message previously couldn't tell whether
+    # `"checks": {}` was a deliberate empty payload, a missing key, or a malformed
+    # non-dict value. With P237's fail-closed on missing security flags, the typed
+    # diagnostic also names which section is empty.
+    if name not in evidence:
+        diagnostics.append(f"{name} section missing")
+        return {}
+    value = evidence[name]
     if isinstance(value, dict):
+        if value == {}:
+            diagnostics.append(f"{name} section is empty")
         return value
     diagnostics.append(f"{name} section must be an object")
     return {}
@@ -1420,6 +1461,15 @@ def evidence_section(evidence: dict[str, Any], name: str, diagnostics: list[str]
 
 def classify_release_payload(evidence: dict[str, Any], root: pathlib.Path, *, verify_drift: bool = True, evidence_root: pathlib.Path | None = None) -> dict[str, Any]:
     section_diagnostics: list[str] = []
+    # CR-12-4-P235 (round-10, EC-2): guard against non-dict top-level evidence. P223
+    # guarded only the `manifest` sub-section; an evidence file with `[]`, `null`, or
+    # scalar at top level still reached `evidence_section(evidence, ...)` and crashed
+    # with AttributeError on the first `evidence.get(name, {})` call. Treat the
+    # malformed payload the same way as malformed sub-sections: typed diagnostic,
+    # empty-dict fallthrough, classification=blocked via the section_diagnostics path.
+    if not isinstance(evidence, dict):
+        section_diagnostics.append("evidence payload must be an object")
+        evidence = {}
     context = evidence_section(evidence, "context", section_diagnostics)
     checks = evidence_section(evidence, "checks", section_diagnostics)
     approval = evidence_section(evidence, "approval", section_diagnostics)
@@ -1534,9 +1584,19 @@ def classify_release_payload(evidence: dict[str, Any], root: pathlib.Path, *, ve
         # `manifest.attestation_bundle` is internally inconsistent — the fallback path
         # is by definition the no-bundle path. Reject so an attacker cannot smuggle a
         # stale bundle through alongside an approved fallback record.
+        #
+        # CR-12-4-P238 (round-10, EC-7): the original check only rejected dicts that
+        # ALSO carried a `sha256` field. A hand-crafted bundle like
+        # `{"path": "x.json", "kind": "..."}` (dict present, no sha256) slipped past.
+        # The symmetric inverse for the `attested` branch's "dict + sha256 required"
+        # is "any non-empty bundle is forbidden under approved-unsupported". Reject
+        # any non-empty dict, plus any non-dict truthy value (string, list, number).
         leftover_bundle = manifest.get("attestation_bundle") if isinstance(manifest, dict) else None
-        if isinstance(leftover_bundle, dict) and leftover_bundle.get("sha256"):
-            blocking.append("approved-unsupported fallback must not carry attestation_bundle evidence")
+        if isinstance(leftover_bundle, dict):
+            if leftover_bundle:  # non-empty dict
+                blocking.append("approved-unsupported fallback must not carry attestation_bundle evidence")
+        elif leftover_bundle:  # non-dict truthy (string, list, number)
+            blocking.append("approved-unsupported fallback must not carry attestation_bundle evidence (non-dict value)")
         fallback = attestation.get("fallback", {})
         if not isinstance(fallback, dict):
             blocking.append("unsupported-attestation fallback is missing, stale, or incomplete")
@@ -1559,9 +1619,30 @@ def classify_release_payload(evidence: dict[str, Any], root: pathlib.Path, *, ve
                 # driven version bump invalidates the fallback approval. The producer
                 # side (`fallback_invalidation_fingerprints`) embeds the same key/value
                 # shape; both sides must agree for `fallback_complete` to validate.
+                #
+                # CR-12-4-P233 (round-10, BH-F3): the original consumer code only
+                # appended `helper_version` to `fingerprints_for_drift` when
+                # `manifest.helper_version` was a dict. A manifest with `helper_version:
+                # null` (or scalar/missing) would compute a digest WITHOUT the key while
+                # the producer ALWAYS adds it — guaranteed digest mismatch with no
+                # actionable diagnostic. Now: always include `helper_version` (default
+                # empty string when manifest field is missing or malformed) and append
+                # a typed diagnostic so the operator sees the structural cause.
+                #
+                # CR-12-4-P245 (round-10, BH-F13): hash the version string for type
+                # uniformity with the file-content hashes; mirrors the producer.
+                # `manifest_diagnostics` (called above for both attested and
+                # approved-unsupported paths) already emits a `manifest:` blocker when
+                # `helper_version` is missing or non-dict — we don't re-raise. We only
+                # need to compute the consumer-side digest contribution: hash an empty
+                # version string when the field is malformed so the digest is
+                # deterministically wrong and `fallback_complete` rejects via its
+                # canonical_sha256 comparison rather than via a phantom-key mismatch.
                 manifest_helper_version = manifest.get("helper_version") if isinstance(manifest, dict) else None
+                helper_version_value = ""
                 if isinstance(manifest_helper_version, dict):
-                    fingerprints_for_drift["helper_version"] = str(manifest_helper_version.get("version", ""))
+                    helper_version_value = str(manifest_helper_version.get("version", ""))
+                fingerprints_for_drift["helper_version"] = hashlib.sha256(helper_version_value.encode("utf-8")).hexdigest()
             # CR-12-4-P120 (round-6): bind the current package-set fingerprint so the
             # fallback is invalidated by inventory drift per AC34/D19.
             current_package_set = manifest.get("package_set_fingerprint") if isinstance(manifest, dict) else None
@@ -1606,12 +1687,49 @@ def classify_release_payload(evidence: dict[str, Any], root: pathlib.Path, *, ve
         blocking.append(f"attestation status must be attested or approved-unsupported; actual={sanitize(status)}")
 
     classification = "blocked" if blocking else ("fallback-approved" if fallback_reasons else "ready")
+    # CR-12-4-P247 (round-10, D21/BH-F2/EC-1): dry-run carve-out. Round-9 P217/P218
+    # made the `--dry-run-clean-exit` path structurally unreachable: any dry-run
+    # always classifies as `local-candidate` (line 1474-1475 above), which appends
+    # the candidate-evidence blocker, which forces `classification = "blocked"`,
+    # which fails the P218 gate `classification in {ready, fallback-approved}`.
+    # Without this carve-out, every healthy dry-run reports red on the workflow
+    # step status — defeating P214's "healthy dry-run reports green" intent.
+    #
+    # Carve-out conditions (must ALL hold):
+    #   1. `checks.dry_run` is True (the run is a dry-run dispatch);
+    #   2. `context_class == "local-candidate"` (the only context dry-runs reach);
+    #   3. `blocking == [candidate-evidence-blocker]` (no OTHER blockers — a real
+    #      defect like missing tests must keep classification=blocked);
+    #   4. no fallback path (fallback-approved takes precedence below).
+    #
+    # When the carve-out fires, `classification = "ready"` but `publish_authorized`
+    # stays False (the gate at line 1685 requires `trusted-main-or-release` context;
+    # local-candidate fails that). AC23 verbatim already permits this combination.
+    # A typed `helper_state` note records the carve-out for auditors.
+    # Parse dry_run from the context section (where `classify_context` reads it from).
+    # Re-parsing is safe: any diagnostic was already added by `classify_context` and
+    # surfaced via `context_diagnostics`, so the carve-out check sees the value
+    # without double-counting forensic noise.
+    dry_run_flag, _ = parse_strict_bool(context.get("dry_run"), field="dry_run")
+    candidate_blocker = f"candidate evidence from {context_class} cannot authorize publishing"
+    dry_run_carve_out = (
+        dry_run_flag
+        and context_class == "local-candidate"
+        and classification == "blocked"
+        and not fallback_reasons
+        and len(blocking) == 1
+        and blocking[0] == candidate_blocker
+    )
+    if dry_run_carve_out:
+        classification = "ready"
     publish_authorized = classification in {"ready", "fallback-approved"} and context_class == "trusted-main-or-release" and approval_approved
     next_action = "publish may proceed with approved owner action" if publish_authorized else "resolve blocking release gates before publishing"
     if context_class == "rerun-review":
         next_action = "rerun-review contexts are never publish-authorized; create a fresh dispatch or new tag to retry"
     if classification == "fallback-approved":
         next_action = "release owner must consciously accept the approved fallback before publish"
+    if dry_run_carve_out:
+        next_action = "dry-run carve-out: classification=ready with publish_authorized=false; merge to trusted-main-or-release to publish"
 
     return {
         "approval": {
@@ -1827,6 +1945,17 @@ def test_results(args: argparse.Namespace) -> int:
 def verify_manifest(args: argparse.Namespace) -> int:
     manifest = read_json(args.manifest)
     diagnostics: list[str] = []
+    # CR-12-4-P236 (round-10, EC-8): guard against non-dict top-level manifest. The
+    # workflow's pre-classify JSON parse check (P227) validates parseability but not
+    # dict shape. A sealed-manifest.json with top-level `[]`, `null`, or scalar
+    # previously crashed via AttributeError on the first `manifest.get(...)` call in
+    # `manifest_diagnostics` (exit 2 with traceback) instead of producing a typed
+    # release-definition blocker. Treat as invalid and surface a typed diagnostic.
+    if not isinstance(manifest, dict):
+        diagnostics.append("sealed manifest must be an object")
+        if args.output:
+            write_json(args.output, {"status": "invalid", "diagnostics": diagnostics})
+        return 1
     if args.no_root:
         root = None
     elif args.root:
@@ -2337,21 +2466,32 @@ def classify_release(args: argparse.Namespace) -> int:
             concurrency_probe_diagnostics.append(
                 f"concurrency-probe: --concurrency-guard path missing: {sanitize(guard_path_arg)}"
             )
+    # CR-12-4-P246 (round-10, BH-F12): build augmented checks in a local dict and
+    # spread `{**evidence, "checks": augmented}` into the classifier call rather than
+    # mutating `evidence["checks"]` in place. The mutation pattern leaked state into
+    # the caller's dict — a future refactor that calls `classify_release_payload`
+    # twice for comparison would see the second call's evidence already mutated.
+    # Mutation-free spread also pairs with the typed-evidence-as-read-only convention.
     if concurrency_probe_diagnostics:
-        checks = evidence.get("checks")
-        if not isinstance(checks, dict):
-            checks = {}
-            evidence["checks"] = checks
+        existing_checks = evidence.get("checks") if isinstance(evidence, dict) else None
+        if not isinstance(existing_checks, dict):
+            existing_checks = {}
             concurrency_probe_diagnostics.insert(0, "checks section missing or malformed while applying concurrency guard")
-        checks["concurrent_same_version"] = True
-        checks["helper_state"] = "partial"
-        raw_check_diagnostics = checks.get("diagnostics", [])
+        raw_check_diagnostics = existing_checks.get("diagnostics", [])
         check_diagnostics = raw_check_diagnostics if isinstance(raw_check_diagnostics, list) else []
-        checks["diagnostics"] = [
-            *check_diagnostics,
-            *concurrency_probe_diagnostics,
-        ]
-    decision = classify_release_payload(evidence, pathlib.Path(args.root), evidence_root=evidence_root_arg)
+        augmented_checks = {
+            **existing_checks,
+            "concurrent_same_version": True,
+            "helper_state": "partial",
+            "diagnostics": [
+                *check_diagnostics,
+                *concurrency_probe_diagnostics,
+            ],
+        }
+        evidence_for_classify = {**evidence, "checks": augmented_checks} if isinstance(evidence, dict) else {"checks": augmented_checks}
+    else:
+        evidence_for_classify = evidence
+    decision = classify_release_payload(evidence_for_classify, pathlib.Path(args.root), evidence_root=evidence_root_arg)
     # CR-12-4-P209 (round-8): release owners reading only `grouped_reasons.blocking`
     # need to distinguish PROBE-DEGRADED (infrastructure issue → retry) from a REAL
     # concurrent run (operational issue → wait). The headline reason emitted by
@@ -2397,10 +2537,17 @@ def classify_release(args: argparse.Namespace) -> int:
         # `classification in {"ready", "fallback-approved"}`. A future blocking reason
         # like "dry-run side-effect attempt detected" would have falsely matched the
         # prior `startswith("dry-run")` predicate and slipped past the exit-0 gate.
+        # CR-12-4-P247 (round-10, D21/BH-F2/EC-1): the P218 gate had made this path
+        # structurally dead because dry-runs always classified as `blocked`. The
+        # corresponding carve-out in `classify_release_payload` now reclassifies
+        # healthy dry-runs as `classification="ready"` (with `publish_authorized=false`),
+        # so the gate fires when intended. Also dropped the dead allowlist literal
+        # `"dry-run dispatch cannot authorize publishing"` — only the f-string
+        # `f"candidate evidence from {context_class} ..."` is emitted by the
+        # classifier, and only the `local-candidate` instance is allowed here.
         if getattr(args, "dry_run_clean_exit", False) and blocking:
             allowed_blockers = {
                 "candidate evidence from local-candidate cannot authorize publishing",
-                "dry-run dispatch cannot authorize publishing",
             }
             classification_ready = decision["classification"] in {"ready", "fallback-approved"}
             if classification_ready and all(b in allowed_blockers for b in blocking):
diff --git a/tests/ci-governance/fixtures/release-readiness-cases.json b/tests/ci-governance/fixtures/release-readiness-cases.json
index 3846164..7e0c4b3 100644
--- a/tests/ci-governance/fixtures/release-readiness-cases.json
+++ b/tests/ci-governance/fixtures/release-readiness-cases.json
@@ -95,7 +95,7 @@
         "attestation": {
           "fallback": {
             "affected_artifact": "Hexalith.FrontComposer.Contracts.1.2.3.nupkg",
-            "approved_against_fingerprints_sha256": "629ca85057eee915e7b126f13643ec090b869b3ad27171d9fedd3b0a0cbe3f6e",
+            "approved_against_fingerprints_sha256": "07aaf9f9f83d789b0ffb91fe3ab3dd934c18e92e318fdb25c03c7e0926166fed",
             "approved_at": "2026-05-19T00:00:00Z",
             "approver": "release-owner",
             "evidence": "attestation-unavailable.md",
@@ -133,7 +133,7 @@
       }
     },
     {
-      "expected_classification": "blocked",
+      "expected_classification": "ready",
       "expected_context_class": "local-candidate",
       "expected_publish_authorized": false,
       "name": "dry-run-from-dispatch",
@@ -287,8 +287,9 @@
       }
     },
     {
-      "expected_classification": "blocked",
+      "expected_classification": "ready",
       "expected_context_class": "local-candidate",
+      "expected_publish_authorized": false,
       "name": "local-candidate",
       "override": {
         "context": {
