# Critical Decisions (READ FIRST — Do NOT Revisit)

> 24 binding decisions. Feature-story budget is 25 (L06) — 3-4 is at 24/25; one slot remains before trim rules apply. D20–D23 added after party-mode review (Winston + Amelia + Sally + Murat convergence); D24 added after advanced-elicitation pass (pre-mortem + failure-mode analysis) to close the registrar-idempotency gap; D1/D3/D11/D15/D21/D23 tightened in the same elicitation pass. Raise a spec-change proposal before revisiting any of them.

| # | Decision | Rationale | Consumed by |
|---|---|---|---|
| D1 | **`IShortcutService` is the single framework surface for global keyboard shortcuts.** Interface: `IDisposable Register(string binding, string descriptionKey, Func<Task> handler)`, `IReadOnlyList<ShortcutRegistration> GetRegistrations()`, `Task<bool> TryInvokeAsync(KeyboardEventArgs e)`. Binding grammar (normalised): all-lowercase, modifiers in canonical order `ctrl+shift+alt+meta+<key>`, bare-chord `g h` with exactly one space. `ShortcutBinding.Normalize(string)` is the single normaliser — used at registration time + inside `TryInvokeAsync` so the lookup is O(1) dictionary hit. Native ARIA/DOM behaviours (`Escape` closes the dialog, arrow keys navigate `role="listbox"`) are NOT routed through `IShortcutService` — those are Fluent UI + browser defaults. **Input-validation rule**: `Register` throws `ArgumentException` on null / empty / whitespace `binding` OR `descriptionKey` OR `handler` — empty descriptionKey would fail the localisation lookup later with a less actionable message, so fail fast at registration. **Handler-exception policy**: if the registered handler throws inside `TryInvokeAsync`, the exception is caught, logged as `HFC2109_ShortcutHandlerFault` at Warning severity with structured payload `{Binding, DescriptionKey, ExceptionType, ExceptionMessage}`, and `TryInvokeAsync` returns `true` (the shortcut fired — it errored — the shell must not re-process the key as fallthrough). Rationale for return-true-on-throw: a handler exception is a handler bug, not a "no shortcut registered" condition; surfacing it to the shell's `@onkeydown` would bubble to Blazor's error boundary and interrupt user flow. `HFC2109` is a NEW diagnostic ID reserved in `AnalyzerReleases.Unshipped.md` at Warning severity (distinct from `HFC2108` Information duplicate-registration). | One normaliser + one dictionary is the simplest correct answer. A framework service ensures framework + adopter shortcuts compete in one arbitration path (D5) — without it, Story 3-3's Ctrl+, inline binding and adopter bindings coexist by accident. The grammar's restricted form eliminates the "is `Ctrl+k` the same as `ctrl+K`?" bikeshed. The `ArgumentException` guard + handler-exception policy were gaps surfaced by post-party-mode failure-mode analysis — without them, null-descriptionKey Register calls would throw deep inside the localisation stack at render time (hard to diagnose), and handler exceptions would reach the Blazor error boundary (bad UX). | Task 1.1, 1.2, 10.1, 10.1g, 10.2; AC1, AC8 |
| D2 | **`ShortcutService` is Scoped** (per-circuit in Blazor Server, per-user in WASM) — mirrors Story 3-1 `IStorageService` (ADR-030) + Story 3-2 `FcLayoutBreakpointWatcher`. Backing store is a `ConcurrentDictionary<string, ShortcutEntry>` to tolerate late registrations (adopter DI extensions that run after `AddHexalithFrontComposer`). Singleton was rejected — shortcuts are user-experience concerns, not process-level infrastructure; a singleton would leak handler references across circuits and fight tenant isolation. | Per-circuit scoping means a Ctrl+K registered by tenant A's adopter code doesn't fire for tenant B sharing the same process. `ConcurrentDictionary` is the standard fix for the "first registrar runs on a background scheduler while the shell is still constructing" race condition — tiny performance hit, big correctness win. | Task 1.2, 10.1; AC1 |
| D3 | **Duplicate registration = last-writer-wins + `HFC2108_ShortcutConflict` Information log.** Adopter code that deliberately wants to override `Ctrl+K` (e.g., to surface a tenant-specific dashboard instead of the framework palette) registers AFTER `AddHexalithFrontComposer` and the shell's earlier registration is replaced — no exception, no blocking. The log carries both registrations' `descriptionKey` so an operator can see who stole the binding. **Registration-order semantics (explicit)**: "last-writer-wins" means DISPOSAL ORDER, not DI-configuration order. At registration time, `Register` is called imperatively from `OnAfterRenderAsync` / `OnInitialized` lifecycles — the earlier call in real-time is the first writer; the later call replaces it. Adopter code that wants to override a framework binding MUST therefore invoke its own `IShortcutService.Register(...)` AFTER `FrontComposerShortcutRegistrar.RegisterShellDefaultsAsync()` has run (which happens in `FrontComposerShell.OnAfterRenderAsync(firstRender: true)` per Task 8.2) — adopters' `OnAfterRenderAsync` on components LOWER in the render tree runs after the shell's, so adopter-component registrations naturally win. Adopter DI-module-level registrations that happen during app startup (before any render) race the shell registrar and are an anti-pattern; prefer adopter-component-level registrations. This is **runtime** conflict detection only; build-time enforcement (Roslyn analyzer that scans all `Register("ctrl+k", ...)` call sites across a solution) is **Story 9-4** per G1 / L10 discipline. | Throwing on duplicate would prevent adopters from deliberately overriding; warning without replacing would deprive adopters of the intended behaviour; replacing without logging would hide the decision from operators. Information severity is chosen (not Warning) because deliberate adopter override is the expected case; elevate to Warning via `[SuppressHFC2108]` (Story 9-4) when true accidental conflict telemetry becomes actionable. Registration-order clarification was gap-filled post-party-mode: without it, adopter packages attempting to override `Ctrl+K` from a DI extension method silently lost to the later-running registrar — the pattern "adopter-component registers AFTER shell render" is the intended path. | Task 1.3, 10.1e; AC1 |
| D4 | **Two-key chords (`g h`) are tracked via a 1500 ms "pending first key" field on `ShortcutService`.** When `TryInvokeAsync(e)` sees a registered chord-prefix key (e.g., `g` is the first key of registered chord `g h`), the service stores `_pendingFirstKey = "g"` and schedules a cancellation via `TimeProvider.CreateTimer(1500ms)` (D22 — `TimeProvider` is injected so tests use `FakeTimeProvider`). A matching second key within the window concatenates to `"g h"` and invokes the chord's handler; on invocation the pending-key field is cleared. A non-matching second key clears `_pendingFirstKey` and the second key is re-evaluated as a fresh single-key lookup. **No single-key registrations are allowed for letter keys without a modifier** — `ShortcutBinding.Normalize` throws `ArgumentException` on `"g"` single letter without modifier. Sub-decisions making the chord state machine deterministic: **(a) timer type** — `TimeProvider.CreateTimer` (NOT `System.Threading.Timer`, NOT `Task.Delay`) so tests advance virtual time; **(b) timeout behaviour** — the 1500 ms expiry clears `_pendingFirstKey` **silently** (no diagnostic, no log — expected flow, mirrors D9's `OperationCanceledException` swallow pattern); **(c) repeat-prefix during pending** — if `_pendingFirstKey == "g"` and a second `g` arrives before timeout, the pending field is overwritten (new prefix replaces old; `g g` is NOT looked up as a chord unless it's a registered chord); **(d) modifier-chord during pending** — if a modifier-bearing binding (`Ctrl+K`, `Ctrl+,`) fires while `_pendingFirstKey` is set, the modifier chord fires AND clears the pending field (modifier bindings are never chord-continuations); **(e) concurrent lookups** — `_pendingFirstKey` + timer field are guarded by a `lock` (or `Interlocked` field swap) because `TryInvokeAsync` can race with a timer callback. 1500 ms rationale: on the long end of IDE convention (VS Code ~1000 ms, IntelliJ ~2000 ms) — chosen to accommodate slower typists in accessibility scenarios (a11y user research caveat — power users can still hit the chord inside the window; slower typists don't get cut off). | Pending-key state stays local to the service; no per-handler state creeps into the public API. Sub-decisions (a)-(d) pin the chord FSM so the developer does not invent them in code review — each was surfaced as a blocker by Amelia in party-mode review. Timer via `TimeProvider` is the D22 connection: chord timeout + debounce (D9) share one abstraction so tests for both are deterministic. | Task 1.2, 10.1c, 10.1d; AC1 |
| D5 | **`FrontComposerShell.razor`'s `.fc-shell-root` `@onkeydown` handler calls `IShortcutService.TryInvokeAsync(e)` and returns immediately if the event target is inside an `<input>`, `<textarea>`, `[contenteditable]` element UNLESS the event includes `Ctrl`, `Shift+Ctrl`, or `Ctrl+Meta`.** The text-input guard is a standard palette-library idiom (prevents `g h` chord from firing while the user types in a search box). Modifier-bearing combos (Ctrl+K, Ctrl+,) are NOT suppressed by the text-input guard because those are deliberately global. The handler call path replaces Story 3-3's inline `if (e.Key == "," && e.CtrlKey ...)` branch (AC8 migration contract). | Keyboard shortcuts that block text input are the #1 user complaint about palette libraries (e.g., Sourcegraph, Slack historical regressions). The modifier-exception preserves the global-everywhere promise of `Ctrl+K`. Shell-level routing (not per-component) centralises the text-input guard in one place — adopters who register a new shortcut don't re-implement the guard. | Task 8.1, 8.2, 8.3, 10.10a; AC8 |
| D6 | **`FrontComposerCommandPaletteFeature` is a new per-concern Fluxor feature.** State shape: `public record FrontComposerCommandPaletteState(bool IsOpen, string Query, ImmutableArray<PaletteResult> Results, ImmutableArray<string> RecentRouteUrls, int SelectedIndex, PaletteLoadState LoadState)`. Initial state: `(false, "", Empty, Empty, 0, Idle)`. Per-concern rather than extending `FrontComposerNavigationState` because palette state has its own persistence key (`"palette-recent"`), its own hydrate flow (independent of nav), and its own effect suite (the debounce timer is unique to this feature). The shell registers the feature via the existing Fluxor assembly scan — no new DI extension method. | Per-concern Fluxor features is the architecture §527-536 pattern already proven across Theme / Density / Navigation / DataGridNavigation. Folding into Navigation would couple two independent persistence lifecycles (nav is bool + dictionary; palette is string[] recent). Keeping them separate lets a test of one feature's reducers not reset the other's state. | Task 2.1-2.5, 10.4; AC2, AC3, AC5 |
| D7 | **`PaletteScorer.Score(query, candidate) : int` is the single pure-function fuzzy matcher — invoked by the effect, never by a reducer.** Algorithm: `ToLowerInvariant` both sides. **Exact prefix** → `100 + matchLen × 2`. **Contains substring** → `50 + matchLen`. **Fuzzy subsequence** (query chars appear in order, non-contiguous) → `10 + matchedCount − gapCount`. No match → `0`. Contextual bonus (`+15` when candidate's bounded context equals current nav context) is applied by the EFFECT after scoring, not inside the pure scorer — so the scorer's unit tests don't need a mock navigation state. | Pure function + effect-side bonus separates "what matches?" (deterministic) from "how is it ranked?" (context-dependent). Tests of the scorer don't care about Fluxor; tests of the effect don't re-derive scoring from scratch. The three-tier score band (100s for prefix, 50s for contains, 10s for subsequence) means sort-by-score always respects user expectation (prefix always outranks subsequence). | Task 4.1, 4.2, 10.3, 10.3a; AC3 |
| D8 | **Scoring runs INSIDE the effect, NOT inside the reducer.** `HandlePaletteQueryChanged` effect enumerates `IFrontComposerRegistry.GetManifests()`, scores every candidate via `PaletteScorer.Score`, applies the contextual bonus, takes top-50 by score (`OrderByDescending(r => r.Score).Take(50)`), and dispatches `PaletteResultsComputedAction(query, results)`. The reducer for that action assigns `state with { Results = action.Results, SelectedIndex = 0, LoadState = Ready }`. Mirrors Story 3-3 ADR-039 (action producers compute — reducers stay pure). | Scoring over N manifests needs `IFrontComposerRegistry` (DI). Reducers are static + pure. Moving compute to the effect preserves the Fluxor static-reducer discipline + isolates I/O-bound enumeration from state replay. The top-50 cap means the reducer assigns a bounded array, not an unbounded enumerable. | Task 3.1, 3.2, 10.4b; AC3 |
| D9 | **Search debounce is a 150 ms `Task.Delay(150, ct)` inside the effect**, with per-dispatch `CancellationTokenSource` so a newer `PaletteQueryChangedAction` cancels the outstanding delay. `CommandPaletteEffects` holds a `_queryCts` field (instance, scoped-per-circuit because effects are scoped). Cancellation → the effect awaits `Task.Delay(..., ct)` which throws `OperationCanceledException` → caught + swallowed (no diagnostic — expected flow). **Typing-speed tradeoff (explicit)**: 150 ms is a MEDIAN-case optimisation, not a universal best. Fast typists (120+ WPM, <60 ms / keystroke) benefit because multiple keystrokes batch into one scoring pass — CPU cost drops linearly. Slow typists (20-40 WPM, 250-400 ms / keystroke) pay a visible 150 ms per keystroke of pure lag because their inter-keystroke interval always exceeds the debounce — the debounce never batches anything for them. Average users (50-80 WPM, ~200 ms / keystroke) sit on the boundary and experience mixed behaviour. 150 ms is the Goldilocks because it matches the median inter-keystroke interval: fast enough to feel live, slow enough to avoid 10-scoring-passes-per-second on fast typists. v1.x may expose `FcShellOptions.PaletteDebounceMs` when telemetry surfaces a bimodal distribution of user typing speeds, but v1 ships the single tuned value. | 150 ms is the UX spec's explicit debounce target (Epic AC §165 + architecture §107). `Task.Delay` + `CancellationToken` is the standard .NET async debouncer pattern; `System.Reactive.Linq.Throttle` would add a Reactive dependency for one use case. Per-dispatch CTS is simpler than a shared-timer pattern that has to deal with the "timer fires while I'm mid-replace" race. The typing-speed-tradeoff note was added post-elicitation Mentor pass to make the choice's inclusive-design tradeoff visible — "slow typists pay lag, fast typists save CPU" is a value judgement worth preserving in the decision doc. | Task 3.1, 10.4c; AC3 |
| D10 | **Recent-route ring buffer is capped at 5 entries, de-duplicated on exact string match, and prepended** (most-recent-first). Storage schema: `ImmutableArray<string>` serialised as a JSON string array under `StorageKeys.BuildKey(tenantId, userId, "palette-recent")`. Fail-closed on null / empty / whitespace tenant OR user — `HFC2105_StoragePersistenceSkipped` (reused from Story 3-1, mirrors Story 3-2 + 3-3 precedent). Hydrate is read-only (ADR-038 mirror) — `PaletteHydratedAction` does NOT trigger persistence. **Tenant-switch behaviour**: `ShortcutService` + `CommandPaletteEffects` are `Scoped` (D2) — on a Blazor Server tenant switch the circuit is torn down and rebuilt, so `state.RecentRouteUrls` starts empty and `HandleAppInitialized` re-hydrates from the NEW tenant's `palette-recent` key. No cross-tenant leakage is possible because the key is `{tenantId}/{userId}/palette-recent` and `IUserContextAccessor` returns the new tenant on reconnect. WASM has one tenant per user per app session so tenant-switch-within-session is not applicable. No additional cleanup code in 3-4 — this is a property inherited from D2 + Story 3-1's fail-closed scope guard. **Hydrate-time route safety filter (post-elicitation Red Team R7)**: `HandleAppInitialized` filters each stored URL through `IsInternalRoute(string url) : bool` BEFORE dispatching `PaletteHydratedAction`. Rule: a valid internal route starts with `/` and does NOT start with `//`, does NOT contain a scheme (`http:`, `https:`, `javascript:`, `data:`), and does NOT contain `\` (Windows-style path-separator bypass). Any filtered-out entry is dropped silently + the aggregate "N entries rejected" count is logged via the existing `HFC2106` diagnostic ID with a NEW reason code `Reason=Tampered` (joining the existing `Reason=Empty` / `Reason=Corrupt` codes per Story 3-2 D15 precedent — one diagnostic ID, multiple reason codes). Rationale: if `IStorageService`'s underlying store is writable from an untrusted source (e.g., adopter-side indexedDB code with broader write scope than the framework realises), a tampered recent-list entry like `/evil?return=//attacker.com` would result in Blazor's `NavigationManager.NavigateTo(...)` sending the user off-site on next palette-open → Recent activation. Internal-route filter is O(N) with N=5 — negligible cost, meaningful defence. | Five is the Sublime Text / VS Code "Recent Files" convention — enough to cover a typical session, small enough to render without scrolling. De-dup-on-exact-string is adequate because routes are canonical path strings (no query-string variants in v1). Ring-buffer semantics means no explicit LRU bookkeeping — the most-recent-at-front order is inherent. Tenant-switch guarantee is inherited from the Scoped lifetime decision — called out explicitly per Winston's party-mode review to prevent a future "did we check this?" audit. The internal-route filter is a defense-in-depth edit post-Red-Team analysis: even under the v1 trusted-adopter model (G20), a storage-layer bug or a future shared-storage-across-tenants regression could leak malicious data into the recent list — fail-closed filtering at hydrate prevents the downstream navigation footgun. | Task 3.3, 3.4, 3.6, 10.4d, 10.5; AC3 |
| D11 | **`FcCommandPalette` is opened via `IDialogService.ShowDialogAsync<FcCommandPalette>(DialogParameters)`**, NOT by toggling a boolean and rendering `FluentDialog` declaratively. Same rationale as Story 3-3 D11: Fluent UI's `IDialogService` owns dialog lifetime; duplicating that in Fluxor state (`IsOpen`) is a source-of-truth fork. **BUT** `FrontComposerCommandPaletteState.IsOpen` still exists — it tracks "should the shell's `IShortcutService.TryInvokeAsync` block further `Ctrl+K` presses?" (idempotent open — D12). The `IsOpen` Fluxor field shadows `IDialogService`'s internal state for the shortcut-arbitration path only; the dialog's actual render lifecycle is owned by Fluent UI. **Dialog-close-path coherence (every dismiss path dispatches `PaletteClosedAction`)**: Fluent UI exposes multiple dismiss paths — Escape key (handled in the component's `@onkeydown`), X-button click (FluentDialog's internal dismiss), backdrop click (if enabled), programmatic `Dialog.CloseAsync()`, and circuit disconnect (dialog implicitly disposes). Without explicit wiring, the X-button / backdrop / disconnect paths close the Fluent UI dialog but NEVER dispatch `PaletteClosedAction`, leaving Fluxor `IsOpen == true` forever and blocking every subsequent `Ctrl+K` via D12's idempotent-open guard. **Required wiring in `FcCommandPalette.razor.cs`**: (a) the `@onkeydown` handler for Escape explicitly dispatches `PaletteClosedAction` BEFORE calling `Dialog.CloseAsync()`; (b) override `DisposeAsync` to check `IDialogInstance.Result` — if the dialog was dismissed without a prior `PaletteClosedAction` dispatch (tracked via an `_explicitlyClosed` bool flag set to true inside the Escape handler AND the result-activation path), dispatch `PaletteClosedAction(UlidFactory.NewUlid())` from `DisposeAsync` as a catch-all before calling `base.DisposeAsync()`. **Circuit-teardown robustness**: the `DisposeAsync` dispatch MUST be wrapped in `try { Dispatcher.Dispatch(new PaletteClosedAction(...)); } catch (ObjectDisposedException) { /* circuit already disposed — nothing to do, state is gone anyway */ }` because `Dispatcher.Dispatch` on a disposed circuit (dirty disconnect, browser tab closed, network partition) throws `ObjectDisposedException`. The catch is intentionally silent — if the circuit is disposed, the Fluxor store is disposed, so the missed dispatch is moot (there's no state to update). This prevents the dispose path from logging scary exceptions on routine disconnects. **Call-site ownership for `Dialog.CloseAsync`**: the component owns the `IDialogInstance` cascading parameter; the Escape handler + OnSelectionClicked activation handler call `Dialog?.CloseAsync()` directly. The `HandlePaletteResultActivated` effect does NOT close the dialog — it dispatches `PaletteClosedAction` + navigation + recent-route; the component's state subscription observes `IsOpen == false` and does not re-close (the close already happened from the click handler that triggered the activation). This means exactly ONE `Dialog.CloseAsync` call per user-initiated close. This makes the Fluxor flag coherent with every Fluent UI dismiss path — X-button, backdrop, disconnect, and programmatic all land `IsOpen = false` exactly once. The resulting invariant is tested via the `PaletteDismissPaths_AllDispatchPaletteClosedAction` `[Theory]` (Task 10.7d). | `IDialogService` is the right lifecycle owner; `IsOpen` in state exists solely for arbitration. Without `IsOpen`, rapid `Ctrl+K` presses would stack multiple dialog instances — a UX regression. The two sources agree because every dismiss path dispatches `PaletteClosedAction` (explicit for Escape / activation; catch-all via `DisposeAsync` for X-button / backdrop / disconnect). Without the `DisposeAsync` catch-all, users who close via the X button would find `Ctrl+K` no-ops forever until circuit restart — a silent-failure footgun surfaced by post-party-mode failure-mode analysis. | Task 5.1, 5.3, 5.4, 10.7, 10.7d; AC2 |
| D12 | **Rapid `Ctrl+K` presses while the palette is already open are NO-OPS.** `FrontComposerShortcutRegistrar.OpenPaletteAsync` reads `IState<FrontComposerCommandPaletteState>.Value.IsOpen` and returns early if true. This is the idempotent-open discipline (mirrors Story 3-3 `FcSettingsDialog` implicit behaviour — Fluent UI dialogs are single-instance by default). **Convention divergence from VS Code**: VS Code's Ctrl+K is a TOGGLE — pressing it with the palette already open CLOSES the palette. 3-4 deliberately diverges: our Ctrl+K is idempotent-OPEN only; closing requires Escape, X-button, backdrop click, or activation. Rationale: toggle semantics conflict with D3 last-writer-wins (adopter override of Ctrl+K would lose the close-behaviour unexpectedly) and with the AC2 focus-trap contract (reopening inside an open trap creates a second trap layer). Users migrating from VS Code may expect toggle; palette "Keyboard Shortcuts" reference (AC6) shows Ctrl+K with "Open command palette" description — the open-only language is the user-facing signal. | Prevents dialog-over-dialog stacking which breaks focus-trap + breaks the close-flow (Escape would close the outermost, leaving the stale copy visible). Checking state before dispatch is cheap (one field read). VS Code divergence note added post-elicitation Mentor pass: without it, a future review could "simplify" the behaviour to toggle for "consistency with VS Code" and break the focus-trap contract — the rationale here is the anti-regression argument. | Task 1.4, 10.1f; AC2 |
| D13 | **Selecting a result dispatches to `NavigationManager.NavigateTo(routeUrl)` for `Category == Projection | Recent | Shortcut(with-route)`, or to the generated command form path for `Category == Command`.** Command form path is `/domain/{CommandName}` (Story 2-2 FullPage render-mode convention) OR the inline-popover path when Story 2-2's density rules resolve to Inline (palette cannot open an inline popover from a dialog — it navigates to the FullPage version in that case). Every successful activation dispatches `PaletteClosedAction` + `RecentRouteVisitedAction(newRoute)` from inside `HandlePaletteResultActivated`. | `NavigationManager.NavigateTo` is the Blazor idiom — avoids inventing a Fluxor navigation action for a concern Blazor already owns. The palette→FullPage convention for commands sidesteps the "dialog-in-dialog" inline-popover mounting issue; users who want inline invocation click directly on the DataGrid row (Story 2-2 primary interaction). | Task 3.5, 10.4e; AC5 |
| D14 | **Nine new palette resource keys + three shortcut-description keys = 12 total, doubled for FR = 24 strings.** Palette: `PaletteTriggerAriaLabel`, `CommandPaletteTitle`, `PaletteSearchPlaceholder`, `PaletteCategoryProjections`, `PaletteCategoryCommands`, `PaletteCategoryRecent`, `PaletteResultCountTemplate` ("{0} results" / "{0} résultats"), `PaletteNoResultsText`, `ShortcutsCategoryLabel`. Shortcut descriptions: `PaletteShortcutDescription`, `SettingsShortcutDescription`, `HomeShortcutDescription`. Shortcut descriptions are resource-key references (not literal strings) on `ShortcutRegistration.DescriptionKey` so the "shortcuts" reference view localises the description column per user culture. `CanonicalKeysHaveFrenchCounterparts` parity test auto-covers the new keys. | One parameterised `{0}` (result count) — all other copy is fixed. EN + FR at v0.1 per UX-DR60. The DescriptionKey indirection avoids baking English strings into registration call sites — an adopter registering their own shortcut provides a resource key, not a string literal. | Task 9.1, 9.2, 10.11; AC2, AC5, AC6 |
| D15 | **Screen-reader result-count announcement uses a visually-hidden `role="status" aria-live="polite" aria-atomic="true"` region inside the palette body** — same pattern as Story 3-3 D20 `FcDensityAnnouncer`. The region's text content is `string.Format(Localizer["PaletteResultCountTemplate"].Value, results.Length)`. Updated on every `PaletteResultsComputedAction` via state subscription. **Mutation-timing guarantee (tight)**: assistive tech (NVDA, JAWS) announces live-region changes on DOM **mutation**, NOT on initial content present at insertion. Therefore on palette open the live region renders with **empty text**; after the first `PaletteResultsComputedAction` reducer-pass completes, a second render tick — scheduled via `await Task.Yield(); StateHasChanged();` inside `FcCommandPalette.OnAfterRenderAsync(firstRender: true)` — populates the region, firing a genuine mutation that SR engines pick up. This reverses the earlier "first render is NOT skipped" language: technically SR users get an announcement immediately after open, but the DOM path is "empty → populated" across two ticks, which is what triggers the announce. Subsequent keystroke-driven `PaletteResultsComputedAction` mutations flow through naturally because the element is already mounted. The `FcDensityAnnouncer` (Story 3-3 D20) used skip-first because density is steady-state at app boot; here the palette is per-open ephemeral and needs the announce-on-open semantic. **Verified-AT matrix for v1.0**: NVDA + Chrome, NVDA + Edge, JAWS + Edge (Windows). VoiceOver + Safari (macOS), VoiceOver + iOS, TalkBack + Chrome (Android) are **NOT verified in 3-4** — the empty-to-populated mutation pattern is a Chromium-family + Windows-AT well-trodden idiom; Safari/VoiceOver may announce with different timing or may require a secondary mutation (`aria-busy` toggle) per anecdotal reports. Documented as **G16** Known Gap (Story 10-2 accessibility CI gate) + surfaced in the per-AC screen-reader smoke test. **Anti-regression inline comment (required at the two-render-ticks choreography site)**: the `_liveRegionText` field declaration + the `OnAfterRenderAsync(firstRender: true)` body that schedules `await Task.Yield(); StateHasChanged();` MUST carry the comment `// D15 empty-then-populate a11y choreography — DO NOT refactor to render populated on first tick. AT engines (NVDA, JAWS) announce aria-live region changes on DOM mutation, NOT on initial content; rendering populated skips the announce. The two-tick sequence (empty first paint → Task.Yield() → populated second paint) forces the mutation. Removing this would silently break SR users — there is no automated test that catches the regression on non-tested ATs.` This mirrors the D16 anti-regression comment pattern for `IBadgeCountService` nullable DI — decision-doc footnotes are insufficient against drive-by simplification refactors. | Screen-reader users need parity with sighted-user experience (result count changing with each keystroke). The empty-then-populate choreography is the standard a11y idiom for live regions that render alongside a dialog open — NVDA/JAWS polling-on-mutation is a browser behaviour, not something we can argue with. Explicit because Sally's party-mode review caught that the previous "first render is NOT skipped" phrasing would have shipped a silent-to-SR palette. The verified-AT-matrix narrowing is a post-party-mode honesty edit: shipping "aria-live works everywhere" without having verified macOS/iOS/Android ATs is an implicit claim we cannot make; G16 tracks the gap explicitly so Story 10-2 picks it up in the accessibility CI gate pipeline. | Task 5.2, 5.5, 10.7b, 10.7c; AC5 |
| D16 | **`IBadgeCountService` is consumed via `IServiceProvider.GetService<IBadgeCountService>()` in `FcPaletteResultList.OnInitialized`.** `[Inject] private IServiceProvider ServiceProvider { get; set; } = default!;` → `_badgeCounts = ServiceProvider.GetService<IBadgeCountService>();`. `GetService` returns `null` when unregistered (unlike `GetRequiredService` which throws — which is what Blazor's `[Inject]` uses under the hood, so `[Inject] IBadgeCountService? BadgeCounts` would NOT tolerate an unregistered service). Badge render path: `@if (_badgeCounts is not null && result.Category == PaletteResultCategory.Projection && _badgeCounts.Counts.TryGetValue(result.ProjectionType, out var count)) { <FluentBadge Intent="Info">@count</FluentBadge> }` — absent service means the badge element is not rendered, not a placeholder. No try-catch, no shim — the null return from `GetService` IS the feature switch. **Anti-regression inline comment (required at consumption site)**: the field declaration AND the `OnInitialized` body MUST carry the comment `// ADR-044 / D16 — IBadgeCountService is optional. DO NOT refactor to [Inject] IBadgeCountService? because Blazor [Inject] resolves via GetRequiredService and will throw when the service is unregistered (pre-Story-3-5 state).` The comment survives future "simplification" refactors that would silently reintroduce the throw. | Avoids a Story 3-5 / Story 3-4 circular-dep where 3-4 would need a shim `IBadgeCountService` registration just to pass tests. `IServiceProvider.GetService<T>()` is the standard .NET DI escape hatch for truly optional services (Microsoft docs pattern) — Blazor's `[Inject]` attribute always resolves via `GetRequiredService` under the hood regardless of the target property's nullability. Story 3-5 registers `services.AddScoped<IBadgeCountService, BadgeCountService>()` and the palette picks it up on the next circuit. The inline-comment requirement is an explicit anti-regression per Winston's party-mode review — a decision-doc footnote is insufficient against drive-by refactors. | Task 6.2, 10.8; AC7 |
| D17 | **Palette dialog keyboard routing: the dialog handles its OWN keys — Escape, ArrowUp, ArrowDown, Enter — via `@onkeydown` on the dialog's root `<div>` wrapped around `FluentSearch`. These keys do NOT go through `IShortcutService`**. Rationale: ARIA pattern for `role="dialog"` + `role="listbox"` specifies the dialog captures these keys natively; routing through a service would risk the dialog handler being replaced by an adopter registration ("adopter bound Escape to something else"). The dialog-local handler is the final authority for intra-dialog keys. `Ctrl+K`, `Ctrl+,`, `g h` DO still go through `IShortcutService` because they are GLOBAL — they fire when the palette is closed too. | Dialog-local keys should stay dialog-local. Mixing them into the shortcut service widens the service's purview beyond its role. The spec itself separates these in UX-DR43 (framework shortcuts) vs the implicit ARIA dialog contract. | Task 5.3, 5.4, 10.7; AC5 |
| D18 | **`FcPaletteTriggerButton` auto-populates `HeaderEnd` ahead of `FcSettingsButton`** when the adopter leaves `HeaderEnd` null. Order: `<FcPaletteTriggerButton /> <FcSettingsButton />`. Symmetric to Story 3-3 D12 (settings button auto-populate) + Story 3-2 D8 (hamburger toggle auto-populate). `FrontComposerShell.HeaderEnd` parameter is UNCHANGED — the auto-populate is a render-time behaviour, not a new parameter. `FrontComposerShellParameterSurfaceTests.verified.txt` remains at 9 parameters. | One button per function, left-to-right visual order (palette primary, settings secondary — per UX spec §82 "palette is the primary navigation method at scale"). Adopter-supplied non-null `HeaderEnd` still wins and replaces both — the escape hatch stays intact. | Task 7.1, 7.2, 10.10; AC2 |
| D19 | **`HFC2108_ShortcutConflict` is a NEW diagnostic ID** at Information severity. Reserved in `AnalyzerReleases.Unshipped.md` per the Story 1-8 G1 discipline. Structured log payload: `{Binding} (normalised), {PreviousDescriptionKey}, {NewDescriptionKey}, {CallSiteHint}` where `CallSiteHint` is the caller's `[CallerFilePath]` + `[CallerLineNumber]` captured on `Register`. Conflict only fires when both registrations are live (replacing a disposed prior registration is silent). | One diagnostic per behavioural category (Story 3-2 D12 precedent). Information severity aligns with the "deliberate adopter override is the expected case" ADR decision (D3). Caller-info captures make the log actionable without a debugger. | Task 1.3, 10.1e |
| D20 | **Stale-result guard: `PaletteClosedAction` cancels `_queryCts` AND the `PaletteResultsComputedAction` reducer no-ops when `IsOpen == false`.** Without this guard, a 150 ms debounce that elapses AFTER the user has closed the palette would land `Results` onto a closed state, and the next palette open would flash stale results before the next query runs. Implementation: (a) in `CommandPaletteEffects.HandlePaletteClosed`, call `_queryCts?.Cancel(); _queryCts?.Dispose(); _queryCts = null;` so any in-flight `Task.Delay` throws `OperationCanceledException` (silently swallowed per D9); (b) in `CommandPaletteReducers.OnPaletteResultsComputed`, guard the state transition: `if (!state.IsOpen) return state; return state with { Results = action.Results, SelectedIndex = 0, LoadState = Ready };` — defensive against the race where `Cancel()` in (a) misses by a thread-scheduling window. Belt + braces: effect cancels the upstream work, reducer refuses the downstream assignment. The `IsOpen` check in the reducer is NOT a purity violation — it reads `state.IsOpen`, not external state. | Amelia's party-mode review surfaced this race: the debounce cancel does NOT automatically cancel the dispatched action, and the reducer as originally written (`state with { Results = action.Results, ... }`) would blindly overwrite. Two guards at different layers is the idempotency-pair pattern (same rationale as D12 idempotent-open + state `IsOpen` field). Cost is two lines of code; the alternative is a flash-of-stale-results bug that only surfaces on slow CI runners under load — i.e., the worst kind of production-only race. | Task 3.2, 3.2a, 10.4b, 10.4c; AC3, AC5 |
| D21 | **Command-activation route contract: canonical `{CommandName}` URL segment is `kebab-case(PascalCase TypeName)` and the existence of the FullPage route is validated at dev-time via `IFrontComposerRegistry` manifest scan — not at navigation time.** When the palette activates a `Category == Command` result, it navigates to `/domain/{boundedContext}/{kebab-command-name}` (extending Story 2-2's FullPage convention with the bounded-context segment to prevent cross-context name collision — e.g., `IncrementCommand` exists in both `Counter` and `Commerce`). The kebab transform lives in `src/Hexalith.FrontComposer.Shell/Routing/CommandRouteBuilder.cs` — a new tiny file exposing `public static class CommandRouteBuilder { public static string KebabCase(string pascal); public static string BuildRoute(string boundedContext, string commandTypeName); }`. Co-location with `ShortcutBinding` was the original choice for "convenience" (D21 rejected alternative below) but was reversed post-party-mode: kebab routing is a command-routing concern, NOT a keyboard-shortcut concern; putting it in `ShortcutBinding` conflates two distinct responsibilities and makes the file confusing for the next maintainer. The one-extra-file cost is trivial compared to semantic clarity. `HasFullPageRoute(string commandTypeName) : bool` is exposed via the **companion interface `IFrontComposerFullPageRouteRegistry`** plus an extension method `FrontComposerRegistryExtensions.HasFullPageRoute` (ratified 2026-04-21 — see spike-notes §D21) rather than appended directly to `IFrontComposerRegistry`. Rationale for the companion-interface pattern: appending to `IFrontComposerRegistry` would be a breaking change for pre-3-4 adopter implementers; the companion surface + extension method lets the framework query reachability when adopters opt in, while staying permissive when they do not. The palette effect consults `registry.HasFullPageRoute(name)` via the extension BEFORE dispatching activation: if `HasFullPageRoute` returns false, the result is NOT added to the palette results set at all — activation is unreachable. Dev-time validation: `FrontComposerRegistry.ValidateManifests()` runs in `OnStart` (Story 1-6's startup validator) and throws `HFC1601_ManifestInvalid` at error severity when a Command manifest entry lacks a matching FullPage route generator. Build never ships a broken palette entry. | Amelia's party-mode review flagged this as a blocker: absent manifest validation, a palette entry targeting `SubmitOrder` would silently 404 at runtime because the FullPage route didn't generate. Route-exists check at registry enumeration is O(1) per entry. Bounded-context in the URL is inherited from Story 2-2's routing convention (§539-547 of architecture); the kebab-case transform is the single normalization needed. Moving the helper out of `ShortcutBinding` into its own `CommandRouteBuilder.cs` is cosmetic but improves code discoverability — a developer searching "where is kebab-case?" expects it near routing, not near keyboard shortcuts. | Task 3.4, 3.4a, 10.4c; AC5 |
| D22 | **`ShortcutService` and `CommandPaletteEffects` take `TimeProvider` as a constructor dependency; default is `TimeProvider.System`; tests inject `Microsoft.Extensions.Time.Testing.FakeTimeProvider`.** Applies to the D4 chord-timeout timer AND the D9 150 ms debounce — both become deterministically testable without wall-clock waits. Implementation: `public ShortcutService(TimeProvider timeProvider, ILogger<ShortcutService> logger) { _timeProvider = timeProvider ?? TimeProvider.System; }`; chord timer becomes `_timeProvider.CreateTimer(ClearPendingFirstKey, null, TimeSpan.FromMilliseconds(1500), Timeout.InfiniteTimeSpan)`; debounce becomes `await Task.Delay(150, _timeProvider, ct)`. Test fixture: `var fakeTime = new FakeTimeProvider(); fakeTime.Advance(TimeSpan.FromMilliseconds(1499));` — asserts on timer state WITHOUT the test suite sitting on real time. DI registration is automatic (`TimeProvider.System` is registered by default in modern ASP.NET Core hosts); for xUnit tests, `services.AddSingleton<TimeProvider>(new FakeTimeProvider())`. | Murat's party-mode review named this as the single most under-specified risk: without `TimeProvider`, the debounce + chord boundary tests inevitably reach for `Task.Delay(<real ms>)` and flake on CI (45% green on first run per risk estimate). `TimeProvider` is the BCL-blessed abstraction for this exact pattern (shipped in .NET 8). One decision covers both timers in 3-4 and unblocks Story 3-6+ if session persistence needs timer-driven expiry. | Task 1.2, 1.2b, 3.1, 10.1, 10.1c, 10.1d, 10.4b; AC1, AC3 |
| D23 | **Shortcut reference is discoverable via a synthetic "Keyboard Shortcuts" entry in the default Commands category AND via the `shortcuts` / `?` / `help` / `keys` / `kb` alias queries.** When `HandlePaletteOpened` populates the initial result set (ADR-043 pre-population), it prepends a synthetic `PaletteResult(Category: Command, DisplayLabel: Localizer["KeyboardShortcutsCommandLabel"], BoundedContext: "", RouteUrl: null, CommandTypeName: "@shortcuts", Score: 0, IsInCurrentContext: false)` with the special sentinel `CommandTypeName == "@shortcuts"`. Activating this result (Enter / click) dispatches `PaletteQueryChangedAction(correlationId, "shortcuts")` rather than navigating — it refills the palette with the shortcut-reference view (AC6). The query-aliases `?`, `help`, `keys`, `kb`, `shortcut` (without the trailing s) all score as exact-prefix matches against the synthetic entry via a small lookup table in `CommandPaletteEffects.ResolveShortcutAliasQuery(string query)` (returns `"shortcuts"` when the query matches any alias, else returns the input unchanged — applied BEFORE debounced scoring). Two new resource keys: `KeyboardShortcutsCommandLabel` ("Keyboard Shortcuts" / "Raccourcis clavier") + `KeyboardShortcutsCommandDescription` ("View all keyboard shortcuts" / "Afficher tous les raccourcis clavier"). **Alias-shadowing precedence (explicit)**: the five aliases (`?`, `help`, `keys`, `kb`, `shortcut`) BYPASS scoring entirely — typing any of them shows the shortcut reference view with ZERO projections / commands in the result set. If an adopter ships a projection or command whose name exact-matches an alias (e.g., a `Help` projection in a domain context), palette users cannot reach it by typing `help` / `Help` / `HELP` — the shortcut-reference view pre-empts. **Mitigation patterns (v1)**: (a) adopters rename the projection (e.g., "Help" → "Support"); (b) adopters document the aliases for their users; (c) users navigate via direct URL or header nav. **Future extensibility (v1.x)**: an adopter-side `IPaletteAliasProvider` extension point would let adopters disable or re-map specific aliases — tracked as Known Gap G17. Total resource-key count rises 12 → 14 (28 strings across EN + FR). | Sally's party-mode review caught that the original `"shortcuts"`-only query was a power-user easter egg — new users who wanted to learn the keyboard system had no discoverable path. A synthetic default entry + alias list solves the WCAG 3.3.2 "Labels or Instructions" gap without requiring a permanent visible tip row. The alias table is small and additive; adopters who want their own discoverability words can ship an adopter-side `PaletteResult` entry via a future extensibility hook (v1.x). The explicit alias-shadowing note was a gap-fill post-party-mode: without it, adopters would ship projections named `Help` and users would silently fail to reach them — a hard-to-diagnose "palette is broken" report. Documented shadowing converts a silent bug into a known constraint with mitigation patterns. Total resource-key count rises 12 → 14 (28 strings across EN + FR). | Task 3.3, 3.3a, 9.1, 9.2, 10.4b; AC6 |
| D25 | **Mac-parity `meta+k` and `meta+,` bindings + `FluentDialogBody` dialog host — post-implementation addenda (DN4(A) ratified 2026-04-21).** The registrar ships five shell-default bindings, not three: `ctrl+k`, `ctrl+,`, `g h`, `meta+k`, `meta+,`. `meta+*` covers macOS where the Command key is the canonical modifier; shipping only `ctrl+*` would leave Mac users with no native-muscle-memory shortcut to open the palette/settings. The extra two bindings cost two extra rows in `IShortcutService.GetRegistrations()` (surfaced in the `shortcuts` query view) and five extra resource-key lookups at registrar-boot time — negligible. The shortcut-description keys are shared across `ctrl+*` and `meta+*` variants (`PaletteShortcutDescription`, `SettingsShortcutDescription`). Mac-parity was discovered post-implementation during Pass-3 review; spec §scope line 22 originally read *"three v1 shell shortcuts"* (a simplification) and has been amended to *"five v1 shell shortcuts (including Mac-parity `meta+*` variants)"* on 2026-04-21 pass-4. **Dialog host**: `FcCommandPalette` renders as `FluentDialogBody` rather than the spec's original `IDialogContentComponent`. `FluentDialogBody` exposes the dialog's native title slot + close button, which were authored separately under `IDialogContentComponent`; the consolidation reduced palette-specific dialog chrome by ~40 LOC and matches Fluent UI v5 RC2's preferred dialog-content shape. No consumer-visible surface change. | Mac parity: pre-mortem F5 ("Mac user reports Ctrl+K doesn't exist on keyboard"). Dialog host: Fluent UI v5 RC2 authored-component convergence post-upgrade. Ratified post-implementation because both deviations surfaced during implementation and the alternative (revert to 3 bindings + `IDialogContentComponent`) would regress user-visible behaviour for no quality gain. | Task 1.4 (registrar surface), 7.1 (palette host), 10.1 (registrar tests); AC1, AC2 |
| D26 | **`BoundedContextChangedAction` + `BoundedContextRouteParser` + shell `LocationChanged` wiring shipped in 3-4 — post-implementation addendum (DN4(A) ratified 2026-04-21).** Story §scope line 22 originally implied the `NavigationState.CurrentBoundedContext` field would be populated in Story 3-6 (session persistence / context restoration); 3-4 needs the field to exist but not necessarily be route-driven. During implementation it became clear that ADR-043's contextual-mode bonus is dead weight without a real route-driven `CurrentBoundedContext` — the effect would apply `+0` to every candidate, producing no palette UX difference from removing the bonus entirely. Shipping the parser + action + reducer + `LocationChanged` subscription in 3-4 makes ADR-043 observable. `BoundedContextRouteParser.Parse` uses a lenient fallback (see C3-D2 → D28) — `/counter/counter-view` resolves to BC `"counter"` via the non-`/domain/*` path; `/domain/commerce/submit-order-command` resolves to `"commerce"` via the `/domain/` path. Lenient fallback matches the Counter.Web sample-adopter convention and the shipped `BoundedContextRouteParserTests` theory data. Case-sensitivity: `ToLowerInvariant` applied at parse time; reducer comparison is `Ordinal` against the already-lowercased state. Story 3-6 will later extend the action payload with tenant/user scope (DN7 placeholder) but does not replace the route-watching wiring. | Without a route-driven BC populator, ADR-043 is unobservable. Shipping the parser now preserves the ADR's value and reduces Story 3-6's scope to scope/session concerns only. | Task 2.1a (Navigation state field), 3.1 (effect consumption), 10.4b (tests); AC3 |
| D27 | **Diagnostic-ID allocation post-review reconciliation — `HFC2109_ShortcutHandlerFault` (Warning), `HFC2110_PaletteScoringFault` (Warning), `HFC2111_PaletteHydrationEmpty` (Information) — ratified 2026-04-21 pass-4 (C3-D1(a)).** Story §scope line 43 originally specified two diagnostics sharing `HFC2109` (a clear spec typo) plus an *"existing HFC2106 additive reason-code extension (`Reason=Tampered` joining Empty/Corrupt)"* for palette hydrate failures. Shipped code allocates three distinct IDs: `HFC2109` → handler-fault; `HFC2110` → scoring fault; `HFC2111` → palette-hydrate-empty / corrupt / tampered (reason code carried in the log payload). Rationale: (a) two diagnostics cannot share an ID — a structured-log query filtering by `{EventId = 2109}` must return exactly one event class; (b) coupling palette-hydrate reasons to `HFC2106` (which covers generic `IStorageService` persistence-skipped failures across Storage, Navigation, Density, Palette) would dilute `HFC2106`'s semantics for operators triaging any one concern. `HFC2111` keeps the palette's hydrate-failure telemetry distinct and searchable. Precedent: pass-3 sprint-status already ratified a similar renumber (HFC2107→HFC2108). Ratified under C3-D1(a) on 2026-04-21 pass-4 via the chunk-3 re-review; no code change, spec addendum only. | Spec typo + semantic-coupling concern both argue for the renumber. The cost is one critical-decisions addendum entry + one G-row; benefit is a coherent diagnostic surface. | Task 1.3, 10.1e; AC1, AC3 |
| D28 | **`BoundedContextRouteParser.Parse` lenient first-segment fallback — ratified 2026-04-21 pass-4 (C3-D2(a)).** `Parse` resolves BC via a two-branch strategy: (a) `/domain/{bc}/{...}` path with ≥3 segments → BC is `segments[1].ToLowerInvariant()`; (b) any other path with ≥2 segments → BC is `segments[0].ToLowerInvariant()`. Branch (b) is the *lenient fallback* that enables adopter-owned BC routes without the `/domain/` prefix ceremony — the Counter.Web sample's `/counter/counter-view` resolves to BC `"counter"` under this branch. Pass-2 edge-hunter suggested anchoring strictly to `/domain/` only; the Pass-3 dismissal implicitly accepted the lenient behaviour but did not formally document the decision. This D28 entry is the formal ratification. `BoundedContextRouteParserTests` codifies the contract with inline-data for both branches; the test file is the source of truth. Case-sensitivity: `ToLowerInvariant` applied at parse time so `/Counter/...` and `/counter/...` resolve identically. Null return: 2-segment `/domain/{bc}` landing (no command/projection sub-path) and 1-segment `/login` paths — by design, contextual bonus does not fire on a bare BC landing. | Counter.Web + any adopter-shipped BC routes benefit from the lenient fallback; forcing `/domain/` everywhere would regress the sample and require pre-existing adopters to rewrite route conventions. Explicit documentation converts the implicit behaviour into a contract that future maintainers cannot accidentally break. | Task 2.1a, 10.4b (added `/admin/users` + `/help/topic` theory data on 2026-04-21 pass-4); AC3 |
| D29 | **`FrontComposerShortcutRegistrar` discards `IDisposable` handles from `IShortcutService.Register(...)` — ratified 2026-04-21 pass-4 (C3-D3(a)).** The registrar calls `_ = shortcuts.Register(binding, descKey, handler)` five times and does not store the returned tokens. Cleanup invariant: (a) `RegisterShellDefaultsAsync` is idempotency-guarded via `Interlocked.Exchange(ref _registered, 1)` (D24), so the same registrar instance cannot re-register; (b) `ShortcutService.Dispose` clears `_entries` at circuit teardown, cleaning up all registrations regardless of whether the registrar stored tokens. The alternative — storing the tokens in a `CompositeDisposable` + making the registrar itself `IDisposable` — would shift the registrar's DI lifetime contract (Scoped → IAsyncDisposable cascade ordering) for no runtime benefit in v1. Hot-reload (adopter re-running DI setup while the circuit is live) would leak without token tracking; this is explicitly a v1.x concern per G26 and is out of scope for v1. XML doc on `FrontComposerShortcutRegistrar` carries the explicit disclosure so maintainers do not mistake the discard for an oversight. | Public-API contract (Register returns IDisposable) remains honest for adopter callers; the registrar's discard is a framework-specific optimisation enabled by the idempotency guard + scope-disposal cleanup. Documenting the convention prevents a future refactor from mistakenly "fixing" the discard in a way that breaks the Scoped lifetime contract. | Task 1.4 (registrar XML-doc), 10.1 (registrar tests); AC1 |
| D24 | **`FrontComposerShortcutRegistrar.RegisterShellDefaultsAsync()` is idempotent — guarded by a `bool _registered` instance field.** Body opens with `if (_registered) return; _registered = true;` BEFORE any `IShortcutService.Register(...)` call. Rationale: `FrontComposerShell.OnAfterRenderAsync(firstRender: true)` is expected to fire exactly once per circuit, but the "firstRender" guarantee is circuit-lifecycle-scoped — under bUnit, hot-reload, and prerender-then-interactive boot sequences the same component instance can see `firstRender: true` multiple times or the registrar can be consumed from multiple locations if adopters fork the shell. Without idempotency, every repeat invocation fires three `Register` calls against the same bindings → three `HFC2108_ShortcutConflict` log entries per reconnect → production log spam + wasted CPU. The flag is instance-scoped (per-Scoped-DI-instance, per-circuit), so a genuine new circuit gets a fresh registrar instance and re-registers correctly. **Thread-safety (ratified 2026-04-21)**: the flag is `int` guarded by `Interlocked.Exchange(ref _registered, 1)` — originally specified as a plain `bool` on the basis that `FrontComposerShortcutRegistrar` is Scoped-per-circuit (D2 precedent) and Blazor's dispatcher is single-threaded per circuit. Post-review analysis (2026-04-21) upgraded to `Interlocked.Exchange` to harden against prerender → interactive boot paths, bUnit double-init, and potential hot-reload re-entry — scenarios where the same DI instance can see overlapping lifecycle method invocations from orchestration code outside Blazor's dispatcher. Cost is one atomic operation; benefit is that a truly-concurrent double-init cannot allocate two sets of registrations. Earlier text advised against this hardening on single-thread grounds — that advice is superseded by the ratification here. **Fail-safe pattern**: the `Register` calls themselves are also safe under repeat invocation thanks to D3 last-writer-wins semantics — the flag is a belt-and-braces optimisation against log noise, NOT a correctness requirement. | Pre-mortem analysis and failure-mode analysis both surfaced this gap independently: P3 (HFC2108 log spam on reconnect) + FM-5 (repeated `OnAfterRenderAsync` → duplicate Register). The fix is two lines of code; the alternative is a drip of `HFC2108` Information logs in production every time a user reconnects — noise that makes true-positive conflict signals harder to triage. Instance-field is preferred over `Lazy<Task>` because the registrar has no return value worth caching; a bool is adequate. Single-thread rationale added post-elicitation Rubber Duck pass: a staff-eng-level reader naturally asks "why not Interlocked?" — the answer is "Blazor's threading model makes it unnecessary", and that answer deserves to live in the rationale rather than as tribal knowledge. | Task 1.4, 1.4a, 10.1f; AC1 |

---

## Cross-story contract table (L01)

| Contract | Producer (3-4) | Consumer | Binding |
|---|---|---|---|
| `IShortcutService` interface + `ShortcutRegistration` record + `ShortcutBinding.Normalize` | 3-4 | Story 3-5 (badge hotkeys if any); any adopter that registers custom shortcuts; Story 6-5 (dev-mode overlay's `Ctrl+Shift+D` binding migrates to this service) | Interface lives in `Contracts/Shortcuts/`. Append-only — new methods require `default` implementations. Binding grammar is frozen — changing normalisation semantics is a v2 break. |
| `FrontComposerCommandPaletteState` shape + action names | 3-4 | Any adopter that wants to hydrate palette state from a custom source (e.g., pre-seeding Recent from server-side history) | State record + action records are `public` — shape changes are v2 breaks. `PaletteHydratedAction(RecentRouteUrls)` is the single adopter-overridable entry. |
| `PaletteResult` record + `PaletteResultCategory` enum | 3-4 | Future adopter-extensibility for custom categories (out-of-scope, v1.x) | Ordinal-stable enum values (`Projection=0, Command=1, Recent=2, Shortcut=3`). Appendable — new category values added at end. |
| `PaletteScorer.Score(query, candidate)` pure function | 3-4 | Any adopter that wants to re-use the scorer outside the palette (e.g., a custom filter bar in a DataGrid) | `public static` — signature frozen. Internationalisation (v2) adds an overload with `CultureInfo` parameter; existing overload stays `ToLowerInvariant` semantics. |
| `IBadgeCountService` consumption via nullable injection | 3-4 (consumer) | Story 3-5 (producer registers the service) | Nullable DI contract — 3-4's palette works whether service is registered or not. No Story 3-5 handshake changes required; registering the service is the entire contract. |
| Story 3-3 `Ctrl+,` migration → `IShortcutService.Register("ctrl+,", ...)` | 3-4 | (closes) Story 3-3 D16 migration contract — `FrontComposerShell.HandleGlobalKeyDown` inline branch removed; `CtrlCommaSingleBindingTest` deleted | Explicit retirement per 3-3 D16 — Task 8.4 deletes the test file. Validation: `FrontComposerShellTests.CtrlCommaInvokesRegisteredShortcut` replaces the deleted test; the user-visible behaviour is unchanged. |
| `FrontComposerShell.HeaderEnd` auto-populate extension to include `<FcPaletteTriggerButton /> <FcSettingsButton />` | 3-4 (extends Story 3-3 D12) | Adopters today (Counter.Web gets both buttons for free); any future Story that adds a third auto-populated button appends after Settings | Opt-out via non-null `HeaderEnd` fragment suppresses both auto-populated buttons — adopter wins. Applies regardless of registry state. |
| `RecentRouteVisitedAction` dispatched on every palette activation | 3-4 | Story 3-6 (session persistence may want to observe the same signal to update the "last visited" field on session blob) | Additive Fluxor action — append-only. Story 3-6 subscribes via its own effect listener; no contract-level change required. |
| `HFC2108_ShortcutConflict` diagnostic ID | 3-4 | Story 9-4 (analyzer replacement scans registrations at build time; emits HFC3xxx from an analyzer at the call site) | Diagnostic ID frozen. Analyzer-emitted HFC3xxx is a separate, higher-severity ID in Story 9-4; runtime `HFC2108` stays as the "adopter deliberately overrode" channel. |
| `IFrontComposerFullPageRouteRegistry.HasFullPageRoute(string commandTypeName) : bool` companion interface + `FrontComposerRegistryExtensions.HasFullPageRoute` extension method + `HFC1601_ManifestInvalid` error diagnostic (D21) | 3-4 (consumer) — Story 2-2 (producer) owns the registry contract | Story 2-2's registry implementation MUST expose `HasFullPageRoute` by Story 3-4 merge. If Story 2-2 has not shipped the method, 3-4 blocks on a Story 2-2 amendment PR. `HFC1601` fires from `FrontComposerRegistry.ValidateManifests()` in `OnStart` — never from the palette runtime path. |
| `TimeProvider` injection into `ShortcutService` + `CommandPaletteEffects` (D22) | 3-4 | Story 3-6 (if session-expiry timers need deterministic tests); Story 6-5 (dev-overlay auto-close timer) | Constructor-parameter contract — default `TimeProvider.System`, test fixtures inject `FakeTimeProvider`. Append-only: future stories that introduce timers SHOULD adopt the same pattern for test determinism. |
| Synthetic "Keyboard Shortcuts" palette entry with `CommandTypeName == "@shortcuts"` sentinel (D23) | 3-4 | Adopters who want to add their own alias-triggered palette entries (v1.x) learn the sentinel pattern; Story 3-5 synthetic "Action Queue" entry (if needed) mirrors it | Sentinel prefix `@` is RESERVED for framework-synthetic palette entries (never a valid generated command route); adopter-side palette entries MUST NOT use the `@` prefix. The five shortcut aliases (`?`, `help`, `keys`, `kb`, `shortcut`) SHADOW exact-match projection / command names — adopters shipping projections whose names collide with these aliases must rename or wait for v1.x `IPaletteAliasProvider` extensibility (tracked as G17). Sentinel + alias table are frozen for v1. |
| `HFC2109_ShortcutHandlerFault` diagnostic ID (D1 handler-exception policy) | 3-4 | Operators / telemetry pipelines — the diagnostic ID is consumed by log-ingestion dashboards. Story 9-4 analyzer continues to emit HFC3xxx at build time; HFC2109 is runtime-only. | Diagnostic ID frozen at Warning severity. Structured payload shape `{Binding, DescriptionKey, ExceptionType, ExceptionMessage}` is appendable (new fields can be added) but existing fields are frozen. |
| `FrontComposerShortcutRegistrar` idempotency (D24 — `bool _registered` instance flag) | 3-4 | Adopters who fork the shell or call `RegisterShellDefaultsAsync` from a non-standard host MUST re-enter via a fresh `FrontComposerShortcutRegistrar` instance to re-register shell defaults — the flag is per-instance, not per-process. | Internal behavioural invariant of the registrar class; not a public API. Adopters typically never touch the registrar directly; invariant is tested via `FrontComposerShortcutRegistrarTests.RegisterShellDefaultsAsync_IsIdempotent_WithinSameInstance`. |

---
