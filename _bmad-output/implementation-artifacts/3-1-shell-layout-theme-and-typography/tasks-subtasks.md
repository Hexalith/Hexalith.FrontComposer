# Tasks / Subtasks

Each checkbox is a stand-alone executable subtask. Do not merge. When a task lists an AC, the subtask implementation evidences that AC; the corresponding Task 10 test locks the behaviour.

## Task 0 — Prereq verification + JS interop spike (≤ 60 min)

- [ ] **0.1** Run `dotnet test` on the current `main` branch — capture the baseline test count (expected ~542). Record in Dev Agent Record's Change Log as the pre-3-1 baseline. Fail-fast gate: if current `main` is red, abort and escalate before starting.
- [ ] **0.2** Verify Fluent UI v5 API surface via MCP tool (or direct source inspection if MCP reports version skew): confirm (a) `FluentLayout` + `FluentLayoutItem` accept `Height` / `Width` parameters, (b) `IThemeService.SetThemeAsync(ThemeSettings)` overload exists and accepts `(string Color, double HueTorsion, double Vibrancy, ThemeMode Mode, bool IsExact)`, (c) `ThemeMode` enum exposes `Light | Dark | System` values (confirm spelling — if FCv5 uses `Default` instead of `System`, adjust D23 accordingly), (d) `Typography` enum exposes `Title1, Subtitle1, Title3, Subtitle2, Body1Strong, Body1, Body2, Caption1`. Document any mismatches as an escalation note BEFORE coding Task 1.
- [ ] **0.3** Inspect Story 2-4 `FcLifecycleWrapper.razor[.cs]` for inline-string vs `IStringLocalizer` usage. If lifecycle copy is inline, file a Known Gap entry (G2 — defer to Task 4.5) noting that the FR `.resx` keys will exist from 3-1 but the wrapper won't consume them until a non-blocking follow-up PR. Do NOT modify `FcLifecycleWrapper` in this story.
- [ ] **0.4** Spike: one-off Blazor page proving `IJSRuntime.InvokeAsync<string[]>("eval", "Object.keys(localStorage)")` round-trip works in a Blazor Server (Interactive) circuit. Discard the spike page after — goal is confidence that the LRU fetch strategy (D16) works. Time-box 20 min; fall back to per-index iteration if the shortcut fails (documented in Known Gap G5 if we fall back).
- [ ] **0.5** `IStorageService` consumer audit (ADR-030 Step 3). Run `Grep -r "IStorageService"` across `src/` and `tests/` and list every consumer with its DI lifetime. Any Singleton consumer must migrate to Scoped (or `IServiceScopeFactory`) in Task 2.x. Expected consumers today: `ThemeEffects` (Scoped), `DensityEffects` (Scoped), DataGrid state code (verify). Record the findings in Dev Agent Record Debug Log; escalate if a Singleton capture is found that was not anticipated.
- [ ] **0.6** **Scoped CSS bundle filename verification (ADR-034 hard gate).** Run `dotnet build src/Hexalith.FrontComposer.Shell/Hexalith.FrontComposer.Shell.csproj` and enumerate the files under `src/Hexalith.FrontComposer.Shell/bin/**/_content/Hexalith.FrontComposer.Shell/`. Confirm `Hexalith.FrontComposer.Shell.styles.css` is present (the RCL convention `{AssemblyName}.styles.css`). If the emitted filename differs (future Blazor tooling change), ESCALATE before any AC4-dependent task runs — do NOT silently adjust Task 9.2's `<link>` guess. Record the exact path in Dev Agent Record Debug Log so Task 9.2's `<link href>` matches byte-for-byte. (D29, ADR-034)

## Task 1 — Contracts additions

- [ ] **1.1** In `src/Hexalith.FrontComposer.Contracts/FcShellOptions.cs` append 4 properties — Order: `AccentColor` (default `"#0097A7"`, `[RegularExpression(@"^#[0-9A-Fa-f]{6}$", ErrorMessage = "AccentColor must be a 6-digit hex color like '#0097A7'.")]`), `LocalStorageMaxEntries` (default `500`, `[Range(50, 10_000)]`), `DefaultCulture` (default `"en"`, `[RegularExpression(@"^[a-z]{2}(-[A-Z]{2})?$", ErrorMessage = "DefaultCulture must be a BCP-47 language tag like 'en' or 'en-US'.")]`), `SupportedCultures` (`IReadOnlyList<string>`, default `new[] { "en", "fr" }`). Add XML doc referencing D1, D14. (AC2)
- [ ] **1.2** Create `src/Hexalith.FrontComposer.Contracts/Rendering/Typography.cs` as `public static class Typography` with 9 `public static readonly Microsoft.FluentUI.AspNetCore.Components.Typography` fields per D2. Create `src/Hexalith.FrontComposer.Contracts/Rendering/TypographyStyle.cs` exposing `public const string CodeFontFamily = "'Cascadia Code', 'Cascadia Mono', Consolas, 'Courier New', monospace"`. Ensure Contracts.csproj has `<PackageReference Include="Microsoft.FluentUI.AspNetCore.Components" />` (already present via Shell; confirm the public API surface). Add XML doc referencing D2, D11 and the living-table policy. (AC5)
- [ ] **1.3** In `src/Hexalith.FrontComposer.Contracts/Diagnostics/FcDiagnosticIds.cs` add `public const string HFC2105_StoragePersistenceSkipped = "HFC2105";` and `public const string HFC2106_ThemeHydrationEmpty = "HFC2106";` + XML doc explaining both are Information severity, runtime-only (no analyzer emission, no `AnalyzerReleases.Unshipped.md` entry).
- [ ] **1.4** Create `src/Hexalith.FrontComposer.Contracts/ContractsMetadata.cs` with `public const string TypographyMappingVersion = "3.1.0"` if not already present. Referenced by `TypographyConstantsTests.cs` per D11.

## Task 2 — LocalStorageService

- [ ] **2.1** Create `src/Hexalith.FrontComposer.Shell/Infrastructure/Storage/LocalStorageService.cs` implementing `IStorageService`. Inject `IJSRuntime`, `IOptions<FcShellOptions>`, `TimeProvider`, `ILogger<LocalStorageService>`. Internal: `ConcurrentDictionary<string, long> _lruTimestamps` (NOT `Dictionary` — Pre-mortem finding #2: Blazor Server circuit async continuations can interleave `SetAsync` calls, and `Dictionary` mutation during `OrderBy(kvp => kvp.Value).First()` scan throws `InvalidOperationException: Collection was modified during enumeration`. `ConcurrentDictionary` allows lock-free reads during the LRU scan; use `ToArray()` or `OrderBy` over the concurrent snapshot for eviction selection), `Channel<PendingWrite> _writeChannel` (unbounded-single-reader per D9), `CancellationTokenSource _cts`. Implement all 5 methods per D9/D15/D16.
- [ ] **2.2** Create `src/Hexalith.FrontComposer.Shell/Infrastructure/Storage/LocalStorageDrainWorker.cs` as an internal partial of `LocalStorageService` (or sibling type) hosting the drain loop started in the constructor via `_ = Task.Run(DrainLoopAsync, _cts.Token)`. `DrainLoopAsync` reads from `_writeChannel.Reader`, calls `_js.InvokeVoidAsync("localStorage.setItem", write.Key, write.SerializedValue)`, ConfigureAwait(false). `FlushAsync` awaits `_writeChannel.Writer.Complete()` (after setting a flag to stop accepting new writes) then `await _drainTask`.
- [ ] **2.3** In `src/Hexalith.FrontComposer.Shell/Extensions/ServiceCollectionExtensions.cs`, migrate `IStorageService` from Singleton to Scoped per ADR-030. Delete the existing line `_ = services.AddSingleton<IStorageService, InMemoryStorageService>()` (currently at line 135), THEN call `services.RemoveAll<IStorageService>()` immediately before the new registration to guarantee no stale descriptor survives (defence-in-depth against adopters pre-registering), THEN register `_ = services.AddScoped<IStorageService, LocalStorageService>()` — use `AddScoped`, NOT `TryAddScoped`, so the registration is authoritative. Document the breaking lifetime change (Singleton → Scoped) in the method's XML `<remarks>` with a migration note for adopters ("any Singleton consumer capturing IStorageService must move to Scoped or take `IServiceScopeFactory`") AND include the SemVer note: "3-1 ships as v0.2.0-preview per ADR-030 Consequences; RELEASE-NOTES.md documents the lifetime flip as a breaking change." Enable `ServiceProviderOptions.ValidateScopes = true` in Counter.Web's host builder so future regressions fail at boot. Update `Hexalith.FrontComposer.Shell.Tests` `TestHost.ConfigureServices` to explicitly register `services.Replace(ServiceDescriptor.Scoped<IStorageService, InMemoryStorageService>())` for the test host. (AC6, ADR-030)
- [ ] **2.4** Create `AddHexalithFrontComposerQuickstart` sugar extension in `src/Hexalith.FrontComposer.Shell/Extensions/ServiceCollectionExtensions.cs` per D28: `public static IServiceCollection AddHexalithFrontComposerQuickstart(this IServiceCollection services, Action<RequestLocalizationOptions>? configureLocalization = null)` chains `services.AddLocalization().AddHexalithShellLocalization(configureLocalization).AddHexalithFrontComposer()` and returns the collection for further fluent chaining. Idempotent against duplicate `AddLocalization()` calls per ASP.NET Core conventions. XML `<remarks>` document: (a) this is opt-IN sugar; the granular 4-line path stays the non-deprecated primary API; (b) use in combination with `ServiceProviderOptions.ValidateScopes = true` on the host builder (Counter.Web's Program.cs does this). (D28)

## Task 3 — beforeunload.js + JS modules

- [ ] **3.1** Create `src/Hexalith.FrontComposer.Shell/wwwroot/js/fc-beforeunload.js` as an ES module exporting `export function register(dotnetRef)` and `export function unregister(sub)`. `register` attaches `window.addEventListener("beforeunload", handler, { capture: true })` where `handler` calls `dotnetRef.invokeMethodAsync("FlushAsync")` and races it against a 200 ms `setTimeout` — whichever resolves first wins (D17).
- [ ] **3.2** Create `src/Hexalith.FrontComposer.Shell/wwwroot/js/fc-prefers-color-scheme.js` as an ES module exporting `export function subscribe(dotnetRef)` and `export function unsubscribe(sub)`. `subscribe` creates `const mq = window.matchMedia("(prefers-color-scheme: dark)")`, emits the current `mq.matches` value once via `dotnetRef.invokeMethodAsync("OnSystemThemeChangedAsync", mq.matches)`, then attaches `mq.addEventListener("change", e => dotnetRef.invokeMethodAsync("OnSystemThemeChangedAsync", e.matches))` per D23.

## Task 4 — IUserContextAccessor wiring

- [ ] **4.1** In `src/Hexalith.FrontComposer.Shell/State/StorageKeys.cs`, delete `DefaultTenantId` + `DefaultUserId` constants. Keep the two `BuildKey` overloads with their existing signatures. Add an XML `<remarks>` explaining the fail-closed contract per D8. Validate that the two deleted constants have NO references in the codebase (grep; if any exist in tests, update them). (D8)
- [ ] **4.2** Modify `src/Hexalith.FrontComposer.Shell/State/Theme/ThemeEffects.cs`: inject `IUserContextAccessor userContextAccessor` into the primary constructor. In both `HandleAppInitialized` and `HandleThemeChanged`: `string? tenantId = userContextAccessor.TenantId; string? userId = userContextAccessor.UserId; if (string.IsNullOrWhiteSpace(tenantId) || string.IsNullOrWhiteSpace(userId)) { logger.LogInformation("{DiagnosticId}: Theme persistence skipped — null/empty/whitespace tenant or user context.", FcDiagnosticIds.HFC2105_StoragePersistenceSkipped); return; }` then `string key = StorageKeys.BuildKey(tenantId, userId, "theme")`. `HandleAppInitialized` additionally logs `HFC2106` when `stored is null` AFTER the context check passes. The interface is flat per `src/Hexalith.FrontComposer.Contracts/Rendering/IUserContextAccessor.cs:17-31` — `TenantId` and `UserId` are direct properties, there is no `.Current` or nested `User`. `IsNullOrWhiteSpace` is mandated by the interface's XML contract (null / empty / whitespace are semantically equivalent). (D8)
- [ ] **4.3** Modify `src/Hexalith.FrontComposer.Shell/State/Density/DensityEffects.cs` with the same refactor pattern (flat `TenantId` + `UserId` + `IsNullOrWhiteSpace` guard). Use `"density"` as the feature segment.
- [ ] **4.4** Sanity spot-check only: run `Grep -r "IUserContextAccessor"` across `src/` and confirm every consumer already uses the flat `TenantId` / `UserId` properties (2-2's `LastUsedValueProvider` is the reference). Do NOT refactor the accessor interface in this story. The shape is flat and authoritative — earlier drafts of this spec incorrectly assumed `.Current`/`User.Id`; the shape at `IUserContextAccessor.cs:17-31` is the truth.
- [ ] **4.5** (Conditional on Task 0.3) If `FcLifecycleWrapper` uses inline lifecycle copy, add its expected resource keys to `FcShellResources.resx` + `.fr.resx` so Story 2-4 can opt in via a follow-up PR. Do NOT change the wrapper. (D12 backward-compatible seeding)

## Task 5 — FrontComposerShell.razor component

- [ ] **5.1** Create `src/Hexalith.FrontComposer.Shell/Components/Layout/FrontComposerShell.razor` + `.razor.cs` + `.razor.css` (razor-scoped) per D3/D4/D6/D20/D26. Razor markup: `<FluentLayout>` with four `<FluentLayoutItem>` children per AC1. The Header does NOT render the Ctrl+K or Settings placeholder buttons — per D26 they are absent from the 3-1 DOM (stories 3-3 and 3-4 add them via their own markup revisions). The parameter surface (D4) still includes `HeaderStart` + `HeaderEnd` slots that 3-4 / 3-3 can use. Code-behind injects `IOptions<FcShellOptions>`, `IThemeService`, `IState<FrontComposerThemeState>`, `IStringLocalizer<FcShellResources>`. `OnAfterRenderAsync(firstRender: true)` calls `IThemeService.SetThemeAsync(BuildThemeSettings())` (helper maps current `ThemeState` + `Options.AccentColor` to `ThemeSettings`), then imports the `fc-beforeunload.js` module + `DotNetObjectReference.Create(this)` (the shell itself exposes `[JSInvokable] FlushAsync`). `Dispose` unsubscribes + disposes the DotNetObjectReference. CSS: `:host { --layout-header-height: 48px; --fc-color-accent: var(--accent-base-color); --fc-color-neutral: var(--colorNeutralForeground1); ... }` per D13/D20. (AC1, AC2, AC4)
- [ ] **5.2** Add `FrontComposerShellParameterSurfaceTests.cs` snapshot (list of expected `[Parameter]` attrs + types) to `tests/Hexalith.FrontComposer.Shell.Tests/Components/Layout/` — locks D4 append-only contract. Part of Task 10.1 but enumerated here for visibility.

## Task 6 — FcThemeToggle + FcSystemThemeWatcher

- [ ] **6.1** Create `src/Hexalith.FrontComposer.Shell/Components/Layout/FcThemeToggle.razor` + `.razor.cs` per D5/D7/D22. Inject `IStateSelection<FrontComposerThemeState, ThemeValue>`, `IDispatcher`, `IUlidFactory`, `IStringLocalizer<FcShellResources>`. Render `<FluentMenuButton>` with three `<FluentMenuItem>` children. On selection: `_dispatcher.Dispatch(new ThemeChangedAction(ulidFactory.NewUlid().ToString(), selected))`. On Fluxor state change, re-render the button's displayed icon + label. `aria-label="@localizer["ThemeToggleAriaLabel"]"`. (AC3)
- [ ] **6.2** Create `src/Hexalith.FrontComposer.Shell/Components/Layout/FcSystemThemeWatcher.razor` + `.razor.cs` (zero-DOM — renders `@code` only). Inject `IJSRuntime`, `IThemeService`, `IState<FrontComposerThemeState>`, `IOptions<FcShellOptions>`. `OnAfterRenderAsync(firstRender: true)`: `_module = await _js.InvokeAsync<IJSObjectReference>("import", "./_content/Hexalith.FrontComposer.Shell/js/fc-prefers-color-scheme.js")` then `_ref = DotNetObjectReference.Create(this)` then `await _module.InvokeVoidAsync("subscribe", _ref)`. `[JSInvokable] public async Task OnSystemThemeChangedAsync(bool isDark)`: if `State.Value.CurrentTheme == ThemeValue.System`, call `IThemeService.SetThemeAsync(isDark ? ThemeMode.Dark : ThemeMode.Light)` — NO action dispatch (D10/D23). `Dispose` unsubscribes + disposes ref. (AC3)

## Task 7 — Semantic color slots CSS

- [ ] **7.1** Embed six `--fc-color-*` CSS custom properties in `FrontComposerShell.razor.css` per D13/D20. Values map to Fluent UI tokens per the table in AC4.
- [ ] **7.2** Create `tests/Hexalith.FrontComposer.Shell.Tests/SlotMappingRegressionTests.cs` with a single `[Fact]` that serialises the binding table (6 CSS-var → Fluent-token pairs + 6 lifecycle-state → slot pairs) to a deterministic string, compares via Verify.XunitV3 against `SlotMappingRegressionTests.BindingTable.verified.txt`. Any edit to the mapping requires re-approving the `.verified.txt` file. (AC4, D13)

## Task 8 — EN/FR resource files

- [ ] **8.1** Create `src/Hexalith.FrontComposer.Shell/Resources/FcShellResources.resx` (EN, default, no culture suffix) + `FcShellResources.fr.resx`. Populate the 8 keys listed in the cheat sheet + the lifecycle state labels (if Task 0.3 found them missing from Story 2-4 scope). Create empty placeholder `FcShellResources.cs` class (a designer partial is auto-generated; alternatively use `ResXFileCodeGenerator`). (AC7, D12)
- [ ] **8.2** In `ServiceCollectionExtensions.cs` add `public static IServiceCollection AddHexalithShellLocalization(this IServiceCollection services, Action<RequestLocalizationOptions>? configure = null)` that (a) registers `IStringLocalizer<FcShellResources>` (implicit via `AddLocalization` + resource convention), (b) calls `services.Configure<RequestLocalizationOptions>` merging `SupportedCultures` from `FcShellOptions` and invoking `configure` if non-null. Do NOT call `AddLocalization` inside — per D24, adopter owns it. (AC7)

## Task 9 — Counter sample rewire

- [ ] **9.1** Rewrite `samples/Counter/Counter.Web/Components/Layout/MainLayout.razor` to collapse the `FluentLayout` wrapper and the `IThemeService.SetThemeAsync` bootstrap (both move into `FrontComposerShell`), BUT preserve the existing `@inject IFrontComposerRegistry` + `FluentNav` block by passing it into the shell's `Navigation` render fragment slot per D25. Resulting markup:

```razor
@inherits LayoutComponentBase
@using Hexalith.FrontComposer.Shell.Components.Layout
@inject IFrontComposerRegistry Registry

<FrontComposerShell>
    <Navigation>
        <FluentNav Padding="@Padding.All2" Style="height: 100%;">
            @foreach (var manifest in Registry.GetManifests())
            {
                <FluentNavItem Href="@($"/{manifest.BoundedContext.ToLowerInvariant()}")">
                    @manifest.Name
                </FluentNavItem>
            }
        </FluentNav>
    </Navigation>
    <ChildContent>@Body</ChildContent>
</FrontComposerShell>
```

Delete the inline `FluentLayout` block, the `@inject IThemeService` line, the `_themeApplied` field, and the `OnAfterRenderAsync` theme bootstrap — those move into `FrontComposerShell`. Keep `@inject IFrontComposerRegistry Registry` because the sample still owns the nav content. Story 3-2 replaces this adopter-authored block with the framework-owned sidebar. (D3, D25)
- [ ] **9.2** Modify `samples/Counter/Counter.Web/Components/App.razor` to add `<link href="_content/Hexalith.FrontComposer.Shell/Hexalith.FrontComposer.Shell.styles.css" rel="stylesheet" />` AFTER the existing Fluent UI stylesheet in `<head>`. The filename is pinned per D29 / ADR-034 and MUST match Task 0.6's verified RCL build output byte-for-byte. If Task 0.6 reports a different emitted filename (future Blazor tooling change), escalate — do NOT silently adjust without updating ADR-034. Ordering constraint: the `<link>` MUST appear AFTER Fluent UI's stylesheet so the shell's `--fc-color-*` custom properties resolve against already-loaded Fluent UI tokens.
- [ ] **9.3** Modify `samples/Counter/Counter.Web/Program.cs` replacing the `builder.Services.AddLocalization()` line (L20) + the `builder.Services.AddHexalithFrontComposer()` line with a single `builder.Services.AddHexalithFrontComposerQuickstart()` call per D28. Enable `ValidateScopes = true` on `builder.Host.UseDefaultServiceProvider(options => options.ValidateScopes = true)` (ADR-030 + D28 consequence). Adopters who need the granular 4-line path keep `AddLocalization().AddHexalithShellLocalization().AddHexalithFrontComposer()` as the non-deprecated primary API — documented in the Quickstart's XML `<remarks>`.
- [ ] **9.4** Modify `samples/Counter/Counter.Web/appsettings.Development.json` adding `"Hexalith:Shell:AccentColor": "#0097A7"`, `"Hexalith:Shell:LocalStorageMaxEntries": 500`, `"Hexalith:Shell:DefaultCulture": "en"`. Use the existing `"Hexalith:Shell"` section (already present per Story 2-4 Task 6.2).

## Task 10 — Tests

- [ ] **10.1** `FrontComposerShellTests.cs` — 7 bUnit tests (one cut per D26 — placeholders removed from DOM, test collapses from 8 to 7). Runs against a `TestHost` that substitutes `IThemeService` with NSubstitute mock. Includes the `FrontComposerShellParameterSurfaceTests.cs` snapshot from Task 5.2. Asserts the Ctrl+K / Settings placeholder buttons are ABSENT (not present-with-aria-disabled).
- [ ] **10.2** `FcThemeToggleTests.cs` — 5 bUnit tests. Uses `Fluxor.Blazor.Web` test harness (or NSubstitute-backed `IDispatcher` + `IState`) from existing Story 1-3 test infrastructure.
- [ ] **10.3** `FcSystemThemeWatcherTests.cs` — 4 bUnit tests. `BunitJSInterop` configures `SetupModule("./_content/Hexalith.FrontComposer.Shell/js/fc-prefers-color-scheme.js")` expectations.
- [ ] **10.4** `LocalStorageServiceTests.cs` — 9 bUnit tests over `BunitJSInterop`. `SetupVoid("localStorage.setItem", ...)` / `Setup<string>("localStorage.getItem", ...)` / `SetupVoid("localStorage.removeItem", ...)` / `Setup<string[]>("eval", "Object.keys(localStorage)", ...)`. Use `FakeTimeProvider` to control LRU ordering.
- [ ] **10.5** `ThemeEffectsScopeTests.cs` — 5 tests. Substitute `IUserContextAccessor` returning (null tenant, non-null user) → skip + log `HFC2105`; (non-null tenant, null user) → skip + log `HFC2105`; (whitespace `"  "` tenant OR user) → skip + log `HFC2105` (covers the interface's XML contract that treats whitespace as "unauthenticated"); (both non-null + non-whitespace) → persist; (both null) → skip once.
- [ ] **10.6** `DensityEffectsScopeTests.cs` — 5 tests mirroring 10.5, including the whitespace-segment case.
- [ ] **10.7** `FcShellResourcesTests.cs` — 3 tests. `CanonicalKeysHaveFrenchCounterparts` reflects over `FcShellResources` designer-generated properties and asserts each is non-null in both EN (CurrentUICulture = "en") and FR (CurrentUICulture = "fr"). `ThemeToggleAriaLabelResolvesInFrench` asserts the FR value matches "Changer de thème". `AddHexalithShellLocalizationRegistersLocalizer` builds a `ServiceProvider` and resolves `IStringLocalizer<FcShellResources>`.
- [ ] **10.8** `FcShellOptionsValidationTests.cs` — 4 new `AccentColor_*`, `LocalStorageMaxEntries_*`, `DefaultCulture_*`, `SupportedCulturesMustIncludeDefault_*` tests. Append to the existing file.
- [ ] **10.9** `ShellThemeToggleE2ETests.cs` — Playwright smoke: boot Counter.Web via `WebApplicationFactory`, Playwright navigates to root, clicks `FcThemeToggle` → Dark, asserts body has `data-theme="dark"` (or the Fluent UI v5 equivalent attribute — verify in Task 0.2), refreshes page, re-asserts, clicks → System, toggles prefers-color-scheme via `page.EmulateMediaAsync({ ColorScheme: "dark" })`, re-asserts. **Additionally (ADR-034 smoke):** assert `await page.EvaluateAsync<string>("getComputedStyle(document.body).getPropertyValue('--fc-color-success').trim()")` returns a non-empty value after `FrontComposerShell` mounts — catches the class of bug where the `<link>` filename is right but the bundle fails to load or is ordered before Fluent UI's stylesheet. Conditional on Aspire MCP browser availability per `feedback_no_manual_validation.md`.
- [ ] **10.10** `TypographyConstantsTests.cs` — 2 tests in `tests/Hexalith.FrontComposer.Contracts.Tests/Rendering/`. `TypographyConstantsMatch3_1_0Baseline` asserts each of 9 constants against the expected enum value. `TypographyMappingVersionIsPinned` asserts `ContractsMetadata.TypographyMappingVersion == "3.1.0"`.
- [ ] **10.11** **PR review gate — NOT a dev-agent subtask.** Dev agent delivers ALL ~40 tests from 10.1-10.14 without trimming. The PR reviewer applies the Occam/Matrix trim during review round, not the dev during implementation — prevents ship-velocity bias in coverage decisions (elicitation War-Room output: Amelia + Winston + Murat aligned). Review criteria: if the 10.1-10.14 count lands above 40 tests, apply cost-benefit scoring to the bottom quartile and cut — document cuts in the PR description. If below 35, flag potential under-coverage. Specific cut candidates surfaced by the party-mode review (Murat): collapse redundant `FcShellOptionsValidationTests` valid-input rows (2 → 1 if two are behavioural duplicates); keep only the semantic Typography test if the second duplicates CSS-cascade behaviour already covered by 10.9.
- [ ] **10.12** `IStorageServiceLifetimeTests.cs` in `tests/Hexalith.FrontComposer.Shell.Tests/Infrastructure/Storage/` — 1 DI test: build a `ServiceProvider` via `AddHexalithFrontComposer` with `ValidateScopes = true`; assert `GetRequiredService<IStorageService>()` inside a scope returns an instance, and that two distinct `IServiceScope` instances yield two distinct `IStorageService` instances (scoped-per-scope invariant). Asserts ADR-030 Step 4.
- [ ] **10.13** `PersistencePrecedenceTests.cs` — 1 **deterministic channel-level** test (not a timer-dependent effect-pipeline test per Elicitation Challenge-from-Critical-Perspective #1). Directly enqueue three `PendingWrite` records into `LocalStorageService._writeChannel` via a test-only internal accessor: `(key="theme", value=Dark)` → `(key="density", value=Comfortable)` → `(key="theme", value=Light)`. Use `ManualResetEventSlim` set by the drain worker after each write completes (test hook exposed via `InternalsVisibleTo` to the test project) so the assertion fires ONLY when all three writes have been observed by the backing `InMemoryStorageService` — zero timer dependency. After the third signal, assert `storage.GetAsync<ThemeValue>("{tenant}:{user}:theme") == Light` and `storage.GetAsync<DensityValue>("{tenant}:{user}:density") == Comfortable`. Proves last-write-wins FIFO semantics without the flake-prone "rapid dispatch + flush + assert" pattern. (Murat's party-mode race-coverage gap + Challenge-from-Critical-Perspective rewrite.)
- [ ] **10.14** `AddHexalithFrontComposerQuickstartTests.cs` — 3 tests (D28). (a) `ResolvesIStorageServiceAsScoped` builds a `ServiceProvider` via the Quickstart + asserts `IStorageService` resolves with Scoped lifetime. (b) `ResolvesIStringLocalizer` asserts `IStringLocalizer<FcShellResources>` resolves without error. (c) `IdempotentAgainstDuplicateAddLocalization` registers `AddLocalization()` FIRST, then calls `AddHexalithFrontComposerQuickstart()` — asserts no duplicate-registration exception and all services still resolve. (D28)

## Task 11 — Regression + zero-warning gate

- [ ] **11.1** `dotnet build /warnaserror` passes with zero warnings (per `TreatWarningsAsErrors=true` project-wide).
- [ ] **11.2** `dotnet test --no-build` passes with cumulative ~585 tests (baseline ~542 + ~43 from 10.1-10.14).
- [ ] **11.3** Manual Aspire MCP verification per `feedback_no_manual_validation.md` fallback: if Playwright E2E in 10.9 is unreachable in dev environment, substitute one scripted Aspire MCP browser session captured as a GIF (theme toggle + refresh + System OS-switch).
- [ ] **11.4** Review `_bmad-output/implementation-artifacts/deferred-work.md` — add the three deferrals surfaced by 3-1 (option-class split, `[SuppressHFC2105]` analyzer, type specimen + axe-core CI) with owning story references.

---
