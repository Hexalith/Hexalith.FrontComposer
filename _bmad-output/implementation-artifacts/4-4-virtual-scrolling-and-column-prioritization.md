# Story 4.4: Virtual Scrolling & Column Prioritization

Status: ready-for-dev

> **Epic 4 § 146-195** · **FR12 (partial)** · **UX-DR7** (FcColumnPrioritizer) · **UX-DR42** (virtualization default + safety rails) · **UX-DR63** (auto-generation scale limit at 15 fields) · **NFR4** (DataGrid render P95 < 300ms on 500 rows) · applies lessons **L01, L02, L03, L04, L06, L07, L09, L10, L11**. Consumes the Story 1-5 `ColumnModel` IR, Story 2-2 `GridViewSnapshot` (frozen schema — including `ScrollTop` field already populated by the record's private setter), Story 3-6 `DataGridNavigationEffects` persistence layer (fail-closed per L03), Story 4-1 role-dispatched `EmitStandardDataGrid` shell, and Story 4-3 reserved-key packing convention for `GridViewSnapshot.Filters`. Provides the FIRST shipped producer of `ScrollTop` captures (turning Story 2-2's reserved field into a live write path) AND the FIRST consumer of a new `ColumnPriorityAttribute` IR path.

---

## Executive summary (Feynman-level, ~45 sec)

Today every generated projection view renders `state.Items.AsQueryable()` into a bare `FluentDataGrid` without virtualization — adopters whose projections hold 500+ rows see the browser stall because the render tree materialises every row up-front. Story 4-3 shipped filters/sort/search to narrow those sets, but a 10,000-row `OrdersProjection` with a "last 90 days" filter still returns 8,200 rows — below any sensible filter-threshold but still large enough to blow the render budget. Story 4-4 closes that gap: **every generated view gains `Virtualize="true"` + `DisplayMode="Table"` + an `ItemSize`-tuned row budget + an `ItemsProvider` server-side lane for 500+ items** while Story 2-2's reserved `ScrollTop` field becomes live (scroll-position captured → persisted via the 3-6 effect → restored within-session).

1. **Virtualize default ON.** `RazorEmitter.EmitStandardDataGrid` now emits `Virtualize="true"` + `DisplayMode="DataGridDisplayMode.Table"` + `ItemSize="{densityRowHeightPx}"` on every generated `FluentDataGrid` (D1 / D21 — Table mode is the Fluent-recommended partner for `Virtualize`; Grid mode is known to exhibit scroll glitches per Fluent docs). The density-to-row-height mapping reuses Story 3-3's `DensityLevel` enum: `Compact=32px`, `Comfortable=44px`, `Roomy=56px`. The value is resolved at render time from the cascading `RenderContext.Density`, NOT at emit time (density is user-scoped, emit-time is compilation-scoped).
2. **Client-side vs server-side lane.** For projections where `state.Items.Count < FcShellOptions.VirtualizationServerSideThreshold` (default `500`), the grid consumes `Items` directly — client-side virtualization only renders visible rows plus `OverscanCount="3"` (D2). For counts at or above the threshold, the generated view routes through a `GridItemsProvider<T>` callback that dispatches `LoadPageAction(viewKey, skip, take, filters, sort, search)` and awaits `IState<LoadedPageState>.Value.PagesByKey[{viewKey, skip}]` — the reducer accumulates fetched pages, and the callback resolves with the requested slice. ETag reuse is inherited from Story 1-5's `IQueryService.QueryAsync` via `QueryRequest.ETag` (no new cache layer).
3. **Server-side pagination mechanics.** `GridItemsProvider<T>` signature is `Func<GridItemsProviderRequest<T>, ValueTask<GridItemsProviderResult<T>>>`. The generated view emits a provider method that (a) reads the hydrated filter/sort/search state from the Fluxor snapshot, (b) dispatches `LoadPageAction(viewKey, request.StartIndex, request.Count, filters, sort, search)`, (c) `await`s a `TaskCompletionSource<GridItemsProviderResult<T>>` the reducer signals when the page lands, (d) cancels pending awaits via `request.CancellationToken` flowing into `LoadPageAction` (D3). No new Fluxor effect — `LoadPageAction` dispatches into the existing Story 1-5 `LoadAction`-adjacent service call with `QueryRequest.Skip` + `QueryRequest.Take` populated.
4. **Scroll-position capture + restore.** The generated grid container carries a scroll-capture JS helper (D4) — bound via `@onscroll` on the outer `div` hosting the grid. On scroll, the helper fires `ScrollCapturedEvent(viewKey, scrollTop)` through a 150 ms `TimeProvider`-anchored debounce (tighter than 4-3's 300 ms because scroll events fire 60 Hz). The reducer updates `GridViewSnapshot.ScrollTop` and chains `CaptureGridStateAction` so 3-6's persistence effect takes over. On view mount, the existing Story 4-3 `RestoreGridStateAction` dispatch (in `OnInitialized`) pulls the snapshot — `ScrollTop` is applied via a JS interop `scrollToOffset(viewKey, top)` call AFTER the first render tick (D5 — within-session restore only; cross-session is reset to 0 because hours-later scroll context is an anti-pattern).
5. **FcColumnPrioritizer gate at 15 fields.** A new Shell component `FcColumnPrioritizer` wraps the `FluentDataGrid`'s column surface. When `model.Columns.Count > 15` (UX-DR63 / UX-DR7 trigger), the emitter wraps the grid's `ChildContent` in a priority-aware `@foreach` that renders ONLY the first 8-10 columns by priority (D6) and appends a `FluentButton Appearance="Outline"` labeled `"More columns ({N} hidden)"` at the end of the header row. Clicking the button opens a `FluentPopover` with a `FluentCheckbox` list of hidden columns; toggling a checkbox dispatches `ColumnVisibilityChangedAction(viewKey, columnKey, isVisible)`. Visibility writes back to `GridViewSnapshot.Filters["__hidden"]` as a CSV of hidden column names (D7 — another reserved key, mirroring 4-3 D3 `__status`/`__search` packing — no blob schema bump).
6. **Column priority attribute.** New `[ColumnPriority(int)]` property-level attribute in Contracts, parsed by `AttributeParser` into `PropertyModel.ColumnPriority`. Lower-numbered = higher priority (1 = most important); unannotated properties get `int.MaxValue` and slot into declaration order after explicitly-prioritised columns. The transform stage (Story 1-5) sorts columns stable-by-priority-then-declaration-order when the count exceeds 15. HFC1028 (Information) fires when two columns share an explicit priority — deterministic tiebreaker is declaration order.
7. **Safety rails — `MaxUnfilteredItems` + `SlowQueryThresholdMs`.** Two new `FcShellOptions` properties: `MaxUnfilteredItems` (int, default 10000) and `SlowQueryThresholdMs` (int, default 2000). When a projection's unfiltered page fetch returns exactly `MaxUnfilteredItems` items (server-capped), the generated view renders a `FluentMessageBar Intent="Info"` with the localised `MaxItemsCapNoticeTemplate` copy. When the first page fetch's elapsed time exceeds `SlowQueryThresholdMs`, the view renders a second `FluentMessageBar Intent="Info"` with the `SlowQueryNoticeMessage` copy — the banner is auto-dismissing after 5 s (matches Story 2-4 `ConfirmedToastDurationMs` precedent). Measurement is `Stopwatch`-based inside the `LoadPageAction`-adjacent service call path (D8).
8. **Row count header display.** When no filters are active, the view renders a simple `"{count} {entityPlural}"` (e.g. `"47 orders"`) above the grid. When filters ARE active, Story 4-3's `FcFilterSummary` already owns the `"{filteredCount} of {totalCount}"` display — 4-4 does NOT duplicate. Count comes from `state.TotalCount` server-supplied per Story 4-3 D13 (D9 — routing discipline: exactly one count surface visible at a time).

The design honors the "capability contract" principle: `GridViewSnapshot` schema UNCHANGED (column visibility packed in reserved key, scroll position uses the already-reserved `ScrollTop` field), decisions land inside the L06 feature-story cap (≤ 25 — 4-4 targets 22), every cross-story seam is explicit (L01), and fail-closed tenant/user scoping is INHERITED unchanged from Story 3-6 per L03 (no new scope-check code). Scope is deliberately narrow: **virtualization + column prioritization + safety rails — nothing else.** Expand-in-row (4.5), empty states (4.6), `FcNewItemIndicator` (Story 3-5 follow-up), and Epic 6 customization overrides are OUT of scope.

---

## Dev Agent Cheat Sheet (L11)

**Goal (one sentence):** Wire `Virtualize="true"` + `DisplayMode="Table"` + density-driven `ItemSize` onto every generated `FluentDataGrid`, route 500+-item projections through a `GridItemsProvider<T>` server-side lane backed by `LoadPageAction` + page-cache reducer, wrap projections with >15 columns in a new `FcColumnPrioritizer` hosting a popover-based visibility toggle, persist column visibility (reserved `__hidden` key in `GridViewSnapshot.Filters`) + scroll position (`GridViewSnapshot.ScrollTop`) via the Story 3-6 effect, and render auto-dismissing Info banners when `MaxUnfilteredItems` caps the result or `SlowQueryThresholdMs` trips on first-page latency.

**Files to create:**

| Path | Purpose |
| --- | --- |
| `src/Hexalith.FrontComposer.Contracts/Attributes/ColumnPriorityAttribute.cs` | New attribute — `[AttributeUsage(AttributeTargets.Property, Inherited=false, AllowMultiple=false)] public sealed class ColumnPriorityAttribute(int Priority)`. Lower = higher display priority. Unannotated properties default to `int.MaxValue`. Used by the generator's Transform stage to stable-sort `ColumnModel` when `Columns.Count > 15`. |
| `src/Hexalith.FrontComposer.Contracts/Rendering/VirtualizationActions.cs` | Fluxor action records: `LoadPageAction(string ViewKey, int Skip, int Take, IReadOnlyDictionary<string,string>? Filters, string? SortColumn, bool SortDescending, string? SearchQuery)`, `LoadPageSucceededAction(string ViewKey, int Skip, IReadOnlyList<object> Items, int TotalCount, long ElapsedMs)`, `LoadPageFailedAction(string ViewKey, int Skip, string ErrorMessage)`, `ColumnVisibilityChangedAction(string ViewKey, string ColumnKey, bool IsVisible)`, `ScrollCapturedAction(string ViewKey, double ScrollTop)`. Same view-key guard discipline as Story 2-2 `CaptureGridStateAction` (non-empty, no-colon-in-tenant-segment). |
| `src/Hexalith.FrontComposer.Contracts/Rendering/VirtualizationReservedKeys.cs` | Public constants: `HiddenColumnsKey = "__hidden"`. Exposed via Contracts because the generator emits literal references. Adds the one reserved key new to 4-4; the 4-3 reserved keys are NOT re-exported here (single-source-of-truth lives in `ReservedFilterKeys.cs` per 4-3 T7.3). |
| `src/Hexalith.FrontComposer.Shell/Components/DataGrid/FcColumnPrioritizer.razor` + `.razor.cs` | Shell component — wraps a `FluentDataGrid` child via `ChildContent` render fragment. Parameters: `ViewKey` (EditorRequired), `AllColumns` (`IReadOnlyList<ColumnDescriptor>`, EditorRequired — descriptor is `(string Key, string Header, int Priority)`), `HiddenColumns` (`IReadOnlyList<string>`, default empty), `MaxVisibleColumns` (`int`, default 10). Renders the "More columns (N hidden)" `FluentButton` after the last visible column header cell; clicking opens a `FluentPopover` with a `FluentCheckbox` list; toggling dispatches `ColumnVisibilityChangedAction`. `role="dialog"` on the popover content; screen reader announces `"{N} columns hidden. Activate to show more."` |
| `src/Hexalith.FrontComposer.Shell/Components/DataGrid/FcSlowQueryNotice.razor` + `.razor.cs` | Auto-dismissing `FluentMessageBar Intent="Info"` — renders when `IState<LoadedPageState>.Value.LastElapsedMsByKey[viewKey] > FcShellOptions.SlowQueryThresholdMs` AND the banner hasn't been dismissed for the current view. Auto-dismiss after 5 s (anchored on injected `TimeProvider`). Dismiss-state is view-scoped component state (not persisted). |
| `src/Hexalith.FrontComposer.Shell/Components/DataGrid/FcMaxItemsCapNotice.razor` + `.razor.cs` | Non-dismissing `FluentMessageBar Intent="Info"` — renders when `state.Items.Count == FcShellOptions.MaxUnfilteredItems` AND no filters are active. Copy: `"Showing first {cap} items. Use filters to find specific records."` Stays visible until the user applies a filter. |
| `src/Hexalith.FrontComposer.Shell/Services/DataGridScrollInterop.cs` | JS interop service. Methods: `Task CaptureScrollDebounceAsync(string viewKey, double scrollTop, CancellationToken ct)` — invokes the component-scoped debounce; `Task ScrollToOffsetAsync(string viewKey, double scrollTop)` — applies the hydrated scroll position after first render tick. JS side lives in `fc-datagrid.js` (new script file below). |
| `src/Hexalith.FrontComposer.Shell/State/DataGridNavigation/LoadedPageState.cs` | Fluxor feature state — `PagesByKey: ImmutableDictionary<(string ViewKey, int Skip), IReadOnlyList<object>>`, `TotalCountByKey: ImmutableDictionary<string, int>`, `LastElapsedMsByKey: ImmutableDictionary<string, long>`, `PendingCompletionsByKey: ImmutableDictionary<(string ViewKey, int Skip), TaskCompletionSource<object>>`. `TaskCompletionSource` is MUTABLE-reference inside an immutable dictionary — acceptable here because the TCS itself is the seam between reducer-land (resolves) and provider-callback-land (awaits); the dictionary key is immutable. |
| `src/Hexalith.FrontComposer.Shell/State/DataGridNavigation/LoadedPageFeature.cs` | Fluxor `Feature<LoadedPageState>` registration — initial state has empty dictionaries. Name: `"LoadedPage"`. |
| `src/Hexalith.FrontComposer.Shell/State/DataGridNavigation/LoadedPageReducers.cs` | Reducers: `LoadPageSucceededAction` → write items into `PagesByKey`, update `TotalCountByKey` + `LastElapsedMsByKey`, resolve the matching TCS from `PendingCompletionsByKey`. `LoadPageFailedAction` → resolve the TCS with a failed result (empty items + TotalCount=0). `ColumnVisibilityChangedAction` → chain to `CaptureGridStateAction` after updating `Filters["__hidden"]` per D7. `ScrollCapturedAction` → chain to `CaptureGridStateAction` after updating `GridViewSnapshot.ScrollTop` per D4. |
| `src/Hexalith.FrontComposer.Shell/State/DataGridNavigation/LoadPageEffects.cs` | Effect for `LoadPageAction`: resolves `IQueryService`, constructs `QueryRequest` with `Skip`/`Take`/`ColumnFilters`/`StatusFilters`/`SearchQuery`/`SortColumn`/`SortDescending`/`ETag`, measures elapsed time via `Stopwatch`, dispatches `LoadPageSucceededAction` on success or `LoadPageFailedAction` on exception. Cancellation respects the `GridItemsProviderRequest<T>.CancellationToken` threaded through the action. |
| `src/Hexalith.FrontComposer.Shell/wwwroot/js/fc-datagrid.js` | New JS module (peer of existing `fc-keyboard.js`). Exports: `captureScrollDebounce(viewKey, scrollTop, dotnetRef)` — ~150 ms scroll debounce per-viewKey Map; `scrollToOffset(viewKey, scrollTop)` — locates `[data-fc-datagrid="{viewKey}"] .fluent-data-grid` scroll container and sets `scrollTop`; reuses the existing `focusFirstColumnFilter` from 4-3's `fc-keyboard.js` (no cross-module duplication). |
| `tests/Hexalith.FrontComposer.Shell.Tests/Components/DataGrid/FcColumnPrioritizerTests.cs` | bUnit: renders first N columns + "More columns" button when > N columns supplied; button label template renders `"More columns ({hiddenCount} hidden)"` with plural-correct FR; popover checkbox list toggles dispatch `ColumnVisibilityChangedAction`; `aria-label` from resource; keyboard-accessible (Tab into button → Space/Enter opens popover); panel `role="dialog"` with `aria-labelledby` on header. 6 tests. |
| `tests/Hexalith.FrontComposer.Shell.Tests/Components/DataGrid/FcSlowQueryNoticeTests.cs` | bUnit: renders only when `LastElapsedMsByKey[viewKey] > threshold`; auto-dismisses after 5 s via `FakeTimeProvider` advance; dismiss is view-scoped (NOT persisted across navigations). 3 tests. |
| `tests/Hexalith.FrontComposer.Shell.Tests/Components/DataGrid/FcMaxItemsCapNoticeTests.cs` | bUnit: renders when `state.Items.Count == FcShellOptions.MaxUnfilteredItems` AND filters are empty; does NOT render when filters are active; does NOT render when count < cap; copy template interpolates the cap value. 3 tests. |
| `tests/Hexalith.FrontComposer.Shell.Tests/State/DataGridNavigation/LoadPageReducerTests.cs` | Reducer: `LoadPageSucceededAction` populates `PagesByKey` at the requested key, sets `TotalCountByKey` + `LastElapsedMsByKey`, resolves the pending TCS; `LoadPageFailedAction` resolves TCS with empty items; `ColumnVisibilityChangedAction` updates `__hidden` CSV AND chains `CaptureGridStateAction`; `ScrollCapturedAction` updates `ScrollTop` AND chains `CaptureGridStateAction`. 5 tests. |
| `tests/Hexalith.FrontComposer.Shell.Tests/State/DataGridNavigation/LoadPageEffectIntegrationTests.cs` | Integration via `FrontComposerTestBase`: dispatching `LoadPageAction` resolves `IQueryService`, issues a `QueryRequest` with populated `Skip`/`Take`/filters, measures elapsed, dispatches `LoadPageSucceededAction` on happy path; dispatches `LoadPageFailedAction` on service exception; cancellation token flows through. 3 tests. |
| `tests/Hexalith.FrontComposer.Shell.Tests/State/DataGridNavigation/ScrollAndColumnVisibilityRoundTripTests.cs` | Round-trip: dispatch `ScrollCapturedAction` → advance `FakeTimeProvider` 250 ms → LocalStorage blob holds updated `ScrollTop`; dispatch `ColumnVisibilityChangedAction` for 3 columns → blob `Filters["__hidden"]` contains CSV; hydrate from fresh state → snapshot matches. Uses `InMemoryStorageService`. 3 tests. |
| `tests/Hexalith.FrontComposer.Shell.Tests/State/DataGridNavigation/ScrollRestoreWithinSessionTests.cs` | bUnit + JS interop stub: within-session navigation away + return restores `ScrollTop` via `scrollToOffset` interop; cross-session hydration (simulated via fresh store) does NOT restore scroll (ScrollTop resets to 0 on hydrate per D5). 2 tests. |
| `tests/Hexalith.FrontComposer.Shell.Tests/State/DataGridNavigation/GridViewPersistenceBlobSchemaLockedTests.cs` (EXTEND) | Extend existing Story 4-3 / 3-6 schema-locked assertions — the round-trip for the new `__hidden` reserved key preserves CSV encoding; `ScrollTop` round-trips as a double with 6-digit precision (matches 3-6's serialization settings). NO new fields asserted; schema shape UNCHANGED. 2 new assertion groups. |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RazorEmitterVirtualizationTests.cs` | Verify/approval: every generated view emits `Virtualize="true"` + `DisplayMode="DataGridDisplayMode.Table"` + `ItemSize` resolved from `RenderContext.Density` + `OverscanCount="3"`; `data-fc-datagrid="{viewKey}"` on the scroll container (already emitted by 4-3 — regression gate); `ItemKey` bound to an aggregate-id-like property when present, otherwise to `x => x` (fallback). 4 tests. |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RazorEmitterItemsProviderTests.cs` | Verify: projection scenario emits `GridItemsProvider<T>` delegate when `state.Items.Count >= VirtualizationServerSideThreshold` check is true in the conditional emission — the provider is ALWAYS emitted; the runtime check is `state.Items.Count >= _shellOptions.Value.VirtualizationServerSideThreshold ? ItemsProvider : Items` branch logic (D2). Snapshot asserts the branch shape. 3 tests. |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RazorEmitterColumnPrioritizerTests.cs` | Verify: projection with > 15 columns emits `FcColumnPrioritizer` wrap + `MaxVisibleColumns="10"` parameter; projection with ≤ 15 emits bare `FluentDataGrid` (regression gate — wrapping never activates below threshold); stable sort by priority is part of the Transform-stage IR (verify at ColumnModel order). 3 tests. |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RazorEmitterBannersTests.cs` | Verify: both `<FcSlowQueryNotice ViewKey="@viewKey" />` + `<FcMaxItemsCapNotice ViewKey="@viewKey" EntityPlural="@entityPlural" />` are emitted above the grid (below 4-3's filter surface) unconditionally; the components own their visibility gating. 2 tests. |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Diagnostics/Hfc1028DiagnosticTests.cs` | HFC1028 fires when two `[ColumnPriority(N)]` attributes declare the same `N` within one projection; payload includes both property names + the colliding priority value; dedupe per projection type. 3 tests. |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Diagnostics/Hfc1029DiagnosticTests.cs` | HFC1029 fires at emit stage when `Columns.Count > 15`; payload includes the column count + the names of the hidden-by-default columns (positions 11+); fires once per projection type (dedupe). 3 tests. |
| `tests/Hexalith.FrontComposer.Contracts.Tests/Attributes/ColumnPriorityAttributeTests.cs` | `[ColumnPriority(1)]` compiles on a property; `Priority` is readable; AttributeUsage constraint rejects class-level application at compile time (reflection-based assertion). 2 tests. |
| `tests/Hexalith.FrontComposer.Contracts.Tests/Rendering/VirtualizationActionsTests.cs` | Each of the 5 new action records throws on empty `ViewKey`; `LoadPageAction` rejects negative `Skip`/`Take`; `ScrollCapturedAction` rejects NaN/Infinity/negative `ScrollTop` (mirrors Story 2-2 `GridViewSnapshot.ScrollTop` validation). 3 tests. |
| `tests/Hexalith.FrontComposer.Contracts.Tests/FcShellOptionsVirtualizationTests.cs` | `MaxUnfilteredItems` range `[100, 1_000_000]` enforced by `[Range]` annotation; `SlowQueryThresholdMs` range `[500, 30_000]`; `VirtualizationServerSideThreshold` range `[50, 10_000]`; threshold validator asserts `VirtualizationServerSideThreshold < MaxUnfilteredItems` (server-side must trigger BEFORE the cap to cap without full-render). 3 tests. |

**Files to extend:**

| Path | Change |
| --- | --- |
| `src/Hexalith.FrontComposer.Contracts/FcShellOptions.cs` | Add three properties per D10: `int MaxUnfilteredItems { get; set; } = 10_000;` with `[Range(100, 1_000_000)]`; `int SlowQueryThresholdMs { get; set; } = 2_000;` with `[Range(500, 30_000)]`; `int VirtualizationServerSideThreshold { get; set; } = 500;` with `[Range(50, 10_000)]`. Every range chosen to keep pathological values out of production while allowing legitimate infrastructure tuning (call-center deployments raise the cap; resource-constrained edge deployments lower it). |
| `src/Hexalith.FrontComposer.Shell/Options/FcShellOptionsThresholdValidator.cs` | Add cross-property check: `VirtualizationServerSideThreshold < MaxUnfilteredItems` (the server-side lane must trigger BEFORE the unfiltered cap — otherwise we full-render the capped result). Failure message: `"FcShellOptions.VirtualizationServerSideThreshold ({threshold}) must be strictly less than MaxUnfilteredItems ({cap})."` |
| `src/Hexalith.FrontComposer.SourceTools/Parsing/AttributeParser.cs` | Add `ParseColumnPriority` helper — reads `[ColumnPriority(N)]` from `PropertySymbol.GetAttributes()`, returns `int?` (null when absent); surfaces on `PropertyModel`. HFC1028 (Information) fires at Parse stage when two `[ColumnPriority]` attributes on the same projection share a value. |
| `src/Hexalith.FrontComposer.SourceTools/Parsing/PropertyModel.cs` | Add `int? ColumnPriority` field (append-only; `Equals`/`GetHashCode` updated). |
| `src/Hexalith.FrontComposer.SourceTools/Transforms/ColumnModel.cs` | Add `int? Priority` field (append-only, `Equals`/`GetHashCode` updated — IR cache stability per Story 1-4). |
| `src/Hexalith.FrontComposer.SourceTools/Transforms/RazorModelTransform.cs` | Sort `Columns` by `(Priority ?? int.MaxValue, DeclarationOrder)` stable; NO-OP when every property has null priority (preserves Story 1-5's declaration-order baseline — critical for `CounterProjectionApprovalTests` regression). When `Columns.Count > 15`, emit HFC1029 Information at emit stage. |
| `src/Hexalith.FrontComposer.SourceTools/Emitters/ProjectionRoleBodyEmitter.cs` | Modify `EmitStandardDataGrid` per D1/D21: emit `Virtualize="true"`, `DisplayMode="DataGridDisplayMode.Table"`, `ItemSize="@DensityRowHeightPx(RenderContext.Density)"`, `OverscanCount="3"`, `ItemKey="@(_itemKeyAccessor)"`. The `_itemKeyAccessor` field is emitted at the top of `BuildRenderTree` — resolves to `x => x.Id`/`x => x.AggregateId`/`x => x.Key` (first match per the `ResolveItemKeyAccessor` helper) or `x => x` fallback (D18). Wrap the `FluentDataGrid` in `FcColumnPrioritizer` ONLY when `model.Columns.Count > 15` (D6). Emit the `LoadPageAction`-dispatching `GridItemsProvider<T>` callback as a private method on the generated view; `ItemsProvider` parameter is bound to it; `Items` parameter is `null` when the provider is active (Fluent requires EXACTLY one of `Items` OR `ItemsProvider`). The runtime branch uses `state.Items.Count >= _shellOptions.Value.VirtualizationServerSideThreshold` for server-vs-client selection (D2). |
| `src/Hexalith.FrontComposer.SourceTools/Emitters/RazorEmitter.cs` | Inject `IOptions<FcShellOptions> ShellOptions` into the generated view (in addition to the Story 4-3 injections). Emit the `FcSlowQueryNotice` + `FcMaxItemsCapNotice` components ABOVE the grid (below 4-3's filter surface), both with `ViewKey` parameter; the banners own their visibility gating. Emit the `@onscroll` handler on the outer `<div data-fc-datagrid="{viewKey}">` container calling `DataGridScrollInterop.CaptureScrollDebounceAsync`. Emit the `OnAfterRenderAsync(firstRender)` override — on first render only, call `DataGridScrollInterop.ScrollToOffsetAsync(viewKey, hydratedSnapshot.ScrollTop)` if the snapshot was hydrated (D5). |
| `src/Hexalith.FrontComposer.SourceTools/Diagnostics/DiagnosticDescriptors.cs` | Add `HFC1028_ColumnPriorityCollision` (Information, per-projection dedupe, fires when two `[ColumnPriority(N)]` share `N`); add `HFC1029_ColumnPrioritizerActivated` (Information, per-projection dedupe, fires at emit stage when `Columns.Count > 15`). |
| `src/Hexalith.FrontComposer.SourceTools/FrontComposerGenerator.cs` | Extend diagnostic switch for HFC1028 + HFC1029; reuse the per-generator-run `HashSet<string>` dedupe pattern (Story 4-2 D6 / 4-3 D20 precedent). |
| `src/Hexalith.FrontComposer.Contracts/Diagnostics/FcDiagnosticIds.cs` | Add XML-doc entries for HFC1028 + HFC1029 matching the HFC1022-HFC1027 format. |
| `src/Hexalith.FrontComposer.SourceTools/AnalyzerReleases.Unshipped.md` | Add rows for HFC1028 (Information) + HFC1029 (Information). |
| `src/Hexalith.FrontComposer.Shell/_Imports.razor` | Existing `@using Hexalith.FrontComposer.Shell.Components.DataGrid` (added by Story 4-3) already covers the new `FcColumnPrioritizer` / `FcSlowQueryNotice` / `FcMaxItemsCapNotice` — NO edit. |
| `src/Hexalith.FrontComposer.Shell/Extensions/ServiceCollectionExtensions.cs` | Register `DataGridScrollInterop` as Scoped; register the new `LoadedPageFeature` via Fluxor `AddFeature`; register `LoadPageEffects` via `AddEffect`. Piggybacks on existing AddFluentUIFrontComposer call chain. |
| `src/Hexalith.FrontComposer.Shell/Resources/FcShellResources.resx` + `.fr.resx` | Add 8 new keys (see D19). 78 → 86 per locale. |
| `tests/Hexalith.FrontComposer.Shell.Tests/Options/FcShellOptionsThresholdValidatorTests.cs` | Extend existing validator test: `VirtualizationServerSideThreshold >= MaxUnfilteredItems` fails validation with expected message. 1 new test. |

**Generated naming rules (L04 preserved):**

- Generator hint prefix unchanged: `{Namespace}.{TypeName}.g.razor.cs`.
- Emitted class name unchanged: `{TypeName}View : ComponentBase, IDisposable`.
- `ItemsProvider` callback is emitted as a PRIVATE METHOD on the generated class, named `LoadPageAsync` (stable across projections — no suffix collisions because it is instance-private).
- `_itemKeyAccessor` field is PRIVATE to the generated class; value resolved once in a static constructor or at first use via `Lazy<Func<T, object>>`; name is `_itemKeyAccessor` (single underscore for instance field + camelCase — matches Story 4-3's `_rowContext` precedent from Story 4-1).
- `data-fc-datagrid="{viewKey}"` attribute on the grid container — UNCHANGED from Story 4-3 (regression gate).

**AC quick index** (one-liner each):

| AC | Intent |
| --- | --- |
| AC1 | Every generated grid renders with `Virtualize="true"` + `DisplayMode="Table"` + density-driven `ItemSize` + `OverscanCount="3"`; `ItemKey` bound deterministically |
| AC2 | Below `VirtualizationServerSideThreshold`, client-side virtualization via `Items=state.Items`; at/above threshold, server-side via `GridItemsProvider<T>` dispatching `LoadPageAction`; exactly one of `Items`/`ItemsProvider` bound at a time |
| AC3 | P95 render time measured via bUnit benchmark on 500 virtualized rows < 300ms (NFR4); slow-query banner auto-dismisses after 5s |
| AC4 | MaxUnfilteredItems cap renders Info banner (non-dismissing) when reached with no filters; SlowQueryThresholdMs trips renders Info banner (auto-dismiss 5s) on first-page latency overrun |
| AC5 | Projections with > 15 columns wrap in `FcColumnPrioritizer` showing first 10 by priority + "More columns (N hidden)" button opening a `FluentCheckbox` popover; ≤15 bypasses wrap |
| AC6 | Column visibility toggles persist to `GridViewSnapshot.Filters["__hidden"]` CSV via Story 3-6 effect; keyboard-navigable; screen reader announces "[N] columns hidden. Activate to show more." |
| AC7 | Scroll position captured via 150ms debounce on `@onscroll`; within-session restore via `scrollToOffset` JS interop after first render; cross-session hydration resets ScrollTop to 0 (anti-pattern per D5) |

**Binding contract(s) with predecessor stories:**

- **Story 2-2 contract** frozen: `GridViewSnapshot.ScrollTop` field is now ACTIVELY WRITTEN — the field existed with private-setter validation (non-negative, finite, non-NaN) since Story 2-2 but had no producer until 4-4. `GridViewSnapshot` schema UNCHANGED — column visibility packed in the existing `Filters` dictionary under the new reserved key `__hidden`. `GridViewPersistenceBlobSchemaLockedTests.BlobSerializesToExpectedJsonShape` stays green.
- **Story 3-6 contract** consumed: `DataGridNavigationEffects` handles all persistence IO. 4-4 does NOT add new effects for navigation persistence — `ColumnVisibilityChangedAction` + `ScrollCapturedAction` reducers chain to `CaptureGridStateAction` which 3-6's effect picks up verbatim. 4-4 DOES add `LoadPageEffects.HandleLoadPage` (new effect for the `IQueryService.QueryAsync` call) — this is a separate effect class in a separate file; it does NOT modify `DataGridNavigationEffects`.
- **Story 4-1 contract** consumed: `EmitStandardDataGrid` is the single shared DataGrid emission path. 4-4 extends the helper with `Virtualize`/`DisplayMode`/`ItemSize`/`ItemsProvider`/`ItemKey`/`OverscanCount` attributes — additive on a STABLE emission contract. StatusOverview's `EmitStatusOverviewBody` opens its own `FluentDataGrid` inline and also gets `Virtualize=true` but with a LOW item count (always ≤ number of distinct enum members, typically ≤ 10) — virtualization is harmless even at small counts, and keeping one emission rhythm simplifies the test surface.
- **Story 4-2 contract** unchanged: `FcStatusBadge` + `SlotAppearanceTable` are NOT consumed by 4-4 (explicit mention to prevent confusion — column prioritization is orthogonal to status rendering).
- **Story 4-3 contract** consumed: the reserved-key packing convention (`__status`, `__search`) is EXTENDED by 4-4 with `__hidden`. The reducer's reserved-key guard discipline stays — column keys starting with `__` are reserved; C# identifier rules prevent collision with projection property names. `FcFilterSummary` + `FcFilterResetButton` + `FcFilterEmptyState` render UNCHANGED — 4-4 does NOT modify the filter summary's count display (D9 — one count surface at a time: 4-4's unfiltered row count display is GATED on `!anyFilterActive`).
- **Story 1-5 contract** frozen: `ColumnModel.TypeCategory` routing unchanged. 4-4 adds `Priority` as a derived IR field; does NOT touch `PropertyName`/`Header`/`IsNullable`/`BadgeMappings`/`FormatHint`/`TypeCategory`. `CounterProjectionApprovalTests` stays green (counter projection has no `[ColumnPriority]` → every column has `Priority=null` → Transform preserves declaration order byte-for-byte).
- **Story 1-4 contract** preserved: IR cacheability — `PropertyModel.ColumnPriority` + `ColumnModel.Priority` are plain value types; `Equals`/`GetHashCode` extended additively. Incremental-build hash-collision scenarios per Story 1-4's own tests stay green.
- **Story 3-3 contract** consumed: `DensityLevel` enum + `RenderContext.Density` cascade. 4-4 reads `Density` at render time (NOT at emit time) to resolve `ItemSize` — because density is user-scoped and can change during a session. `DensityRowHeightPx` is a pure static helper on a new `DataGridDensityMetrics` type.
- **Story 3-5 contract** unchanged: `IBadgeCountService` is NOT consumed by 4-4.
- **Story 3-4 contract** consumed unchanged: `IShortcutService` — the `/` binding (4-3) continues to focus the first column filter; 4-4 does NOT add new shortcuts.

**Scope guardrails (what NOT to implement in 4-4):**

- ❌ **Expand-in-row detail** (Story 4.5) — `GridViewSnapshot.ExpandedRowId` field is unchanged by 4-4; 4.5 is the producer.
- ❌ **`FcEmptyState` CTA flow** (Story 4.6) — the Story 4-1 `FcProjectionEmptyPlaceholder` and Story 4-3 `FcFilterEmptyState` render unchanged.
- ❌ **Field descriptions as tooltips** (Story 4.6) — column-header tooltips from `[Description]` attributes are Story 4.6.
- ❌ **`FcFieldPlaceholder`** (Story 4.6) — unsupported-type rendering is 4.6.
- ❌ **Cross-tenant page-cache sharing** — `LoadedPageState.PagesByKey` is keyed by `ViewKey` which already carries the bounded-context segment; tenant scoping is handled at the `IQueryService` boundary via `QueryRequest.TenantId`. 4-4 does NOT add per-tenant isolation — it INHERITS Story 3-6's fail-closed tenant scope per L03.
- ❌ **Pre-fetch / prefetch / read-ahead** — `GridItemsProvider<T>` fetches the CURRENT page on scroll. Read-ahead optimization is Epic 5 territory (SignalR + optimistic fetch).
- ❌ **`FcColumnPrioritizer` drag-to-reorder** — column order in 4-4 is strictly priority-then-declaration. Drag-reorder UI is Epic 6 Level 2/3 customization.
- ❌ **Per-viewport responsive column hiding** — UX-DR7's "phone" variant says the "More columns" button moves above the grid; 4-4 ships the desktop layout only. The responsive variant is Story 10-2 or an Epic 9-3 UX polish entry (deferred with L10 story link).
- ❌ **`FcNewItemIndicator`** (FR21 / 3-5 follow-up) — highlight for new rows outside current filters. Not in scope.
- ❌ **ColumnPriority conflict resolution UI** — HFC1028 fires as a BUILD diagnostic; runtime falls back deterministically to declaration order. No adopter-facing runtime surface.
- ❌ **Boolean column virtualize-specific behaviours** — a Boolean column in a virtualized grid renders the same as in a non-virtualized grid (Story 1-5's `CheckmarkFilled`/`Dismiss` text rendering). No sparkline, no mini-chart.
- ❌ **ETag-per-page invalidation** — `QueryRequest.ETag` is page-scoped (each page's request carries its own ETag from the previous page's response). Cross-page ETag reconciliation is Epic 5 SignalR work.
- ❌ **Virtualization for `DetailRecord` / `Timeline` strategies** — these roles render a FluentCard + FluentStack (not a DataGrid). Virtualize is inapplicable. 4-4's changes land ONLY on the `EmitStandardDataGrid` path consumed by Default / ActionQueue / StatusOverview / Dashboard strategies (per Story 4-1 D16).
- ❌ **Scroll-position capture across routes (not just within-session)** — Story 3-6 D6 makes LocalStorage the persistence substrate; scroll restore across hours-long sessions is disorienting, not useful. 4-4's `ScrollTop` blob field is persisted but hydration sets it to 0 (D5); only within-session navigation (route-change while session is alive) applies the stored value.

**Start-here task order:** T1 → T2 → T3 → T4 → T5 → T6 → T7 → T8 → T9 (sequential; T8 resource parity after all new keys land; T9 diagnostic docs last).

**Test count expectation:** target ≈ **53 new tests**. Breakdown: 12 Shell component tests (FcColumnPrioritizer 6 + FcSlowQueryNotice 3 + FcMaxItemsCapNotice 3), 13 Shell reducer + persistence + scroll-restore tests (LoadPageReducer 5 + LoadPageEffect integration 3 + ScrollAndColumnVisibility round-trip 3 + ScrollRestoreWithinSession 2), 12 SourceTools emitter snapshots (virtualization shape 4 + ItemsProvider branch 3 + prioritizer wrap 3 + banners emission 2), 6 SourceTools diagnostic tests (HFC1028 payload/dedupe/collision 3 + HFC1029 payload/dedupe/cap 3), 7 Contracts tests (ColumnPriorityAttribute usage 2 + VirtualizationActions validation 3 + FcShellOptionsVirtualization range 2 + threshold validator cross-property 1 — minus 1 for validator already counted elsewhere = 8 total across Contracts + Options), 3 resource parity tests. Matrix-scoring (L07) identified 8 candidates below 3.0 cut: (a) per-browser scroll-restore behaviour — Playwright CI / Story 10-2; (b) theme × density matrix for ItemSize — Story 10-2; (c) RTL column prioritizer popover — Story 10-2; (d) memory-leak test for page cache eviction — Story 10-2 / Epic 9-5; (e) virtualize performance P99 — Story 10-2; (f) simulated 100k-row sandbox — Epic 5 adopter-facing harness; (g) 3 additional concurrent-page-fetch race variations — matrix < 3.0 (cancellation token suffices for v1). 53/22 ≈ 2.4 tests per decision (narrower than 4-3's 62/22 ≈ 2.8 because 4-4 reuses more of 4-3's infrastructure).

---

## Story

As a business user,
I want DataGrids to handle large datasets smoothly via virtual scrolling and auto-manage column visibility when projections have many fields,
so that performance stays fast on 500+-row projections and the view remains usable when a projection declares dozens of properties.

**Business value:** Until 4-4 lands, every `OrdersProjection` with 5,000 rows is a render-tree materialization bomb — opening the view stalls the browser tab for 3-4 seconds while Blazor builds the full DOM, and scrolling through is stuttery because every row is a real DOM node. After 4-4 ships, the same projection renders in < 200ms because only the ~15 visible rows are in the DOM at any time; scrolling triggers the Fluent `Virtualize` machinery which tears down off-screen rows and fetches server-side pages transparently. For a fulfillment operator scanning 5,000 daily orders or a finance analyst filtering a 10,000-row transaction ledger, this is the difference between "the tool loads" and "the tool is unusable." NFR4's "P95 render time < 300ms on 500 virtualized rows" is UNREACHABLE without 4-4. It is also the story that operationalises Story 2-2's reserved `ScrollTop` field (dormant since 2-2 landed) and unlocks the FR12 "smart browsing" promise for the common case of wide projections (>15 fields) where the current output is visually unwieldy.

---

## Cross-story contract table (L01)

| Contract | Producer | Consumer | Binding |
|---|---|---|---|
| `ColumnPriorityAttribute(int Priority)` (new Contracts type, `AttributeTargets.Property`, `Inherited=false`, `AllowMultiple=false`) | 4-4 | `AttributeParser.ParseColumnPriority` → `PropertyModel.ColumnPriority` → `ColumnModel.Priority` → `RazorModelTransform` stable sort | Frozen at v1: lower = higher display priority; unannotated properties = `int.MaxValue`; `int.MinValue` is accepted (reserved for "pin to front" use cases without introducing a flag). Priority collisions within one projection emit HFC1028 Information; deterministic tiebreaker = declaration order. Future `[ColumnPriority(int, ColumnGroup="...")]` overloads are append-only positional-parameter additions. |
| `LoadPageAction` / `LoadPageSucceededAction` / `LoadPageFailedAction` / `ColumnVisibilityChangedAction` / `ScrollCapturedAction` (5 new Fluxor action records, Contracts project) | 4-4 | Shell `LoadPageReducers` + `LoadPageEffects`; column-visibility reducer chains to `CaptureGridStateAction` (Story 2-2 contract); scroll-capture reducer chains to `CaptureGridStateAction` | Record shape frozen at v1. All constructors validate non-empty `ViewKey` + reject colons in user-supplied segments (Story 2-2 `CaptureGridStateAction` discipline). `LoadPageAction.CancellationToken` flows through to the `IQueryService` call — cancellation unblocks the provider callback's `Task.WhenAny(tcs.Task, cancelTask)`. `ScrollCapturedAction.ScrollTop` MUST be non-negative finite (mirrors `GridViewSnapshot.ScrollTop` validation). |
| `VirtualizationReservedKeys.HiddenColumnsKey` (`"__hidden"`) | 4-4 | Shell column-visibility reducer; 3-6 persistence effect (transparent — dictionary serialised verbatim); generator emits literal references during prioritizer wrap emission | PUBLIC constant. Name prefix `__` prevents collision with any real projection property (C# identifier rules). Value is CSV of column KEYS (property names) that are hidden. Changing the string is a MAJOR version bump — invalidates every persisted blob with hidden columns. |
| `GridViewSnapshot.Filters["__hidden"]` semantics — reserved key | 2-2 (shape) / 4-4 (semantic) | Shell column-visibility reducer; `FcColumnPrioritizer` reads at render to compute the hidden set | Shape frozen by 2-2 D30. Semantics ADDED by 4-4: the dictionary may contain `__hidden` as a CSV of hidden column names. Reducer validation: reserved-key names are case-sensitive; collision impossible by C# identifier convention (4-3 D3 continues to hold). |
| `GridViewSnapshot.ScrollTop` (Story 2-2 field, producer finally arrives) | 4-4 | 3-6 persistence effect (already wired — it serialises the full snapshot); `FcScrollRestorer`-less: the generated view's `OnAfterRenderAsync(firstRender)` reads the hydrated snapshot's `ScrollTop` and calls `DataGridScrollInterop.ScrollToOffsetAsync(viewKey, scrollTop)` | Validation UNCHANGED from Story 2-2: non-negative finite double. 4-4's `ScrollCapturedAction` reducer re-applies the same validation before assigning — defence in depth. Cross-session hydration resets `ScrollTop` to 0 at hydrate time per D5 (the hydrate reducer in 2-2 takes the blob verbatim; 4-4 adds a `ResetScrollOnHydrateMiddleware` step in the reducer path that clamps `ScrollTop` to 0 when the hydrate is sourced from LocalStorage — within-session producer-to-consumer dispatches bypass the middleware). |
| `GridViewPersistenceBlob` schema | 3-6 | 4-4 (transparent — blob holds the existing `ScrollTop` double field AND the `Filters` dictionary with the new reserved-key packing) | **UNCHANGED** per Story 4-3 D3 discipline. No new fields, no renames. `BlobSerializesToExpectedJsonShape` regression test stays green. |
| `LoadedPageState` Fluxor feature state (new Shell-scoped feature) | 4-4 | `FcSlowQueryNotice` reads `LastElapsedMsByKey`; `FcMaxItemsCapNotice` reads `state.Items.Count` via the existing generator-emitted state feature (NOT `LoadedPageState`); server-side `GridItemsProvider<T>` callback reads `PagesByKey` + awaits `PendingCompletionsByKey[key]` TCS | Feature state shape frozen at v1. Future additions (e.g., `LastQueryErrorByKey`) are append-only fields. `TaskCompletionSource<object>` in `PendingCompletionsByKey` uses `object` not `T` because the feature state is non-generic (Fluxor limitation) — the callback casts the result to `IReadOnlyList<T>` on return; type safety is lost at the dictionary boundary but regained at the callback. Acceptable for v1; Epic 9-5 AOT/trim analyzer may surface this later. |
| `GridItemsProvider<T>` emission (Fluent UI's delegate type) | 4-4 generator | Generated `{TypeName}View` binds the callback to `FluentDataGrid<T>.ItemsProvider`; Fluent invokes the callback on scroll | Shape OWNED BY FLUENT UI v5 — 4-4 adopts unchanged. Callback return type is `ValueTask<GridItemsProviderResult<T>>` where the result is `(IEnumerable<T> Items, int TotalItemCount)`. Cancellation via `GridItemsProviderRequest<T>.CancellationToken`. |
| `FcColumnPrioritizer` / `FcSlowQueryNotice` / `FcMaxItemsCapNotice` Blazor components | 4-4 | Every generated `{TypeName}View` where `model.Columns.Count > 15` (prioritizer only) OR unconditionally (the two banners always emit, own their visibility) | Parameter lists frozen at v1. Future parameters append-only. `FcColumnPrioritizer` MUST NOT expose a `MaxVisibleColumns` overridable per-view via component parameter — 10 is the fixed default (UX-DR7 "first 8-10 columns by priority"); Epic 6 template override is the customization path. A `MaxVisibleColumns` parameter IS exposed for adopters who wrap `FcColumnPrioritizer` manually outside generator emission — the generator always passes 10. |
| `data-fc-datagrid="{viewKey}"` DOM attribute (Story 4-3 contract) | 4-3 | 4-4 `scrollToOffset` JS helper queries this selector to locate the scroll container | Selector format frozen per Story 4-3 D10; 4-4 adds a second consumer of the attribute without changing the attribute name or scope. |
| `FcShellOptions.MaxUnfilteredItems` / `SlowQueryThresholdMs` / `VirtualizationServerSideThreshold` | 4-4 | `LoadPageEffects` reads `MaxUnfilteredItems` to clamp `QueryRequest.Take`; `FcSlowQueryNotice` reads `SlowQueryThresholdMs`; generator emits runtime switch on `VirtualizationServerSideThreshold`; validator enforces `VirtualizationServerSideThreshold < MaxUnfilteredItems` | Additive options; existing adopter configurations compile unchanged. Default values selected to match UX-spec section `Pagination & Virtual Scrolling` (10k cap, 2s slow-query, 500 server-side split). Threshold validator cross-property invariant blocks startup if an adopter misconfigures the pair (fail-loud at startup per architecture §110 "validate-on-start" discipline). |
| `HFC1028_ColumnPriorityCollision` (Information, Parse-stage) | 4-4 | Operator / telemetry dashboards; Story 9-4 may layer analyzer hardening | Per-projection-type DEDUPED — fires once per projection that has ≥ 2 columns with the SAME explicit priority value; payload: `{ProjectionType, CollidingPriorityValue, CollidingPropertyNames[]}`. Does NOT fire for `null`-priority collisions (every unannotated property has `null` — that's not a collision, that's the default). |
| `HFC1029_ColumnPrioritizerActivated` (Information, emit-stage) | 4-4 | Operator / telemetry dashboards | Per-projection-type DEDUPED — fires once per projection where `Columns.Count > 15`; payload: `{ProjectionType, ColumnCount, HiddenByDefaultColumnNames[]}` (the positions 11+ slice). Does NOT fire below the threshold; a projection with exactly 15 columns does NOT trigger (strict `>` comparison). |
| 8 new EN+FR resource keys = 16 new strings; total shell resource count goes 78 → 86 per locale | 4-4 | `FcColumnPrioritizer` + `FcSlowQueryNotice` + `FcMaxItemsCapNotice` + generator-emitted row-count label | Append-only to `FcShellResources.resx` + `FcShellResources.fr.resx`. EN/FR parity enforced by `CanonicalKeysHaveFrenchCounterparts`. French U+00A0 NBSP before colon in `PrioritizerColumnsPanelTitle` (`"Visibilité des colonnes"` — no colon, no NBSP needed) and `MaxItemsCapNoticeTemplate` (embedded punctuation rules per Story 4-1 D19 precedent). Plural-sensitive keys use `.NET` `IStringLocalizer` composition via string templates — no ICU plurals in v1 (Epic 9-5 ICU migration). |

---

## Acceptance Criteria

| AC | Given / When / Then | Primary tasks |
|---|---|---|
| **AC1** | **Given** any generated projection view dispatched through the `Default` / `ActionQueue` / `StatusOverview` / `Dashboard` strategies from Story 4-1. **When** `{TypeName}View` renders the `FluentDataGrid`. **Then** the grid is emitted with `Virtualize="true"`, `DisplayMode="DataGridDisplayMode.Table"` (D21 — required for virtualize correctness per Fluent docs), `ItemSize="@(DataGridDensityMetrics.ResolveRowHeightPx(RenderContext.Density))"` where `Density` resolves to `32`/`44`/`56` pixels for `Compact`/`Comfortable`/`Roomy`, `OverscanCount="3"` (rendered-but-off-screen row buffer), and `ItemKey="@(_itemKeyAccessor)"` bound to `x => x.Id` / `x => x.AggregateId` / `x => x.Key` (first-match resolution per D18) or `x => x` fallback when no id-shaped property exists. **And** the outer container emits `<div data-fc-datagrid="{viewKey}" @onscroll="OnScrollAsync">` (the `@onscroll` handler delegates to `DataGridScrollInterop.CaptureScrollDebounceAsync` with a 150 ms `TimeProvider`-anchored debounce per D4). **And** the `DetailRecord` and `Timeline` strategies are UNCHANGED — they do not use `FluentDataGrid` for their primary content (FluentCard + FluentStack respectively). | T1, T2, T3, T5 |
| **AC2** | **Given** a projection whose `state.Items.Count < FcShellOptions.VirtualizationServerSideThreshold` (default 500). **When** the view renders. **Then** `FluentDataGrid<T>.Items` is bound to `state.Items.AsQueryable()` (Story 1-5 client-side path), and `ItemsProvider` is `null`. **Given** a projection whose `state.Items.Count >= VirtualizationServerSideThreshold`. **When** the view renders. **Then** `Items` is `null` and `ItemsProvider` is bound to the private `LoadPageAsync` method. **When** `ItemsProvider` is invoked by Fluent (on scroll into a new page range). **Then** the method dispatches `LoadPageAction(viewKey, request.StartIndex, request.Count, snapshot.Filters, snapshot.SortColumn, snapshot.SortDescending, snapshot.Filters.GetValueOrDefault("__search"))`, awaits the matching `TaskCompletionSource` in `LoadedPageState.PendingCompletionsByKey[(viewKey, request.StartIndex)]` (registered synchronously in `LoadPageEffect` before the `IQueryService` call), and returns `new GridItemsProviderResult<T>(items, totalCount)` when the effect resolves via `LoadPageSucceededAction`. **When** `request.CancellationToken` is signalled (user scrolls past). **Then** the provider cancels the pending await via `Task.WhenAny` against a `CancellationToken.WaitHandle` task; the effect observes cancellation and bails out of the query. **And** exactly ONE of `Items` / `ItemsProvider` is non-null at a time (Fluent constraint — the generator emits a runtime `if/else` branch, NOT both bound at once). | T2, T3, T4, T6 |
| **AC3** | **Given** a generated projection view with virtualization enabled and 500 rows of test data. **When** the bUnit benchmark test renders the view once. **Then** measured P95 render time across 10 runs is < 300ms (NFR4 gate). **Given** the slow-query banner threshold. **When** `LoadPageEffect.HandleLoadPage` measures elapsed time via `Stopwatch` and the value exceeds `FcShellOptions.SlowQueryThresholdMs` (default 2000 ms). **Then** `LoadPageSucceededAction` carries the elapsed time; `LoadedPageReducers` writes it into `LastElapsedMsByKey[viewKey]`; `FcSlowQueryNotice` becomes visible at next render with localised copy `"Loading is slow. Add filters to narrow results."` (`SlowQueryNoticeMessage`). **When** the banner has been visible for 5 s (`FakeTimeProvider.Advance(5_000)` in tests). **Then** the banner is auto-dismissed; the banner component's private `_dismissed` field is set; the banner does NOT re-appear for the current view without a fresh slow-query signal (the dismiss state is component-scoped, not feature-state; re-visiting the view via route change resets it). **And** the auto-dismiss duration matches the Story 2-4 `ConfirmedToastDurationMs` default (5 s) — consistent auto-dismiss rhythm across Info banners. | T1, T3, T4, T8 |
| **AC4** | **Given** a projection with NO active filters whose total unfiltered row count equals `FcShellOptions.MaxUnfilteredItems` (default 10000 — the server clamps `Take` at the cap; `LoadPageEffect.HandleLoadPage` reads `MaxUnfilteredItems` from options and passes the clamped value via `QueryRequest.Take` when no filters are present). **When** the view renders. **Then** `FcMaxItemsCapNotice` is visible with localised copy `"Showing first {cap} items. Use filters to find specific records."` (`MaxItemsCapNoticeTemplate`, cap interpolated) — visibility condition is `state.Items.Count == FcShellOptions.MaxUnfilteredItems && !snapshot.Filters.Any(kvp => !kvp.Key.StartsWith("__"))` (the reserved-key entries `__status`/`__search`/`__hidden` are not treated as "active filters" here — only real column filters dismiss the cap notice; reserved-key sort + hidden-columns DOES NOT count as filtering). **Given** filters become active. **When** the view re-renders. **Then** the cap notice disappears (reactive to the `any-real-filter` condition). **And** the cap notice does NOT auto-dismiss — it remains visible as long as the cap condition holds. | T1, T2, T4 |
| **AC5** | **Given** a projection whose transformed `model.Columns.Count > 15` (UX-DR63 threshold). **When** the emitter runs. **Then** HFC1029 Information fires (per-projection dedupe), AND the `FluentDataGrid`'s `ChildContent` is wrapped in `<FcColumnPrioritizer ViewKey="@viewKey" AllColumns="@_allColumnsDescriptor" HiddenColumns="@_hiddenColumns" MaxVisibleColumns="10">`. The prioritizer component only renders its internal column list plus the "More columns ({N} hidden)" button — the `PropertyColumn` components themselves are emitted inside the wrap as they were in Story 4-3, but those with `Key` in `HiddenColumns` skip rendering via an `@if` check. **Given** the prioritizer's "More columns" button is clicked. **When** the user opens the popover. **Then** a `FluentPopover` renders with a `role="dialog"` + `aria-labelledby="{popoverTitleId}"`; the popover body contains `FluentCheckbox` elements — one per hidden column — each bound via two-way to a `HiddenColumns.Contains(key)` inverted state. **When** a checkbox is toggled. **Then** `ColumnVisibilityChangedAction(viewKey, columnKey, isVisible)` is dispatched; the reducer reads the current `Filters["__hidden"]` CSV, adds or removes the column key, writes back (removes the entry when CSV becomes empty), and chains `CaptureGridStateAction`. **Given** a projection with `Columns.Count <= 15`. **When** the emitter runs. **Then** the `FcColumnPrioritizer` wrap is OMITTED from emission (`CounterProjectionApprovalTests` regression gate for Story 1-5 + 4-1 + 4-3 default path). | T1, T2, T3, T5 |
| **AC6** | **Given** a user toggles column visibility on Projection A in view A. **When** the user navigates to view B and returns to view A within the session. **Then** the prioritizer respects the persisted `__hidden` CSV from `GridViewSnapshot.Filters` (restored per Story 4-3 AC5 discipline — `OnInitialized` dispatches `RestoreGridStateAction` BEFORE the first `LoadAction`). **When** the user closes the browser and reopens hours later. **Then** the column visibility STATE is persisted (LocalStorage via Story 3-6 effect); scroll position is NOT (cross-session `ScrollTop` is clamped to 0 on hydrate per D5). **Given** the popover is open. **When** the user presses `Tab`. **Then** focus cycles through the checkboxes in column declaration order; `Esc` closes the popover; `Enter` or `Space` toggles the focused checkbox. **And** the "More columns" button's `aria-label` resolves to `"{N} columns hidden. Activate to show more."` via `PrioritizerMoreColumnsAriaLabelTemplate`; the popover announces `"Column visibility"` (`PrioritizerColumnsPanelTitle`) via `aria-labelledby`. | T1, T3, T5, T8 |
| **AC7** | **Given** the grid container is emitted with `data-fc-datagrid="{viewKey}"` + `@onscroll="OnScrollAsync"`. **When** the user scrolls. **Then** `OnScrollAsync` reads `event.target.scrollTop` via `JSInvokable`, dispatches a debounced callback (150 ms per D4), and on debounce-complete dispatches `ScrollCapturedAction(viewKey, scrollTop)`. **Given** the reducer receives the action. **When** the reducer executes. **Then** it validates `scrollTop >= 0 && !double.IsNaN(scrollTop) && !double.IsInfinity(scrollTop)` (defence-in-depth — matches `GridViewSnapshot` private-setter validation), updates the snapshot's `ScrollTop`, and chains `CaptureGridStateAction` so 3-6's effect persists. **Given** the view mounts and `RestoreGridStateAction` hydrates a snapshot with non-zero `ScrollTop`. **When** the WITHIN-SESSION navigation condition holds (the reducer pathway ending in `CaptureGridStateAction` was the most recent source — NOT the `GridViewHydratedAction` pathway from LocalStorage). **Then** `OnAfterRenderAsync(firstRender: true)` calls `DataGridScrollInterop.ScrollToOffsetAsync(viewKey, snapshot.ScrollTop)` which locates the scroll container and sets its `.scrollTop` property. **Given** the hydration source is LocalStorage (cross-session startup). **When** `GridViewHydratedAction` lands. **Then** the reducer clamps `ScrollTop` to 0 before writing to `ViewStates` (per D5 — cross-session scroll restore is disorienting, intentionally suppressed). **And** the capture rate of 150 ms is tighter than Story 4-3's 300 ms filter debounce because scroll events fire at ~60 Hz and a 300 ms debounce would feel laggy; 150 ms is within the Schroeder 100-200 ms "instant" threshold. | T1, T2, T3, T4, T6 |

---

## Critical decisions (READ FIRST — do NOT revisit)

> **22 binding decisions.** Feature-story budget is ≤ 25 (L06). 4-4 lands at 22/25, leaving a 3-decision buffer for review rounds before Occam / Matrix trimming is required. Raise a spec-change proposal before revisiting any entry.

| # | Decision | Rationale | Consumed by |
|---|---|---|---|
| **D1** | **`Virtualize="true"` is emitted on EVERY generated `FluentDataGrid` unconditionally** — Default / ActionQueue / StatusOverview / Dashboard strategies (DetailRecord + Timeline do not emit `FluentDataGrid`). No opt-out, no per-projection annotation to disable. | UX-DR42 states virtualization is THE DEFAULT; opt-out surfaces proliferate code paths and give adopters rope to hang themselves with (a 50,000-row unvirtualized grid is the 0-user-actually-wants scenario). Virtualize-at-10-items is no slower than non-virtualize-at-10-items in Fluent v5 (verified via the Fluent team's benchmarks in the linked docs). Rejected: (a) activate when `Items.Count > N` — two code paths to test, user sees different scroll behaviour before/after filtering; (b) `[NoVirtualize]` attribute — negative-opt-out is an anti-pattern; (c) configurable via `FcShellOptions.EnableVirtualization` — too coarse; Epic 6 Level 3 slot override handles the rare opt-out. | T2; AC1 |
| **D2** | **`VirtualizationServerSideThreshold` (default 500) is the `Items.Count`-based runtime switch between client-side and server-side virtualization.** Below → `Items=state.Items.AsQueryable()`; at/above → `ItemsProvider=LoadPageAsync`. The generator emits BOTH paths and the runtime branch picks. | UX-DR42's "<500 client-side, 500+ server-side" maps 1:1 to a runtime comparison. Emitting both at emit time means the adopter doesn't need to recompile when their data volume crosses the threshold — the switch is per-render. Rejected: (a) emit-time branch based on projection metadata — projections don't know their runtime cardinality; (b) always server-side — wastes a round-trip for 10-row projections; (c) always client-side with manual paging API — contradicts UX-DR42 no-load-more discipline. | T2, T3; AC2 |
| **D3** | **`GridItemsProvider<T>` is implemented as a private method on the generated class that dispatches `LoadPageAction` + awaits a `TaskCompletionSource<object>` registered by the effect.** The TCS lives in `LoadedPageState.PendingCompletionsByKey[(viewKey, skip)]`; the effect registers it synchronously BEFORE awaiting `IQueryService.QueryAsync` so races are impossible. The provider callback awaits `await Task.WhenAny(tcs.Task, cancellationTask)` — cancellation from Fluent's scroll-away unwinds cleanly. | Fluxor-native — every side effect goes through the action pipeline; no direct service access from the generated view. Rejected: (a) direct `IQueryService.QueryAsync` call from the provider — bypasses Fluxor discipline, double-loads overlap with `LoadAction`; (b) `IObservable<GridItemsProviderResult<T>>` subscription — heavier machinery for what is a one-shot request/response; (c) promise-per-page shared memoization dictionary — the TCS pattern IS the memoization keyed by `(viewKey, skip)`. | T1, T3, T4; AC2 |
| **D4** | **Scroll capture uses a 150 ms `TimeProvider`-anchored debounce on `@onscroll`** — tighter than Story 4-3's 300 ms filter debounce because scroll events fire at ~60 Hz and a 300 ms debounce would feel laggy when the user stops scrolling. The debounce anchor is the injected `TimeProvider` (FakeTimeProvider in tests). Per-view `CancellationTokenSource`; on dispose the CTS cancels. | Schroeder's 100-200 ms "instant" threshold — 150 ms is the midpoint. Reuses the Story 3-6 `TimeProvider.Delay(DebounceInterval, ct)` idiom. Rejected: (a) 300 ms to match 4-3 — user-visible lag; (b) `requestAnimationFrame` batching — JS-side complexity for no material win; (c) no debounce, dispatch every scroll — 60 Hz dispatch storm; (d) capture on scroll-END only (`scrollend` event) — only Chromium supports reliably, Safari/Firefox parity concerns. | T1, T4; AC7 |
| **D5** | **Cross-session hydration of `GridViewSnapshot.ScrollTop` is CLAMPED to 0.** The reducer handling `GridViewHydratedAction` (defined in Story 2-2 / 3-6) is extended by 4-4 to zero-out `ScrollTop` before writing to `ViewStates`. Within-session dispatches — `ScrollCapturedAction` → `CaptureGridStateAction` → existing 2-2 reducer — pass through unchanged. | Restoring a scroll position from hours ago is disorienting — the user has lost context, the data may have shifted (SignalR reconciliation in Epic 5 could have inserted rows), and there's nothing on screen to orient against. UX convention: scroll position is within-session only. Column-visibility is cross-session because it's a user PREFERENCE, not a transient pointer. Filter state is cross-session because it's a user INTENT, not a transient pointer. Rejected: (a) restore across sessions — disorienting, no user research supports it; (b) restore with a confirmation prompt — friction for a feature users don't ask for; (c) restore only when elapsed-since-capture < 1h — arbitrary, still surprising; (d) restore only if the row count is unchanged — requires pre-hydration count query, round-trip cost. | T2, T3, T4; AC6, AC7 |
| **D6** | **`FcColumnPrioritizer` activates when `model.Columns.Count > 15` (strict greater-than; 15 columns is still bare-grid).** The first 10 columns by priority (D17 stable sort: `(Priority ?? int.MaxValue, DeclarationOrder)`) render by default; the remainder are hidden-by-default behind the "More columns" toggle. `MaxVisibleColumns=10` is hardcoded in the generator emission (UX-DR7 "first 8-10 columns" — 10 is the high end, chosen to give adopters slightly more breathing room before the toggle is needed). | UX-DR63 uses "15 fields" as the trigger; strict `>` means "exactly 15 is fine, 16 triggers." 10 as the visible default balances UX-DR7's "8-10" range toward the user-forgiving end. Rejected: (a) trigger at `>=` 15 — contradicts UX-DR63 literal reading; (b) `MaxVisibleColumns` exposed as an option — adopters with "16 columns, show 12" is a rare case, Epic 6 template override handles it; (c) dynamic visible count based on viewport width — Epic 10-2 responsive work. | T1, T2; AC5 |
| **D7** | **Column visibility is packed into the existing `GridViewSnapshot.Filters` dictionary under the reserved key `__hidden`** (CSV of hidden column keys). Matches Story 4-3 D3 reserved-key pattern (`__status`, `__search`). No new field on `GridViewSnapshot`, no schema change to `GridViewPersistenceBlob`. | Single persistence surface means single serialisation surface means single test surface. The 4-3 `__` prefix convention is load-bearing — 4-4 adds one more reserved key without breaking the contract. Column names starting with `__` are prevented by C# identifier rules (the adopter would need `@__hidden` to even declare such a property, and even then the generator emits property names without `@` — collision at emit time requires pathological effort). Rejected: (a) add `HiddenColumns: EquatableArray<string>` field to `GridViewSnapshot` — blob schema bump, cascades to `BlobSerializesToExpectedJsonShape` regen + persistence-blob migration; (b) separate dictionary `SpecialFilters: Dictionary<string, string>` — doubles serialization surface; (c) JSON-encoded sub-state in a reserved `__meta` key — opaque in LocalStorage devtools, hard to inspect. | T1, T2, T3, T4; AC5, AC6 |
| **D8** | **Slow-query detection is `Stopwatch`-based inside `LoadPageEffect.HandleLoadPage`** — measures elapsed time from effect entry to `IQueryService.QueryAsync` completion; emits `LoadPageSucceededAction.ElapsedMs`; the reducer writes into `LastElapsedMsByKey[viewKey]`. `FcSlowQueryNotice` subscribes and renders when the value exceeds `FcShellOptions.SlowQueryThresholdMs`. | In-effect measurement captures the user-observed latency (DI resolution + network + deserialization). Pushing into the component would miss the effect-resolution prefix. Rejected: (a) measure in the generated view via `DateTimeOffset.UtcNow` — clock-skew concerns in WASM + cross-machine time jitter; `Stopwatch` is monotonic; (b) measure in `IQueryService` itself — adopters may forget to surface; (c) measure via `Activity` spans — overkill for a single metric, OpenTelemetry surface is Epic 5. | T3, T4; AC3, AC4 |
| **D9** | **Row-count display is EMITTED above the grid but OWNED per-condition: Story 4-3's `FcFilterSummary` handles `{filteredCount} of {totalCount}` when filters are active; 4-4 renders a simpler `{count} {entityPlural}` label when filters are inactive.** The two are mutually exclusive — the generator emits a runtime `@if (anyFilterActive) { <FcFilterSummary .../> } else { <span>@unfilteredLabel</span> }`. | Exactly one count surface visible at a time prevents the "3 of 47 and also 47 orders" confusion. Rejected: (a) always render 4-3's `FcFilterSummary` with a "no filter" mode — pollutes 4-3's component contract; (b) always render 4-4's row-count label — duplicates 4-3's count info when filtered; (c) no row-count display without filters — loses the "47 orders" scannability signal. | T1, T2; AC4 |
| **D10** | **Three new `FcShellOptions` properties: `MaxUnfilteredItems=10_000`, `SlowQueryThresholdMs=2_000`, `VirtualizationServerSideThreshold=500`.** Each gets a `[Range]` attribute constraining pathological values. `FcShellOptionsThresholdValidator` adds a cross-property invariant: `VirtualizationServerSideThreshold < MaxUnfilteredItems`. | Defaults match UX-spec `Pagination & Virtual Scrolling` literal values (10k / 2s / 500). `[Range]` prevents adopter misconfig from blowing up the runtime (e.g. `MaxUnfilteredItems=0` would show an empty grid always). Cross-property validator ensures the server-side lane triggers BEFORE the cap (otherwise the cap would be reached via a full-render path). Rejected: (a) hardcoded constants — adopters whose infra can handle 100k rows need the override; (b) two options only (skip server-side threshold) — conflates policy (`MaxUnfilteredItems` is infrastructure capacity) with mechanism (`VirtualizationServerSideThreshold` is UX lane-switch); (c) per-projection override via attribute — Epic 6 customization territory. | T7, T8; AC3, AC4 |
| **D11** | **`FcMaxItemsCapNotice` is NON-dismissing — it stays visible as long as the cap condition holds.** `FcSlowQueryNotice` is AUTO-dismissing after 5 s. | The cap is an ongoing infrastructure constraint — dismissing would hide the user's need to filter. The slow-query notice is a one-time signal — once the user has seen it and applied filters (or chosen to ignore), re-showing on every subsequent render is nagging. Rejected: (a) both auto-dismiss — cap notice becomes invisible after user blinks; (b) both non-dismiss — slow-query banner nags forever; (c) dismiss-with-session-memory — adds LocalStorage surface for ephemeral signal, scope creep. | T1; AC3, AC4 |
| **D12** | **Virtualization is applied to the `EmitStandardDataGrid` path ONLY — `DetailRecord` (FluentCard) and `Timeline` (FluentStack) strategies are untouched.** | These strategies do not render `FluentDataGrid` — virtualization is meaningless. DetailRecord renders a single card; Timeline renders a vertical stack of ≤ 50 events typically. If Timeline ever needs virtualization, the owning story would extend `EmitTimelineBody` to wrap `FluentStack` content in `<Virtualize>` directly — but that's Epic 4-7 / 10-2 territory, not 4-4. Rejected: (a) retrofit virtualization into Timeline — out of scope; (b) emit a hybrid FluentDataGrid for Timeline with `DisplayMode="Table"` — violates Story 4-1 AC4 (Timeline MUST be FluentStack-based). | T2, T3; AC1 |
| **D13** | **`ItemKey` is resolved at emit time via `ResolveItemKeyAccessor(model)` — first-match over `Id` / `AggregateId` / `Key` property names.** Fallback: `x => x` (item-as-key — Blazor will use reference equality, acceptable for immutable records). | `ItemKey` drives Blazor's `@key` directive inside the virtualized row renderer; stable keys prevent ghost-render bugs when pages swap in/out. Projection types generally expose an identifier field; first-match resolution covers DDD-style `AggregateId`, CRUD-style `Id`, and key-value shapes. Fallback to `x => x` is safe for records (value equality). Rejected: (a) require an `[ItemKey]` attribute on projection types — friction for adopters; (b) use `HashCode.Combine` over all properties — overhead per render; (c) fallback to `typeof(T).Name + index` — breaks with reordering. | T1, T2; AC1 |
| **D14** | **`ColumnPriorityAttribute` accepts ANY 32-bit int including negatives.** Lower = higher priority. `int.MinValue` is accepted (reserved use case: adopters who want "pin this column to front" without introducing a flag enum). Unannotated = `null` in the IR, materialized as `int.MaxValue` for sort purposes. | Simpler attribute shape; future-proofs for adopters needing "pin-first" or "pin-last" semantics via extreme values. Rejected: (a) `[Range(1, 1000)]` on the attribute — restrictive; adopters with 500 properties need more room; (b) enum-based priority (`High`/`Medium`/`Low`) — loses fine-grained control; (c) `double` priority — floating-point equality comparison pitfalls. | T7; AC5 |
| **D15** | **HFC1028 fires at PARSE stage when a projection has ≥ 2 properties with the SAME explicit non-null `[ColumnPriority]` value.** HFC1029 fires at EMIT stage when the sorted `Columns.Count > 15`. Both Information severity; both per-projection-type deduplicated. | Parse-stage for HFC1028 matches Story 4-1 HFC1022 precedent (parse-time diagnosis of adopter input). Emit-stage for HFC1029 matches Story 4-3 HFC1027 precedent (emit-time recognition of a model structure — the transform stage has produced the final column list). Information severity (not Warning) because both are legitimate adopter choices — two columns can have the same priority if the adopter accepts declaration-order tiebreak; > 15 columns is a conscious modelling choice. Rejected: (a) HFC1028 at emit — redundant information is available at parse; (b) Warning severity — wrong signal for intentional states; (c) single HFC code for both conditions — different payload shapes. | T7, T9; AC5 |
| **D16** | **`LoadedPageState.PendingCompletionsByKey` uses `TaskCompletionSource<object>` (non-generic) because Fluxor feature states are non-generic.** The `GridItemsProvider<T>` callback casts the resolved `object` to `IReadOnlyList<T>` — type safety is lost at the dictionary boundary and recovered at the callback. Documented as a v1 seam. | Fluxor requires feature state to be concrete types (not open generics). `TaskCompletionSource<object>` sidesteps the generic constraint without introducing per-T feature states (which would multiply the state surface by the number of projections). The cast is safe-by-construction — the effect always resolves with `IReadOnlyList<T>` where T matches the dispatch. Rejected: (a) `TaskCompletionSource<IReadOnlyList<object>>` — forces an intermediate list copy; (b) per-projection feature state (`LoadedPageState<T>`) — O(projection-count) feature types, registration bloat; (c) direct callback via `Func<int, int, Task<GridItemsProviderResult<T>>>` stored in state — closures-in-state is a Fluxor anti-pattern (state must be serialisable); (d) generator emits a per-projection feature state — same as (b). | T3, T6; AC2 |
| **D17** | **Column sort order is `(Priority ?? int.MaxValue, DeclarationOrder)` with stable sort (preserves declaration order within same priority).** Applied in the Transform stage — `RazorModelTransform` owns the sort; the Emit stage consumes the sorted list as-is. | Deterministic; cacheable (Story 1-4 IR cacheability — same input produces same sort). Stable sort preserves the "feels natural" reading order for unannotated projections. Rejected: (a) emit-time sort — pushes logic into the stage that should only emit; (b) runtime sort in the view — wastes CPU per-render; (c) alphabetical secondary key — loses declaration intent. | T7; AC5 |
| **D18** | **`_itemKeyAccessor` is emitted as a PRIVATE LAZY FIELD on the generated class**: `private readonly Lazy<Func<{TypeName}, object>> _itemKeyAccessor = new(() => ResolveItemKeyAccessor())` where `ResolveItemKeyAccessor` returns the first-match lambda per D13. | Lazy because the accessor is resolved via reflection ONCE per class (not per render); Lazy gives thread-safe init-once semantics under Blazor Server's concurrent circuit. Rejected: (a) eager init in constructor — reflection at constructor time delays first-render; (b) static field — fine for one-type-per-assembly but wrong for generic scenarios; (c) dynamic delegate recompiled per render — pointless re-resolution. | T2; AC1 |
| **D19** | **8 new resource keys for EN + FR, totalling 16 new strings.** Names: `PrioritizerMoreColumnsButtonLabelTemplate` ("More columns ({0} hidden)" / "Plus de colonnes ({0} masquées)"), `PrioritizerMoreColumnsAriaLabelTemplate` ("{0} columns hidden. Activate to show more." / "{0} colonnes masquées. Activez pour en afficher davantage."), `PrioritizerColumnsPanelTitle` ("Column visibility" / "Visibilité des colonnes"), `PrioritizerColumnCheckboxAriaLabelTemplate` ("Toggle visibility of {0}" / "Basculer la visibilité de {0}"), `SlowQueryNoticeMessage` ("Loading is slow. Add filters to narrow results." / "Le chargement est lent. Ajoutez des filtres pour affiner les résultats."), `MaxItemsCapNoticeTemplate` ("Showing first {0} items. Use filters to find specific records." / "Affichage des {0} premiers éléments. Utilisez des filtres pour trouver des enregistrements spécifiques."), `VirtualizedSkeletonRowAriaLabel` ("Loading rows…" / "Chargement des lignes…"), `UnfilteredRowCountTemplate` ("{0} {1}" / "{0} {1}"). French translations follow Story 4-1 / 4-2 / 4-3 conventions (U+00A0 where punctuation requires, though the current keys have no French double-punctuation that needs NBSP — the U+2026 HORIZONTAL ELLIPSIS is used for "Loading…" consistent with Fluent UI conventions). | Narrower than 4-3's 10-key addition (13 in final after Sally roundtable) — 4-4's visual surface is smaller (one prioritizer + two banners + one row-count label) than 4-3's six-component filter surface. Rejected: (a) reuse 4-3's `FilterSummaryShowingTemplate` for the unfiltered count — semantic mismatch ("Showing N of M" vs "N {entities}"); (b) hardcoded English strings in components — breaks UX-DR60 localization contract; (c) ICU-format keys — Epic 9-5 migration. | T8; AC1-AC7 |
| **D20** | **`ItemSize` pixel values are `32`/`44`/`56` for `Compact`/`Comfortable`/`Roomy`** — resolved at RENDER time from `RenderContext.Density` via `DataGridDensityMetrics.ResolveRowHeightPx(Density)`. Not resolved at emit time because density is user-scoped. | Fluent v5's `FluentDataGrid.ItemSize` is `float` (pixels). The three values align with the Story 3-3 density levels. Runtime resolution means a user who switches density via Story 3-3's FcSettingsDialog sees the change without a view re-mount. Rejected: (a) hardcode one ItemSize (44px) — breaks density; (b) resolve via CSS variable — Fluent requires the float value for virtualization sizing math; (c) emit density-specific views — triples the emit surface. | T1, T2; AC1 |
| **D21** | **`DisplayMode="DataGridDisplayMode.Table"` is required when `Virtualize="true"`** — per Fluent v5 docs (see `FluentDataGrid` MCP: *"Specifically when using Virtualize, it is highly recommended to use the Table display mode as the Grid mode can exhibit odd scrolling behavior"*). | Non-negotiable Fluent constraint; following Fluent's recommendation eliminates a class of hard-to-repro scroll-jitter bugs. Rejected: (a) stay with `DisplayMode="Grid"` — scroll bugs in 4-4's first-look review would reopen; (b) conditional display mode — adds runtime logic for no benefit (Table mode works correctly without virtualize too). | T2; AC1 |
| **D22** | **Test volume: 53 tests. Matrix cut (L07): 8 candidates scored below 3.0 — deferred to Story 10-2 visual specimen (theme × density ItemSize, RTL popover, scroll across browsers), Playwright CI (cross-browser scroll), Epic 5 (100k-row adopter-facing harness), Epic 9-5 (memory-leak monitoring for page cache).** Shell components (12), reducers + effects + round-trip (13), emitter snapshots (12), diagnostics (6), contracts + options (7 — includes the existing validator extension), resource parity (3). | Narrower than 4-3 (62/22 ≈ 2.8) because 4-4 reuses more infrastructure verbatim: scroll capture is a single-action pipeline reusing 4-3's debounce pattern; column-visibility reducer is ~40 lines mirroring 4-3's filter reducer pattern; the only truly new machinery is `LoadPageEffect` (server-side page fetch) and `FcColumnPrioritizer` (popover UI). Matrix scoring per L07 identified the cut candidates (all legitimate concerns) and assigned them to owning stories per L10. 53/22 ≈ 2.4 tests per decision. | T3, T4, T5, T6, T7, T8, T9 |

---

## Tasks / Subtasks

### T1 — Shell components for prioritizer + banners + scroll interop (AC1, AC4, AC5, AC6, AC7; ≈ 4h)

- [ ] **T1.1** Create `src/Hexalith.FrontComposer.Shell/Components/DataGrid/FcColumnPrioritizer.razor` + `.razor.cs` per D6. Parameters: `ViewKey` (EditorRequired), `AllColumns` (`IReadOnlyList<ColumnDescriptor>`, EditorRequired), `HiddenColumns` (`IReadOnlyList<string>`, default empty), `MaxVisibleColumns` (`int`, default 10), `ChildContent` (`RenderFragment<ColumnVisibilityContext>`, EditorRequired — the generator emits the column `@foreach` body inside). Renders the "More columns ({N} hidden)" `FluentButton Appearance="Outline"` to the right of the last visible header cell; clicking opens a `FluentPopover` with a `FluentCheckbox` list; toggling dispatches `ColumnVisibilityChangedAction`. `role="dialog"` on popover; `aria-labelledby="{popoverTitleId}"`.
- [ ] **T1.2** Create `src/Hexalith.FrontComposer.Shell/Components/DataGrid/FcSlowQueryNotice.razor` + `.razor.cs` per D8/D11. `FluentMessageBar Intent="Info"` wired to `IState<LoadedPageState>.Value.LastElapsedMsByKey[ViewKey] > ShellOptions.CurrentValue.SlowQueryThresholdMs`. Auto-dismiss after 5 s via `FakeTimeProvider`-anchored `Task.Delay`. Dismiss state is component-scoped (`_dismissed` bool).
- [ ] **T1.3** Create `src/Hexalith.FrontComposer.Shell/Components/DataGrid/FcMaxItemsCapNotice.razor` + `.razor.cs` per D11. `FluentMessageBar Intent="Info"` visible when `state.Items.Count == ShellOptions.MaxUnfilteredItems && !anyRealFilterActive` (where `anyRealFilterActive` excludes reserved keys starting with `__`). Non-dismissing.
- [ ] **T1.4** Create `src/Hexalith.FrontComposer.Shell/Services/DataGridScrollInterop.cs` per D4. Injects `IJSRuntime`. Methods: `CaptureScrollDebounceAsync(viewKey, scrollTop, dotnetRef, ct)` — invokes JS `captureScrollDebounce(viewKey, scrollTop, dotnetRef)` which applies a per-viewKey 150 ms debounce and calls back into .NET via `[JSInvokable]`; `ScrollToOffsetAsync(viewKey, scrollTop)` — invokes `scrollToOffset(viewKey, scrollTop)` which locates the scroll container and applies the offset.
- [ ] **T1.5** Create `src/Hexalith.FrontComposer.Shell/wwwroot/js/fc-datagrid.js`. Export `captureScrollDebounce(viewKey, scrollTop, dotnetRef)` — per-viewKey `Map<string, number>` of pending timeout IDs; on new call, clear pending, set a new timeout for 150 ms; timeout callback invokes `dotnetRef.invokeMethodAsync('HandleScrollDebounceAsync', viewKey, scrollTop)`. Export `scrollToOffset(viewKey, scrollTop)` — queries `[data-fc-datagrid="${viewKey}"] .fluent-data-grid-scroll-container` (or the Fluent-specific selector for the inner scroller), sets `.scrollTop = scrollTop`; no-op if element missing.
- [ ] **T1.6** Register the script in `src/Hexalith.FrontComposer.Shell/Shell/FrontComposerShell.razor` alongside `fc-keyboard.js` — add `<script type="module" src="_content/Hexalith.FrontComposer.Shell/js/fc-datagrid.js"></script>`.
- [ ] **T1.7** Smoke-test the components in a manual scratch page or existing specimen: seed a faked projection with 20 columns; assert the prioritizer wrapping activates + the popover renders + the checkbox toggles dispatch actions.

### T2 — Source-generator emit-stage upgrade (AC1, AC2, AC4, AC5; ≈ 4h)

- [ ] **T2.1** Modify `src/Hexalith.FrontComposer.SourceTools/Emitters/ProjectionRoleBodyEmitter.cs` — in `EmitStandardDataGrid`, emit `Virtualize="true"`, `DisplayMode="DataGridDisplayMode.Table"`, `ItemSize="@(DataGridDensityMetrics.ResolveRowHeightPx(RenderContext.Density))"`, `OverscanCount="3"`, `ItemKey="@(_itemKeyAccessor.Value)"` attributes on the `FluentDataGrid`. Emit the `_itemKeyAccessor` as a private field per D18. Resolution helper `ResolveItemKeyAccessor` is a new static on the emitter (first-match over `Id`/`AggregateId`/`Key`).
- [ ] **T2.2** Emit the `FcColumnPrioritizer` wrap conditionally per D6: when `model.Columns.Count > 15`, wrap the inner `ChildContent` render fragment in `<FcColumnPrioritizer ViewKey="@viewKey" AllColumns="@_allColumnsDescriptor" HiddenColumns="@(snapshot.Filters.TryGetValue(\"__hidden\", out var csv) ? csv.Split(',') : Array.Empty<string>())" MaxVisibleColumns="10" ChildContent="...">`. Emit the `_allColumnsDescriptor` initializer reading from the transformed `ColumnModel` list.
- [ ] **T2.3** Emit the client-side vs server-side runtime branch per D2: `if (state.Items.Count >= _shellOptions.Value.VirtualizationServerSideThreshold)` → `ItemsProvider="@LoadPageAsync"`, `Items=null`; else → `Items="@state.Items.AsQueryable()"`, `ItemsProvider=null`. Emit the `LoadPageAsync` private method per D3.
- [ ] **T2.4** Emit the two banners (`FcSlowQueryNotice` + `FcMaxItemsCapNotice`) above the grid, below Story 4-3's filter surface. Both always emit; visibility is component-side gated.
- [ ] **T2.5** Emit the outer container `<div data-fc-datagrid="@viewKey" @onscroll="OnScrollAsync">` per D4 (or preserve the Story 4-3-emitted `data-fc-datagrid` and add the `@onscroll` handler). Emit `OnScrollAsync` method that reads `ScrollTop` via `ScrollEventArgs` → calls `DataGridScrollInterop.CaptureScrollDebounceAsync`.
- [ ] **T2.6** Emit the `OnAfterRenderAsync(firstRender: true)` override per D5/AC7: if the hydrated snapshot has non-zero `ScrollTop` AND the hydration source was within-session (NOT cross-session — tracked by a feature-state flag `HydratedFromLocalStorageByKey[viewKey]` that is reset on first Capture), call `DataGridScrollInterop.ScrollToOffsetAsync(viewKey, snapshot.ScrollTop)`.
- [ ] **T2.7** Emit the row-count display per D9: `@if (anyRealFilterActive) { (Story 4-3 FcFilterSummary handles it) } else { <span class="fc-row-count">@string.Format(UnfilteredRowCountTemplate, state.Items.Count, entityPlural)</span> }`.
- [ ] **T2.8** Verify Story 4-3 + 4-1 regression: `CounterProjectionApprovalTests` stays green (counter has few columns, no ColumnPriority, under the 15 threshold → no prioritizer wrap; the Virtualize + ItemSize + DisplayMode additions ARE captured in the snapshot — regenerate the baseline deliberately).

### T3 — Fluxor state: LoadedPageFeature + reducers + effects + action records (AC2, AC3, AC5, AC6, AC7; ≈ 4h)

- [ ] **T3.1** Create `src/Hexalith.FrontComposer.Contracts/Rendering/VirtualizationActions.cs` with the 5 action records per D3/D7. Each constructor validates `ViewKey` non-null/whitespace (matches Story 2-2 D30 discipline); `LoadPageAction` validates `Skip >= 0`, `Take > 0`, `Take <= int.MaxValue`; `ScrollCapturedAction` validates `ScrollTop >= 0 && finite`.
- [ ] **T3.2** Create `src/Hexalith.FrontComposer.Contracts/Rendering/VirtualizationReservedKeys.cs` with `HiddenColumnsKey = "__hidden"` public const.
- [ ] **T3.3** Create `src/Hexalith.FrontComposer.Shell/State/DataGridNavigation/LoadedPageState.cs` per D16 — record with four `ImmutableDictionary` fields; TCS dictionary keyed by `(string ViewKey, int Skip)`.
- [ ] **T3.4** Create `src/Hexalith.FrontComposer.Shell/State/DataGridNavigation/LoadedPageFeature.cs` — `Feature<LoadedPageState>` with empty initial state.
- [ ] **T3.5** Create `src/Hexalith.FrontComposer.Shell/State/DataGridNavigation/LoadedPageReducers.cs`:
  - `LoadPageSucceededReducer`: writes items into `PagesByKey[(viewKey, skip)]`, updates `TotalCountByKey[viewKey]` + `LastElapsedMsByKey[viewKey]`, resolves `PendingCompletionsByKey[(viewKey, skip)]` TCS (calls `SetResult(items)`), removes the TCS entry.
  - `LoadPageFailedReducer`: resolves TCS with empty items (`SetResult(EmptyList)`) — allows the provider callback to surface a user-visible empty state without crashing; separate `LastQueryErrorByKey` dictionary tracks the error message for future Epic 5 error-state component.
  - `ColumnVisibilityChangedReducer`: reads current `Filters["__hidden"]` CSV, adds/removes column key, writes back, chains `CaptureGridStateAction`.
  - `ScrollCapturedReducer`: updates `GridViewSnapshot.ScrollTop` (defensive revalidation per D4), chains `CaptureGridStateAction`.
- [ ] **T3.6** Create `src/Hexalith.FrontComposer.Shell/State/DataGridNavigation/LoadPageEffects.cs`:
  - `HandleLoadPageAsync(LoadPageAction action, IDispatcher dispatcher)`: resolves `IQueryService` from an injected `IServiceProvider`, constructs `QueryRequest` from the action's filter/sort/search, measures elapsed via `Stopwatch.StartNew()` + `Stopwatch.Stop()`, `await IQueryService.QueryAsync(request, ct)`, on success dispatches `LoadPageSucceededAction(viewKey, skip, result.Items, result.TotalCount, stopwatch.ElapsedMilliseconds)`, on exception dispatches `LoadPageFailedAction(viewKey, skip, ex.Message)`. Registers the TCS in `PendingCompletionsByKey` BEFORE invoking the service (race-free per D3).
- [ ] **T3.7** Extend `src/Hexalith.FrontComposer.Shell/State/DataGridNavigation/DataGridNavigationReducers.cs` (or the existing hydration reducer): on `GridViewHydratedAction`, if the hydration source flag indicates LocalStorage (via the `HydratedFromLocalStorageByKey` boolean dictionary tracked in the reducer), clamp `ScrollTop` to 0 per D5. Within-session `CaptureGridStateAction` chains pass through unchanged.
- [ ] **T3.8** Register `LoadedPageFeature` + `LoadPageEffects` + `DataGridScrollInterop` in `src/Hexalith.FrontComposer.Shell/Extensions/ServiceCollectionExtensions.cs` alongside existing Fluxor registrations. Scoped lifetime for `DataGridScrollInterop`.

### T4 — Tests: Shell components + reducers + effects + round-trip + scroll restore (AC1-AC7; ≈ 5h)

- [ ] **T4.1** Create `tests/Hexalith.FrontComposer.Shell.Tests/Components/DataGrid/FcColumnPrioritizerTests.cs` (6 tests per cheat sheet).
- [ ] **T4.2** Create `tests/Hexalith.FrontComposer.Shell.Tests/Components/DataGrid/FcSlowQueryNoticeTests.cs` (3 tests).
- [ ] **T4.3** Create `tests/Hexalith.FrontComposer.Shell.Tests/Components/DataGrid/FcMaxItemsCapNoticeTests.cs` (3 tests).
- [ ] **T4.4** Create `tests/Hexalith.FrontComposer.Shell.Tests/State/DataGridNavigation/LoadPageReducerTests.cs` (5 tests — happy path, failed path, column-visibility chain, scroll-capture chain, TCS resolution ordering).
- [ ] **T4.5** Create `tests/Hexalith.FrontComposer.Shell.Tests/State/DataGridNavigation/LoadPageEffectIntegrationTests.cs` using `FrontComposerTestBase` + a stubbed `IQueryService` (3 tests: happy path, exception path, cancellation path).
- [ ] **T4.6** Create `tests/Hexalith.FrontComposer.Shell.Tests/State/DataGridNavigation/ScrollAndColumnVisibilityRoundTripTests.cs` using `InMemoryStorageService` + `FakeTimeProvider` (3 tests).
- [ ] **T4.7** Create `tests/Hexalith.FrontComposer.Shell.Tests/State/DataGridNavigation/ScrollRestoreWithinSessionTests.cs` using bUnit + a mocked `IJSRuntime` that records `scrollToOffset` invocations (2 tests: within-session restore fires; cross-session hydrate clamps to 0 and does NOT fire).
- [ ] **T4.8** Extend `tests/Hexalith.FrontComposer.Shell.Tests/State/DataGridNavigation/GridViewPersistenceBlobSchemaLockedTests.cs` — assert the `__hidden` reserved key round-trips correctly inside the `Filters` dictionary; schema shape UNCHANGED (no field additions). 2 new assertion groups.
- [ ] **T4.9** Add a bUnit NFR4 benchmark test in `tests/Hexalith.FrontComposer.Shell.Tests/Performance/VirtualizedGridRenderBenchmarkTests.cs` — render the view 10 times over 500 rows, assert P95 < 300 ms. Use `Stopwatch` per render; sort + take [95]th. Mark as `[Category("performance")]` so CI can exclude on underpowered runners.

### T5 — Tests: Source-generator emit snapshots (AC1, AC2, AC4, AC5; ≈ 3h)

- [ ] **T5.1** Create `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RazorEmitterVirtualizationTests.cs` (4 tests — Virtualize/DisplayMode/ItemSize/OverscanCount attributes present; ItemKey resolved to each of Id/AggregateId/Key/fallback scenarios).
- [ ] **T5.2** Create `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RazorEmitterItemsProviderTests.cs` (3 tests — runtime branch emitted with threshold check; LoadPageAsync method emitted; Items=null when ItemsProvider is bound).
- [ ] **T5.3** Create `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RazorEmitterColumnPrioritizerTests.cs` (3 tests — wrap emitted when > 15 columns; wrap omitted at/below 15; priority sort correctness via the transform stage).
- [ ] **T5.4** Create `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RazorEmitterBannersTests.cs` (2 tests — both banners unconditionally emitted with correct parameters).
- [ ] **T5.5** Regenerate `CounterProjectionApprovalTests` baseline to capture the new Virtualize + DisplayMode + ItemSize + OverscanCount + ItemKey attributes AND the two banner components above the grid. Verify the diff is DELIBERATE (no logic drift).
- [ ] **T5.6** CI gate confirmation (Murat roundtable 2026-04-23 precedent from Story 4-3 T5.6): `CounterProjectionApprovalTests` remains the single most load-bearing regression gate; document the lane name + workflow file in Dev Notes so downstream stories can cite by name.

### T6 — Generator transform stage updates (AC5; ≈ 2h)

- [ ] **T6.1** Modify `src/Hexalith.FrontComposer.SourceTools/Parsing/AttributeParser.cs` per D14 — parse `[ColumnPriority(N)]` into `PropertyModel.ColumnPriority` nullable int.
- [ ] **T6.2** Modify `src/Hexalith.FrontComposer.SourceTools/Parsing/PropertyModel.cs` — add `int? ColumnPriority` field (append-only; Equals/GetHashCode updated per Story 1-4 IR cacheability discipline).
- [ ] **T6.3** Modify `src/Hexalith.FrontComposer.SourceTools/Transforms/ColumnModel.cs` — add `int? Priority` field (append-only, Equals/GetHashCode updated).
- [ ] **T6.4** Modify `src/Hexalith.FrontComposer.SourceTools/Transforms/RazorModelTransform.cs` per D17 — stable sort `Columns` by `(Priority ?? int.MaxValue, DeclarationOrder)`. NO-OP when every column has null priority (preserves baseline). Emit HFC1029 Information at emit stage (via `DiagnosticDescriptors` + `FrontComposerGenerator` switch) when `sorted.Count > 15`.
- [ ] **T6.5** Add HFC1028 emission in AttributeParser per D15 — when two `[ColumnPriority]` attributes share a non-null value within one projection type, emit Information; payload: `{ProjectionType, CollidingPriorityValue, CollidingPropertyNames[]}`. Dedupe: per projection type, fire once.

### T7 — Contracts + actions + FcShellOptions extensions (AC1-AC7; ≈ 2h)

- [ ] **T7.1** Create `src/Hexalith.FrontComposer.Contracts/Attributes/ColumnPriorityAttribute.cs` per D14.
- [ ] **T7.2** Extend `src/Hexalith.FrontComposer.Contracts/FcShellOptions.cs` per D10 with three new properties + `[Range]` annotations.
- [ ] **T7.3** Extend `src/Hexalith.FrontComposer.Shell/Options/FcShellOptionsThresholdValidator.cs` — cross-property check `VirtualizationServerSideThreshold < MaxUnfilteredItems`.
- [ ] **T7.4** Create `tests/Hexalith.FrontComposer.Contracts.Tests/Attributes/ColumnPriorityAttributeTests.cs` (2 tests: usage constraint, priority readable).
- [ ] **T7.5** Create `tests/Hexalith.FrontComposer.Contracts.Tests/Rendering/VirtualizationActionsTests.cs` (3 tests: empty ViewKey throws, LoadPageAction range validation, ScrollCapturedAction validates NaN/Infinity/negative).
- [ ] **T7.6** Create `tests/Hexalith.FrontComposer.Contracts.Tests/FcShellOptionsVirtualizationTests.cs` (3 tests: 3 Range validations + 1 cross-property validator invariant — split into 4 tests if separation is cleaner).

### T8 — Resource keys + localisation (AC1-AC7; ≈ 1.5h)

- [ ] **T8.1** Add 8 keys to `src/Hexalith.FrontComposer.Shell/Resources/FcShellResources.resx` per D19.
- [ ] **T8.2** Add 8 French translations to `src/Hexalith.FrontComposer.Shell/Resources/FcShellResources.fr.resx`. The current keys do not require U+00A0 NBSP before colon (none of them embed French double-punctuation per Story 4-1 D19 convention); the ellipsis in `VirtualizedSkeletonRowAriaLabel` uses U+2026 HORIZONTAL ELLIPSIS for consistency with Fluent UI.
- [ ] **T8.3** Update `FcShellResourcesTests.CanonicalKeysHaveFrenchCounterparts` — expected count 78 → 86 per locale.
- [ ] **T8.4** Add an ICU-adjacency note in Dev Notes: `PrioritizerMoreColumnsButtonLabelTemplate` plural ambiguity ("1 hidden" vs "0 hidden" vs "2 hidden") is TRACKED for Epic 9-5 ICU migration; v1 uses the template verbatim with plural-neutral framing.

### T9 — Diagnostic descriptors + documentation (AC5; ≈ 1h)

- [ ] **T9.1** Add `HFC1028_ColumnPriorityCollision` + `HFC1029_ColumnPrioritizerActivated` descriptors in `src/Hexalith.FrontComposer.SourceTools/Diagnostics/DiagnosticDescriptors.cs` — Information severity, structured payload per D15.
- [ ] **T9.2** Modify `FrontComposerGenerator.cs` generator pipeline — emit HFC1028 at Parse stage, HFC1029 at Emit stage; dedupe via per-generator-run `HashSet<string>` (project-type names).
- [ ] **T9.3** Update `src/Hexalith.FrontComposer.Contracts/Diagnostics/FcDiagnosticIds.cs` — add XML-doc entries for HFC1028 + HFC1029 matching the HFC1022-HFC1027 format.
- [ ] **T9.4** Update `src/Hexalith.FrontComposer.SourceTools/AnalyzerReleases.Unshipped.md` — add rows for HFC1028 + HFC1029 (Information).
- [ ] **T9.5** Create `tests/Hexalith.FrontComposer.SourceTools.Tests/Diagnostics/Hfc1028DiagnosticTests.cs` + `Hfc1029DiagnosticTests.cs` (3 tests each per cheat sheet — 6 total).
- [ ] **T9.6** Add Known Gaps entries in Dev Notes per L10: drag-to-reorder (Epic 6 Level 2); responsive prioritizer layout for phone (Story 10-2 or Epic 9-3); cross-browser scroll restore (Playwright CI / Story 10-2); virtualize performance P99 beyond NFR4's P95 (Story 10-2); 100k-row adopter harness (Epic 5); page-cache memory-leak monitoring (Epic 9-5); AOT/trim analyzer for `TaskCompletionSource<object>` seam (Epic 9-5).

---

## Dev Notes

### Relevant architecture patterns and constraints

- **Zero-override architecture** (`architecture.md:30`): `FluentDataGrid`, `FluentPopover`, `FluentCheckbox`, `FluentButton`, `FluentMessageBar` all consumed verbatim. No custom CSS, no token overrides. The `Virtualize` + `DisplayMode="Table"` pairing follows the Fluent team's own published guidance (`FluentDataGrid` MCP notes) — we inherit their tuning, we don't second-guess.
- **Convention-based rendering** (`architecture.md:493-497`): "IR metadata (field types, role hints, badge mappings)" — 4-4 operationalises the (previously empty) `ColumnModel.Priority` IR field. After 4-4, every generated view has a cap on its visible-column count that scales with projection complexity.
- **Fluxor single-source-of-truth discipline** (Story 1-3 ADR-008): every state change is an immutable `record` action; reducers are pure; effects are the only IO boundary. 4-4's `LoadPageAction` dispatches through `LoadPageEffects` (sole IO gate); the `ColumnVisibilityChangedAction` + `ScrollCapturedAction` reducers chain to Story 2-2's `CaptureGridStateAction` so persistence stays single-pipelined through Story 3-6's existing effect.
- **IR cache stability** (Story 1-4 incremental-build contract): 4-4 adds `PropertyModel.ColumnPriority` + `ColumnModel.Priority` as derived fields; `RazorModel` unchanged in shape but carries the sorted `Columns` list. IR byte-stability preserved — Story 1-4 cacheability tests stay green (the stable sort is deterministic over the same input).
- **Blazor Server + Blazor Auto compatibility** (UX-DR59): `IJSRuntime` for scroll interop works in both modes; `DataGridScrollInterop` is circuit-scoped in Server, app-scoped in WASM (standard Blazor scope rules). The JS module `fc-datagrid.js` ships via the Shell's static web assets (`_content/Hexalith.FrontComposer.Shell/js/`).
- **Persistence fail-closed** (L03 tenant isolation): relies on Story 3-6's existing fail-closed `TryResolveScope` in `DataGridNavigationEffects`. 4-4 does NOT add new scope-check code; it relies on the existing guard. `LoadPageEffects.HandleLoadPage` routes through `IQueryService.QueryAsync` which honours `QueryRequest.TenantId` (populated upstream). Any null/empty tenant short-circuits at the service boundary — the 4-4 effect does not need its own guard because it's not persisting anything that could leak across tenants.
- **NFR4 performance budget**: 500 virtualized rows P95 < 300ms. Fluent UI v5's `Virtualize` implementation renders only visible rows + `OverscanCount="3"` — ~15-18 rows in the render tree at any time. Benchmark harness in T4.9 asserts this directly.

### Source tree components to touch

**Create (new files):** per cheat sheet "Files to create" table — 24 files (3 components, 3 component tests, 1 service, 1 JS module, 1 feature state, 1 feature registration, 1 reducers file, 1 effects file, 5 emitter / diagnostic / integration tests, 3 contracts types (ColumnPriorityAttribute + VirtualizationActions + VirtualizationReservedKeys), 3 contracts + options tests, 1 performance benchmark).

**Extend (existing files):** per cheat sheet "Files to extend" table — 12 files (FcShellOptions, FcShellOptionsThresholdValidator, AttributeParser, PropertyModel, ColumnModel, RazorModelTransform, ProjectionRoleBodyEmitter, RazorEmitter, DiagnosticDescriptors, FrontComposerGenerator, FcDiagnosticIds, AnalyzerReleases.Unshipped, FcShellResources.resx/.fr.resx, ServiceCollectionExtensions, FrontComposerShell.razor, DataGridNavigationReducers).

### Previous story intelligence (L11)

**Story 4-3 (predecessor; ready-for-dev at 4-4 creation):**
- 4-3's reserved-key packing convention (`__status`, `__search`) is the direct template for 4-4's `__hidden`. Same reducer discipline — reserved keys start with `__`, column-key keys don't, collision-by-construction impossible.
- 4-3's 300 ms `TimeProvider`-anchored debounce pattern is the template for 4-4's 150 ms scroll debounce. Same CancellationTokenSource + TimeProvider.Delay + per-component dispose. The 150 ms vs 300 ms difference is Decision-Sensitive (D4).
- 4-3's `FcFilterSummary` owns `{filteredCount} of {totalCount}` display when filters are active; 4-4 renders `{count} {entityPlural}` ONLY when filters are inactive (D9 — mutually exclusive surfaces).
- 4-3 added the `data-fc-datagrid` attribute + `@onscroll` container — 4-4 extends the existing container, does not introduce a second one.
- 4-3's `CounterProjectionApprovalTests` baseline is the regression anchor for "no `__status`/`__search`/`__hidden` adopter annotations means the counter view stays visually stable." 4-4's added Virtualize + ItemSize + DisplayMode + OverscanCount + ItemKey attributes are DELIBERATE diffs — re-approval is required (T5.5).

**Story 4-2 (predecessor; ready-for-dev at 4-4 creation):**
- 4-2's `FcStatusBadge` + `SlotAppearanceTable` are NOT consumed by 4-4 — column prioritization is orthogonal to badge rendering. Explicit mention prevents confusion.
- 4-2's dedupe-via-HashSet pattern for HFC1025 is the template for 4-4's HFC1028/HFC1029 dedupe. Same per-generator-run `HashSet<string>` keyed by projection type name.

**Story 4-1 (predecessor; in review at 4-4 creation):**
- 4-1's `EmitStandardDataGrid` is the single modification site for Virtualize emission (D12). `EmitTimelineBody` + `EmitDetailRecordBody` are untouched (they don't use FluentDataGrid).
- 4-1's `Emit{Strategy}Body` dispatch is untouched — 4-4 doesn't add a strategy; it enriches every Default/ActionQueue/StatusOverview/Dashboard body with virtualization.
- 4-1's three-way Loading/Empty/Data shell is 4-4's render context — virtualization attributes live in the Data branch only; Loading still uses `FcProjectionLoadingSkeleton`; Empty still uses `FcProjectionEmptyPlaceholder` or `FcFilterEmptyState` (4-3).

**Story 3-6 (done):**
- `DataGridNavigationEffects.HandleCaptureGridState` + `HandleRestoreGridState` + `HandleClearGridState` are the IO layer 4-4 triggers via Fluxor action chaining. Zero new navigation-persistence effects; zero changes to existing effect code.
- 3-6's 250 ms debounce vs 4-4's 150 ms scroll debounce — these are SEPARATE debounce layers. 4-4's 150 ms happens first (scroll-to-action), dispatches `ScrollCapturedAction` which chains `CaptureGridStateAction`; 3-6's 250 ms happens in `HandleCaptureGridState` and coalesces rapid capture events. Total envelope up to 400 ms — well within UX-perceptual instant threshold for scroll-position persistence.
- 3-6's fail-closed discipline (L03 — null/empty tenant/user short-circuits via HFC2105 Info) is inherited unchanged. 4-4 does NOT add scope-check code.
- `GridViewPersistenceBlobSchemaLockedTests.BlobSerializesToExpectedJsonShape` is a HARD gate — D7 preserves the schema; T4.8 extends the test with additional round-trip assertions for the new reserved key without shape changes.

**Story 2-2 (done):**
- `CaptureGridStateAction` / `RestoreGridStateAction` / `ClearGridStateAction` reducer contracts are frozen. 4-4 chains into them via column-visibility + scroll-capture reducers (D7/D4).
- `GridViewSnapshot.ScrollTop` field has private-setter validation (non-negative finite). 4-4's `ScrollCapturedAction` re-validates before dispatch — defence in depth.
- ViewKey format `{bc}:{projectionTypeFqn}` (Story 2-2 D30) is preserved byte-for-byte (inherited from Story 4-3).

**Story 1-5 (done):**
- `ColumnModel.TypeCategory` routing unchanged. 4-4 adds `Priority` alongside; emit stage consumes.
- `HumanizeEnumLabel` consumed unchanged (reused by Story 4-2 for chip labels).
- `CounterProjectionApprovalTests` regression baseline: Counter projection has few columns (below 15), no `[ColumnPriority]` → no prioritizer wrap; the Virtualize + ItemSize + DisplayMode + OverscanCount + ItemKey additions are deliberate diffs — re-approval required (T5.5).

**Story 3-3 (done):**
- `DensityLevel` enum (`Compact`/`Comfortable`/`Roomy`) consumed by 4-4 at render time — `DataGridDensityMetrics.ResolveRowHeightPx(Density)` returns `32`/`44`/`56` px respectively. Runtime resolution means density change (via FcSettingsDialog) takes effect on next render without view re-mount.
- `RenderContext.Density` cascading parameter is the consumption point — 4-4 reads `[CascadingParameter] RenderContext RenderContext` in the generated view and passes `RenderContext.Density` into `DataGridDensityMetrics`.

**Story 3-4 (done / in-review):**
- `IShortcutService` — no new shortcuts from 4-4. The `/` binding (4-3) continues to focus the first column filter.

### Architecture compliance

- **`AnalyzerReleases` discipline:** HFC1028 + HFC1029 rows land in `AnalyzerReleases.Unshipped.md` BEFORE the descriptors are referenced in code. Order matters.
- **`FcDiagnosticIds.cs` canonical constants:** HFC1028 + HFC1029 get XML-doc constants following the HFC1022-HFC1027 pattern.
- **AOT / trim discipline (NFR68 — trim warnings block CI):** `ColumnPriorityAttribute` is a plain attribute type; parsing is reflection-free at generator time (Roslyn `ISymbol.GetAttributes()`). The generated view's `_itemKeyAccessor` uses `Lazy<Func<T, object>>` with a closure — trim-safe (no `Type.GetType`, no dynamic invocation). The `TaskCompletionSource<object>` boundary in `LoadedPageState.PendingCompletionsByKey` is a known seam — documented as v1 limitation per D16; Epic 9-5 AOT analyzer may revisit.
- **Nullable value type discipline:** `PropertyModel.ColumnPriority` + `ColumnModel.Priority` are `int?`. Five new actions fail on null/empty `ViewKey` (matches Story 2-2 D30 discipline).
- **IStringLocalizer + ResourceManager dual pattern:** 8 new resource keys accessed via `IStringLocalizer<FcShellResources>` at runtime. No ICU format keys in v1.

### Library/framework requirements

- **Fluent UI Blazor v5** (in dependency closure; Microsoft.FluentUI.AspNetCore.Components). Components used: `FluentDataGrid<T>` with `Virtualize` + `DisplayMode="Table"` + `ItemSize` + `OverscanCount` + `ItemKey` + `ItemsProvider`; `FluentPopover` for prioritizer panel; `FluentCheckbox` for visibility toggles; `FluentButton Appearance="Outline"` for "More columns"; `FluentMessageBar Intent="Info"` for the two banners. All confirmed present in Fluent UI v5 per MCP lookup.
- **`TimeProvider`** abstraction (required for testable debounce); `Microsoft.Extensions.TimeProvider.Testing`'s `FakeTimeProvider` in test projects. Already in `Directory.Packages.props` per Story 3-6 precedent.
- **No new NuGet packages.** Zero external dependencies added.
- **Target-framework compatibility:** `FcColumnPrioritizer` + siblings target `net10.0` (Shell TFM). `RazorEmitter` changes target `netstandard2.0` (SourceTools TFM — Roslyn constraint). `ColumnPriorityAttribute` + `VirtualizationActions` + `VirtualizationReservedKeys` target `net10.0;netstandard2.0` (Contracts TFMs).

### File structure requirements

- `FcColumnPrioritizer` + `FcSlowQueryNotice` + `FcMaxItemsCapNotice` live under `src/Hexalith.FrontComposer.Shell/Components/DataGrid/` (folder established by Story 4-3).
- `DataGridScrollInterop` lives under `Services/` (alongside Story 4-3's `DataGridFocusScope`).
- `LoadedPageFeature` + `LoadedPageReducers` + `LoadPageEffects` + `LoadedPageState` live under `State/DataGridNavigation/` alongside Story 2-2 / 3-6 / 4-3 state artifacts.
- `fc-datagrid.js` lives under `wwwroot/js/` as a peer of `fc-keyboard.js` (Story 4-3 precedent).
- Test folders mirror source structure.

### Testing standards

- **bUnit** for Shell component tests (precedent from Story 2-2 through 4-3).
- **Verify / approval testing** for SourceTools emitter snapshots (`Verify` attribute + snapshot files under `Emitters/snapshots/`).
- **xUnit** + **Shouldly**.
- **CSharpGeneratorDriver** for generator invocation in tests (architecture.md §548-553).
- **FakeTimeProvider** for all debounce assertions — NEVER real clock waits.
- **InMemoryStorageService** for round-trip persistence tests (matches Story 3-6 pattern).
- **NFR4 performance benchmark** (T4.9) runs locally + CI under `[Category("performance")]` — excluded from PR-gate fast suite, included in the nightly / pre-release suite.
- **French locale assertion:** `CanonicalKeysHaveFrenchCounterparts` parity test (8 new keys each locale).
- **Automated validation only** — no `[HUMAN-EXECUTED]` gates in the AC set. Story 3-6 and Story 4-3 established the pattern (Aspire MCP + Claude browser for end-to-end visual verification where needed, but unit + bUnit + snapshot tests cover 4-4's surface end-to-end).

### Project Structure Notes

- **No new projects.** All changes land in existing assemblies (Contracts, SourceTools, Shell).
- **No new packaging changes.** Semantic-release flow handles version bumps.
- **No csproj changes** beyond the AnalyzerReleases.Unshipped.md tracking for HFC1028/HFC1029.

### Known Gaps (L10 discipline — owning story linked)

- **Drag-to-reorder columns in `FcColumnPrioritizer`.** Per D6, v1 presents a checkbox list — column order within the "More columns" popover matches declaration priority. Drag-reorder UI is **Story 6-3 "Level 3 slot-level field replacement"** customization — an adopter who wants drag-reorder writes a custom prioritizer component via Epic 6 override, or waits for Epic 6-3 if the feature reaches ubiquity.
- **Responsive prioritizer layout for phone viewports (< 768px).** Per UX-DR7, phone variant moves "More columns" button above the DataGrid. 4-4 ships the desktop layout only. **Story 10-2 "accessibility CI gates and visual specimen verification"** is the owning story; if it lands before Epic 9-3 responsive polish, the specimen check will drive the fix.
- **Cross-browser scroll-restore behaviour (`.scrollTop` setter nuances in Safari/WebKit).** Unit tests stub `IJSRuntime`; real-browser parity is **Story 10-2 Playwright CI** territory.
- **Virtualize performance P99 / 100k-row adopter harness.** NFR4 gates on P95 at 500 rows. P99 tail latency + very-large-projection behaviour land in **Story 10-2 visual specimen + Epic 5 "reliable real-time experience" adopter harness**.
- **Page-cache memory leak (`LoadedPageState.PagesByKey` grows unbounded across long sessions).** v1 ships an unbounded cache; per-view LRU eviction is **Epic 9-5 "Diataxis documentation site"** + an Epic 9-6 cleanup story (to be created before 4-4 ships, per L10). Observability (Activity spans on page fetches) is Epic 5 work.
- **AOT/trim analyzer for `TaskCompletionSource<object>` boundary.** The Fluxor non-generic state constraint forces the cast at the dictionary boundary. **Epic 9-4 "diagnostic ID system and deprecation policy"** + an analyzer entry (new diagnostic, deferred).
- **ICU-format plural-sensitive `PrioritizerMoreColumnsButtonLabelTemplate`.** "1 hidden" vs "0 hidden" vs "2 hidden" currently rendered as `"{0} hidden"` — plural-neutral framing. Full ICU is **Story 9-5 "Diataxis documentation site"** ICU migration. Documented in Dev Notes T8.4.
- **Pre-fetch / read-ahead for known-next-page.** Per scope guardrail, `GridItemsProvider<T>` fetches current page on scroll. Read-ahead is **Epic 5 "Reliable real-time experience"** territory (SignalR + optimistic fetch).
- **Unfiltered-cap safety rail acknowledgment UI.** `FcMaxItemsCapNotice` says "use filters"; a richer flow (inline filter palette, auto-open status chips) is **Story 4.6 "empty states, field descriptions and unsupported types"** territory — the empty-filtered-state component there is the symmetric UX surface.
- **Boolean 3-state filter interaction with column prioritization.** Story 4-3 deferred Boolean filtering to Epic 6; if/when Boolean filters ship, their interaction with "hidden columns" needs verification — **Story 6-3** owns.
- **`FcNewItemIndicator` (FR21 / 3-5 follow-up) interaction with virtualization.** A new item outside the current visible page range must still be highlighted — the indicator's implementation story needs to know `_itemKeyAccessor` exists. **Story 3-5 follow-up OR Epic 4-7.**

### References

- **PRD**:
  - [Source: _bmad-output/planning-artifacts/prd/functional-requirements.md#FR12] — Business user filter/sort/text-search with session persistence (partial; 4-4 adds the virtualization + column prioritization surface)
  - [Source: _bmad-output/planning-artifacts/prd/non-functional-requirements.md#NFR4] — P95 render time < 300ms on 500 virtualized rows
- **Epics**:
  - [Source: _bmad-output/planning-artifacts/epics/epic-4-rich-datagrid-projection-interaction.md#Story-4.4] — Epic 4 § 146-195 ACs
  - [Source: _bmad-output/planning-artifacts/epics/requirements-inventory.md#UX-DR7] — FcColumnPrioritizer wrapper for >15 fields
  - [Source: _bmad-output/planning-artifacts/epics/requirements-inventory.md#UX-DR42] — Virtual scrolling default + ItemsProvider 500+ + safety rails
  - [Source: _bmad-output/planning-artifacts/epics/requirements-inventory.md#UX-DR63] — Auto-generation scale limit at 15 fields
- **UX**:
  - [Source: _bmad-output/planning-artifacts/ux-design-specification/ux-consistency-patterns.md#Pagination-Virtual-Scrolling] — Virtualize default, ItemsProvider mechanics, safety rails
  - [Source: _bmad-output/planning-artifacts/ux-design-specification/component-strategy.md#FcColumnPrioritizer] — Component anatomy + keyboard + popover structure
  - [Source: _bmad-output/planning-artifacts/ux-design-specification/user-journey-flows.md#Auto-generation-scale-limit] — 15-field trigger + 8-10 visible by priority + "More columns" toggle
- **Architecture**:
  - [Source: _bmad-output/planning-artifacts/architecture.md#Convention-Based-Rendering] — IR metadata (role hints + badge mappings + priority) — 4-4 operationalises Priority
  - [Source: _bmad-output/planning-artifacts/architecture.md#Fluxor-State-Management] — Reducer + effect discipline
- **Predecessor stories**:
  - [Source: _bmad-output/implementation-artifacts/2-2-action-density-rules-and-rendering-modes/critical-decisions-read-first-do-not-revisit.md#D30] — `GridViewSnapshot.ScrollTop` field (producer finally arrives in 4-4)
  - [Source: _bmad-output/implementation-artifacts/3-6-session-persistence-and-context-restoration.md#ADR-050] — `DataGridNavigationEffects` persistence layer
  - [Source: _bmad-output/implementation-artifacts/3-3-display-density-and-user-settings.md] — `DensityLevel` enum + `RenderContext.Density` cascade
  - [Source: _bmad-output/implementation-artifacts/4-1-projection-role-hints-and-view-rendering.md] — `EmitStandardDataGrid` shared emission path
  - [Source: _bmad-output/implementation-artifacts/4-2-status-badge-system.md] — HFC dedupe pattern precedent
  - [Source: _bmad-output/implementation-artifacts/4-3-datagrid-filtering-sorting-and-search.md] — Reserved-key packing convention (D3), `data-fc-datagrid` attribute, TimeProvider-anchored debounce pattern, FcFilterSummary count-display ownership
  - [Source: _bmad-output/implementation-artifacts/1-5-source-generator-transform-and-emit-stages.md] — `ColumnModel` / `RazorModelTransform`; `CounterProjectionApprovalTests` regression anchor
  - [Source: _bmad-output/implementation-artifacts/1-4-source-generator-parse-stage.md] — IR cacheability discipline (Equals/GetHashCode for ColumnModel/PropertyModel extensions)
- **Lessons ledger**:
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L01] — Cross-story contract clarity — applied in cross-story contract table above
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L02] — Fluxor feature scope producer + consumer — `LoadedPageFeature` ships reducers + effect in-story; consumer is the emitter's `LoadPageAsync` method (same story)
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L03] — Fail-closed tenant/user — inherited from Story 3-6 + `IQueryService.QueryAsync` boundary
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L04] — Generated name collision detection — preserved via FQN `viewKey` format (Story 2-2 D30 unchanged)
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L06] — Decision budget (≤25 for feature) — 4-4 at 22/25, 3-decision buffer
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L07] — Test cost-benefit — 53 tests, 8 deferred via matrix scoring
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L09] — ADR rejected-alternatives discipline — every D in the decisions table carries ≥ 2 rejected alternatives
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L10] — Deferrals linked to owning stories — applied in Known Gaps
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L11] — Dev Agent Cheat Sheet — present above; story is just under the "≥ 30 decisions OR ≥ 80 new tests" soft trigger (22 decisions, 53 tests) but cheat sheet retained for readability given the emitter + feature-state + server-side provider surface breadth
- **Fluent UI Blazor v5** (external):
  - `FluentDataGrid<T>` API — confirmed via MCP Fluent UI tooling (`Virtualize` bool, `DisplayMode` enum, `ItemSize` float, `OverscanCount` int, `ItemKey` `Func<T, object>`, `ItemsProvider` `GridItemsProvider<T>?`, `RefreshDataAsync(bool force)` method)
  - **Fluent team guidance**: *"Specifically when using Virtualize, it is highly recommended to use the Table display mode as the Grid mode can exhibit odd scrolling behavior"* — motivates D21
  - `FluentPopover` + `FluentCheckbox` + `FluentButton` + `FluentMessageBar` — confirmed in dependency closure via prior-story usage

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) (story creation — Jerome Piquot, `bmad-create-story` skill, 2026-04-23)

### Debug Log References

### Completion Notes List

### File List
