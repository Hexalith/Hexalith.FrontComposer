---
title: 'Story 9.7: Add story-ID and commit-scope evidence'
type: 'chore'
created: '2026-08-25'
status: 'done'
baseline_commit: 'ceae00a4f9788222ed19153acfc05d68d0bc85d1'
story_id: '9.7'
review_loop_iteration: 10
context:
  - '{project-root}/_bmad-output/project-context.md'
  - '{project-root}/_bmad-output/implementation-artifacts/epic-9-context.md'
  - '{project-root}/_bmad-output/implementation-artifacts/epic-9-retro-2026-08-11.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** Story 9.7's strict evidence gate cannot attribute its own published delivery commit because that self-enforcing commit predates the exact `9.7` subject rule and also contains visible unrelated paths. Rewriting published history is forbidden.

**Approach:** Add one hard-authorized `bootstrap-owned` disposition for the exact Story 9.7 baseline/delivery tuple. It counts only the code-bound File List path inventory as story-owned, keeps every unlisted path visible, and preserves ordinary classification and reconciliation semantics while report labels distinguish ownership-contributing listed paths (`owned`) from listed paths in non-owning classifications (`listed-unowned`).

## Boundaries & Constraints

**Always:** Authorize only story `9.7`, baseline `ceae00a4f9788222ed19153acfc05d68d0bc85d1`, and commit `fd04bdd97fbdd4976a0f213e46a316be199fd8a9`. Require the commit to be a non-merge whose sole parent is that baseline, not match `9.7`, and touch both listed guard paths `eng/validate-story-artifacts.py` and `eng/tests/test_validate_story_artifacts.py`. Accept at most one full-SHA `bootstrap-owned` declaration with a non-empty reason. Reconcile only the exact code-bound listed path inventory; report all unlisted paths. Permit multiple exactly declared, in-range full-SHA `shared` or `process` rows with non-empty reasons: they may classify an otherwise `unmapped` commit but must never suppress `interleaved`. Classify `2dcc43fea9aa39c42d15b1028fa5ef774b5d8b06` and `c4df029050cb241f74cafd04a01f7718eae1ec0c` as `shared`.

**Ask First:** Any other bootstrap tuple, reusable authorization source, disposition kind, ownership rule, or commit/status semantic.

**Never:** Rewrite history, auto-detect bootstrap commits, accept a wildcard or movable ref, let story text authorize arbitrary bootstrap ownership, suppress unlisted paths, treat the whole bootstrap commit as story-owned, weaken ordinary unmapped/interleaved failures, or use path-level unrelated declarations as commit exceptions.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|---------------|---------------------------|----------------|
| Authorized bootstrap | Exact story/baseline/SHA/parent and both listed guard paths | Listed paths reconcile as `bootstrap-owned`; unlisted paths remain reported | Pass |
| Wrong authorization | Any story, baseline, SHA, parent, merge shape, or matching subject differs | No ownership is granted | Fail closed |
| Invalid declaration | Multiple bootstrap rows, stale/short SHA, empty reason, or missing guard path | No ownership is granted | Fail closed |
| Declared shared commits | Exact in-range full-SHA declarations, including `2dcc43fe...` and `c4df0290...`, with `shared` and reasons | Commits remain visible but contribute no ownership; an interleaved story commit still fails | Pass |
| Excluded committed path | A default or `--exclude` pattern covers a path a story commit touched | The path is printed under its own `excluded` label, carries no ownership, and cannot make the commit `interleaved` | Pass |
| Excluded listed path | The same pattern also covers a File List entry | The entry has no matching story-owned change; the failure names both resolving edits | Fail closed |
| Contradicted identity | A well-formed `title`, H1, or filename identity disagrees with `story_id` | The artifact cannot be reconciled by any mode | Fail closed, in strict and legacy mode alike |
| Malformed document structure | Unterminated fence, non-column-zero frontmatter delimiter, or a dispositions heading at any level but 2 | Structure that would silently change what the gate sees is named | Fail closed |
| Authoring example | A File List row, declaration, or checked task appears only inside a fence or an indented example, at any nesting depth | It is example content and never becomes document structure | Pass |
| Declaration on a merge | A disposition names a merge SHA | Merges are listed separately and the declaration grants nothing | Pass, rendered without effect |
| Inert declaration | A `shared`/`process` row names a commit whose subject already matches the story | Interleaved precedence stands; the report marks the declaration ignored | Pass |
| Workspace mutation | The tree changes while the gate runs | The run cannot certify a stable snapshot | Fail closed |

</frozen-after-approval>

## Code Map

- `eng/validate-story-artifacts.py:236-260,328-470,583-1085,1194-1450,1708-1950,2045-2140` -- the immutable bootstrap inventory, fail-closed CLI and base overrides, one shared frontmatter/body split feeding source-numbered scanning outside frontmatter/fences/indented examples, exact and normalized story identity, prose-tolerant disposition grammar at its exact heading level, one substitutable git process boundary, canonical range evidence with hard-bound authorization, matching-first classification, stable workspace snapshots, exclusion-bounded listed-path reconciliation, classification-aware labels, one delimiter/control-safe report boundary, and nested-aware task extraction.
- `eng/tests/test_validate_story_artifacts.py:111-1492,1493-2915,2916-3414,3415-3468,3469-3694,3695-4195,4196-4375,4376-4542` -- workflow/documentation contracts, fallback and frontmatter guards, real-heading and exact-source-line metadata extraction, bounded task parsing and fence closure, disposition/matching classification, every report-value surface, candidate/workspace mutation detection, strict identity/exclusion/non-owning-extra regressions, bootstrap inventories/history and canonical artifacts, and the document-parsing boundaries that decide what text becomes structure.
- `tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs:141-169` and `_bmad-output/contracts/analyzer-policy-exception-ledger-v1.json:98-103` -- isolated blocking CI-pin fact and exact CA1707 identifier-inventory reseal.
- `.agents/skills/bmad-build/step-04-review.md:19-52` and `.claude/skills/bmad-build/step-04-review.md:19-52` -- synchronized reviewer-facing strict gate and one-time exception contract.
- `.agents/skills/bmad-build/spec-template.md:6,74-79` and `docs/reference/story-artifact-validation.md:13-171` -- fail-closed `story_id` authoring, reusable disposition scaffold, and the operator-facing identity, document-structure, story-match, classification, path-label, and disposition grammar.
- `_bmad-output/implementation-artifacts/story-review-reconciliation-checklist.md:22-30` -- operator contract for commit dispositions and anti-bypass behavior.

## Tasks & Acceptance

**Execution:**
- [x] `eng/validate-story-artifacts.py`, `eng/tests/test_validate_story_artifacts.py`, workflow files, CI pin, and operator checklist -- preserve the delivered exact story-ID, canonical-ref, ancestry, per-commit path, merge, workspace, and File List evidence behavior.
- [x] `eng/validate-story-artifacts.py` -- add the hard-bound `bootstrap-owned` authorization, bind it to the canonical Story 9.7 artifact and immutable bootstrap-owned path set, reconcile only those authorized listed paths, and label listed paths as owned only for ownership-contributing classifications.
- [x] `eng/tests/test_validate_story_artifacts.py` -- prove the exact authorization succeeds and every artifact, tuple, topology, declaration, guard-path, and mutable-File-List deviation fails closed; prove shared/process/unmapped paths never receive an owned label and unlisted paths stay visible.
- [x] `.agents/skills/bmad-build/step-04-review.md` and `_bmad-output/implementation-artifacts/story-review-reconciliation-checklist.md` -- document the one-time human-authorized recovery and prohibit routine substitution for correct commit attribution.
- [x] `_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md` -- record all exact dispositions and refreshed verification evidence.

**Acceptance Criteria:**
- Given the exact authorized historical tuple and canonical Story 9.7 artifact, when strict validation runs to `HEAD`, then `fd04bdd9...` is reported as `story-id=no-match | disposition=bootstrap-owned`, only its listed paths are labeled `owned`, its unlisted paths remain visible as `unowned`, and `2dcc43fe...` is visible as `shared` with listed paths labeled `listed-unowned` rather than contributing ownership.
- Given any authorization or structural deviation, when validation runs, then it fails with actionable evidence and grants no bootstrap ownership.
- Given ordinary matching, unmapped, interleaved, `shared`, or `process` commits, when validation runs, then their existing classification and reconciliation semantics remain unchanged, and report labels remain classification-aware. The vocabulary is four-valued: a listed path is `owned` only for ownership-contributing classifications and `listed-unowned` otherwise; a path an exclusion pattern covers is `excluded` regardless of classification and carries no ownership; every other path is `unowned`.

### Review Findings

- [x] [Review][Patch] Extract the Story 9.7 CI-pin assertions into their own `[Fact]` and reseal the analyzer-policy identifier inventory [tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs:141]
- [x] [Review][Patch] Renegotiate the frozen Approach and AC 3 to admit classification-aware report labels, and record it in the Spec Change Log [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:20]
- [x] [Review][Defer] Constrain `shared`/`process` disposition authorization the way review loop 3 constrained `bootstrap-owned` [eng/validate-story-artifacts.py:631] — deferred, pre-existing; they only withhold ownership and never grant it, so mutable story text cannot broaden attribution — tightening is a follow-up story
- [x] [Review][Patch] A documented-unrelated entry written with a trailing slash crashes the validator [eng/validate-story-artifacts.py:731]
- [x] [Review][Patch] The runtime `.claude/skills/bmad-build` mirror was not updated, so the strict gate is inert [.claude/skills/bmad-build/step-04-review.md:20]
- [x] [Review][Patch] The new blocking CI gate is non-hermetic and fails on any dirty working tree [eng/tests/test_validate_story_artifacts.py:2028]
- [x] [Review][Patch] The story-ID matcher's left guard is asymmetric, so version strings match the story [eng/validate-story-artifacts.py:866]
- [x] [Review][Patch] A deferred-work entry added by this story is contradicted by the same commit [_bmad-output/implementation-artifacts/deferred-work.md:2579]
- [x] [Review][Patch] Two paths inside the bootstrap commit are declared nowhere in the spec [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:138]
- [x] [Review][Patch] Four load-bearing invariants are unpinned — deleting each keeps the suite green [eng/tests/test_validate_story_artifacts.py:1826]
- [x] [Review][Patch] History-coupled bootstrap tests have no availability guard [eng/tests/test_validate_story_artifacts.py:1832]
- [x] [Review][Patch] Verification and Test Evidence omit the Governance C# lane and record a stale candidate [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:160]
- [x] [Review][Patch] The `story_id` template placeholder is an undocumented fail-closed trap [.agents/skills/bmad-build/spec-template.md:6]
- [x] [Review][Patch] A second recognized task heading is silently skipped, and the dispositions parser rejects prose [eng/validate-story-artifacts.py:1467]
- [x] [Review][Patch] `last_updated` drifted from the format the sprint tooling emits [_bmad-output/implementation-artifacts/sprint-status.yaml:44]
- [x] [Review][Patch] Code Map line anchors no longer resolve to the symbols they name [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:42]
- [x] [Review][Defer] Merge commits contribute no path evidence [eng/validate-story-artifacts.py:1049] — deferred, pre-existing
- [x] [Review][Defer] `--exclude` is applied to workspace paths but not committed paths [eng/validate-story-artifacts.py:1250] — deferred, pre-existing
- [x] [Review][Defer] An explicit `story_id` is never cross-checked against title, H1, or filename [eng/validate-story-artifacts.py:562] — deferred, pre-existing
- [x] [Review][Defer] Non-UTF-8 commit subjects raise a traceback instead of a validation failure [eng/validate-story-artifacts.py:1004] — deferred, pre-existing
- [x] [Review][Defer] The baseline ref is not re-resolved after collection although the candidate is [eng/validate-story-artifacts.py:1171] — deferred, pre-existing
- [x] [Review][Defer] The bootstrap exception has no retirement path and pins the suite to this repository's history [eng/validate-story-artifacts.py:219] — deferred, pre-existing
- [x] [Review][Defer] Gate 2a pack baseline properties are not mirrored on the Gate 1 restore [.github/workflows/quality.yml:71] — deferred, pre-existing
- [x] [Review][Defer] `step-03-implement.md` never tells the implementer to put the story ID in commit subjects [.agents/skills/bmad-build/step-03-implement.md:1] — deferred, pre-existing
- [x] [Review][Patch] Legacy best-effort discovery passes `NO_VCS` or an invalid baseline to `git diff` and silently omits unstaged tracked changes [eng/validate-story-artifacts.py:762]
- [x] [Review][Patch] Duplicate `story_id` or `baseline_commit` frontmatter scalars silently replace the earlier security-critical value [eng/validate-story-artifacts.py:667]
- [x] [Review][Patch] An explicitly empty `--base` override silently falls back to story frontmatter [eng/validate-story-artifacts.py:319]

- [x] [Review][Patch] RESOLVED 2026-08-25 (loop 5): add `references/Hexalith.EventStore` and `references/Hexalith.Tenants` to the File List so `5817f191` classifies `owned` instead of `interleaved`, and delete the now-false `## Documented Unrelated Workspace State` entry for `references/Hexalith.EventStore` (adding it to the File List claims it). Record the reversal in the loop-5 Spec Change Log entry, including that this absorbs a pre-existing workspace change, which the Epic 9 constraint discourages -- accepted by human decision. Original finding: Story 9.7's own strict gate exits 1 at HEAD because `5817f191` commits declared-unrelated submodule pointers under a `fix(9.7):` subject — `references/Hexalith.EventStore` is declared in `## Documented Unrelated Workspace State` as "Story 9.7 neither modifies nor claims it", `references/Hexalith.Tenants` is declared nowhere, and the authored rule in `step-05-present.md` forbids staging a Documented-Unrelated path. The in-range merge of PR #97 means the history is published, so the frozen Never blocks amending. Human call needed between: add both paths to the File List (contradicting the unrelated declaration), declare a disposition on `5817f191` (which relies on the bypass below), re-land the submodule bump under a non-9.7 subject, or renegotiate the frozen block. [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:174]
- [x] [Review][Patch] RESOLVED 2026-08-25 (loop 5): close the bypass with an asymmetry rather than the "touches no File List path" rule, which would invalidate the story's own `2dcc43fe` row (it touches three listed paths). A `shared`/`process` disposition MAY suppress `unmapped` (subject does not claim this story) but MUST NOT suppress `interleaved` (subject claims this story but touches unowned paths); the `interleaved` check at `eng/validate-story-artifacts.py:1130` now runs before matching ownership at `:1137` and non-matching disposition handling at `:1140`, and regressions prove a `fix(9.7)` commit touching an unlisted path still fails while a matching listed-only commit stays owned. Both existing shared rows stay valid because neither subject matches `9.7`. Original finding: a mutable `shared`/`process` disposition suppressed an `interleaved`/`unmapped` hard failure, which the frozen Never forbids ("weaken ordinary unmapped/interleaved failures"). Reproduced before the patch: a `fix(9.7)` commit touching an unlisted path reported `interleaved` + exit 1, then reported `disposition=shared` with the path merely labelled `unowned` and no failure once one row was added to story text. The recorded 2026-08-25 deferral rationale ("they only withhold ownership and never grant it") covered ownership broadening but not hard-failure suppression, so the behavior was re-decided. [eng/validate-story-artifacts.py:1732]
- [x] [Review][Patch] RESOLVED 2026-08-25 (loop 5): keep `c4df0290...` and renegotiate the frozen block -- amend the frozen Always and the I/O matrix to admit multiple exactly-declared, reasoned `shared` rows rather than the single enumerated commit, correct the Execution task's "both exact dispositions" wording, and add the loop-5 Spec Change Log entry that loops 1-4 each received and this change did not. Original finding: a third `shared` disposition (`c4df0290...`) exceeds the frozen Always, which authorizes classifying exactly `2dcc43fe...`, and the I/O matrix's single "Later shared commit" row and the Execution task's "record both exact dispositions"; no Spec Change Log entry records the renegotiation — it appears only in Test Evidence prose. [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:107]
- [x] [Review][Patch] The historical CLI test pins immutable range `ceae00a4..2dcc43fe` while reading the mutable live story artifact, so the blocking Gate 2b lane is red at HEAD (`Ran 92 tests ... FAILED (failures=1)`); bisected green at `a229be7e`, red at `d4385378`, which added the `c4df0290` disposition and two File List entries absent from that range [eng/tests/test_validate_story_artifacts.py:2028]
- [x] [Review][Patch] Three of the four changed `.agents/skills/bmad-build` files were never mirrored into the runtime `.claude` copy, so the `story_id` template field, the "persist canonical story_id" planning rule, and the entire step-05 completion gate and staging discipline are inert — `.claude/skills/bmad-build/step-05-present.md` contains zero occurrences of `validate-story-artifacts`; this is the same defect class already patched once for `step-04-review.md` [.claude/skills/bmad-build/step-05-present.md:51]
- [x] [Review][Patch] The ownership-contributing classification filter is unpinned — deleting the `if commit.classification in OWNERSHIP_CONTRIBUTING_CLASSIFICATIONS` line leaves the full 92-test suite with only the one pre-existing failure, so the loop-2/loop-3 anti-broadening invariant can be silently lost [eng/validate-story-artifacts.py:1893]
- [x] [Review][Patch] `extract_frontmatter` does not strip trailing YAML comments from `story_id`, so a correctly-substituted value that keeps the template's own hint comment fails with `invalid explicit story_id: expected exactly two numeric segments`, and the message never mentions the comment [eng/validate-story-artifacts.py:1067]
- [x] [Review][Patch] Verification and Test Evidence record results that do not reproduce at HEAD — the "92 tests / 2 optional skips" claim has one failure, and the strict gate cited as passing against `a229be7e` exits 1 at the delivered HEAD; no evidence exists for `d4385378` or `5817f191` [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:172]
- [x] [Review][Patch] Duplicate non-critical frontmatter scalars still silently overwrite, and `title` feeds legacy story-ID resolution yet is absent from `CRITICAL_FRONTMATTER_SCALAR_KEYS` [eng/validate-story-artifacts.py:51]
- [x] [Review][Patch] `extract_checked_tasks` closes a task section only on `## `, so every `### Review Findings` checked item is extracted as a task requiring evidence — currently benign only because each bullet happens to carry a bracketed path [eng/validate-story-artifacts.py:1467]
- [x] [Review][Patch] `CHECKED_TASK_HEADINGS` is a closed two-spelling set with no fail-closed path when a story contains `- [x]` lines but no recognized heading matched, so checked-task evidence validation is silently skipped [eng/validate-story-artifacts.py:53]
- [x] [Review][Patch] Strict mode reports a raw `git failure while resolving baseline: ref is empty` for a missing or empty `baseline_commit`, and has no code guard for `NO_VCS` (legacy mode has one; strict mode relies on prose only) [eng/validate-story-artifacts.py:961]
- [x] [Review][Patch] `test_malformed_stale_or_empty_dispositions_fail_closed` accepts `"malformed" in stderr or "stale" in stderr` for all six sub-cases, so an empty-reason regression still passes because the fixture SHAs are also stale [eng/tests/test_validate_story_artifacts.py:1239]
- [x] [Review][Patch] Four deferred-work entries added by this story are filed under the Story 9.6 heading rather than the new Story 9.7 heading, and the merge-resolution entry is duplicated verbatim [_bmad-output/implementation-artifacts/deferred-work.md:2588]
- [x] [Review][Patch] The `last_updated` finding was resolved backwards -- fixed during this review: restored to the emitter's `%m-%d-%Y %H:%M` format — the prior `08-18-2026 09:45` matched the emitter's `DATE_FORMAT = "%m-%d-%Y %H:%M"`, and the patch changed it to an ISO value that only matches the tolerated fallback [_bmad-output/implementation-artifacts/sprint-status.yaml:44]
- [x] [Review][Patch] Review Findings line anchors are stale although the anchor-drift finding is marked patched — only Code Map and Suggested Review Order were refreshed; nine cited anchors resolve elsewhere (`:611`->621, `:790`->838, `:911`->780, `:953`->1003, `:1078`->1126, `:1149`->1061, `:1360`->1399, `:546`->552, `:312`->324) [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:376]
- [x] [Review][Patch] Untested seams and test hygiene: `--candidate requires --story` survives deletion, the bootstrap-on-merge branch is never exercised end-to-end, `BOOTSTRAP_HISTORY_AVAILABLE` has no CI assertion that history is present, two tests bundle three scenarios without `subTest`, and `setUp` re-executes the validator module for each of 14 tests [eng/tests/test_validate_story_artifacts.py:44]
- [x] [Review][Patch] The new report vocabulary (`owned`/`listed-unowned`/`unowned`/`interleaved`/`unmapped`/`shared`/`process`/`bootstrap-owned`) and the `## Commit Scope Dispositions` grammar are defined only inside this one story artifact — no `docs/` entry and no `spec-template.md` scaffold [.agents/skills/bmad-build/spec-template.md:6]
- [x] [Review][Defer] `4.1.1` is hard-coded in three unlinked places (`quality.yml`, `eng/release_compatibility.py`, three `CiGovernanceTests.cs` regexes) with no single source of truth [.github/workflows/quality.yml:82] -- deferred, owned by the release-compatibility spec declared `shared`
- [x] [Review][Defer] Strict mode spawns one `git diff-tree` per non-merge commit and prints an unbounded report on every run including failures, with no `--quiet` or `--limit` [eng/validate-story-artifacts.py:1079] -- deferred, pre-existing
- [x] [Review][Patch] Closing task extraction on every nested heading lets checked execution work under ordinary `### Implementation` subsections evade evidence validation; retain nested task collection through the next same-or-higher-level section while excluding `### Review Findings` and its descendants [eng/validate-story-artifacts.py:1467]
- [x] [Review][Patch] A `shared`/`process` declaration can downgrade a story-matching, listed-only commit from `owned`; preserve interleaved precedence and apply non-owning dispositions only when the subject does not match the story [eng/validate-story-artifacts.py:1732]
- [x] [Review][Patch] Raw terminal controls in commit subjects, disposition reasons, and Git paths can spoof report lines or terminal state; quote only control-bearing values through one deterministic rendering boundary while leaving ordinary output unchanged [eng/validate-story-artifacts.py:1260]

- [x] [Review][Patch] Author-controlled documented-unrelated reasons and `check_file_list` paths bypass the escaping boundary this story introduced [eng/validate-story-artifacts.py:1322]
- [x] [Review][Patch] `contains_terminal_control` misses Unicode bidi and zero-width overrides, and `format_git_path` renders them raw through its `ensure_ascii=False` branch [eng/validate-story-artifacts.py:1359]
- [x] [Review][Patch] No test covers the C1 half (`0x7F`-`0x9F`) of the escape predicate; all three escaping tests use only `\x1b`, so narrowing the predicate leaves the suite green [eng/tests/test_validate_story_artifacts.py:1386]
- [x] [Review][Patch] The `usable_classified_paths` bounding handed to `check_file_list` has no test; reverting it to the raw map leaves all 108 tests green while one bare top-level bullet re-opens a blanket File List exemption [eng/validate-story-artifacts.py:370]
- [x] [Review][Patch] The candidate-ref TOCTOU re-resolution has no test at all; `candidate ref moved during validation` appears nowhere in the suite and a constant-false comparison stays green [eng/validate-story-artifacts.py:1171]
- [x] [Review][Patch] Exclude filtering in candidate mode is untested; no fixture passes `--exclude` or creates a `DEFAULT_EXCLUDE_PATTERNS` path, so dropping the filter stays green while story-automator scratch output spuriously reddens the gate [eng/validate-story-artifacts.py:1244]
- [x] [Review][Patch] `parse_frontmatter_scalar` treats an apostrophe anywhere in a value as an opening quote, so trailing comments survive: `parse_frontmatter_scalar("Story 9.7 don't panic # real comment")` returns the comment intact [eng/validate-story-artifacts.py:691]
- [x] [Review][Patch] `extract_story_id` scans YAML frontmatter and fenced code blocks for the legacy H1, so a `#`-prefixed frontmatter comment or a fenced example yields a false `conflicting legacy story identities` hard failure [eng/validate-story-artifacts.py:562]
- [x] [Review][Patch] Task-heading recognition is prefix- and level-bound: `CHECKED_TASK_HEADINGS` stores the literal `## `, so `### Tasks / Subtasks` is unrecognized and bare `## Tasks` hard-fails in 3 repository artifacts (`rel-1`, `rel-3.history`, `rel-5`); match the heading text at any level, add bare `tasks`, and drop the unreachable `task_heading_level = ... if heading else 2` fallback [eng/validate-story-artifacts.py:1481]
- [x] [Review][Patch] The degraded-baseline fallback reports only on failure paths; a passing run that silently fell back to a bare workspace diff gives the reader no indication the declared baseline was never used [eng/validate-story-artifacts.py:784]
- [x] [Review][Patch] Malformed `## Commit Scope Dispositions` declarations pass silently in legacy mode; `commit_scope_disposition_failures` is only assembled inside `collect_commit_scope_evidence` and is absent from `metadata_failures` [eng/validate-story-artifacts.py:212]
- [x] [Review][Patch] Four guards added by this change are unreachable by the suite and survive deletion: the trailing-NUL check, the 40-hex SHA assertion, the malformed-status-row check, and `invalid legacy story identity in filename` [eng/validate-story-artifacts.py:1103]
- [x] [Review][Patch] Minor consistency batch: `--candidate` is not `.strip()`ped although `--base` is; disposition parse errors report a section-relative line number that cannot be jumped to; `parse_cli_unrelated` does not `rstrip("/")` although `extract_classified_paths` now does; `BOOTSTRAP_OWNED_GUARD_PATHS` duplicates two `BOOTSTRAP_OWNED_PATHS` members instead of deriving from it; the merge branch extends bootstrap authorization failures unguarded, emitting a misleading "must touch both guard paths" message; and the report prints `disposition={commit.classification}` while `MergeEvidence` uses that key for an actual disposition [eng/validate-story-artifacts.py:1276]
- [x] [Review][Patch] The `### Review Findings` exclusion is an exact-string match, so a suffixed heading such as `### Review Findings -- second pass (2026-08-12)` is not excluded and its reviewer bookkeeping is collected as execution tasks demanding path evidence; 2 repository artifacts hit this today (`11-15-storage-scope-and-snapshot-publisher-consolidation.md`, `spec-9-3-define-explicit-command-target-identity.md`). Match the exclusion by prefix, and emit any remaining checked item outside a recognized task section as a visible notice rather than a silent drop [eng/validate-story-artifacts.py:1500]
- [x] [Review][Patch] Resolved from D1 -- record in Design Notes that the strict gate is workflow-enforced via `step-04-review.md`, and that `quality.yml` Gate 2b guarantees only that the validator itself is not broken, so no reader mistakes the pinned test command for the gate [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:138]
- [x] [Review][Patch] Resolved from D2 -- document in the story-artifact reference that a bare canonical story ID anywhere in a commit subject makes that commit story-matching, so an auto-generated `Revert "fix(9.7): ..."` or a `see 9.7 for context` subject is classified `interleaved` and cannot be dispositioned away [docs/reference/story-artifact-validation.md:13]
- [x] [Review][Patch] Resolved from D4 -- an unmerged path is reported three times because `unmerged_states` adds it to `unresolved` while `state[0]` and `state[1]` are both `U`; report a `UU` path only under `unresolved` [eng/validate-story-artifacts.py:1214]
- [x] [Review][Patch] Metadata section extraction accepts headings inside YAML frontmatter and fenced examples, allowing fake File List entries or commit dispositions to enter strict evidence; use only real Markdown headings outside both regions while preserving exact source lines [eng/validate-story-artifacts.py:656]
- [x] [Review][Patch] Checked-task extraction accepts task headings/items inside YAML frontmatter and fenced examples, and fence-like content with a suffix closes an active fence; share the real-Markdown scan while preserving any-level headings, nested execution subsections, and Review Findings exclusions [eng/validate-story-artifacts.py:1625]
- [x] [Review][Patch] Commit subjects, disposition/documented-unrelated reasons, and Git paths containing the report delimiter `|` or Unicode U+2028/U+2029 separators are emitted raw; route every such value surface through deterministic JSON quoting without changing ordinary output [eng/validate-story-artifacts.py:1493]
- [x] [Review][Patch] Candidate re-resolution does not detect staged, unstaged, untracked, or unresolved workspace mutation after the first snapshot; compare a final snapshot at the end of strict evidence collection and fail visibly on any state change [eng/validate-story-artifacts.py:1291]
- [x] [Review][Defer] Resolved from D1 -- design story-branch CI enforcement so the mechanical commit-scope report gates story completion by machinery rather than by workflow convention [.github/workflows/quality.yml:126] — deferred, needs a branch-to-spec resolution convention that does not exist yet
- [x] [Review][Defer] Resolved from D2 -- consider a `misattributed` disposition kind for subject-only false matches [eng/validate-story-artifacts.py:1732] — deferred, reopening the frozen Boundaries is disproportionate to a trap that has not yet fired
- [x] [Review][Defer] Resolved from D4 -- decide whether an unresolved merge-conflict path should be a hard validation failure rather than merely reported [eng/validate-story-artifacts.py:1214] — deferred, a policy change that deserves its own decision rather than a review patch
- [x] [Review][Defer] The deferred-work retirement entry still describes a "twelve-entry `_PATHS` frozenset" although loop 5 expanded it to thirteen [_bmad-output/implementation-artifacts/deferred-work.md:2616] — deferred, pre-existing
- [x] [Review][Defer] The report has no machine-readable emission, so the calling skill must parse classifications out of English prose; `json` is imported solely for escaping [eng/validate-story-artifacts.py:1275] — deferred, pre-existing
- [x] [Review][Patch] Resolved from D1 -- directory-prefix `Documented Unrelated Changes` coverage exempted committed story code in legacy mode (regression vs `ceae00a4`); parent coverage is now bounded to workspace state via `path_is_classified_covered`/`workspace_classified_scope`, committed paths require an exact classification entry, and `test_classified_directory_cannot_cover_committed_paths` pins both halves [eng/validate-story-artifacts.py:1552]
- [x] [Review][Patch] Resolved from D2 -- the canonical live-CLI report asserted exit 0 for `--candidate HEAD` with no expiry inside the blocking quality lane, so the first later commit touching 9.7's shared bookkeeping paths would have turned `main` red; pinned to `STORY_9_7_DELIVERY_COMMIT` and added to `BOOTSTRAP_HISTORY_REFS` [eng/tests/test_validate_story_artifacts.py:2668]
- [x] [Review][Defer] Resolved from D3 -- merge commits are recorded but never path-reconciled, so conflict-resolution content escapes File List evidence [eng/validate-story-artifacts.py:1158] — deferred, new scope at loop 8 and nothing is currently mis-attributed (both merges in 9.7's range carry no combined-diff content); the follow-up must use `git diff-tree --cc`, not `-m`, which would flood classification with whole-branch paths
- [x] [Review][Patch] A 4-space-indented example is parsed as document structure, injecting fake `## File List` entries, headings, and checked tasks past the fence guard [eng/validate-story-artifacts.py:831]
- [x] [Review][Patch] An empty `## Tasks` heading anywhere demotes every real checked task from a hard failure to a stdout notice [eng/validate-story-artifacts.py:2249]
- [x] [Review][Patch] Checked items in `1.`/`*` lists yield no task, no notice, and no failure [eng/validate-story-artifacts.py:58]
- [x] [Review][Patch] Indented/nested frontmatter keys are promoted to top level, so a nested `story_id` becomes the document identity [eng/validate-story-artifacts.py:915] -- also clears two false `duplicate frontmatter key` failures on existing planning artifacts
- [x] [Review][Patch] An explicit `story_id` is never cross-checked against filename/H1, so a stale value silently misclassifies the whole range [eng/validate-story-artifacts.py:704] -- only a well-formed contradiction fails; the explicit field stays authoritative over malformed legacy spellings
- [x] [Review][Patch] An empty or whitespace-only explicit `story_id` silently falls back to inference instead of failing closed [eng/validate-story-artifacts.py:686]
- [x] [Review][Patch] A leading-zero `story_id` (`09.7`) is left unnormalized and matches no commit subject [eng/validate-story-artifacts.py:624] -- the identity now normalizes and the matcher accepts either spelling, replacing the previous zero-preserving assertion
- [x] [Review][Patch] `--exclude` and default exclusions apply to workspace paths but not commit-derived paths in candidate mode [eng/validate-story-artifacts.py:1661] -- an excluded path stays visible in the report and is evidence in neither half
- [x] [Review][Patch] A File List entry with a trailing slash never matches its changed path, producing simultaneous false missing and false extra failures [eng/validate-story-artifacts.py:1931]
- [x] [Review][Patch] Two report surfaces added by this change still emit author-controlled text raw, contradicting the completed "every value surface" patch [eng/validate-story-artifacts.py:878] -- the malformed-declaration echo, the stray-item notice, and both checked-task failures now quote through the same boundary
- [x] [Review][Patch] Disposition rows written with `*`/`+` or without a bullet are silently dropped rather than reported malformed [eng/validate-story-artifacts.py:874]
- [x] [Review][Patch] A thematic break or prose bullet inside the dispositions section raises a false malformed-declaration failure [eng/validate-story-artifacts.py:874] -- carrying the `|` delimiter or a full SHA is what makes a line a declaration attempt, so no separate thematic-break rule was added: one was written first and removed because deleting it left the suite green
- [x] [Review][Patch] Unterminated frontmatter silently blanks the whole document and misattributes the cause to an empty File List [eng/validate-story-artifacts.py:904]
- [x] [Review][Patch] `extract_frontmatter` and the line scanner disagree on frontmatter boundaries (`---` in a scalar, BOM, leading blank line) with no failure [eng/validate-story-artifacts.py:753] -- both now read one shared split
- [x] [Review][Patch] Double-quoted frontmatter scalar escapes are never unescaped, unlike single-quoted ones [eng/validate-story-artifacts.py:991]
- [x] [Review][Patch] An unresolvable `--base` fails open to a workspace-only bare diff instead of failing closed [eng/validate-story-artifacts.py:377] -- an unusable baseline from story frontmatter keeps its reported bare-diff fallback
- [x] [Review][Patch] A `shared`/`process` commit's listed paths are reported as unexplained File List extras, telling the author both to list and not to list them [eng/validate-story-artifacts.py:1880] -- the declaration explains the path; it still contributes no ownership and stays labelled `listed-unowned`
- [x] [Review][Patch] A non-UTF-8 commit subject raises an uncaught `UnicodeDecodeError` traceback instead of a validation failure [eng/validate-story-artifacts.py:1389]
- [x] [Review][Patch] A nested or suffixed recognized task heading closes the outer Tasks section early and demotes its checked tasks to notices [eng/validate-story-artifacts.py:1970]
- [x] [Review][Patch] `format_git_path` escapes the same path two different ways depending on which branch handles it [eng/validate-story-artifacts.py:1761]
- [x] [Review][Patch] `Commit Scope Dispositions` is honored at any heading level, widening the grammar the reference doc and template pin [eng/validate-story-artifacts.py:591] -- a wrong level is reported rather than silently ignored, so real declarations cannot disappear
- [x] [Review][Patch] Tests monkeypatch the stdlib `subprocess` module process-wide, breaking under any parallel runner [eng/tests/test_validate_story_artifacts.py:2171] -- every git invocation routes through one module attribute, and a regression test pins the indirection
- [x] [Review][Patch] Missing tests: a committed excluded path in range, a malformed frontmatter `title` identity, and strict mode with no derivable identity [eng/tests/test_validate_story_artifacts.py:2557]
- [x] [Review][Patch] Story artifact hygiene: duplicate `## Suggested Review Order` sections, stale Review Findings line anchors, Test Evidence citing `23b0b556` rather than delivered `743618d5`, and frontmatter `status: done` against sprint `review` [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:5]
- [x] [Review][Defer] Section parsing walks the document four times and pads section bodies, inflating a two-heading section to 607 lines [eng/validate-story-artifacts.py:809] — deferred, pre-existing
- [x] [Review][Defer] `markdown_h1_headings` builds every H1 for a caller that breaks after the first [eng/validate-story-artifacts.py:646] — deferred, pre-existing
- [x] [Review][Defer] `BOOTSTRAP_OWNED_STORY_PATH` is spelled out a second time as a raw string inside `BOOTSTRAP_OWNED_PATHS` and can drift [eng/validate-story-artifacts.py:237] — deferred, pre-existing
- [x] [Review][Defer] Dead `excluded_subsection_level is None` condition reads as a guard but cannot be false [eng/validate-story-artifacts.py:1677] — deferred, pre-existing
- [x] [Review][Defer] `contains_terminal_control` also returns true for the `|` delimiter, which its name and docstring do not cover [eng/validate-story-artifacts.py:1508] — deferred, pre-existing
- [x] [Review][Defer] Notices on stdout and failures on stderr can interleave out of order under CI block buffering [eng/validate-story-artifacts.py:405] — deferred, pre-existing
- [x] [Review][Defer] `classified_path_reason` returns an arbitrary dict-order reason when two classified entries overlap [eng/validate-story-artifacts.py:1518] — deferred, pre-existing
- [x] [Review][Defer] Candidate movement is detected but a baseline ref that moves during collection is not [eng/validate-story-artifacts.py:1281] — deferred, pre-existing
- [x] [Review][Defer] Fenced content is now dropped from `evidence_text`, losing the `blocker` exemption for a story whose only mention is fenced [eng/validate-story-artifacts.py:1747] — deferred, pre-existing
- [x] [Review][Defer] `--end-of-options` is passed unconditionally and surfaces as an opaque git failure on git < 2.24 [eng/validate-story-artifacts.py:958] — deferred, pre-existing
- [x] [Review][Patch] Legacy and no-VCS mode crash: `main` collected the workspace scope unconditionally, so `collect_workspace_evidence` raised an uncaught `RuntimeError` on the documented freeform/`NO_VCS` invocation [eng/validate-story-artifacts.py:421] -- the scope is now collected only when a classification could consume it, and reported as a validation failure when it cannot be collected
- [x] [Review][Patch] A `---` inside an indented YAML block scalar terminated frontmatter early, silently dropping a later `story_id` and falling back to inference with no failure [eng/validate-story-artifacts.py:813]
- [x] [Review][Patch] Exclusions bounded reconciliation but not classification, so a `fix(9.7)` commit touching a default-excluded path was falsely `interleaved` [eng/validate-story-artifacts.py:1547] -- excluded paths stay visible under a new `excluded` label and carry no ownership
- [x] [Review][Patch] An unterminated fenced code block silently dropped the rest of the document -- File List, tasks, and declarations -- with no cause reported [eng/validate-story-artifacts.py:901]
- [x] [Review][Patch] An unreadable or non-UTF-8 story artifact raised a traceback instead of returning a validation failure [eng/validate-story-artifacts.py:603]
- [x] [Review][Patch] A double-quoted `\U` escape above U+10FFFF raised `ValueError` from `chr()` [eng/validate-story-artifacts.py:1077]
- [x] [Review][Patch] A level-1 `# Tasks` heading never closed its section, so every later checked item became an execution task demanding path evidence [eng/validate-story-artifacts.py:2066] -- a task heading is a section heading, levels 2 to 6
- [x] [Review][Patch] A recognized task section holding only unchecked items raised a false "no recognized task heading matched" failure [eng/validate-story-artifacts.py:2124] -- both states still fail closed, because an empty `## Tasks` heading must not demote real checked work to a notice, but each now says which state it is
- [x] [Review][Patch] `--candidate HEAD --base ""` reported the invocation error and then a page of derived File List failures [eng/validate-story-artifacts.py:348] -- an invalid invocation now validates nothing
- [x] [Review][Patch] Deleting `process` from the non-owning classifications left the suite green; the regression declared only `shared` [eng/tests/test_validate_story_artifacts.py:2702]
- [x] [Review][Patch] Both checked-task failure sites emitted author-controlled text unescaped without a test [eng/tests/test_validate_story_artifacts.py:4134]
- [x] [Review][Patch] The `outside_review_level` exclusion survived deletion, although losing it turns 7 repository artifacts into hard failures [eng/tests/test_validate_story_artifacts.py:4114]
- [x] [Review][Patch] Renames were never exercised, so removing `-c status.renames=false` stayed green while a real staged rename collapses the workspace block [eng/tests/test_validate_story_artifacts.py:2578]
- [x] [Review][Patch] Tab expansion in `leading_indent_columns` survived deletion because every indented-example fixture used spaces [eng/tests/test_validate_story_artifacts.py:4097]
- [x] [Review][Patch] `test_bootstrap_history_is_available_in_the_ci_checkout` hard-failed in a shallow clone while every sibling skipped [eng/tests/test_validate_story_artifacts.py:2963] -- it stays an assertion under `CI` and skips on a partial clone
- [x] [Review][Patch] The canonical historical report pinned the candidate but copied the live mutable spec into the clone, so any later File List edit would redden the blocking lane [eng/tests/test_validate_story_artifacts.py:3163] -- both sides are pinned now, and only the validator comes from the working tree
- [x] [Review][Patch] The process-boundary test counted `subprocess.run(` occurrences in raw source, so a comment could break it [eng/tests/test_validate_story_artifacts.py:3993] -- the invariant is now structural: no caller outside `run_subprocess` calls it
- [x] [Review][Patch] The runtime-mirror test hardcoded four filenames and compared newline-normalized text, hiding both a fifth diverging file and the on-disk EOL drift [eng/tests/test_validate_story_artifacts.py:112] -- it enumerates the tree and compares bytes with a diagnostic that separates content drift from line-ending drift, and the four files whose on-disk endings had drifted from the repository's `eol=crlf` policy were normalized (content unchanged: `git diff` reports no edit for them)
- [x] [Review][Patch] `step-oneshot.md` created a spec from the template but never resolved the fail-closed `story_id` placeholder [.agents/skills/bmad-build/step-oneshot.md:46]
- [x] [Review][Patch] The loop-8 deferred-work entries were bare bullets, although `bmad-loop-sweep` consumes the ledger as `source_spec`/`summary`/`evidence` records [_bmad-output/implementation-artifacts/deferred-work.md:2719]
- [x] [Review][Patch] Spec bookkeeping: no loop-7 change-log or evidence entry, and `review_loop_iteration` read 5 against eight recorded loops [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:8]
- [x] [Review][Patch] The reference documented only strict `--candidate` mode [docs/reference/story-artifact-validation.md:23] -- invocation modes, every option, default exclusions, exit codes, and the documented-unrelated sections are now covered
- [x] [Review][Patch] The reference introduced its classification table two paragraphs before the table and used vocabulary the table had not yet defined [docs/reference/story-artifact-validation.md:88]
- [x] [Review][Patch] Story identity is now parsed in legacy mode too, so a malformed `title`, H1, or filename fails a run that never consults the ID [docs/reference/story-artifact-validation.md:63] -- documented in the reference and the reconciliation checklist
- [x] [Review][Patch] Dead code introduced by this change: `extract_section_heading_lines` had no callers, and `path_is_classified_covered` re-normalized keys both producers already strip [eng/validate-story-artifacts.py:2304]
- [x] [Review][Defer] A disposition declared on a merge SHA is accepted and rendered but has no effect [eng/validate-story-artifacts.py:1506] -- deferred, coupled to merge reconciliation
- [x] [Review][Defer] Ledger hygiene: loop-4 entries resolved by later loops remain filed open [_bmad-output/implementation-artifacts/deferred-work.md:2589] -- deferred, owned by the sweep

**Review loop 10 (2026-08-26, /bmad-code-review):**

- [x] [Review][Decision] RESOLVED 2026-08-26 (loop 10): DEFER with a scoped follow-up for the Debug build lane — the blast radius is a local dev-build break caught by the first person to run the app, the only option that verifies anything needs CI submodule-init infrastructure outside this story, and a SHA pin would buy a maintenance tax plus a second gitlink lockstep of the kind currently red. Original finding: two submodule gitlinks are claimed story-owned but compiled by no lane — `references/Hexalith.EventStore` (`516f2489`→`64a21632`) and `references/Hexalith.Tenants` (`536e5c33`→`865a80ac`) are in the File List and reported `owned`, but their only consumers (`src/Hexalith.FrontComposer.AppHost`, `src/Hexalith.FrontComposer.UI`) are `Build Solution="Release|*" Project="false"` in the slnx while every workflow builds Release. A breaking upstream change ships with all blocking lanes green. Options: (a) add a Debug build of the Debug-only projects to the lane that already restores config-less, (b) pin the approved gitlink SHAs in a `CiGovernanceTests` fact, (c) drop both paths from the File List and let the maintainer own them.
- [x] [Review][Decision] RESOLVED 2026-08-26 (loop 10): EXTEND THIS STORY — add the `bmad-build-auto` mirrors to the File List, carry `story_id` frontmatter and the `## Commit Scope Dispositions` scaffold into its `spec-template.md`, add the `Validate Commit Scope` section to its `step-04-review.md`, and widen the mirror guard to enumerate both skills. Original finding: the `bmad-build-auto` sibling skill was never updated, so the gate is inert for unattended runs — `.agents/` and `.claude/skills/bmad-build-auto/` carry their own `spec-template.md` (no `story_id` frontmatter, no `## Commit Scope Dispositions` section) and their own `step-04-review.md` (zero occurrences of `Validate Commit Scope`), and the new mirror guard enumerates only `.agents/skills/bmad-build`. `step-01-clarify-and-route.md` already carries a runtime `story_id`, so the identity exists and is simply never written into the artifact. Options: extend this story's frozen File List, or file a follow-up story.
- [x] [Review][Decision] RESOLVED 2026-08-26 (loop 10): RENEGOTIATE BOTH — update AC 3 and the Design Notes label paragraph to the delivered four-valued vocabulary, grow the I/O & Edge-Case Matrix to cover the behaviour nine loops added, and record the renegotiation in the Spec Change Log. Original finding: the `excluded` path label was added in loop 9 but never renegotiated into AC 3 or Design Notes — AC 3 and the Design Notes paragraph still describe a two-valued `owned`/`listed-unowned` vocabulary while the code emits four labels, and the loop-9 Change Log KEEP list asserts classification-aware labels survived unchanged. Either renegotiate the frozen AC or drop the label.
- [x] [Review][Decision] RESOLVED 2026-08-26 (loop 10): RENEGOTIATE — covered by the same frozen-block edit as the label vocabulary above. Original finding: the frozen I/O & Edge-Case Matrix never grew across nine loops — still its original four rows while the code gained the `excluded` label, workspace-mutation detection, contradicted-`story_id` failure, unterminated fence/frontmatter failures, and merge-SHA dispositions. The Spec Change Log carries the narrative; the machine-readable matrix acceptance is checked against does not.
- [x] [Review][Decision] RESOLVED 2026-08-26 (loop 10): UPDATE THE RECORDED RATIONALE, keep the behaviour — `declared_non_owning_paths` already bounds the exemption to paths the declared commit actually touched, resolved from git evidence rather than story text, so the constraint the finding asks for is already implemented; only the stale rationale needs correcting. Original finding: a mutable `shared`/`process` declaration can now suppress a File List extras failure — `check_file_list` skips the extras failure for `path not in declared_non_owning` [eng/validate-story-artifacts.py:1973], but the recorded deferral rationale justifies leaving `shared`/`process` unconstrained on the grounds that "they only withhold ownership and never grant it, so mutable story text cannot broaden attribution". Story text can now suppress a reconciliation failure, which that rationale does not cover.
- [x] [Review][Decision] RESOLVED 2026-08-26 (loop 10): FILE IT WITH AN OWNER — add a ledger entry naming the Release Owner and the REL-6 conflict so a lane red in every iteration's evidence stops being invisible; the assertion itself is not changed by this story. Original finding: the blocking Governance lane is red at delivery with no owner — every iteration records `CiGovernanceTests` at 66/67 with `ReleaseWorkflow_DelegatesToReusableDomainReleaseAfterCiGate` failing on release coordinate `4eb33928` vs Builds gitlink `22a578b5`, with no deferred-work entry, issue, or owner. Per the REL-6 lockstep rule the `references/Hexalith.Builds` gitlink is deliberately independent of the release tool pin, so the assertion itself may encode the wrong invariant. Release Owner call.

- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): An indented example nested under an open list item injects real `## File List` entries — the indented-code guard raises its threshold to `list_content_indent`, so a four-space example under a numbered instruction is parsed as live structure. Reproduced: an example File List indented under `1.` yields `['injected/path.md', 'real/path.md']`, while the same example at top level correctly yields only `['real/path.md']`. Fail-open: an undocumented changed path can be silently covered by an entry that only ever appeared in prose [eng/validate-story-artifacts.py:885]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): The shipped `Commit Scope Dispositions` scaffold fails the parser the same commit added — the template's explanatory HTML comment contains the ` | ` row delimiter, so the declaration-attempt gate classifies it as a malformed declaration. `python3 eng/validate-story-artifacts.py --story .claude/skills/bmad-build/spec-template.md --candidate HEAD` exits 1 with `malformed Commit Scope Dispositions declaration at line 78`. Identical in both mirrors [.claude/skills/bmad-build/spec-template.md:78]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): A second `Commit Scope Dispositions` heading at level 3 smuggles declarations past the pinned-heading guard — the level check inspects only the first resolved heading, and `extract_sections` merges by normalized text regardless of level. Reproduced: a `### Commit Scope Dispositions` block yields both declarations with zero failures [eng/validate-story-artifacts.py:628]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): A listed path matching an exclusion pattern is labelled `excluded` and then hard-fails as an unexplained File List extra — `collect_reconciled_changed_files` drops it via `not is_excluded(path, patterns)` so it never enters `changed_set`, then `check_file_list` demands it. Unsatisfiable: the author is told both to list it and not to list it [eng/validate-story-artifacts.py:1732]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): Repository-root `bin/`, `obj/`, `node_modules/` and bare `docs/_site` are not excluded — `fnmatch` on `**/bin/**` requires a literal `/bin/` substring. Verified: `src/a/bin/x.dll` excluded, `bin/Debug/x.dll` not; same for `obj/`, `node_modules/`. Now load-bearing because exclusions bound classification: a root-level `obj/` path swept into a story commit makes it `interleaved` and hard-fails [eng/validate-story-artifacts.py:19]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): `--unrelated`/`--reason` — the documented escape hatch — has no CLI-level test and no pairing validation; a surplus `--reason` is silently dropped and an unpaired `--unrelated` gets a generic default [eng/validate-story-artifacts.py:1139]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): `step-05-present.md` mandates a post-commit `--candidate HEAD` re-run but never states its precondition — the transition commit's own paths (`{spec_file}`, `sprint-status.yaml`) must already be in the File List, or the mandated re-run classifies it `interleaved` and hard-fails after the spec has already been flipped to `done` [.claude/skills/bmad-build/step-05-present.md:19]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): Non-UTF-8 commit subjects are escaped twice — `format_report_value` is applied at collection and again at render, producing a redundant second layer no other subject gets; no test asserts the rendered form [eng/validate-story-artifacts.py:1456]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): A lone surrogate escape passes the codepoint guard — `0 <= codepoint <= 0x10FFFF` admits `D800`–`DFFF` and `chr` succeeds, so the value reaches subprocess args and raises `UnicodeEncodeError` instead of a validation failure, in a gate whose product is actionable diagnostics [eng/validate-story-artifacts.py:1077]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): Code Map test-line anchors are wrong and run past end of file — cites `111-1490,1495-2915,2918-3695,3697-4379` for a 4368-line file whose class boundaries are 111/1484/2907/3406/3460/3686/4187 [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:44]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): Review Findings line anchors are still stale although two findings claiming to refresh them are marked `[x]` — `:1130`→1574, `:1253`→1727, `:692`→958, `:2026`→2123 [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:96]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): `sprint-status.yaml` header comment and emitted value disagree, and the value is not ISO — `# last_updated: 2026-08-25` against `last_updated: 08-26-2026 01:40` in the same commit [_bmad-output/implementation-artifacts/sprint-status.yaml:2]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): The reference contradicts `--changed-file` behaviour — it states "Exclusions bound classification and both halves of reconciliation", but `collect_changed_files` returns provided paths before any `is_excluded` filtering [docs/reference/story-artifact-validation.md:45]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): The new reference doc is orphaned from the workflow that needs it — `step-04-review.md`, `step-05-present.md` and the reconciliation checklist all use its vocabulary (`owned`/`listed-unowned`/`bootstrap-owned`, the disposition grammar) while none of them links to it; it is reachable only from `docs/reference/index.md` [docs/reference/story-artifact-validation.md:1]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): Merge-row `disposition=`/`reason=` rendering is unpinned — replacing the render branch with `if False:` leaves all 175 tests green; neither merge test asserts the rendered disposition text [eng/validate-story-artifacts.py:1783]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): `StoryArtifactValidatorGate_IsBlockingAndExact` never asserts `quality.yml` still triggers on `pull_request`, so "blocking" is not actually pinned; its `ShouldNotContain("if:")` substring assertions also match any key ending in those characters and any `run:` body [tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs:141]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): A `shared`/`process` declaration for a commit whose subject matches the story is silently inert — the author believes the commit is dispositioned and gets no signal [eng/validate-story-artifacts.py:1574]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): A `bootstrap-owned` declaration is never authorized in legacy mode — authorization runs only inside `candidate_mode`, so the declaration passes unexamined (inert rather than a bypass, but unreported) [eng/validate-story-artifacts.py:380]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): A path touched by an `unmapped` commit is reported twice — once as unmapped-commit evidence and again as an unexplained File List extra; `unmapped_touched_paths` does not exist [eng/validate-story-artifacts.py:1965]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): The test module runs `git` at import time with no exception handling, so a checkout without a git binary collapses the blocking Gate 2b lane into a collection error instead of a clean skip [eng/tests/test_validate_story_artifacts.py:46]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): `DOCUMENTED_UNRELATED_HEADINGS` and `DOCUMENTED_BLOCKER_HEADINGS` are `set` literals, so a path declared under two accepted spellings gets a `PYTHONHASHSEED`-dependent reason and a run-dependent report order — for a gate whose product is a reviewable report, use an ordered sequence plus a duplicate-declaration failure [eng/validate-story-artifacts.py:70]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): Repeated full-document scans and per-item set rebuilds on the gate's hot path — `parse_story_metadata` drives `scan_semantic_lines` five times (the ledger entry says four); `set(excluded_paths)` is rebuilt once per path inside a comprehension condition; `path_is_classified_unrelated` rebuilds a normalized set on every call inside per-path loops [eng/validate-story-artifacts.py:600]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): `missing_listed_guards` is unfalsifiable — deleting it leaves the suite green, and it is subsumed by the `listed_touched_paths != BOOTSTRAP_OWNED_PATHS` check because both guard paths are members of the inventory. Loop 8 deleted a rule on exactly this principle [eng/validate-story-artifacts.py:1360]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): The new CI step uses the module form `python3 -m unittest eng.tests.test_validate_story_artifacts` (relying on implicit namespace packages; there is no `eng/tests/__init__.py`) while every neighbouring step uses the path form, and the workflow's Gate-enumerating header comment was not updated for the new lane. The Governance fact pins the exact module-form string, so both change together [.github/workflows/quality.yml:126]
- [x] [Review][Patch] RESOLVED 2026-08-26 (loop 10): The spec frontmatter reads `status: 'done'` while `sprint-status.yaml` reads `review` and the Change Log's last entry says the story "remains `in-review`" [_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md:5]

- [x] [Review][Defer] `step-03-implement.md` was never updated, so the first implementation commit hard-fails the gate this story introduces [.claude/skills/bmad-build/step-03-implement.md:1] — deferred, pre-existing; already filed in the ledger
- [x] [Review][Defer] Merges never report paths, so the frozen "report all unlisted paths" is not delivered for them [eng/validate-story-artifacts.py:1486] — deferred, pre-existing; disclosed and filed twice already
- [x] [Review][Defer] `ReviewVerifierTests` skip unconditionally because `.agents/skills/bmad-story-automator/` does not exist in this repository, so the only tests asserting that a validator failure blocks story completion are verified nowhere that runs — including the new blocking Gate 2b lane, where they are the "2 optional skips" every iteration reports [eng/tests/test_validate_story_artifacts.py:3406] — deferred, pre-existing
- [x] [Review][Defer] The permissive direction of `is_accepted_extra_reason` is unpinned — forcing it to `return False` leaves all 175 tests green, so no test exercises a listed path exempted by an accepted reason [eng/validate-story-artifacts.py:2040] — deferred, pre-existing

## Spec Change Log

- 2026-08-26, review loop 10 -- Adversarial review reproduced two document-structure bypasses in the gate's own parser: the heading matcher ran on `line.strip()`, so an authoring example indented under an open list item opened a live `## File List` whose entries merged into the canonical one (fail-open -- prose could account for a real changed file), and the pinned-level guard inspected only the first `Commit Scope Dispositions` heading while `extract_sections` merges every occurrence, so a `### Commit Scope Dispositions` smuggled declarations past it. The shipped template also failed the parser the same change added, because its scaffold explains the grammar using the row delimiter. With human approval the frozen block is renegotiated for the delivered four-valued label vocabulary and the Edge-Case Matrix grows from four rows to thirteen, covering the behaviour nine loops added. The unattended `bmad-build-auto` skill is brought into scope: it carried its own template and review step, so the gate was inert exactly where no human would notice. KEEP: the exact code-bound story/baseline/SHA/sole-parent tuple, the immutable thirteen-path intersection, fail-closed declaration handling, listed-only reconciliation, unlisted-path visibility, interleaved precedence, and every delivered classification and reconciliation semantic outside these fixes.
- 2026-08-25, review loop 1 -- The strict gate exposed a self-bootstrap contradiction: the commit introducing exact Story 9.7 attribution could not satisfy a rule that did not yet exist, and published history cannot be rewritten. With explicit human approval, the frozen contract now permits one exact full-SHA `bootstrap-owned` tuple and the later release-compatibility commit is declared `shared`. This avoids the known-bad alternatives of history rewriting, generic exemptions, hidden interleaving, or whole-commit ownership. KEEP: canonical SHA/ancestry checks, exact ID boundaries, per-path visibility, separate workspace evidence, ordinary disposition semantics, existing CI enforcement, and the delivered validator behavior outside this exception.
- 2026-08-25, review loop 2 -- Adversarial review found the non-frozen Design Notes called every File List member `owned`, contradicting the approved rule that `shared` and `process` commits contribute no ownership. The report contract now labels a listed path `owned` only for `owned`, `interleaved`, or `bootstrap-owned` classifications and uses `listed-unowned` for listed paths in every non-owning classification. The authorization must also bind the canonical Story 9.7 artifact so a copied fixture cannot reuse it. This avoids misleading staging evidence and reusable recovery artifacts. KEEP: the exact code-bound story/baseline/SHA/sole-parent tuple, both listed and touched guard paths, fail-closed declaration handling, listed-only reconciliation, unlisted-path visibility, pure authorization tests, historical CLI integration, and operator anti-copy guidance.
- 2026-08-25, review loop 3 -- Adversarial review proved that binding `bootstrap-owned` to the canonical artifact still let later story text broaden historical ownership by adding either currently unowned bootstrap-commit path to the mutable File List. The non-frozen design now requires the exact twelve-path bootstrap-owned set to be code-bound and requires any changed intersection between the bootstrap commit and the File List to fail closed. This avoids converting the story artifact into an authorization source while still allowing unrelated future File List entries that the bootstrap commit never touched. KEEP: canonical artifact binding; the exact story/baseline/SHA/sole-parent/subject tuple; both guard paths; fail-closed malformed and multiple declarations; classification-aware `owned`, `listed-unowned`, and `unowned` labels; shared/process non-ownership; full per-path visibility; pure authorization tests; historical CLI integration; and operator anti-copy guidance.
- 2026-08-25, review loop 4 -- Human-approved review reconciliation amended the frozen Approach and third acceptance criterion to state the classification-aware report-label contract already introduced in loop 2. This avoids the contradictory reading that truthful `listed-unowned` labels violate an otherwise unchanged classification and reconciliation contract. The patch also makes the gate hermetic, mirrors it into the runtime Claude skill, closes parser and story-ID boundary gaps, pins the immutable authorization inventories, and records both intentionally unowned bootstrap paths. KEEP: the exact authorization tuple and twelve-path intersection; listed-only ownership; all-path visibility; ordinary classification and reconciliation behavior; the blocking CI command; fail-closed malformed declarations; and the loop-3 anti-broadening proof.
- 2026-08-25, review patch follow-up -- Review found three fail-open legacy/CLI parsing seams outside the frozen authorization contract: unusable baselines could hide unstaged tracked paths, duplicate critical frontmatter scalars overwrote their predecessors, and an empty explicit base was treated as absent. The patch now resolves a usable legacy baseline or reports and uses a bare-diff workspace fallback, rejects duplicate `story_id`/`baseline_commit` scalars, and rejects an explicitly empty `--base`. KEEP: strict candidate evidence, the approved bootstrap tuple and frozen block, ordinary valid-baseline discovery, staged/untracked discovery, and existing story-ID precedence.
- 2026-08-25, review loop 5 -- Human-approved reconciliation reverses the prior unrelated classification for `references/Hexalith.EventStore`, claims both that path and `references/Hexalith.Tenants` for the published `5817f191...` Story 9.7 commit, and expands the exact code-bound bootstrap-owned inventory from twelve to thirteen paths because the bootstrap commit also touched Tenants. This intentionally absorbs a pre-existing workspace change despite the Epic 9 isolation preference. The frozen contract now admits multiple exact, reasoned `shared`/`process` rows while forbidding either kind from suppressing `interleaved`; current-artifact tests validate the current range instead of combining mutable metadata with an old candidate. The patch also synchronizes runtime workflow mirrors, documents the report grammar, rejects every duplicate frontmatter scalar and inline-comment ambiguity, bounds checked-task extraction, and pins strict CLI/history/filter seams. KEEP: the exact bootstrap story/baseline/SHA/topology tuple, code-bound path inventory, listed-only reconciliation, all-path visibility, classification-aware labels, canonical refs, workspace evidence, and blocking CI command.
- 2026-08-25, review loop 5 patch follow-up -- Review found that the first heading-boundary fix excluded legitimate nested execution tasks, that matching listed-only commits could still be downgraded by non-owning dispositions, and that control-bearing Git/report values were emitted raw. The patch now keeps task collection active across nested execution subsections while excluding Review Findings, gives all matching listed-only commits `owned` precedence over `shared`/`process`, and deterministically quotes control-bearing subjects, disposition reasons, and Git paths. KEEP: interleaved precedence, non-matching shared/process behavior, ordinary report text, repeated recognized task sections, checked Review Findings exclusion, and the complete exact bootstrap authorization contract.
- 2026-08-25, review loop 6 patch -- Review found escaping gaps in author-controlled reasons and paths, unpinned fail-closed and ref-stability seams, legacy-parser false positives, incomplete task-heading boundaries, silent degraded discovery, and duplicate conflict-state reporting. The patch extends deterministic escaping through C0/C1 and Unicode format controls, adds end-to-end and mutation-sensitive regressions for every named seam, parses legacy H1s only outside frontmatter/fences, recognizes supported task-heading text at any Markdown level, reports stray checked items and degraded fallback visibly, rejects malformed dispositions in both modes with source lines, and reports unmerged paths once. Documentation now states the workflow-versus-CI enforcement boundary and the bare-story-ID subject trap. KEEP: the frozen bootstrap tuple and thirteen-path intersection, exact matching/interleaving precedence, classification-aware ownership labels, all-path visibility, and ordinary human-readable report output.
- 2026-08-25, review loop 6 patch follow-up -- Consolidated review found that metadata and task parsers still treated frontmatter/fenced examples as executable Markdown, report quoting omitted pipe and Unicode line/paragraph separators, and the strict collector protected the candidate ref but not its workspace snapshot. The patch centralizes source-numbered semantic-line scanning with valid fence closure, excludes examples from section/disposition/File List/task evidence, preserves exact disposition and task source lines, quotes every affected value surface, and compares a final staged/unstaged/untracked/unresolved snapshot. `review_loop_iteration` remains unchanged because this is a patch-only pass. KEEP: the frozen authorization block, any-level and nested task semantics, Review Findings exclusions, ordinary report text, candidate re-resolution, classification behavior, and reconciliation rules.
- 2026-08-25, review loop 7 patch -- Review found that directory-prefix `Documented Unrelated Changes` coverage exempted committed story code in legacy mode, a regression against `ceae00a4`, and that the canonical live-CLI report asserted exit 0 for `--candidate HEAD` with no expiry inside the blocking quality lane, so the first later commit touching Story 9.7's shared bookkeeping paths would have turned `main` red. The patch bounds parent coverage to workspace state, requires an exact classification entry for a committed path, and pins the canonical report to the delivered commit; merge path reconciliation was filed as new scope rather than patched. KEEP: exact classification entries for committed paths, directory coverage for uncommitted state, the immutable bootstrap tuple, and the blocking CI command.
- 2026-08-25, review loop 8 patch -- Consolidated review found that author-controlled text could still become document structure, that story identity was accepted without cross-checking or normalization, and that several boundaries were enforced on one half of the gate only. The patch gives both parsers one shared frontmatter/body split with BOM handling and an unterminated-frontmatter failure, drops indented examples from semantic scanning, reads checked items from every list marker, keeps nested and suffixed recognized task sections open while requiring some section to collect them, rejects empty and contradicted explicit `story_id` values and normalizes padded ones on both sides of subject matching, pins `Commit Scope Dispositions` to its exact heading level while tolerating prose and reporting non-conforming declaration attempts, resolves double-quoted scalar escapes, fails closed on an unresolvable `--base`, bounds committed paths by the same exclusions as workspace paths, stops reporting a declared non-owning commit's listed paths as unexplained extras, reports a non-UTF-8 commit subject instead of raising, and routes every git invocation and every report value through one boundary each. KEEP: the frozen bootstrap tuple and thirteen-path intersection, listed-only reconciliation, all-path visibility, classification-aware labels, interleaved precedence, ordinary human-readable report output, and the blocking CI command.
- 2026-08-25, review loop 9 patch -- Adversarial review found nine behavior defects, five guards no test could fail, four test-quality defects, and documentation and bookkeeping gaps. The patch stops the documented no-VCS invocation from raising, keeps an indented block-scalar `---` from ending frontmatter, bounds classification by the same exclusions as reconciliation under a new visible `excluded` path label, reports an unterminated fence and an unreadable story artifact, guards the codepoint escape range, treats a task heading as a section heading, distinguishes "no heading matched" from "checked work outside every section", and validates nothing after an invalid invocation. Regressions now pin the `process` kind, both task-escaping sites, the review-findings exclusion, rename handling, and tab indentation; the mirror test enumerates the tree and compares bytes; the canonical historical report is pinned on the artifact side as well as the range; and the process-boundary invariant is structural. `review_loop_iteration` is reconciled to 9. KEEP: the frozen bootstrap tuple and thirteen-path intersection, listed-only reconciliation, all-path visibility, classification-aware labels, interleaved precedence, fail-closed identity, and the blocking CI command.

## Commit Scope Dispositions

- `fd04bdd97fbdd4976a0f213e46a316be199fd8a9` | `bootstrap-owned` | self-enforcing Story 9.7 delivery predates its exact commit-ID gate; human-approved one-time recovery bound to the immutable baseline and guard paths
- `2dcc43fea9aa39c42d15b1028fa5ef774b5d8b06` | `shared` | later release-compatibility work changed shared CI, governance, and deferred-work paths without belonging to Story 9.7
- `c4df029050cb241f74cafd04a01f7718eae1ec0c` | `shared` | earlier Fluent UI and test-stability work resealed the shared analyzer-policy inventory without belonging to Story 9.7
- `f37124607b0f4c54c8be3f2fd0223fadb89d7e5c` | `shared` | earlier dependency maintenance touched the later-claimed EventStore pointer without belonging to Story 9.7
- `0bfb143e6d52cf83abcbd893e7f1c679f17d598b` | `shared` | earlier EventStore dependency maintenance touched the later-claimed pointer without belonging to Story 9.7
- `cdfff09daef6266ab538d6f745fd257b573351de` | `shared` | earlier dependency maintenance touched the later-claimed EventStore pointer without belonging to Story 9.7

## Design Notes

`bootstrap-owned` is a code-authorized historical recovery, not a general third disposition. The story declaration and canonical artifact path must match the immutable authorization tuple; copying the text, editing frontmatter, or moving a baseline cannot create authority.

The bootstrap-owned set is also immutable and code-bound. After the loop-5 human scope reversal, the intersection of the bootstrap commit's touched paths and the canonical story File List must be exactly these thirteen paths; adding the remaining historically unowned path or removing an authorized path invalidates the declaration and grants no bootstrap ownership:

- `.agents/skills/bmad-build/spec-template.md`
- `.agents/skills/bmad-build/step-02-plan.md`
- `.agents/skills/bmad-build/step-04-review.md`
- `.agents/skills/bmad-build/step-05-present.md`
- `.github/workflows/quality.yml`
- `_bmad-output/implementation-artifacts/deferred-work.md`
- `_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `_bmad-output/implementation-artifacts/story-review-reconciliation-checklist.md`
- `eng/tests/test_validate_story_artifacts.py`
- `eng/validate-story-artifacts.py`
- `references/Hexalith.Tenants`
- `tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs`

Future File List entries that the bootstrap commit did not touch do not change this intersection and therefore do not broaden or invalidate the historical authorization.

The bootstrap commit's one touched path outside the immutable authorized set is `_bmad-output/implementation-artifacts/spec-actions-29316660112-fix-cicd.md`. It is intentionally absent from the Story 9.7 File List, remains visible as `unowned`, and never contributes to reconciliation.

Path labels describe ownership, not mere File List membership, and the vocabulary is four-valued. An exclusion pattern wins first: a covered path is `excluded` under any classification. Otherwise, for `owned`, `interleaved`, and `bootstrap-owned` commits, listed paths are `owned` and other paths are `unowned`; for `shared`, `process`, `unmapped`, and `unrelated` commits, listed paths are `listed-unowned` and other paths are `unowned`. Reconciliation still admits only listed paths from ownership-contributing classifications.

Two boundaries decide what the gate can see, and both are now enforced in one place each. Document structure is only real Markdown outside frontmatter, fenced blocks, and indented examples, read through a single frontmatter/body split that `extract_frontmatter` and the semantic-line scanner share; an author's example can therefore show a File List row, a declaration, or a checked task without becoming one. Story identity is the frontmatter `story_id` when present, rejected when blank and when a well-formed filename, `title`, or H1 identity contradicts it, and normalized so a padded spelling is the same identity on both sides of subject matching.

Exclusions and non-owning declarations bound reconciliation symmetrically. A default or `--exclude` pattern now filters committed and workspace paths alike, and a listed path that only a declared `shared` or `process` commit touched is explained by that declaration rather than reported as an unexplained File List extra. Neither rule suppresses a path from the report and neither grants ownership.

The earlier rationale for leaving `shared`/`process` declarations unconstrained -- that "they only withhold ownership and never grant it, so mutable story text cannot broaden attribution" -- no longer describes what they do: a declaration can now suppress an unexplained-extra failure. That suppression is intended and is bounded by resolved git evidence rather than story text: `declared_non_owning_paths` exempts only paths the declared commit actually touched, so naming a SHA cannot excuse a path that SHA never changed. What story text still cannot do is broaden ownership or suppress an `interleaved` failure.

Exclusion patterns bound classification as well as reconciliation, and an excluded path is reported under its own `excluded` label. Nothing is hidden: the path is printed, it simply carries no ownership and cannot make a listed commit `interleaved`. The bootstrap authorization deliberately reads the unfiltered path set, so no CLI flag can reshape the immutable intersection.

Story identity is parsed on every run, including legacy mode, which never consumes the resulting ID. A malformed or self-contradicting `title`, H1, or filename therefore fails a legacy run too. That is intentional -- an artifact that disagrees with itself about which story it is cannot be reconciled by any mode -- and it is documented in both the reference and the reconciliation checklist so the failure is not mistaken for a mode bug.

The strict commit-scope gate is enforced by the review workflow in `.agents/skills/bmad-build/step-04-review.md` and its synchronized runtime mirror. The blocking `quality.yml` Gate 2b command runs the validator's regression suite; it proves the validator has not been broken, but it does not discover a story artifact from the branch and run that artifact's strict report. CI-level story-completion enforcement remains deferred until a branch-to-spec resolution convention exists.

## Verification

**Commands:**
- `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py` -- both modules compile.
- `python3 -m unittest eng.tests.test_validate_story_artifacts` -- all mandatory fixtures pass with no new skip.
- `dotnet build tests/Hexalith.FrontComposer.Shell.Tests/Hexalith.FrontComposer.Shell.Tests.csproj --configuration Release --no-restore` -- the changed Governance project builds with zero warnings and errors.
- `DiffEngine_Disabled=true dotnet tests/Hexalith.FrontComposer.Shell.Tests/bin/Release/net10.0/Hexalith.FrontComposer.Shell.Tests.dll -method Hexalith.FrontComposer.Shell.Tests.Governance.CiGovernanceTests.StoryArtifactValidatorGate_IsBlockingAndExact` and the corresponding `AnalyzerPolicyGovernanceTests.AnalyzerPolicy_IdentifierInventory_MatchesSeal` method -- the isolated CI pin and its exact analyzer inventory both pass.
- `DiffEngine_Disabled=true dotnet tests/Hexalith.FrontComposer.Shell.Tests/bin/Release/net10.0/Hexalith.FrontComposer.Shell.Tests.dll -class Hexalith.FrontComposer.Shell.Tests.Governance.CiGovernanceTests -parallel none` -- run the complete affected C# Governance class and record any unrelated repository-policy blocker separately from the focused Story 9.7 fact.
- `python3 eng/validate-story-artifacts.py --story _bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md --candidate HEAD` -- exact bootstrap/shared report passes and keeps unowned paths visible.
- `pwsh ./eng/validate-docs.ps1` -- the reusable story-validation reference and navigation entry satisfy DocFX validation.
- `git diff --check` -- changed files are whitespace-clean.

## Test Evidence

- Pre-change baseline: `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py && python3 -m unittest eng.tests.test_validate_story_artifacts` passed 72 tests with 2 existing optional `ReviewVerifierTests` skips.
- Iteration-1 known-bad evidence: the focused suite passed 76 tests with the same 2 optional skips, and the live strict report passed, but review proved its report still mislabeled listed paths in the `shared` commit as `owned` and allowed a copied fixture path to reuse the hard-coded tuple. Iteration 2 must replace this evidence rather than treating it as acceptance proof.
- Iteration-2 known-bad evidence: `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py && python3 -m unittest eng.tests.test_validate_story_artifacts` passed 76 tests with the same 2 optional `ReviewVerifierTests` skips, and the live strict report passed with truthful classification labels. Review nevertheless proved that mutating the canonical File List could broaden bootstrap ownership, so this evidence cannot satisfy iteration 3.
- Iteration-3 evidence: `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py && python3 -m unittest eng.tests.test_validate_story_artifacts` passed 83 tests with the same 2 optional `ReviewVerifierTests` skips. The suite includes the exact historical CLI report, copied-artifact rejection, pure tuple/topology/declaration/guard checks, independent declared-versus-resolved baseline deviations that prove fail-closed `unmapped`/`listed-unowned` behavior with no ownership, a canonical-metadata regression that adds a historically unowned bootstrap path to the File List and likewise proves no ownership, and an end-to-end regression proving checked tasks under the exact `## Tasks & Acceptance` heading are extracted and evidence-validated. `python3 eng/validate-story-artifacts.py --story _bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md --candidate HEAD` passed against canonical candidate `f35523436db525197dcc223ddbe8aa0db97bbdf3`; it reported the bootstrap commit's twelve authorized paths as `owned`, both other paths as `unowned`, and the shared commit's three listed paths as `listed-unowned` while preserving every other path as `unowned`.
- Iteration-4 evidence: `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py && python3 -m unittest eng.tests.test_validate_story_artifacts` passed 89 tests with the same 2 optional `ReviewVerifierTests` skips and no history skip in this full checkout. New regressions pin the four closed inventories, repeated task headings, disposition prose, normalized trailing-slash classifications, version-token boundaries, clean-clone historical execution, and history availability guards. The Shell.Tests Release project built with 0 warnings / 0 errors; `StoryArtifactValidatorGate_IsBlockingAndExact` and `AnalyzerPolicy_IdentifierInventory_MatchesSeal` each passed 1/1 after the exact 6,997-token hash reseal. The full affected `CiGovernanceTests` class ran 67 tests: 66 passed and the pre-existing `ReleaseWorkflow_DelegatesToReusableDomainReleaseAfterCiGate` failed because the committed Builds gitlink is `22a578b576a515d2af214fe81859447fffc97981` while unchanged release coordinates pin `4eb33928a1d8c7775f97221cf9edc171db0cb5f8`; Story 9.7 changes neither surface. The live strict gate passed against canonical candidate `a229be7e7e8ea8af43c537a9ed24f13175cc1416`: the bootstrap commit's twelve authorized paths remained `owned`, its two now-declared outside paths remained `unowned`, the release-compatibility paths remained `listed-unowned`, and the earlier analyzer-ledger commit remained visible as an explicitly reasoned `shared` commit. The only unrelated workspace state was the pre-existing maintainer-owned `references/Hexalith.EventStore` pointer, which remained visible and unclaimed.
- Iteration-4 review-patch evidence: `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py` passed, the three focused regressions passed 3/3, and `python3 -m unittest eng.tests.test_validate_story_artifacts` passed 92 tests with the same 2 optional `ReviewVerifierTests` skips. The added subcases prove missing, `NO_VCS`, and unresolvable legacy baselines all use an explicit bare-diff fallback that rediscovers unstaged `README.md`; duplicate `story_id` and duplicate `baseline_commit` each fail closed; and an explicitly empty `--base` is rejected without a traceback. The strict Story 9.7 gate passed against canonical candidate `a229be7e7e8ea8af43c537a9ed24f13175cc1416`, preserving the exact bootstrap/shared labels and the visible documented unrelated `references/Hexalith.EventStore` workspace pointer.
- Matrix audit: `python3 -m unittest -v eng.tests.test_validate_story_artifacts.BootstrapOwnedAuthorizationTests` passed 14/14 with no skips. The exact tuple and canonical historical CLI tests cover the authorized-bootstrap and later-shared rows; the authorization-dimension and immutable-intersection tests cover wrong authorization; and the duplicate-declaration parser test plus the authorization-dimension cases cover invalid declarations including stale/short SHA, empty reason, missing guard paths, topology, and File List deviations.
- Iteration-5 evidence: `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py && python3 -m unittest eng.tests.test_validate_story_artifacts` passed 103 tests with the same 2 optional `ReviewVerifierTests` skips. The 16-test `BootstrapOwnedAuthorizationTests` matrix passed with no skips and now requires the repository's bootstrap history, pins the ownership-contributing classification filter, exercises bootstrap rejection on a merge, and validates the current canonical artifact rather than combining mutable story metadata with a historical candidate. The live strict gate passed against canonical candidate `88bae03fb6daac6b6433c8c38e7e87d6ff882fb2`: `fd04bdd9...` reports thirteen exact authorized paths as `owned` and the spec-actions path as `unowned`; all five exact non-owning declarations remain visible with listed paths as `listed-unowned`; and `5817f191...` reports both EventStore and Tenants as `owned`. The Shell.Tests Release project built with 0 warnings / 0 errors; `StoryArtifactValidatorGate_IsBlockingAndExact` and `AnalyzerPolicy_IdentifierInventory_MatchesSeal` each passed 1/1. The full affected `CiGovernanceTests` class again ran 67 tests: 66 passed and only the pre-existing `ReleaseWorkflow_DelegatesToReusableDomainReleaseAfterCiGate` failed because unchanged release coordinates pin `4eb33928a1d8c7775f97221cf9edc171db0cb5f8` while the approved Builds gitlink differs. `pwsh ./eng/validate-docs.ps1` passed after adding the reusable story-validation reference.
- Iteration-5 review-patch follow-up evidence: the seven focused review regressions passed 7/7, covering nested execution-task validation, checked Review Findings exclusion, interleaved precedence, both matching-commit disposition kinds, and subject/reason/path control escaping. `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py && python3 -m unittest eng.tests.test_validate_story_artifacts` passed 108 tests with the same 2 optional `ReviewVerifierTests` skips; the 16-test `BootstrapOwnedAuthorizationTests` matrix again passed with no skips. The strict gate passed against canonical candidate `88bae03fb6daac6b6433c8c38e7e87d6ff882fb2` with the exact bootstrap/shared/owned labels and no unresolved workspace state. The Shell.Tests Release project built with 0 warnings / 0 errors, and the two focused Governance facts each passed 1/1. The complete `CiGovernanceTests` class remained 66/67 solely because the pre-existing `ReleaseWorkflow_DelegatesToReusableDomainReleaseAfterCiGate` assertion compares the unchanged `4eb33928...` release coordinates with the different approved Builds gitlink. `pwsh ./eng/validate-docs.ps1` and `git diff --check` both passed. The story remains `in-review`.
- Iteration-6 review-patch evidence: `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py && python3 -m unittest eng.tests.test_validate_story_artifacts` passed 127 tests with the same 2 optional `ReviewVerifierTests` skips. `python3 -m unittest -v eng.tests.test_validate_story_artifacts.BootstrapOwnedAuthorizationTests` passed all 20 tests with no skips, covering every frozen matrix row plus the previously unpinned NUL, canonical-SHA, status-row, filename, immutable-inventory, and current-history seams. The strict gate passed against canonical candidate `23b0b5564404bf025b2faa19d7d1a0737f8b5c85`, preserving the exact thirteen-path bootstrap ownership, visible unowned bootstrap path, five non-owning declarations, and later matching submodule commits; current workspace paths all reconcile through the File List. The Shell.Tests Release project built with 0 warnings / 0 errors, and `StoryArtifactValidatorGate_IsBlockingAndExact` plus `AnalyzerPolicy_IdentifierInventory_MatchesSeal` each passed 1/1. The complete `CiGovernanceTests` class remained 66/67 solely on the same pre-existing `ReleaseWorkflow_DelegatesToReusableDomainReleaseAfterCiGate` Builds-coordinate mismatch at `CiGovernanceTests.cs:804`; Story 9.7 changes neither coordinate. `pwsh ./eng/validate-docs.ps1` passed, and `git diff --check` reported no whitespace errors.
- Iteration-6 consolidated review-patch evidence: the 8 focused parser/report/workspace regressions passed 8/8. `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py && python3 -m unittest eng.tests.test_validate_story_artifacts` passed 134 tests with the same 2 optional `ReviewVerifierTests` skips, and the 20-test `BootstrapOwnedAuthorizationTests` matrix passed with no skips. The strict gate passed against canonical candidate `23b0b5564404bf025b2faa19d7d1a0737f8b5c85` with stable workspace snapshots and the exact bootstrap/shared/owned labels. The Shell.Tests Release project built with 0 warnings / 0 errors; `StoryArtifactValidatorGate_IsBlockingAndExact` and `AnalyzerPolicy_IdentifierInventory_MatchesSeal` each passed 1/1. The complete `CiGovernanceTests` class remained 66/67 solely on the unchanged `ReleaseWorkflow_DelegatesToReusableDomainReleaseAfterCiGate` mismatch between release coordinate `4eb33928a1d8c7775f97221cf9edc171db0cb5f8` and approved Builds gitlink `22a578b576a515d2af214fe81859447fffc97981`; Story 9.7 changes neither coordinate. `pwsh ./eng/validate-docs.ps1` and `git diff --check` passed.
- Iteration-7 evidence (recorded during loop 9 from the delivered loop-7 working tree, which loop 7 did not record): `python3 -m unittest eng.tests.test_validate_story_artifacts` passed 135 tests with the same 2 optional `ReviewVerifierTests` skips, and the strict gate passed against candidate `743618d5e933f18c0d22cab78d675e96ffc251b1` with the exact thirteen-path bootstrap ownership and the five non-owning declarations. The loop-7 patch itself is pinned by `test_classified_directory_cannot_cover_committed_paths` and by the canonical historical report's pin to `STORY_9_7_DELIVERY_COMMIT`.
- Iteration-8 review-patch evidence: `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py && python3 -m unittest eng.tests.test_validate_story_artifacts` passed 164 tests with the same 2 optional `ReviewVerifierTests` skips. A mutation pass reverted each of the 22 guards added by this patch one at a time and re-ran the suite; all 22 were caught. An earlier round of that pass showed a separate thematic-break rule in the disposition parser surviving, because the declaration-attempt gate already subsumed it, so that unfalsifiable rule was deleted rather than shipped. A differential parse of all 373 repository `_bmad-output` Markdown artifacts against the `743618d5` validator showed only intended differences: File List entries with a trailing slash now normalize, one notice containing the `|` delimiter is now quoted, and two planning artifacts lose false `duplicate frontmatter key` failures that came from nested mapping keys. The strict gate passed against canonical candidate `743618d5e933f18c0d22cab78d675e96ffc251b1`, the delivered HEAD -- earlier iteration entries cite the candidate that was HEAD when they ran -- preserving the exact thirteen-path bootstrap ownership, the visible `unowned` bootstrap path, all five non-owning declarations with `listed-unowned` labels, and the later matching submodule commits. No C# source, workflow, or contract file changed in this pass, so the existing Release `Hexalith.FrontComposer.Shell.Tests` assembly was reused: `StoryArtifactValidatorGate_IsBlockingAndExact` and `AnalyzerPolicy_IdentifierInventory_MatchesSeal` each passed 1/1, and the complete `CiGovernanceTests` class remained 66/67 solely on the unchanged pre-existing `ReleaseWorkflow_DelegatesToReusableDomainReleaseAfterCiGate` mismatch between release coordinate `4eb33928a1d8c7775f97221cf9edc171db0cb5f8` and the approved Builds gitlink. `pwsh ./eng/validate-docs.ps1` passed after the reference-grammar update, and `git diff --check` reported no whitespace errors.
- Iteration-9 review-patch evidence: `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py && python3 -m unittest eng.tests.test_validate_story_artifacts` passed 175 tests with the same 2 optional `ReviewVerifierTests` skips. A mutation pass reverted each of the 16 code guards added this round one at a time -- no-VCS coverage reporting, the column-zero frontmatter delimiter, classification exclusions, the unterminated-fence report, the unreadable-artifact report, the codepoint range guard, the task heading level, the outside-section message, the invocation abort, the `process` non-owning kind, both task-escaping sites, the review-findings exclusion, rename detection, tab expansion, and the process boundary -- and all 16 were caught. The two test-side guards were verified by hand: introducing content drift and then line-ending-only drift into a `.claude` mirror each failed the enumerating byte comparison with the matching diagnostic, and editing the live spec's File List left the canonical historical report green, proving it is no longer coupled to a mutable artifact. The strict gate passed against canonical candidate `743618d5e933f18c0d22cab78d675e96ffc251b1`. No C# source, workflow, or contract file changed in this round either, so the existing Release `Hexalith.FrontComposer.Shell.Tests` assembly was reused: `StoryArtifactValidatorGate_IsBlockingAndExact` and `AnalyzerPolicy_IdentifierInventory_MatchesSeal` each passed 1/1, and the complete `CiGovernanceTests` class remained 66/67 on the unchanged pre-existing `ReleaseWorkflow_DelegatesToReusableDomainReleaseAfterCiGate` Builds-coordinate mismatch. `pwsh ./eng/validate-docs.ps1` and `git diff --check` both passed.

- Iteration-10 review-patch evidence: `python3 -m py_compile eng/validate-story-artifacts.py eng/tests/test_validate_story_artifacts.py && python3 -m unittest eng.tests.test_validate_story_artifacts` passed 187 tests with the same 2 optional `ReviewVerifierTests` skips. The new 12-test `ReviewLoop10HardeningTests` class pins the column-zero heading rule, the list-content-indent example threshold, nested-bullet survival, all-occurrence dispositions levelling, HTML-comment tolerance in the disposition parser, both shipped `spec-template.md` mirrors parsing clean, root-level and bare-directory exclusion coverage with no over-match, `--unrelated`/`--reason` pairing, lone-surrogate rejection, and deterministic classified-heading order. A mutation pass reverted each of the 8 new code guards one at a time -- the column-zero heading guard, the list-content-indent threshold, the all-occurrence level check, the HTML-comment skip, the exclusion pattern forms, the surrogate range, the CLI pairing check, and a `.claude/skills/bmad-build-auto` mirror byte -- and all 8 were caught; the first run of the list-content-indent mutant survived, so `test_nested_bullets_inside_a_list_keep_their_meaning` was added and the mutant re-run and caught. The strict gate passed against candidate `HEAD` with the exact thirteen-path bootstrap ownership, the visible `unowned` bootstrap path, and all five non-owning declarations rendered `listed-unowned`. The Shell.Tests Release project built with 0 warnings / 0 errors; `StoryArtifactValidatorGate_IsBlockingAndExact` passed 1/1 with its new `pull_request`/`push` trigger assertions, and the complete `CiGovernanceTests` class remained 66/67 solely on the unchanged pre-existing `ReleaseWorkflow_DelegatesToReusableDomainReleaseAfterCiGate` Builds-coordinate mismatch, now filed in the ledger with the Release Owner as owner.

## File List

- `_bmad-output/implementation-artifacts/spec-9-7-add-story-id-and-commit-scope-evidence.md`
- `_bmad-output/implementation-artifacts/deferred-work.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `_bmad-output/implementation-artifacts/story-review-reconciliation-checklist.md`
- `eng/validate-story-artifacts.py`
- `eng/tests/test_validate_story_artifacts.py`
- `.agents/skills/bmad-build/spec-template.md`
- `.agents/skills/bmad-build/step-02-plan.md`
- `.agents/skills/bmad-build/step-04-review.md`
- `.agents/skills/bmad-build/step-05-present.md`
- `.agents/skills/bmad-build/step-oneshot.md`
- `.github/workflows/quality.yml`
- `.claude/skills/bmad-build/spec-template.md`
- `.claude/skills/bmad-build/step-02-plan.md`
- `.claude/skills/bmad-build/step-04-review.md`
- `.claude/skills/bmad-build/step-05-present.md`
- `.claude/skills/bmad-build/step-oneshot.md`
- `.agents/skills/bmad-build-auto/spec-template.md`
- `.agents/skills/bmad-build-auto/step-04-review.md`
- `.claude/skills/bmad-build-auto/spec-template.md`
- `.claude/skills/bmad-build-auto/step-04-review.md`
- `_bmad-output/contracts/analyzer-policy-exception-ledger-v1.json`
- `docs/reference/index.md`
- `docs/reference/story-artifact-validation.md`
- `references/Hexalith.EventStore`
- `references/Hexalith.Tenants`
- `tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs`

## Suggested Review Order

**Exact bootstrap boundary**

- Start with the immutable authorization surface defining exactly what historical recovery may own.
  [`validate-story-artifacts.py:236`](../../eng/validate-story-artifacts.py#L236)

- Verify every declared and resolved tuple dimension fails closed.
  [`validate-story-artifacts.py:1301`](../../eng/validate-story-artifacts.py#L1301)

- Confirm canonical range evidence authorizes, then classifies, every non-merge commit.
  [`validate-story-artifacts.py:1385`](../../eng/validate-story-artifacts.py#L1385)

**Document parsing**

- Read the one shared frontmatter/body split, whose delimiters are column-zero lines.
  [`validate-story-artifacts.py:796`](../../eng/validate-story-artifacts.py#L796)

- Follow the semantic-line scan that drops frontmatter, fences, and indented examples, and reports an unterminated fence.
  [`validate-story-artifacts.py:836`](../../eng/validate-story-artifacts.py#L836)

- Check story identity: empty, padded, contradicted, and unreadable artifacts.
  [`validate-story-artifacts.py:723`](../../eng/validate-story-artifacts.py#L723)

- Check the disposition grammar, its exact heading level, and prose tolerance.
  [`validate-story-artifacts.py:911`](../../eng/validate-story-artifacts.py#L911)

- Confirm task extraction keeps nested sections open and says which failure state it is in.
  [`validate-story-artifacts.py:2045`](../../eng/validate-story-artifacts.py#L2045)

**Ownership, reporting, and process boundaries**

- Confirm one exclusion set bounds classification and both halves of reconciliation.
  [`validate-story-artifacts.py:1708`](../../eng/validate-story-artifacts.py#L1708)

- Confirm a declared non-owning commit explains a listed path without owning it.
  [`validate-story-artifacts.py:1934`](../../eng/validate-story-artifacts.py#L1934)

- Trace one deterministic quoting boundary and the `excluded` path label.
  [`validate-story-artifacts.py:1737`](../../eng/validate-story-artifacts.py#L1737)

- Confirm every git invocation routes through one substitutable process boundary.
  [`validate-story-artifacts.py:1194`](../../eng/validate-story-artifacts.py#L1194)

- Confirm an invalid invocation validates nothing, and that a missing workspace is reported rather than raised.
  [`validate-story-artifacts.py:328`](../../eng/validate-story-artifacts.py#L328)

- Read contributor semantics for invocation, identity, structure, grammar, and the workflow-versus-CI enforcement boundary.
  [`story-artifact-validation.md:13`](../../docs/reference/story-artifact-validation.md#L13)

**Regression proof**

- Review the parser boundaries that decide what text becomes document structure.
  [`test_validate_story_artifacts.py:3697`](../../eng/tests/test_validate_story_artifacts.py#L3697)

- Review the strict-mode identity, exclusion, rename, invocation, and non-owning-extra regressions.
  [`test_validate_story_artifacts.py:2578`](../../eng/tests/test_validate_story_artifacts.py#L2578)

- Exercise the exact historical report, pinned on both the range and every artifact in it.
  [`test_validate_story_artifacts.py:3163`](../../eng/tests/test_validate_story_artifacts.py#L3163)

- End with the exact historical bootstrap authorization matrix.
  [`test_validate_story_artifacts.py:2918`](../../eng/tests/test_validate_story_artifacts.py#L2918)
