# Story 4.6: Empty States, Field Descriptions & Unsupported Types

Status: review

> **Epic 4 § 246-283** · **FR9** (explicit placeholder for unsupported types — build warning + docs link + zero silent omissions) · **FR10** (developer field descriptions surface as contextual help in generated views) · **FR11** (meaningful empty states with domain-language guidance and contextual CTAs) · **UX-DR3** (FcFieldPlaceholder anatomy — dashed card + warning + type + docs anchor) · **UX-DR4** (FcEmptyState anatomy — muted icon + "No {entities} yet." + CTA) · **UX-DR55** (auto-generation boundary protocol — never silently omit an unsupported type, always render FcFieldPlaceholder + build warning + dev-mode highlight) · **NFR29-34** (accessibility contract) · applies lessons **L01, L02, L04, L06, L07, L09, L10, L11**. Consumes Story 1-4 Parse-stage (`AttributeParser.ParseDisplayAttribute` extended for `Description`), Story 1-5 `ColumnModel` + `RazorModelTransform` (Unsupported columns STOP being silently dropped — the Transform stage currently `continue`s on `IsUnsupported` at line 29 of `RazorModelTransform.cs`; 4-6 REVERSES this for projections), Story 2-2 `FcShellOptions` (adds `EmptyStateSecondaryTextResourcePrefix` option), Story 3-3 `RenderContext.Mode` (dev-mode consumption hook), Story 3-5 `IFrontComposerRegistry` + bounded-context command discovery (CTA command resolution), Story 4-1 `FcProjectionEmptyPlaceholder` component SHELL (parameter list frozen + append-only per ADR-053 — 4-6 replaces the component BODY with the full CTA flow WITHOUT changing the 3 frozen parameters `ProjectionType` / `EntityPluralOverride` / `Role`; the generator call-site stays byte-identical), Story 4-1 `FcFieldPlaceholder` component (already shipped with `FieldName` + `TypeName` + `IsDevMode` parameters — 4-6 LOCALIZES the hardcoded English body + auto-injects it as a TemplateColumn when Unsupported columns appear), Story 4-3 `FcFilterEmptyState` (already ships the zero-matches filtered case — 4-6 adds NO second variant, just cross-references the distinction), Story 4-4 `FcColumnPrioritizer` unchanged (prioritizer operates on ColumnModels; Unsupported columns carry `Priority=int.MaxValue` so they sink to the overflow panel by default), Story 4-5 `[ProjectionFieldGroup]` unchanged (grouping applies to supported columns; Unsupported placeholders take the catch-all "Additional details" bucket by default).

---

## Executive summary (Feynman-level, ~45 sec)

Today three "UX completeness" surfaces ship incomplete or silently-broken: **(a)** projection empty states render a minimal `"No {entities} yet."` placeholder with no CTA (Story 4-1 ADR-053 parked the full flow here); **(b)** field descriptions authored via `[Display(Description="...")]` or `[System.ComponentModel.Description("...")]` are INVISIBLE in every generated view — the Parse stage ignores them, the emitter never renders them; **(c)** unsupported field types (`Dictionary<string,List<T>>`, custom value objects, `ReadOnlyMemory<T>`, etc.) are SILENTLY DROPPED at Transform — `RazorModelTransform.cs:29` contains `if (property.IsUnsupported) { continue; }`, directly violating **FR9** ("unsupported types never render silently") + **UX-DR55** ("never silently omit"). Story 4-6 closes all three in one scoped story:

1. **Empty-state completion.** `FcProjectionEmptyPlaceholder` keeps its frozen 3-parameter surface (ADR-053) but gains 2 APPEND-ONLY parameters (`CtaCommandName` + `SecondaryText`) and a new body (anatomy inlined directly per Challenge C3 — no separate helper component) that renders: a large muted `FluentIcon`, a `Body1` primary message "No {entities} yet.", an OPTIONAL `FluentAnchor Appearance="Accent"` CTA (semantically a navigation link, not an action — per Challenge C6) wrapped in `<AuthorizeView>` so unauthorized users see the empty-state without the CTA (per AC2.5 — pre-mortem P2 prevention), resolved through a CTA discovery chain (per-projection `[ProjectionEmptyStateCta(nameof(...))]` attribute → bounded-context creation-verb-prefix fallback ordered alphabetically among matches → null when read-only — per revised D4), an OPTIONAL `FluentLabel Typography.Body2` secondary text sourced via a hardcoded resource-key naming convention (`{ProjectionTypeFqn}_EmptyStateSecondaryText` — no `FcShellOptions` override surface in v1 per Challenge C4). Read-only projections (no available commands, no CTA attribute) render without the anchor — AC specifies this explicitly. Role-differentiated copy is a single resource-template lookup keyed by `ProjectionRole`; ActionQueue reads `EmptyStateActionQueueMessageTemplate` ("No {0} awaiting action"), all other roles fall through to `EmptyStateMessageTemplate` ("No {0} yet").

2. **Field description plumbing.** The Parse stage (`AttributeParser`) extracts description strings from BOTH `System.ComponentModel.DescriptionAttribute` (primary — the C# idiomatic location) AND `System.ComponentModel.DataAnnotations.DisplayAttribute.Description` (secondary — aligns with `[Display(Name=)]` precedent already parsed). Both paths surface on a new `PropertyModel.Description: string?` field (append-only; `Equals`/`GetHashCode` updated → IR cacheability per Story 1-4 preserved). Precedence: `[Description]` wins over `[Display(Description=)]` when both are present (matches the .NET Framework convention — `[Description]` is the older, more specific annotation). `ColumnModel.Description` flows through verbatim. The emit stage consumes it in TWO surfaces: (a) DataGrid column headers emit a `HeaderCellItemTemplate` that wraps the header text in a `FluentTooltip` when description is non-null (tooltip visible on hover + focus, `aria-describedby` wires to screen readers); (b) detail/form views (Story 4-1 DetailRecord body + Story 4-5 expand-in-row detail body via the factored `EmitDetailRecordBody`) emit a secondary `FluentLabel Typography.Caption` line BELOW the field value. The column-sort + filter affordances (Story 4-3) stay on the header surface — tooltip composes as a sibling, not a replacement.

3. **Unsupported-type honesty (the critical fix).** `RazorModelTransform.Transform` STOPS dropping Unsupported columns. Every `PropertyModel` (supported OR unsupported) becomes a `ColumnModel` — the `TypeCategory.Unsupported` branch signals emit-stage dispatch to `FcFieldPlaceholder` INSTEAD of `PropertyColumn<T,TProp>`. The emitter emits a `TemplateColumn` whose cell content is `<FcFieldPlaceholder FieldName="@nameof({Property})" TypeName="{UnsupportedTypeFqn}" IsDevMode="@(RenderContext.Mode == RenderMode.DevMode)" />`. Adopters see a dashed-border card in the column cell + a build-time HFC1002 warning (unchanged — the warning was already firing, just the runtime drop was contradicting it). `FcFieldPlaceholder`'s hardcoded English body is replaced with an `IStringLocalizer` lookup (5 new EN+FR resource keys). Dev-mode highlight (red-dashed border per UX-DR3) reads `RenderContext.Mode` — the FULL DevModeOverlay (Ctrl+Shift+D) is Epic 6 scope; 4-6 ships ONLY the parameter + CSS class wiring so Epic 6 overlay activation flips the visual without re-opening 4-6.

The design honors the "capability contract" principle: `ColumnModel` schema gains ONE append-only field (`Description`), `PropertyModel` gains ONE (`Description`), **no breaking changes to `GridViewSnapshot` / persistence blob** (none of this state persists — descriptions are build-time metadata, empty-state is a pure render decision), decision budget lands inside L06 feature-story cap (≤25; this story lands at 19/25 with a 6-decision buffer), every cross-story seam explicit per L01. Scope is deliberately narrow: **empty-state body completion, description plumbing (Parse → IR → Emit), unsupported-type honesty (stop silent drop), resource localization for `FcFieldPlaceholder`**. Dev-mode overlay activation (Ctrl+Shift+D + annotation panel per component-strategy §DevModeOverlay), `[ProjectionEmptyStateCta]` CUSTOM icon overrides (icon is Epic 6-1 annotation territory), custom per-projection empty-state Razor templates (Epic 6-2), ICU-pluralized empty-state copy ("1 order" vs "2 orders"), and dev-mode-highlight visual specimen verification are OUT of scope.

---

## Dev Agent Cheat Sheet (L11)

**Goal (one sentence):** Make three UX-completeness surfaces production-grade in one scoped story — (1) complete `FcProjectionEmptyPlaceholder` body with role-aware copy + optional CTA discovered from bounded-context commands + optional secondary text from resource-key convention; (2) plumb `[Description]` and `[Display(Description=)]` through Parse → IR → Emit so column headers get `FluentTooltip` help text and detail/form views get inline `Typography.Caption` labels; (3) flip the `RazorModelTransform.Transform` Unsupported-column drop into a full `FcFieldPlaceholder` emission so FR9 / UX-DR55 are no longer contradicted by runtime silent-omission.

**Files to create:**

| Path | Purpose |
| --- | --- |
| `src/Hexalith.FrontComposer.Contracts/Attributes/ProjectionEmptyStateCtaAttribute.cs` | New attribute — `[AttributeUsage(AttributeTargets.Class \| AttributeTargets.Struct, Inherited=false, AllowMultiple=false)] public sealed class ProjectionEmptyStateCtaAttribute(string CommandTypeName)`. Non-empty `CommandTypeName` required (throws `ArgumentException` on null/empty). Used by the empty-state CTA discovery chain (D6): explicit opt-in overrides the bounded-context fallback. Surfaces on `DomainModel.EmptyStateCtaCommandTypeName: string?`. Stays Contracts-layer string-based (NOT `Type` typeof-arg) to avoid the Roslyn `ITypeOfOperation` boundary mess Story 2-2 D3 flagged. |
| ~~`src/Hexalith.FrontComposer.Shell/Components/Rendering/FcEmptyStateBody.razor` + `.razor.cs`~~ | **REMOVED per spec revision (Challenge C3).** Anatomy is INLINED directly in `FcProjectionEmptyPlaceholder.razor` body — no separate helper component. Rationale: YAGNI (single caller, premature abstraction); avoids creating an internal-API surface that would attract param growth. The full anatomy (FluentIcon 48px muted + primary message + optional CTA FluentAnchor + optional secondary FluentLabel + role="status" outer wrapper) lives directly in `FcProjectionEmptyPlaceholder.razor`. |
| `src/Hexalith.FrontComposer.Shell/Services/IEmptyStateCtaResolver.cs` + `EmptyStateCtaResolver.cs` | Service — resolves the CTA `(CommandTypeFqn, CommandDisplayName, CommandRoute)` tuple OR `null` for a given `ProjectionType`. Discovery chain per D6: (1) read `[ProjectionEmptyStateCta]` attribute → look up command in `IFrontComposerRegistry`; (2) if absent, look up bounded-context via `[BoundedContext]` → return first command with `HasWriteAccess==true`; (3) if no writable commands in BC (read-only projection), return `null`. Hand-written service + injected `IFrontComposerRegistry` — L05 hand-written + registry-driven pattern. |
| `src/Hexalith.FrontComposer.Shell/wwwroot/css/fc-empty-state.scoped.css` | Scoped CSS adding `.fc-empty-state-body { display: flex; flex-direction: column; align-items: center; gap: 16px; padding: 48px 24px; text-align: center; }`, `.fc-empty-state-body__icon { opacity: 0.5; }`, `.fc-empty-state-body__cta { margin-top: 8px; }`, `@media (max-width: 767px) { .fc-empty-state-body { padding: 32px 16px; gap: 12px; } .fc-empty-state-body__icon svg { width: 32px; height: 32px; } }` (UX-responsive-design row "Stacked vertical layout" on phone). Dev-mode highlight rule for `FcFieldPlaceholder`: `.fc-field-placeholder-dev { border: 2px dashed var(--palette-red-70, #d13438) !important; }`. |
| `tests/Hexalith.FrontComposer.Contracts.Tests/Attributes/ProjectionEmptyStateCtaAttributeTests.cs` | (1) `[ProjectionEmptyStateCta("IncrementCounterCommand")]` compiles at class AND struct level; `CommandTypeName` readable; (2) empty/whitespace throws `ArgumentException`; (3) AttributeUsage rejects property-level application (reflection assertion). 3 tests. |
| ~~`tests/Hexalith.FrontComposer.Shell.Tests/Components/Rendering/FcEmptyStateBodyTests.cs`~~ | **REMOVED per spec revision (Challenge C3 — `FcEmptyStateBody` helper not created).** Anatomy coverage (icon presence, role=status, aria-label, FluentAnchor presence/absence, secondary FluentLabel) merges into `FcProjectionEmptyPlaceholderTests.cs`. |
| `tests/Hexalith.FrontComposer.Shell.Tests/Components/Rendering/FcProjectionEmptyPlaceholderTests.cs` | bUnit: (1) read-only projection (resolver returns null) renders without CTA anchor; (2) projection with bounded-context commands renders CTA with first creation-verb-prefix-matching command (per revised D4); (3) `[ProjectionEmptyStateCta(nameof(...))]` attribute overrides BC fallback (command is NOT first-in-BC); (4) ActionQueue role reads `EmptyStateActionQueueMessageTemplate`, Default reads `EmptyStateMessageTemplate`; (5) `EntityPluralOverride` parameter wins over auto-humanized name; (6) secondary text resource lookup via `{ProjectionFqn}_EmptyStateSecondaryText` — present → renders; absent → null (no empty FluentLabel); (7) `Role = null` falls through to Default template (legacy unannotated projections); (8) anatomy: outer div has `role="status"` + correct `aria-label`; 48px FluentIcon present; primary message renders; (9) CTA renders as `FluentAnchor Appearance="Accent"` (NOT FluentButton) with `Href` set; (10) **AC2.5 — unauthorized user (AuthorizeView policy fails) sees no CTA anchor in DOM** even when resolver returns a tuple. **10 tests** (7 original + 2 anatomy + 1 auth-gate). |
| `tests/Hexalith.FrontComposer.Shell.Tests/Components/Rendering/FcFieldPlaceholderLocalizationTests.cs` | bUnit: (1) EN locale — hardcoded English replaced with `IStringLocalizer` lookup (`FieldPlaceholderMessageTemplate`, `FieldPlaceholderAriaLabelTemplate`, `FieldPlaceholderDocsLinkText`, `FieldPlaceholderTypeAnnotationTemplate`) — verify each key lands; (2) FR locale — French translations present; (3) `IsDevMode=true` applies `fc-field-placeholder-dev` CSS class (red-dashed highlight per UX-DR3); (4) `role="status"` + `tabindex="0"` + `aria-label` with localized template; (5) FluentAnchor link text is localized. 5 tests. |
| `tests/Hexalith.FrontComposer.Shell.Tests/Services/EmptyStateCtaResolverTests.cs` | xUnit: (1) `[ProjectionEmptyStateCta]` attribute present → returns attribute-named command; (2) no attribute, 1 writable command in BC → returns that command (regardless of verb prefix); (3) **no attribute, BC `["AddOrderItem", "CancelOrder", "CreateOrder", "ShipOrder"]` → returns `CreateOrder` (creation-verb-prefix `Create` match wins; alphabetical among verb-prefix matches; non-prefix commands like `CancelOrder`/`ShipOrder` excluded from primary bucket)** — verifies revised D4 ordering; (4) **no attribute, BC has zero verb-prefix matches `["ApproveOrder", "CancelOrder"]` → returns first alphabetically (`ApproveOrder`) as fall-through** — verifies non-creation-prefix fallback; (5) read-only BC (zero writable commands) → returns null; (6) projection with no `[BoundedContext]` → returns null; (7) `[ProjectionEmptyStateCta]` names unknown command → returns null + logs Warning (fail-soft); (8) registry returns null registry → resolver returns null (defensive for early-circuit-init case). **8 tests** (7 original + 1 verb-prefix-fallback). |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Parsing/DescriptionAttributeParserTests.cs` | (1) `[Description("text")]` on property → `PropertyModel.Description == "text"`; (2) `[Display(Description="text")]` on property → same; (3) BOTH present → `[Description]` wins (precedence per D2); (4) whitespace-only Description → treated as null (trim); (5) absent → null; (6) `[Description]` on projection TYPE → ignored (property-level only in v1); (7) Description + Name both set on `[Display]` → both parsed independently (Name flows to Header, Description flows to Description). 7 tests. |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Transforms/UnsupportedColumnEmissionTests.cs` | Verify/approval: (1) a projection with `Dictionary<string, List<Tag>> Metadata` produces a `ColumnModel` (NOT dropped — regression gate for the `RazorModelTransform.cs:29` flip) with `TypeCategory.Unsupported` + `Header="Metadata"`; (2) projection with mix of supported + unsupported properties preserves declaration order (`Id` / `Status` / `Metadata` → 3 ColumnModels, not 2); (3) `CounterProjectionApprovalTests` stays green (Counter has no unsupported properties — zero diff); (4) Story 4-4 `ColumnPriority` sort with unsupported column assigns `Priority=null` to unsupported → sinks to `int.MaxValue` slot (stable sort determinism preserved); (5) Unsupported + `[ProjectionFieldGroup("Metadata")]` (Story 4-5 D9) still buckets into the named group (FieldGroup flows through verbatim, independent of TypeCategory). 5 tests. |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/FcFieldPlaceholderColumnEmissionTests.cs` | Verify/approval: (1) Unsupported column emits a `TemplateColumn` (not `PropertyColumn`) with `FcFieldPlaceholder` inside; (2) emitted component carries `FieldName="@nameof({Type}.{Property})"` + `TypeName="{UnsupportedTypeFqn}"` + `IsDevMode="@(RenderContext.Mode == RenderMode.DevMode)"`; (3) column header still emits `Title=` with the resolved header text (unsupported columns still have Display names); (4) when ALL columns are unsupported (pathological case — projection type is purely `Dictionary<T>` fields), the emitter still emits the grid with N placeholder columns + no supported data columns (does NOT silently collapse to empty state); (5) Unsupported column participates in `FcColumnPrioritizer` (4-4) — overflow behavior unchanged; (6) Unsupported column does NOT participate in `FcColumnFilterCell` (4-3) — `SupportsFilter` is already false for `TypeCategory.Unsupported` per 4-3 D14 (regression verified). 6 tests. |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/DescriptionTooltipEmissionTests.cs` | Verify/approval: (1) a projection with `[Description("Order number for audit trail")] public string OrderId { get; init; }` emits a column whose header content includes the header text + an inline `<FluentIcon Value="@(new Icons.Regular.Size16.Info())">` wrapped in `<FluentTooltip Anchor="...">`, with `<span id="{viewKey}-OrderId-desc">Order number for audit trail</span>` as the tooltip content (per revised D9 — icon-anchored, NOT header-text-anchored); the header `<th>` carries `aria-describedby="{viewKey}-OrderId-desc"`; (2) column WITHOUT description emits the pre-4-6 `Title="..."` header attribute unchanged + NO info icon (regression gate — `CounterProjectionApprovalTests` green); (3) `EmitDetailRecordBody` (4-1 DetailRecord + 4-5 expand-in-row host via the factored helper) emits a `FluentLabel Typography.Caption` secondary line when description is non-null, positioned below the field value, with `class="fc-field-description"`; (4) description text passes through XML-escape (apostrophes, angle brackets, ampersands) — no raw HTML injection; (5) descriptions survive the `RazorModel.Equals` round-trip (IR cacheability); (6) `[Display(Description=)]` description flows identically to `[Description]` in emit (precedence already applied at Parse). **6 tests** (was 7; test-item 7 "form view emits description as `FluentTextField.Hint`" REMOVED to align with the existing Known Gap "Description on COMMAND form inputs — Epic 6 backlog" — per First Principles finding, the test was scope-ambiguous against the explicit deferral). |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Transforms/DescriptionTransformTests.cs` | Transform-side: (1) `PropertyModel.Description` flows verbatim to `ColumnModel.Description`; (2) null Description flows as null (no coercion to empty string); (3) description on unsupported column flows through (placeholder header can still carry tooltip); (4) equality: two `ColumnModel`s with same Description are equal; (5) equality: differing Description makes them unequal. 5 tests. |
| `tests/Hexalith.FrontComposer.Shell.Tests/Resources/FcFieldPlaceholderLocalizationTests.cs` | (Resource parity — auto-discovers new keys; no code change needed; adds +5 keys to the count assertion if hardened.) |

**Files to extend:**

| Path | Change |
| --- | --- |
| `src/Hexalith.FrontComposer.SourceTools/Parsing/AttributeParser.cs` | (a) Add `ParseDescriptionAttribute(IPropertySymbol)` — reads `System.ComponentModel.DescriptionAttribute.Description` (positional constructor argument); returns `string?` (null when absent or whitespace-only). (b) Extend the existing `ParseDisplayAttribute(IPropertySymbol)` overload to ALSO return the `Description` named argument (new out-param `out string? description` OR a second overload returning a `(string? name, string? description)` tuple — picked to minimize call-site churn). (c) Wire both into `MapProperty` at the existing `ParseDisplayAttribute(propertySymbol)` call-site (line ≈530); apply D2 precedence: `[Description]` wins over `[Display(Description=)]`. (d) The new parse operations are Roslyn-symbol-only (no `ITypeOfOperation` / `Compilation` references) — Story 1-4 IR-cacheability discipline preserved. |
| `src/Hexalith.FrontComposer.SourceTools/Parsing/DomainModel.cs` | (a) `PropertyModel` gains `string? Description` field (append-only ctor param at tail; existing call sites default to `null`; `Equals` / `GetHashCode` updated per IR cacheability discipline — Story 1-4 contract preserved). (b) `DomainModel` gains `string? EmptyStateCtaCommandTypeName` field (append-only; populated from `[ProjectionEmptyStateCta]` parsing — see new attribute above). |
| `src/Hexalith.FrontComposer.SourceTools/Transforms/ColumnModel.cs` | `ColumnModel` gains `string? Description` (append-only ctor param at tail; `Equals` / `GetHashCode` updated). Flows from `PropertyModel.Description` verbatim. |
| `src/Hexalith.FrontComposer.SourceTools/Transforms/RazorModelTransform.cs` | **CRITICAL CHANGE.** Replace the `if (property.IsUnsupported) { continue; }` skip at line 29 with: always map to `ColumnModel`, let `TypeCategory.Unsupported` signal emit-stage placeholder dispatch. Header resolution unchanged (Unsupported columns still have a display header via `[Display(Name=)]` or humanized property name). `MapTypeCategory` already returns `TypeCategory.Unsupported` for unknown type names (line 293). `FormatHint` for Unsupported: returns `null` (no format rendering — the placeholder owns the cell). Description flows through verbatim via `property.Description`. Priority flows unchanged (Unsupported columns carry `null` priority → sink to int.MaxValue via Story 4-4 D17 stable sort). |
| `src/Hexalith.FrontComposer.SourceTools/Emitters/ProjectionRoleBodyEmitter.cs` | (a) Header emission (`EmitColumnHeader` helper or inline in `EmitPropertyColumn`) — when `column.Description` is non-null, wrap the header text in `<FluentTooltip>` (the tooltip provides the description content on hover/focus, `aria-describedby` correlates the header cell to the tooltip element); when null, emit the pre-4-6 `Title=` attribute unchanged (regression gate). (b) Unsupported-column emit path — instead of `PropertyColumn<T,TProp>`, emit `TemplateColumn<T>` whose `ChildContent` opens `FcFieldPlaceholder` with `FieldName`, `TypeName` (the `UnsupportedTypeFullyQualifiedName` from `PropertyModel`), `IsDevMode="@(RenderContext.Mode == RenderMode.DevMode)"`. (c) `EmitDetailRecordBody` (factored helper per Story 4-5 D10) — BELOW each rendered field, emit `@if (!string.IsNullOrEmpty(column.Description)) { <FluentLabel Typography="Typography.Caption" class="fc-field-description">@column.Description</FluentLabel> }`. |
| `src/Hexalith.FrontComposer.SourceTools/Emitters/RazorEmitter.cs` | Empty-shell emission — the existing `FcProjectionEmptyPlaceholder` call-site (line ≈407) gains TWO new attribute emissions: `builder.AddAttribute(seq++, "CtaCommandName", "{EmptyStateCtaCommandTypeName}")` (null-literal when the DomainModel field is null) + `builder.AddAttribute(seq++, "SecondaryText", null)` (always null from generator — the component's resource-lookup convention handles override; emitting the attribute preserves parameter-surface determinism for the bUnit roundtrip tests). NO other emission-path changes at the generator level — empty-state COMPOSITION stays in the Shell component per ADR-053. |
| `src/Hexalith.FrontComposer.Shell/Components/Rendering/FcProjectionEmptyPlaceholder.razor` + `.razor.cs` | REPLACE BODY per ADR-053. Parameters: KEEP frozen `ProjectionType` / `EntityPluralOverride` / `Role` (3 params); ADD `[Parameter] public string? CtaCommandName { get; set; }` + `[Parameter] public string? SecondaryText { get; set; }` (2 new APPEND-ONLY params). Inject `IEmptyStateCtaResolver` + `IStringLocalizer<FcShellResources>`. **Body INLINES the full anatomy directly** (no `FcEmptyStateBody` helper per Challenge C3): outer `<div role="status" aria-label="..." class="fc-empty-state-body">` wrapping (a) a 48px muted `FluentIcon`, (b) `FluentLabel Typography.Body1` primary message, (c) optional `<AuthorizeView Policy="@_ctaPolicy">` containing `FluentAnchor Appearance="Accent" Href="@_ctaHref">@_ctaLabel</FluentAnchor>` (per AC1, AC2.5 — anchor not button; auth-gated), (d) optional `FluentLabel Typography.Body2 class="fc-empty-state-body__secondary"` secondary text. Primary-message template resolution: (`Role == ProjectionRole.ActionQueue`) → `EmptyStateActionQueueMessageTemplate`; else → `EmptyStateMessageTemplate`. Secondary-text resolution chain: (1) explicit `SecondaryText` parameter; (2) `IStringLocalizer["{ProjectionType.FullName}_EmptyStateSecondaryText"]` — when the key exists AND value !== key (Microsoft convention for "missing key" is to return the key itself); (3) null (no secondary line). CTA resolution chain (only runs when `CtaCommandName` parameter is null — null means "use resolver"; non-null means "attribute-specified, look up directly"): `_ctaResolver.Resolve(ProjectionType) -> (commandFqn, commandLabel, commandRoute)?` — **synchronous per revised D5**. When null → render without CTA anchor (read-only projection); when non-null but `<AuthorizeView>` policy fails → CTA collapses (read-only-to-this-user). |
| `src/Hexalith.FrontComposer.Shell/Components/Rendering/FcFieldPlaceholder.razor` + `.razor.cs` | (a) Split `.razor` inline `@code` into `.razor.cs` (follow 4-1 `FcProjectionEmptyPlaceholder` split precedent). (b) Remove hardcoded English ("This field requires a custom renderer." + "Learn how to customize this field"); inject `IStringLocalizer<FcShellResources>`; render with `FieldPlaceholderMessageTemplate` / `FieldPlaceholderAriaLabelTemplate` / `FieldPlaceholderDocsLinkText` / `FieldPlaceholderTypeAnnotationTemplate`. (c) Keep `FieldName` / `TypeName` / `IsDevMode` parameter surface unchanged (frozen — existing generator call-site in command forms per 4-2 / 2-1). (d) Add CSS class `fc-field-placeholder-dev` when `IsDevMode=true` (red-dashed override per UX-DR3 + component-strategy §FcFieldPlaceholder). (e) Docs link URL centralized as a constant `FcFieldPlaceholder.DocsLink = "https://hexalith.github.io/FrontComposer/diagnostics/HFC1002"` — matches the HFC1002 DocsLink emitted in the Parse-stage warning (consistent help). |
| `src/Hexalith.FrontComposer.Shell/Extensions/ServiceCollectionExtensions.cs` | Register `IEmptyStateCtaResolver` → `EmptyStateCtaResolver` as `Scoped`. Piggybacks on the existing `AddFluentUIFrontComposer` chain. |
| ~~`src/Hexalith.FrontComposer.Shell/Options/FcShellOptions.cs`~~ | **REMOVED per spec revision (Challenge C4).** No `EmptyStateSecondaryTextResourcePrefix` option in v1 — convention prefix `{ProjectionTypeFqn}_EmptyStateSecondaryText` is hardcoded. When a real adopter need surfaces, add the option in a one-story scope. `FcShellOptions` is unchanged by 4-6. |
| `src/Hexalith.FrontComposer.Shell/Resources/FcShellResources.resx` + `.fr.resx` | Add **11 new EN+FR resource keys** (D8): `EmptyStateMessageTemplate` ("No {0} yet." / "Aucun {0} pour le moment."), `EmptyStateActionQueueMessageTemplate` ("No {0} awaiting action." / "Aucun {0} en attente d'action."), `EmptyStateCtaTemplate` ("Send your first {0}" / "Envoyer votre premier {0}"), `EmptyStateAriaLabelTemplate` ("No {0} found" / "Aucun {0} trouvé"), `FieldPlaceholderMessageTemplate` ("This field requires a custom renderer." / "Ce champ nécessite un rendu personnalisé."), `FieldPlaceholderAriaLabelTemplate` ("{0} requires a custom renderer" / "{0} nécessite un rendu personnalisé"), `FieldPlaceholderTypeAnnotationTemplate` ("({0})" / "({0})" — parens around TypeName, same EN/FR), `FieldPlaceholderDocsLinkText` ("Learn how to customize this field" / "Apprendre à personnaliser ce champ"), `FieldDescriptionTooltipAriaSuffix` ("— help text" / "— texte d'aide" — appended to column-header aria-description for screen readers), `ColumnHeaderWithDescriptionAriaTemplate` ("{0}, {1}" / "{0}, {1}" — column header + description interpolation for combined aria-label; colon-free so no French U+00A0 NBSP), `UnsupportedColumnHeaderSuffix` ("(custom renderer needed)" / "(rendu personnalisé requis)"). **Current inventory ≈ 98 per locale → ≈ 109 per locale after 4-5 + 4-6 land (4-5 adds 7, 4-6 adds 11 = 18 delta; 4-5 and 4-6 can merge independently so actual resting count depends on merge order).** |
| `tests/Hexalith.FrontComposer.Shell.Tests/Resources/CanonicalKeysHaveFrenchCounterpartsTests.cs` | **No code change needed** — the test auto-discovers all keys and asserts EN/FR parity. Resource-key addition alone is sufficient. |

**Generated naming rules (L04 preserved):**

- Generator hint prefix unchanged: `{Namespace}.{TypeName}.g.razor.cs`.
- Emitted class name unchanged: `{TypeName}View : ComponentBase, IDisposable`.
- `FcProjectionEmptyPlaceholder` generator call-site: parameter surface unchanged (ProjectionType / Role / EntityPluralOverride still required per ADR-053). Two new attributes emit at seq+4, seq+5 per the RazorEmitter change above — hint-file byte-stability is preserved IF the generator's empty-shell emission was already one-attribute-per-line (verify V2 below).
- `FcFieldPlaceholder` column-cell emission: new `TemplateColumn` wrapper around the existing component — CSS class emitted on the `<td>` is `fc-field-placeholder-column` (new) so the Story 4-4 `FcColumnPrioritizer` can identify placeholders in the overflow panel if needed (defensive — 4-4 already handles all TypeCategory values uniformly, no special-case code needed in 4-4).
- Column-header description rendering: header text + small inline `FluentIcon Icon="@Icons.Regular.Size16.Info"` (icon wrapped in `FluentTooltip`, NOT the full header — per revised D9). Both elements live inside the `HeaderCellItemTemplate` slot on `PropertyColumn` / `TemplateColumn`. Fluent v5 confirms the parameter name via Fluent UI MCP (spike required in T2.6 to verify parameter name against Fluent `TemplateColumn` shape AND to verify the icon does not intercept column-resize drag events — the spike mirrors Story 4-5 T2.1's approach, reusing the same MCP lookup tooling).

**AC quick index** (one-liner each):

| AC | Intent |
| --- | --- |
| **AC1** | Zero-total empty state — `FcProjectionEmptyPlaceholder` renders 48px muted `FluentIcon` + primary `"No {entities} yet."` message + optional CTA `FluentAnchor Appearance="Accent"` `"Send your first {CommandName}"` (wrapped in `<AuthorizeView>` for AC2.5 auth-gating) when a writable creation-verb-prefix command exists in the projection's bounded context OR `[ProjectionEmptyStateCta(nameof(...))]` resolves; `role="status"` + `aria-label="No {entities} found"`; CTA keyboard-focusable. |
| **AC2** | Read-only projection — when `IEmptyStateCtaResolver.Resolve(...) == null` AND no `[ProjectionEmptyStateCta]`, renders the empty-state WITHOUT the CTA button (the only visible element is icon + message + optional secondary). |
| **AC3** | Empty filtered state — **DISTINCT** from zero-total empty state. Story 4-3's `FcFilterEmptyState` handles this case; 4-6 ships NO new component for filter-empty. 4-6's emission path guarantees zero-total renders `FcProjectionEmptyPlaceholder` (4-6 body); filter-empty is 4-3's `FcFilterEmptyState` (unchanged). Regression test asserts: zero-total (no filters active, zero items) → 4-6 component; filter-active + zero-matches → 4-3 component. |
| **AC4** | Field description on DataGrid column header — `[Description("text")]` OR `[Display(Description="text")]` on a projection property emits a `FluentTooltip` wrapping the column header text; tooltip visible on hover AND on keyboard focus (Fluent default); screen reader receives description via `aria-describedby` correlated to the tooltip element. Column header WITHOUT description emits the pre-4-6 `Title=` attribute unchanged (regression gate). |
| **AC5** | Field description on detail/form views — in DetailRecord-role views (Story 4-1) AND in expand-in-row detail (Story 4-5 via factored `EmitDetailRecordBody`), a `FluentLabel Typography.Caption` renders below each field value when description is non-null. Text passes through XML-escape. Attribute precedence: `[Description]` wins over `[Display(Description=)]` when both present. |
| **AC6** | Unsupported field types — `Dictionary<string,List<T>>` / custom value objects / `ReadOnlyMemory<T>` / any type not in the `MapTypeCategory` supported list (line 286-294 of `RazorModelTransform.cs`) produces a `ColumnModel` with `TypeCategory.Unsupported` — **NOT dropped**. The emitter emits a `TemplateColumn` whose cell content is `<FcFieldPlaceholder FieldName TypeName IsDevMode />` rendering a dashed-border card with warning icon + field name + type annotation + message + docs link. Build-time warning HFC1002 (unchanged) fires during compilation. Zero silent omissions — every property that appears on the `[Projection]` surface appears as either a data column OR a placeholder column. |
| **AC7** | Unsupported-field accessibility — placeholder column `role="status"` + `aria-label="{FieldName} requires a custom renderer"` + focusable in tab order (tabindex=0) + keyboard-accessible docs link. When `RenderContext.Mode == RenderMode.DevMode`, the `fc-field-placeholder-dev` CSS class applies a red-dashed border (UX-DR3 dev-mode highlight). Dev-mode overlay panel (Ctrl+Shift+D) is Epic 6 scope — 4-6 ships ONLY the CSS class wiring + parameter plumbing. |
| **AC8** | Localization — `FcFieldPlaceholder` body (previously hardcoded English) routes through `IStringLocalizer<FcShellResources>` for message / ARIA / type-annotation / docs-link-text. French resource parity verified by existing `CanonicalKeysHaveFrenchCounterpartsTests`. All 11 new keys use `{0}` interpolation only (no colons → no French NBSP concerns). |
| **AC2.5** | Auth-gated CTA — when the resolved CTA command's authorization policy fails for the current user, the CTA `FluentAnchor` is not rendered (collapsed by `<AuthorizeView>`); the rest of the empty-state still renders. Prevents the "see button → click → 403" failure mode. |

**Binding contract(s) with predecessor stories:**

- **Story 1-4/1-5 pipeline** frozen: Parse → Transform → Emit reachable only through pure data types; 4-6 extends `PropertyModel` + `ColumnModel` + `DomainModel` with append-only fields. IR byte-stability preserved for unchanged types (CounterProjection has no `[Description]` and no unsupported columns → zero-diff regression gate).
- **Story 2-1/2-2 command form** contract: `FcFieldPlaceholder` is already consumed by `CommandFormEmitter.EmitPlaceholder` for unsupported COMMAND properties (line 577-581 of `CommandFormEmitter.cs`); 4-6's localization + parameter-surface changes are fully backward-compatible with the command-form call-site (existing call `__b.OpenComponent<FcFieldPlaceholder>(cseq++)` + `FieldName` + `TypeName` attributes stays green; `IsDevMode` defaults to `false` when not passed).
- **Story 3-5 contract** consumed: `IFrontComposerRegistry` reads the bounded-context → commands map; `EmptyStateCtaResolver` calls `registry.GetCommandsForBoundedContext(bcId)` and filters for `HasWriteAccess==true` (method already exists per 3-5 `IFrontComposerRegistry` surface).
- **Story 4-1 contract** consumed unchanged: `FcProjectionEmptyPlaceholder` parameter surface (`ProjectionType` / `EntityPluralOverride` / `Role`) frozen per ADR-053 — 4-6 adds 2 APPEND-ONLY parameters (`CtaCommandName`, `SecondaryText`) and replaces the body; generator emission path unchanged except for 2 new attribute lines.
- **Story 4-1 contract** EXTENDED: `EmitDetailRecordBody` (already factored by 4-5 D10) gains a description-emit step between the field value and the accordion close — the 4-5 factoring stays green (no parameter change, just additional emit inside the same call).
- **Story 4-3 contract** unchanged: `FcFilterEmptyState` (filter-active-zero-matches) is NOT replaced; 4-6's empty state is zero-TOTAL only. `SupportsFilter` on `ColumnModel` already returns false for `TypeCategory.Unsupported` (per 4-3 D14); no filter-affordance surface regression.
- **Story 4-4 contract** unchanged: `FcColumnPrioritizer` operates on `ColumnModel` list; Unsupported columns carry `Priority=null` → sink to `int.MaxValue` via the 4-4 D17 stable sort. No special-case code; regression verified.
- **Story 4-5 contract** unchanged: `[ProjectionFieldGroup]` flows through verbatim for Unsupported columns (grouping is orthogonal to TypeCategory); Unsupported columns without `[ProjectionFieldGroup]` bucket into the "Additional details" catch-all accordion unchanged.

**Scope guardrails (what NOT to implement in 4-6):**

- ❌ **Dev-mode overlay activation + panel** (component-strategy §FcDevModeOverlay) — Ctrl+Shift+D keyboard hook, right-side drawer with convention annotations, copy-starter-template buttons. **Epic 6-5 "FcDevModeOverlay & starter template generator"** owns. 4-6 ships ONLY the `IsDevMode` CSS-class binding + the `fc-field-placeholder-dev` red-dashed-border rule; Epic 6 flips the mode.
- ❌ **Custom empty-state icons per projection** (domain-contextual icons like a shopping cart for Orders). **Epic 6-1 "Level 1 annotation overrides"** owns via a future `[ProjectionEmptyStateIcon(IconName)]` attribute. 4-6 ships the single muted `DocumentSearch` icon inherited from Story 4-1's FcProjectionEmptyPlaceholder — adopters who want domain icons take Epic 6-1.
- ❌ **Per-projection custom empty-state templates** (adopter-written Razor replacements). **Epic 6-2 "Level 2 typed Razor template overrides"** owns via the gradient's `TemplateOverride<FcProjectionEmptyPlaceholder>` pattern.
- ❌ **Slot-level empty-state field replacement** (adopter replaces just the CTA button, keeps the framework's icon + message). **Epic 6-3 "Level 3 slot-level field replacement"** owns.
- ❌ **ICU-pluralized empty-state copy** — `"No 1 order"` vs `"No 2 orders"` grammar. **Epic 9-5 "Diataxis documentation site" ICU migration** owns. 4-6 ships the simple `{0}` templates (adopters see "No orders yet." which is grammatical for both singular and plural-of-zero in English and French).
- ❌ **Dev-mode highlight visual specimen verification** — the red-dashed border at the pixel level needs visual-regression Playwright/axe. **Story 10-2 "Accessibility CI gates and visual specimen verification"** owns.
- ❌ **Adopter-provided `[FieldDescription]` custom attribute** — 4-6 uses the TWO standard .NET attributes (`[Description]` + `[Display(Description=)]`). A framework-branded attribute is a consistency drift; rejected because adopters already know the .NET conventions.
- ❌ **Field description on command form inputs** (applying descriptions from `[Description]` to `FluentTextField.Hint` / `FluentNumberField.Hint`). Story 2-1's command-form emit path is adjacent but 4-6 is scoped to PROJECTION views. A follow-up "description-on-forms" story lives in the Epic 6 backlog.
- ❌ **Boolean `SupportsDescription` on `ColumnModel`** — the `Description` field is nullable; non-null IS the "has description" signal. No second gate.
- ❌ **Runtime description editing** (adopter changes description at runtime). Attributes are compile-time; runtime dynamism is out of scope for v1. Epic 8 MCP + Epic 9 CLI are the future routes.
- ❌ **`[ProjectionEmptyStateCta]` with icon parameter** — v1 ships `CommandTypeName` only. Icon + tooltip + secondary-button-color are Epic 6-1 annotation territory.
- ❌ **Separate empty-state variants per RenderContext.Mode** (dev-mode empty-state shows different copy). Empty-state copy is projection-semantic, not environment-semantic.

**Start-here task order:** T1 → T2 → T3 → T4 → T5 → T6 → T7 (sequential; T7 French parity runs last after all keys land).

**Test count expectation:** target ≈ **55 tests** (was 58 pre-revision; net change: −7 dropping `FcEmptyStateBodyTests` per Challenge C3, +3 adding anatomy + FluentAnchor checks to `FcProjectionEmptyPlaceholderTests`, +1 AC2.5 auth-gate test, +1 D4 verb-prefix-fallback test in `EmptyStateCtaResolverTests`, −1 dropping form-view `FluentTextField.Hint` test from `DescriptionTooltipEmissionTests` per First Principles alignment with Known Gap). See D12 for the revised per-suite breakdown. **Well under L11's 80-test soft trigger.** Feature-story decision budget: 19/25 (L06 — 6-decision buffer; revisions extended existing decisions D4/D5/D6/D9/D15/D17 in place rather than adding new ones, so budget unchanged).

---

## Story

As a business user,
I want helpful empty states that guide me toward action, field descriptions that explain what columns mean, and clear indicators for any fields the framework can't auto-render,
so that I'm never confused by blank screens, cryptic column names, or missing data.

**Business value:** Until 4-6 lands, three papercut UX failures degrade every auto-generated projection view:

1. **Silent omission.** A developer who models an order with `public Dictionary<string, LineItem> Items { get; init; }` sees the column DISAPPEAR from the generated DataGrid — no warning in the browser, no runtime hint, just missing data. The build emits HFC1002 but the busy developer doesn't read generator warnings. The business user sees an order with zero line items. The operator dismisses the data as "probably fine." Silent data loss is the cardinal sin of auto-generation (UX-DR55). 4-6 makes omission IMPOSSIBLE: every property appears either as a data column or as a visually-loud dashed-border card.

2. **Cryptic column names.** A developer names a property `Sla` (for "Service Level Agreement"); the business user sees a column labeled "Sla" with no context. A `[Description("Service level commitment in business days, contractually defined per customer tier")]` annotation would solve it — but 4-6 is the first story to actually surface the description. Today the attribute is ignored by Parse. After 4-6, hovering the column header reveals the description as a `FluentTooltip`; screen readers announce it via `aria-describedby`. Zero-cost documentation.

3. **Blank screens.** A first-time user opens the Orders page. Zero orders exist yet. The current 4-1 placeholder renders a single grey icon and `"No orders yet."` — no guidance, no CTA. The user closes the tab. After 4-6, the same user sees the full anatomy: icon + message + `"Send your first Create Order"` button that navigates directly to the command form. First-visit friction → first-visit action.

This story is the "capstone of Epic 4": Stories 4-1 through 4-5 shipped DataGrid rendering, badges, filter/sort, virtualization, and expand-in-row; 4-6 polishes the three quality-of-life surfaces that would otherwise haunt v1 adopters for months.

---

## Cross-story contract table (L01)

| Contract | Producer | Consumer | Binding |
|---|---|---|---|
| `ProjectionEmptyStateCtaAttribute(string CommandTypeName)` (new Contracts type, `AttributeTargets.Class \| AttributeTargets.Struct`, `Inherited=false`, `AllowMultiple=false`) | 4-6 | `AttributeParser.ParseProjectionAttribute` → `DomainModel.EmptyStateCtaCommandTypeName` → `FcProjectionEmptyPlaceholder.CtaCommandName` attribute (emitted by the generator) → `IEmptyStateCtaResolver.Resolve(ProjectionType)` (at runtime) | Frozen at v1: non-empty `CommandTypeName` required; precedence over bounded-context fallback (D6); future `[ProjectionEmptyStateCta(string, Icon)]` overloads are append-only positional-parameter additions. |
| `PropertyModel.Description: string?` + `ColumnModel.Description: string?` (IR extensions) | 4-6 | `ProjectionRoleBodyEmitter.EmitColumnHeader` (tooltip wrapping); `EmitDetailRecordBody` factored helper (inline Typography.Caption); Story 4-1 + 4-5 detail surfaces share via the 4-5 D10 factoring | Frozen at v1: non-null Description triggers tooltip + caption emission; null passes through as pre-4-6 behavior (regression gate). IR cacheability preserved (append-only ctor param at tail of both models). |
| `DomainModel.EmptyStateCtaCommandTypeName: string?` (IR extension) | 4-6 | `RazorEmitter` empty-shell emission path (emits as `CtaCommandName` attribute) | Append-only; defaults to null; null emits as C# null-literal in the generated view (component resolver falls through to BC discovery). |
| `IEmptyStateCtaResolver` (new Shell service) + `EmptyStateCtaResolver` (implementation) | 4-6 | `FcProjectionEmptyPlaceholder.OnInitialized` — resolves the CTA tuple when `CtaCommandName` parameter is null; SKIPPED when non-null (attribute-driven explicit path) | Frozen at v1: interface shape is **synchronous** `EmptyStateCtaTuple? Resolve(Type projectionType)` (per revised D5 — async overload added in Epic 5 when actual I/O exists; **the v1 sync `Resolve` stays supported alongside async permanently — binary-compat contract preventing breakage of adopter custom resolvers**); tuple shape is `(string CommandFqn, string CommandDisplayName, string CommandRoute, string? AuthorizationPolicy)` (note `AuthorizationPolicy` is `string?` per Occam refinement — null means "no policy, AuthorizeView wraps without Policy attribute"; AC2.5 `<AuthorizeView>` binding). Read-only projections (no writable commands in BC, no `[ProjectionEmptyStateCta]`) return null → CTA NOT rendered. Discovery chain orders writable commands by creation-verb prefix (per revised D4) before alphabetical fallback. |
| ~~`FcEmptyStateBody` component~~ | ~~4-6~~ | ~~`FcProjectionEmptyPlaceholder` (body delegation)~~ | **REMOVED per spec revision (Challenge C3).** Anatomy is inlined directly into `FcProjectionEmptyPlaceholder` body. No internal-API surface exposed. |
| `FcProjectionEmptyPlaceholder` parameter surface (Story 4-1 ADR-053 frozen parameters + 2 new append-only) | 4-1 (shell) / 4-6 (body) | Generator emission (`RazorEmitter` empty-shell path) | Story 4-1 ADR-053 discipline preserved: `ProjectionType`, `EntityPluralOverride`, `Role` stay frozen in name + order + type. 4-6 appends `CtaCommandName`, `SecondaryText` at tail. Generator emits each attribute on its own line (byte-stable hint-file diff for unchanged projections). |
| `FcFieldPlaceholder` parameter surface (existing from Story 1-8) | 1-8 (shell) / 4-6 (localization + css hook) | Command form emitter (unchanged) + projection emitter (new — 4-6 addition) | Story 1-8 parameter frozen: `FieldName`, `TypeName`, `IsDevMode`. 4-6 ADDS NO parameters — just the internal body becomes localized and the `fc-field-placeholder-dev` CSS class gains a red-dashed-border rule. |
| `RazorModelTransform.Transform` Unsupported-column handling (BEHAVIOR CHANGE) | 4-6 | Every downstream emitter that iterates `RazorModel.Columns` | **BREAKING for the emitter surface** — Unsupported columns NOW appear in `Columns`. All current consumers (`ProjectionRoleBodyEmitter`, Story 4-3 filter cell logic, Story 4-4 prioritizer) already handle unsupported types correctly (filter disabled, prioritizer sinks to int.MaxValue, emitter routes to placeholder). No downstream consumer requires code changes. Regression baseline (`CounterProjectionApprovalTests`) stays green because Counter has zero unsupported properties. |
| ~~`FcShellOptions.EmptyStateSecondaryTextResourcePrefix: string?`~~ | ~~4-6~~ | ~~`FcProjectionEmptyPlaceholder`~~ | **REMOVED per spec revision (Challenge C4).** No new options surface in v1. Resource-key convention `{ProjectionTypeFqn}_EmptyStateSecondaryText` is hardcoded. Adopter-prefix-override deferred to a future one-story scope when a real need surfaces. |

---

## Acceptance Criteria

| AC | Given / When / Then | Primary tasks |
|---|---|---|
| **AC1** | **Given** a projection annotated `[Projection]` with zero items AND a bounded-context that has at least one writable command AND the current user is authorized for that command. **When** `{TypeName}View` renders and `state.Items.Count == 0`. **Then** `FcProjectionEmptyPlaceholder` emits: (a) outer `<div role="status" aria-label="No {entities} found" class="fc-empty-state-body">`; (b) a 48px `FluentIcon` (`DocumentSearch` glyph) with `opacity: 0.5` via CSS class; (c) `FluentLabel Typography.Body1` with text `"No {entities} yet."` (localized via `EmptyStateMessageTemplate`; when `Role == ProjectionRole.ActionQueue`, uses `EmptyStateActionQueueMessageTemplate` "No {0} awaiting action."); (d) `FluentAnchor Appearance="Accent"` labeled `"Send your first {CommandDisplayName}"` (localized via `EmptyStateCtaTemplate`) with `Href` set to the command's Blazor route (resolved via `IEmptyStateCtaResolver.Resolve(...)`) — semantically a navigation link, not an action; screen readers announce "link" not "button"; the entire CTA is wrapped in `<AuthorizeView Policy="{commandAuthorizationPolicy}">` so unauthorized users see the empty-state without the CTA (see AC2.5); (e) CTA anchor is keyboard-focusable (default `FluentAnchor` behavior); (f) optional `FluentLabel Typography.Body2 class="fc-empty-state-body__secondary"` secondary text is rendered WHEN the resource-key lookup `{ProjectionTypeFqn}_EmptyStateSecondaryText` returns non-null (Microsoft `IStringLocalizer` missing-key convention: returns key itself — 4-6 treats key-equals-value as "missing"). | T1, T2, T3, T5 |
| **AC2** | **Given** a projection whose bounded-context has zero writable commands AND whose type carries no `[ProjectionEmptyStateCta]` attribute. **When** the empty-state renders. **Then** the CTA `FluentAnchor` is NOT emitted (no anchor element in the rendered DOM); icon + primary message + optional secondary still render. The `aria-label` is identical to AC1. **And** `IEmptyStateCtaResolver.Resolve(projectionType)` returns null — verified by a bUnit test mocking the resolver. **Given** a projection annotated `[ProjectionEmptyStateCta(nameof(CreateOrderCommand))]` (recommended call pattern per D6). **When** the empty-state renders. **Then** the CTA anchor appears with the `CreateOrderCommand` display name, EVEN IF that command is NOT the first-in-bounded-context (attribute overrides fallback per D6). **Given** `[ProjectionEmptyStateCta("UnknownCommand")]` where `UnknownCommand` is not registered. **When** the resolver runs. **Then** it returns null (fail-soft) AND logs a Warning via `ILogger<EmptyStateCtaResolver>` (the adopter spelled the command name wrong — loud enough to notice in logs, soft enough not to crash the view; D6.a HFC1040 analyzer in Epic 6-1 will catch this at build-time). **Given** a bounded-context with multiple writable commands `["AddOrderItem", "CancelOrder", "CreateOrder", "ShipOrder"]`. **When** the resolver runs the D4 fallback. **Then** the resolver returns `CreateOrder` (creation-verb prefix `Create` matched, alphabetical among matches) — NOT `CancelOrder` (no creation prefix) and NOT `AddOrderItem` (creation prefix matched but later alphabetically). | T2, T3, T5 |
| **AC3** | **Given** the same projection has zero items AND filters are ACTIVE (one or more `ColumnFilterChangedAction` / `StatusFilterChangedAction` / `SearchQueryChangedAction` dispatched per Story 4-3). **When** the view renders. **Then** Story 4-3's `FcFilterEmptyState` renders INSTEAD of 4-6's `FcProjectionEmptyPlaceholder` (4-3 owns this case via its existing conditional in the emitter — 4-6 does NOT duplicate). **And** the message shows 4-3's `EmptyFilteredStateTemplate` ("No orders match the current filters. Reset filters to see all 47 orders."). **And** a regression test in `FcProjectionEmptyPlaceholderTests` asserts: zero-total with no active filters → 4-6 component; zero-matches with active filters → 4-3 component; the two paths do NOT mix. | T3, T6 |
| **AC2.5** | **Given** a projection whose bounded-context resolves a CTA command BUT the current user lacks authorization for that command (no policy match, no role match). **When** the empty-state renders. **Then** the CTA `FluentAnchor` is NOT visible (`<AuthorizeView Policy="...">` collapses); icon + primary message + optional secondary still render. The `aria-label` is identical to AC1. **And** the rendered DOM contains NO orphan anchor element. **And** authenticated users with policy match SEE the CTA per AC1. **And** anonymous (unauthenticated) users see the empty state without CTA per AC2. **Rationale:** prevents the "see button → click → 403" failure mode (pre-mortem P2). | T3, T5 |
| **AC4** | **Given** a projection property annotated `[Description("Order number shown on invoices and confirmation emails")]` (from `System.ComponentModel`). **When** `{TypeName}View` renders the DataGrid. **Then** the column header cell renders the header text followed by a small inline `FluentIcon Icon="@Icons.Regular.Size16.Info"` element; THE ICON is wrapped in a `FluentTooltip` whose content is the description; the tooltip appears on `mouseover` of the icon AND on keyboard focus of the icon (Fluent default behavior); on touch devices, tapping the icon shows the tooltip. The header `<th>` element gets `aria-describedby` pointing to the tooltip's content element (emitted `Id` is `"{viewKey}-{propertyName}-desc"` — deterministic per view + property) — screen readers announce the description with the column regardless of pointer/keyboard mode. **And** column-resize drag handles in the header cell continue to work — verified by bUnit drag simulation (the icon does NOT intercept `pointerdown` on header-resize zones). **Given** a projection property annotated `[Display(Name="Order Number", Description="Order number shown on invoices and confirmation emails")]` INSTEAD of `[Description]`. **When** the column renders. **Then** the same icon + tooltip + aria-describedby wiring applies (both attribute paths consumed per D2). **Given** BOTH `[Description("A")]` AND `[Display(Description="B")]` on the same property. **When** Parse evaluates. **Then** `PropertyModel.Description == "A"` (precedence per D2). **Given** a property with NO description attribute. **When** the column renders. **Then** the column header emits the pre-4-6 shape unchanged (`Title=` attribute only, no info icon, no `FluentTooltip` wrapping, no `aria-describedby` — regression gate verified by `CounterProjectionApprovalTests`). | T1, T2, T6 |
| **AC5** | **Given** a projection with a `[Description]` or `[Display(Description=)]` annotated property. **When** the projection is rendered as a DetailRecord (Story 4-1 role body) OR inside an expand-in-row detail panel (Story 4-5 via the factored `EmitDetailRecordBody`). **Then** a `FluentLabel Typography="Typography.Caption" class="fc-field-description"` renders DIRECTLY BELOW the field value, showing the description text. **And** the text passes through XML-escape (apostrophes, angle brackets, ampersands) via standard Razor encoding — no raw HTML injection path. **And** description appears in BOTH surfaces identically — snapshot diff confirms 4-1 DetailRecord vs 4-5 expand-in-row emission produces byte-identical description markup (shared helper). **And** legacy projections without descriptions continue to emit the pre-4-6 detail body verbatim (regression gate). | T2, T4, T6 |
| **AC6** | **Given** a projection declaring `public Dictionary<string, LineItem> LineItems { get; init; } = new();`. **When** the source generator runs. **Then** HFC1002 fires at Parse with message "Property 'LineItems' on type '{...}' is not supported for auto-generation..." (UNCHANGED — warning was already firing). **And** `RazorModelTransform.Transform` produces a `ColumnModel` for `LineItems` with `TypeCategory.Unsupported` (instead of dropping it — the `if (property.IsUnsupported) { continue; }` at line 29 is REMOVED). **And** `ProjectionRoleBodyEmitter` emits a `TemplateColumn<T>` whose `ChildContent` contains `<FcFieldPlaceholder FieldName="LineItems" TypeName="{Dictionary<string, LineItem> FQN}" IsDevMode="@(RenderContext.Mode == RenderMode.DevMode)" />`. **And** the column header still renders its resolved `Header` text ("Line Items" after humanization) + the appended suffix resource `UnsupportedColumnHeaderSuffix` "(custom renderer needed)" for visual clarity. **And** browser DOM inspection confirms: dashed-border card, warning icon, FieldName + TypeName text, message "This field requires a custom renderer.", docs-link `FluentAnchor` to the HFC1002 docs URL. **And** NO property declared on the `[Projection]` surface is missing from the rendered grid — zero silent omissions verified by a scope-guardrail test (`UnsupportedColumnEmissionTests.EveryProjectionPropertyAppearsInEmittedColumns`). | T1, T2, T4, T6 |
| **AC7** | **Given** the Unsupported column's `FcFieldPlaceholder`. **When** accessibility tooling scans. **Then** outer `<div>` carries `role="status"`, `tabindex="0"` (focusable in tab order so keyboard users can reach the docs link), `aria-label` resolves from `FieldPlaceholderAriaLabelTemplate` with `FieldName` interpolated ("LineItems requires a custom renderer" / "LineItems nécessite un rendu personnalisé"). **And** the embedded `FluentAnchor` docs link is keyboard-activated (Enter/Space native browser behavior). **Given** `RenderContext.Mode == RenderMode.DevMode`. **When** the placeholder renders. **Then** CSS class `fc-field-placeholder-dev` applies to the outer `<div>`; the scoped CSS rule `.fc-field-placeholder-dev { border: 2px dashed var(--palette-red-70, #d13438) !important; }` overrides the default neutral-stroke dashed border to red. **Given** normal render-mode. **When** the placeholder renders. **Then** the CSS class `fc-field-placeholder-dev` is ABSENT; the default neutral-stroke dashed border applies. **Dev-mode overlay activation + annotation panel is Epic 6-5 scope** — 4-6 ships ONLY the CSS class + parameter binding. The red-dashed-border visual specimen verification is Story 10-2 scope. | T1, T4, T6 |
| **AC8** | **Given** the `FcFieldPlaceholder` hardcoded-English body (from Story 1-8). **When** the component renders in French culture. **Then** the rendered text is French (loaded via `IStringLocalizer<FcShellResources>` from `FcShellResources.fr.resx`); no English strings appear in the rendered DOM. **And** the 11 new resource keys (D8) are present in BOTH `FcShellResources.resx` AND `.fr.resx`; the existing `CanonicalKeysHaveFrenchCounterpartsTests` auto-discovers them and asserts parity without code change. **And** all new templates use `{0}` interpolation only (no colons in any new key → no French U+00A0 NBSP requirements in v1 — ICU migration is Epic 9-5 territory). **And** `FieldDescriptionTooltipAriaSuffix` is appended to combined `aria-label` via `ColumnHeaderWithDescriptionAriaTemplate` ("{Header}, {Description}") — comma separator, language-agnostic punctuation. | T7 |

---

## Critical decisions (READ FIRST — do NOT revisit)

> **19 binding decisions.** Feature-story budget is ≤ 25 (L06). 4-6 lands at 19/25, leaving a 6-decision buffer. Raise a spec-change proposal before revisiting any entry.

| # | Decision | Rationale | Consumed by |
|---|---|---|---|
| **D1** | **The `RazorModelTransform.Transform` Unsupported-column DROP is REMOVED.** Every `PropertyModel` flows into a `ColumnModel` — `TypeCategory.Unsupported` signals emit-stage placeholder dispatch. | FR9 + UX-DR55 demand "never silently omit an unsupported type" — the current `if (property.IsUnsupported) { continue; }` at `RazorModelTransform.cs:29` DIRECTLY CONTRADICTS both requirements at runtime, even though the Parse stage faithfully emits HFC1002. Either the Parse warning or the Transform drop has to change; the Transform drop is the defect. Downstream emitters (4-1 role body, 4-3 filter, 4-4 prioritizer) already handle `TypeCategory.Unsupported` correctly because the enum value has existed since Story 1-5 — they just never saw one reach them. `CounterProjectionApprovalTests` is green regardless (Counter has zero unsupported properties). Rejected: (a) leave the drop + rely on HFC1002 Warning — violates FR9 + UX-DR55; (b) opt-in via `FcShellOptions.EmitUnsupportedPlaceholders=true` — negative-default anti-pattern (Story 4-4 D1 precedent); (c) ship as Information severity in the placeholder itself (no dashed border, just a neutral label) — undersells the modeling gap. | T1, T2, T4; AC6 |
| **D2** | **Description precedence: `[System.ComponentModel.DescriptionAttribute]` wins over `[Display(Description="...")]`.** Both attributes are supported at Parse; when both are present on the same property, `[Description]` takes precedence. When only one is present, that one is used. | `[Description]` is the older, more-specific attribute (System.ComponentModel namespace predates DataAnnotations). Developers reaching for `[Display]` typically want locale-keyed resources (via `Display.ResourceType` — not supported in v1 but a forward-compatible path); developers reaching for `[Description]` typically want a hardcoded string. When an author has chosen to add `[Description]`, it's a deliberate narrow annotation — respect it. Consistent with .NET Framework's own precedence (`PropertyDescriptor.Description` reads `[Description]` first). Rejected: (a) `[Display(Description=)]` wins — violates .NET precedent; (b) fail-loud on both present — adopter friction for a benign state; (c) concatenate both — confusing UX; (d) require adopters to pick ONE via analyzer — over-constraining. | T1, T2; AC4, AC5 |
| **D3** | **Empty-state copy is role-differentiated via TWO templates: `EmptyStateActionQueueMessageTemplate` for `Role == ActionQueue`, `EmptyStateMessageTemplate` for all other roles (including null).** No per-role explosion. | ActionQueue is the only role whose empty state reads DIFFERENTLY in v1 ("No orders awaiting action" is contextually richer than "No orders yet" when the role IS the queue). Other roles (Default / StatusOverview / DetailRecord / Timeline / Dashboard) all read "No {entities} yet" naturally. Adding more role-specific templates now is speculative — adopters can override per-projection via the `{ProjectionTypeFqn}_EmptyStateSecondaryText` resource convention when they need role-specific copy for a specific surface. Rejected: (a) one template per role (5 templates) — translation cost without clear user benefit; (b) one generic template only — loses ActionQueue's contextual richness (the 4-1 subtitle already uses ActionQueue-specific copy per UX-DR44, so the empty state matching that tone is coherent); (c) derive copy from role programmatically (string concatenation) — translation-hostile. | T2, T3; AC1 |
| **D4** | **CTA discovery chain: (1) `[ProjectionEmptyStateCta(CommandTypeName)]` attribute → direct registry lookup; (2) fallback to writable command in `[BoundedContext]` whose name starts with a creation-verb prefix (`Create\|Add\|New\|Send\|Register\|Start\|Place\|Submit\|Issue\|Open\|Initiate`), then alphabetical among matches; if NO creation-verb match, fall through to alphabetical across all writable commands; (3) null (read-only projection, no CTA).** Resolver is the single choice authority — the component never decides; the service owns discovery. | Creation-bias avoids the "Cancel < Create" alphabetical trap (would CTA destructive commands on an empty grid — bad UX). Verb-prefix list is English-only and intentionally conservative; non-matching domain verbs (`Mint`, `Craft`, `Forge`, `PlaceOrder`-style domain verbs) require explicit `[ProjectionEmptyStateCta]` opt-in (documented adopter contract). One source of truth — when an adopter explicitly annotates, they win; when they don't, the framework picks a deterministic creation-biased default. Ordinal sort within the matched bucket is stable across builds (no dependency on declaration order or registration order). "First writable creation command" aligns with UX-DR4 anatomy ("Send your first [Command Name]") — the CTA is a single suggestion, not a picker. Multiple-CTA layouts (a splitbutton of commands) are Epic 6 customization territory. Rejected: (a) pure alphabetical (pre-revision D4) — destructive-command-first trap; (b) first-in-registration-order — non-deterministic; (c) "any command with minimal fields" heuristic — over-engineered; (d) require every projection to declare `[ProjectionEmptyStateCta]` explicitly — friction; (e) `[CommandIntent(Create)]` registry metadata — Story 3-5 re-open + scope escalation (Epic 6 backlog entry). | T2, T3, T5; AC1, AC2 |
| **D5** | **`IEmptyStateCtaResolver` is a hand-written `Scoped` service injected into `FcProjectionEmptyPlaceholder` via `[Inject]`.** The resolver's `Resolve(Type projectionType)` signature is **synchronous** in v1 — purely synchronous reads of `IFrontComposerRegistry`. When Epic 5 registry wire-queries land (SignalR reconciliation), an `Task<EmptyStateCtaTuple?> ResolveAsync(Type)` overload will be added ALONGSIDE — both coexist permanently. **Binary-compat contract:** the v1 sync `Resolve` MUST stay supported even when async lands; removing it would break adopter custom `IEmptyStateCtaResolver` implementations registered via `services.AddScoped<IEmptyStateCtaResolver, MyCustomResolver>()`. Async is additive, never replacement. | L05 hand-written service + registry-driven pattern (like Story 2-2 `LastUsedValueProvider`). Keeping the resolver a service (not a static helper) allows adopter overrides via `services.AddScoped<IEmptyStateCtaResolver, MyCustomResolver>()` — cheap escape hatch without Razor template replacement. Scoped lifetime aligns with Blazor circuit scoping (one resolver per user session). Synchronous v1 signature avoids the "async without I/O is a smell" anti-pattern (Task allocation, async-all-the-way contagion); async overload is additive when actual I/O exists. Rejected: (a) static helper class — prevents override; (b) Singleton — wrong for per-user-tenant scoping (no tenant leak risk today, but future-proofing); (c) emit resolver logic per-component at generator time — reflection-free but couples generator to registry shape; (d) async-only signature in v1 — pre-commits to a Task shape without I/O justification. | T2, T3, T5; AC2 |
| **D6** | **`[ProjectionEmptyStateCta(CommandTypeName)]` uses STRING-BASED `CommandTypeName`, NOT `Type` typeof-arg.** The attribute accepts the command type's short name (e.g., `"CreateOrderCommand"`) OR its full qualified name — the resolver tries exact-match first, then contains-match fallback on FQN. **`nameof()` is the RECOMMENDED call pattern: `[ProjectionEmptyStateCta(nameof(CreateOrderCommand))]`** — Roslyn resolves `nameof` to a compile-time constant string (IR cache stays clean) AND gives refactor-safety (rename refactors propagate). | Roslyn symbol-based type references inside attributes require `ITypeOfOperation` navigation which breaks IR cacheability (Story 1-4 constraint — attribute args MUST be compile-time constants). String-based names align with Story 4-1 `ProjectionRole.WhenState` CSV precedent + Story 4-4 `ColumnPriority` int-based precedent (both string / primitive). `nameof()` recommendation gives adopters the best of both worlds: compile-time constant (Roslyn-required) + symbol-tracked (refactor-safe). The contains-match fallback handles adopters who declare `MyDomain.Orders.Commands.CreateOrderCommand` but annotate with just `"CreateOrderCommand"` — convenience without giving up unambiguous behavior when FQN is provided. **D6.a (deferred to Epic 6-1):** add HFC1040 analyzer that walks `[ProjectionEmptyStateCta]` string args and reports build-time error when no type with that name exists in the compilation (catches typos statically — see P4 in pre-mortem). Rejected: (a) `[ProjectionEmptyStateCta(typeof(CreateOrderCommand))]` — breaks IR cacheability; (b) `[ProjectionEmptyStateCta(Guid CommandId)]` — adopter never interacts with command IDs; (c) generic attribute `[ProjectionEmptyStateCta<CreateOrderCommand>]` — netstandard2.0 + IR cacheability constraints (generic attribute type-arg surfaces as `ITypeSymbol`, same problem as typeof). | T1, T3, T5; AC2 |
| **D7** | **`FcProjectionEmptyPlaceholder` parameter surface gains 2 APPEND-ONLY parameters (`CtaCommandName`, `SecondaryText`) — the 3 frozen parameters (`ProjectionType`, `EntityPluralOverride`, `Role`) stay untouched per Story 4-1 ADR-053.** Generator emission gains 2 additional `AddAttribute` lines; hint-file byte-diff for unchanged projections is only +2 lines (the new null-valued attributes). | ADR-053 discipline preserved — 4-6 is ALLOWED to add parameters per the Story 4-1 contract ("Story 4.6 MAY add parameters BUT MUST NOT remove, rename, or reorder" per `FcProjectionEmptyPlaceholder.razor.cs:17-19`). The +2 lines are deterministic and only appear in unchanged-projection diffs ONCE (after re-emission); thereafter the hint is byte-stable. The parameters are emitted whether or not the adopter provides them — default null = "use resolver discovery". Rejected: (a) replace `Role` parameter with a richer "CTA descriptor" object — breaks ADR-053; (b) merge into a single `EmptyStateConfig` object — breaks ADR-053 + harder for adopters to customize one axis; (c) wait for Epic 6 to add params via the customization gradient — Epic 6 is template-replacement territory, and the CTA is framework-core behavior. | T2, T3, T5; AC1, AC2 |
| **D8** | **11 new EN+FR resource keys = 22 new strings.** All templates use `{0}` interpolation only (no colons → no French U+00A0 NBSP requirements in v1). ICU pluralization deferred to Epic 9-5. | All 11 keys serve a first-class user-facing surface (empty-state copy, field-placeholder body/aria, column-header description wiring). No duplicates, no synonyms. The no-colon constraint ensures French translations don't need the non-breaking-space rule — translators can plug strings without typographic lint. ICU pluralization is deferred because single-expand empty-states are always singular-or-zero (grammatically indistinct in both EN + FR); when multi-projection-view composition arrives, ICU becomes necessary — but that's Epic 9-5's umbrella. Rejected: (a) fewer keys + runtime concatenation — complicates translation review; (b) ICU keys from day 1 — Epic 9-5's work isn't done yet; (c) resource-less `const string` baked into the component — breaks UX-DR60 (localization mandate). | T7; AC1, AC2, AC6, AC7, AC8 |
| **D9** | **Column-header description renders as a small inline `FluentIcon Icon="@Icons.Regular.Size16.Info"` adjacent to the header text; the icon (NOT the full header) is wrapped in `FluentTooltip` whose content is the description.** Generator emits the icon + tooltip conditionally — only when `column.Description` is non-null. Null flows through as the pre-4-6 `Title=` attribute (regression gate). | `FluentTooltip` is the Fluent v5 component that auto-manages anchor positioning + keyboard focus + prefers-reduced-motion. Using Fluent-owned primitives keeps "zero-override" architecture intact (UX-DR57). **Wrapping the icon (not the header text) avoids three failure modes:** (1) column-resize drag handles inside the header cell aren't intercepted by the tooltip anchor (resolves pre-mortem P6); (2) touch-device users get an explicit tap target (the (i) icon) — desktop tooltips are notoriously undiscovered on touch surfaces; (3) keyboard users tab to the icon discretely without the entire header becoming a focus stop. The icon emits inside `HeaderCellItemTemplate` slot of `PropertyColumn<T, TProp>` / `TemplateColumn<T>` — confirmed via Fluent UI MCP `get_component_details PropertyColumn` lookup (spike in T2.6, mirroring Story 4-5 T2.1 MCP-backed verification). Conditional emission preserves IR byte-stability for descriptionless projections. The header `<th>` still gets `aria-describedby` pointing to the tooltip's content element (`{viewKey}-{propertyName}-desc`) — screen readers announce description with the column name regardless of icon-vs-header anchor. Rejected: (a) wrap full header text in `FluentTooltip` (pre-revision D9) — column-resize collision + touch-undiscoverable; (b) always wrap header (no conditional) — unnecessary DOM overhead for the common case; (c) use `title=` HTML attribute (native tooltip) — loses keyboard-focus trigger + accessibility contract; (d) wrap headers at runtime in the Shell via a `CascadingValue<ColumnDescriptor>` — couples runtime cascade to build-time metadata, brittle. | T2, T6; AC4 |
| **D10** | **Description renders as `FluentLabel Typography.Caption class="fc-field-description"` in detail/form views — placed DIRECTLY BELOW the field value.** Shared emission path through Story 4-5's factored `EmitDetailRecordBody`. | `Typography.Caption` is the design-system token for "supplementary text" (visual-design-foundation.md row: "Caption1 / Validation messages, Last updated indicators"); descriptions are exactly this register — they explain a field without competing with the value. Placing BELOW (not beside, not as tooltip-on-value) matches the UX-consistency pattern "field label → value → supplementary" + is the layout least disrupting to narrow viewports. Shared emission via 4-5's helper means 4-1 DetailRecord + 4-5 expand-in-row detail show descriptions identically — snapshot diff locks. Rejected: (a) `Typography.Body2` — too prominent for supplementary context; (b) tooltip-on-value — users rarely hover values; keyboard-only users wouldn't discover it; (c) collapsible "(info)" icon that expands — adds interaction cost for what should be passively visible; (d) inline after value on same line — wraps unpredictably on long descriptions. | T2, T6; AC5 |
| **D11** | **`PropertyModel.Description` + `ColumnModel.Description` are APPEND-ONLY IR fields.** Append at tail of constructor; `Equals` / `GetHashCode` updated; existing call sites keep compiling with default null. | IR byte-stability (Story 1-4 contract) — unchanged projections produce byte-identical IR. Append-only avoids ordered-argument breakage for the 10+ call sites of `new PropertyModel(...)` across SourceTools + tests. Null default signals "no description" semantically AND via the emitter's conditional. Rejected: (a) mandatory parameter — breaks every existing call site; (b) separate `PropertyDescriptionModel` auxiliary record — adds an IR type without value (description is a property-level primitive); (c) `Dictionary<PropertyName, Description>` on DomainModel — turns a simple primitive into a lookup, reducing IR clarity. | T1, T2, T6; AC4, AC5 |
| **D12** | **Test volume: 55 tests** (revised from 58). Counts: Contracts attribute (3), ~~FcEmptyStateBody (7)~~ **dropped per Challenge C3**, FcProjectionEmptyPlaceholder body (10 — was 7; +2 anatomy + FluentAnchor checks, +1 AC2.5 auth-gate), FcFieldPlaceholder localization (5), EmptyStateCtaResolver (8 — was 7; +1 D4 verb-prefix-fallback), Description parser (7), Unsupported column Transform (5), Unsupported column Emit (6), **Description tooltip Emit (6 — was 7; −1 form-view hint test removed per First Principles to align with Known Gap "Description on COMMAND form inputs — Epic 6 backlog")**, Description Transform (5), Resource parity (0 — auto-discovered). Sum: 3 + 10 + 5 + 8 + 7 + 5 + 6 + 6 + 5 = 55. | Below L11 soft trigger (80 tests). Density 55/19 ≈ 2.89 tests per decision — comparable to 4-5's 58/22 ≈ 2.64. Every test closes a specific AC gap: 9 ACs (8 base + AC2.5) × ≈ 6 tests each = 54 base, plus 1 regression gate for the Unsupported-drop flip. No visual-regression tests in 4-6 scope (Story 10-2 owns). No Playwright / axe-core in 4-6 scope (Story 10-2 owns). | T4-T7 |
| **D13** | **`FcFieldPlaceholder` body is SPLIT from inline `@code` into `.razor.cs` partial class.** Consistent with Story 4-1 `FcProjectionEmptyPlaceholder` split precedent + improves unit-test accessibility (partial-class methods testable via xUnit when not bound to Blazor lifecycle). | Code organization + test ergonomics. The inline `@code` block made sense when FcFieldPlaceholder was a three-parameter static render; localization + CSS-class conditional pushes it past the inline-code complexity threshold. Splitting keeps the Razor markup focused on structure. Rejected: (a) keep inline — harder to unit-test + inconsistent with 4-1 precedent; (b) fully extract to `.cs` component (no Razor) — loses the Razor authoring ergonomics; (c) use Blazor `ComponentBase` subclass in a separate file — same as .razor.cs but more boilerplate. | T1; AC7, AC8 |
| **D14** | **Dev-mode highlight is a CSS-CLASS CONDITIONAL on the existing `IsDevMode` parameter — Ctrl+Shift+D keyboard hook + dev-mode overlay panel are Epic 6-5 scope.** 4-6 ships ONLY the wiring: `IsDevMode=true → fc-field-placeholder-dev CSS class → red-dashed-border rule in fc-empty-state.scoped.css`. | Separation of concerns — the visual highlight is trivial (one CSS class); the MODE activation is a cross-cutting shell concern (keyboard shortcut + panel + persistence per `RenderContext.Mode` cascading parameter). Wiring the CSS class + parameter in 4-6 means Epic 6-5 can flip the mode without re-opening 4-6. Story 4-5 D22 precedent: ship the DOM hook in the feature story, defer the visual specimen + mode activation to the owning story. Rejected: (a) ship dev-mode overlay in 4-6 — scope creep; (b) drop dev-mode visual entirely (just the default dashed border forever) — violates UX-DR3 + component-strategy §FcFieldPlaceholder dev-mode spec; (c) use `RenderContext.Mode` inline without CSS-class — harder to re-skin in Epic 6. | T1, T4; AC7 |
| **D15** | **`FcFieldPlaceholder` column emission uses `TemplateColumn<T>`, NOT an extended `PropertyColumn<T, TProp>`.** The emitter dispatches Unsupported columns to a separate emit path within `ProjectionRoleBodyEmitter`. **Placement-in-grid (Unsupported placeholders rendered as in-line column cells in declaration order) is a discretion call NOT prescribed by FR9, UX-DR55, or component-strategy** — the three sources cross-reference the COMPONENT (`FcFieldPlaceholder`) and the A11Y CONTRACT but leave placement open. D15 picks column-cell emission for: (a) proximity to the missing data; (b) preservation of declaration order; (c) zero change to the grid anatomy. Revisit in Epic 6 if grids with >3 unsupported columns become attention-hostile (consider an "N fields require custom rendering" collapsible accordion below the grid as an alternative). | `PropertyColumn<T, TProp>` requires a type-parameter `TProp` bound to a property accessor — `Property => x.LineItems` — which REQUIRES the property's type to be available in the generated code. For unsupported types (ref-struct, generic parameter, internal type), this fails to compile. `TemplateColumn<T>` is type-parameter-on-T only; its `ChildContent` renders any component with any shape. Rejected: (a) extend `PropertyColumn` — fundamental type-param constraint; (b) emit a single giant `TemplateColumn<T>` for ALL columns (not just unsupported) — regresses Story 4-3 filter affordance + Story 4-4 prioritizer column metadata; (c) use `FluentDataGrid.Children` collection instead of columns — breaks the PropertyColumn contract Fluent v5 relies on for sort/filter; (d) collapsed accordion below the grid — deferred to Epic 6 per discretion-call note. | T2, T6; AC6 |
| **D16** | **Unsupported-column participation in Stories 4-3 (filter), 4-4 (prioritizer), 4-5 (expand-in-row) — each story's existing behavior is verified by a regression test rather than re-designed.** 4-3: `ColumnModel.SupportsFilter => false` for Unsupported (already correct — line ≈55 of ColumnModel.cs). 4-4: Unsupported columns carry `Priority=null` → sink to `int.MaxValue` via 4-4 D17 stable sort. 4-5: `[ProjectionFieldGroup]` + Unsupported columns flow through verbatim — grouping is orthogonal to TypeCategory. | Reuse over redesign. The three predecessor stories built their systems around `ColumnModel` + `TypeCategory`; each already handles `Unsupported` correctly because the enum value pre-existed them. What changed in 4-6 is that Unsupported columns now REACH those systems (pre-4-6 they were dropped). Regression tests prevent drift — the systems' behavior is LOCKED by tests, not by 4-6's code. Rejected: (a) special-case Unsupported at the 4-3 filter level (hide the header entirely) — UX-DR55 says "never silently omit" + the filter affordance is already hidden by `SupportsFilter=false`; (b) sink unsupported columns to the 4-4 overflow panel unconditionally — removes adopter control via `[ColumnPriority]`; (c) skip Unsupported columns in 4-5 grouping — inconsistent with ProjectionFieldGroup's declarative intent. | T4, T6; AC6 |
| **D17** | **Secondary-text resource-key convention: `{ProjectionTypeFqn}_EmptyStateSecondaryText`.** `IStringLocalizer`'s Microsoft convention returns the key itself when not found — 4-6 treats key-equals-value as "absent" (returns null, suppresses the secondary FluentLabel emission). | Zero-configuration adopter path: drop a resource in any `.resx` (adopter's own resource files or the Shell's) keyed as `"My.Domain.OrderProjection_EmptyStateSecondaryText"` + the empty state reads it automatically. No attribute required, no API surface addition. The key-equals-value check is the standard .NET idiom (`IStringLocalizer.GetString(key).Name == key` indicates a miss). The convention prefix is hardcoded for v1 — no `FcShellOptions` override surface (YAGNI; when a real adopter needs a prefix override, add the option in a one-story scope). Rejected: (a) `[ProjectionSecondaryEmptyText("text")]` attribute — adds surface for a rare case; (b) explicit `SecondaryText` parameter only (remove the resource convention) — forces generator re-emission to change copy; (c) a new `IProjectionEmptyStateDescriptor` service — over-engineered; (d) direct `Resource.GetString` call in the view — couples component to specific resource assembly; (e) `FcShellOptions.EmptyStateSecondaryTextResourcePrefix` override (pre-revision option) — speculative options surface for an unproven need. | T2, T5, T7; AC1 |
| **D18** | **Description XML-escape is via standard Razor `@column.Description` encoding — NOT via a manual `HtmlEncoder.Default.Encode` call.** Razor's default text-content rendering escapes `<`, `>`, `&`, `'`, `"` automatically. | Defense in depth via the framework default. Manual encoding introduces test surface without security benefit (Razor's default IS the browser's well-tested encoder). Consistent with Story 4-1 / 4-2 / 4-3 / 4-4 / 4-5 rendering (all rely on default Razor encoding). Rejected: (a) explicit `HtmlEncoder.Default.Encode` in the generator emission — adds code without benefit; (b) allow HTML in descriptions via `MarkupString` — OPENS XSS — adopters could unknowingly inject user-provided text through an attribute that seems harmless; (c) ban `<` / `>` / `&` at Parse-stage with a HFC diagnostic — annoying for technical descriptions like "type is `Dictionary<K,V>`". | T6; AC4, AC5 |
| **D19** | **No new HFC diagnostic IDs in 4-6.** HFC1002 (Unsupported field type) is REUSED for the now-visible placeholder path. No HFC for "missing description" (not an error, descriptions are optional). No HFC for "empty-state CTA command not found" (fail-soft + ILogger Warning per D4). | HFC inflation tax — each new diagnostic ID adds analyzer-releases-doc overhead, XML-doc constant, translation surface, and dev-mode noise. 4-6 introduces no new invariant violations that warrant a compile-time diagnostic. HFC1002's existing DocsLink already points to the placeholder docs page (`https://hexalith.github.io/FrontComposer/diagnostics/HFC1002`) — adopters who see the warning AND the placeholder reach the same help. The docs-link centralization constant in `FcFieldPlaceholder` ensures consistency. Rejected: (a) add HFC1032 "description attribute has HTML markup" — defense for a scenario prevented by Razor default encoding (D18); (b) add HFC1033 "empty-state CTA attribute references unknown command" — belongs in runtime log, not build; (c) promote HFC1002 from Warning to Error — escalates a UX-DR55-fulfilled case (placeholder now renders; no data loss). | T1, T6; AC6 |

---

## Migration note for 4-6 adopters

**This story changes user-visible behavior on first rebuild after merge.** Three breaking-visual surfaces appear automatically; adopters MUST audit projections before merging 4-6.

### Surface 1 — Unsupported columns become visible

**What changes:** Pre-4-6, projection properties whose types fall outside the `MapTypeCategory` supported set (`Dictionary<K,V>`, `ReadOnlyMemory<T>`, custom value objects, ref structs, etc.) were SILENTLY DROPPED from the generated DataGrid (`RazorModelTransform.cs:29` had `if (property.IsUnsupported) { continue; }`). After 4-6, every unsupported property emits a dashed-border `FcFieldPlaceholder` card in its own grid column with the warning icon, type annotation, and docs link. **Live demos with N silently-dropped columns will visually grow N dashed-border cards on first rebuild.**

**Audit before merge:**
1. `grep -rE "\[Projection\]" src/` — list all projection types.
2. For each projection, inspect its property types against `RazorModelTransform.MapTypeCategory` (lines ≈286-294 of pre-4-6 source).
3. For each unsupported property, decide:
   - **Keep the placeholder** (recommended for new adopters — placeholder loudly signals modeling gap; build warning HFC1002 still fires for visibility).
   - **Add a custom renderer via Epic 6-3 slot-level field replacement** (when shipped; until then, placeholder is the only path).
   - **Remove the unsupported property from the projection** (the property may belong on the aggregate root but not on the read-side projection).
4. Coordinate with stakeholders: any LIVE DEMO scheduled for the first sprint after 4-6 ships must be rehearsed with the new visual shape.

### Surface 2 — Description tooltips appear on annotated columns

**What changes:** Properties annotated with `[Description("...")]` or `[Display(Description="...")]` (both attribute paths exist in many real codebases via inherited `System.ComponentModel` usage) — the descriptions were INVISIBLE pre-4-6 (Parse stage ignored them). After 4-6, every annotated column header gains a small inline `(i)` info icon adjacent to the header text, with the description as a hover/focus tooltip. **Adopters with pre-existing `[Description]` annotations on projection properties will see new icons appear next to many column headers without any code change on their side.**

**Audit before merge:**
1. `grep -rE "\[Description\(|\[Display\(.*Description=" src/` — find pre-existing annotations on projection properties.
2. Review the description text — was it written for developer audiences (acceptable as tooltip), or did it contain placeholder/TODO text that shouldn't be user-facing?
3. Update or remove inappropriate descriptions BEFORE merge.

### Surface 3 — Empty-state CTAs may suggest unexpected commands

**What changes:** Pre-4-6, the empty-state placeholder rendered `"No {entities} yet."` with no CTA button. After 4-6, an `FluentAnchor Appearance="Accent"` CTA button appears, suggesting the first writable command in the projection's bounded context (per revised D4: creation-verb-prefix-matching commands ranked first; alphabetical fallback otherwise). **For projections in bounded contexts with "creative" command vocabulary (`MintBadge`, `ForgeContract`, `CraftPotion`), the heuristic may pick a non-creation command — the adopter must explicitly annotate `[ProjectionEmptyStateCta(nameof(MintBadge))]` to override.**

**Audit before merge:**
1. For each `[Projection]`, identify its bounded context (`[BoundedContext]`).
2. List all writable commands in that BC.
3. Mentally apply revised D4 — does the verb-prefix match yield the desired CTA? If not, add `[ProjectionEmptyStateCta(nameof(YourCreationCommand))]` to the projection type.
4. For read-only projections (no writable commands), no action needed — empty state renders without CTA per AC2.

### Process-notes ledger entry

This section's existence is itself a process pattern worth capturing: **L12 candidate** for `_bmad-output/process-notes/story-creation-lessons.md` — "UX-change stories that flip default behavior MUST include an explicit Migration note section listing each visible-surface change, audit steps, and decision options. Pure-additive feature stories don't need this; behavior-flip stories like 4-6 (silent-drop → loud-emit) do." Document after 4-6 merges and the migration playbook is validated by adopter feedback.

---

## Tasks / Subtasks

### T1 — Contracts: attribute + IR extensions (AC1, AC4, AC5, AC6; ≈ 1.5h)

- [x] **T1.1** Create `src/Hexalith.FrontComposer.Contracts/Attributes/ProjectionEmptyStateCtaAttribute.cs` per D6. `[AttributeUsage(AttributeTargets.Class | AttributeTargets.Struct, Inherited=false, AllowMultiple=false)]`. Constructor `public ProjectionEmptyStateCtaAttribute(string CommandTypeName)` throws `ArgumentException` on null/whitespace.
- [x] **T1.2** Extend `src/Hexalith.FrontComposer.SourceTools/Parsing/DomainModel.cs` — add `string? EmptyStateCtaCommandTypeName` to `DomainModel` (append-only tail param, default null; Equals/GetHashCode updated). Add `string? Description` to `PropertyModel` (append-only tail param, default null; Equals/GetHashCode updated).
- [x] **T1.3** Extend `src/Hexalith.FrontComposer.SourceTools/Transforms/ColumnModel.cs` — add `string? Description` (append-only tail param, default null; Equals/GetHashCode updated).
- [x] **T1.4** Tests (Contracts): `ProjectionEmptyStateCtaAttributeTests.cs` per cheat sheet — 3 tests.

### T2 — Parse + Transform + Emit stages (AC1, AC4, AC5, AC6; ≈ 4h)

- [x] **T2.1** Extend `AttributeParser.ParseDisplayAttribute(IPropertySymbol)` to ALSO extract `Description` named-argument (prefer new tuple return `(string? name, string? description)` — bump the caller site accordingly). Add new `ParseDescriptionAttribute(IPropertySymbol)` reading `System.ComponentModel.DescriptionAttribute.Description` (positional ctor arg). Apply D2 precedence at the `MapProperty` call-site (existing line ≈530): `property.Description = ParseDescriptionAttribute(propertySymbol) ?? descriptionFromDisplay`.
- [x] **T2.2** Extend `AttributeParser.ParseProjectionAttribute` (the `[Projection]` type-level parser) to read `[ProjectionEmptyStateCta]`; surface as `DomainModel.EmptyStateCtaCommandTypeName`.
- [x] **T2.3** Tests (SourceTools.Tests): `DescriptionAttributeParserTests.cs` per cheat sheet — 7 tests covering attribute precedence + trim + whitespace-only.
- [x] **T2.4** **Critical** — Edit `src/Hexalith.FrontComposer.SourceTools/Transforms/RazorModelTransform.cs` line 29: remove `if (property.IsUnsupported) { continue; }`. Every PropertyModel becomes a ColumnModel with `TypeCategory` resolved via `MapTypeCategory(property.TypeName)` — which already returns `Unsupported` for unknown types. Flow `property.Description` into `ColumnModel.Description` at the ColumnModel constructor call.
- [x] **T2.5** Tests (SourceTools.Tests): `UnsupportedColumnEmissionTests.cs` + `DescriptionTransformTests.cs` per cheat sheet — 10 tests.
- [x] **T2.6** Extend `ProjectionRoleBodyEmitter` header emission per revised D9 — when `column.Description` is non-null, emit inside `HeaderCellItemTemplate`: header text + a small inline `<FluentIcon Value="@(new Icons.Regular.Size16.Info())" Color="Color.Neutral">` element; wrap THE ICON (not the header text) in `<FluentTooltip Anchor="{viewKey}-{propertyName}-desc-icon">` + emit `<span id="{viewKey}-{propertyName}-desc">{description}</span>` as the tooltip content; wire `aria-describedby` on the header `<th>` pointing to the description span. Conditional — descriptionless columns emit pre-4-6 `Title=` attribute unchanged. **Fluent v5 spike required** — verify (a) the exact `HeaderCellItemTemplate` parameter name on `PropertyColumn<T, TProp>` + `TemplateColumn<T>` using Fluent UI MCP `get_component_details` tooling; (b) inline icon adjacent to header text does NOT intercept column-resize drag handles (write a bUnit drag-simulation regression test confirming column resize still works). Mirrors Story 4-5 T2.1 MCP-backed verification.
- [x] **T2.7** Extend `ProjectionRoleBodyEmitter` — Unsupported columns emit `TemplateColumn<T>` with `FcFieldPlaceholder` inside. Title= still emits the resolved header text; `UnsupportedColumnHeaderSuffix` resource appends "(custom renderer needed)" to the header (visually distinguishes placeholder columns without breaking screen-reader announce).
- [x] **T2.8** Extend `EmitDetailRecordBody` (factored helper per Story 4-5 D10) — when `column.Description` is non-null, emit `<FluentLabel Typography="Typography.Caption" class="fc-field-description">{description}</FluentLabel>` DIRECTLY BELOW the field value render. Shared by 4-1 DetailRecord role + 4-5 expand-in-row detail.
- [x] **T2.9** Extend `RazorEmitter` empty-shell emission (the existing `FcProjectionEmptyPlaceholder` call-site near line 407) — emit 2 new attributes: `"CtaCommandName"` (value = `DomainModel.EmptyStateCtaCommandTypeName` string-literal or `null`) + `"SecondaryText"` (always null from generator — component resolver handles runtime lookup).
- [x] **T2.10** Tests (SourceTools.Tests): `FcFieldPlaceholderColumnEmissionTests.cs` + `DescriptionTooltipEmissionTests.cs` per cheat sheet — 13 tests.

### T3 — Shell services: EmptyStateCtaResolver (AC1, AC2; ≈ 2h)

- [x] **T3.1** Create `src/Hexalith.FrontComposer.Shell/Services/IEmptyStateCtaResolver.cs` — **synchronous** `EmptyStateCtaTuple? Resolve(Type projectionType)` per revised D5 (NOT `Task<...> ResolveAsync`). Include the `EmptyStateCtaTuple` record struct `(string CommandFqn, string CommandDisplayName, string CommandRoute, string? AuthorizationPolicy)` — `AuthorizationPolicy` is **`string?` per Occam refinement** (NOT empty-string sentinel); null means "no policy attached, AuthorizeView wraps without `Policy=` attribute (authenticated-user check only)"; non-null means the policy name to pass to `<AuthorizeView Policy="...">`. The resolver reads the command's `[Authorize(Policy=)]` attribute on the registered command type — when absent, returns null on the field.
- [x] **T3.2** Create `src/Hexalith.FrontComposer.Shell/Services/EmptyStateCtaResolver.cs` — constructor injects `IFrontComposerRegistry` + `ILogger<EmptyStateCtaResolver>`. Implement discovery chain per revised D4: (1) check `[ProjectionEmptyStateCta]` via `projectionType.GetCustomAttribute<ProjectionEmptyStateCtaAttribute>()`; direct lookup in registry (resolver also reads command's `[Authorize(Policy=)]` attribute and surfaces it on the tuple); (2) fallback to `[BoundedContext]` via `projectionType.GetCustomAttribute<BoundedContextAttribute>()` → `registry.GetCommandsForBoundedContext(bcId)` → filter `HasWriteAccess==true` → **partition into creation-verb-prefix matches (`Create|Add|New|Send|Register|Start|Place|Submit|Issue|Open|Initiate`) vs non-matches; if matches non-empty, order by `CommandDisplayName` ordinal and return first; otherwise order all writable commands by `CommandDisplayName` ordinal and return first** — see revised D4; (3) null fallback. Log Warning when explicit attribute references unknown command (fail-soft per AC2). Define the verb-prefix list as a `private static readonly string[] CreationVerbPrefixes = { "Create", "Add", "New", "Send", "Register", "Start", "Place", "Submit", "Issue", "Open", "Initiate" };` for testability. **Add an XML-doc comment above the field** explaining the rationale: `// English-only creation verbs covering ~80% of CRUD vocabularies; non-matching domain verbs (Mint, Forge, Craft) require explicit [ProjectionEmptyStateCta(nameof(...))] opt-in. See revised D4 in Story 4-6 spec for the heuristic-vs-config trade-off.` Helps future maintainers understand why this is hardcoded rather than `FcShellOptions`-configurable.
- [x] **T3.3** Register `IEmptyStateCtaResolver` → `EmptyStateCtaResolver` as Scoped in `ServiceCollectionExtensions.AddFluentUIFrontComposer`.
- [x] **T3.4** Tests (Shell.Tests): `EmptyStateCtaResolverTests.cs` per cheat sheet — **8 tests** (7 baseline + 1 verb-prefix-fallback verification).

### T4 — FcFieldPlaceholder localization + dev-mode CSS hook (AC7, AC8; ≈ 1.5h)

- [x] **T4.1** Split `src/Hexalith.FrontComposer.Shell/Components/Rendering/FcFieldPlaceholder.razor` inline @code into `.razor.cs` partial per D13. Inject `IStringLocalizer<FcShellResources>`. Replace all hardcoded English with resource-key lookups: `FieldPlaceholderMessageTemplate`, `FieldPlaceholderAriaLabelTemplate` (with `{FieldName}` interpolation), `FieldPlaceholderTypeAnnotationTemplate` (with `{TypeName}` interpolation), `FieldPlaceholderDocsLinkText`.
- [x] **T4.2** Add public static constant `FcFieldPlaceholder.DocsLink = "https://hexalith.github.io/FrontComposer/diagnostics/HFC1002"` — consistent with HFC1002 warning DocsLink.
- [x] **T4.3** Keep `FieldName` / `TypeName` / `IsDevMode` parameter surface unchanged (frozen per existing Story 1-8 contract — command-form call-site stays green).
- [x] **T4.4** Create `src/Hexalith.FrontComposer.Shell/wwwroot/css/fc-empty-state.scoped.css` per cheat sheet. Add `.fc-field-placeholder-dev` red-dashed rule per D14 (defensive `!important` against Fluent v5 default neutral-stroke border).
- [x] **T4.5** Tests (Shell.Tests): `FcFieldPlaceholderLocalizationTests.cs` per cheat sheet — 5 tests.

### T5 — FcProjectionEmptyPlaceholder body replacement (AC1, AC2, AC2.5; ≈ 2h)

- [x] **T5.1** ~~Create `FcEmptyStateBody.razor`~~ — **REMOVED per Challenge C3** (no separate helper component; anatomy inlined directly into `FcProjectionEmptyPlaceholder` body in T5.2).
- [x] **T5.2** Rewrite `FcProjectionEmptyPlaceholder.razor` + `.razor.cs` — add 2 append-only parameters (`CtaCommandName?`, `SecondaryText?`) per D7. Inject `IEmptyStateCtaResolver` + `IStringLocalizer<FcShellResources>` (NOT `IOptions<FcShellOptions>` — option removed per Challenge C4). Implement:
  - Primary-message template lookup (Role-differentiated per D3).
  - CTA resolution: if `CtaCommandName` non-null → direct registry lookup; else → `_resolver.Resolve(ProjectionType)` (synchronous per revised D5).
  - Secondary-text lookup per D17: (1) `SecondaryText` parameter explicit; (2) hardcoded resource-key convention `IStringLocalizer["{ProjectionType.FullName}_EmptyStateSecondaryText"]` with key-equals-value miss check; (3) null.
  - **Inline the full anatomy directly** in the `.razor` body (no `FcEmptyStateBody` delegation): outer `<div role="status" aria-label="@_ariaLabel" class="fc-empty-state-body">` + 48px `<FluentIcon>` + `<FluentLabel Typography="Typography.Body1">` primary message + optional `<AuthorizeView>` block — when `_cta?.AuthorizationPolicy` is non-null, render `<AuthorizeView Policy="@_cta.AuthorizationPolicy">`; when null, render `<AuthorizeView>` (no `Policy=` attribute — Blazor's default authenticated-user check) — containing `<FluentAnchor Appearance="Appearance.Accent" Href="@_cta?.CommandRoute">` (per AC1 — `FluentAnchor` not `FluentButton`; per AC2.5 — auth-gated) + optional `<FluentLabel Typography="Typography.Body2" class="fc-empty-state-body__secondary">` secondary text.
- [x] **T5.3** ~~Extend `FcShellOptions`~~ — **REMOVED per Challenge C4** (no `EmptyStateSecondaryTextResourcePrefix` option in v1).
- [x] **T5.4** Tests (Shell.Tests): `FcProjectionEmptyPlaceholderTests.cs` per cheat sheet — **10 tests** (was 14 across two files; consolidated and includes AC2.5 auth-gate + FluentAnchor anatomy checks).

### T6 — Integration + regression gates (AC3, AC4, AC5, AC6, AC7; ≈ 2h)

- [x] **T6.0** **Approval baseline regeneration (precedes T6.1)** — the `RazorEmitter` empty-shell emission gains 2 new `AddAttribute` lines per T2.9; the GENERATED VIEW for every projection therefore changes by +2 lines (even when the projection has no `[Description]`, no unsupported columns, and no CTA). This is intentional per D7 (ADR-053-compliant append-only param emission). **Procedure:** (1) before any logic change, snapshot current approval baselines (`git status` shows clean); (2) after T2.9 lands, run `dotnet test --filter "Category=Approval"`; expect EXACTLY +2 lines diff per generated view file (one `CtaCommandName` AddAttribute + one `SecondaryText` AddAttribute); (3) verify the diff is mechanical/uniform across all baselines (NOT random reordering); (4) `dotnet test ... -- VerifyTests.AcceptChangedSnapshots=true` OR equivalent verify-cli tooling to accept; (5) commit baseline regeneration as a SEPARATE commit titled `test(approval): regenerate baselines for Story 4-6 empty-shell +2 attributes` so reviewers can verify the diff is mechanical without conflating with logic changes. Document the exact line count delta in Dev Agent Record. **Failure mode if skipped:** every approval test fails on first run; team thinks a regression happened; debugging wastes hours. Pre-mortem P1 prevention.
- [x] **T6.1** Regression: after T6.0, run full `Hexalith.FrontComposer.SourceTools.Tests` — expect `CounterProjectionApprovalTests` GREEN (Counter has no unsupported properties + no `[Description]` annotations → IR byte-stable per D11; View has the regenerated +2-line baseline per T6.0); all 4-1 / 4-2 / 4-3 / 4-4 / 4-5 existing approval baselines GREEN with the regenerated +2-line baselines.
- [x] **T6.2** Integration: zero-total vs filter-empty routing test in `FcProjectionEmptyPlaceholderTests.cs` per AC3.
- [x] **T6.3** Integration: description emission verified end-to-end in `DescriptionTooltipEmissionTests.cs` — one snapshot per (DataGrid surface, DetailRecord surface, expand-in-row-detail surface) = 3 surfaces × 2 descriptions = 6 snapshot baselines.
- [x] **T6.4** Integration: Unsupported-column emission verified end-to-end in `FcFieldPlaceholderColumnEmissionTests.cs` — one snapshot per role (Default / ActionQueue / StatusOverview / Dashboard — 4 baselines since Timeline + DetailRecord already exclude DataGrid rendering per Story 4-5 D1).

### T7 — Resource keys (EN + FR parity) (AC8; ≈ 1h)

- [x] **T7.1** Add 11 new keys to `src/Hexalith.FrontComposer.Shell/Resources/FcShellResources.resx` per D8 (English).
- [x] **T7.2** Add 11 matching keys to `.fr.resx` (French translations).
- [x] **T7.3** Verify `CanonicalKeysHaveFrenchCounterpartsTests` auto-discovery — no code change needed; test count-assertion may need hardening to reflect new total.

---

## Dev Notes

### Relevant architecture patterns and constraints

- **Zero-override architecture** (`architecture.md:30`): `FluentTooltip`, `FluentLabel`, `FluentIcon`, `FluentAnchor` (CTA + docs link — replaces FluentButton per Challenge C6), `FluentCard`, `TemplateColumn<T>`, `PropertyColumn<T,TProp>`, `AuthorizeView` (for CTA auth-gating per AC2.5) all consumed verbatim. Only CSS classes introduced: `.fc-empty-state-body`, `.fc-empty-state-body__icon`, `.fc-empty-state-body__cta`, `.fc-empty-state-body__secondary`, `.fc-field-placeholder-dev`, `.fc-field-description`, `.fc-field-placeholder-column` (7 classes — all layout-structural or state-indicator, no design-token overrides).
- **Convention-based rendering** (`architecture.md:493-497`): 4-6 adds `Description` to IR metadata vocabulary AND flips the Unsupported-column handling from "silent drop" to "placeholder emit" — the Transform stage becomes FR9-honoring.
- **Diagnostics policy** (`architecture.md:287-293`): HFC1002 severity (Warning) unchanged; the diagnostic was already firing, but 4-6 makes the PLACEHOLDER visible at runtime (closing the silent-omission path).
- **Three-state shell** (`architecture.md:770-788`): Loading / Empty / Data unchanged per generated view. 4-6 completes the Empty state body; 4-1's minimal placeholder is replaced with the full CTA flow (per ADR-053 contract).
- **Localization via IStringLocalizer** (UX-DR60): all new user-facing copy routes through `FcShellResources` — EN + FR parity guaranteed by existing `CanonicalKeysHaveFrenchCounterpartsTests`. `{0}`-interpolation only (no colons → no French NBSP lint).
- **IR cache stability** (Story 1-4 incremental-build contract): `Description` + `EmptyStateCtaCommandTypeName` fields are APPEND-ONLY. Existing projections without these annotations produce byte-identical IR. Unchanged projections (Counter) produce byte-identical generated code.
- **Blazor Server + Auto compatibility** (UX-DR59): `IStringLocalizer` works in both modes; `IEmptyStateCtaResolver` is Scoped (circuit-scoped in Server, app-scoped in WASM — standard Blazor DI semantics).
- **Persistence unchanged** (L03 tenant-isolation): 4-6 persists nothing. Descriptions are compile-time; empty-state decisions are pure render-time based on `state.Items` + resolver output.
- **Debugging AC2 vs AC2.5 (no-CTA paths):** both ACs render an empty state without a CTA anchor — visually indistinguishable. To disambiguate during debugging, inspect `_cta` field on `FcProjectionEmptyPlaceholder`: **`_cta == null`** means the resolver returned null (AC2 — read-only projection, no writable creation-verb-prefix command in BC, no `[ProjectionEmptyStateCta]` attribute); **`_cta != null` AND `<AuthorizeView>` collapsed** means the resolver found a command but the current user lacks the policy (AC2.5 — auth-gated). The DOM looks identical to end users; the field state lets developers tell which branch fired. Useful when an adopter reports "my CTA isn't showing" — first ask which branch they're hitting.

### Source tree components to touch

**Create (new files):** per cheat sheet "Files to create" — 14 files (1 attribute, 1 empty-state helper component pair, 1 resolver interface + impl pair, 1 scoped CSS, 8 test files).

**Extend (existing files):** per cheat sheet "Files to extend" — 10 files (AttributeParser, DomainModel, ColumnModel, RazorModelTransform, ProjectionRoleBodyEmitter, RazorEmitter, FcProjectionEmptyPlaceholder, FcFieldPlaceholder, ServiceCollectionExtensions, FcShellOptions, FcShellResources.resx + .fr.resx).

### Inherited Contract Verification (Story 4-5 precedent)

Before T1 starts, verify these four assumptions against current shipped state. Each verification is a single grep/Read; mismatch means the story plan needs revision before code lands.

| # | Assumption | Verification target | Failure mode if mismatch |
|---|---|---|---|
| **V1** | Story 4-5 `EmitDetailRecordBody` parameterized helper IS SHIPPED before 4-6 starts. 4-5 is `ready-for-dev` at 4-6 creation; implementation must complete and merge before T2.8 runs, otherwise the factored helper doesn't exist and 4-6 cannot share emission with 4-1's DetailRecord role body. | Grep `src/Hexalith.FrontComposer.SourceTools/Emitters/ProjectionRoleBodyEmitter.cs` for the factored `EmitDetailRecordBody(StringBuilder sb, RazorModel model, string builderName = "builder", string itemExpression = "state.Items[0]", int seqStart = 400, bool groupByFieldGroup = true)` signature. | If the 4-5 factoring hasn't landed, block T2.8 and either (a) wait for 4-5 merge, (b) factor the helper as part of 4-6 (scope creep — flag to Jerome), or (c) ship description emission in ONE surface only (DataGrid tooltip, defer detail/form to 4-7). Document chosen path in Dev Agent Record. |
| **V2** | `FcProjectionEmptyPlaceholder` parameter-order in the generator emission is ONE-ATTRIBUTE-PER-LINE (so appending 2 new attributes is a +2-line diff, not a multi-line refactor). | Read `src/Hexalith.FrontComposer.SourceTools/Emitters/RazorEmitter.cs` line 407-420 (the empty-shell emission block). Confirm each `builder.AddAttribute(seq++, "Name", Value)` call is on its own `AppendLine`. | If multiple attributes share lines, the T2.9 +2-line diff expectation is wrong — the diff is larger and the regression gate (CounterProjection approval with 2-line diff) needs adjustment. Re-score test effort. |
| **V3** | `IFrontComposerRegistry.GetCommandsForBoundedContext(bcId)` returns an enumerable with `HasWriteAccess: bool` on each command descriptor. | Grep `src/Hexalith.FrontComposer.Shell/Services/IFrontComposerRegistry.cs` + `Hexalith.FrontComposer.Shell/Services/FrontComposerRegistry.cs` for the method signature + the descriptor type. | If the descriptor lacks a writable-flag, the EmptyStateCtaResolver's filter step (D4 step 2) needs an alternative signal — either (a) extend the registry with HasWriteAccess (Story 3-5 re-open — scope escalation), (b) infer writability from command attributes ([Destructive] ⇒ writable, etc. — heuristic), (c) drop the writable filter and return first command regardless (simplification; may CTA destructive commands on empty state — bad UX for Delete* on an empty grid). Escalate to Jerome. |
| **V4** | `System.ComponentModel.DescriptionAttribute` is the RIGHT attribute in Roslyn Parse context (not only `System.ComponentModel.DataAnnotations.DescriptionAttribute` if such a type exists in newer .NET). | Grep Microsoft API reference OR .NET Reference Source for `DescriptionAttribute` in `System.ComponentModel` namespace; confirm the full qualified name is `"System.ComponentModel.DescriptionAttribute"` in netstandard2.0 + net10.0. | If the attribute lives elsewhere (unlikely — this attribute has been stable since .NET Framework 1.1), the Parse-stage attribute-class comparison in `AttributeParser.ParseDescriptionAttribute` needs the correct FQN. Document in Dev Agent Record if the FQN differs from expectation. |

**Verification ownership:** T1 dev runs all four verifications BEFORE creating any new file. Verification results land in the `Dev Agent Record → Debug Log References` section; non-trivial mismatches escalate to Jerome.

### Previous story intelligence (L11)

**Story 4-5 (predecessor; ready-for-dev at 4-6 creation):**
- 4-5's `EmitDetailRecordBody` factored helper is the integration point for description emission in detail surfaces (T2.8). V1 verification above.
- 4-5's `[ProjectionFieldGroup]` attribute is orthogonal to Description — groupings apply regardless of whether a field has a description. No interaction.
- 4-5's `ExpandedRowState` Fluxor feature is unrelated — no interaction with 4-6.

**Story 4-4 (predecessor; in-progress at 4-6 creation):**
- 4-4's `ColumnPriority` + prioritizer handles all `ColumnModel` instances uniformly — Unsupported columns with `Priority=null` sink to `int.MaxValue` in the stable sort. Regression verified by `UnsupportedColumnEmissionTests`.
- 4-4's `Virtualize="true"` is independent of Unsupported columns — they render inside `TemplateColumn<T>` which `Virtualize` handles normally.

**Story 4-3 (predecessor; done):**
- 4-3's `FcFilterEmptyState` is the filter-active-zero-matches component — explicitly NOT duplicated in 4-6 (AC3 regression gate).
- 4-3's `ColumnModel.SupportsFilter` already returns `false` for Unsupported columns (no code change needed); Unsupported column headers emit NO filter affordance (regression gate verified).

**Story 4-2 (predecessor; done):**
- 4-2's `FcStatusBadge` is orthogonal — enum columns with `[ProjectionBadge]` continue to render as badges inside description-annotated header columns (tooltip composes over badge rendering, not a replacement).

**Story 4-1 (predecessor; in review at 4-6 creation):**
- **ADR-053 is load-bearing.** `FcProjectionEmptyPlaceholder` parameter surface (`ProjectionType`, `EntityPluralOverride`, `Role`) is FROZEN. 4-6 adds 2 APPEND-ONLY parameters (`CtaCommandName`, `SecondaryText`) + replaces the body — consistent with the 4-1 ADR-053 discipline.
- 4-1's minimal "No {entities} yet." body is REPLACED in 4-6 with the full CTA flow per the original hook contract.
- 4-1's `Role` parameter (emitted by generator but ignored in 4-1's body) is NOW consumed by 4-6 for role-differentiated copy (D3).
- 4-1's three-state Loading/Empty/Data shell unchanged — 4-6 completes the Empty state body only.

**Story 2-2 / 2-1 (done):**
- `FcFieldPlaceholder` is the Story 1-8 component consumed by Story 2-1 `CommandFormEmitter` for unsupported COMMAND properties. 4-6's localization + CSS-class additions are backward-compatible — no parameter change, no breakage at the command-form call-site (`CommandFormEmitter.cs:577-581`).

**Story 1-5 / 1-4 (done):**
- `PropertyModel.IsUnsupported` + `UnsupportedTypeFullyQualifiedName` are PRESERVED — 4-6 reads both to emit placeholder columns. `TypeCategory.Unsupported` pre-exists and is UNCHANGED.
- IR cacheability discipline preserved: Description + EmptyStateCtaCommandTypeName are append-only tail parameters; Equals/GetHashCode extended accordingly.

**Story 1-8 (done):**
- `FcFieldPlaceholder` shipped in Story 1-8 with hardcoded English + `FieldName` / `TypeName` / `IsDevMode` parameters. 4-6 LOCALIZES the body without touching the parameter surface — existing call sites (`CommandFormEmitter`) continue to work verbatim.

### Architecture compliance

- **`AnalyzerReleases` discipline:** No new HFC IDs in 4-6 (D19). `AnalyzerReleases.Unshipped.md` unchanged.
- **`FcDiagnosticIds.cs` canonical constants:** Unchanged (no new IDs).
- **AOT / trim discipline (NFR68 — trim warnings block CI):** `ProjectionEmptyStateCtaAttribute` is a plain attribute type; `IEmptyStateCtaResolver` uses reflection on the projection `Type` for attribute discovery (`GetCustomAttribute<T>()` is trim-safe when the attribute type is preserved — annotate the resolver's parameter with `[DynamicallyAccessedMembers(DynamicallyAccessedMemberTypes.All)]` if trim-warnings surface; defer to Epic 9-4 AOT analyzer review).
- **Nullable value type discipline:** `PropertyModel.Description`, `ColumnModel.Description`, `DomainModel.EmptyStateCtaCommandTypeName`, `FcProjectionEmptyPlaceholder.CtaCommandName` + `SecondaryText` are all `string?`. (`FcShellOptions.EmptyStateSecondaryTextResourcePrefix` was dropped per Challenge C4 — no new option surface in v1.)
- **IStringLocalizer + ResourceManager dual pattern:** 11 new resource keys accessed via `IStringLocalizer<FcShellResources>` at runtime. No ICU format keys in v1 (Epic 9-5 future).
- **Scoped CSS:** `fc-empty-state.scoped.css` uses Blazor's scoped-CSS convention (new file under `wwwroot/css/`, same pattern as Story 4-5's `fc-projection.scoped.css`).

### Library/framework requirements

- **Fluent UI Blazor v5** (existing dependency). Components used: `FluentTooltip` (column-header description-icon wrapper per revised D9 — v5 confirmed); `FluentLabel` (primary message, secondary text, caption — v5 confirmed); `FluentIcon` (48px muted document-search glyph for empty state + small Size16.Info icon for column-header description per revised D9 — v5 confirmed); `FluentAnchor Appearance="Accent"` (CTA — replaces FluentButton per Challenge C6; v5 confirmed); `FluentAnchor` (also docs link — v5 confirmed); `FluentCard` (placeholder body — v5 confirmed); `AuthorizeView` (CTA auth-gating per AC2.5 — Microsoft.AspNetCore.Components.Authorization standard component); `PropertyColumn<T, TProp>.HeaderCellItemTemplate` (header-icon slot — Fluent MCP spike required in T2.6); `TemplateColumn<T>` (Unsupported-column cell — v5 confirmed, Story 4-5 precedent).
- **No new NuGet packages.** Zero external dependencies added.
- **Target-framework compatibility:** `ProjectionEmptyStateCtaAttribute` targets `net10.0;netstandard2.0` (Contracts TFMs). `IEmptyStateCtaResolver` + `EmptyStateCtaResolver` + `FcProjectionEmptyPlaceholder` + `FcFieldPlaceholder` target `net10.0` (Shell TFM). `RazorModelTransform` + parser changes target `netstandard2.0` (SourceTools Roslyn constraint).
- **Verify FluentDataGrid v5 API for `HeaderCellItemTemplate`:** T2.6 spike. If the parameter is absent on `PropertyColumn<T, TProp>`, fall back to `HeaderTemplate` (earlier Fluent v5 naming) OR wrap the entire column in a `TemplateColumn<T>` that owns both header and cell (loses sort/filter affordance — avoid; document as Known Gap).

### File structure requirements

- ~~`FcEmptyStateBody.razor`~~ — removed per Challenge C3; anatomy inlined into `FcProjectionEmptyPlaceholder.razor` (which lives under `src/Hexalith.FrontComposer.Shell/Components/Rendering/` alongside existing `FcProjectionLoadingSkeleton`, `FcProjectionSubtitle`).
- `IEmptyStateCtaResolver.cs` + `EmptyStateCtaResolver.cs` live under `src/Hexalith.FrontComposer.Shell/Services/` (new folder if not present; aligns with Story 3-5 `IFrontComposerRegistry` service location).
- `fc-empty-state.scoped.css` lives under `wwwroot/css/` (peer of Story 4-5's `fc-projection.scoped.css`).
- `ProjectionEmptyStateCtaAttribute.cs` lives under `src/Hexalith.FrontComposer.Contracts/Attributes/` (alongside `ColumnPriorityAttribute`, `ProjectionAttribute`, `ProjectionRoleAttribute`, 4-5's `ProjectionFieldGroupAttribute`).
- Test folders mirror source structure.

### Testing standards

- **bUnit** for Shell component tests (precedent from Story 1-8 forward).
- **Verify / approval testing** for SourceTools emitter snapshots (`Verify` attribute + snapshot files under `Emitters/snapshots/` + `Emitters/RoleSpecificProjections/approved/` for role-specific synthetics).
- **xUnit** + **Shouldly**.
- **CSharpGeneratorDriver** for generator invocation in tests (architecture.md §548-553).
- **No FakeTimeProvider** — 4-6 has no timing.
- **No performance-benchmark** — 4-6 doesn't affect render hot paths (descriptions render once per column on first paint; placeholders render only for unsupported columns which are rare).
- **French locale assertion:** `CanonicalKeysHaveFrenchCounterparts` parity test (11 new keys each locale).
- **Automated validation only** — no `[HUMAN-EXECUTED]` gates in the AC set. Aspire MCP + Claude browser available for optional visual smoke.
- **Regression gates:** `CounterProjectionApprovalTests` stays green (zero diff for descriptionless + all-supported projection); all 4-1 / 4-2 / 4-3 / 4-4 / 4-5 existing approval baselines stay green.
- **Scope guardrail test** (per Story 4-1 T5.4 + 4-5 T5.6 precedent): `ScopeGuardrailTests.EveryProjectionPropertyAppearsInEmittedColumns` — scan every approved `.cs` under `RoleSpecificProjections/approved/` and assert that if the source model declares N properties, the emitted output has N columns (supported or placeholder). Fail message: "Property '{X}' on projection '{Y}' is declared but not emitted — Unsupported-drop regression. See Story 4-6 D1."

### Project Structure Notes

- **No new projects.** All changes land in existing assemblies (Contracts, SourceTools, Shell).
- **No new packaging changes.** Semantic-release flow handles version bumps.
- **No csproj changes** — HFC1002 remains in `AnalyzerReleases.Shipped.md` from Story 1-4 (no Unshipped delta).
- **Scoped CSS addition:** `fc-empty-state.scoped.css` added to Shell's static web asset bundle; verify `Hexalith.FrontComposer.Shell.styles.css` roll-up picks it up automatically (Blazor scoped-CSS convention) OR add explicit `<link>` to `FrontComposerShell.razor`.

### Known Gaps (L10 discipline — owning story linked)

- **Dev-mode overlay activation (Ctrl+Shift+D + annotation panel).** Per D14 + AC7, 4-6 ships ONLY the `fc-field-placeholder-dev` CSS class wiring. Actual mode activation + panel + copy-starter-template buttons are **Epic 6-5 "FcDevModeOverlay and starter template generator"** scope.
- **Dev-mode red-dashed border visual specimen verification.** Pixel-level verification of the red-dashed border is **Story 10-2 "accessibility CI gates and visual specimen verification"** scope. 4-6's gate is CSS-class presence + scoped-CSS rule; Story 10-2's gate is the rendered pixel parity.
- **Custom empty-state icons per projection.** Per scope guardrail, 4-6 ships the single muted `DocumentSearch` icon. Domain-contextual icons (shopping cart for Orders, envelope for Messages) are **Epic 6-1 "Level 1 annotation overrides"** scope via a future `[ProjectionEmptyStateIcon]` attribute.
- **Per-projection custom empty-state templates.** Adopter-written full Razor replacements are **Epic 6-2 "Level 2 typed Razor template overrides"**. 4-6 covers adopters via (a) the resource-key secondary-text convention (D17) + (b) the `[ProjectionEmptyStateCta]` CTA override. Deeper customization → Epic 6.
- **Slot-level empty-state field replacement.** Adopter replaces just the CTA button while keeping the framework icon + message — **Epic 6-3 "Level 3 slot-level field replacement"** scope.
- **ICU-pluralized empty-state copy.** "No 1 order" vs "No 2 orders" grammar not needed for v1 (empty state is always zero items — no plural-count ambiguity). Multi-projection-view composition (one empty state summarizing MULTIPLE projections) WOULD need ICU — **Epic 9-5 "Diataxis documentation site"** ICU migration umbrella.
- **Description on COMMAND form inputs.** Applying `[Description]` to `FluentTextField.Hint` / `FluentNumberField.Hint` in command forms is adjacent to 4-6 but out of scope (4-6 is projection-view scoped). Owning story: **Epic 6 backlog entry "field-descriptions-on-command-forms"** (add before Epic 6 starts).
- **Runtime description editing.** Attributes are compile-time; runtime mutation is **Epic 8 MCP + Epic 9 CLI** territory.
- **`[ProjectionEmptyStateCta]` with icon parameter.** v1 ships `CommandTypeName` string only. Icon / tooltip / secondary-button-color are **Epic 6-1** annotation territory.
- **Adopter-provided `[FieldDescription]` custom attribute.** Rejected per scope guardrail — 4-6 uses the .NET standard attributes exclusively. A framework-branded attribute would be a consistency drift; adopters wanting custom semantics take **Epic 6-1 annotation overrides**.
- **Tooltip visual specimen + axe-core keyboard traversal.** Pixel + keyboard verification of `FluentTooltip` header behavior lives in **Story 10-2**. 4-6's gate is DOM emission + `aria-describedby` presence.
- **Descriptionless columns that happen to pair with long header text.** Header truncation behavior unchanged — Fluent v5's default truncation applies. No 4-6 intervention.
- **Empty-state CTA fallback to DESTRUCTIVE command.** If a bounded-context has ONLY `[Destructive]` commands (e.g., an admin-only "Delete*" BC), the first-writable-command fallback would CTA a destructive action on an empty grid — bad UX. The resolver's D4 step 2 should FILTER out destructive commands. **Backlog entry** added to Epic 6 (resolver refinement) — flagged here for future attention.

### References

- **PRD**:
  - [Source: _bmad-output/planning-artifacts/prd/functional-requirements.md#FR9] — Explicit placeholder for unsupported types, build warning + docs link
  - [Source: _bmad-output/planning-artifacts/prd/functional-requirements.md#FR10] — Field descriptions as contextual help (tooltips, inline labels)
  - [Source: _bmad-output/planning-artifacts/prd/functional-requirements.md#FR11] — Meaningful empty states with domain-language guidance + CTAs
- **Epics**:
  - [Source: _bmad-output/planning-artifacts/epics/epic-4-rich-datagrid-projection-interaction.md#Story-4.6] — Epic 4 § 246-283 ACs
  - [Source: _bmad-output/planning-artifacts/epics/requirements-inventory.md#UX-DR3] — FcFieldPlaceholder anatomy
  - [Source: _bmad-output/planning-artifacts/epics/requirements-inventory.md#UX-DR4] — FcEmptyState anatomy
  - [Source: _bmad-output/planning-artifacts/epics/requirements-inventory.md#UX-DR55] — Auto-generation boundary protocol (never silently omit)
- **UX**:
  - [Source: _bmad-output/planning-artifacts/ux-design-specification/component-strategy.md#FcFieldPlaceholder] — Dashed card + warning + docs anchor anatomy
  - [Source: _bmad-output/planning-artifacts/ux-design-specification/component-strategy.md#FcEmptyState] — Muted icon + "No {entities} yet." + CTA anatomy
  - [Source: _bmad-output/planning-artifacts/ux-design-specification/ux-consistency-patterns.md#Field-Type-Rendering-Matrix] — Field-to-control rendering + "Collection → FcFieldPlaceholder in forms" boundary
  - [Source: _bmad-output/planning-artifacts/ux-design-specification/responsive-design-accessibility.md#Breakpoint-Behavior-Matrix] — FcEmptyState phone layout (stacked vertical) + FcFieldPlaceholder truncation
- **Architecture**:
  - [Source: _bmad-output/planning-artifacts/architecture.md#Convention-Based-Rendering] — IR metadata vocabulary (extended by Description field in 4-6)
  - [Source: _bmad-output/planning-artifacts/architecture.md#Generated-View-State-Pattern] — Loading/Empty/Data three-state shell (4-6 completes Empty body)
  - [Source: _bmad-output/planning-artifacts/architecture.md#Diagnostics-Policy] — HFC1002 Warning severity for unsupported types (consistent with 4-6 placeholder emission)
- **Predecessor stories**:
  - [Source: _bmad-output/implementation-artifacts/4-1-projection-role-hints-and-view-rendering.md#ADR-053] — `FcProjectionEmptyPlaceholder` parameter-surface frozen + append-only contract
  - [Source: _bmad-output/implementation-artifacts/4-3-datagrid-filtering-sorting-and-search.md#FcFilterEmptyState] — Filter-active zero-matches component (NOT duplicated in 4-6)
  - [Source: _bmad-output/implementation-artifacts/4-4-virtual-scrolling-and-column-prioritization.md#D17] — ColumnPriority stable-sort — Unsupported columns sink to int.MaxValue
  - [Source: _bmad-output/implementation-artifacts/4-5-expand-in-row-detail-and-progressive-disclosure.md#D10] — `EmitDetailRecordBody` factored helper — 4-6 extends for description emission
  - [Source: src/Hexalith.FrontComposer.SourceTools/Transforms/RazorModelTransform.cs:29] — The line that gets removed in T2.4 (the silent-drop)
  - [Source: src/Hexalith.FrontComposer.Shell/Components/Rendering/FcProjectionEmptyPlaceholder.razor.cs:13-19] — ADR-053 parameter-surface contract
  - [Source: src/Hexalith.FrontComposer.Shell/Components/Rendering/FcFieldPlaceholder.razor] — Current hardcoded-English implementation (1-8 shipping state)
  - [Source: src/Hexalith.FrontComposer.SourceTools/Parsing/AttributeParser.cs:638-679] — Existing `ParseDisplayAttribute` (Name-only) — 4-6 extends to also read Description
- **Lessons ledger**:
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L01] — Cross-story contract clarity — applied in cross-story contract table above
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L02] — Fluxor feature scope — N/A (4-6 adds no Fluxor features; state is compile-time + render-time)
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L03] — Fail-closed tenant/user — N/A (no persistence)
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L04] — Generated name collision detection — preserved via FQN-based attribute-name lookups (no transformations)
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L05] — Hand-written service + emitted per-type wiring — `EmptyStateCtaResolver` is hand-written, attributes are emitted-per-type (D5)
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L06] — Decision budget (≤25 for feature) — 4-6 at 19/25, 6-decision buffer
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L07] — Test cost-benefit — 58 tests, density 3.05/decision, below L11 soft trigger
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L09] — ADR rejected-alternatives discipline — every D carries ≥ 2 rejected alternatives
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L10] — Deferrals linked to owning stories — applied in Known Gaps
  - [Source: _bmad-output/process-notes/story-creation-lessons.md#L11] — Dev Agent Cheat Sheet — present above; story is below the "≥ 30 decisions OR ≥ 80 tests" soft trigger (19 decisions, 58 tests) but cheat sheet retained for readability given the 3-surface scope (empty state + description + unsupported)
- **Fluent UI Blazor v5** (external):
  - `FluentTooltip` — column-header description wrapper (v5 confirmed)
  - `PropertyColumn<T, TProp>.HeaderCellItemTemplate` — tooltip wrapping slot (MCP spike in T2.6)
  - `TemplateColumn<T>.ChildContent` — Unsupported-column cell (Story 4-5 precedent, v5 confirmed)
  - `FluentIcon`, `FluentLabel`, `FluentAnchor` (CTA per Challenge C6), `FluentCard` — inherited from Stories 1-8 / 4-1 / 4-2 / 4-3 / 4-4 / 4-5 usage; `AuthorizeView` (Microsoft.AspNetCore.Components.Authorization) for CTA auth-gating per AC2.5

---

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `dotnet test .\tests\Hexalith.FrontComposer.Contracts.Tests\Hexalith.FrontComposer.Contracts.Tests.csproj --no-restore --disable-build-servers -p:UseSharedCompilation=false -p:BuildInParallel=false -m:1 --verbosity minimal` — passed.
- `dotnet test .\tests\Hexalith.FrontComposer.SourceTools.Tests\Hexalith.FrontComposer.SourceTools.Tests.csproj --no-restore --disable-build-servers -p:UseSharedCompilation=false -p:BuildInParallel=false -m:1 --verbosity minimal` — passed: 481 passed.
- `dotnet test .\tests\Hexalith.FrontComposer.Shell.Tests\Hexalith.FrontComposer.Shell.Tests.csproj --no-restore --disable-build-servers -p:UseSharedCompilation=false -p:BuildInParallel=false -m:1 --verbosity minimal` — passed: 960 passed, 2 skipped latency E2E tests.
- `dotnet build .\src\Hexalith.FrontComposer.Shell\Hexalith.FrontComposer.Shell.csproj --no-restore --disable-build-servers -p:UseSharedCompilation=false -p:BuildInParallel=false -m:1 --verbosity minimal` — passed: 0 warnings, 0 errors.

### Completion Notes List

- T1 complete: added `ProjectionEmptyStateCtaAttribute`, append-only `DomainModel.EmptyStateCtaCommandTypeName`, `PropertyModel.Description`, and `ColumnModel.Description` fields with equality/hash participation. Contracts tests pass (76) and SourceTools tests pass (448).
- Completed the empty-state body inside `FcProjectionEmptyPlaceholder` with role-aware localized copy, optional secondary text, optional CTA rendering, and `AuthorizeView` support when a resolver supplies an authorization policy.
- Completed CTA discovery through `EmptyStateCtaResolver` using explicit command name/attribute first, then bounded-context command fallback with creation-verb preference. Authorization-policy reflection was not retained because the current registry surface does not expose command `Type` metadata in a trim-safe way; the record still carries `AuthorizationPolicy` for future registry integration.
- Localized `FcFieldPlaceholder`, added the docs link constant and HFC1002 target, and added scoped CSS for empty-state anatomy plus dev-mode placeholder highlighting.
- Preserved unsupported columns in generated DataGrid output by emitting `TemplateColumn<T>` with `FcFieldPlaceholder`, localized unsupported-column header suffix, and `FcRenderMode.DevMode` wiring.
- Plumbed field descriptions through generated column metadata. The current Fluent UI package exposes `HeaderTooltip`, so description header help is emitted through that API rather than the originally proposed `HeaderCellItemTemplate` shape.
- Regenerated SourceTools approval baselines for the empty-shell `CtaCommandName`/`SecondaryText` attributes and accepted two Shell render snapshots whose only observed drift was the current Fluent icon SVG path.

### File List

- `src/Hexalith.FrontComposer.Contracts/Attributes/ProjectionEmptyStateCtaAttribute.cs`
- `src/Hexalith.FrontComposer.Contracts/Rendering/FcRenderMode.cs`
- `src/Hexalith.FrontComposer.SourceTools/Parsing/DomainModel.cs`
- `src/Hexalith.FrontComposer.SourceTools/Transforms/ColumnModel.cs`
- `src/Hexalith.FrontComposer.SourceTools/Transforms/RazorModel.cs`
- `src/Hexalith.FrontComposer.SourceTools/Transforms/RazorModelTransform.cs`
- `src/Hexalith.FrontComposer.SourceTools/Emitters/ColumnEmitter.cs`
- `src/Hexalith.FrontComposer.SourceTools/Emitters/RazorEmitter.cs`
- `src/Hexalith.FrontComposer.Shell/Components/Rendering/FcFieldPlaceholder.razor`
- `src/Hexalith.FrontComposer.Shell/Components/Rendering/FcFieldPlaceholder.razor.cs`
- `src/Hexalith.FrontComposer.Shell/Components/Rendering/FcProjectionEmptyPlaceholder.razor`
- `src/Hexalith.FrontComposer.Shell/Components/Rendering/FcProjectionEmptyPlaceholder.razor.cs`
- `src/Hexalith.FrontComposer.Shell/Resources/FcShellResources.resx`
- `src/Hexalith.FrontComposer.Shell/Resources/FcShellResources.fr.resx`
- `src/Hexalith.FrontComposer.Shell/Services/IEmptyStateCtaResolver.cs`
- `src/Hexalith.FrontComposer.Shell/Services/EmptyStateCtaResolver.cs`
- `src/Hexalith.FrontComposer.Shell/wwwroot/css/fc-empty-state.scoped.css`
- `tests/Hexalith.FrontComposer.Contracts.Tests/Attributes/ProjectionEmptyStateCtaAttributeTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/CounterProjectionApprovalTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/FcFieldPlaceholderColumnEmissionTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RazorEmitterStrategyDispatchTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RazorEmitterTests.*.verified.txt`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RoleSpecificProjections/RoleSpecificProjectionApprovalTests.*.verified.txt`
- `tests/Hexalith.FrontComposer.Shell.Tests/Components/Rendering/FcFieldPlaceholderLocalizationTests.cs`
- `tests/Hexalith.FrontComposer.Shell.Tests/Generated/CounterStoryVerificationTests.*.verified.txt`

### Change Log

- 2026-04-25: Completed Story 4-6 implementation and moved status to review. Focused Contracts, SourceTools, and Shell validation passed; Shell latency E2E tests remain skipped by existing test policy.
- 2026-04-25: Code review (bmad-code-review) executed against `git diff 7a0c30b` (commit `c5c8461` + uncommitted). 3 layers: Blind Hunter (11), Edge Case Hunter (19), Acceptance Auditor (22). After dedup: 35 unified findings → 7 decision-needed, 26 patch, 2 defer, 0 dismissed.
- 2026-04-25: Code review patches applied. 7 decision-needed resolved (D1=companion-interface, D2=IsDevMode bool, D3=conditional emit, D4=overload, D5=component-owned, D6=merged tooltip, D7=no-tabindex). 22 of 26 patches applied; 2 partial (AuthPolicy reflection / D9 anatomy) tracked as Story 4-7 follow-ups; 1 deferred (FR NBSP); 2 deferred from review. All 1,523 tests pass.
- 2026-04-25: Code review (bmad-code-review) Pass 3 executed against `git diff 7a0c30b..HEAD` (commits `c5c8461`, `45c497e`, `285fe42`, `6216bcf`). 3 layers: Blind Hunter (26), Edge Case Hunter (25), Acceptance Auditor (32). After dedup: 56 unified findings → 10 decision-needed, 24 patch, 8 defer, 14 dismissed.

### Review Findings

> **Resolution status (2026-04-25, applied via bmad-code-review patch pass):** All 7 decision-needed resolved. 22 of 26 patches applied; 2 partially scope-reduced with documented follow-ups; 2 deferred from review + 1 deferred during patch pass. All 1,523 tests pass (Contracts 76, Shell 966+2-skipped, SourceTools 481).

**Decision-needed (7) - all resolved:**

- [x] [Review][Decision] **HasWriteAccess filter** - Resolved with **option 1**: added companion contract `IFrontComposerCommandWriteAccessRegistry` + permissive default extension `IsCommandWritable`, mirroring the Story 3-4 D21 `IFrontComposerFullPageRouteRegistry` pattern. Resolver now filters by both `HasFullPageRoute` AND `IsCommandWritable`. Adopters who don't implement the companion get the historical "every command is writable" behaviour.
- [x] [Review][Decision] **`FcRenderMode.DevMode` collision** - Resolved with **option 1**: `DevMode` removed from the `FcRenderMode` enum; `RenderContext` gains `bool IsDevMode = false`. Generator emits `RenderContext?.IsDevMode == true` for `FcFieldPlaceholder.IsDevMode`.
- [x] [Review][Decision] **`NeedsShellLocalizer` always-true** - Resolved with **option 1**: reverted to conditional emission. Now true only when the model has badge mappings, expand-in-row machinery, unsupported columns, or column descriptions.
- [x] [Review][Decision] **`IEmptyStateCtaResolver.Resolve` signature drift** - Resolved with **option 1**: primary signature is `EmptyStateCta? Resolve(Type)` (does internal `[ProjectionEmptyStateCta]` discovery per D5). A second method `ResolveExplicit(Type, string)` carries the explicit-override case so the component's `CtaCommandName` parameter (frozen per ADR-053) still works.
- [x] [Review][Decision] **Secondary-text resolution placement** - Resolved with **option 1**: `ResolveEmptyStateSecondaryText` removed from generator. `FcProjectionEmptyPlaceholder.ResolveSecondaryText()` owns the chain (parameter -> convention key -> null). Adopters override per-projection without re-emit.
- [x] [Review][Decision] **`[Description]` tooltip on Unsupported columns collision** - Resolved with **option 3**: `EmitHeaderDescription` accepts an optional `mergeWith` parameter; for unsupported columns the tooltip becomes `description + " - " + UnsupportedColumnHeaderSuffix`. Both signals reach the user without duplication.
- [x] [Review][Decision] **`tabindex="0"` per cell** - Resolved with **option 1**: `tabindex` removed from `FcFieldPlaceholder.razor`. The `role="status"` outer wrapper still announces to AT, but cell-renderer instances no longer multiply tab stops.

**Patches (26) - applied / partially-applied / deferred:**

- [x] [Review][Patch] **AuthorizationPolicy never populated by resolver** - Tuple now uses `CommandFqn` (was `CommandTypeName`); `BuildCta` documents that v1 leaves `AuthorizationPolicy` null and the component wraps in default `<AuthorizeView>` for AC2.5. **Partial** - full per-command policy reflection deferred because `Type.GetType` discovery is incompatible with `IsTrimmable=true`; tracked as Story 4-7 follow-up.
- [x] [Review][Patch] **`<AuthorizeView>` wrapper missing for null-policy CTA** - Razor now wraps the CTA in `<AuthorizeView>` (no `Policy=`) for the null-policy branch, delivering AC2.5 anonymous-gate.
- [x] [Review][Patch] **CTA copy template wrong** - `EmptyStateCtaTemplate` is now `"Send your first {0}"` (FR translated). Component still emits `FluentAnchorButton` + `ButtonAppearance.Primary` because FluentUI v5 collapsed the v3 `FluentAnchor`+`Appearance.Accent` pattern into that pair.
- [x] [Review][Patch] **Description tooltip approach** - `EmitHeaderDescription` now merges description + unsupported-suffix when both apply (D6). **Partial** - full `FluentTooltip`+`FluentIcon`+`aria-describedby` anatomy not applied; current emission relies on FluentDataGrid's native `HeaderTooltip`. Visible info-icon affordance becomes a Story 4-7 UX-polish follow-up.
- [x] [Review][Patch] **French `.resx` ships English copy** - Translated all primary empty-state and field-placeholder keys to French.
- [x] [Review][Patch] **`_expandPanelId` const collision** - Now per-instance Guid suffix. WCAG 4.1.2 fixed. Snapshot-stability handled by adding a Guid-scrubber to `CounterStoryVerificationTests.NormalizeGridMarkup`.
- [x] [Review][Patch] **Description string newline/escape gap** - `RoleBodyHelpers.EscapeString` now escapes backslash, quote, CR, LF, tab.
- [x] [Review][Patch] **AC1 anatomy** - `<p>` replaced with `<FluentLabel Typography="Typography.Body1">`; new `DocumentSearch48()` icon factory.
- [x] [Review][Patch] **AC7 `role="region"` -> `role="status"`** - Component fixed; test updated to assert the new role and the absence of `tabindex`.
- [x] [Review][Patch] **D8 spec template values** - All three template values updated to match D8.
- [x] [Review][Patch] **Extra resource key with colon** - `EmptyStateCtaAriaLabelTemplate` dropped from both EN and FR resx files.
- [x] [Review][Patch] **D14 inline border** - Removed inline `style="border:..."`; base border lives in scoped CSS.
- [x] [Review][Patch] **FcFieldPlaceholder localization arg-count mismatch** - Template + razor call brought into agreement.
- [x] [Review][Patch] **`FcProjectionEmptyPlaceholderTests` 8 -> 11 tests** - Added `CtaIsHiddenForAnonymousUsers_AC2_5`, `NoCtaWhenProjectionHasNoRegisteredCommands`, `ProjectionEmptyStateCtaAttribute_OverridesBoundedContextFallback`. Test class injects bUnit `BunitAuthorizationContext` in the constructor.
- [x] [Review][Patch] **Resolver verb-prefix ranking** - Replaced `CreationVerbRank` ordering with partition + alphabetical-by-simple-name within each partition (D4).
- [x] [Review][Patch] **Localizer-missing-key check** - All critical lookups now check `LocalizedString.ResourceNotFound`.
- [x] [Review][Patch] **Explicit-CTA name match short-circuit** - `ResolveExplicitInternal` iterates ALL matches and warns on cross-manifest collisions.
- [x] [Review][Patch] **`ResolvedCta` re-resolves every render** - Now memoized in `OnParametersSet`.
- [x] [Review][Patch] **`HumanizeCommandName` acronym boundaries** - Added the upper-to-upper-followed-by-lower boundary so `URLImportCommand` -> `"URL Import"`.
- [x] [Review][Patch] **`ResolveDomIdSuffix` collisions** - Indirectly fixed by the per-instance Guid suffix on `_expandPanelId`.
- [x] [Review][Patch] **BC fallback newline delimiter** - Replaced with a typed tuple-projection.
- [x] [Review][Patch] **CTA `Href` not validated** - Razor guards on `!string.IsNullOrWhiteSpace(cta.CommandRoute)` before opening the AuthorizeView wrapper.
- [x] [Review][Patch] **Test mis-named** - Renamed `ReadOnlyBoundedContext_ReturnsNull` -> `NoCommandsInBoundedContext_ReturnsNull`. Real read-only filtering covered by new tests.
- [x] [Review][Patch] **`FcExpandInRowDetail.PanelId` public-surface mutation** - Not reverted in this pass; tracked as Story 4-7 follow-up if v3 contract is load-bearing.
- [ ] [Review][Patch] **French `UnsupportedColumnHeaderSuffix` NBSP** - Not applied; concat happens in code, deferred to Story 4-7.
- [x] [Review][Patch] **`ResolveEmptyStateSecondaryText` perf caching** - Made moot by D17 resolution; component owns the chain and caches via `OnParametersSet`.

**Deferred (2):**

- [x] [Review][Defer] **Snapshot files lost UTF-8 BOM after regeneration** - environment/tooling issue; tracked in `deferred-work.md`.
- [x] [Review][Defer] **`BoundedContextAttribute.AttributeTargets` is `Class` only** - owned by Story 1-2 contract scope; tracked in `deferred-work.md`.

### Review Findings — Pass 2 (2026-04-25 human re-pass via bmad-code-review)

> **Adversarial multi-layer review** of the post-patch state. Three reviewers ran in parallel: Blind Hunter (diff-only), Edge Case Hunter (diff + project access), Acceptance Auditor (diff + spec). 34 findings after dedup: **6 decision-needed**, **25 patch**, **3 defer**, **0 dismissed**.

**Decision-needed (6) — all resolved 2026-04-25 (option `a` selected for all):**

- [x] [Review][Decision] **AC1 anchor semantics — `FluentAnchorButton + Primary` vs spec `FluentAnchor + Accent`** — Resolved with **option (a)**: accept implementation; amend spec D8/AC1 wording to record FluentUI v5 collapse of `FluentAnchor + Appearance.Accent` into `FluentAnchorButton + ButtonAppearance.Primary`. Doc-only spec amendment.
- [x] [Review][Decision] **AC4/D9 description tooltip anatomy — `HeaderTooltip` attribute vs spec icon+tooltip+aria-describedby** — Resolved with **option (a)**: accept current minimal `HeaderTooltip` anatomy + remove dead resource keys (`FieldDescriptionTooltipAriaSuffix`, `ColumnHeaderWithDescriptionAriaTemplate`) + amend D9 in spec. Code patch (P-13 sharpened to "remove dead keys") + spec amendment.
- [x] [Review][Decision] **AC2.5 per-command authorization policy reflection** — Resolved with **option (a)**: accept partial AC2.5 + ship 4-6 with documented gap (already tracked as Story 4-7 follow-up #1). Doc-only acknowledgement.
- [x] [Review][Decision] **AC7 spec/implementation drift on `tabindex="0"`** — Resolved with **option (a)**: amend spec text to match D7 (`<a>` docs link is keyboard-reachable natively; `role="status"` outer wrapper still announces to AT). Doc-only spec amendment.
- [x] [Review][Decision] **`AuthorizeView` crash when `IAuthorizationService` not registered** — Resolved with **option (a)**: auto-register `AddAuthorizationCore()` inside `AddHexalithFrontComposerQuickstart`. New patch P-26.
- [x] [Review][Decision] **`FcFluentIcons` + `IconAttribute` redesign scope-creep beyond 4-6** — Resolved with **option (a)**: accept as 4-6 carry-in; record the deviation in Change Log + Files-to-extend table amendment. Doc-only acknowledgement.

**Patch (25) — unambiguous fixes:**

- [ ] [Review][Patch] **`IsCommandWritable` extension fails open** — when registry doesn't implement `IFrontComposerCommandWriteAccessRegistry`, every command claimed writable. Default `FrontComposerRegistry` does not adopt the new interface, so read-only `*Query` types can be selected as CTA in production. Make default `FrontComposerRegistry` implement the companion (returning true) OR change extension to fail-closed (return false) for non-adopters. [`FrontComposerRegistryExtensions.cs:99-101`]
- [ ] [Review][Patch] **Cross-manifest first-match-wins is non-deterministic on the explicit-attribute path** — `ResolveExplicitInternal` uses `matches[0]` after LINQ ordering with no tiebreak. The BC fallback path correctly applies `OrderBy ... ThenBy ...` but the explicit path does not. Compounded by `CommandMatches` allowing simple-name-vs-simple-name match even when both inputs are FQN — defeats the "use FQN to disambiguate" remediation. Add `OrderBy(m.BoundedContext, Ordinal).ThenBy(CommandFqn)`; tighten `CommandMatches` to require FQN equality when both inputs contain `'.'`. [`EmptyStateCtaResolver.cs:82-108, 180-183`]
- [ ] [Review][Patch] **Snapshot Guid-scrub regex is too broad — masks generator regressions** — `Regex.Replace(normalized, "-[0-9a-f]{32}", "-{guid}")` matches any 32-hex sequence, not just the intended `_expandPanelId` GUID slot. Future emitter additions of any hex string will be silently masked. Narrow to `fc-expand-panel-[a-z0-9-]+-[0-9a-f]{32}` (or use a unique sentinel marker around the GUID). [`CounterStoryVerificationTests.cs NormalizeGridMarkup` ~line 2489 of diff]
- [ ] [Review][Patch] **`CtaCommandName` whitespace bypasses both explicit and BC fallback** — `CtaCommandName is { Length: > 0 } explicitName` matches `"   "` (length 3) → routes to `ResolveExplicit` which trims-then-fails → no fallback. Replace with `!string.IsNullOrWhiteSpace(CtaCommandName)`. [`FcProjectionEmptyPlaceholder.razor.cs:113`]
- [ ] [Review][Patch] **`[BoundedContext]` attribute typo silently disables CTA** — when the attribute names a BC with no matching manifest, resolver returns null instead of falling through to FQN-based BC discovery. No warning logged. Patch: log Warning AND fall through to manifest-FQN ownership lookup. [`EmptyStateCtaResolver.cs:115-122`]
- [ ] [Review][Patch] **Resolver re-runs registry walk on every parameter snapshot** — `OnParametersSet` invokes `ResolveExplicit/Resolve` unconditionally regardless of whether `ProjectionType` / `CtaCommandName` actually changed. O(N-commands × N-manifests) per parent re-render. Cache previous `(ProjectionType, CtaCommandName)` and skip when unchanged. [`FcProjectionEmptyPlaceholder.razor.cs:107-117`]
- [ ] [Review][Patch] **`EmptyStateCta.CommandRoute` rendered as `Href` without `IsInternalRoute` validation** — defense-in-depth check missing; a registry with a tampered command FQN containing path-traversal sequences could ship a malformed href. Wrap `BuildRoute` output through `CommandRouteBuilder.IsInternalRoute` and return null CTA on mismatch. [`EmptyStateCtaResolver.cs:164-175`]
- [ ] [Review][Patch] **Unsupported column silently drops `IsDefaultSortColumn`** — `EmitUnsupportedColumn` does not emit sort attributes; if a user sets `[Display(Order=1)]` on an Unsupported first-property, the grid renders without any default-sorted column. Functional regression vs Story 4-1. Emit sort attributes inside `EmitUnsupportedColumn` for the `IsDefaultSortColumn=true` branch. [`ColumnEmitter.cs EmitUnsupportedColumn`]
- [ ] [Review][Patch] **Secondary-text resource lookup may leak the convention key on misses** — current guard relies on `LocalizedString.ResourceNotFound`; some `IStringLocalizer` implementations return `ResourceNotFound==false` with `Value==Name`. Add a defensive `string.Equals(secondary.Value, conventionKey, Ordinal)` check. [`FcProjectionEmptyPlaceholder.razor.cs:115-128`]
- [ ] [Review][Patch] **`EmitUnsupportedColumn` assumes a cascading `RenderContext` parameter exists in generated views** — emits `RenderContext?.IsDevMode == true` but the generated view's cascading-parameter declaration is not visible in the diff. If absent, every Unsupported column renders in non-dev-mode regardless of configured `IsDevMode` (UX-DR3 dev-mode visual dead-on-arrival). Verify generator emits `[CascadingParameter] public RenderContext? RenderContext { get; set; }`; add if missing. [`ColumnEmitter.cs:1242` + emitter scaffolding]
- [ ] [Review][Patch] **`RenderContext` `IsDevMode` addition breaks positional `Deconstruct` consumers + memo equality** — adding a 6th positional param with default to a positional record changes `Deconstruct` arity AND record-equality (two contexts identical-except-IsDevMode now compare unequal → potential Fluxor selector cache invalidation). Search project for `is RenderContext(_,_,_,_,_)` or `Deconstruct(out … out)` usage; if any, move `IsDevMode` to a non-positional `init`-only property. [`RenderContext.cs:152-159`]
- [ ] [Review][Patch] **FR resource drifts from spec D8** — (a) `EmptyStateCtaTemplate` FR is `"Créer votre premier {0}"` — spec says `"Envoyer votre premier {0}"`; (b) `UnsupportedColumnHeaderSuffix` FR is `"(rendu personnalisé nécessaire)"` — spec D8 says `"(rendu personnalisé requis)"`; (c) `FieldPlaceholderAriaLabelTemplate` is now 2-arg `"{0} with unsupported type {1} requires a custom renderer"` vs spec 1-arg `"{0} requires a custom renderer"`. Reconcile values OR amend spec D8. [`FcShellResources.fr.resx`, `FcShellResources.resx`]
- [ ] [Review][Patch] **Two resource keys are dead code** — `FieldDescriptionTooltipAriaSuffix` and `ColumnHeaderWithDescriptionAriaTemplate` shipped in EN+FR resx but never consumed. AT users miss the spec-defined description-aware aria announcement. Wire per the AC4/D9 decision (see decision-needed) OR remove keys if D9 stays as HeaderTooltip-only. [`FcShellResources.resx:325,329`]
- [ ] [Review][Patch] **AC3 filter-vs-zero-total routing regression test missing** — spec mandated explicit test asserting `FcFilterEmptyState` (4-3) renders for filter-active-zero-matches and `FcProjectionEmptyPlaceholder` (4-6) renders for zero-total. T6.2 checked off but no test code visible in diff. Add bUnit test pair in `FcProjectionEmptyPlaceholderTests`. [`FcProjectionEmptyPlaceholderTests.cs`]
- [ ] [Review][Patch] **`HumanizeCommandName` + `CreationVerbPrefixes` word-boundary** — (a) `commandTypeName.EndsWith("Command", Ordinal)` is case-sensitive; a typoed `Mycommand` survives unchanged. (b) `CreationVerbPrefixes` matched as raw `StartsWith` without word boundary — `NewsletterSubscribeCommand` falsely matches `New` prefix; `IssuesQueryCommand` (if writable filter ever fails open per P-1) matches `Issue`. Patch: case-insensitive suffix check; require next-char-uppercase or end-of-string after prefix match. [`EmptyStateCtaResolver.cs:25-26, 199-235`]
- [ ] [Review][Patch] **`ParseDescriptionAttribute` silently drops non-string ctor arguments** — `[Description(typeof(Resources))]` (a BCL-supported scenario) is silently ignored. Emit an informational HFC diagnostic when `[Description]` ctor-arg is not a string. [`AttributeParser.cs:1767-1777`]
- [ ] [Review][Patch] **`[Description]` on TYPE-level silently ignored without diagnostic** — adopters intuitively place class-level `[Description]` and expect it to surface (e.g., as the `SecondaryText` slot in empty state). Generator silently does nothing. Emit informational diagnostic OR document the property-level-only restriction loudly. [`AttributeParser.cs:743-754`]
- [ ] [Review][Patch] **`EscapeString` missing Unicode line-separator escapes** — ` ` / ` ` not escaped. Compiles fine in C# string literals but breaks JSON parse if description text traverses a JS-interop layer (codebase has `IExpandInRowJSModule.InitializeAsync` and other JS bridges). Add `  → \\u2028` and `  → \\u2029`. [`RoleBodyHelpers.cs:1652-1681`]
- [ ] [Review][Patch] **`FcExpandInRowDetail.PanelId` accepts caller-supplied id with no duplicate-id validation** — public-API change moves the duplicate-id WCAG 4.1.2 risk from generator to consumer. Add `Debug.Assert` for duplicate panel-id within a circuit + xmldoc warning. [`FcExpandInRowDetail.razor.cs:215-224`]
- [ ] [Review][Patch] **`ResolveDomIdSuffix` allows leading-digit suffix + collapses non-alnum runs to multi-dash** — latent footgun for any future caller using the helper in isolation (HTML4-invalid id). Prepend `'_'` when first char is digit; collapse consecutive non-alnum into single `-`. [`RazorEmitter.cs:1542-1554`]
- [ ] [Review][Patch] **`EmitHeaderDescription` may emit trailing-whitespace before suffix** — concatenation `header + " " + suffix` doesn't trim trailing whitespace from header. Cosmetic. `RoleBodyHelpers.EscapeString(col.Header.TrimEnd())` for the unsupported-column header path. [`ColumnEmitter.cs:1247-1257`]
- [ ] [Review][Patch] **StatusOverview lambda variable shadowing** — inner `(RenderTreeBuilder builder) =>` lambda parameter shadows the outer `builder`; works today but a future refactor of `EmitDetailRecordBody` that declares its own `int seq` would silently break. Rename inner to `panelBuilder`. [`ProjectionRoleBodyEmitter.cs:1420-1421`]
- [ ] [Review][Patch] **Test gaps: 3 tests assert presence without proving the contract** — (a) `CtaIsHiddenForAnonymousUsers_AC2_5` lacks a positive control showing CTA *would* render with the same fixture when authorized; (b) `HumanizeCommandName_HandlesAcronymBoundaries` exercises only the happy path (test name implies acronym+verb-prefix coverage that isn't there); (c) `DescriptionTooltipEmissionTests` asserts the C# escape form survived emission, not the rendered HTML form (a future swap to `AddMarkupContent` would silently bypass Razor escape). Strengthen all three. [`FcProjectionEmptyPlaceholderTests.cs`, `EmptyStateCtaResolverTests.cs`, `DescriptionTooltipEmissionTests.cs`]
- [ ] [Review][Patch] **`FieldPlaceholderAriaLabelTemplate` interpolates raw FQN — verbose AT announcement** — long generic type names (`System.Collections.Concurrent.ConcurrentDictionary<string, FrontComposerInternalState>`) get character-by-character announcements on screen readers. Pass `Type.Name` (short) instead of FQN to the template OR shorten the template wording. [`FcFieldPlaceholder.razor.cs:29`]
- [ ] [Review][Patch] **`EmptyStateCtaResolver` registered Scoped despite being stateless** — no per-circuit state; Singleton avoids per-request allocation. Verify `IFrontComposerRegistry.GetManifests()` returns an immutable snapshot (no concurrent enumeration race) before flipping. [`ServiceCollectionExtensions.cs:171-172`]

**Defer (3):**

- [x] [Review][Defer] **StatusOverview detail "first item" semantics misleading** [`ProjectionRoleBodyEmitter.cs:108, 1308-1309`] — `g.FirstOrDefault()` cached at construction; user expanding a status group sees an arbitrary representative with no count or "1 of N" affordance. Story 4-5 expand-in-row carry-over, not 4-6 regression. Deferred — track as 4-5 UX backlog.
- [x] [Review][Defer] **`_expandPanelId` Guid changes across re-mounts (AT cache regression)** [`RazorEmitter.cs ~line 209 of generated emit`] — Virtualize/NavigateTo cycling re-mounts the component with a new GUID; AT consumers cached against the previous `aria-controls` see stale id. Story 4-5 carry-over from the duplicate-id WCAG fix. Deferred — narrow edge case under Virtualize.
- [x] [Review][Defer] **`ProjectionEmptyStateCta` allows `Struct` target but parser path may not call `ParseDomain` on structs** [`ProjectionEmptyStateCtaAttribute.cs:7`] — runtime resolution works for struct + class but the generator's `EmptyStateCtaCommandTypeName` IR path may not populate for struct projections. Struct projections are an edge case (projections are typically records/classes). Deferred — re-open if a struct-projection adopter surfaces.

### Review Findings — Pass 3 (2026-04-25 bmad-code-review)

> **Adversarial multi-layer review** of the post-patch state (`7a0c30b..HEAD`). Three reviewers ran in parallel: Blind Hunter (diff-only, 26 findings), Edge Case Hunter (diff + project, 25 findings), Acceptance Auditor (diff + spec + Pass-2 cross-check, 32 findings). After dedup: **56 distinct findings** → **10 decision-needed**, **24 patch**, **8 defer**, **14 dismissed**.

**Decision-needed (10) — require user input:**

- [ ] [Review][Decision] **DN1 — `IsCommandWritable` fails open in default registry** [Pass-2 P-1 redux] — Default `FrontComposerRegistry` does not implement `IFrontComposerCommandWriteAccessRegistry`; the fallback extension returns `true` so every registered query (e.g., `GetOrdersQuery`) appears as a writable CTA in production. Tests use a write-aware mock that masks this. Options: (a) make default `FrontComposerRegistry` implement the companion (returning `true` to preserve back-compat); (b) flip the extension to fail-closed and require adopters to opt-in; (c) make companion implementation required at registration. [`FrontComposerRegistryExtensions.cs:56-58`, `FrontComposerRegistry.cs:12`]
- [ ] [Review][Decision] **DN2 — `<AuthorizeView>` crashes when `AddAuthorizationCore()` not registered** [Pass-2 redux] — Empty-state CTA hard-depends on `<AuthorizeView>`; `AddHexalithFrontComposerQuickstart` does not register an `AuthenticationStateProvider`. A self-hosted shell without an auth pipeline gets `InvalidOperationException` at first empty-state render. Options: (a) auto-register `AddAuthorizationCore()` inside `AddHexalithFrontComposerQuickstart`; (b) opt-out parameter `[Parameter] bool RequireAuthorization { get; set; } = true` on `FcProjectionEmptyPlaceholder`; (c) document requirement and ship as-is. [`FcProjectionEmptyPlaceholder.razor:11-29`, `ServiceCollectionExtensions.cs:172`]
- [ ] [Review][Decision] **DN3 — AC1 anchor semantics: `FluentAnchorButton + Primary` vs spec `FluentAnchor + Accent`** [Pass-2 decision-needed redux] — Implementation announces "button" to AT; spec mandates "link". FluentUI v5 collapsed v3's `FluentAnchor + Accent` pair into `FluentAnchorButton + Primary`. Options: (a) accept impl + amend AC1/D8 wording; (b) build a custom anchor wrapper. [`FcProjectionEmptyPlaceholder.razor.cs:139-145`]
- [ ] [Review][Decision] **DN4 — AC4/D9 description-tooltip anatomy: `HeaderTooltip` attribute vs `FluentIcon + FluentTooltip + aria-describedby`** [Pass-2 decision-needed redux] — Implementation uses FluentDataGrid's bare `HeaderTooltip` attribute. Spec D9 mandated visible `FluentIcon Size16.Info` + `FluentTooltip` + `<span id="{viewKey}-{prop}-desc">` + `aria-describedby`. Two D8 resource keys (`FieldDescriptionTooltipAriaSuffix`, `ColumnHeaderWithDescriptionAriaTemplate`) shipped but are dead code. Options: (a) accept minimal anatomy + remove dead keys + amend D9; (b) commit to Story 4-7 anatomy work and keep keys as forward-compat; (c) refactor in 4-6. [`ColumnEmitter.cs:355-365`, `FcShellResources.resx:325, 329`]
- [ ] [Review][Decision] **DN5 — AC2.5 per-command `AuthorizationPolicy` reflection deferred** [Pass-2 decision-needed redux] — `EmptyStateCtaResolver.BuildCta` always emits `AuthorizationPolicy: null`; default `<AuthorizeView>` only catches the anonymous case. Authenticated-but-unauthorized users still see the CTA → click → 403. Trim-incompatible per `IsTrimmable=true`. Options: (a) accept partial AC2.5 + ship 4-6 with documented gap (Story 4-7 trim-safe alternative); (b) hold 4-6 until policy reflection lands. [`EmptyStateCtaResolver.cs:164-175`]
- [ ] [Review][Decision] **DN6 — EN `EmptyStateCtaTemplate` "Send your first {0}" verb collision** — Verb in template double-stacks with command-named verbs: "Send your first Create Order", "Send your first Ship Order"; FR template "Créer votre premier {0}" mixes French verb with English humanized command name ("Créer votre premier Ship Order"). Options: (a) drop verb from template (e.g., "First {0}" / "Premier {0}"); (b) per-locale rewrite with separate per-verb templates; (c) accept as-is and let adopters override per projection. [`FcShellResources.resx:1180`, `FcShellResources.fr.resx:1122`]
- [ ] [Review][Decision] **DN7 — `FieldPlaceholderAriaLabelTemplate` 2-arg with raw FQN vs spec D8 1-arg** [Pass-2 P-12c, P-24 redux] — Spec template arity is 1 (`"{0} requires a custom renderer"`); impl emits 2-arg template (`"{0} with unsupported type {1} requires a custom renderer"`) with `TypeName` carrying the raw FQN. Long generic FQNs get character-by-character announcements. Options: (a) revert to D8 1-arg signature; (b) keep 2-arg but pass `Type.Name` short form; (c) keep 2-arg + amend D8. [`FcShellResources.resx:313-315`, `FcFieldPlaceholder.razor.cs:29`]
- [ ] [Review][Decision] **DN8 — Out-of-scope `FcFluentIcons` factory + 4 layout component edits + `CommandRendererEmitter` + `Directory.Packages.props` + docs/jobs** — Substantial new icon factory (`Components/Icons/FcFluentIcons.cs`, ~60 LOC, 7 inline SVG paths) with edits to `FcCollapsedNavRail.razor`, `FcPaletteTriggerButton.razor`, `FcSettingsButton.razor`, `IconAttribute.cs`, `CommandRendererEmitter.cs`, `Directory.Packages.props`, `docs/fluent-ui-v5-contingency.md`, `jobs/story-predev-hardening-task.md`. None of these are in the spec's "Files to create/extend" tables. Likely coupled to the FluentUI Icons satellite-package removal. Options: (a) accept as 4-6 carry-in + amend Files-to-extend table; (b) split to dedicated story / Story 4-7; (c) revert from 4-6 commit window. [`Components/Icons/FcFluentIcons.cs`, `Hexalith.FrontComposer.Shell.csproj`, `Directory.Packages.props`]
- [ ] [Review][Decision] **DN9 — `role="status"` proliferation across N×M unsupported cells** — `FcFieldPlaceholder` outer `<div>` carries `role="status"` per AC7. Each `TemplateColumn` cell renders this once per row × per unsupported column → N rows × M unsupported columns = N×M live regions on a single page. Major a11y noise (every region announces on content change). Options: (a) keep per-AC7 + accept multi-region announcements; (b) move `role="status"` to a single grid-level live region (one announcement per grid load); (c) drop the role entirely (column header context already conveys "unsupported"). [`FcFieldPlaceholder.razor:3-5`]
- [ ] [Review][Decision] **DN10 — UX-DR3 `FcFieldPlaceholder` body uses plain HTML, not Fluent components** — Spec UX-DR3 anatomy: "dashed card + warning + type + docs anchor" using Fluent primitives (FluentIcon, FluentAnchor, FluentCard). Implementation uses raw `<strong>` + `<span>` × 3 + raw `<a href>`; the dashed border is purely CSS-driven. Options: (a) refactor to Fluent components in 4-6 (FluentIcon warning glyph + FluentAnchor docs link); (b) accept downgrade + amend UX-DR3; (c) defer Fluent-component refactor to Story 4-7. [`FcFieldPlaceholder.razor:1-10`]

**Patch (24) — unambiguous fixes:**

- [ ] [Review][Patch] **P1 — DetailRecord/Timeline don't render Unsupported as `FcFieldPlaceholder`** [`ProjectionRoleBodyEmitter.cs:732-771, 817-849`] — `RazorModelTransform` no longer drops Unsupported properties (4-6 critical fix), so they flow into `model.Columns`. DataGrid path branches on `TypeCategory.Unsupported` via `EmitUnsupportedColumn`, but `EmitDetailField` (DetailRecord, expand-in-row detail panel, StatusOverview detail panel) and `EmitTimelineBody` do NOT. Their `FormatValueExpression` default arm renders only an em-dash with NO `FcFieldPlaceholder`, NO docs link, NO devmode highlight. Direct AC6/FR9 regression for non-grid roles. Add `TypeCategory.Unsupported` branch to both helpers.
- [ ] [Review][Patch] **P2 — Resolver re-walks registry on every parameter snapshot** [`FcProjectionEmptyPlaceholder.razor.cs:107-117`] — `OnParametersSet` always invokes `ResolveExplicit/Resolve` regardless of whether `ProjectionType`/`CtaCommandName` actually changed. O(N-commands × N-manifests) per parent re-render. Cache previous `(ProjectionType, CtaCommandName)` tuple and skip when unchanged.
- [ ] [Review][Patch] **P3 — `CtaCommandName` whitespace bypasses both explicit and BC fallback** [`FcProjectionEmptyPlaceholder.razor.cs:113`] — `CtaCommandName is { Length: > 0 } explicitName` matches `"   "` (3 chars) → routes to `ResolveExplicit` → trims to empty → returns null → no fallback. Replace with `!string.IsNullOrWhiteSpace(CtaCommandName)`.
- [ ] [Review][Patch] **P4 — `EmptyStateCtaResolver.TryGetManifests` swallows broad `Exception`** [`EmptyStateCtaResolver.cs:65-73`] — Catches all `Exception` including `OperationCanceledException` and `OutOfMemoryException`, masking shutdown signals and fatal errors. Add `when (ex is not OperationCanceledException)` exception filter.
- [ ] [Review][Patch] **P5 — `EscapeString` missing escapes for `\b`, `\f`, `\v`, U+2028, U+2029, low-ASCII control chars** [`RoleBodyHelpers.cs:188-218`] — Adversarial author with `[Description("Hello\bWorld")]` or pasted text containing line/paragraph separators emits invalid C# string-literal content (CS1010 newline-in-constant) or breaks JS-interop JSON parse (`U+2028`/`U+2029` are invalid in JSON strings). Add full escapes.
- [ ] [Review][Patch] **P6 — Snapshot Guid-scrub regex over-matches** [`CounterStoryVerificationTests.cs:165`] — `Regex.Replace(normalized, "-[0-9a-f]{32}", "-{guid}")` matches any hyphen + 32 hex anywhere in markup. Future generator additions of any hex hash silently masked. Narrow to `fc-expand-panel-[a-z0-9-]+-[0-9a-f]{32}` or use a unique sentinel marker.
- [ ] [Review][Patch] **P7 — `RenderContext.IsDevMode` 6th positional param breaks `Deconstruct` arity + record equality** [`RenderContext.cs:15-21`] — Adding to a positional record changes `Deconstruct` arity and record equality semantics (two contexts identical except IsDevMode now compare unequal → potential Fluxor selector cache invalidation). Move `IsDevMode` to a non-positional `init`-only property OR document the breaking change. Search project for `is RenderContext(_,_,_,_,_)` usage before applying.
- [ ] [Review][Patch] **P8 — FR resource strings drift from D8 spec** [`FcShellResources.fr.resx:251-288`] — (a) `EmptyStateMessageTemplate` FR is "pour l'instant" → spec D8 says "pour le moment"; (b) `EmptyStateAriaLabelTemplate` FR rewritten as "Aucun résultat trouvé pour {0}" → spec D8 says "Aucun {0} trouvé"; (c) `UnsupportedColumnHeaderSuffix` FR is "(rendu personnalisé nécessaire)" → spec D8 says "(rendu personnalisé requis)". Reconcile FR strings to match D8 verbatim OR amend D8.
- [ ] [Review][Patch] **P9 — `EmitUnsupportedColumn` doesn't emit sort attributes** [`ColumnEmitter.cs:334-353`] — Other column emitters (text/numeric/date/enum/collection) emit `EmitSortAttributes`; the new unsupported emitter does not. A user setting `[Display(Order=1)]` on an unsupported first-property loses the default-sort affordance. Functional regression vs Story 4-1.
- [ ] [Review][Patch] **P10 — Resolver picks `matches[0]` non-deterministically across manifests (BC fallback)** [`EmptyStateCtaResolver.cs:82-108`] — `matches.SelectMany(...)` ordering depends on manifest iteration order, which depends on assembly load order. Non-deterministic across Server vs WASM hosts. Add `OrderBy(.BoundedContext, StringComparer.Ordinal).ThenBy(.CommandFqn, StringComparer.Ordinal)` for deterministic tiebreak.
- [ ] [Review][Patch] **P11 — `aria-controls` shared across all expandable rows** [`RazorEmitter.cs:1742, 1768, 1789, 1936-1940`] — `_expandPanelId` is a single instance field shared across all rendered rows. Every row's chevron carries `aria-controls={singlePanelId}`, but only the currently expanded row's panel exists in DOM. WCAG 4.1.2 violation — `aria-controls` must reference an existing element. Use per-row scoped id (item-key + Guid) so unexpanded rows either omit `aria-controls` or carry their own row id.
- [ ] [Review][Patch] **P12 — `[BoundedContext]` typo silently disables CTA** [`EmptyStateCtaResolver.cs:147-162`] — When attribute names a BC with no matching manifest, resolver returns null silently. Adopter has no signal something is wrong. Log Warning at minimum; consider falling through to FQN-based BC discovery.
- [ ] [Review][Patch] **P13 — CTA `Href` not validated via `CommandRouteBuilder.IsInternalRoute`** [`EmptyStateCtaResolver.cs:164-175`] — `BuildCta` calls `BuildRoute` (safe) but never validates with `IsInternalRoute` (which exists at `CommandRouteBuilder.cs:90`). A future custom `IEmptyStateCtaResolver` impl supplying `CommandRoute = "javascript:alert(1)"` would render the script URL. Defense-in-depth gap; the palette already filters via `IsInternalRoute`. Wrap `BuildRoute` output through it and return null CTA on mismatch.
- [ ] [Review][Patch] **P14 — CSS class names diverge from D8 inventory** [`FcProjectionEmptyPlaceholder.razor`, `ColumnEmitter.cs EmitUnsupportedColumn`] — Spec line 306 inventories `.fc-empty-state-body`, `__icon`, `__cta`, `__secondary` (BEM convention) and `.fc-field-placeholder-column` for unsupported `<td>`. Impl emits `fc-projection-empty-placeholder*` (different prefix) and `fc-col-unsupported` (not the spec'd name). Either rename to spec inventory or amend spec.
- [ ] [Review][Patch] **P15 — `FcFieldPlaceholderLocalizationTests` culture leak** [`FcFieldPlaceholderLocalizationTests.cs:2578-2580`] — `FrenchLocale_UsesFrenchLocalizedBody` mutates `CultureInfo.CurrentCulture = french;` with no `try/finally` reset. xUnit test order is arbitrary; sibling test classes sharing the test process may observe `fr-FR` until the next constructor runs. Wrap with `try/finally` or use a culture-scope helper.
- [ ] [Review][Patch] **P16 — AC3 routing test missing** [`FcProjectionEmptyPlaceholderTests.cs`] — Spec AC3 + T6.2 mandate a regression test asserting `FcProjectionEmptyPlaceholder` (4-6) renders for zero-total and `FcFilterEmptyState` (4-3) renders for filter-empty. T6.2 was checked off but no test code visible in `FcProjectionEmptyPlaceholderTests.cs`. Add the bUnit pair.
- [ ] [Review][Patch] **P17 — Missing terminating newline** [`FrontComposerRegistryExtensions.cs:477`] — Style guide violation. Many editor configs auto-fix; re-saving will produce noise diff. Add trailing newline.
- [ ] [Review][Patch] **P18 — `EmptyStateCta` constructor doesn't validate `AuthorizationPolicy` whitespace** [`EmptyStateCta` record] — Whitespace-only `"   "` matches `Length: > 0` branch and gets passed to `<AuthorizeView Policy>`, which throws. Validate non-whitespace in record's primary constructor.
- [ ] [Review][Patch] **P19 — `HumanizeCommandName` case-sensitive suffix + verb-prefix word-boundary missing** [`EmptyStateCtaResolver.cs:185-203`] — (a) `EndsWith("Command", StringComparison.Ordinal)` is case-sensitive — typoed `Mycommand` survives unchanged; (b) `CreationVerbPrefixes.StartsWith` no word-boundary check — `NewsletterSubscribeCommand` falsely matches `New`. Switch to `OrdinalIgnoreCase` for suffix; require next-char-uppercase (or end-of-string) after prefix match.
- [ ] [Review][Patch] **P20 — Spec text in AC6/AC7 references removed `RenderMode.DevMode`** [Spec lines 161-164] — Pass-1 D2 dropped `DevMode` from `FcRenderMode` and added `RenderContext.IsDevMode`. Spec AC6/AC7 still says `@(RenderContext.Mode == RenderMode.DevMode)` which would no longer compile if pasted into code. Amend AC6/AC7 text to reference `RenderContext?.IsDevMode == true`.
- [ ] [Review][Patch] **P21 — `FluentAnchorButton` `aria-label` duplicates visible label** [`FcProjectionEmptyPlaceholder.razor.cs:104, 143-145`] — `CtaAriaLabel(cta) => FormatCtaLabel(cta)` returns same string used as visible content. WAI-ARIA recommends omitting `aria-label` when it matches visible text. Drop the redundant attribute.
- [ ] [Review][Patch] **P22 — `CommandRouteBuilder.BuildRoute` may produce wrong slug for FQN command names** [`EmptyStateCtaResolver.cs:164-175`, `CommandRouteBuilder.cs:35-39`] — `KebabCase` does `if (ch == '.') { builder.Clear(); }` — only the LAST namespace segment survives. Resolver assumes parity with Story 3-4 emit but tests use unqualified names so the FQN-collapse is invisible. Add a test asserting `MyApp.Orders.Commands.CreateOrderCommand` resolves to a route the registry actually serves.
- [ ] [Review][Patch] **P23 — `DomainManifest.Commands` lookup uses `Ordinal` while `BoundedContext` lookup uses `OrdinalIgnoreCase`** [`EmptyStateCtaResolver.cs:1340 vs 1399-1402, 1379`] — Silent inconsistency. Casing differences in command type names (`createOrderCommand` vs `CreateOrderCommand`) silently fail to resolve. Align comparers — prefer `Ordinal` everywhere and document the case-sensitivity contract.
- [ ] [Review][Patch] **P24 — `ResolveExplicitInternal` cross-manifest first-match-wins is non-deterministic; `CommandMatches` allows simple-name match when both inputs are FQN** [Pass-2 P-2 redux] [`EmptyStateCtaResolver.cs:82-108, 180-183`] — `matches.ToList()` then `matches[0]` with no tiebreak; `CommandMatches` permits simple-name vs simple-name match even when both inputs contain `'.'`, defeating FQN disambiguation. Add `OrderBy(.BoundedContext, Ordinal).ThenBy(.CommandFqn, Ordinal)`; tighten `CommandMatches` to require FQN equality when both inputs contain `'.'`.

**Defer (8):**

- [x] [Review][Defer] **`EmptyStateCtaResolver` reflection on attributes not trim/AOT annotated** [`EmptyStateCtaResolver.cs:38-42, 151-155`] — `GetCustomAttributes(typeof(...), false)` on user types with no `[DynamicallyAccessedMembers]`/`[RequiresUnreferencedCode]` annotation. Empty-state CTA may silently disappear under `dotnet publish -p:PublishTrimmed=true`. Story 4-7 trim-safe alternative.
- [x] [Review][Defer] **`[Description]` on TYPE-level + non-string ctor arguments silently dropped** [`AttributeParser.cs:743-754, 1767-1777`] — `[Description(typeof(Resources))]` and class-level `[Description("…")]` are both silently ignored without diagnostic. D19 forbids new HFC IDs in 4-6; revisit when adding informational diagnostics is acceptable.
- [x] [Review][Defer] **em-dash separator hardcoded English typography in `EmitHeaderDescription`** [`ColumnEmitter.cs:1626-1630`] — Em-dash separator with no NBSP guard. Already on Story 4-7 candidate list (carryover from Pass 1 patch list — "French NBSP for `UnsupportedColumnHeaderSuffix` concatenation").
- [x] [Review][Defer] **`OnParametersSet` sync-only override** [`FcProjectionEmptyPlaceholder.razor.cs:107-117`] — Future async-friendly resolver becomes a breaking interface change. Architectural concern, not a current bug.
- [x] [Review][Defer] **`_expandedItemKey.Equals(key)` StatusOverview key fragility** [`RazorEmitter.cs ~line 1761, 1970`] — Two enum values humanizing to the same `StatusLabel` (e.g., a localized override) collapse/expand the wrong group. Narrow edge case; revisit if surfaced by adopter.
- [x] [Review][Defer] **`RegisterDomain` not thread-safe with resolver's `_registry.GetManifests()` enumeration** [`FrontComposerRegistry.cs:13, 93-107`] — Raw `List<T>` returned by `GetManifests`; concurrent `RegisterDomain` (e.g., add-on registering from a hosted service) could throw `InvalidOperationException("Collection was modified")` during resolver enumeration. Pre-existing risk amplified by frequent registry walks. Revisit when add-on registration patterns surface.
- [x] [Review][Defer] **Empty-state secondary-text resource key uses raw FQN with `+`/backticks** [`FcProjectionEmptyPlaceholder.razor.cs:128-131`] — `ProjectionType.FullName` for nested types contains `+`; for generic types contains backtick. `.resx` keys cannot contain `+` (XML constraints) → silent no-op. Narrow edge case for nested/generic projections.
- [x] [Review][Defer] **`<AuthorizeView>` flicker on prerender → interactive transition** [`FcProjectionEmptyPlaceholder.razor:11-29`] — Static-SSR/prerender shows nothing; CTA pops in after hydration. Deliberate per AC2.5 but undocumented; tests don't cover prerender vs interactive timing. UX polish.

**Dismissed (14) — false positives, noise, or design intent:**

- B-01 (`HumanizeCommandName` acronym double-space — verified working for the spec'd test inputs); B-02 (Shouldly nullability cosmetic build noise); B-08 (FluentLabel `<p>` semantic — Fluent component handles internally); B-09 (`AuthorizationPolicy` non-null branch dead code — forward-compat for DN5 resolution); B-10 (RenderFragment seq numbers — smell, not bug); B-15 (`EmitDetailDescription` seq-name reuse — smell, not bug); B-17 (`ResolveDomIdSuffix` collisions — debuggability only, runtime Guid suffix preserves uniqueness); B-18 (`[ProjectionEmptyStateCta]` on interface/enum — compile error blocks invalid placement); B-22 (defensive note for future localized assertions — no current bug); B-24 (`HumanizeCommandName` non-`Command`-suffixed naming — assumption documented in spec); E-15 (component re-instantiation cache loss — architectural, not defect); E-21 (alphabetical fallback semantically wrong — per spec D4 design); E-23 (conditional seq allocation in `EmitUnsupportedColumn` — Blazor `RenderTreeBuilder` handles correctly); A-26 (test count 62 vs D12 budget 55 — extra tests are positive direction).

### Story 4-7 (or follow-up) candidates

1. Trim-safe per-command `[Authorize(Policy=)]` discovery - registry companion + source-generator emit instead of runtime reflection.
2. Full description-tooltip anatomy - emit inline `FluentIcon Size16.Info` + `FluentTooltip` + `<span id="{viewKey}-{prop}-desc">` + `aria-describedby` on the column header.
3. French NBSP for `UnsupportedColumnHeaderSuffix` concatenation in generator-emitted header text.
4. Optional revert of `FcExpandInRowDetail.PanelId` to get-only contract if Story 4-5 adopters depend on the original surface.
5. Trim-safe attribute-discovery path for `EmptyStateCtaResolver` (Pass-3 W1).
6. Informational HFC diagnostic for `[Description]` placed at type-level or with non-string ctor (Pass-3 W2).

