# Known Gaps (Explicit, Not Bugs)

Each row cites a specific owning story per L10 discipline.

| ID | Gap | Owning story | Rationale for deferral |
|---|---|---|---|
| G1 | `IShortcutService` abstraction with keyboard shortcut registration, conflict detection at build time, and HFC2xxx warning for duplicate bindings. 3-3 ships a single inline `@onkeydown` handler on `.fc-shell-root` for Ctrl+, — full service infrastructure lands in Story 3-4. | **Story 3-4** (FcCommandPalette and keyboard shortcuts) | Story 3-4 epic AC §185-189 scopes `IShortcutService` + build-time conflict warnings. Shipping the service in 3-3 would fork ownership of shortcut infrastructure across two stories. 3-3's inline binding satisfies epic AC §124 today and is trivially migrated to the service pattern when 3-4 lands. |
| G2 | Command palette "Settings" result that opens the same dialog via `Ctrl+K` → typing "Settings" → `Enter`. 3-3 ships the dialog + direct invocation paths (button + Ctrl+,) but not the palette integration. | **Story 3-4** (FcCommandPalette and keyboard shortcuts) | The palette is the 3-4 deliverable. Story 3-4 adds a `RegisterCommand("settings", () => DialogService.ShowDialogAsync<FcSettingsDialog>(...))` call — no new API on the settings dialog. |
| G3 | Per-component density overrides via Level-1 annotations (e.g., `[ForcedDensity(DensityLevel.Compact)]` on a `[Projection]` class). 3-3 ships the precedence resolver with `DensitySurface` enum + tier-5 per-component "outside the resolver" escape hatch documented in XML, but no attribute-based mechanism. | **Story 6-1** (Level-1 annotation overrides) | Per-component overrides are the first tier of the customisation gradient (Epic 6). Shipping an attribute in 3-3 would preempt Epic 6's annotation pattern. Custom components hardcoding `data-fc-density="compact"` on their root DOM element (documented in `DensityPrecedence.cs` XML) is the v1.0 placeholder. |
| G4 | Factory hybrid defaults for DataGrid / DetailView / DevModeOverlay surfaces actively wired through the precedence resolver. 3-3 ships the `DensitySurface` enum + `GetFactoryDefault` switch covering all six values but only wires the `Default` surface into the `DensityEffects` resolver call paths. | **Each owning story** — Story 4-1 (DataGrid), Story 4-5 (DetailView / expanded rows), Story 6-5 (DevModeOverlay) | Surface-specific wiring adds a feature-unique consumer path. Shipping all five wires in 3-3 ahead of the consumers lands dead code; shipping the enum + resolver lets each story plug in without changing the enum or the function signature. |
| G5 | Multi-tenant deployment-default density (per-tenant `FcShellOptions.DefaultDensity` resolved from `IUserContextAccessor.TenantId` at render time). v1 ships `FcShellOptions.DefaultDensity` as a startup singleton bound from `IConfiguration` — identical across all tenants. | **v1.x** — requires an `IOptionsMonitor<FcShellOptions>`-based resolver keyed on tenant, which crosses into Epic 7 tenant-context-propagation scope. | 3-3's primary audience is single-tenant Counter.Web. Multi-tenant per-adopter defaults are a v1.x feature gated on Epic 7 shipping first. |
| G6 | Cross-device density preference sync via server-side `IStorageService` implementation. 3-3 persists per-device to LocalStorage (inherited from Story 3-1 ADR-030). | **v2** (UX spec §106) | Cross-device sync requires server-side preference storage + per-user authentication scope — a significant architectural addition that UX spec §106 explicitly rules out of v1. |
| G7 | Density parity specimen view + Playwright screenshot diff CI gate (three density levels × two themes × two directions). 3-3 ships the `FcDensityPreviewPanel` component, which is ONE of the inputs 10-2's specimen pipeline consumes. | **Story 10-2** (accessibility CI gates) | Specimen infrastructure is a multi-week 10-2 deliverable. 3-3 ships the primitives; 10-2 assembles them into the CI gate. |
| G8 | `forced-colors` (Windows High Contrast) mode verification of the settings dialog controls — FluentRadioGroup radios, FluentDialog chrome, FcDensityPreviewPanel specimen. Framework inherits Fluent UI's forced-colors behaviour by default but 3-3 does not add a CI gate. | **Story 10-2** (accessibility CI gates) | Requires Playwright `forced-colors` emulation — Story 10-2's scope. |
| G9 | RTL (`dir="rtl"`) verification of the settings dialog layout — radio alignment, close-button placement, preview-panel orientation. | **v2** (UX spec §36) | v1 primary audience is EN + FR (LTR); adding RTL CI gates blocks shipping. |
| ~~G10~~ | ~~Accessibility announcement of density transitions to screen readers~~ — **PROMOTED TO SCOPE as D20 (FcDensityAnnouncer) during Freya review 2026-04-19.** 3-3 now ships a framework-level `aria-live="polite"` region on the shell that announces EffectiveDensity transitions for both user-driven and viewport-forced changes. See D20 in Critical Decisions. Row kept as strikethrough for traceability. | — | — |
| G11 | `FcSettingsDialog` opens with an animated enter/exit transition. v1.0 uses `FluentDialog` defaults which already honour `prefers-reduced-motion`. Custom transitions that deviate from Fluent UI would fight the zero-override invariant. | **v2** (UX spec §37 mentions motion-design variations as inspiration-only, not v1 commitments) | v1 is functional, not showcasing. Custom transitions are a brand-enhancement concern, not a framework capability. |
| G12 | Validation that `FcShellOptions.DefaultDensity` does not "conflict" with the UX spec's recommended factory floor for a primary surface (e.g., the adopter sets `Compact` but their primary surface is a CommandForm which the UX spec recommends as `Comfortable`). 3-3 trusts the adopter's choice without a HFC2xxx warning. | **v1.x** (diagnostic governance improvements; same path as Story 3-1 G3 / Story 3-2 G8) | Adopter-set deployment defaults are deliberate choices. Warning on every "Compact default" boot would be noise for call-center adopters who legitimately want Compact. Reconsider if adopter feedback indicates accidental-Compact confusion. |
| G13 | Same-circuit tenant switch (SSO silent re-auth reusing the Blazor circuit). `FrontComposerDensityState.UserPreference` retains previous tenant's value until the circuit restarts; the next `UserPreferenceChangedAction` writes the new value under the new tenant's storage key (correctly scoped). In-memory state leak only — same profile as Story 3-2 G18. | **Story 3-1** (circuit lifecycle + `IUserContextAccessor` change semantics + `AppInitializedAction` re-fire contract) | Cross-feature tenant-boundary concern: Story 3-1 owns the circuit-restart contract. Enforcing tenant-scoped in-memory Fluxor state across all features (Theme, Density, Navigation) is a cross-feature concern, not a per-story one. |
| G14 | Adopter-overridable density radio labels (e.g., an adopter-specific "Dense" / "Standard" / "Spacious" translation). 3-3 ships fixed `DensityCompactLabel` / `DensityComfortableLabel` / `DensityRoomyLabel` via `FcShellResources`. | **v1.x** — adopters who want different copy register their own `IStringLocalizer<FcShellResources>` replacement (the Story 3-1 D21 pattern), which already works for every framework-owned string | The `IStringLocalizer` replacement pattern is the existing escape hatch. No new framework surface needed. |
| G15 | **Roslyn analyzer + PostCSS AST walker replacement for the `DensityNoPerComponentLogicLintTest` grep tripwire** (Murat review 2026-04-19). The current grep-based lint suffers from false negatives on obfuscation (CSS-in-JS, computed class names, string concatenation like `var(--fc-dens` + `ity)`) and false positives on comments/docs. It is a TRIPWIRE for v1 — sufficient to catch naive regressions but not a durable gate. Replacement: a Roslyn analyzer (`HFC_Analyzer_DensityInvariant`) that walks Razor + C# syntax trees for forbidden `--fc-density` references outside the allow-list, plus a PostCSS walker for CSS files. Both emit `HFC3xxx`-range diagnostics and run in CI. | **Story 9-x** (diagnostic governance improvements — same bucket as Story 3-1 G3 and Story 3-2 G8). Target replacement within 2 sprints of 3-3 merge. | Grep-based lints rot into rituals nobody trusts (false-positive spam trains contributors to suppress-and-commit). AST-based lints stay durable. Not worth the 2-3 day investment in 3-3 when the tripwire catches the 90% case; worth it when the lint count starts exceeding 5 and grep coverage fragments. |

---
