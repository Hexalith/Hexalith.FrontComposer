# Story 4.1: Projection Role Hints & View Rendering

Status: ready-for-dev

> **Epic 4 § 5-54** · **FR4** · **UX-DR44** · **UX-DR45** · **UX-DR55** (loading/empty handoff) · **NFR4** (generated-view render budget) · applies lessons **L01, L02, L04, L06, L07, L09, L10, L11**. Extends the reducer-only `ProjectionRoleAttribute` contract frozen in Story 1-2, the source-generator Parse/Transform/Emit pipeline from Stories 1-4/1-5, and the `IBadgeCountService` producer seam shipped in Story 3-5. Opens the first contract surface on which Stories 4.2–4.6 layer (badges, filtering, virtual scrolling, expand-in-row, empty-states).

---

## Executive summary (Feynman-level, ~45 sec)

Today every `[Projection]`-annotated type emits the same generic DataGrid via `RazorEmitter` — the existing `ProjectionRoleAttribute` declares intent (ActionQueue / StatusOverview / DetailRecord / Timeline / Dashboard) but nothing in the generator or runtime consumes that intent. Story 4.1 **wires the role from attribute → IR → generated view body** so five rendering strategies land:

1. **Parse-stage extension.** `ProjectionRoleAttribute` gains an optional `WhenState` string (CSV of state-enum member names — the "explicit states" mandated by the epic AC block for ActionQueue). `AttributeParser` carries it + the existing role name through `DomainModel` into `RazorModel` (L04 full-name determinism preserved — no name transforms).
2. **Transform-stage branch.** `RazorModelTransform` selects a strategy per role (`Default` when attribute absent, `ActionQueue` / `StatusOverview` / `DetailRecord` / `Timeline` / `Dashboard` otherwise) and emits a discriminated `ProjectionRenderStrategy` onto the IR. Unsupported / unknown role values fall back to `Default` and raise a build warning.
3. **Emit-stage role dispatch.** `RazorEmitter` gains one `Emit{Strategy}Body` path per strategy. Each path still wraps the three-state Loading / Empty / Data shell (FluentSkeleton placeholders matching layout → FcEmptyState hook reserved for Story 4.6 → strategy-specific body). Contextual subtitle (UX-DR44) renders above each body and pulls its count from `IBadgeCountService` (3-5) rather than a per-type query.
4. **Runtime wiring.** Generated views consume cascading `RenderContext` (tenant / user / mode / density / read-only) already established by Story 2-2; badge-count subscription uses the `IState<FrontComposerCapabilityDiscoveryState>` mirror pattern proven in 3-5's `FrontComposerNavigation` consumer path (one subscription per view, not per row).
5. **Contract boundary.** 4-1 ships **the view shell + subtitle + per-role body skeleton**. DataGrid filters / sort (4.3), virtualization (4.4), expand-in-row (4.5), `FcEmptyState` content (4.6), and `FcFieldPlaceholder` expansion (4.6) all consume extension points this story freezes — no overlap, no re-litigation.

The design honors the "capability contract" principle: attribute shape and role enum match **PRD FR4 verbatim** (not the epic AC's `[ProjectionRoleHint]` / `Default` drift — see D1), keeps the source-generator deterministic and cache-stable (no Roslyn symbol references past Parse), lands decisions inside the L06 feature-story cap (≤ 25), and leaves every cross-story seam explicit per L01.

---

## Dev Agent Cheat Sheet (L11)

**Goal (one sentence):** Wire `ProjectionRoleAttribute` through the generator so each role emits its own DataGrid / Card / Timeline body with a contextual subtitle, leaving filtering / virtualization / expand-in-row / content of empty-states as extension points for 4.2–4.6.

**Files to create** (netstandard2.0 SourceTools unless noted):

| Path | Purpose |
| --- | --- |
| `src/Hexalith.FrontComposer.SourceTools/Transforms/ProjectionRenderStrategy.cs` | Enum `{ Default, ActionQueue, StatusOverview, DetailRecord, Timeline, Dashboard }` (parser-friendly; matches `Contracts/Attributes/ProjectionRole.cs` 1:1 + synthetic `Default` sentinel) |
| `src/Hexalith.FrontComposer.SourceTools/Emitters/ProjectionRoleBodyEmitter.cs` | One `Emit{Strategy}Body(StringBuilder, RazorModel)` static per role (ActionQueue / StatusOverview / DetailRecord / Timeline / Dashboard / Default) |
| `src/Hexalith.FrontComposer.Shell/Components/Rendering/FcProjectionRoutes.cs` | Static route-builder helpers (e.g., `StatusFilter(bcRoute, statusValue)`) — **H2 indirection per review round 2** so Story 4.3 can change URL format without re-emitting every StatusOverview view |
| `src/Hexalith.FrontComposer.Shell/Components/Rendering/FcProjectionLoadingSkeleton.razor` + `.razor.cs` + `.razor.css` | Shared FluentSkeleton placeholder layout (N rows × M columns approximation); `SkeletonLayout` enum + `Layout` parameter per D6 |
| `src/Hexalith.FrontComposer.Shell/Components/Rendering/FcProjectionEmptyPlaceholder.razor` + `.razor.cs` | **Story 4.6 hook** — minimal `"No {entityPlural} yet."` placeholder body; Story 4.6 replaces with the full `FcEmptyState` CTA flow without touching the generator contract. Carries optional `Role` parameter (emitted from generator, unused in 4-1, consumed by 4.6 for role-differentiated CTA — H4) |
| `src/Hexalith.FrontComposer.Shell/Components/Rendering/FcProjectionSubtitle.razor` + `.razor.cs` | Consumes `IBadgeCountService` + `IFrontComposerRegistry` to render the UX-DR44 subtitle from the projection type + role + WhenState tuple |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Parsing/ProjectionRoleAttributeParserTests.cs` | Parse-side: WhenState CSV split, trim, case sensitivity, empty vs absent, unknown-member diagnostic HFC1022 |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Transforms/RazorModelRoleStrategyTests.cs` | Transform-side: role → strategy mapping, unknown role fallback to Default |
| `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/ProjectionRoleBodyEmitterTests.cs` | Snapshot tests per strategy (Verify/approval) |
| `tests/Hexalith.FrontComposer.Shell.Tests/Components/Rendering/FcProjectionSubtitleTests.cs` | Subtitle copy + localization + null-service fallback |
| `tests/Hexalith.FrontComposer.Shell.Tests/Components/Rendering/FcProjectionLoadingSkeletonTests.cs` | Skeleton renders N rows × M columns, respects density, exposes `aria-busy` |
| `tests/Hexalith.FrontComposer.Shell.Tests/Components/Rendering/FcProjectionEmptyPlaceholderTests.cs` | Placeholder renders domain-humanized label, Story-4.6 replacement contract test |

**Files to extend:**

| Path | Change |
| --- | --- |
| `src/Hexalith.FrontComposer.Contracts/Attributes/ProjectionRoleAttribute.cs` | Add optional `string? WhenState { get; init; }` (CSV). Append-only — existing single-arg ctor + `Role` property untouched. |
| `src/Hexalith.FrontComposer.SourceTools/Parsing/DomainModel.cs` | Add `string? ProjectionRoleWhenState` field (append-only; `Equals`/`GetHashCode` updated); constructor arg optional at tail so existing call sites keep compiling. |
| `src/Hexalith.FrontComposer.SourceTools/Parsing/AttributeParser.cs` | Extract `WhenState` from `ProjectionRoleAttribute` `NamedArguments`; split on `,`, trim, drop empties; diagnose unknown-member names as HFC1022. |
| `src/Hexalith.FrontComposer.SourceTools/Transforms/RazorModel.cs` | Add `ProjectionRenderStrategy Strategy` and `EquatableArray<string> WhenStates` fields. |
| `src/Hexalith.FrontComposer.SourceTools/Transforms/RazorModelTransform.cs` | Map `DomainModel.ProjectionRole` → `ProjectionRenderStrategy`; carry `WhenStates` through. |
| `src/Hexalith.FrontComposer.SourceTools/Emitters/RazorEmitter.cs` | Replace monolithic `BuildRenderTree` body with a `switch (model.Strategy)` that delegates to `ProjectionRoleBodyEmitter.Emit{Strategy}Body`. Existing Loading / Empty guards are re-homed into shared emitter helpers (`EmitLoadingShell`, `EmitEmptyShell`). |
| `src/Hexalith.FrontComposer.SourceTools/Diagnostics/DiagnosticDescriptors.cs` | Add `HFC1022_ProjectionWhenStateMemberUnknown` (Warning), `HFC1023_ProjectionRoleDashboardFallback` (Information, emitted once per Dashboard-annotated type — Story 9-x will lift to full Dashboard rendering). |
| `src/Hexalith.FrontComposer.SourceTools/FrontComposerGenerator.cs` | Extend diagnostic switch for HFC1022 / HFC1023. |
| `src/Hexalith.FrontComposer.Contracts/Diagnostics/FcDiagnosticIds.cs` | Add XML-doc entries for HFC1022 / HFC1023 (build-time; mirror the HFC1001-HFC1021 format). |
| `src/Hexalith.FrontComposer.SourceTools/AnalyzerReleases.Unshipped.md` | Add rows for HFC1022 + HFC1023. |
| `src/Hexalith.FrontComposer.Shell/Resources/FcShellResources.resx` + `.fr.resx` | Add 8 new keys (see D19). 58 → 66 per locale. |

**Generated naming rules (L04 preserved):**

- Generator hint prefix unchanged: `{Namespace}.{TypeName}.g.razor.cs` (keeps projection-pipeline collision-free vs. command-pipeline suffix `.Command`).
- Emitted class name unchanged: `{TypeName}View : ComponentBase, IDisposable`.
- Role body is inlined into `BuildRenderTree`; no separate partial, no new generated file per role — keeps the generator's "one input, one output per pipeline" invariant (architecture §390-400).

**AC quick index** (one-liner each):

| AC | Intent |
| --- | --- |
| AC1 | `ActionQueue` role renders inline-action DataGrid sorted by priority, filtered by `WhenState` CSV |
| AC2 | `StatusOverview` role renders aggregate-count DataGrid with click-through to filtered DataGrid |
| AC3 | `DetailRecord` role renders `FluentCard` + `FluentAccordion` single-entity body (expand-in-row consumer in 4.5) |
| AC4 | `Timeline` role renders vertical chronological list with timestamp + status badges |
| AC5 | Default (no attribute) renders compact sortable DataGrid with inline actions per density rules |
| AC6 | Contextual subtitle under view title: ActionQueue / StatusOverview / Default / DetailRecord / Timeline copy matches UX-DR44 |
| AC7 | Role-enum cap is permanent — 5 roles + Default sentinel; unknown values fall back to Default + HFC warning |
| AC8 | Every generated view handles 3 states: Loading (FluentSkeleton matching layout), Empty (placeholder hook), Data (strategy body); `aria-busy` during loading |
| AC9 | `WhenState` CSV accepts `"Pending,Submitted"` form; parse-time HFC1022 for unknown enum members; runtime filter uses member names (string compare) |
| AC10 | `Dashboard` role is accepted but renders the Default body + HFC1023 information diagnostic (full Dashboard rendering deferred to Story 6-3) |
| AC11 | Subtitle fetches count from `IBadgeCountService` (3-5 service) not per-type query; nullable-inject fallback renders subtitle-without-count |
| AC12 | French locale parity: every new subtitle + placeholder key resolves from `FcShellResources.fr.resx` with `CanonicalKeysHaveFrenchCounterparts` green |

**Binding contract(s) with predecessor stories:**

- **Story 1-2 contract** frozen: `ProjectionAttribute`, `BoundedContextAttribute`, `ProjectionRole` enum values, `BadgeSlot` — all append-only per ADR-003. 4-1 ONLY adds an optional `WhenState` property to the existing `ProjectionRoleAttribute`.
- **Story 1-4/1-5 pipeline** frozen: Parse → Transform → Emit stages reachable only through pure data types (no `ISymbol` reference past Parse, no `Compilation` reference past Transform). 4-1 extends IR types additively — `EquatableArray<T>` for collections.
- **Story 2-2 contract** frozen: `ProjectionContext` cascading parameter for command-row binding, `RenderContext` for tenant/user/mode/density. 4-1's generated views CASCADE `ProjectionContext` downward so Story 2-2's command renderers keep working when rendered inside these views.
- **Story 3-5 contract** consumed: `IBadgeCountService.Counts[type]` for subtitle counts; nullable-inject pattern (`IServiceProvider.GetService`) so adopters without badges see subtitle degrade to "N entities" with N=0.

**Scope guardrails (what NOT to implement in 4-1):**

- ❌ **Filtering / sorting / search** (Story 4.3) — generated DataGrids render `FluentDataGrid` without column filters / sort chips / search input. `PropertyColumn<T,TProp>` remains the single column type.
- ❌ **Virtual scrolling** (Story 4.4) — DataGrid continues to render the full `Items` collection; `<Virtualize>` / `ItemsProvider` land in 4.4. `FcColumnPrioritizer` is out of scope.
- ❌ **Expand-in-row detail** (Story 4.5) — `DetailRecord` emits a standalone FluentCard body, NOT an expand-in-row host. 4.5 wires the row-expansion panel that composes DetailRecord.
- ❌ **Full `FcEmptyState`** (Story 4.6) — 4-1 ships a minimal `FcProjectionEmptyPlaceholder` with "No {entities} yet." copy + no CTA. 4.6 replaces the placeholder with the full CTA flow under the SAME component name (or adds a replacement pattern — decided in 4.6). The emitter's empty shell call-site stays stable.
- ❌ **Status badge palette** (Story 4.2) — enum columns keep the existing `HumanizeEnumLabel` text rendering. Coloured badge pill lands in 4.2.
- ❌ **`ColumnPriority` attribute** (Story 4.4) — column order stays declaration-order.
- ❌ **DataGrid state persistence** (Story 3-6 ADR-050) — 4-1 does NOT dispatch `CaptureGridStateAction`; Story 4.3's filter/sort events are the first dispatcher.
- ❌ **SignalR live refresh** (Epic 5) — generated view still dispatches `LoadAction` on `OnInitialized` via the existing Story 1-5 pattern; SignalR nudge handling lands in Epic 5.

**Start-here task order:** T1 → T2 → T3 → T4 → T5 → T6 → T7 → T8 (sequential; T8 French parity runs last because it depends on all new resource keys landing first).

**Test count expectation:** target ≈ **89 new tests** after four review rounds (revised round 4 per Bob — honest inventory sum, replacing the stale "≈77" claim). Breakdown by task: T1.6 ≥9 + T1.7 =6 + T1.8 =2 + T2.6 ≥7 + T3.10 ≥18 + T3.11 =4 + T3.12 =1 + T4.5 ≥12 + T5.2–T5.5 ≥6 + T5.6 =1 + T5.7 =2 + T6.6 ≥12 + T6.7 =1 + T7.2 =4 + T8.5 =5 ≈ 90 (rounded 89 net). Trajectory: Round 1 (Winston/Bob/Murat/Sally) +7 → Round 2 (pre-mortem/consistency/challenge/graph/hindsight) +10 → Round 3 (War Room/ToT/SCAMPER/Occam/FMA) +5 → Round 4 (Winston: structural-equality + RowContext code-shape + Enum-signature; Murat: T5.6 rename; Bob: accounting honesty) +5. **Crosses L11's 80-test soft trigger.** Accepted rationale: every round-3 and round-4 addition is a runtime-failure-mode anchor (incremental-cache hash collision, emitter class-field-hoist regression, URL corruption on `[Flags]` enums, reflection-over-generic-state trim breakage) — not vanity coverage. L11's trigger is a signal-for-review, not a cap; review has happened four times. **Escape hatch if Dev hits 90+ during implementation (per Murat):** T7.2's three DiagnosticDescriptor tests (`HFC1022MessageFormatMatchesSpec` / `HFC1023MessageFormatMatchesSpec` / `HFC1024MessageFormatMatchesSpec`) collapse into a single `AllHFCxxxxMessageFormatsMatchSpec` Theory, saving 2 tests. Do not exercise this collapse preemptively — L11 is a signal, not a budget.

---

## Story

As a developer,
I want projections annotated with `[ProjectionRole(role)]` (optionally with an explicit-states filter) to render according to their declared role — action queue, status overview, detail record, chronological timeline, or default DataGrid — with a contextual subtitle that orients business users,
so that business users get purpose-fit views of my domain without me writing a single line of rendering code.

**Business value:** The first shipped surface where FrontComposer's role-hint promise becomes visible. Until 4-1 lands, every generated projection looks identical to every other projection — a generic DataGrid, useful but not intent-driven. After 4-1, annotating `[ProjectionRole(ProjectionRole.ActionQueue, WhenState="Pending,Submitted")]` on an `OrdersProjection` yields an inline-action DataGrid sorted by priority with the subtitle "3 orders awaiting your approval" — WITHOUT custom Razor, WITHOUT renderer registration, WITHOUT overriding the generator. This is FR4's entire premise; it is also the first story an adopter can paste into their press release as a full FrontComposer differentiator vs. hand-rolled Fluent UI apps.

---

## Cross-story contract table (L01)

| Contract | Producer | Consumer | Binding |
|---|---|---|---|
| `ProjectionRoleAttribute.WhenState` (new optional nullable string property; additive) | 4-1 | 4-1 `AttributeParser.Parse`; 4.3 future `ColumnFilterProvider` MAY reuse at runtime | Append-only on the existing `ProjectionRoleAttribute`. Pre-4-1 annotations (no `WhenState`) remain valid; `WhenState` null means "no state filter" (runtime renders every row regardless of state). Frozen for v1 — the property name + CSV format + trim + case-sensitive member match are part of the stable shape. **ActionQueue + WhenState contract:** when `Role == ActionQueue` AND `WhenState != null`, the generated view MUST filter rows to the listed state-enum member names AT RENDER TIME (not at query time — filtering at the projection-state level is 4.3's ServerSide filter concern). |
| `DomainModel.ProjectionRoleWhenState` (new nullable string field on the IR) | 4-1 Parse stage | 4-1 Transform stage `RazorModelTransform` | Append-only on `DomainModel`; `Equals` / `GetHashCode` updated so the IR stays cacheable across incremental builds (Story 1-4 ADR-006 contract). |
| `ProjectionRenderStrategy` enum (new transform-stage IR; `{ Default, ActionQueue, StatusOverview, DetailRecord, Timeline, Dashboard }`) | 4-1 Transform stage | 4-1 Emit stage `ProjectionRoleBodyEmitter` | New type, but narrowly scoped to SourceTools assembly. Stable for v1; future roles (Summary/KPI, RelationshipMap) add enum members, keeping the Default fallback as the "unknown-role" escape. |
| `RazorModel.Strategy` + `RazorModel.WhenStates` (new fields on the transform-stage IR) | 4-1 Transform stage | 4-1 Emit stage | Additive on an already-frozen record (Story 1-5). Rewrite of `RazorModel.Equals`/`GetHashCode` keeps incremental-build cache correct. |
| `ProjectionRoleBodyEmitter.Emit{Strategy}Body(StringBuilder, RazorModel)` static method signatures | 4-1 | Out-of-tree fork consumers MAY extend for adopter-specific strategies in v1.x (not supported in v1) | Public static shape frozen in v1; we allow new `Emit{NewStrategy}Body` methods to appear (additive) but the existing six signatures don't change. Adopters who want custom strategies use Story 6 customization gradient overrides — NOT emitter forks. |
| `FcProjectionSubtitle` Blazor component (new Shell type; ships the UX-DR44 copy) | 4-1 | Every generated `{TypeName}View` emits `<FcProjectionSubtitle ProjectionType="@typeof({TypeName})" Role="ProjectionRole.{role}" WhenState="@whenStateLiteral" />` above its body | Parameters frozen: `ProjectionType` (`Type`, `EditorRequired`), `Role` (`ProjectionRole`, `EditorRequired`), `WhenState` (`string?`, default null). Future parameters append-only. |
| `FcProjectionLoadingSkeleton` Blazor component (new Shell type) | 4-1 | Every generated `{TypeName}View` Loading branch | Parameters: `ColumnCount` (`int`, `EditorRequired`), `RowCount` (`int`, default 5), `AriaLabel` (`string`, default resource key). Frozen shape; Story 4.4 `FcColumnPrioritizer` MAY wrap the skeleton with a narrower `ColumnCount` without touching the component's API. |
| `FcProjectionEmptyPlaceholder` Blazor component (new Shell type — **Story 4.6 REPLACES this**) | 4-1 | Every generated `{TypeName}View` Empty branch | Parameters: `ProjectionType` (`Type`, `EditorRequired`), `EntityPluralOverride` (`string?`, default null — generator emits `typeof(X).Name` pluralized; adopter may override). **Story 4.6 contract:** 4.6 REPLACES the component body (CTA button, secondary text, role=status, aria-label) WITHOUT changing the parameter list OR the generator call-site. The placeholder's existence lets 4-1 ship a usable empty state immediately AND lets 4.6 be a pure Shell-level swap. |
| `ProjectionContext` cascading parameter (Story 2-2 D27) | 2-2 generated command renderers | 4-1 generated projection views CASCADE `ProjectionContext` from per-row `<CascadingValue>` so command buttons inside rows pre-fill derivable fields | Contract re-used as frozen; 4-1's generator emits the `<CascadingValue Value="_rowContext">` wrap when a row emits an inline command (AC1 `ActionQueue` and AC5 `Default` paths). |
| `IBadgeCountService.Counts` (Story 3-5 D1 / ADR-044) | 3-5 `BadgeCountService` | 4-1 `FcProjectionSubtitle` nullable DI consumer | Contract unchanged; subtitle reads `Counts.GetValueOrDefault(projectionType, 0)` on each render (subscribes via `IObservable<BadgeCountChangedArgs>` to refresh). Missing service → subtitle renders "0 {entities}" gracefully (nullable-DI pattern identical to 3-4's palette consumer). |
| `IFrontComposerRegistry.GetManifests()` (Story 1-2 registry) | 1-2 | 4-1 `FcProjectionSubtitle` uses it to resolve bounded-context display label when rendering StatusOverview / ActionQueue subtitles that name the BC | Contract unchanged; registry enumeration is already wrapped in a try/catch at 4-1's call site so a throwing registry degrades to subtitle-without-BC-label (mirrors 3-6 D6 A9 pattern). |
| Two new diagnostic IDs `HFC1022_ProjectionWhenStateMemberUnknown` (Warning) + `HFC1023_ProjectionRoleDashboardFallback` (Information) | 4-1 | Operator / telemetry dashboards; Story 9-4 will layer analyzer hardening | Build-time diagnostics; reserved in `DiagnosticDescriptors.cs`, `FcDiagnosticIds.cs`, and `AnalyzerReleases.Unshipped.md`. Structured payload shape appendable; existing fields frozen. |
| 8 new EN+FR resource keys = 16 new strings; total shell resource count goes 58 → 66 per locale | 4-1 | Renders inside `FcProjectionSubtitle` + `FcProjectionEmptyPlaceholder`; EN/FR parity enforced by `CanonicalKeysHaveFrenchCounterparts` | Append-only to `FcShellResources.resx` + `FcShellResources.fr.resx`. |

---

## Acceptance Criteria

| AC | Given / When / Then | Primary tasks |
|---|---|---|
| **AC1** | **Given** a projection annotated `[ProjectionRole(ProjectionRole.ActionQueue, WhenState = "Pending,Submitted")]`. **When** `{TypeName}View` renders with non-empty data. **Then** the generated body emits a compact `FluentDataGrid` verified against three independent sub-ACs: **AC1a (Grid shape + ordering)** — grid inline-includes the column defined by the projection's first `[ProjectionBadge]`-annotated property if present (ordering proxy), OR the first `DateTime`/`DateTimeOffset` property, OR declaration order as final fallback, with `DescendingByDefault = true` on that column; per-row `<CascadingValue Value="_rowContext" IsFixed="true">` wraps each row's action surface (D14). **AC1b (Inline action column)** — when the projection declares inline commands (Story 2-2 renderer surface), the action column renders at the tail of the grid with button components pre-fill derivable fields from `_rowContext`. **AC1c (WhenState filter application)** — `state.Items` is filtered client-side to rows whose state-enum property value matches any member of the parsed `WhenStates` list (`"Pending", "Submitted"` after trim); row count displayed equals the FILTERED count, not the full `Items.Count`; filter result is memoized by reference-equality on `state.Items` per D10 (C1). **AC1d (Row-context isolation — F4 per review round 2)** — when navigation away from an ActionQueue view triggers a route change that loads a DIFFERENT projection view on the same circuit, the new view's `_rowContext` MUST be fresh (its `AggregateId` equals the first item of the new projection, not the last-clicked item of the previous view). Verified via a bUnit test that renders two ActionQueue views on the same `TestContext`, clicks a row in view-A, navigates to view-B via a stubbed `NavigationManager`, asserts view-B's `ProjectionContext.AggregateId != view-A's last-clicked AggregateId`. Guards against `IsFixed="true"` cascade leaks on layout-owned hosts. **And** the subtitle (AC6) renders `"N {entityPlural} awaiting your action"`. Each sub-AC passes or fails independently — the Dev Agent MUST report which clause failed in Completion Notes. | T1, T2, T3, T4 |
| **AC2** | **Given** a projection annotated `[ProjectionRole(ProjectionRole.StatusOverview)]`. **When** `{TypeName}View` renders. **Then** the generated body groups `state.Items` by the projection's status-enum property value and emits a `FluentDataGrid` whose rows are status → count aggregates (one row per distinct status, sorted by descending count). **And** each row carries a click handler dispatching `NavigationManager.NavigateTo(FcProjectionRoutes.StatusFilter(bcRoute, statusValue))` (H2 indirection per D11) — the filtered-DataGrid destination for Story 4.3; the nav-fires assertion uses a bUnit `NavigationManagerInterceptor` and checks the URL STRING returned by the helper, not hard-coded format. **And** the subtitle renders `"N total across M statuses"`. **And** when the projection has no recognisable status-enum property (none of its enum properties carry `[ProjectionBadge]`), HFC1022 does NOT fire (the fallback is silent — StatusOverview degrades to "N entities total"); this is by design because fixing "no status column" is an adopter-side modelling decision, not a generator warning. **⚠ Deferred-verify marker (per Bob review / G4):** the click-through DESTINATION (`?filter=status:X` query-string → filtered DataGrid) is Story 4.3's responsibility. For Story 4-1, AC2 is closed when (a) the navigation event FIRES with the correct URL, verified via a bUnit `NavigationManagerInterceptor` stub, AND (b) a `[DeferredVerify(Story = "4.3")]` marker is attached to the AC in Completion Notes. 4-1 DOES NOT depend on 4.3 landing — AC2's navigation-fires test is the v1 gate. | T2, T3 |
| **AC3** | **Given** a projection annotated `[ProjectionRole(ProjectionRole.DetailRecord)]`. **When** `{TypeName}View` renders with a single entity in `state.Items` (or with `state.Items.Count > 1`, the first entity is rendered — the multi-row selector lands in Story 4.5 expand-in-row). **Then** the generated body emits a `FluentCard` verified against two sub-ACs: **AC3a (Card structure)** — vertical field list inside the card; primary fields (first 6 declaration order — see D12) as top-level `<FluentLabel>` / `<FluentText>` pairs; when total supported properties ≤ 6 the accordion is OMITTED to avoid empty-accordion DOM noise. **AC3b (Secondary accordion)** — when total supported properties > 6, properties in positions 7+ render inside a single `<FluentAccordion Expanded="false">` section. **And** the subtitle renders `"{EntityName} overview"` (AC6 — copy nudge per Sally). **And** when `state.Items` is empty the Empty shell (AC8) runs — NOT a blank card. Each sub-AC passes or fails independently. | T2, T3 |
| **AC4** | **Given** a projection annotated `[ProjectionRole(ProjectionRole.Timeline)]`. **When** `{TypeName}View` renders. **Then** the generated body emits a vertical list with one row per `state.Items` entry, ordered by the projection's first `DateTimeOffset` or `DateTime` property descending (most recent first), rendering as `<FluentStack Orientation="Vertical">` with each item carrying a `FluentPersona`-style timestamp + domain label + status-badge column (badge renders as humanized text until Story 4.2 lands colour). **And** the subtitle renders `"N events"`. **And** when the projection has no date/time property, the transform stage emits HFC1022 (Warning) `"Timeline projection {type} has no DateTime property — ordering falls back to declaration order"` and the runtime renders declaration-order. | T2, T3 |
| **AC5** | **Given** a projection with NO `[ProjectionRole]` annotation. **When** `{TypeName}View` renders. **Then** the generated body emits the Default strategy: a standard compact `FluentDataGrid<T>` with `PropertyColumn<T, string?>` entries (one per non-unsupported property), sortable headers (default `FluentDataGrid` sort — no filter chips until Story 4.3), no inline-action column (inline actions surface in AC1's ActionQueue path only). **And** the subtitle renders `"N {entityPlural}"`. **And** this matches the existing Story 1-5 emitter output plus the new subtitle component — the Default path is explicitly a **drop-in compatible extension**, not a rewrite, so existing `CounterProjectionView.approved.cs` snapshot diffs boil down to "subtitle + loading shell + empty shell inserts" (no column changes, no removals). **DoD gate (per Bob review):** the Story 1-5 default-path regression test suite (`CounterProjectionApprovalTests` + `RazorEmitterDefaultBodyShapeTests` + any other Story-1-5 anchor tests) MUST be green before AC5 is marked closed — the Dev Agent explicitly logs the pre-change test run and the post-change test run in Completion Notes. If any Story 1-5 test regresses beyond the expected three inserts, AC5 stays OPEN and the regression is root-caused before re-baseline. | T2, T3, T5 |
| **AC6** | **Given** any generated projection view. **When** the view title renders. **Then** `FcProjectionSubtitle` emits a subtitle `<p class="fc-projection-subtitle">` whose copy matches: ActionQueue = `HomeActionQueueSubtitleTemplate` (`"{0} {1} awaiting your action"` / `"{0} {1} en attente d'action"`); StatusOverview = `HomeStatusOverviewSubtitleTemplate` (`"{0} total across {1} statuses"` / `"{0} au total dans {1} statuts"`); Default = `HomeDefaultSubtitleTemplate` (`"{0} {1}"` with entity-plural); DetailRecord = `HomeDetailSubtitleTemplate` (`"{0} overview"` / `"{0} - aperçu"` — per Sally's review: "detail" landed flat as a breadcrumb; "overview" reads as an intimate heading); Timeline = `HomeTimelineSubtitleTemplate` (`"{0} events"` / `"{0} événements"`). **And** when `IBadgeCountService` is registered, the count value comes from `Counts.GetValueOrDefault(projectionType, 0)`; when absent, the count falls back to `state.Items?.Count ?? 0`. **And** entity-plural uses `DisplayAttribute.GroupName`-or-`Name`-or-humanized `TypeName` resolution chain (existing `RazorModelTransform.ResolveHeader` pattern). **Loading-state rendering (SCAMPER-M round 3):** during `state.IsLoading == true`, `FcProjectionSubtitle` renders an EMPTY fragment (`<></>`) — not `"0 entities"`. Rendering `"0 entities"` during load is a visual flash-of-false-info before data arrives; empty fragment keeps the subtitle slot reserved in layout (Blazor renders nothing visible) and fills only when data lands. Matches UX-DR44's "contextual subtitles orient business users" — a wrong count doesn't orient; it misleads. | T6, T7 |
| **AC7** | **Given** the `ProjectionRole` enum shipped by Contracts (`ActionQueue`, `StatusOverview`, `DetailRecord`, `Timeline`, `Dashboard`). **When** a new role value is proposed. **Then** the addition requires a cross-story spec change — 4-1 freezes the attribute-side enum at 5 values + the `Default` synthetic transform sentinel (no sixth user-facing value in v1). **And** if Parse encounters an `int`-based role value outside the enum (e.g., adopter unsafe cast), Transform maps to `ProjectionRenderStrategy.Default` and `HFC1024_UnknownProjectionRoleValue` is emitted (Warning per D15, split from HFC1022 at round 2 — H6) with `"Unknown ProjectionRole value {intValue} on {type} — falling back to Default rendering."` **And** the transform-stage enum adds `Default` as a sentinel, NEVER surfaced through the `[ProjectionRole]` attribute (the attribute has no way to express "default" — absence IS default). | T2, T7 |
| **AC8** | **Given** any generated projection view, regardless of role. **When** `state.IsLoading == true`. **Then** the body renders `<FcProjectionLoadingSkeleton ColumnCount="N" RowCount="5" />` where N = count of non-unsupported properties (capped at 8 to stay close to the default DataGrid visual budget). **And** the skeleton container carries `aria-busy="true"` + `role="status"`. **When** `state.Items is null || state.Items.Count == 0` AND `state.IsLoading == false`. **Then** the body renders `<FcProjectionEmptyPlaceholder ProjectionType="@typeof({TypeName})" />`. **When** `state.Items.Count > 0`. **Then** the strategy-specific body renders per AC1-AC5. **And** the three-way branch replaces the existing Story 1-5 "if loading / if empty / render grid" inline blocks — the new shared `EmitLoadingShell` / `EmitEmptyShell` emitter helpers are the single source of truth. | T2, T3, T5 |
| **AC9** | **Given** `[ProjectionRole(ProjectionRole.ActionQueue, WhenState = "Pending,Submitted")]` on a projection whose status enum type declares members `Pending`, `Submitted`, `Approved`, `Rejected`. **When** Parse evaluates the attribute. **Then** `ProjectionRoleWhenState = "Pending,Submitted"` lands on `DomainModel` verbatim. **When** Transform processes the model. **Then** `WhenStates = ["Pending", "Submitted"]` lands on `RazorModel` (CSV-split, `Trim()`, ordinal-case `StringSplitOptions.RemoveEmptyEntries`). **Given** the CSV contains an unknown member (`"Pending,Xxxx"`). **When** Parse evaluates the attribute. **Then** HFC1022 Warning fires with `"ProjectionRole.WhenState on {type} references unknown enum member 'Xxxx' — member will be treated as always-no-match at runtime. Valid members: Pending, Submitted, Approved, Rejected."` (the diagnostic MUST list the known valid member names verbatim — F6 per review round 2, to short-circuit the most common adopter failure mode: casing typos like `"pending"` vs `"Pending"`). **Member list cap (War Room round 3):** if the status-enum declares more than 10 members, the diagnostic lists the first 10 alphabetically plus `"… and {N} more"` — prevents a 200-member enum from producing a wall-of-text warning. AND the Transform stage still passes `Xxxx` through to `WhenStates` (renderer compares as string; unknown members simply never match, which is the correct fail-soft). **Given** an empty `WhenState = ""`. **When** Parse evaluates the attribute. **Then** `ProjectionRoleWhenState = null` (empty CSV = absent); runtime renders every row regardless of state. | T1, T2 |
| **AC10** | **Given** `[ProjectionRole(ProjectionRole.Dashboard)]` on a projection. **When** Transform processes the model. **Then** `RazorModel.Strategy = ProjectionRenderStrategy.Dashboard` AND Emit dispatches to `EmitDashboardBody` which delegates to `EmitDefaultBody` (same DataGrid output as the no-attribute case). **And** HFC1023 Information fires `"Dashboard projection rendering is deferred to Story 6-3 — {type} falls back to Default DataGrid rendering in v1."` **once per Dashboard-annotated projection per compilation — see D16 for dedup ownership (round 4: no HashSet required; Roslyn's `IIncrementalGenerator` per-input invocation provides the "once per type" guarantee; incremental-cache hits bypass re-emission per D19 + T1.8)**. **And** the subtitle still renders the Default-strategy `"N {entityPlural}"` (Dashboard role has no canonical subtitle copy in v1). Dashboard is reserved — not broken. | T2, T3, T7 |
| **AC11** | **Given** `FcProjectionSubtitle` is registered in the Shell assembly. **When** it resolves `IBadgeCountService` via nullable DI (`IServiceProvider.GetService`). **Then** a present service wins for the count value — the subtitle reads `service.Counts.GetValueOrDefault(projectionType, 0)` AND subscribes to `service.CountChanged` (filtered to the projection type) to refresh on updates. **And** when the service is absent (adopter has not registered `IBadgeCountService` — not the default Shell, but allowed for v1.x adopters using their own badge source), the subtitle falls back to `state?.Items?.Count ?? 0` by reading `IState<{TypeName}State>` via the projection-type-specific generated hook (each generated view passes its `state.Items.Count` as a cascading value `SubtitleFallbackCount` — avoids reflection-over-generic-state). **And** disposal unsubscribes from `CountChanged` — no leaked subscriptions across circuit teardown (mirrors 3-5 D20 disposal discipline). **And** if `IBadgeCountService.CountChanged.Subscribe(...)` THROWS during `OnInitialized` (misconfigured service, Rx internal error), the subtitle catches the exception, logs it via `ILogger.LogWarning` (one-shot per component instance), and degrades to subtitle-without-count (identical to "service absent" branch) — matches Story 3-5's "degrade gracefully on Badge service failure" ethos per FMA round 3. Similarly, `_countSubscription?.Dispose()` during `DisposeAsync` wraps in try/catch so a throwing observer doesn't break circuit teardown. | T3, T6 |
| **AC12** | **Given** the shell's French locale. **When** any generated view renders with a French culture. **Then** the subtitle + empty-placeholder copy resolves from `FcShellResources.fr.resx`. **And** the existing `CanonicalKeysHaveFrenchCounterparts` test passes with the new 8 keys added to both files (58 → 66 per locale). **And** the three highest-emotional-weight keys (`HomeActionQueueSubtitleTemplate`, `HomeDefaultSubtitleTemplate`, `HomeEmptyPlaceholderText`) have a native-French-speaker pre-merge review attached to the PR per D19 (follows Story 3-5 D19 precedent — not all 8 gated, just the three first-impression strings). | T8 |

---

## Critical decisions (READ FIRST — do NOT revisit)

> **22 binding decisions.** Feature-story budget is ≤ 25 (L06). 4-1 lands at 22/25, leaving a 3-decision buffer for review rounds before Occam / Matrix trimming is required. Raise a spec-change proposal before revisiting any entry.

| # | Decision | Rationale | Consumed by |
|---|---|---|---|
| **D1** | **Keep `ProjectionRoleAttribute` + `ProjectionRole` enum unchanged from PRD FR4 / existing codebase** — attribute name is `ProjectionRole` (not `ProjectionRoleHint`), enum members are `ActionQueue`, `StatusOverview`, `DetailRecord`, `Timeline`, `Dashboard` (not `Default`). Treat Epic 4 AC wording `[ProjectionRoleHint(RoleHint.X)]` + the parenthetical "(ActionQueue, StatusOverview, DetailRecord, Timeline, Default)" as loss-of-precision drift between Epic authoring and PRD FR4. "Default" is the **rendering path** chosen when no attribute is present — surfaced internally through a transform-stage `ProjectionRenderStrategy.Default` sentinel, NEVER through a public `ProjectionRole.Default` enum value. The transform-stage enum has one MORE member than the attribute enum (`Default` added to the public 5) — this is by design. | PRD FR4 is the authoritative capability contract per §functional-requirements.md: "Developer can tag a projection with `[ProjectionRole(role)]` … Dashboard — capped at 5." The existing codebase already ships matching attribute + enum; Stories 3-4 / 3-5 already DEPEND on `ProjectionRoleAttribute` + `ProjectionRole.ActionQueue` (`IActionQueueProjectionCatalog` scans for `ProjectionRole.ActionQueue`). Renaming to `ProjectionRoleHint` or adding a `Default` enum value would break both the PRD contract AND the 3-5 catalog's reflection scan. The epic AC text is ambiguous enough that either interpretation could be defended — we anchor on PRD because PRD is version 1.0 spec output and Epic 4 is a story-breakdown artifact. Rejected: (a) rename to `ProjectionRoleHint` + deprecate `ProjectionRoleAttribute` — breaking the 3-5 catalog for a cosmetic rename; (b) add `ProjectionRole.Default` — no call-site has any reason to say `[ProjectionRole(Default)]` (absence already means default); (c) treat Epic as authoritative over PRD — inverts the spec hierarchy established by §implementation-readiness-report-2026-04-12.md ("FR4 is the capability contract; ACs restate it"). | T1, T2; AC1, AC5, AC7 |
| **D2** | **`WhenState` lives on `ProjectionRoleAttribute` as an optional `string? WhenState { get; init; }` property** — NOT a second constructor argument. Adopters who want the filter write `[ProjectionRole(ProjectionRole.ActionQueue, WhenState = "Pending,Submitted")]`; adopters who don't write `[ProjectionRole(ProjectionRole.ActionQueue)]` unchanged. The attribute stays `AllowMultiple = false, Inherited = false`. The attribute ships in netstandard2.0 Contracts so the source generator can see it during Parse (existing constraint). | Named-argument init is the canonical C# attribute-extension pattern — does not break any existing call site. Keeping the positional `role` argument lets the 3-5 catalog continue to use `GetCustomAttribute<ProjectionRoleAttribute>()?.Role` with no change. Rejected: (a) add a second positional arg `string? whenState = null` — works but requires adopters writing `[ProjectionRole(Role.ActionQueue, null)]` to disambiguate vs. `WhenState`; the named-arg form is strictly more readable; (b) dedicated `[ProjectionStateFilter("Pending,Submitted")]` companion attribute — two attributes for one concern, surfaced on the same projection, is a worse ergonomic; (c) strongly-typed `Enum[]` parameter — attributes cannot carry arbitrary enum arrays without generic attribute support (.NET 7+) AND the generator would need the actual enum `Type` at Roslyn-time, requiring a `Type` parameter which is brittle; string CSV is the idiomatic shape for "enum member names" (matches `[Authorize(Roles="A,B")]`, `[FlagsAttribute]` naming patterns). | T1, T2, T3; AC1, AC9 |
| **D3** | **Parse-stage `WhenState` extraction** — `AttributeParser.Parse` reads the `NamedArguments` collection on the `ProjectionRoleAttribute` `AttributeData`, finds the `WhenState` entry, and trims the CSV into a `List<string>`. If the state-enum type can be discovered (the projection declares exactly one enum-typed property OR an enum-typed property carrying `[ProjectionBadge]` — same heuristic Transform uses for StatusOverview grouping), the parser validates each trimmed member name against the enum's member set (case-sensitive ordinal match — enum member names are identifiers, case-significant). Unknown members emit `HFC1022_ProjectionWhenStateMemberUnknown` Warning per member. Known members pass through unchanged. Unknown members STILL PASS THROUGH to `RazorModel.WhenStates` (string-level filter at runtime simply never matches — fail-soft, no render-time crash). **ProjectionRoleWhenState stored on `DomainModel` as the raw CSV string** (`"Pending,Submitted"`), not the parsed list, so the IR stays small + the Transform stage owns the split (single canonical parsing rule). | Parse-side validation catches the common adopter typo (`"Pendng"` for `"Pending"`) at build time where it is cheap to fix. Fail-soft pass-through (don't drop the member) preserves forward-compat: a future projection might alias member names or use compile-time-string constants. CSV format matches the attribute-ecosystem precedent; alternatives (JSON, semicolon-separated) would be strictly more surprising. Rejected: (a) validate via Roslyn `SymbolDisplay` and skip unknown members — hides the typo from the adopter; (b) require an explicit `[EnumTypeReference]` companion attribute — bureaucratic overhead; (c) parse the CSV in Emit instead of Transform — two copies of the parse rule, harder to test. | T1, T2; AC9 |
| **D4** | **`ProjectionRenderStrategy` is a new SourceTools-internal enum** in `src/Hexalith.FrontComposer.SourceTools/Transforms/ProjectionRenderStrategy.cs` with values `Default`, `ActionQueue`, `StatusOverview`, `DetailRecord`, `Timeline`, `Dashboard`. The mapping in `RazorModelTransform` is `DomainModel.ProjectionRole` (stringly-typed, from Parse) → `ProjectionRenderStrategy` (strongly-typed, for Emit): exact match on enum member name → respective strategy; null → `Default`; anything else (unknown value) → `Default` + HFC1022 warning emitted. The enum stays in SourceTools (not Contracts) because it is a transform-stage IR, not a public API consumer-facing concern. | Decoupling the transform strategy from the public `ProjectionRole` enum lets future stories add strategies without growing the public attribute surface (e.g., Story 6-3 may introduce `Dashboard` specialised rendering as a strategy without changing the attribute). It also lets the Default sentinel stay synthetic — no "this role is invalid as an attribute value" ambiguity. Rejected: (a) reuse `ProjectionRole` + synthetic `Default = -1` — confuses consumers of the public enum; (b) string-based strategy (no enum) — gives up compile-time mapping + exhaustiveness checks in the Emit switch; (c) one strategy class per role via polymorphism — over-engineered for v1's fixed 6-element set, exhausts the Emit switch's ability to detect missing cases at compile time. | T2, T3; AC7 |
| **D5** | **Emit stage dispatches strategy via `switch (RazorModel.Strategy)` inside `RazorEmitter.Emit`** — each case calls a static helper on `ProjectionRoleBodyEmitter` (`EmitDefaultBody`, `EmitActionQueueBody`, `EmitStatusOverviewBody`, `EmitDetailRecordBody`, `EmitTimelineBody`, `EmitDashboardBody`). **Exhaustiveness discipline (per Winston review):** the switch MUST use C# switch-EXPRESSION form (not switch-STATEMENT) so the compiler enforces exhaustive coverage at build time, AND the default arm THROWS `InvalidOperationException($"Unhandled ProjectionRenderStrategy: {model.Strategy}. Add a case to RazorEmitter.Emit.")` — never silently falls through to Default rendering. When a future story (4.2 / 6-3) adds a strategy, a missing case fails the BUILD loudly rather than shipping a blank-view bug. Shared scaffolding (usings, class declaration, OnInitialized, Dispose, formatters, subtitle host, Loading shell, Empty shell) lives in `RazorEmitter` itself as private static helpers (`EmitClassHeader`, `EmitFormatters`, `EmitLoadingShell`, `EmitEmptyShell`, `EmitSubtitleInvocation`). Strategy bodies compose those helpers — they do NOT duplicate the class scaffolding. | One switch in one place is the easiest structure to audit for missing cases; Roslyn's exhaustiveness analyzer warns on missing cases when the switch expression targets a SourceTools-internal enum (D4). Splitting into six emitter classes would scatter shared state (sequence counters, StringBuilder cadence) across types with no corresponding maintainability win. Rejected: (a) strategy pattern with `IProjectionBodyEmitter` implementations in DI — source generators cannot use runtime DI (netstandard2.0 constraint); (b) emit a separate generated file per strategy — breaks the "one projection, one generated view" invariant that keeps diff review manageable; (c) fold all strategies into `RazorEmitter.Emit` inline — pushes the file past 1,000 lines and duplicates the Loading/Empty shells six times. | T3, T5; AC1-AC5, AC10 |
| **D6** | **`EmitLoadingShell` uses `FcProjectionLoadingSkeleton` — with role-aware layout variant (per Sally review)** — the generator emits `<FcProjectionLoadingSkeleton ColumnCount="@N" RowCount="5" Layout="SkeletonLayout.DataGrid" />` for DataGrid-shaped roles (Default / ActionQueue / StatusOverview), `Layout="SkeletonLayout.Card"` for DetailRecord (single-entity card — renders a title bar + 6 label/value row skeletons inside a card-shaped outer), and `Layout="SkeletonLayout.Timeline"` for Timeline (renders 5 vertical stack items each with a timestamp pill + label shimmer). `N = Math.Min(columns.Count, 8)` (the 8 cap matches Story 4.4's `FcColumnPrioritizer` visible-column target). `Layout` is a new enum `SkeletonLayout { DataGrid, Card, Timeline }` on the component. `aria-busy="true"` + `role="status"` on the outer container regardless of layout. Rationale for role-aware variant: a 5-row table skeleton preceding a single `FluentCard` render is a jarring "lie" (Sally) — the skeleton should preview the shape of the body it replaces. | Centralising the skeleton into a component keeps the generated view body short and lets Story 4.4's density-aware column-count adjustment happen without touching the emitter. `FluentSkeleton` is already in the dependency closure (Fluent UI v5). Rejected: (a) generate `<FluentSkeleton>` inline per view — 5 rows × N columns × every generated view = ~30 extra lines per view, churn-prone; (b) use a static CSS class pulse animation — loses Fluent UI v5 accessibility baseline; (c) skip the skeleton entirely and show "Loading..." text — regresses NFR29's "first interactive < 300ms" perceived performance because the view blanks. | T3, T5; AC8 |
| **D7** | **`EmitEmptyShell` uses `FcProjectionEmptyPlaceholder`** — the generator emits `<FcProjectionEmptyPlaceholder ProjectionType="@typeof({TypeName})" />`. The component itself renders a muted `<FluentStack>` with a `FluentIcon` at **generous size** (`Icons.Regular.Size32.DocumentSearch` — 32px, explicitly chosen per Sally review; 16px would read as "something failed to load" cold, 32px reads as "intentional calm") + text `"No {entityPlural} yet."` where entityPlural comes from `typeof(T).Name` humanized + pluralized (simple "s" suffix — matches UX-DR44 pluralization note "no grammatical pluralization in v1; Story 9-5 lifts to ICU-style"). Component layout: vertical stack, 24px top padding, icon above text, text class `fc-empty-placeholder-text` renders at 1.125rem muted (`var(--neutral-foreground-hint)`). **Story 4.6 replaces the component body** (adds CTA button + secondary text + role=status + aria-label), WITHOUT changing the parameter list, WITHOUT changing the generator call-site, WITHOUT changing the component type name. Story 4.6's work becomes a Shell-level component swap — the source generator never re-emits. | Decoupling the emit-site from the empty-state copy lets Story 4.6 ship as a pure runtime change — no source-generator regression surface. The minimal placeholder is still a usable empty state today (not a `<TODO>` blank) so adopters who ship on v1.0 before 4.6 lands see a functional view. Rejected: (a) emit the full empty state inline in 4-1 and refactor in 4.6 — generates more generator diff than necessary and couples 4-1 to Story 4.6's CTA resolution (commands per bounded context); (b) defer the empty shell entirely and let the Loading/Data blocks leave blank on zero items — violates UX-DR30 "empty states never silent"; (c) require adopter override for empty state — negates the "works without ceremony" promise. | T3; AC8 |
| **D8** | **`FcProjectionSubtitle` consumes `IBadgeCountService` via nullable DI** — `[Inject(Optional = true)] public IBadgeCountService? BadgeCountService { get; set; }` AKA the `IServiceProvider.GetService<>` nullable pattern that 3-4 / 3-5 already established. On `OnInitialized`, the component subscribes to `BadgeCountService?.CountChanged.Subscribe(OnCountChanged)`, disposing in `IAsyncDisposable.DisposeAsync`. When the service is absent, the fallback count comes from a cascading `SubtitleFallbackCount` (int?, default null) that each generated view emits alongside the subtitle: `<CascadingValue Name="SubtitleFallbackCount" Value="@state.Items?.Count"><FcProjectionSubtitle ... /></CascadingValue>`. If the cascade is also absent (host is composing the subtitle outside a generated view — adopter scenario), the subtitle renders with count `0`. | Nullable DI is the established pattern. The cascading-value fallback avoids reflection on the generated state type (`{TypeName}State`) which would be trim / AOT hostile. Rejected: (a) require `IBadgeCountService` registered — breaks v1.x adopters with their own badge source (e.g., pushing counts from a custom SignalR hub); (b) read from `IState<TState>` in the subtitle component via reflection — AOT/trim hostile (G22 would compound); (c) skip the count when service is absent — regresses UX-DR44 ("contextual subtitles orient business users" — "N entities" with N=0 is still informative). | T6; AC6, AC11 |
| **D9** | **`FcProjectionSubtitle` disposal discipline** — `IAsyncDisposable`. `DisposeAsync` does `_countSubscription?.Dispose()` then completes the `ValueTask`. Subscription stored as a `IDisposable?` field bound in `OnInitialized`. No Rx-over-Rx, no `SemaphoreSlim`, no cancellation chain. | Mirrors 3-5 D20 `BadgeCountService` disposal and the broader Shell pattern: each Blazor component that subscribes to a long-lived `IObservable<T>` owns its own `IDisposable`. Keeps the subtitle circuit-safe. Rejected: (a) rely on GC (observer weak-ref) — non-deterministic, flaky under test; (b) no disposal — leaks subscribers across circuit restarts, same failure mode 3-5 D20 closed; (c) subscribe via `CompositeDisposable` — adds Rx ceremony for one subscription. | T6; AC11 |
| **D10** | **Filter execution for ActionQueue's `WhenState`** — runtime filter inside `{TypeName}View.BuildRenderTree`. The generator emits code that identifies the **single status-enum-typed property** on the projection (same heuristic as StatusOverview's grouping) and filters `state.Items` via `.Where(x => x.{property}.ToString() is "Pending" or "Submitted")` (inline-emitted string switch over the parsed `WhenStates` list). **Memoization (C1 per review round 2)** — the emitted view caches the filtered enumeration against `(state.Items, WhenStates)` so that unrelated `StateHasChanged` ticks (e.g., subtitle count updates from `IBadgeCountService`, unrelated cascading-value refreshes) do NOT re-execute the `.Where`. Emit pattern: `private IEnumerable<T>? _filteredItemsCache; private object? _filteredItemsKey; private IEnumerable<T> GetFilteredItems() { var key = state.Items; if (!ReferenceEquals(key, _filteredItemsKey)) { _filteredItemsCache = state.Items.Where(...).ToList(); _filteredItemsKey = key; } return _filteredItemsCache!; }` — the reducer replaces `state.Items` with a new list reference on every data change (Fluxor contract), so reference-equality keying is sufficient; `WhenStates` is immutable per compilation so it doesn't participate in the key. **Deterministic tiebreaker (per Bob review):** ordering source = `RazorModel.Columns` (already declaration-ordered from Parse stage); priority = (1) enum-typed property with ANY `[ProjectionBadge]`-annotated enum member anywhere on the enum type, (2) first enum-typed property in declaration order, (3) none found. Ties inside (1) broken by declaration order. Comparison is Roslyn `TypeCategory == Enum`; no reflection over property-name strings. If no status-enum property exists on the projection, Transform emits HFC1022 Warning `"ActionQueue projection {type} declares WhenState but has no enum property — filter will be silently dropped at runtime."` AND the generator emits `.Where(_ => true)` so the render pipeline still works. | Runtime string comparison keeps the generator simple — no need for generator-time reflection into the enum's member list beyond the Parse-side validation. The `.ToString()` call on an enum in C# is baseline allocation-free for the common case (JIT-intrinsic fast path) and the projection grid is typically < 100 rows so any hot-path concern is moot. Rejected: (a) emit a precomputed `HashSet<StatusEnum>` at class scope — faster lookups, but the generator would need the enum's fully-qualified name in the IR, tangling Parse with symbol reference (breaks architecture §395 "no Roslyn symbols past Parse"); (b) emit one `Where` per WhenState token OR-joined — readable but quadratic; (c) push filtering to `state` reducer — reducer would need to know about role hints, violating Fluxor purity + coupling state to view strategy. **v2 evolution path (ToT round 3):** projection-query-level push-down — moving the filter to the read-model query so the state pipeline only ever carries filtered rows — is the right long-term architecture, but requires Story 5-4's SignalR/read-model boundary to be in place. v1 ships the client-side filter; Story 5-4 may replace it. See Story 5-4 planning notes. **Heuristic note (ToT round 3):** the "first status-enum property" detection algorithm is duplicated with D11 (StatusOverview grouping); consolidation to a single `ResolveStatusEnumProperty` helper is deferred to Story 4.3 when the heuristic gets a third consumer (filter UI). **Dev Agent directive (round 4 per Bob):** DO NOT consolidate D10 and D11's heuristic into a shared helper during 4-1 implementation — the duplication is intentional. Story 4.3 owns the consolidation as part of introducing the third consumer. A thoughtful dev DRYing this now breaks the deferred-refactor sequencing and forces 4.3 to re-derive the extraction. Leave two copies; add a `// TODO(Story 4.3): consolidate with D11's equivalent in RazorModelTransform.EmitStatusOverviewBody — see D10/D11 heuristic-note` comment at each site so Story 4.3's dev finds the pair. | T3; AC1, AC9 |
| **D11** | **StatusOverview grouping emit path** — the generator finds the same single status-enum-typed property used by D10 (**same deterministic tiebreaker: `[ProjectionBadge]`-carrying enum type first, else declaration-order first enum, ties by declaration order**) and emits `state.Items.GroupBy(x => x.{property}).Select(g => new { Status = g.Key, Count = g.Count() })` as the `Items` source for a FluentDataGrid whose two columns are `Status` (humanized enum label) + `Count` (`N0` format). Group ordering is by descending `Count` then by enum member declaration order (stable sort — `OrderByDescending(g => g.Count()).ThenBy(g => (int)g.Key)`). Row click dispatches `NavigationManager.NavigateTo(FcProjectionRoutes.StatusFilter(boundedContextRoute, g.Key))` — **H2 indirection per review round 2**: the URL format lives inside a static helper `FcProjectionRoutes.StatusFilter(string bcRoute, Enum statusValue)` in Shell (signature hardened at round 4 per Winston — `Enum` not `object`, so `[Flags]` combined values and nullable-enum unboxing mishaps fail at compile time; status value is passed through `Uri.EscapeDataString` to prevent a space or comma in the enum's `ToString()` from corrupting the URL), NOT baked into the emitted view. Story 4.3 can change the filter-query-string encoding (e.g., `?filter=status:X` → `?q=eq(status,X)`) by editing the helper alone, without re-emitting every StatusOverview view. 4-1's helper implementation: `$"{bcRoute}?filter=status:{Uri.EscapeDataString(statusValue.ToString())}"` — same byte output as the naive `{statusValue}` formatter for the common single-value enum case, defensible for the edge cases. The `boundedContextRoute` is computed at emit time from the projection's `[BoundedContext]` attribute (`IFrontComposerRegistry.GetManifests()` would give it at runtime, but emit-time constant is cheaper). **Fallback:** no status-enum property → `EmitDefaultBody` renders instead; AC2's "N total" subtitle still works via the default count path. | Emit-time route constant keeps the generated view self-contained (no registry lookup at render time). The two-column group table is the minimal StatusOverview shape — 4.2 will swap `Status` cell for the badge palette, 4.3 will wire the `?filter=status:` param into real filtering. Rejected: (a) full 6-slot badge matrix now — overlaps Story 4.2 scope; (b) registry lookup at render for the BC route — adds a dependency the subtitle already consumes + creates an unnecessary service dependency on the grouped-view; (c) render a chart — out of scope (UX-DR44 defines a DataGrid shape). **Heuristic note (ToT round 3):** the "first status-enum property" detection algorithm is duplicated with D10 (ActionQueue filter); consolidation to a single `ResolveStatusEnumProperty` helper is deferred to Story 4.3 when the heuristic gets a third consumer (filter UI). **Dev Agent directive (round 4 per Bob):** see D10 for the "do not DRY during 4-1" directive — applies identically here. Leave the two copies in 4-1 even though it looks like low-hanging DRY fruit. | T3; AC2 |
| **D12** | **DetailRecord primary / secondary field split** — the generator splits `Properties` (filtered to supported types per `IsUnsupported` guard) into **primary** = first 6 in declaration order OR first `min(6, count)` if count < 6; **secondary** = the remainder (positions 7+). Primary fields render inline as `<FluentLabel>` + value; secondary fields render inside `<FluentAccordion Expanded="false">` sections. When the projection has ≤ 6 supported properties, the accordion is omitted (empty-accordion DOM noise avoided, following Story 3-5 D17 precedent). | The 6-field primary cap matches the UX spec's "6-8 primary fields" (UX-DR17 / Epic 4.5 AC). Declaration-order is the simplest rule that is both deterministic and cache-stable — no need for a `[FieldGroup]` attribute in v1 (deferred to Story 6-3 Annotation Override). Rejected: (a) 8-field cap — leaves two rows of primary fields which visually compete with the accordion's collapsed-but-visible header; (b) infer "primary" via `[Display(Order)]` — adds an attribute dependency 4-1 doesn't need (Story 4.4's `[ColumnPriority]` will provide ordering); (c) adopter-defined `[PrimaryField]` attribute — attribute sprawl without a v1 need. | T3; AC3 |
| **D13** | **Timeline ordering property detection** — the generator scans `RazorModel.Columns` (which is already sorted declaration-order and filtered to supported types) for the first column whose `TypeCategory == DateTime` (existing category from `RazorModelTransform.MapTypeCategory`). **Deterministic tiebreaker (per Bob review):** ordering source = `RazorModel.Columns` declaration order; priority = (1) first property whose `TypeCategory == DateTime` (which covers both `DateTime` AND `DateTimeOffset` per the existing category mapping), (2) no match → fallback path. Ties are impossible by construction (declaration order has no equal positions). Found → ordering column = that property; sort `Descending`. Not found → HFC1022 Warning `"Timeline projection {type} has no DateTime property — ordering falls back to declaration order."` + ordering column is null (no `OrderBy`). The filter IS emitted as `.OrderByDescending(x => x.{property})` (or omitted on fallback). | The DateTime-first convention matches domain modelling conventions (events carry `OccurredAt`; audit rows carry `Timestamp`). Declaration-order fallback is strictly better than a crash. Rejected: (a) require a `[TimelineOrder]` attribute — attribute sprawl; (b) sort by multiple DateTime properties — ambiguous, over-engineered; (c) warn as Error on missing DateTime — blocks projects that legitimately want chronological rendering of bounded events without a time column (e.g., a step-by-step wizard journey). | T3; AC4 |
| **D14** | **Row-level `ProjectionContext` cascade for ActionQueue / Default paths** — the generator emits `<CascadingValue Value="@_rowContext" IsFixed="true">` as an outer wrap around each row's inline action column (**`IsFixed="true"` per Winston review** — inline commands do not mutate context mid-row, so marking the cascade fixed prevents O(rows) re-render storm when Story 4.4 virtualization reuses row components on scroll). `_rowContext` is built inline per-row as `new ProjectionContext(projectionTypeFqn: "{Namespace}.{TypeName}", boundedContext: "{BoundedContext}", aggregateId: item.{AggregateIdProperty}, fields: ImmutableDictionary.CreateRange<string, object?>(...))` where `{AggregateIdProperty}` is the first property whose name matches `"Id"`, `"AggregateId"`, or `"{TypeName}Id"` (**deterministic tiebreaker:** ordering source = `Properties` declaration order carried from Parse stage; case comparison = `StringComparison.OrdinalIgnoreCase`; first match wins; if no match, `aggregateId = null` and a build-time `HFC1022` Warning is NOT emitted — null aggregate-id is a valid modelling choice for read-only timeline projections). Fields dictionary includes every non-unsupported property as `PropertyName → value`. **Reference stability:** `ProjectionContext` (Story 2-2 contract) MUST remain value-equatable — the contract was designed as a `readonly record struct` in Story 2-2 D27 (verify at T3 exit); building `new ProjectionContext(...)` per row is cheap because structural equality means the `IsFixed="true"` cascade only re-renders descendants when field values change. If Story 2-2 shipped it as a class, Story 4-1 DOES NOT change that contract — instead, file a Story 2-2 follow-up and use `IsFixed="true"` alone (still correct; just allocates one context per row per render pass). | Story 2-2 Decision D27 froze `ProjectionContext` as the cascading parameter for command-row derivable-field resolution. 4-1 is the first story that actually cascades it at per-row scope — without this wire, any `[Destructive]` command rendered inline via Story 2-2's renderer cannot pre-fill its aggregate-id derivable field. The "Id"/"AggregateId"/"{Type}Id" heuristic matches the Story 1-4 / 1-5 `CounterProjection` sample (`public string Id { get; set; }`) and covers 95% of CQRS/ES projection conventions. The `IsFixed="true"` bake-in is load-bearing for Story 4.4 — retrofitting it after virtualization ships is expensive. Rejected: (a) require `[AggregateId]` marker attribute — attribute sprawl; (b) cascade only projection-type + BC (no aggregateId) — breaks Story 2-2's derivable-field pre-fill for per-row commands; (c) build `_rowContext` outside the row loop — cascades the WRONG item's aggregate-id to subsequent rows; (d) skip `IsFixed="true"` "until Story 4.4 proves the regression" — reactive architecture, violates "design for scale when cheap" principle. | T3; AC1, AC5 |
| **D15** | **Unknown-role fallback emits `HFC1024_UnknownProjectionRoleValue`** (H6 per review round 2 — originally shared HFC1022, split to keep the diagnostic semantically narrow). If the attribute parses but the role value is outside the enum (`(ProjectionRole)999` from an unsafe cast), Parse stores the numeric value as the `ProjectionRole` string (e.g., `"999"`); Transform's `TryParse<ProjectionRenderStrategy>` fails, falls back to `Default`, emits HFC1024 Warning with `"Unknown ProjectionRole value '999' on {type} — falling back to Default rendering."` The projection still emits a working Default view. | Fail-soft to Default keeps the build green; a warning surfaces the issue. **Original rationale for reusing HFC1022 (rejected at round 2):** "category overlap is tolerable — both are unknown symbolic reference." **Why overturned:** HFC1022 has a trajectory of accreting meaning (unknown WhenState member in 4-1; likely unknown `[SlotOverride]` target in Epic 6; likely unknown analyzer hint in Story 9-4). A trash-can diagnostic stops being searchable. One code per adopter-failure-category preserves operator signal. Rejected: (a) emit Error — blocks builds for an edge case that almost never happens in real code; (b) silent fallback — hides the adopter's mistake; (c) keep sharing HFC1022 — stated reason above (overturned). | T2, T7; AC7 |
| **D16** | **Dashboard role emits HFC1023 Information once per Dashboard-annotated type per compilation** — deferring full Dashboard rendering to Story 6-3 (Level 3 Slot-level customization gradient is the v1 escape path for rich Dashboard tile content). `ProjectionRenderStrategy.Dashboard` dispatches to `EmitDashboardBody` which calls `EmitDefaultBody` under the hood AND the Transform stage emits HFC1023 at the projection's source location with `"Dashboard projection rendering is deferred to Story 6-3 — {type} falls back to Default DataGrid rendering in v1."` **Dedup ownership (corrected at review round 4 per Winston):** Roslyn's `IIncrementalGenerator` Transform is a STATIC method (per netstandard2.0 source-generator discipline) invoked per-input-item by the `CreateSyntaxProvider` / `ForAttributeWithMetadataName` pipeline — there is no instance to hang a `HashSet<string>` off, and each Dashboard projection hits Transform exactly ONCE per compilation by construction, so NO dedup structure is needed at the Transform stage. The "once per Dashboard-annotated type per compilation" property is an emergent guarantee of Roslyn's per-input invocation model, not a dedup implementation. **Across incremental rebuilds:** the incremental-cache contract (D19 + T1.8) means a cache-hit skips Transform entirely — no diagnostic re-emission. Only a cache-miss (clean build, source edit, compilation parameter change) re-invokes Transform; that re-invocation correctly re-emits HFC1023 as a fresh compilation signal ("your Dashboard projection is still deferred"). An adopter rebuilding a 50-aggregate domain sees the same HFC1023 once per Dashboard projection per clean compilation — this is correct, not a bug. Implementation: simple conditional at Transform (`if (strategy == Dashboard) context.ReportDiagnostic(HFC1023, ...)`); no `HashSet`, no static state, no instance ownership. | Preserves the PRD FR4 enum cap at 5 while making it explicit that Dashboard is **reserved, not broken**. A developer annotating `[ProjectionRole(ProjectionRole.Dashboard)]` today sees a working DataGrid + a build log explaining the deferral. Rejected: (a) remove Dashboard from the enum — PRD FR4 explicitly lists it at capped=5 including Dashboard; (b) emit Error — blocks adopters who want to start wiring dashboards before v1.1; (c) silent fallback without HFC1023 — surprises the developer later; (d) instance-scoped `HashSet<string>` dedup (round 2 F3 phrasing) — overturned at round 4: there is no Transform instance in an `IIncrementalGenerator`, and Roslyn's per-input invocation model already provides "once per type per compilation" without any dedup code; (e) file-system scratch dedup across rebuilds — unnecessary, incremental cache already suppresses re-emission on cache-hit paths. | T3, T7; AC10 |
| **D17** | **Three new diagnostic IDs reserved** (split from two at review round 2 — H6): `HFC1022_ProjectionWhenStateMemberUnknown` at **Warning** severity (structured payload `{ProjectionTypeName, AttributeMemberName, ValidMemberNames[], ValidMemberNamesTruncatedCount}` — `ValidMemberNames` per F6 so the message can render the "Valid members:" list verbatim; capped at 10 alphabetically with `ValidMemberNamesTruncatedCount` counting the remainder per War Room round 3); `HFC1023_ProjectionRoleDashboardFallback` at **Information** severity (structured payload `{ProjectionTypeName}`); `HFC1024_UnknownProjectionRoleValue` at **Warning** severity (structured payload `{ProjectionTypeName, RawRoleValue}`). All three ship with full entries in `DiagnosticDescriptors.cs` (descriptor + message format), `FcDiagnosticIds.cs` (documentation XML), `AnalyzerReleases.Unshipped.md` (Warning/Information rows), AND the `FrontComposerGenerator.GetDescriptor` switch arm (so runtime diagnostics flow through the same translation layer). | One diagnostic per behavioral category is the established pattern (Story 3-2 D12 / 3-4 D19 / 3-5 D18 / 3-6 D11) — and per H6, explicitly resisting the drift of HFC1022 into a trash-can code for every "unknown symbolic reference." Warning for WhenState because it indicates an adopter modelling mistake (typo that WILL cause no-render rows at runtime); Information for Dashboard because it is a known-deferred feature, not a bug; Warning for unknown-role-value because it indicates an unsafe cast at attribute-site (developer mistake). Structured payloads give operators searchable fields. Rejected: (a) single combined HFC1022 covering both WhenState + unknown-role — overturned at round 2 (H6); (b) Warning for Dashboard — false positive noise; (c) skip the analyzer release entries — breaks the analyzer-release contract enforced by Story 9-4's build-time gate. | T2, T7; AC9, AC10 |
| **D18** | **Resource key naming + 8 new keys** — follow the Story 3-5 D19 template-vs-plain convention. New EN keys: `HomeActionQueueSubtitleTemplate` (`"{0} {1} awaiting your action"`), `HomeStatusOverviewSubtitleTemplate` (`"{0} total across {1} statuses"`), `HomeDefaultSubtitleTemplate` (`"{0} {1}"`), `HomeDetailSubtitleTemplate` (`"{0} overview"` — Sally copy nudge), `HomeTimelineSubtitleTemplate` (`"{0} events"`), `HomeEmptyPlaceholderText` (`"No {0} yet."`), `HomeEmptyPlaceholderAriaLabel` (`"No {0} found"`), `FcProjectionLoadingSkeletonAriaLabel` (`"Loading {0}"`). FR translations: `"{0} {1} en attente d'action"`, `"{0} au total dans {1} statuts"`, `"{0} {1}"` (identity — French plural handling is noun-dependent, Story 9-5 covers ICU pluralization), `"{0} - aperçu"`, `"{0} événements"`, `"Aucun {0} pour le moment."`, `"Aucun {0} trouvé"`, `"Chargement de {0}"`. All keys carry an inline `<comment>` tag referencing Story 4-1 + the relevant AC. **Pre-dev-complete French pre-pass: three keys need native-speaker review before merge** — `HomeActionQueueSubtitleTemplate`, `HomeDefaultSubtitleTemplate`, `HomeEmptyPlaceholderText`. The remaining 5 are deferred to Story 9-5 per Known Gap G23 precedent. **Pre-pass discipline (per Sally's review):** the reviewer MUST see sample RENDERS with realistic French nouns substituted (`commandes`, `factures`, `événements` — not `{0}` placeholders or synthetic "entities" shells); if any sample reads grammatically wrong (e.g., `"3 commande en attente"` singular-vs-plural mismatch) the key is ESCALATED to Story 9-5 ICU pluralization rather than merged as-is. Sample-render pack lives at `tests/Hexalith.FrontComposer.Shell.Tests/Resources/FrenchPrePassSamples.md` (T8 artifact). | Minimal key count matches UX spec wording 1:1; `{0}` / `{1}` placeholders use `string.Format(IStringLocalizer[...].Value, ...)` at call site (existing shell convention). Pre-pass on 3 highest-impact keys matches Story 3-5 D19's discipline (first-impression strings get native review, long-tail doesn't). Rejected: (a) defer all 8 to 9-5 — same trust gap Story 3-5 closed with partial pre-pass; (b) pre-pass all 8 — unnecessary when 5 are compositional placeholders with low emotional weight; (c) skip French entirely until 9-5 — breaks `CanonicalKeysHaveFrenchCounterparts` and NFR60. | T8; AC6, AC12 |
| **D19** | **Incremental-build cache stability (Story 1-4 ADR-006)** — the new `DomainModel.ProjectionRoleWhenState` field + `RazorModel.Strategy` / `RazorModel.WhenStates` fields MUST participate in both `Equals` AND `GetHashCode`. A Parse-cached `DomainModel` whose `ProjectionRoleWhenState` changes between builds MUST produce a different hash → force Transform → force Emit re-run. Without this, an adopter editing `WhenState = "Pending"` → `WhenState = "Pending,Submitted"` would see the generator cache hit and emit the old filter. **Test gate:** new `IncrementalGeneratorCacheParityTests` rows under `tests/Hexalith.FrontComposer.SourceTools.Tests/Incremental/` assert `Equals` flips to `false` when any new field is mutated. | Story 1-4 shipped the incremental-generator harness; 4-1's additions MUST ride that harness or the generator silently ships stale output. Rejected: (a) rely on existing `Equals` via structural record equality — `DomainModel` is a class (not a record) because it holds `EquatableArray<T>` fields; structural `Equals` is hand-rolled; (b) re-run generator on every build — defeats the incremental-generator performance baseline; (c) parse `WhenState` at Emit instead of storing in IR — duplicates the parse rule + fragments cache semantics. | T1, T2; AC9 |
| **D20** | **L04 generated-name determinism preserved** — the generator continues to emit exactly one artifact per projection: `{Namespace}.{TypeName}.g.razor.cs`. The strategy-specific body lives inside the single emitted file; no second file per role. Snapshot tests (`RazorEmitterApprovalTests.CounterProjection_approved`) stay the single source of truth for the Default strategy; new approval files land for each role (`CounterProjection.ActionQueue.approved.cs` via parameterized tests on synthetic projections under `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RoleSpecificProjections/`). | Story 1-5 locked `{Namespace}.{TypeName}.g.razor.cs` as the canonical artifact; adding per-role files would break the "one in, one out" invariant adopters rely on for `\.g\.razor\.cs$` ignore patterns and diff review. Synthetic test projections avoid polluting the sample domain with role-per-projection test fixtures (Counter has exactly one projection). Rejected: (a) emit `{Type}.{Role}.g.razor.cs` per role — breaks L04 determinism; (b) fold approval tests into one synthetic "all roles" projection — one projection cannot carry 6 roles via single attribute; (c) test each role as a full integration test without approval — loses snapshot diff visibility on the emitted shape, which is the primary regression surface. | T5; AC5 |
| **D21** | **Subtitle count subscription is per-view, not per-row** — the subtitle component subscribes once in its own `OnInitialized` lifecycle; each generated view instantiates exactly one `<FcProjectionSubtitle>` at render time (NOT one per row). Row-level components do NOT subscribe to `IBadgeCountService`. The count value refreshes when `IBadgeCountService.CountChanged` emits for the specific projection type (subtitle filters by `args.ProjectionType == _projectionType` before dispatching `StateHasChanged`). | Row-level subscriptions would scale O(rows × views × circuits) — a 500-row grid on 3 tabs equals 1,500 subscriptions, which will observably lag `CountChanged` fanout. Per-view subscription is O(views × circuits), which is 2-3 orders of magnitude smaller. The filter inside `OnCountChanged` prevents rebuilds triggered by OTHER projections' count changes. Rejected: (a) subscribe in each row for "more real-time" updates — wrong scale; (b) skip the filter — causes `StateHasChanged` on every count change across all projections; (c) subscribe in the host layout once and fan out via cascading value — couples the subtitle to a layout-level owner the generator doesn't control. | T6; AC11 |
| **D22** | **Scope guardrail enforcement via a focused test** — `RazorEmitterScopeGuardrailTests.ProjectionViewBodyStaysMinimal` scans every emitted approval file under `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/approved/` (plus the new `RoleSpecificProjections/approved/`) and asserts that the generated body does NOT include forbidden tokens: `Virtualize`, `ItemsProvider`, `FluentSearch`, `FcColumnPrioritizer`, `FcEmptyState` (the full CTA variant), `CaptureGridStateAction`, `HubConnection`. A positive hit fails the test and points the implementer at the scope-guardrails list above. Each forbidden token is tied to a future story; the assertion message names the story. | Scope creep in the generator is the highest-cost regression (every adopter rebuilds). A single test that scans the approval corpus catches "accidentally emitting Story 4.3's filter column" faster than any review round. The forbidden-token list is explicit, not pattern-matched — false positives would halt the build until the story-owner updates the exclusion list intentionally. Rejected: (a) rely on review-round vigilance — reviewer fatigue, silent regressions; (b) forbid by regex (`/Virtual\w+/`) — over-matches on adopter-named types; (c) enforce per-story via separate guard files — fragmentation, harder to find when the generator changes. | T5, T7; AC5 |

---

## Architecture decision records

### ADR-051 — PRD FR4 is authoritative over Epic 4 AC drift on attribute name + 5th role

**Status:** Accepted (Story 4-1 D1).

**Context:** Epic 4 Story 4.1 AC text reads `[ProjectionRoleHint(RoleHint.ActionQueue, WhenState = "Pending,Submitted")]` with roles `ActionQueue, StatusOverview, DetailRecord, Timeline, Default`. PRD FR4 reads `[ProjectionRole(role)]` with roles `ActionQueue, StatusOverview, DetailRecord, Timeline, Dashboard — capped at 5`. The existing codebase ships `ProjectionRoleAttribute` + `ProjectionRole { ActionQueue, StatusOverview, DetailRecord, Timeline, Dashboard }` (matches PRD). Story 3-5's `IActionQueueProjectionCatalog` already depends on both the attribute name and `ProjectionRole.ActionQueue`.

**Decision:** PRD FR4 wins. Keep `ProjectionRoleAttribute` + `ProjectionRole` enum unchanged. Treat Epic AC's `[ProjectionRoleHint]` + `Default` as drift between Epic authoring (narrative story breakdown) and PRD FR4 (versioned capability contract). "Default" becomes an internal `ProjectionRenderStrategy.Default` transform-stage sentinel — NEVER a public enum value.

**Rejected alternatives:**

- **Rename `ProjectionRoleAttribute` → `ProjectionRoleHintAttribute` AND add `ProjectionRole.Default` enum value.** Breaks the PRD FR4 capability contract + the 3-5 catalog's reflection scan (`GetCustomAttribute<ProjectionRoleAttribute>()?.Role == ProjectionRole.ActionQueue`). Requires migration for the adopter sample (Counter) + every test that references the types by their current names. Cosmetic rename with zero capability benefit.
- **Treat Epic as authoritative over PRD.** Inverts the spec hierarchy established by §implementation-readiness-report-2026-04-12.md, which ships the PRD as v1.0 contract and the Epic as its story-breakdown. Every other Epic 1-3 story has resolved PRD-vs-Epic drift by deferring to PRD. Changing the rule for Epic 4 creates a one-off precedent that future Epic authors can weaponize.
- **Introduce both names as aliases.** Both `[ProjectionRole]` and `[ProjectionRoleHint]` attributes registered as equivalent. The catalog checks both. Doubles the attribute surface + test matrix, violates PRD FR4's "capped at 5 — no more, no less" discipline.

**Consequences:**

- Epic 4 text remains unchanged (editorial drift acceptable; story spec is the canonical place to resolve it).
- 3-5 catalog works unmodified.
- Counter sample + all existing tests compile unchanged.
- A future adopter reading Epic 4 expecting `[ProjectionRoleHint]` gets a compile error on the annotation; Dev Notes §Known gaps records the drift with a pointer to PRD FR4 as the authoritative shape. Story 9-5 documentation synchronizes the Epic text with PRD at next docs pass.

**Verification:** `ProjectionRoleAttributeTests.AttributeNameMatchesPrdFR4`, `ProjectionRoleTests.EnumMembersMatchPrdFR4CapOf5`, `IActionQueueProjectionCatalogTests.StillScansProjectionRoleAttribute` (regression lock — Story 3-5 coverage).

---

### ADR-052 — Role-dispatched body emission via single-switch strategy, not polymorphism

**Status:** Accepted (Story 4-1 D4 + D5).

**Context:** Six render strategies (Default, ActionQueue, StatusOverview, DetailRecord, Timeline, Dashboard) share a common view shell (class header, lifecycle hooks, formatters, Loading skeleton, Empty placeholder, subtitle) but differ in the `state.Items.Count > 0` body. Two canonical structures: (a) `switch (strategy)` dispatch to strategy-specific static helpers; (b) polymorphism via `IProjectionBodyEmitter` implementations with `IEnumerable<IProjectionBodyEmitter>` injection.

**Decision:** Single-switch strategy in `RazorEmitter.Emit`, delegating to `ProjectionRoleBodyEmitter.Emit{Strategy}Body` static methods. **Switch is an EXPRESSION (not statement) with a throwing default arm** — per Winston review, this guarantees compile-time exhaustiveness. When a future story adds a strategy without wiring the case, the build fails with a clear error instead of shipping a blank-view regression. Shared scaffolding (loading shell, empty shell, formatters, subtitle host, class header) stays in `RazorEmitter` private helpers.

**Rejected alternatives:**

- **Polymorphism via `IProjectionBodyEmitter`.** Runtime DI is unavailable in netstandard2.0 source generators; source generators cannot use `Microsoft.Extensions.DependencyInjection`. A hand-rolled `IEnumerable<>` via reflection-scanned strategy classes adds complexity with no adapter-extensibility payoff (adopters extend rendering via the Story 6 customization gradient, not by implementing emitter strategies).
- **Six separate emitter classes, each owning the full class header + scaffolding.** Duplicates loading/empty/subtitle code six times; any future shared-scaffolding change (e.g., adding telemetry hooks in Story 9-4) requires six file edits instead of one.
- **String-templated body via Razor-like placeholder resolution.** Loses compile-time exhaustiveness check on strategies; a missing case produces silent empty emissions. The switch with SourceTools-internal enum surfaces exhaustiveness warnings at compile time when roles are added.

**Consequences:**

- Adding a new role (e.g., Story 6-3 `Dashboard` specialisation, or a future `SummaryKpi`) requires (a) adding an enum member, (b) adding a `case` in the switch, (c) adding one `EmitXBody` static — compile-time exhaustiveness catches missing switch arms.
- `RazorEmitter.cs` line count grows modestly (Default body stays; additions are the switch + calls); `ProjectionRoleBodyEmitter.cs` stays small (5-10 static methods, each ~30-60 lines).
- Approval-test harness catches strategy-specific output regressions via snapshot per role.

**Verification:** `RazorEmitterStrategyDispatchTests.EveryStrategyDispatchIsExhaustive`, `ProjectionRoleBodyEmitterTests.Emit{Strategy}Body_MatchesApprovedSnapshot` (six methods × 1 test each = 6), `RazorEmitterScopeGuardrailTests.ProjectionViewBodyStaysMinimal` (D22).

---

### ADR-053 — `FcProjectionEmptyPlaceholder` is a Story-4.6-replaceable Shell component, NOT an emitter-level empty block

**Status:** Accepted (Story 4-1 D7).

**Context:** Every generated projection view needs an empty-state body. Story 4.6 specifies the full FcEmptyState with CTA button, secondary text, role=status, aria-label — that work is not 4-1's scope. Two choices: (a) emit a minimal inline empty block in 4-1, replaced by a Story-4.6 regeneration pass; (b) emit a stable `<FcProjectionEmptyPlaceholder>` invocation in 4-1, where Story 4.6 swaps the Shell component body without touching the generator.

**Decision:** Emit `<FcProjectionEmptyPlaceholder ProjectionType="@typeof({TypeName})" />` in 4-1. Story 4.6 replaces the component body (CTA button, secondary text, etc.) WITHOUT changing the generator call-site.

**Rejected alternatives:**

- **Inline minimal empty block in 4-1; regenerate in 4.6.** Every adopter rebuilds on 4.6 upgrade. Generator diff under `\.g\.razor\.cs$` creates noise. Regression surface (a 4.6 generator bug re-emits all projection views).
- **Ship a full empty state in 4-1 (CTA, icon, secondary text).** Overlaps Story 4.6's AC scope. Couples 4-1 to command-per-bounded-context resolution (which command is "the" one to pre-fill for CTA?). Violates scope guardrail L07 (test count inflation).
- **Generate no empty block; adopter registers a global empty component.** Negates the "works without ceremony" promise (FR11). First-run Counter has a blank region on empty data — UX regression.

**Consequences:**

- 4-1 ships a functional empty placeholder today (muted icon + "No {entities} yet." text).
- Story 4.6 becomes a pure Shell-level change: replace the component body, update its tests, keep parameter list + type name stable.
- The emitter contract `<FcProjectionEmptyPlaceholder ProjectionType="@typeof(T)" Role="ProjectionRole.{role}" />` is frozen; **parameter list is EXPLICITLY APPEND-ONLY** (Graph-of-Thoughts finding at review round 2): Story 4.6 MAY add parameters (e.g., `SecondaryText`, `CtaCommand`) but MUST NOT remove, rename, or reorder existing parameters. This is the same append-only discipline applied to `ProjectionAttribute` + `ProjectionRoleAttribute` at Story 1-2 — consistent across the framework's public surfaces.
- **Role parameter emitted in 4-1 even though 4-1 doesn't consume it** (H4 per review round 2): the generator emits `Role="ProjectionRole.{role}"` on every `<FcProjectionEmptyPlaceholder>` invocation. 4-1's component ignores the value; 4.6 consumes it for role-differentiated CTA copy ("Create first order" on ActionQueue-empty vs. "View past activity" on Timeline-empty). Zero emitter diff when 4.6 lands — saves every adopter a rebuild.
- **This is the first instance of a broader pattern** — Shell-component-as-pluggable-slot. The generator emits a stable invocation to a Shell component whose body is replaced out-of-band by future stories. Story 6-3's `[SlotOverride]` mechanism consumes this pattern (not yet implemented; the pattern precedent matters). The mechanism: frozen parameter list + component name; body swap is a Shell-internal concern. Any future "Story X replaces component Y" spec should cite ADR-053 as the template.
- No generator regeneration on 4.6 upgrade.

**Verification:** `FcProjectionEmptyPlaceholderTests.RendersMinimalPlaceholder_WithEntityPluralAndIcon`, `FcProjectionEmptyPlaceholderTests.ParameterSurface_IsFrozenForStory4_6` (documents the frozen shape; the Role parameter is declared but not consumed in 4-1 — test asserts it is present + optional + typed `ProjectionRole`), `RazorEmitterScopeGuardrailTests.ProjectionViewBodyStaysMinimal` asserts the emitter never emits "SendYourFirstCommand" / CTA copy inline.

---

## Tasks / Subtasks

All tasks MUST run under the `bmad-dev-story` workflow discipline — each task's acceptance is verified by tests written in the same task, not batched later.

### T1 — Parse-stage WhenState extraction + DomainModel extension (AC9, D2, D3, D19)

- [ ] **T1.1** Extend `ProjectionRoleAttribute` with `WhenState` init-only property (default null). Update XML-doc.
- [ ] **T1.2** Extend `DomainModel` with `ProjectionRoleWhenState` (nullable string) trailing constructor parameter (optional, default null). Update `Equals` + `GetHashCode`.
- [ ] **T1.3** Extend `AttributeParser.Parse` to read `WhenState` from `NamedArguments`; populate `ProjectionRoleWhenState` on the parsed `DomainModel`.
- [ ] **T1.4** Add enum-member validation: when the projection declares an enum-typed property (with `[ProjectionBadge]` OR declaration-order first enum), parse the CSV and emit HFC1022 Warning for unknown members. Unknown members still flow through to the IR.
- [ ] **T1.5** Reserve `HFC1022_ProjectionWhenStateMemberUnknown` + `HFC1023_ProjectionRoleDashboardFallback` in `DiagnosticDescriptors.cs` + `FcDiagnosticIds.cs` + `AnalyzerReleases.Unshipped.md`. Wire both into `FrontComposerGenerator.GetDescriptor`.
- [ ] **T1.6** Tests (SourceTools): `ProjectionRoleAttributeParserTests.ReadsWhenStateFromNamedArgument`, `...CsvSplitTrimsAndRemovesEmptyEntries`, `...UnknownMemberEmitsHFC1022`, `...EmptyWhenStateIsTreatedAsNull`, `...ValidMembersPassThrough`, **`...ExplicitNullWhenStateIsTreatedAsAbsent`** (FMA round 3 — `[ProjectionRole(Role.ActionQueue, WhenState = null)]` must behave identically to omitting `WhenState` entirely; asserts `ProjectionRoleWhenState == null` on DomainModel), **`...HFC1022MessageListsUpTo10ValidMembersAlphabetically`** (War Room round 3 — enum with 15 members produces message listing first 10 alphabetically + `"… and 5 more"`). ≥ 9 tests (+1 from baseline).
- [ ] **T1.7** Incremental-cache parity tests (D19): `DomainModelCacheEqualityTests.WhenStateFieldIsPartOfEquality`, `RazorModelCacheEqualityTests.StrategyAndWhenStatesAreParticipantsInEquality`, **`RazorModelCacheEqualityTests.TwoIndependentTransformsOfIdenticalSourceProduceEqualModels` (round 4 per Winston)** — run `RazorModelTransform.Transform` twice over the same `DomainModel` instance (independent `EquatableArray<string>` allocations for `WhenStates`), assert `model1.Equals(model2) == true` AND `model1.GetHashCode() == model2.GetHashCode()`. Necessary because Roslyn's incremental cache compares successive IR snapshots by value-equality; if `EquatableArray<T>.Equals` degrades to reference-equality (e.g., a refactor replaces it with a bare `ImmutableArray<T>`), incremental caching silently breaks for every role-annotated projection on every rebuild — `WhenStateFieldIsPartOfEquality` catches "different inputs produce different equality," but NOT "same inputs produce different hashes." This test closes that blind spot. **`DomainModelCacheEqualityTests.TwoIndependentParsesOfIdenticalSourceProduceEqualModels` (round 4)** — same assertion at the Parse-stage IR. 6 tests (+2 from round 3).
- [ ] **T1.8** **Incremental-generator cache-hit test (per Winston review)**: `IncrementalGeneratorCacheHitTests.SameInputTwiceProducesZeroNewSymbols` — compiles the synthetic ActionQueueProjection.cs, captures `GeneratorRunResult.Results[0].GeneratedSources`, re-runs the generator with the identical input, asserts the second run's `TrackedSteps` show `IncrementalStepRunReason.Cached` for Parse/Transform/Emit nodes. Also `...EditingWhenStateInvalidatesCache` — same input except `WhenState = "Pending"` → `"Pending,Submitted"`, asserts Parse/Transform/Emit rerun (NOT Cached). Guards against D19's silent-stale-emit bug. 2 tests.

### T2 — Transform-stage strategy mapping (AC1-AC5, AC7, AC10, D4, D15, D16)

- [ ] **T2.1** Add `ProjectionRenderStrategy` enum in `SourceTools/Transforms/`.
- [ ] **T2.2** Add `Strategy` + `WhenStates` (EquatableArray<string>) fields to `RazorModel`; update `Equals` + `GetHashCode` + constructor.
- [ ] **T2.3** Extend `RazorModelTransform.Transform` to map `DomainModel.ProjectionRole` → `ProjectionRenderStrategy` (null/empty → Default; exact match → strategy; unknown → Default + HFC1022).
- [ ] **T2.4** Extend `RazorModelTransform.Transform` to split `DomainModel.ProjectionRoleWhenState` CSV into `RazorModel.WhenStates` (empty array when null; trim + RemoveEmptyEntries).
- [ ] **T2.5** For Dashboard strategy, emit HFC1023 Information unconditionally at Transform (`if (strategy == Dashboard) context.ReportDiagnostic(HFC1023, ...)`). **No dedup structure required** — corrected at review round 4 per Winston: Roslyn's `IIncrementalGenerator` invokes the static Transform per-input-item, so each Dashboard projection produces exactly one HFC1023 per compilation by construction. Cache-hit rebuilds skip Transform entirely (D19 + T1.8), suppressing re-emission; clean rebuilds correctly re-emit. Do NOT add a `HashSet<string>` — there is no Transform instance to own it and the per-input invocation model already provides the "once per type per compilation" property.
- [ ] **T2.6** Tests: `RazorModelRoleStrategyTests.MapsAllKnownRoleNames`, `...NullDomainRoleMapsToDefault`, `...UnknownRoleValueMapsToHFC1024` (renamed from `UnknownRoleValueMapsToDefaultWithHFC1022` — H6 round 2 diagnostic split), `...WhenStateCsvIsSplitAndTrimmed`, `...DashboardRoleEmitsHFC1023Once`, **`...TransformRoleMappingIsExhaustive`** (FMA round 3 — asserts the Transform-stage `DomainModel.ProjectionRole → ProjectionRenderStrategy` mapping uses switch-expression-with-throwing-default, parity with Emit-stage ADR-052; a future role added to `ProjectionRenderStrategy` without a mapping case must fail the build LOUDLY, not silently become Default). ≥ 7 tests.

### T3 — Emit-stage role body dispatch (AC1-AC5, AC8, AC10, D5, D6, D7, D10-D14) + Contracts-surface gate

- [ ] **T3.1** Extract shared scaffolding from `RazorEmitter.Emit` into private static helpers: `EmitClassHeader`, `EmitFormatters`, `EmitLoadingShell`, `EmitEmptyShell`, `EmitSubtitleInvocation`.
- [ ] **T3.2** Create `ProjectionRoleBodyEmitter` with static methods `EmitDefaultBody`, `EmitActionQueueBody`, `EmitStatusOverviewBody`, `EmitDetailRecordBody`, `EmitTimelineBody`, `EmitDashboardBody`.
- [ ] **T3.3** `EmitDefaultBody`: re-home the existing Story 1-5 DataGrid emission into this method. Output equivalence proved by the existing `CounterProjection.approved.cs` snapshot (diff boils down to subtitle + loading shell + empty shell inserts).
- [ ] **T3.4** `EmitActionQueueBody`: compact DataGrid + inline-action cascading-value wrap + runtime `Where` filter on `WhenStates` list (inline-emitted string switch). Emit HFC1022 if WhenState is declared but no status-enum property exists.
- [ ] **T3.5** `EmitStatusOverviewBody`: status-enum grouping → 2-column DataGrid (Status, Count) with click-through navigation to `?filter=status:{value}`. Fallback to Default when no status enum.
- [ ] **T3.6** `EmitDetailRecordBody`: `FluentCard` with primary (first 6) fields inline + `FluentAccordion` for secondary (7+).
- [ ] **T3.7** `EmitTimelineBody`: `FluentStack Vertical` ordered by first DateTime property descending (declaration-order fallback + HFC1022 when none).
- [ ] **T3.8** `EmitDashboardBody`: delegate to `EmitDefaultBody` + HFC1023 emitted from Transform (T2.5).
- [ ] **T3.9** Replace `RazorEmitter.Emit` body with `switch (model.Strategy)` dispatch. Keep existing Default approval test green.
- [ ] **T3.10** Tests (SourceTools): `RazorEmitterStrategyDispatchTests.EveryStrategyDispatchIsExhaustive` (runs all 6 roles through a synthetic fixture), `...ActionQueueBodyFiltersByWhenState`, `...ActionQueueBodyMemoizesFilterResult` (C1 per review round 2 — asserts the emitted view re-runs `.Where` only when `state.Items` reference changes, not on unrelated `StateHasChanged`), `...StatusOverviewBodyGroupsByEnumProperty`, `...StatusOverviewRowClickFiresNavigationWithFcProjectionRoutesHelper` (G8 per review round 2 — bUnit `NavigationManagerInterceptor` stub, asserts `NavigateTo` is called with EXACTLY the URL returned by `FcProjectionRoutes.StatusFilter(bcRoute, statusValue)`; the test references the helper, not a hard-coded format string, so Story 4.3 changing the helper auto-updates this test), `...DetailRecordBodySplitsPrimaryVsSecondary`, `...TimelineBodyOrdersByDateTime`, `...DashboardBodyDelegatesToDefault`, `...EmptyShellAlwaysInvokesFcProjectionEmptyPlaceholder`, `...EmptyShellEmitsRoleParameter` (H4 per review round 2 — asserts the emitted invocation includes `Role="ProjectionRole.{role}"` for every strategy, since 4-1's generator emits the parameter even though 4-1's component doesn't consume it), `...LoadingShellAlwaysInvokesFcProjectionLoadingSkeleton`, `...LoadingShellEmitsCorrectLayoutVariantPerStrategy` (G5 per review round 2 — DataGrid/Card/Timeline roles emit matching `Layout=` literal), `...SwitchDefaultArmThrowsOnUnhandledStrategy` (exhaustiveness discipline per ADR-052), `...RowContextDoesNotLeakAcrossNavigation` (F4 per review round 2 — renders two ActionQueue views on the same bUnit `TestContext`, clicks a row in view-A, navigates to view-B, asserts view-B's rendered `ProjectionContext.AggregateId` ≠ view-A's clicked-row AggregateId; guards against layout-owned cascade leaks), **`...SubtitleIsEmittedOutsideRowCascade`** (FMA round 3 — asserts the emitted `<FcProjectionSubtitle>` invocation lives ABOVE the row loop / outside any `<CascadingValue Value="_rowContext">` wrap; if an emitter refactor accidentally placed the subtitle inside the row cascade, the subtitle would receive per-row context and re-render per row — catastrophic subscription fanout), **`...RowContextIsConstructedAsLoopLocal_NotClassField` (round 4 per Winston)** — code-shape companion to `RowContextDoesNotLeakAcrossNavigation`: regex-scans every approved `.cs` under `RoleSpecificProjections/approved/` and asserts the output does NOT declare `private ProjectionContext _rowContext` (or `protected`, or `internal`) at class scope; asserts that EVERY mention of `_rowContext` appears inside a row-iteration construct (`foreach`, `for (`, `@foreach`, or within a `<CascadingValue Value="_rowContext"` wrapped by such an iterator). The behavioral `RowContextDoesNotLeakAcrossNavigation` test catches the symptom; this code-shape test locks the invariant at the emitter level — if a future refactor hoists `_rowContext` into a class field, the symptom might be masked by a test-order quirk, but the code-shape scan catches the regression deterministically. Fail message: `"_rowContext must be a per-iteration local in the row loop, not a class field — see D14 + AC1d for rationale."` ≥ 18 tests (+1 over round 3).
- [ ] **T3.11** **Contracts-surface tests MOVED from T7 (per Bob/Murat review)** — the L01 cross-story contract surface must be verified BEFORE emit-stage exit, not at T7 (75% into the story). New tests in `Hexalith.FrontComposer.Contracts.Tests`: `ProjectionRoleAttributeTests.WhenStatePropertyExistsAsOptional`, `ProjectionRoleAttributeTests.AttributeNameMatchesPrdFR4`, `ProjectionRoleTests.EnumMembersMatchPrdFR4CapOf5`, `ProjectionRoleTests.DashboardIsStillPartOfEnum`. 4 tests (rebalanced from original 6 — enum cap + attribute shape don't need 6 separate tests, per Murat review).
- [ ] **T3.12** **ProjectionContext reference-stability assertion (C2 per review round 2)** — D14's per-row cascade assumes `ProjectionContext` is a `readonly record struct` (structural equality + zero heap allocation per row). Story 2-2 D27 froze this shape, but 4-1 is the FIRST per-row consumer and would absorb the allocation cost silently if 2-2 drifted. Add `ProjectionContextTypeTests.IsReadonlyRecordStruct` under `Hexalith.FrontComposer.Contracts.Tests`: asserts `typeof(ProjectionContext).IsValueType == true` AND `typeof(ProjectionContext).GetCustomAttribute<IsReadOnlyAttribute>() != null`. If the assertion fails, 4-1 dev HALTS and files a Story 2-2 follow-up — the D14 cascade is load-bearing for Story 4.4 virtualization and cannot ship on a class. 1 test.

### T4 — FcProjectionLoadingSkeleton + FcProjectionEmptyPlaceholder Shell components (AC8, D6, D7)

- [ ] **T4.1** Create `FcProjectionLoadingSkeleton.razor` + `.razor.cs` + `.razor.css`. Parameters: `ColumnCount`, `RowCount` (default 5), `AriaLabel` (default resource), **`Layout` (`SkeletonLayout` enum, default `SkeletonLayout.DataGrid`) — G5 per review round 2, matches D6 role-aware skeleton variants**. Also create `SkeletonLayout.cs` (enum `{ DataGrid, Card, Timeline }`) in the same folder.
- [ ] **T4.2** Create `FcProjectionEmptyPlaceholder.razor` + `.razor.cs`. Parameters: `ProjectionType`, `EntityPluralOverride`, **`Role` (`ProjectionRole?`, default null) — H4 per review round 2: emitted from generator for every projection view, unused in 4-1 body, consumed by Story 4.6 for role-differentiated CTA**. Component body ignores `Role` today; only the parameter shape is frozen.
- [ ] **T4.3** Create `FcProjectionRoutes.cs` (H2 per review round 2) — static class with **`public static string StatusFilter(string bcRoute, Enum statusValue)` (signature hardened at review round 4 per Winston)**: reject `object` in favor of `Enum` so `[Flags]` combined values and nullable-enum unboxing mishaps surface at compile time; wrap the value via `Uri.EscapeDataString(statusValue.ToString())` so an adopter enum named `Rejected, Cancelled` (hypothetical `[Flags]` on a status projection) doesn't corrupt the URL. Implementation: `ArgumentNullException.ThrowIfNull(bcRoute); ArgumentNullException.ThrowIfNull(statusValue); return $"{bcRoute}?filter=status:{Uri.EscapeDataString(statusValue.ToString())}";` — same byte output as before for the common single-value case, defensible for the edge cases. Indirection point for Story 4.3 query-string format changes.
- [ ] **T4.4** Pluralization helper (simple "s" suffix for English; French uses identity — ICU deferred to Story 9-5).
- [ ] **T4.5** Tests (Shell): `FcProjectionLoadingSkeletonTests.RendersNxMGrid_AriaBusyTrue_RoleStatus`, `...RespectsColumnCountCap`, `...RendersCardLayoutWhenLayoutIsCard`, `...RendersTimelineLayoutWhenLayoutIsTimeline`; `FcProjectionEmptyPlaceholderTests.RendersHumanizedEntityPlural_WithIconAndMessage`, `...ParameterSurfaceIsFrozenForStory4_6` (asserts ProjectionType + EntityPluralOverride + Role are all present, append-only; Role is optional and ignored by 4-1 body); `FcProjectionRoutesTests.StatusFilterBuildsCorrectUrl`, `...StatusFilterEscapesSpecialCharsInStatusValue`, **`...StatusFilterThrowsOnNullBcRoute`** (FMA round 3 — `ArgumentNullException` so broken nav surfaces at dev-time, not as a malformed runtime URL), **`...StatusFilterThrowsOnNullStatusValue`** (FMA round 3 — same), **`...StatusFilterUriEscapesStatusValue`** (round 4 per Winston — passing an enum whose `ToString()` contains a URL-unsafe character — e.g., `[Flags]` `Pending | Submitted` renders as `"Pending, Submitted"` with a space — asserts the resulting URL contains `%20` or `%2C%20`, not raw comma-space, so a combined-flags value can't silently corrupt routing), **`...StatusFilterRequiresEnumParameter`** (round 4 per Winston — compile-time signature lock: calling the helper with a non-`Enum` type must be a compile error; the test asserts the signature type via reflection `typeof(FcProjectionRoutes).GetMethod("StatusFilter")!.GetParameters()[1].ParameterType == typeof(Enum)`). ≥ 12 tests (+2 from round 3 for Enum-signature hardening).

### T5 — Generator integration + scope-guardrail test (AC5, AC8, D20, D22)

- [ ] **T5.1** Wire everything together — ensure `CounterProjection.approved.cs` diff is minimal (subtitle + loading + empty inserts ONLY; no column changes).
- [ ] **T5.2** Add synthetic test projections under `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/RoleSpecificProjections/` — 5 happy-path synthetics (ActionQueue, StatusOverview, DetailRecord, Timeline, Dashboard) PLUS 3 **negative-path synthetics per Murat review**: `ActionQueueNoEnumProjection.cs` (ActionQueue annotation but no enum property — asserts HFC1022 + `.Where(_ => true)` fallback emission), `WhenStateTypoProjection.cs` (WhenState contains unknown enum member — asserts HFC1022 + member passes through), `DashboardWrongShapeProjection.cs` (Dashboard on a projection that exposes domain-inappropriate properties — asserts HFC1023 + Default-body fallback). 8 synthetics total.
- [ ] **T5.3** New approval files per synthetic (8 total): `{Name}.approved.cs`. Verify the approval-test harness picks them up. Approvals for negative-path synthetics MUST capture the expected diagnostic comments alongside the emitted file.
- [ ] **T5.4** Scope-guardrail test (D22): `RazorEmitterScopeGuardrailTests.ProjectionViewBodyStaysMinimal` — scans every approved `.cs` and asserts forbidden tokens are absent. Fail message names the future story owning each token. **Companion size-budget assertion (C5 per review round 2):** `RazorEmitterScopeGuardrailTests.ApprovedFileStaysUnderSizeBudget` — reads each approved `.cs` under `approved/` + `RoleSpecificProjections/approved/`, asserts `fileInfo.Length <= baseline + tolerance` where baseline is captured at the story's merge commit and tolerance is `baseline * 1.15` (15% headroom for legitimate future patches). Belt-and-suspenders: catches scope creep that introduces a NEW token the denylist doesn't know about (Story 4.3 could emit `FcFilterChip` — not in the denylist, but the size budget flags it). Baseline values tracked in a `Baselines.cs` constant file under the test assembly; Story 4.x updates require explicit baseline bump with PR callout. 1 test.
- [ ] **T5.5** Update `CounterProjection.approved.cs` to include the new subtitle + loading shell + empty shell emissions (re-baseline). **Diff-review discipline (per Murat review):** any PR touching `CounterProjection.approved.cs` MUST include an explicit callout in the PR description listing (a) which lines changed, (b) which change is attributable to subtitle/loading/empty inserts vs. unintended emitter drift, (c) reviewer sign-off on each category. The canonical Story 1-5 approval is the regression anchor for the entire generator — silent re-baselining turns approval tests into rubber stamps. If the default-body bytes drift beyond the expected three inserts, STOP and investigate before re-baselining. **Mechanical diff-token gate (F1 per review round 2):** add `CounterProjectionApprovalTests.DiffStaysWithinExpectedInsertionSet` — parses line-level diff between regenerated output and `.approved.cs`; asserts every ADDED line (after normalizing whitespace) contains at least one of the approved insertion tokens: `FcProjectionSubtitle`, `FcProjectionLoadingSkeleton`, `FcProjectionEmptyPlaceholder`, `SubtitleFallbackCount`, `EmitSubtitleInvocation`. Unexpected line additions fail the test with `"Unexpected emitter drift on line {N}: '{content}' — see Story 4-1 T5.5 discipline. Author must (a) justify the drift in the PR description, (b) bump the approved insertion-token set in this test if the drift is intentional."` Catches author-authored callouts that LIE about the diff scope. 1 test.
- [ ] **T5.6** **Emitted-view reflection-discipline test (renamed at review round 4 per Murat — honesty rescope from prior "AOT smoke")**: `EmittedViewReflectionDisciplineTests.EmittedViewsHaveNoAotHostilePatterns` — runs the generator over the 5 happy-path synthetics, parses each emitted `.razor.cs` with `SyntaxFactory.ParseCompilationUnit`, then walks the syntax tree and asserts the emitted output contains **none** of the following AOT/trim-hostile patterns: `MakeGenericMethod` / `MakeGenericType` (runtime generic specialization), `Activator.CreateInstance(Type)` (runtime type instantiation without `[DynamicallyAccessedMembers]`), `Assembly.GetTypes()` / `Type.GetMembers()` without `[DynamicallyAccessedMembers]` annotations, `Type.GetMethod(string)` reflection against generic state. **Honest naming:** this is NOT a real AOT-smoke gate — MSBuild `PublishAot=true` + `TrimMode=full` properties on a `CSharpCompilation` are just metadata flags; the IL Linker and AOT compiler are entirely separate toolchains (`Microsoft.NET.ILLink.Tasks`, the AOT analyzer) that a standalone Roslyn compilation does not invoke. Running `dotnet publish /p:PublishAot=true` against an adopter app is the only real AOT gate, and that lives in Epic 11's adopter-shipping surface. **What this test DOES verify:** the generator's output discipline — no emitted view uses reflection patterns known to break trim/AOT. A real `dotnet publish -r <rid> /p:PublishAot=true` is still required for end-to-end certification in Epic 11; 4-1 is not that gate. **Optional hardening (deferred):** if the test catches a false-negative in the future, wire `Microsoft.NET.ILLink.Tasks` analyzer assemblies into the compilation's `AnalyzerReferences` and switch to trim-warning-based assertions. Not worth the Roslyn-version-coupling cost for v1 — the syntax-walk approach catches the regression class D8 actually worried about (generic-state reflection) at a fraction of the complexity. 1 test.
- [ ] **T5.7** **Per-row filter microbenchmark assertion (per Murat review, NFR4 anchor)**: `RazorEmitterPerformanceTests.ActionQueueWhereFilterStaysUnder5MsFor500Rows` — benchmarks the emitted `.Where(x => x.{property}.ToString() is ...)` filter on 500 synthetic rows × 4 `WhenState` members × 10 iterations (warmup 3, measure 7); asserts median < 5ms using `Stopwatch.GetTimestamp` (no BenchmarkDotNet — story-internal gate, not full benchmark harness). Locks the "projections < 500 rows is free" assumption as a test, not a hope. **Navigation-jank sub-test (F2 per review round 2; threshold loosened per War Room round 3):** add `...SubtitleRerendersStayBelowThresholdAcrossNavChurn` — with a bUnit `TestContext`, render 3 projection views on a single stubbed circuit; toggle the circuit's active view 100 times via stubbed `NavigationManager`; assert the cumulative `StateHasChanged` call count across all rendered subtitles stays below **500** (loosened from 300 per War Room — the original 300 threshold was too tight for CI variability; user can't distinguish 310 re-renders from 300; keep the test as a *smoke gate* against subscription storms, not a precision benchmark). Include `Debug.WriteLine($"actual={actual}")` before the assertion so regressions log the actual count for forensics. Guards against circuit-reuse subscription storms on nav-heavy adopters. 2 tests total (renamed from 1).

### T6 — FcProjectionSubtitle Shell component (AC6, AC11, D8, D9, D21)

- [ ] **T6.1** Create `FcProjectionSubtitle.razor` + `.razor.cs`. Parameters: `ProjectionType`, `Role` (ProjectionRole), `WhenState` (string?).
- [ ] **T6.2** Nullable DI for `IBadgeCountService`; cascading `SubtitleFallbackCount` fallback when service absent.
- [ ] **T6.3** Per-type filter on `CountChanged` (D21) — `StateHasChanged` only when `args.ProjectionType == _projectionType`.
- [ ] **T6.4** Disposal via `IAsyncDisposable` (D9). **Teardown-race hardening (FMA round 3):** maintain a `private bool _disposed` flag set in `DisposeAsync` before subscription disposal; `OnCountChanged` checks `if (_disposed) return;` before calling `StateHasChanged` — prevents `"Cannot invoke on disposed"` warnings when `CountChanged` fires during the gap between disposal-start and subscription-dispose. Mirrors Story 3-5 D20 pattern. Also wrap subscription-dispose in try/catch so a throwing observer doesn't break circuit teardown (matches AC11 hardening).
- [ ] **T6.5** Resource-key-driven copy per role (AC6 copy map in D18).
- [ ] **T6.6** Tests (Shell): `FcProjectionSubtitleTests.RendersActionQueueCopy_WithBadgeServiceCount`, `...FallsBackToCascadingCount_WhenServiceAbsent`, `...DisposesSubscriptionCleanly`, `...FiltersCountChangedByProjectionType`, `...RendersEachRoleCopyVariant` (Theory), `...FrenchLocaleRendersFrenchCopy`, **`...SkipsStateHasChangedWhenCountChangedForOtherProjectionType` (C6 per review round 2)** — renders two subtitles bound to different `ProjectionType` values; fires `CountChanged` for projection-type-A; asserts subtitle-B's `StateHasChanged` call count did NOT increment (filter-before-StateHasChanged discipline). Distinct from `FiltersCountChangedByProjectionType` which only checks the rendered count VALUE — this test checks the re-render suppression mechanism. **`...RendersEmptyFragmentDuringLoading` (SCAMPER-M round 3)** — asserts that during `state.IsLoading == true`, the component produces no visible text (no "0 entities" flash-of-false-info). **`...DegradesGracefullyWhenSubscribeThrows` (FMA round 3)** — substitutes a stub `IBadgeCountService` whose `CountChanged.Subscribe` throws; asserts component still renders without crashing, logs one warning via `ILogger`, falls back to cascading count. **`...IgnoresCountChangedAfterDisposal` (FMA round 3)** — subscribes, disposes, fires `CountChanged` on the stub; asserts no `"Cannot invoke on disposed"` warning appears. ≥ 12 tests (+3 from round 2).
- [ ] **T6.7** **Subscription-leak bUnit test (per Murat review, NFR-anchored)**: `FcProjectionSubtitleLeakTests.Rendering100SubtitlesAndDisposingReturnsSubscriberCountToBaseline` — renders 100 `FcProjectionSubtitle` instances bound to a stub `IBadgeCountService` that exposes `SubscriberCount`; disposes all instances; asserts `SubscriberCount == baseline` within a single event loop turn (no reliance on GC). Guards D9 disposal discipline — catches circuit-teardown leaks that would otherwise manifest as production GC pressure. 1 test.
- [ ] **T6.8** **French sample rendering gate — moved from T8.4 per SCAMPER round 3.** After T6.5 resource-key-driven copy lands, but BEFORE T6.6's copy-string-sensitive tests are written, render the 3 pre-pass keys (`HomeActionQueueSubtitleTemplate`, `HomeDefaultSubtitleTemplate`, `HomeEmptyPlaceholderText`) against realistic French nouns (`commandes`, `factures`, `événements`) and emit a sample pack to `tests/Hexalith.FrontComposer.Shell.Tests/Resources/FrenchPrePassSamples.md`. Circulate to the named native-French-speaker reviewer (T8.4). If reviewer flags a rejected rendering at THIS stage, update D18's proposed FR string BEFORE T6.6 locks the copy — avoids the wasteful cycle of "write tests pinning French copy, then rewrite copy, then rewrite tests." Decouples copy-discovery from test-cost. Rationale per SCAMPER-R: inspection should precede lock-in, not trail it.

### T7 — Diagnostics + generator release notes + Contracts test harness (AC7, AC9, AC10, D15, D16, D17)

- [ ] **T7.1** Confirm HFC1022 + HFC1023 entries in `FcDiagnosticIds.cs`, `DiagnosticDescriptors.cs`, `AnalyzerReleases.Unshipped.md`. (Entries landed in T1.5; this task validates the analyzer-release gate: running `dotnet build` produces no `RS2008` analyzer-release warnings.)
- [ ] **T7.2** Tests (SourceTools): `DiagnosticDescriptorTests.AllHFC1xxxIdsHaveAnalyzerReleaseEntries`, `...HFC1022MessageFormatMatchesSpec`, `...HFC1023MessageFormatMatchesSpec`. 4 tests.
- [ ] **T7.3** ~~Tests (Contracts)~~ **MOVED to T3.11 (per Bob/Murat review)** — contracts-surface tests now gate emit-stage exit instead of running after all emit work is done. T7 focuses on diagnostics + release notes only.

### T8 — Resource keys + French pre-pass (AC12, D18)

- [ ] **T8.1** Add 8 EN keys to `FcShellResources.resx` with inline `<comment>` tags linking Story 4-1 + ACs.
- [ ] **T8.2** Add 8 FR translations to `FcShellResources.fr.resx` (matching keys).
- [ ] **T8.3** Verify `CanonicalKeysHaveFrenchCounterparts` passes (58 → 66 parity).
- [ ] **T8.4** **Pre-merge French pre-pass SIGN-OFF gate (sampling step moved to T6.8 per SCAMPER round 3).** The rendered-sample pack (`FrenchPrePassSamples.md`) was produced during T6.8 and circulated to the named native-French-speaker reviewer. T8.4's job is to collect the reviewer's verdict and execute the branching decision. MARK as USER-GATED in completion notes, code-review pickup (Story 3-5 D19 precedent). **Named reviewer assignment (C4 per review round 2):** the pre-pass is NOT "whichever reviewer claims the PR"; the Dev Agent MUST identify a native-French-speaking teammate by handle (GitHub @mention) in the PR description, and explicitly block merge until that teammate's sign-off lands as a PR review comment. If no native-French-speaking teammate is available for this story's merge window, the Dev Agent takes the DEFER path (below) — shipping a guessed FR translation without native review is explicitly forbidden. **Explicit ship-vs-block branching (F5 per review round 2):** one of three paths: (SHIP) all three render grammatically correct — merge as-is, log reviewer sign-off in Completion Notes; (REVISE) reviewer suggests a correction that still fits the `string.Format` pattern — apply, re-render samples, re-request sign-off, then SHIP; (DEFER) reviewer flags a fundamental grammatical issue (singular/plural mismatch, gender agreement, ICU pluralization required) — (a) open a Story 9-5 issue referencing the rejected rendering as evidence, (b) revert the offending FR key to `identity-pass` (French translation = English source, so the key exists and `CanonicalKeysHaveFrenchCounterparts` passes, but French users see the English copy until 9-5 lands ICU). **Do NOT ship grammatically broken French in main.** The DEFER path must be logged in the story's Known Gaps with a pointer to the filed 9-5 issue.
- [ ] **T8.5** Test: `FrenchLocaleSubtitleTests.RendersFrenchCopyForEachRole` (Theory, 5 roles). 5 tests.

---

## Dev Notes

### Relevant architecture patterns

- **3-stage source generator pipeline** (Parse → Transform → Emit) — per §390-400 of `architecture.md`. 4-1 extends each stage additively; no Roslyn symbol references past Parse.
- **Incremental cache participation** — per ADR-006. Every new IR field (D19) participates in `Equals` + `GetHashCode`.
- **Per-projection emission invariant** — one generated file per projection (`{Namespace}.{TypeName}.g.razor.cs`). Role-specific bodies are inlined, not separate files (D20).
- **Role-hint-aware convention rendering** — per §495 of `architecture.md` ("A single `ProjectionRenderer<T>` handles all projections using IR metadata (field types, role hints, badge mappings)."). 4-1 is the first story to operationalize `role hints` from that sentence.
- **Nullable DI** — `IServiceProvider.GetService<T>` pattern (Story 3-4 D16, Story 3-5 D6). 4-1 reuses for `FcProjectionSubtitle` ↔ `IBadgeCountService`.
- **Cascading `ProjectionContext`** — Story 2-2 D27. 4-1 is the first story to cascade at per-row scope (D14).
- **L03 fail-closed storage scope** — not directly applicable (4-1 does NOT persist state to storage); inherited defensive posture: any NEW storage write would pass through 3-1/3-5/3-6's existing `StorageKeys.BuildKey` + `TryResolveScope` helpers.
- **Subtitle count subscription scope** — per-view, not per-row (D21). Scales to large grids without subscription fan-out.

### Source tree components touched (paths, modules, naming)

```
src/Hexalith.FrontComposer.Contracts/
  Attributes/
    ProjectionRoleAttribute.cs            [EXTEND]  + string? WhenState { get; init; }                        (T1.1)
  Diagnostics/
    FcDiagnosticIds.cs                    [EXTEND]  + HFC1022, HFC1023, HFC1024                               (T1.5)

src/Hexalith.FrontComposer.SourceTools/
  AnalyzerReleases.Unshipped.md           [EXTEND]                                                             (T1.5, T7.1)
  Diagnostics/
    DiagnosticDescriptors.cs              [EXTEND]                                                             (T1.5)
  FrontComposerGenerator.cs               [EXTEND]  GetDescriptor switch (+ HFC1024)                           (T1.5)
  Parsing/
    AttributeParser.cs                    [EXTEND]  WhenState extraction + validation                          (T1.3, T1.4)
    DomainModel.cs                        [EXTEND]  ProjectionRoleWhenState field + Equals/GetHashCode         (T1.2)
  Transforms/
    ProjectionRenderStrategy.cs           [NEW]                                                                (T2.1)
    RazorModel.cs                         [EXTEND]  Strategy + WhenStates fields                               (T2.2)
    RazorModelTransform.cs                [EXTEND]  strategy mapping + CSV split + HFC1023/HFC1024 dispatch    (T2.3-T2.5)
  Emitters/
    ProjectionRoleBodyEmitter.cs          [NEW]     static Emit{Strategy}Body methods                          (T3.2-T3.8)
    RazorEmitter.cs                       [REFACTOR]  switch-expression dispatch + shared private helpers      (T3.1, T3.9)

src/Hexalith.FrontComposer.Shell/
  Components/Rendering/
    FcProjectionEmptyPlaceholder.razor    [NEW]     + Role parameter (H4)                                      (T4.2)
    FcProjectionEmptyPlaceholder.razor.cs [NEW]                                                                (T4.2)
    FcProjectionLoadingSkeleton.razor     [NEW]     + Layout enum parameter (G5 / D6)                          (T4.1)
    FcProjectionLoadingSkeleton.razor.cs  [NEW]                                                                (T4.1)
    FcProjectionLoadingSkeleton.razor.css [NEW]                                                                (T4.1)
    SkeletonLayout.cs                     [NEW]     enum { DataGrid, Card, Timeline } (G5)                     (T4.1)
    FcProjectionRoutes.cs                 [NEW]     static StatusFilter helper (H2)                            (T4.3)
    FcProjectionSubtitle.razor            [NEW]                                                                (T6.1)
    FcProjectionSubtitle.razor.cs         [NEW]                                                                (T6.1-T6.5)
  Resources/
    FcShellResources.resx                 [EXTEND]  + 8 keys                                                   (T8.1)
    FcShellResources.fr.resx              [EXTEND]  + 8 FR keys                                                (T8.2)

tests/
  Hexalith.FrontComposer.Contracts.Tests/
    Attributes/ProjectionRoleAttributeTests.cs        [EXTEND or NEW]                         (T3.11, round 2 move from T7)
    Attributes/ProjectionRoleTests.cs                 [NEW]                                   (T3.11)
    ProjectionContext/ProjectionContextTypeTests.cs   [NEW]                                   (T3.12, C2 struct/readonly assertion)
  Hexalith.FrontComposer.Shell.Tests/
    Components/Rendering/
      FcProjectionEmptyPlaceholderTests.cs            [NEW]                                   (T4.5)
      FcProjectionLoadingSkeletonTests.cs             [NEW]                                   (T4.5)
      FcProjectionRoutesTests.cs                      [NEW]                                   (T4.5, H2)
      FcProjectionSubtitleTests.cs                    [NEW]                                   (T6.6)
      FcProjectionSubtitleLeakTests.cs                [NEW]                                   (T6.7)
      FrenchLocaleSubtitleTests.cs                    [NEW]                                   (T8.5)
    Resources/
      FrenchPrePassSamples.md                         [NEW]                                   (T8.4, D18 rendered-sample pack)
  Hexalith.FrontComposer.SourceTools.Tests/
    Diagnostics/DiagnosticDescriptorTests.cs          [EXTEND]                                (T7.2)
    Emitters/
      ProjectionRoleBodyEmitterTests.cs               [NEW]                                   (T3.10)
      RazorEmitterScopeGuardrailTests.cs              [NEW]                                   (T5.4 + size-budget C5)
      RazorEmitterStrategyDispatchTests.cs            [NEW]                                   (T3.10)
      RazorEmitterPerformanceTests.cs                 [NEW]                                   (T5.7 + nav-jank F2)
      CounterProjectionApprovalTests.cs               [EXTEND]                                (T5.5 + diff-token gate F1)
      EmittedViewReflectionDisciplineTests.cs         [NEW]                                   (T5.6, renamed round 4 from GeneratorAotSmokeTests)
      RoleSpecificProjections/                        [NEW DIR]                               (T5.2)
        ActionQueueProjection.cs                      [NEW]  synthetic fixture
        StatusOverviewProjection.cs                   [NEW]
        DetailRecordProjection.cs                     [NEW]
        TimelineProjection.cs                         [NEW]
        DashboardProjection.cs                        [NEW]
        ActionQueueNoEnumProjection.cs                [NEW]  negative-path (T5.2)
        WhenStateTypoProjection.cs                    [NEW]  negative-path (T5.2)
        DashboardWrongShapeProjection.cs              [NEW]  negative-path (T5.2)
        approved/
          ActionQueueProjection.approved.cs           [NEW]                                   (T5.3)
          ... (8 approvals total; 5 happy-path + 3 negative-path)
    Incremental/
      DomainModelCacheEqualityTests.cs                [EXTEND]                                (T1.7)
      RazorModelCacheEqualityTests.cs                 [EXTEND]                                (T1.7)
      IncrementalGeneratorCacheHitTests.cs            [NEW]                                   (T1.8)
    Parsing/
      ProjectionRoleAttributeParserTests.cs           [NEW]                                   (T1.6)
    Transforms/
      RazorModelRoleStrategyTests.cs                  [NEW]                                   (T2.6)
```

### Detected conflicts or variances

1. **Epic 4 AC drift vs. PRD FR4.** Resolved by ADR-051 / D1. PRD wins; Epic text is editorial drift. Story 9-5 docs pass synchronizes Epic to PRD language.
2. **`ProjectionRole.Dashboard` deferred in v1.** Resolved by D16 / AC10. Role is reserved (not broken); full rendering lands in Story 6-3. HFC1023 Information surfaces the deferral.
3. **`FcEmptyState` (full CTA variant) is Story 4.6 scope, not 4-1.** Resolved by D7 / ADR-053 / AC8. 4-1 ships minimal `FcProjectionEmptyPlaceholder`; 4.6 swaps the component body without touching generator.
4. **Badge palette colour rendering is Story 4.2 scope.** Status enum cells stay text-humanized until 4.2. Scope-guardrail test (D22) blocks accidental early emission.
5. **Subtitle count: `IBadgeCountService` wins; cascading-value fallback is the v1.x adopter-overridden path.** Documented in D8 + AC11.
6. **Incremental-cache participation is non-optional.** Without the Equals/GetHashCode updates on `DomainModel` + `RazorModel`, `WhenState` edits would cache-hit silently. D19 + T1.7 test gate.

### Testing standards summary

- **Approval tests** for every emitted artifact (existing Story 1-5 harness). New approvals per synthetic role projection — 5 happy-path + 3 negative-path per Murat review (T5.2 / T5.3).
- **Incremental-cache parity tests** for every new IR field (T1.7) PLUS **incremental-generator cache-hit tests** (T1.8, per Winston review) — same-input-twice → Cached, WhenState-edit → Rerun.
- **Diagnostic-descriptor tests** for every new HFC ID (T7.2) — includes the new `HFC1024_UnknownProjectionRoleValue` split out from HFC1022 at review round 2 (H6).
- **Contract tests** (Contracts.Tests) validating attribute + enum shape per PRD FR4 — **gated at T3.11 exit** (not T7, per Bob/Murat review), so a contract break surfaces before emit-stage work compounds. **Plus `ProjectionContextTypeTests.IsReadonlyRecordStruct`** (T3.12, C2 per review round 2) — asserts the Story 2-2 D27 contract didn't drift to a class; halts 4-1 dev if it did, since D14 per-row cascade is load-bearing for Story 4.4 virtualization.
- **Scope-guardrail tests** (D22 / T5.4) — (a) denylist scan for forbidden tokens with story-pointer fail messages, (b) **size-budget assertion (C5 per review round 2)** — approved files stay within 15% of baseline; catches scope creep that introduces new tokens outside the denylist.
- **Mechanical diff-token gate** (T5.5, F1 per review round 2) — `CounterProjectionApprovalTests.DiffStaysWithinExpectedInsertionSet` asserts every added line contains an approved insertion token; fails author-authored callouts that lie about diff scope.
- **Emitted-view reflection-discipline test** (T5.6, renamed at review round 4 per Murat — honesty rescope from "AOT smoke") — syntax-walks each emitted view and asserts no AOT/trim-hostile reflection patterns (`MakeGenericMethod`, `Activator.CreateInstance(Type)` without `[DynamicallyAccessedMembers]`, reflection over generic state). NOT a real AOT gate (MSBuild flags on a `CSharpCompilation` don't invoke IL Linker); end-to-end AOT certification lives in Epic 11's `dotnet publish` surface. Guards D8's concrete regression class (generic-state reflection) at Roslyn-level; deferred hardening via `Microsoft.NET.ILLink.Tasks` analyzers if false-negatives appear.
- **Performance assertions** (T5.7, per Murat review, NFR4 anchor) — (a) per-row WhenState filter < 5ms median on 500 rows × 4 states, (b) **navigation-jank sub-test (F2 per review round 2)** — 100 nav toggles across 3 subtitles keeps cumulative StateHasChanged < 300. Guards circuit-reuse subscription storms.
- **Subscription-leak test** (T6.7, per Murat review, D9 anchor) — 100 Subtitle renders + dispose → subscriber count returns to baseline without GC reliance.
- **Filter-before-StateHasChanged test** (T6.6, C6 per review round 2) — fires `CountChanged` for projection-A, asserts subtitle-B's StateHasChanged count did NOT increment. Distinct from count-value filtering; tests the re-render suppression mechanism.
- **Row-context isolation test** (T3.10, F4 per review round 2) — navigates between two ActionQueue views, asserts `ProjectionContext.AggregateId` doesn't leak across the circuit boundary.
- **CounterProjection approval diff-review discipline** (T5.5, per Murat review) — PR callout required on any re-baseline; three expected inserts (subtitle / loading / empty) catalogued before merge. Now mechanically enforced by the F1 diff-token gate above.
- **Resource-key parity test** (existing `CanonicalKeysHaveFrenchCounterparts`) covers French locale — no new test needed. Story-scoped subset helper deferred to Story 9-5 (Known Gaps G7).
- **French pre-pass ship/revise/defer gate** (T8.4, F5/C4 per review round 2) — named native-French-speaker reviewer required; rendered-sample pack at `FrenchPrePassSamples.md`; defer path = identity-pass with 9-5 issue logged. No grammatically broken FR ships to main.
- **bUnit component tests** for `FcProjectionSubtitle` / `FcProjectionLoadingSkeleton` / `FcProjectionEmptyPlaceholder` / `FcProjectionRoutes` — Shell.Tests assembly (T4.5, T6.6).
- **Theory-driven locale tests** for French copy variant (T8.5).
- **Banned:** real-time assertions, `Task.Delay(N)` with N > 50ms, reflection-over-generic-state. Use `FakeTimeProvider` only if a test measures timeout behavior (none in 4-1 — subtitle subscription is event-driven, not timer-driven).

### Adopter opt-out

- Adopter can register a custom `IProjectionSubtitleProvider` (FUTURE contract; not introduced in 4-1 — the Story 6 customization gradient will supply it). For 4-1, the escape path is the Story 6 `[SlotOverride]` mechanism (not yet available) or full `FcProjectionSubtitle` replacement via `TryAddScoped` on the component's assembly-scoped registration (Blazor-level override).
- Adopter can disable the subtitle globally by registering a no-op `FcProjectionSubtitle` variant (last-writer-wins on the Blazor assembly scan order — Story 3-5 D16 precedent).
- Adopter with their own badge source (non-`IBadgeCountService` implementation) can leave `IBadgeCountService` unregistered; subtitle falls back to the cascading count. No adopter-code change required.

### Known gaps

- **G1** — Dashboard role rendering deferred to Story 6-3. Surfaced at build as HFC1023 Information. v1 falls back to Default DataGrid.
- **G2** — ICU pluralization (`"1 order" / "2 orders"` French-locale-correct) deferred to Story 9-5 L10N pass. v1 uses simple "s" suffix for English, identity for French (confirmed with Sally).
- **G3** — Story 4.6's full `FcEmptyState` CTA wiring is out of scope for 4-1. 4-1 ships a minimal placeholder; 4.6 swaps the component body.
- **G4** — Filter persistence for StatusOverview click-through (navigating to `?filter=status:X`) depends on Story 4.3 implementing the query-string filter protocol. v1 4-1 ships the navigation event; 4.3 wires the destination. **AC2 verification uses `[DeferredVerify(Story = "4.3")]` marker** (per Bob review) — 4-1's gate is "navigation fires with correct URL"; 4.3's gate is "destination resolves into filtered grid".
- **G5** — Adopter-supplied `IProjectionSubtitleProvider` contract deferred to Story 6 customization gradient work. v1.0 adopters who want custom subtitle copy override the component itself.
- **G6** — **Adopter-hintable `PrimaryFieldCount` on `[ProjectionRole]`** (per Sally review) — v1 caps DetailRecord primary fields at 6 (D12); accepted for 4-1 but filed as a follow-up: adopters should be able to write `[ProjectionRole(ProjectionRole.DetailRecord, PrimaryFieldCount = 8)]` for field-dense domains. Deferred to Story 6-3 (Annotation Override) since it belongs with the broader per-projection customization surface.
- **G7** — **`CanonicalKeysHaveFrenchCounterparts` rolls up three signals into one failure mode** (Graph-of-Thoughts finding, review round 2): the existing global parity test covers Story 4-1 D18 resource keys, NFR60 localization compliance, AND regression protection for every pre-existing key from Story 3-5 D19 etc. A single missing key produces a vague failure. Non-blocking for 4-1 (the test does its job), but filed as a follow-up: add a story-scoped subset helper `CanonicalKeysHaveFrenchCounterparts_Story4_1Keys` (accepts a keylist constant) so future failures isolate to the story that introduced the drift. Deferred to Story 9-5 (L10N pass) since story-scoped subsets are a localization-infrastructure concern.
- **G8** — **`HFC1024_UnknownProjectionRoleValue` reserves a new diagnostic range** (H6 per review round 2) — 4-1 splits `HFC1022` into two codes to avoid trash-can accretion. Expected trajectory: Epic 6 will reserve `HFC1025_UnknownSlotOverrideTarget` for `[SlotOverride]` unknown targets, Story 9-4 will reserve `HFC1026_AnalyzerHintUnknown`. The `HFC10xx` block's growth rate is ~1 code per major story from here; no immediate cap concern, but Story 9-4's analyzer-release gate may want to validate non-overlapping ranges across modules if FrontComposer splits into multiple source generators.
- **G9** — **Dev-time preview sample for `[ProjectionRole]` annotations** (War Room round 3) — adopters today validate their role-annotation choice by running the app; a `dotnet new fc-sample` template with every role pre-annotated on a fake projection would close the 30-second feedback loop to a 3-second "look at the sample" comparison. Not 4-1's scope (sample scaffolding is docs work); filed for Story 9-5 documentation pass.
- **G10** — **Enum member names containing comma are out of scope** (Failure Mode Analysis round 3) — a projection declaring `enum State { [EnumMember(Value = "A,B")] A, ... }` with `WhenState = "A,B,C"` cannot be correctly parsed by the CSV-split rule in D3. This is a technically-legal but vanishingly-rare pattern; adopters using `[EnumMember]` to alias enum values for serialization purposes SHOULD NOT rely on aliases matching WhenState tokens. v1 uses raw C# enum identifier names, ignoring `[EnumMember(Value)]` overrides. Documented; no story owner assigned.

### NFR references

- **NFR4** (Performance — Web Latency): generated DataGrid render with 500 virtualized rows P95 < 300ms. 4-1 does NOT introduce virtualization (Story 4.4 scope); 4-1's Default body stays identical to Story 1-5's output for row count. Timeline / DetailRecord / StatusOverview bodies are smaller per item (no PropertyColumn fan-out). ActionQueue filter adds one `.Where()` — O(N) for v1's < 500-row projections is free.
- **NFR8** (Performance — Generator): full-solution rebuild for 50-aggregate reference domain < 4s. 4-1 adds ~1 branch per projection + one subtitle emission — negligible generator-side cost. Incremental-cache participation (D19) keeps the fast path fast.
- **NFR9** (Performance — Test suite): CI budget. Story 4-1 target ≈ 55 new tests; full suite grows from ~1,004 to ~1,060. Budget trajectory stays under Story 10's CI governance gate (Epic 10 Story 10-5 introduces the flaky-test quarantine).
- **NFR17** (Privacy): no new storage writes. Subtitle count is derived from `IBadgeCountService.Counts` (already-aggregated, non-PII). Filter is CSV of enum member names (non-PII by contract — enum members are source code identifiers).
- **NFR60** (Localization): 8 new keys + 8 FR translations → `CanonicalKeysHaveFrenchCounterparts` green. Three highest-impact keys pre-reviewed per D18.
- **NFR87** (Session-persistence subset): 4-1 does NOT persist any state. The subtitle + view renderer is purely render-time. Persistence deferred to Story 3-6 + Story 4.3.

### References

- [Source: _bmad-output/planning-artifacts/epics/epic-4-rich-datagrid-projection-interaction.md#Story 4.1: Projection Role Hints & View Rendering]
- [Source: _bmad-output/planning-artifacts/prd/functional-requirements.md#FR4]
- [Source: _bmad-output/planning-artifacts/epics/requirements-inventory.md#UX-DR44]
- [Source: _bmad-output/planning-artifacts/epics/requirements-inventory.md#UX-DR45]
- [Source: _bmad-output/planning-artifacts/epics/requirements-inventory.md#UX-DR55]
- [Source: _bmad-output/planning-artifacts/architecture.md#Rendering abstraction] — §397-400 `IRenderer<TModel, TOutput>` + convention-based `ProjectionRenderer<T>`
- [Source: _bmad-output/planning-artifacts/architecture.md#Projection renderers] — §495-497 circuit-breaker error boundary
- [Source: _bmad-output/planning-artifacts/architecture.md#Source generator pipeline] — §390-400 3-stage pipeline
- [Source: _bmad-output/planning-artifacts/architecture.md#File structure] — §860-900 generated file naming
- [Source: _bmad-output/planning-artifacts/ux-design-specification/design-direction-decision.md#Projection role hints — v1 set and growth path] — the authoritative role → view pattern mapping (lines 113-123)
- [Source: _bmad-output/planning-artifacts/ux-design-specification/component-strategy.md#FcEmptyState] — the full Story 4.6 shape that `FcProjectionEmptyPlaceholder` will morph into
- [Source: _bmad-output/implementation-artifacts/3-5-home-directory-badge-counts-and-new-capability-discovery.md#Critical decisions] — D1-D20, esp. D1 (Scoped service) + D6 (nullable DI) + D20 (disposal)
- [Source: _bmad-output/implementation-artifacts/2-2-action-density-rules-and-rendering-modes/...] — Decision D27 cascading `ProjectionContext`
- [Source: _bmad-output/implementation-artifacts/1-5-source-generator-transform-and-emit-stages.md] — existing `RazorEmitter` + approval-test harness
- [Source: _bmad-output/process-notes/story-creation-lessons.md] — L01, L02, L04, L06, L07, L09, L10, L11

---

## Dev Agent Record

### Agent Model Used

_To be populated by dev agent on implementation._

### Debug Log References

_To be populated by dev agent on implementation._

### Completion Notes List

_To be populated by dev agent on implementation._

### File List

_To be populated by dev agent on implementation._

### Change Log

| Date (UTC) | Change | By |
| ---------- | ------ | -- |
| 2026-04-22 | Story 4-1 created via `/bmad-create-story` — 22 binding decisions (3-decision L06 buffer remains), 3 ADRs (ADR-051 PRD-vs-Epic arbitration / ADR-052 single-switch strategy / ADR-053 Story-4.6-replaceable placeholder), 8 ACs expanded into 12-row table, 55-test target, L04 / L11 cheat sheet, cross-story contract table covers 12 seams, scope guardrail test (D22) enforces role-body minimalism. USER-GATED: French pre-pass on 3 subtitle keys (D18 / T8.4). | create-story agent (claude-opus-4-7) |
| 2026-04-22 | Review round via `/bmad-party-mode` (Winston / Bob / Murat / Sally). Applied: (1) AC1 / AC3 split into sub-ACs with verification markers per Bob; (2) AC2 `[DeferredVerify(Story="4.3")]` marker + G4 update per Bob; (3) AC5 Story-1-5 regression DoD gate per Bob; (4) D5 / ADR-052 switch-expression with throwing default per Winston; (5) D6 role-aware skeleton layouts (DataGrid / Card / Timeline) per Sally; (6) D7 generous 32px icon spec per Sally; (7) D10 / D11 / D13 deterministic tiebreakers per Bob; (8) D14 `IsFixed="true"` + ProjectionContext reference-stability per Winston; (9) D18 French pre-pass rendered-samples discipline per Sally; (10) DetailRecord subtitle copy `"overview"` / `"aperçu"` per Sally; (11) T1.8 incremental-generator cache-hit test per Winston; (12) T3.11 Contracts-surface gate moved from T7 per Bob/Murat; (13) T5.2 / T5.3 three negative-path approvals per Murat; (14) T5.5 diff-review discipline per Murat; (15) T5.6 AOT smoke + T5.7 500-row perf assertion + T6.7 100-Subtitle leak test per Murat; (16) G6 `PrimaryFieldCount` follow-up per Sally. Test count ≈ 55 → ≈ 62 (+7). Decision budget unchanged at 22/25. | party-mode orchestrator (claude-opus-4-7) |
| 2026-04-22 | Review round 4 via `/bmad-party-mode` (Winston/Bob/Murat/Sally). Applied 8 changes: **Winston (pipeline integrity):** (1) D16 + T2.5 corrected — HFC1023 dedup ownership rewritten; Roslyn `IIncrementalGenerator` per-input invocation already provides "once per type per compilation" so no `HashSet` is needed (prior "instance-scoped HashSet" phrasing was technically wrong for a static Transform); (2) T4.3 `FcProjectionRoutes.StatusFilter` signature hardened from `(string, object)` to `(string, Enum)` with `Uri.EscapeDataString`; T4.5 adds `StatusFilterUriEscapesStatusValue` + `StatusFilterRequiresEnumParameter` tests; (3) T1.7 adds `TwoIndependentTransformsOfIdenticalSourceProduceEqualModels` + Parse-stage equivalent — catches `EquatableArray<T>` reference-equality regressions that the existing "different inputs produce different equality" tests can't detect; (4) T3.10 adds `RowContextIsConstructedAsLoopLocal_NotClassField` code-shape assertion — locks the emitter invariant behind AC1d at the source-scan level, not just behaviorally. **Murat (test honesty):** (5) T5.6 renamed `GeneratorAotSmokeTests` → `EmittedViewReflectionDisciplineTests` — the original name overclaimed; MSBuild `PublishAot=true` on a `CSharpCompilation` doesn't invoke IL Linker, so the test now honestly verifies "no AOT/trim-hostile reflection patterns in emitted output" instead of pretending to be a real AOT gate (deferred to Epic 11 `dotnet publish`). **Bob (accounting + ambiguity):** (6) D10 + D11 heuristic-duplication notes gained an explicit "DO NOT consolidate during 4-1; duplication is intentional per Story 4.3 deferred-refactor sequencing" Dev Agent directive; (7) AC10 gained an explicit D16 dedup-ownership pointer so Amelia reads the round-4-corrected scope before implementing; (8) Cheat Sheet test-count claim reconciled — honest inventory sum is ≈89 (crosses L11's 80-test soft trigger), accepted with rationale + Murat's T7.2 Theory-collapse escape hatch named but not exercised. **Sally:** ship-it, no changes requested (her prior-round contributions on subtitle copy / skeleton layout variants / 32px empty-placeholder icon / French pre-pass discipline all landed in rounds 1–2 and still hold). **French pre-pass non-decision:** Bob flagged the T8.4 manual-step gate against the user-memory automation preference; left unchanged pending user ruling (DEFER path already provides an automated fallback). Test count ≈ 84 (round 3) → ≈ 89 (round 4, +5). Decision budget unchanged at 22/25 — all round-4 changes refine existing Ds (no new D). Cheat sheet + source tree + testing-standards sections synced. | party-mode orchestrator (claude-opus-4-7) |
| 2026-04-22 | Review round 2 via `/bmad-advanced-elicitation` (pre-mortem + self-consistency + challenge-from-critical-perspective + graph-of-thoughts + hindsight reflection). Applied 21 changes: **Pre-mortem (F1-F6):** (F1) T5.5 mechanical diff-token gate; (F2) T5.7 nav-jank sub-test; (F3) D16 dedup-scope clarification across rebuilds; (F4) AC1d + T3.10 row-context-leak test; (F5) T8.4 explicit ship/revise/defer branching for French pre-pass; (F6) AC9 + HFC1022 message lists valid member names. **Self-consistency (G1/G3/G5/G8):** (G1) source-tree T-reference column; (G3) removed dead `ProjectionSubtitleFragment.cs` entry; (G5) T4.1 `Layout` parameter added; (G8) T3.10 StatusOverview nav-fires test. **Challenge (C1/C2/C4/C5/C6):** (C1) D10 WhenState filter memoization; (C2) T3.12 `ProjectionContextTypeTests.IsReadonlyRecordStruct` to catch Story 2-2 drift before 4-1 ships; (C4) T8.4 named native-French-speaker reviewer requirement; (C5) T5.4 size-budget assertion as belt-and-suspenders over the denylist; (C6) T6.6 filter-before-StateHasChanged test. **Graph of Thoughts:** ADR-053 explicit append-only parameter discipline; Known Gaps G7 `CanonicalKeysHaveFrenchCounterparts` roll-up note. **Hindsight (H1/H2/H4/H6):** (H1) ADR-053 "first pluggable slot" framing; (H2) `FcProjectionRoutes.StatusFilter` indirection (D11 + AC2 + T4.3); (H4) `FcProjectionEmptyPlaceholder.Role` parameter emitted now for 4.6 consumption; (H6) `HFC1024_UnknownProjectionRoleValue` split from HFC1022 (D15 + D17 + AC7 + Known Gaps G8). Test count ≈ 62 → ≈ 72 (+10). Decision budget unchanged at 22/25 (all changes refine existing decisions; no new D). Cheat-sheet + source-tree + testing-standards sections synced. | advanced-elicitation orchestrator (claude-opus-4-7) |
| 2026-04-22 | Review round 3 via `/bmad-advanced-elicitation` (War Room + Tree of Thoughts + SCAMPER + Occam's Razor + Failure Mode Analysis). Applied 14 changes: **War Room (R2/R3/R4):** (R2) Known Gaps G9 dev-time preview sample deferred to Story 9-5; (R3) T5.7 nav-jank threshold loosened from <300 to <500 with forensic log; (R4) HFC1022 member list capped at 10 alphabetically + "…and N more" (D17 + AC9). **Tree of Thoughts (A/D):** (A) D10 rationale adds "projection-query push-down is v2 (Story 5-4)" + status-enum-property heuristic consolidation deferred to Story 4.3 (D10/D11 cross-ref). **SCAMPER (M/R):** (M) AC6 subtitle renders empty fragment during Loading (no "0 entities" flash-of-false-info); (R) French pre-pass sampling moved from T8.4 to new T6.8 (decouples copy-discovery from test-cost). **Failure Mode Analysis (C1/C2/C4/C5/C6):** (C1) T1.6 ExplicitNullWhenStateIsTreatedAsAbsent + Known Gaps G10 comma-in-enum-name out of scope; (C2) T2.6 TransformRoleMappingIsExhaustive (parity with Emit-stage ADR-052); (C4) AC11 subscribe-throw hardening + T6.4 `_disposed` race flag + T6.6 degradation tests; (C5) T4.5 FcProjectionRoutes null-guards; (C6) T3.10 SubtitleIsEmittedOutsideRowCascade. **Explicitly skipped per user choice (flagged ⚠️):** Occam-A D10 memoization reversal, Occam-B C5 size-budget drop, Occam-C T3.12 struct-check drop, ToT-C D3 ImmutableArray IR amendment — user opted to keep round-2 additions rather than reverse them. Test count ≈ 72 → ≈ 77 (+5). Decision budget unchanged at 22/25. | advanced-elicitation orchestrator (claude-opus-4-7) |
