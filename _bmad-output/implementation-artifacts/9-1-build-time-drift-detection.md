# Story 9.1: Build-Time Drift Detection

Status: review

> **Epic 9** - Developer Tooling & Documentation. Covers **FR7**, **NFR8**, **NFR97**, and the first build-time handoff toward Stories **9-2**, **9-4**, and **9-5**. Builds on the existing SourceTools incremental generator, HFC diagnostic catalog discipline, Story 1-8 hot-reload/rebuild findings, Story 6-2/6-3/6-4 customization contract-version patterns, and Story 8-6 schema fingerprinting boundaries. Applies lessons **L01**, **L06**, **L07**, **L08**, **L10**, and **L14**.

---

## Executive Summary

Story 9-1 adds a build-time drift detector for FrontComposer generated UI contracts:

- Compare the current parsed domain declaration shape against a trusted generated-UI baseline snapshot.
- Emit actionable HFC diagnostics when projection properties, command form fields, bounded-context names, renderer-impacting metadata, or known generated contract expectations drift.
- Keep the detector inside the SourceTools incremental pipeline and under the NFR8 < 500 ms rebuild budget.
- Avoid runtime reflection and runtime environment data; drift evidence is compile-time structural metadata only.
- Leave CLI inspection/migration UX to Story 9-2, full diagnostic documentation governance to Story 9-4, and public docs-site publication to Story 9-5.

---

## Story

As a developer,
I want the framework to detect mismatches between my backend domain declarations and the generated UI at build time,
so that I catch breaking changes as compile-time diagnostics instead of discovering them as silent runtime bugs.

### Adopter Job To Preserve

An adopter should be able to rename, remove, add, or retag a `[Projection]`, `[Command]`, or `[BoundedContext]` declaration and receive a precise build diagnostic explaining what changed, what generated UI behavior is affected, and how to reconcile the baseline. The build should teach the fix without requiring the adopter to run the app, click through generated pages, or inspect `obj/` output by hand.

---

## Acceptance Criteria

| AC | Given | When | Then |
| --- | --- | --- | --- |
| AC1 | A `[Projection]`-annotated type has a trusted prior generated-UI baseline | The source generator parses the current type shape | It compares current structural metadata with the baseline and emits compile-time drift diagnostics for mismatches. |
| AC2 | A projection property is removed | The build runs | A diagnostic identifies the projection type, removed property name, affected UI surface, and docs link; the default severity is Warning unless configured stricter. |
| AC3 | A projection property is added | The build runs | A diagnostic identifies the added property and states whether it creates a new DataGrid column, detail field, MCP projection field metadata, or unsupported placeholder. |
| AC4 | A likely rename occurs, with one removed property and one added property of compatible category in the same declaration | The build runs | Rename diagnostics are emitted only when the comparison can deterministically match exactly one removed member and one added member within the same stable declaration identity. Ambiguous matches are reported as separate add/remove drift. The message follows the Epic 9 wording shape: `Property '{OldName}' was expected on {TypeName} but not found. '{NewName}' was added. If this is a rename, update the generated output. See HFC{id}.` |
| AC5 | A projection or command property type/category changes, such as `string` to `int` or nullable to non-nullable | The build runs | A Warning diagnostic states that generated form input, DataGrid column, filter behavior, badge/format behavior, or MCP descriptor metadata may change rendering behavior. |
| AC6 | A `[BoundedContext]` name changes | The build runs | A diagnostic states that navigation grouping, generated registration, persisted session state, MCP resource grouping, and badge/action-queue grouping may be affected. |
| AC7 | Display metadata changes, such as `[Display(Name)]`, `[Display(GroupName)]`, `[Description]`, `[ProjectionRole]`, `[ProjectionFieldGroup]`, `[ColumnPriority]`, `[RelativeTime]`, `[Currency]`, `[ProjectionEmptyStateCta]`, `[ProjectionBadge]`, `[Icon]`, `[Destructive]`, or `[RequiresPolicy]` | The build runs | Drift is classified by impact category and emits at most one bounded diagnostic per affected declaration and category. |
| AC8 | A trusted baseline is missing for a project that opted into drift detection | The build runs | The detector emits a single actionable baseline-missing diagnostic that distinguishes first-run/no-baseline state from invalid configured baseline paths and explains the 9-1 manual reconciliation path plus the future Story 9-2 CLI owner. |
| AC9 | A baseline exists but is empty, malformed, too large, duplicate-keyed, invariant-violating, generated by an unsupported schema version, produced by an unsupported algorithm, or duplicated across configured baseline files | The build runs | The detector fails closed with deterministic Error diagnostics, suppresses unsafe partial drift comparison, and never silently passes unverifiable content. There is no last-writer-wins baseline merge. |
| AC10 | Drift detection runs in an unchanged compilation | Incremental generator tracking is enabled | Existing `Parse`, `ParseCommand`, and template parse outputs remain Cached/Unchanged; unchanged `AdditionalText` baseline content and unrelated file edits do not re-run domain parsing or unrelated generated outputs. |
| AC11 | Drift detection runs after adding or modifying one domain property | Performance is measured | Median incremental rebuild added overhead remains below the NFR8 500 ms budget on a representative bounded fixture with warmup excluded, repeated measured iterations, and recorded median/p95 for cache-hit and cache-miss paths. |
| AC12 | The detector emits diagnostics | A developer views the compiler output | Each message includes stable What, Expected, Got, Fix, and DocsLink fields or structured equivalent, uses HFC SourceTools IDs, carries `DiagnosticDescriptor.HelpLinkUri`, and exposes bounded properties such as `BaselinePath`, `DeclarationPath`, `DeclarationName`, `MemberName`, `DriftKind`, `ExpectedShapeHash`, `ActualShapeHash`, `SchemaVersion`, and `AlgorithmVersion` when available. Paths are normalized to deterministic repo-relative or fixed sentinel forms before entering messages/properties. |
| AC13 | Drift affects hidden, tenant-scoped, policy-gated, MCP-only, or user-authored baseline metadata | The build runs | Diagnostics are structural and sanitized; they never include tenant IDs, user IDs, claims, tokens, command payload values, query rows, cache keys, ETags, local absolute paths, raw JSON fragments, generated source snippets, oversized arbitrary text, or raw exception text. |
| AC14 | A trim-enabled or native-AOT host relies on the default reflection `IActionQueueProjectionCatalog` | The analyzer can see the project setting and missing adopter override evidence | A build-time diagnostic warns that the reflection catalog is trim-incompatible and points to the source-generated catalog/override path. |
| AC15 | A host policy catalog check cannot be proven at build time | The build runs | The story records the limitation explicitly; runtime validators remain authoritative and the build-time diagnostic is emitted only where evidence is reliable. |
| AC16 | The detector emits many changes in a large projection or command | Output is generated | Diagnostics are sorted by bounded context, declaration kind, declaration name, member name, then drift kind using ordinal comparison; the first 50 drift diagnostics are emitted after sorting, followed by one deterministic truncation summary that reports the omitted count. |
| AC17 | A baseline update is intentional | The developer follows the documented workflow | The story provides an internal deterministic comparison service/result model that Story 9-2 can wrap with CLI inspect/migration commands, but Story 9-1 does not ship public CLI verbs, command UX, code fixes, source rewrites, or a public API shape. |
| AC18 | Existing generated code is compiled after drift detection changes | The regression suite runs | Normal builds are diagnostics-only and do not rewrite trusted baselines or alter generated UI output. Existing projection/command generation remains byte-stable across repeated runs, equivalent input ordering, partial type declaration ordering, and equivalent baseline file ordering, unless an explicitly invoked future baseline-generation path changes metadata. |
| AC19 | Drift detection runs under non-default cultures such as `fr-FR` or `tr-TR` | Diagnostics and generated output are compared | Classification, sorting, hashes, numeric formatting, diagnostics, and generated bytes remain culture-invariant unless localization is explicitly introduced by a later story. |

---

## Tasks / Subtasks

- [x] T1. Define the drift baseline contract (AC1-AC9, AC13, AC16)
  - [x] Add SourceTools-owned baseline models for projection, command, bounded-context, renderer-impacting metadata, and generated artifact expectations.
  - [x] Define the baseline owner, checked-in file location contract, schema version, algorithm version, deterministic parse rules, and trust boundary. Story 9-1 reads only analyzer `AdditionalText` baseline artifacts, never live runtime state.
  - [x] Keep models SDK-neutral and runtime-data-free. Do not reference Shell components, Fluent UI types, EventStore clients, MCP SDK DTOs, tenant/user services, or runtime reflection outputs.
  - [x] Include only structural fields already present in `DomainModel`, `CommandModel`, `PropertyModel`, `RazorModel`, `CommandFormModel`, `RegistrationModel`, and MCP descriptor transforms.
  - [x] Include a baseline schema version and algorithm identifier so unsupported baselines fail closed instead of comparing incompatible content.
  - [x] Cap maximum baseline file size, declaration count, property count, nesting depth, and diagnostic count through explicit options.
  - [x] Validate analyzer-configured size/count/severity options before comparison; invalid option values emit deterministic configuration diagnostics and fall back only to documented safe defaults.
  - [x] Reject empty/whitespace baselines, duplicate declaration/member identities, duplicate identities across multiple configured baseline files, mixed valid/invalid entries, and invariant-violating snapshots before drift comparison.

- [x] T2. Load baselines through the incremental pipeline (AC8-AC11, AC16)
  - [x] Use Roslyn `AdditionalTextsProvider` / analyzer config inputs for checked-in baseline files rather than ad hoc file IO in emitters.
  - [x] If multiple baseline `AdditionalText` files are configured, normalize and sort by deterministic repo-relative path, merge only unique identities, and fail closed on duplicates instead of allowing file-order-dependent overwrite behavior.
  - [x] Keep `ForAttributeWithMetadataName` discovery for `[Projection]`, `[Command]`, and `[ProjectionTemplate]`; drift inputs are an additional provider, not a replacement parse path.
  - [x] Ensure baseline changes invalidate only drift comparison and shared generated baseline metadata, not unrelated projection/command outputs.
  - [x] Add an internal test-only observation seam for parse/comparison counts or tracked-step inspection; do not expose it as public API.
  - [x] Add tests proving unrelated file edits and unchanged `AdditionalText` content keep existing tracked steps Cached/Unchanged.
  - [x] Add malformed, unsupported-version, oversized, and missing-baseline fixtures.

- [x] T3. Implement structural comparison and classification (AC1-AC7, AC13, AC16)
  - [x] Compare by stable declaration identity: namespace + type name + contract family + bounded context, with explicit behavior for global namespace.
  - [x] Merge partial declarations deterministically before comparison; source locations may choose a stable primary declaration, but partial file order must not affect identity, hashes, diagnostics, or generated output.
  - [x] Classify property added, removed, likely rename, type/category change, nullability change, display/description change, enum member/badge change, role/group/priority change, command density change, derivable/non-derivable split change, authorization policy change, destructive/icon metadata change, and bounded-context name change.
  - [x] Reuse existing type category and transform logic instead of duplicating field inference rules.
  - [x] Treat likely rename as a deterministic one-to-one classification only; never rewrite source or update baselines automatically in this story.
  - [x] Sort diagnostics by bounded context, declaration kind, declaration name, member name, then drift kind using ordinal comparison before applying the diagnostic cap.

- [x] T4. Add HFC diagnostics and catalog discipline (AC2-AC9, AC12, AC14-AC16)
  - [x] Reserve a contiguous SourceTools ID block after the current HFC1057 allocation, unless Story 9-4 has already assigned a final range.
  - [x] Add descriptors to `DiagnosticDescriptors`, constants to `FcDiagnosticIds`, and rows to `AnalyzerReleases.Unshipped.md`.
  - [x] Use `DiagnosticDescriptor.HelpLinkUri` for each new diagnostic where supported by the current Microsoft.CodeAnalysis package.
  - [x] Ensure every diagnostic message carries What, Expected, Got, Fix, and DocsLink fields or equivalent structured parameters.
  - [x] Add a diagnostic precedence table for missing baseline, malformed/empty baseline, unsupported schema, unsupported algorithm, oversized baseline, duplicate/invariant failure, structural drift, metadata drift, renderer-impacting drift, truncation summary, redaction failure, and trim/AOT reflection-catalog evidence.
  - [x] Add diagnostics for invalid drift-detector analyzer-config options and duplicate baseline identities across configured files; both must be stable, sanitized, and suppress unsafe downstream comparison where needed.
  - [x] Use deterministic Error diagnostics for trust-breaking baseline validation failures and suppress downstream drift conclusions for the affected baseline.
  - [x] Add catalog tests proving IDs are unique, shaped as HFCxxxx, documented in release notes, and linked to canonical diagnostics docs.

- [x] T5. Emit or refresh baseline metadata for generated UI contracts (AC1, AC3, AC5, AC7, AC17, AC18)
  - [x] Define the checked-in baseline file naming and folder contract. Prefer a deterministic SourceTools-owned location under the adopter project, not `obj/`.
  - [x] Generate baseline material from the same parse/transform data that produces Razor, Fluxor, registration, MCP manifests, and template/slot/view descriptor metadata.
  - [x] Do not hash or diff generated C# source text; compare structural metadata so formatting or banner churn does not create false positives.
  - [x] Preserve existing generated output hint names: namespace-qualified projection hints and `.Command` command hint prefixes.
  - [x] Do not automatically rewrite or refresh trusted checked-in baselines during normal builds; normal Story 9-1 behavior is diagnostics-only.
  - [x] Ensure any emitted metadata is deterministic, timestamp-free, culture-invariant, path-normalized, and byte-stable for identical structural inputs.
  - [x] Document that Story 9-2 owns the user-facing CLI command to inspect or update these baselines.

- [x] T6. Add trim/AOT reflection-catalog diagnostics where evidence is reliable (AC14, AC15)
  - [x] Detect `PublishTrimmed=true` / native AOT project settings only through analyzer config/build metadata available to SourceTools.
  - [x] Warn when the default `ReflectionActionQueueProjectionCatalog` path appears to be used without a source-generated or adopter-supplied `IActionQueueProjectionCatalog`.
  - [x] If adopter override evidence is not statically knowable, emit guidance as Warning/Info only and leave runtime validation as authoritative.
  - [x] Keep this diagnostic narrow to the documented Story 3-5 G22 gap; do not scan assemblies, inspect runtime types, add runtime dependencies, or start a broad DI-lifetime analyzer in this story.

- [x] T7. Tests and verification (AC1-AC19)
  - [x] Unit tests for baseline parsing, schema-version rejection, unsupported algorithm, oversized/truncated output, missing baseline, empty/whitespace baseline, duplicate identities, duplicate identities across baseline files, invalid analyzer-config options, invariant violations, mixed valid/invalid entries, and malformed baseline.
  - [x] Matrix-driven classification tests for property add/remove/deterministic rename/type/nullability, command field add/remove/type, bounded-context change, display metadata, role/group/priority, enum/badge, command density, derivable split, destructive/icon, policy metadata changes, and generated contract expectation drift.
  - [x] Integration tests using `CSharpGeneratorDriver` and `AdditionalText` baselines proving diagnostics appear at the correct source location and have `HelpLinkUri`.
  - [x] Golden diagnostic fixtures for no drift, added declaration, removed declaration, changed type/signature, reordered declarations with no semantic drift, oversized drift truncation, invalid baseline, and redaction sentinel.
  - [x] Incremental caching tests proving unrelated edits do not invalidate existing generator outputs and unchanged baselines do not re-run unrelated parse work.
  - [x] Performance tests extending `IncrementalRebuildBenchmarkTests` or a sibling benchmark with warmup excluded, at least 20 measured iterations where practical, median/p95 reporting, and representative cache-hit/cache-miss paths; mark machine-sensitive tests as benchmark/integration rather than normal unit gates if needed.
  - [x] Regression tests proving generated Razor/Fluxor/registration/MCP output still compiles after adding drift detection and remains byte-stable across repeated runs, equivalent input order changes, partial declaration order changes, and equivalent baseline file ordering.
  - [x] Redaction tests with tenant/user/token/path/ETag/payload sentinels in source and baseline fixtures, diagnostic properties, exception paths, raw JSON fragments, generated source snippets, and oversized values.
  - [x] Culture-invariance test under `fr-FR` or `tr-TR` proving stable sorting, formatting, hashes, diagnostics, and generated bytes.
  - [x] Targeted suite: `tests/Hexalith.FrontComposer.SourceTools.Tests/Diagnostics`, `.../Incremental`, `.../Integration`, and existing emitter snapshot tests only when intentionally touched.
  - [x] Full regression: `dotnet build Hexalith.FrontComposer.sln -p:TreatWarningsAsErrors=true -p:UseSharedCompilation=false`.

---

## Dev Notes

### Existing SourceTools State

- `FrontComposerGenerator` is already an `IIncrementalGenerator` using `SyntaxProvider.ForAttributeWithMetadataName` for `[Projection]`, `[Command]`, and `[ProjectionTemplate]`. Preserve this shape; drift detection should add comparison inputs, not replace the parse pipeline.
- Current generator fan-out:
  - Projection: Razor view, Fluxor feature, actions, reducers, registration.
  - Command: form, actions, lifecycle feature, registration, renderer, last-used subscriber, lifecycle bridge, optional full-page route.
  - Shared: projection template manifest and MCP manifest.
- Existing tracked step names include `Parse`, `ParseCommand`, and `ParseProjectionTemplate`; benchmark and cache tests assert these names.
- `DomainModel`, `CommandModel`, and `PropertyModel` are pure data and already include most fields needed for drift comparison. Some fields such as `SourceFilePath`, `SourceLine`, and `SourceColumn` are useful for locations but must not become baseline identity.

### Diagnostic ID Discipline

- SourceTools diagnostics live in HFC1000-HFC1999.
- Current `AnalyzerReleases.Unshipped.md` allocates through HFC1057.
- `DiagnosticCatalogTests` reflect over `DiagnosticDescriptors` and `FcDiagnosticIds`; update both when adding new IDs.
- Several older generator descriptors do not yet carry `HelpLinkUri`; Story 9-1 should set `HelpLinkUri` on new descriptors and avoid broad churn to older IDs unless needed.
- HFC1010 already exists as the hot-reload/full-rebuild guidance ID; do not reuse it for general drift. Either route only the documented full-rebuild categories through HFC1010 or allocate new drift-specific IDs.

### Baseline Contract

Use one structural baseline source of truth. Recommended shape:

```json
{
  "schemaVersion": "frontcomposer.generated-ui-baseline.v1",
  "algorithm": "frontcomposer-structural-v1",
  "contracts": [
    {
      "family": "projection",
      "type": "Acme.Shipping.ShipmentProjection",
      "boundedContext": "Shipping",
      "displayName": "Shipments",
      "role": "ActionQueue",
      "properties": [
        {
          "name": "Priority",
          "category": "Enum",
          "nullable": false,
          "displayName": "Priority",
          "description": null,
          "columnPriority": 1,
          "fieldGroup": null,
          "displayFormat": "Default"
        }
      ]
    }
  ]
}
```

The final JSON schema can differ, but it must remain deterministic, reviewable, and free of runtime values.

Baseline ownership and trust boundary:

- Story 9-1 owns the SourceTools baseline schema models, parser, validator, comparison result model, and diagnostics.
- The trusted baseline is a checked-in or analyzer-configured `AdditionalText` artifact. Story 9-1 must not read `obj/`, generated source text, application runtime services, environment values, live tenant/user state, or reflection output as trust evidence.
- Baseline identity is structural: schema version, algorithm version, contract family, bounded context, namespace/type name, and member identity. Source file paths and line/column values may locate diagnostics but must not define trust identity.
- Multiple configured baseline files are allowed only as deterministic inputs to the same trust boundary. Normalize their paths, sort them ordinally, and reject duplicate contract/member identities across files before comparison; never let file enumeration order decide which baseline wins.
- Unknown schema, unknown algorithm, malformed JSON, empty/whitespace content, duplicate identities, invariant violations, and oversized content are trust failures. They emit deterministic Error diagnostics and suppress downstream comparison for that baseline.
- Missing baseline is not the same failure as invalid baseline. A missing baseline gets one actionable first-run/reconciliation diagnostic; an existing invalid baseline gets the highest-precedence trust-failure diagnostic.
- Story 9-1 may generate deterministic metadata needed by SourceTools, but it must not automatically rewrite trusted checked-in baselines during normal builds. Story 9-2 owns the public baseline inspect/update workflow.

### Developer Workflow

During build, SourceTools compares current declarations against the checked-in structural baseline. If drift is found, diagnostics identify the changed declaration, drift kind, affected generated UI behavior category, and the reconciliation action available in Story 9-1. In Story 9-1 the developer can inspect or manually update the checked-in baseline artifact; Story 9-2 owns the public CLI workflow for inspecting, updating, dry-running, or migrating baselines.

Allowed 9-1 affected-behavior labels are bounded to structural generated UI contract categories:

- Projection surface: generated route/view, DataGrid column, detail field, filter/sort/search behavior, role/badge/format/priority/group/empty-state metadata, unsupported placeholder, and MCP projection field metadata.
- Command surface: generated form field, input category, validation/rejection metadata, destructive/icon/density metadata, lifecycle bridge metadata, and command registration.
- Bounded-context surface: navigation grouping, generated registration, persisted session grouping, MCP resource grouping, and badge/action-queue grouping.
- Template/customization surface: template, slot, view descriptor, and generated contract expectation metadata already present in SourceTools IR.

### Diagnostic Contract

Story 9-1 should reserve a contiguous HFC SourceTools range after HFC1057 unless Story 9-4 has already assigned the final range. Suggested names are placeholders; implementation should follow the current repository naming convention.

| Precedence | Category | Default severity | Behavior |
| --- | --- | --- | --- |
| 1 | Missing baseline | Warning | Emit one actionable first-run/reconcile diagnostic; no drift comparison for the missing baseline. |
| 2 | Empty or malformed baseline | Error | Fail closed and suppress drift comparison. |
| 3 | Unsupported schema version | Error | Fail closed and suppress drift comparison. |
| 4 | Unsupported algorithm version | Error | Fail closed and suppress drift comparison. |
| 5 | Oversized baseline or exceeded shape bounds | Error | Fail closed and suppress drift comparison. |
| 6 | Duplicate identity or invariant violation | Error | Fail closed and suppress drift comparison. |
| 7 | Structural drift | Warning unless configured stricter | Emit deterministic add/remove/rename/type/nullability diagnostics. |
| 8 | Metadata or renderer-impacting drift | Warning unless configured stricter | Emit one bounded diagnostic per declaration/category. |
| 9 | Trim/AOT reflection-catalog evidence | Warning/Info depending evidence strength | Warn only on statically reliable evidence; runtime validators remain authoritative. |
| 10 | Truncation summary | Warning | Emit after the first 50 sorted drift diagnostics and include omitted count. |

Diagnostic properties should be stable, bounded, and sanitized. Use these keys where applicable: `BaselinePath`, `DeclarationPath`, `DeclarationName`, `MemberName`, `DriftKind`, `ExpectedShapeHash`, `ActualShapeHash`, `SchemaVersion`, and `AlgorithmVersion`. Omit absent values or use a fixed sentinel; never emit raw JSON, generated source text, absolute local paths, secrets, payload values, or exception text. Path values must be normalized to repo-relative forward-slash forms when possible, or a fixed sentinel such as `<outside-project>` when not safely relativizable.

### Comparison Seam

The Story 9-2 handoff is an internal deterministic SourceTools seam, not a CLI design. Keep comparison logic available as a pure internal service/function that accepts parsed declaration models, validated baseline models, and bounded options, then returns comparison results/diagnostic facts. The seam owns partial-declaration merge order and multi-baseline duplicate detection so analyzer and future CLI callers cannot drift. Do not introduce public command verbs, console formatting, code fixes, source rewrites, or public package API shape in Story 9-1.

### Performance and Incrementality

Measure the NFR8 target as added incremental-generator overhead for drift detection on a representative bounded fixture, not as total clean build wall-clock time. Exclude warmup runs, record median and p95 where practical, and cover cache-hit and cache-miss paths. Keep one focused benchmark/perf guard plus lower-level incremental invalidation tests; do not create a broad machine-fragile performance suite.

### Architecture Constraints

- SourceTools targets `netstandard2.0` and references `Microsoft.CodeAnalysis.CSharp` with `PrivateAssets="all"`.
- The current package pin is `Microsoft.CodeAnalysis.CSharp` 4.12.0; do not move it inside this story unless a build break forces it.
- Implement Story 9-1 under `src/Hexalith.FrontComposer.SourceTools` generator/analyzer internals with tests under `tests/Hexalith.FrontComposer.SourceTools.Tests`. Do not add public CLI, runtime rendering package, Shell runtime, MCP runtime, or docs-site dependencies in this story.
- Prefer existing serializer/package patterns. Do not add runtime package dependencies or reflection-based baseline parsing for the detector. **Exception (Story 9-1, code-review 2026-05-07 D2):** `System.Text.Json` is permitted as the baseline JSON parser. Rationale: it is the standard reflection-free parser; SourceTools targets `netstandard2.0` where `Utf8JsonReader`/`JsonDocument` work without any runtime SDK contract; adding it does not pull in adopter-side runtime services, and a hand-rolled tokenizer would carry higher fail-open risk for trust-failure detection. Treat this as a single, named exception; do not extrapolate to other packages without an explicit decision.
- Microsoft Learn confirms `IIncrementalGenerator` instances should not store state directly; keep comparison state in incremental values, not generator instance fields.
- Microsoft Learn confirms `AdditionalTextsProvider` supplies non-code files to an incremental generator; use that for baselines.
- Microsoft Learn confirms `ForAttributeWithMetadataName` only transforms nodes carrying a matching attribute metadata name and notes partial-type behavior; partial declarations matter for drift and hot-reload classification.
- `DiagnosticDescriptor.HelpLinkUri` is the Roslyn API for diagnostic documentation links; use it on new descriptors.
- Analyzer-config values that tune drift bounds or severities are build inputs. Validate and normalize them through the incremental pipeline; invalid values should not cause generator exceptions, silent disablement, or culture-dependent parsing.

### Cross-Story Contract Table

| Producer | Consumer | Contract |
| --- | --- | --- |
| Story 1-8 | Story 9-1 | Hot-reload guide, NFR8 benchmark harness, HFC1010 full-rebuild classification, and generator step tracking. |
| Stories 1-4 through 1-5 | Story 9-1 | Parse/Transform/Emit IR split, deterministic generator output, unsupported placeholder strategy, and no silent drop discipline. |
| Stories 2-2 through 2-5 | Story 9-1 | Command density, derived fields, destructive command metadata, lifecycle bridge generation, and validation/rejection metadata that can drift. |
| Stories 3-1 through 3-6 | Story 9-1 | Bounded-context navigation/session persistence behaviors affected by context name drift. |
| Stories 4-1 through 4-6 | Story 9-1 | Projection role, badge, filter, priority, group, description, unsupported-field, and empty-state metadata that changes generated UI. |
| Stories 6-2 through 6-6 | Story 9-1 | Template/slot/view contract-version drift patterns and diagnostic shape discipline. |
| Story 7-3 | Story 9-1 | `RequiresPolicy` metadata, runtime policy validators, and deferred build-time policy catalog checks. |
| Story 8-6 | Story 9-1 | Schema fingerprint/package-boundary discipline; Story 9-1 detects generated UI baseline drift, not MCP negotiation behavior. |
| Story 9-2 | Story 9-1 | Future CLI inspect/update commands consume the baseline and diff library produced here. |
| Story 9-4 | Story 9-1 | Final diagnostic ID documentation/deprecation governance. |
| Story 9-5 | Story 9-1 | Public docs pages and migration guide publication. |

### Scope Guardrails

Do not implement these in Story 9-1:

- Public CLI commands for inspecting or updating baselines. Owner: Story 9-2.
- Automated source rewrites, rename refactoring, code fixes, or migration tools. Owner: Story 9-2 / Story 9-4.
- Public comparison APIs, command-line output formatting, or baseline update UX. Owner: Story 9-2.
- Public diagnostics documentation site pages. Owner: Story 9-4 / Story 9-5.
- MCP runtime schema negotiation, client fingerprint comparison, or agent-visible compatibility responses. Owner: Story 8-6.
- Broad DI lifetime analysis, service-locator audits, policy-catalog whole-host proof, assembly scanning, runtime type inspection, or runtime authorization redesign. Owners: deferred follow-ups / Story 9-4.
- Visual specimen drift, accessibility screenshot gates, or generated UI screenshot baselines. Owner: Story 10-2.
- Mutation testing rollout. Owner: Story 10-4.
- A new baseline file watcher outside Roslyn incremental inputs.

### Known Gaps / Follow-Ups

| Gap | Owner |
| --- | --- |
| CLI baseline inspect/update/dry-run command. | Story 9-2 |
| HFC diagnostic docs pages and final ID governance. | Story 9-4 |
| Public docs and migration guide flow. | Story 9-5 |
| Visual/specimen drift enforcement. | Story 10-2 |
| Mutation score expansion for drift classifier. | Story 10-4 |
| Full build-time proof of host policy catalog registration. | Story 9-4 or dedicated auth/tooling follow-up |
| Broad DI/service-lifetime analyzer for MCP/Shell services. | Deferred architecture/tooling follow-up |

---

## References

- [Source: `_bmad-output/planning-artifacts/epics/epic-9-developer-tooling-documentation.md#Story-9.1`] - story statement and acceptance criteria foundation.
- [Source: `_bmad-output/planning-artifacts/prd/functional-requirements.md#FR7`] - build-time drift detection requirement.
- [Source: `_bmad-output/planning-artifacts/prd/non-functional-requirements.md#NFR8`] - incremental generator rebuild budget.
- [Source: `_bmad-output/planning-artifacts/prd/developer-tool-specific-requirements.md#Build-time-error-quality`] - Expected/Got/Fix/DocsLink error quality.
- [Source: `_bmad-output/planning-artifacts/architecture.md#Source-Generator-as-Infrastructure`] - generator as infrastructure and diagnostic-first policy.
- [Source: `_bmad-output/planning-artifacts/architecture.md#Diagnostic-Condition-Table`] - HFC diagnostic severity pattern.
- [Source: `docs/hot-reload-guide.md#Diagnostic-Reservation-HFC1010`] - HFC1010 rebuild diagnostic reservation and generator-diff limitation.
- [Source: `src/Hexalith.FrontComposer.SourceTools/FrontComposerGenerator.cs`] - current incremental generator pipeline and output fan-out.
- [Source: `src/Hexalith.FrontComposer.SourceTools/Parsing/DomainModel.cs`] - existing pure parse IR and diagnostic-location data.
- [Source: `src/Hexalith.FrontComposer.SourceTools/Diagnostics/DiagnosticDescriptors.cs`] - current SourceTools diagnostic descriptors.
- [Source: `src/Hexalith.FrontComposer.SourceTools/AnalyzerReleases.Unshipped.md`] - current HFC SourceTools allocation.
- [Source: `tests/Hexalith.FrontComposer.SourceTools.Tests/Benchmarks/IncrementalRebuildBenchmarkTests.cs`] - NFR8 benchmark harness.
- [Source: `tests/Hexalith.FrontComposer.SourceTools.Tests/Caching/IncrementalCachingTests.cs`] - existing incremental caching assertions.
- [Source: `src/Hexalith.FrontComposer.Shell/Badges/ReflectionActionQueueProjectionCatalog.cs`] - documented trim/AOT reflection-catalog gap.
- [Source: `src/Hexalith.FrontComposer.Contracts/Badges/IActionQueueProjectionCatalog.cs`] - adopter override path for action-queue catalog.
- [Source: Microsoft Learn `IIncrementalGenerator`](https://learn.microsoft.com/en-us/dotnet/api/microsoft.codeanalysis.iincrementalgenerator) - generator instance lifetime and incremental initialization.
- [Source: Microsoft Learn `IncrementalGeneratorInitializationContext.AdditionalTextsProvider`](https://learn.microsoft.com/en-us/dotnet/api/microsoft.codeanalysis.incrementalgeneratorinitializationcontext.additionaltextsprovider) - AdditionalText input provider.
- [Source: Microsoft Learn `SyntaxValueProvider.ForAttributeWithMetadataName`](https://learn.microsoft.com/en-us/dotnet/api/microsoft.codeanalysis.syntaxvalueprovider.forattributewithmetadataname) - attribute-driven incremental discovery.
- [Source: Microsoft Learn `DiagnosticDescriptor.HelpLinkUri`](https://learn.microsoft.com/en-us/dotnet/api/microsoft.codeanalysis.diagnosticdescriptor.helplinkuri) - diagnostic docs link property.

---

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- 2026-05-07: `dotnet build src\Hexalith.FrontComposer.SourceTools\Hexalith.FrontComposer.SourceTools.csproj -p:TreatWarningsAsErrors=true --no-restore` passed after netstandard compatibility cleanup.
- 2026-05-07: `dotnet test tests\Hexalith.FrontComposer.SourceTools.Tests\Hexalith.FrontComposer.SourceTools.Tests.csproj --no-restore` passed: 723 passed, 2 skipped benchmark tests.
- 2026-05-07: `dotnet build Hexalith.FrontComposer.sln -p:TreatWarningsAsErrors=true -p:UseSharedCompilation=false` passed: 0 warnings, 0 errors.
- 2026-05-07: `dotnet test Hexalith.FrontComposer.sln --no-restore` passed: Contracts 159, MCP 264 + 1 skipped, SourceTools 723 + 2 skipped benchmarks, Shell 1542, Shell.Bench 2.

### Completion Notes List

- 2026-05-02: Story created via `/bmad-create-story 9-1-build-time-drift-detection` during recurring pre-dev hardening job. Ready for party-mode review on a later run.
- 2026-05-03: Party-mode review via `/bmad-party-mode 9-1-build-time-drift-detection; review;` completed with Winston, Amelia, John, and Murat. Applied story hardening for baseline trust ownership, developer reconciliation workflow, deterministic rename/cap ordering, fail-closed baseline precedence, diagnostic properties/sanitization, SourceTools-only comparison seam, no automatic baseline rewrites, performance/incrementality measurement bounds, trim/AOT scope guardrails, culture/byte-stability tests, and matrix/golden fixture guidance. Final recommendation: ready-for-dev after story update.
- 2026-05-03: Advanced elicitation via `/bmad-advanced-elicitation 9-1-build-time-drift-detection` completed two batches focused on security, failure modes, determinism, and simplification. Applied multi-baseline duplicate rejection, analyzer-config option validation, partial declaration merge determinism, normalized diagnostic path rules, and seam ownership clarifications. Final recommendation: ready-for-dev.
- 2026-05-07: Implemented Story 9-1 build-time drift detection in SourceTools. Added internal baseline parser/validator, analyzer-config option binder, incremental `AdditionalTextsProvider` baseline loading, internal comparison seam, structural/metadata drift classification, sanitized HFC1058-HFC1070 diagnostics with HelpLinkUri/properties, trim/AOT reflection-catalog advisory, deterministic partial-declaration ordering, and active drift test coverage. Normal generation remains diagnostics-only and byte-stable.

### Party-Mode Review

- 2026-05-03T09:10:15+02:00: `bmad-party-mode` review completed for selected story `9-1-build-time-drift-detection`.
- Command/skill invocation: `/bmad-party-mode 9-1-build-time-drift-detection; review;`.
- Participating BMAD agents: Winston (System Architect), Amelia (Senior Software Engineer), John (Product Manager), Murat (Master Test Architect and Quality Advisor).
- Findings summary: baseline ownership/trust boundary, 9-2 comparison seam, diagnostic precedence/taxonomy/properties, fail-closed severity, deterministic ordering/truncation, reconcile workflow, generated-output byte stability, incremental/performance measurement, redaction/culture oracles, and trim/AOT scope needed to be explicit before dev.
- Changes applied: tightened AC4, AC8-AC13, AC16-AC19; added baseline contract, developer workflow, diagnostic contract, comparison seam, and performance/incrementality sections; hardened T1-T7 with trust-failure coverage, deterministic comparison/cap rules, no automatic baseline rewrites, SourceTools-only boundaries, matrix/golden fixtures, redaction, culture, cache, perf, and byte-stability tests; clarified scope guardrails.
- Findings deferred: public CLI inspect/update/dry-run and command UX to Story 9-2; final diagnostic documentation/governance to Story 9-4; public docs flow to Story 9-5; visual/specimen drift to Story 10-2; mutation testing expansion to Story 10-4; broad DI/runtime authorization proof to Story 9-4 or a dedicated follow-up.
- Final recommendation: ready-for-dev.

### Advanced Elicitation

- 2026-05-03T09:20:34+02:00: `bmad-advanced-elicitation` completed for selected story `9-1-build-time-drift-detection`.
- Command/skill invocation: `/bmad-advanced-elicitation 9-1-build-time-drift-detection`.
- Batch 1 methods: Pre-mortem Analysis; Failure Mode Analysis; Red Team vs Blue Team; Security Audit Personas; Self-Consistency Validation.
- Reshuffled Batch 2 methods: Chaos Monkey Scenarios; Occam's Razor Application; Comparative Analysis Matrix; Hindsight Reflection; Socratic Questioning.
- Findings summary: remaining risks centered on file-order-dependent baseline merges, partial declaration ordering, invalid analyzer-config options, diagnostic path leakage, and duplicated comparison behavior between analyzer and future CLI consumers.
- Changes applied: tightened AC9, AC12, and AC18; hardened T1-T4 and T7 with duplicate-baseline, invalid-option, partial-order, and equivalent-baseline-order coverage; clarified baseline trust handling, diagnostic path normalization, and comparison seam ownership.
- Findings deferred: none.
- Final recommendation: ready-for-dev.

### File List

- `_bmad-output/implementation-artifacts/9-1-build-time-drift-detection.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `src/Hexalith.FrontComposer.Contracts/Diagnostics/FcDiagnosticIds.cs`
- `src/Hexalith.FrontComposer.SourceTools/AnalyzerReleases.Unshipped.md`
- `src/Hexalith.FrontComposer.SourceTools/Diagnostics/DiagnosticDescriptors.cs`
- `src/Hexalith.FrontComposer.SourceTools/Drift/DriftDetection.cs`
- `src/Hexalith.FrontComposer.SourceTools/FrontComposerGenerator.cs`
- `src/Hexalith.FrontComposer.SourceTools/Hexalith.FrontComposer.SourceTools.csproj`
- `src/Hexalith.FrontComposer.SourceTools/Parsing/AttributeParser.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Baseline/DriftAnalyzerConfigOptionsTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Baseline/DriftBaselineMissingDiagnosticTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Baseline/DriftBaselineTrustFailureTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Comparison/DriftClassifierBoundedContextTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Comparison/DriftClassifierMetadataTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Comparison/DriftClassifierProjectionPropertyTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Comparison/DriftClassifierRenameTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Comparison/DriftClassifierTypeAndNullabilityTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Comparison/DriftComparisonServiceTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Diagnostics/DriftDiagnosticCatalogTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Diagnostics/DriftDiagnosticContractTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Diagnostics/DriftDiagnosticOrderingAndTruncationTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Diagnostics/DriftDiagnosticPrecedenceTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Diagnostics/DriftDiagnosticRedactionTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Incremental/DriftIncrementalCacheTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Regression/DriftByteStabilityRegressionTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Regression/DriftCultureInvarianceTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/Seam/DriftSeamPublicSurfaceContractTests.cs`
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Drift/TrimAot/TrimAotReflectionCatalogDiagnosticTests.cs`

### Change Log

- 2026-05-07: Completed build-time drift detection implementation and moved story to review.
- 2026-05-07: Code review (Chunk A — implementation source) findings appended below.
- 2026-05-07: Applied all 30 Chunk A patches (3 from resolved decisions + 8 high + 13 medium + 6 cleanup, plus the AttributeParser-sort patch reverted to deferred). Solution build passes `TreatWarningsAsErrors=true`; SourceTools test suite 722 passing / 2 skipped (existing benchmark skips). Solution test suite 2,689 passing / 3 skipped. Status held at `review` because Chunks B (validation tests) and C (quality-attribute tests) remain to review before flipping to `done`.
- 2026-05-07: Code review (Chunk B — validation tests) findings appended below. Applied all 38 Chunk B patches (15 critical/high + 19 medium + 4 low) and routed 4 minor pre-existing items to `deferred-work.md`. Two AC7 metadata categories from CB-7 (ProjectionBadge slot mappings, Destructive contract flag) were promoted to deferred during application — both required production-API surface area beyond the Chunk B test scope. Solution build passes `TreatWarningsAsErrors=true`; SourceTools test suite 769 passing / 2 skipped. Solution test suite 2,736 passing / 3 skipped. Status held at `review` until Chunk C (quality-attribute tests — byte-stability, culture invariance, incremental cache, trim/AOT, public surface) is reviewed.

### Review Findings — Chunk A (Implementation Source)

> Source: `/bmad-code-review` adversarial pass. Range `2882d95^..HEAD`, scoped to `Drift/DriftDetection.cs`, `Diagnostics/DiagnosticDescriptors.cs`, `FrontComposerGenerator.cs`, `Parsing/AttributeParser.cs`, `FcDiagnosticIds.cs`, `AnalyzerReleases.Unshipped.md`, csproj. Reviewers: Blind Hunter, Edge Case Hunter, Acceptance Auditor.

#### Decision-Needed (Resolved 2026-05-07)

- [x] [Review][Decision] Trim/AOT diagnostic without adopter-override evidence — **Resolved: implement evidence detection.** Search the compilation for any `IActionQueueProjectionCatalog` symbol implementation other than `ReflectionActionQueueProjectionCatalog`; emit HFC1070 only when none is found. (Promoted to Patch P-D1 below.)
- [x] [Review][Decision] `System.Text.Json` runtime package — **Resolved: update the spec to allow STJ explicitly.** Architecture Constraints will be amended to permit `System.Text.Json` for baseline parsing as the reflection-free, netstandard2.0-compatible standard parser. (Promoted to Patch P-D2 below — spec update only.)
- [x] [Review][Decision] `DisplayFormat.ToString()` serialization ambiguity — **Resolved: investigate-then-fix.** Read `PropertyModel.DisplayFormat` declaration and propose the precise fix during patch application. (Promoted to Patch P-D3 below.)

#### Patch — From Resolved Decisions

- [x] [Review][Patch] **P-D1** Implement Trim/AOT adopter-override evidence detection `[FrontComposerGenerator.cs / DriftDetection.cs Trim/AOT branch]` — Search the compilation for `IActionQueueProjectionCatalog` symbol implementations; emit HFC1070 only when none is found other than `ReflectionActionQueueProjectionCatalog`. AC14 satisfied.
- [x] [Review][Patch] **P-D2** Update spec Architecture Constraints to permit `System.Text.Json` for baseline parsing `[_bmad-output/implementation-artifacts/9-1-build-time-drift-detection.md → Architecture Constraints]` — Add explicit allowance with rationale (reflection-free, netstandard2.0-compatible, standard analyzer pattern).
- [x] [Review][Patch] **P-D3** Investigate `PropertyModel.DisplayFormat` declaration and patch the comparison serialization `[Drift/DriftDetection.cs:912, 948]` — Read source, choose canonical wire format (enum-name string with `"None"` sentinel; null-guard if reference), align with baseline schema.

#### Patch — Must-Fix (High)

- [x] [Review][Patch] `PathsEqual` `EndsWith` fallback allows wrong-baseline-trusted via suffix match `[Drift/DriftDetection.cs:823-828]` — configured `baseline.json` matches any AdditionalText ending in `baseline.json`. Use segment-aligned suffix or strict equality on normalized repo-relative path.
- [x] [Review][Patch] `_oversizedHint` test sentinel wired into production trust path `[Drift/DriftDetection.cs:567-577]` — production loader rejects baselines containing the substring `_oversizedHint`; remove the sentinel and drive the oversized branch by `MaxBaselineBytes` only.
- [x] [Review][Patch] `hasContracts` gate suppresses HFC1058 and drops removed-declaration drift `[FrontComposerGenerator.cs ~ drift orchestration]` — AC2 (property removal) cannot fire when source becomes empty; missing-baseline diagnostic also swallowed. Don't gate on `hasContracts`; let comparison run so removed declarations surface, and gate only on `options.Enabled`.
- [x] [Review][Patch] Drift load+comparison fires whenever any JSON `AdditionalText` is present, not gated on `HfcDriftDetectionEnabled` `[FrontComposerGenerator.cs / DriftBaselineLoader.Load]` — opt-in story is currently opt-out. Gate the entire baseline load+compare block on `optionsResult.Options.Enabled`.
- [x] [Review][Patch] `DriftBaselineInput.IsCandidate` accepts every `*.json` AdditionalText — T5 violation `[Drift/DriftDetection.cs IsCandidate]` — arbitrary JSON files become baselines and trigger HFC1060/HFC1064. Restrict to documented prefix (e.g. `frontcomposer.drift-baseline*.json`) or analyzer-config keyed inputs.
- [x] [Review][Patch] AC7 metadata coverage gap: missing drift classification for `[ProjectionBadge]`, `[RelativeTime]`, `[Currency]`, `[ProjectionEmptyStateCta]` `[Drift/DriftDetection.cs CompareProperty/CompareContractMetadata]` — AC7 enumerates these as required categories. Extend baseline+current property models and add `AddMetadataIfChanged` calls.
- [x] [Review][Patch] `AddIfChanged`-family asymmetry drops null→value transitions `[Drift/DriftDetection.cs:1108-1116, 1118-1130, 1170-1172]` — adding a `Destructive`/`Icon`/`Derivable`/Display flag to source against a null baseline is silently ignored. Treat null→non-null transitions as drift on both sides.
- [x] [Review][Patch] `HFC1059` (configured baseline path not found) forces `ComparisonEnabled=false` for the entire run `[Drift/DriftDetection.cs:548-552]` — Baseline Contract says "suppress downstream comparison for *that* baseline" only. Limit fail-closed scope to the configured baseline.

#### Patch — Should-Fix (Medium)

- [x] [Review][Patch] Oversize check uses `input.Text.Length` (chars) but option is named `MaxBaselineBytes` `[Drift/DriftDetection.cs:567-577]` — multi-byte UTF-8 baselines pass while exceeding the byte cap. Use `Encoding.UTF8.GetByteCount` or compare bytes.
- [x] [Review][Patch] UTF-8 BOM crashes `JsonDocument.Parse` → HFC1060 on a valid baseline `[Drift/DriftDetection.cs:592]` — strip BOM (`EF BB BF`) before parse or use `Utf8JsonReader` over bytes.
- [x] [Review][Patch] `schemaVersion`/`algorithm` parsing: trim+ordinal compare; non-string/whitespace/case variants currently mis-classified `[Drift/DriftDetection.cs:606-633]`; raw values also leak unsanitized into diagnostic property bag. Trim+strict-ordinal compare and sanitize before assigning to properties.
- [x] [Review][Patch] Property name case-only duplicates throw `ArgumentException` in `ToDictionary(Ordinal)` `[Drift/DriftDetection.cs:1055-1056]` — pre-validate uniqueness or wrap with try/catch and emit invariant diagnostic.
- [x] [Review][Defer] `AttributeParser` only orders properties when type is partial — Reverted to deferred. The Roslyn `GetMembers()` ordering is doc-soft but stable in current compiler versions; an unconditional sort breaks ~10 unrelated approval/declaration-order tests across the suite. Re-evaluate if a Roslyn upgrade ever produces a different order, and use the snapshot-side normalization (already applied via `BuildBadgeSignature` and per-property dictionaries) as the byte-stability backstop.
- [x] [Review][Patch] Drift orchestration combines `CompilationProvider` `[FrontComposerGenerator.cs drift hookup]` — invalidates on every compilation change, breaking AC10 incremental cache. Compilation is only used for `ToLocation`; resolve `Location` from precomputed file/line/column instead.
- [x] [Review][Patch] `DriftSanitizer.IsUnsafe` over-broad — `Contains("token", IgnoreCase)` etc. flags benign names `TokenStore`, `EtagPolicy`, `UserCount`, `TenantStatus` `[Drift/DriftDetection.cs:1474-1490]`. Limit unsafe detection to value-shape patterns (UUID-like, base64 JWT, full path with drive); do not match against member names.
- [x] [Review][Patch] `NormalizePath` strips full path on any `:` `[Drift/DriftDetection.cs:1516-1528]` — POSIX absolute paths leak (no colon, no normalization), and benign colon-bearing strings get truncated. Use a robust path normalization (project-relative trim) and don't conflate normalization with redaction.
- [x] [Review][Patch] `SHA256.Create()` allocated and disposed per fact `[Drift/DriftDetection.cs:1456-1469]` — generator runs on every keystroke; up to ~500 hashes per pass. Cache one thread-safe instance for the pipeline pass.
- [x] [Review][Patch] Hash inputs miss `SchemaVersion`+`AlgorithmVersion`; null-baseline collisions `[Drift/DriftDetection.cs:1329-1330, 1370-1371]` — `null + "|" + ...` and `"" + "|" + ...` collide. Include `<none>` markers and schema/algorithm in hash material.
- [x] [Review][Patch] FieldGroup change emits two diagnostics for one source change `[Drift/DriftDetection.cs:1164-1167]` — `ProjectionFieldGroup` + `Display.GroupName` both fire; inflates diagnostic count toward 50-cap. Emit single combined diagnostic.
- [x] [Review][Patch] `LinePosition` fallback in `ToLocation` embeds absolute path into `Location` `[Drift/DriftDetection.cs:1432-1454]` — bypasses sanitizer; absolute drive path appears in IDE diagnostics. Run `SourcePath` through `DriftSanitizer.NormalizePath` before `Location.Create`. Also use `OrdinalIgnoreCase` for Windows tree.FilePath compare.
- [x] [Review][Patch] Command drift snapshot drops source location `[Drift/DriftDetection.cs:962-964]` — passes `string.Empty, -1, -1` while projection branch threads real `SourceFilePath/Line/Column`. IDE squiggles never appear on commands. Thread `CommandModel.SourceFilePath/Line/Column` through.

#### Patch — Cleanup (Low)

- [x] [Review][Patch] Hardcoded literal `"HFC1058"` in HFC1058 gate `[FrontComposerGenerator.cs ~1602-1610]` — use `DriftConstants.MissingBaselineId` to avoid silent breakage on rename.
- [x] [Review][Patch] `TryReadString` returns `""` for empty values, `null` for missing key `[Drift/DriftDetection.cs:442-447]` — empty `<HfcDriftBaselinePath/>` becomes "configured" and triggers HFC1059. Return `null` when trimmed string is empty.
- [x] [Review][Patch] Numeric option parsing uses `NumberStyles.None` `[Drift/DriftDetection.cs:412]` — rejects `+50` and signed values; use `NumberStyles.Integer` plus `parsed >= 0` guard. Also replace `key.Split('.').Last()` with `LastIndexOf` to avoid array allocation.
- [x] [Review][Patch] Unused `Hexalith.FrontComposer.Schema` project reference `[SourceTools.csproj]` — no `using Hexalith.FrontComposer.Schema` in SourceTools sources. Remove unless Chunk B/C consumes it.
- [x] [Review][Patch] `DriftComparisonService.Compare` and `DriftComparisonResult.Diagnostics` use `public` modifier on internal types — Comparison Seam mandates "no public API shape". Tighten to `internal` to prevent later surface drift.
- [x] [Review][Patch] `SafeMessage` token list misses normalized variants `[Drift/DriftDetection.cs:1492-1500]` — paths normalized to forward-slash bypass `C:\\` token. Broaden to include `C:/`, `Bearer`, `Authorization`, JWT shape.
- [x] [Review][Patch] When `trustFailed`, `DriftBaselineLoader.Load` still returns populated `contracts` array `[Drift/DriftDetection.cs:566-682]` — defensive: return `DriftBaselineSet(ImmutableArray.Empty)` to make fail-closed self-evident.

#### Deferred

- [x] [Review][Defer] SourceText-based incremental cache equality optimization for `DriftBaselineInput` — perf optimization; current string-content equality is correct but allocates more.
- [x] [Review][Defer] `HfcDriftSeverity` descriptor-cloning vs `dotnet_diagnostic.HFCxxxx.severity` editorconfig — design refactor; current implementation works but is non-idiomatic.
- [x] [Review][Defer] Diagnostic `MessageFormat = "{0}"` parameterization — AC12 explicitly allows "structured equivalent"; properties bag covers structure.
- [x] [Review][Defer] Contract-level `[Description]` field not modeled in baseline — minor AC7 gap; only property-level `Description` is compared today.
- [x] [Review][Defer] Cross-file duplicate origin path tracking detail — diagnostic detail improvement (which file caused the duplicate).
- [x] [Review][Defer] Truncation `sortKey: "~~~~|Truncation"` is misleading — code clarity nit.
- [x] [Review][Defer] Load-phase diagnostic flooding cap — hostile baselines could flood with HFC1059-HFC1064; cap separately.
- [x] [Review][Defer] Dictionary allocation cost in `CompareContract` — micro-perf optimization; sorted-merge walk would avoid four allocations per contract.
- [x] [Review][Defer] `DriftBaselineInput.GetHashCode` uses non-deterministic `string.GetHashCode` — incremental cache equality uses `Equals`; minor.
- [x] [Review][Defer] HFC1069 redaction precedence position not documented in spec table — owned by Story 9-4 governance.
- [x] [Review][Defer] `DomainModel` cast skip silently in snapshot `From` — edge case, may hide drift on unparsable projections.

### Review Findings — Chunk B (Validation Tests)

> Source: `/bmad-code-review` adversarial pass. Range: uncommitted diff scoped to `tests/Hexalith.FrontComposer.SourceTools.Tests/CompilationHelper.cs`, `tests/.../Drift/Baseline/`, `tests/.../Drift/Comparison/`, `tests/.../Drift/Diagnostics/`. Excludes `tests/.../Drift/Regression/` (Chunk C scope) and `src/` (Chunk A done). Reviewers: Blind Hunter, Edge Case Hunter, Acceptance Auditor.

#### Patch — Critical / High

- [x] [Review][Patch] Reflection-based `Compare` lookup is brittle — `OrderBy(parameters.Length).FirstOrDefault()` plus arity-2 filter `[DriftComparisonServiceTests.cs:288-305, ~124-127]`. If production `Compare` signature has a third arg (options/severity/cancellation), the test silently throws "Compare method missing" rather than asserting determinism/order-independence. Pin to the exact signature (`typeof(DriftComparisonService).GetMethod("Compare", new[] { typeof(DriftCurrentSnapshot), typeof(DriftBaselineSet) })`) or use direct invocation. Also remove the brittle `type.IsAbstract ? null! : Activator.CreateInstance(type)!` pattern at line 300. Sources: blind+edge.
- [x] [Review][Patch] Order-independence helpers ignore `reverse` argument `[DriftComparisonServiceTests.cs:115-116]` — `PartialPermutationDomain(reverse) => SampleDomain` and `PermutedBaseline(reverse) => SampleBaseline` return the same instance regardless. Partial-declaration order independence and baseline-file order independence tests therefore compare an object to itself — tautologies. AC18 unverified. Implement actual reversal of partial declarations / baseline file ordering. Sources: edge+auditor.
- [x] [Review][Patch] Duplicate-identity test type-name mismatch `[DriftBaselineTrustFailureTests.cs:91, 104, 108]` — fixture declares `Acme.Shipping.ShipmentProjection` while compiled source class is `TestDomain.ShipmentProjection`; assertion matches `Acme.*` so the test only exercises the duplicate-without-real-declaration branch. The realistic "duplicate poisons a real declaration" scenario is uncovered. Align fixture type identity to the compiled source. Source: edge.
- [x] [Review][Patch] Diagnostic-location assertion weakened to `EndsWith(".cs")` `[DriftDiagnosticContractTests.cs:103-114, 382-387]` — after P12 (decoupling from CompilationProvider), the test was relaxed from `Location.SourceTree.FilePath` to `Location.GetMappedLineSpan().Path`. A path of `"baseline-stub.cs"` or any `.cs` literal sentinel passes. Strengthen: assert path equals (or ends with) the actual source filename and `LineSpan.StartLinePosition` references the declaration position. Sources: blind+auditor.
- [x] [Review][Patch] AC4 rename message asserts only fragments `[DriftClassifierRenameTests.cs:50-53]` — substring asserts on `OldName`, `NewName`, `TypeName`, etc. independently. A regression dropping single quotes or reordering clauses in the AC4 wording shape (`Property '{OldName}' was expected on {TypeName} but not found. '{NewName}' was added. If this is a rename, ...`) would still pass. Replace with anchored regex matching the full sentence shape. Source: auditor.
- [x] [Review][Patch] AC12 "What:" label not asserted in message-shape test `[DriftDiagnosticContractTests.cs:78-83]` — only Expected/Got/Fix/DocsLink are asserted. Production templates emit "What:" too; add the assertion. Source: auditor.
- [x] [Review][Patch] AC7 metadata theory misses six attribute categories patched in Chunk A `[DriftClassifierMetadataTests.cs:19-25]` — **partially applied.** Added new Fact tests for `RelativeTime` (DateTimeOffset property surface) and `RequiresPolicy` (command contract surface). `ProjectionBadge` (requires `BadgeSlot` enum-arg syntax + interplay with HFC1025 partial-coverage diagnostic) and `Destructive` (command bool→bool? mapping) were attempted during application but did not land — promoted to `DEF-9-1B-5`/`DEF-9-1B-6`. `ProjectionRole`, `Currency`, `ProjectionEmptyStateCta`, `Icon` remain coverage gaps and are tracked as `DEF-9-1B-7`. Source: auditor.
- [x] [Review][Patch] AC3 + T7 matrix: command field add and command field remove uncovered `[Drift/Comparison directory, no file]` — only command type-change is exercised in `DriftClassifierTypeAndNullabilityTests.cs:107-131`. Add command-property add and command-property remove tests. Source: auditor.
- [x] [Review][Patch] AC16 sort key only varies `MemberName` `[DriftDiagnosticOrderingAndTruncationTests.cs:73-95]` — bounded context, declaration kind, declaration name, and drift kind dimensions are constant in the scenario. Build a scenario varying all five dimensions and assert deterministic ordinal sort. Source: auditor.
- [x] [Review][Patch] HFC1059 configured-baseline-path branch entirely uncovered `[DriftDiagnosticPrecedenceTests.cs / DriftBaselineTrustFailureTests.cs]` — no test sets `build_property.HfcDriftBaselinePath`. Branches uncovered: configured path with no matching AdditionalText (HFC1059), segment-aligned suffix match, whitespace-only path (P24), backslash-vs-forward-slash normalization. Source: edge.
- [x] [Review][Patch] Oversized cap boundary uncovered `[DriftBaselineTrustFailureTests.cs:50-52, DriftDiagnosticPrecedenceTests.cs:48-50]` — tests use `MaxBaselineBytes=100` arbitrary. Missing: exact `MaxBaselineBytes` boundary (off-by-one — `>` strict so equal is allowed; not pinned), and multi-byte UTF-8 fixture proving P10 byte-vs-char distinction. Source: edge.
- [x] [Review][Patch] `HfcDriftMax*` empty/whitespace removed without positive replacement `[DriftAnalyzerConfigOptionsTests.cs:24-26, 58-62]` — `[InlineData("HfcDriftMaxDiagnostics", "")]` and `[InlineData("HfcDriftMaxBaselineBytes", "")]` were deleted citing P24 ("empty=unset"). Add positive test asserting empty/whitespace yields no configuration diagnostic and the default cap applies. Sources: blind+edge+auditor.
- [x] [Review][Patch] `HfcDriftSeverity` case-insensitivity / aliases untested `[DriftAnalyzerConfigOptionsTests.cs:29]` — only `"Catastrophic"` (invalid) tested. Add positive theory entries for `"warning"`, `"ERROR"`, `"Info"`, `"Information"`, plus surrounding-whitespace forms. Source: edge.
- [x] [Review][Patch] `ReadPositiveInt` boundary cases untested `[DriftAnalyzerConfigOptionsTests.cs:22-29]` — missing `1` (min boundary), `500` (max boundary), `501` (max+1), `+50` (P25 leading-sign fix), `2147483648` (Int32 overflow). Source: edge.
- [x] [Review][Patch] `HfcDriftDetectionEnabled` opt-in coverage thin `[multiple test files]` — only `"true"` tested; missing `"True"`, `"TRUE"` (case-insensitivity), `"false"` (negative path), garbage values like `"yes"`, and the legacy `FrontComposerDriftDetectionEnabled` fallback chain. Sources: blind+edge.

#### Patch — Should-Fix (Medium)

- [x] [Review][Patch] AC13 redaction sentinels seeded only into source attributes `[DriftDiagnosticRedactionTests.cs:22-32, 39-49]` — sentinels are placed in `[Display]` / `[BoundedContext]` source attributes. Baseline JSON `displayName`/`description`/`displayFormat` fields are not seeded with sentinels. AC13 enumerates baseline metadata as a redaction surface. Also extend sentinel list to include zero-width chars (U+200B, U+202E), control chars, JWT-shaped tokens. Sources: edge+auditor.
- [x] [Review][Patch] `MultipleCategoriesChangedOnSameDeclaration` uses sloppy `[4,8]` bound `[DriftClassifierMetadataTests.cs:48-51, 82-83]` — AC7 says "≤1 per category". With 4 categories changed, count must equal exactly 4. Tighten upper bound to 4. Sources: edge+auditor.
- [x] [Review][Patch] Type-change theory missing transitions `[DriftClassifierTypeAndNullabilityTests.cs:21-25]` — only primitive↔primitive covered. Add scalar→collection (`string`→`IReadOnlyList<string>`), enum→string, value-type→reference-type, generic-arg changes (`IList<int>`→`IList<string>`). Source: edge.
- [x] [Review][Patch] Nullability-hint regex not anchored `[DriftClassifierTypeAndNullabilityTests.cs:103]` — `(required|breaking|tight)` matches substrings inside unrelated tokens (e.g., a docslink containing `"required"`). Anchor with word boundaries. Source: edge.
- [x] [Review][Patch] Truncation off-by-one boundaries (49, 50, 51) not exercised `[DriftDiagnosticOrderingAndTruncationTests.cs:39-50]` — tests cover 60 and 70 only. Add theory cases at cap-1 / cap / cap+1 to pin the `Take(50)` and omittedCount math. Source: edge.
- [x] [Review][Patch] Truncation summary uniqueness uses `"truncat"` substring match `[DriftDiagnosticOrderingAndTruncationTests.cs:49-50]` — could match incidental occurrences in docslinks/fix-text. Pin to HFC ID `HFC1068` instead. Source: edge.
- [x] [Review][Patch] AC16 truncation summary precedence not pinned `[DriftDiagnosticOrderingAndTruncationTests.cs:39-51]` — assert HFC ID, severity Warning, and that the summary appears after the 50 emitted diagnostics (not interleaved). Source: auditor.
- [x] [Review][Patch] Redaction test does not assert original suppression `[DriftDiagnosticRedactionTests.cs:67-86]` — asserts fallback exists but does not assert the would-have-leaked diagnostic is absent. Spec precedence row 11 says redaction failure SUPPRESSES the original. Add `diagnostics.ShouldNotContain(d => d.Id == "HFC*offending*")` style check. Source: edge.
- [x] [Review][Patch] `HfcDriftMaxDiagnostics`=1 boundary untested — single-cap case (1 emitted + 1 truncation summary for ≥2 drifts) is unverified. Production min=1, so this is a real boundary. Source: edge.
- [x] [Review][Patch] UTF-8 BOM (P11) not exercised in any Chunk B test — add a baseline fixture with `﻿` prefix asserting it parses as a normal baseline (no HFC1060). Source: edge.
- [x] [Review][Patch] Property-name case-collision invariant (P13) not covered — fixture with both `"Foo"` and `"foo"` properties on one contract should produce HFC1064/invariant diagnostic. Source: edge.
- [x] [Review][Patch] `InMemoryAnalyzerConfigOptions.TryGetValue` returns `string.Empty` instead of `null` on miss `[CompilationHelper.cs:38-46, DriftBaselineTrustFailureTests.cs:182-198]` — diverges from real Roslyn `AnalyzerConfigOptions` contract (default `null`). Initialize `value = null!` then assign only on hit. Sources: blind+edge.
- [x] [Review][Patch] Duplicate `InMemoryDriftOptionsProvider`/`InMemoryOptions` between `CompilationHelper.cs:32-48` and `DriftBaselineTrustFailureTests.cs:182-198` — consolidate to use the helper. Source: blind.
- [x] [Review][Patch] BoundedContext rename test never exercises empty/whitespace boundedContext `[DriftClassifierBoundedContextTests.cs]` — production reads with `?? string.Empty`; add a case where source removes `[BoundedContext]` (rename "Sales" → `""`) and assert diagnostic still emits with the `Got` value sanitized. Source: edge.
- [x] [Review][Patch] Rename heuristic compatibility boundaries untested `[DriftClassifierRenameTests.cs:87-110]` — only `string`→enum incompatibility tested. Add `Int32`→`Int64`, `DateTime`→`DateTimeOffset`, `String`→`String?` to pin where rename heuristic stops collapsing add+remove pairs. Source: edge.
- [x] [Review][Patch] AC9 `DuplicateIdentityAcrossFiles_OrderAgnostic` doesn't assert no-last-writer-wins `[DriftBaselineTrustFailureTests.cs:117-135]` — both orderings produce empty contract drift due to P29 EmptyResult fail-closed. Add assertion: duplicate-id diagnostic IS PRESENT in BOTH orderings AND no contract drift leaks (the comparison was actually suppressed, not silently merged). Source: auditor.
- [x] [Review][Patch] AC8 missing-vs-invalid-path test does not assert distinct HFC IDs `[DriftBaselineMissingDiagnosticTests.cs:60-65]` — AC8 says the two cases MUST carry different IDs. Add `missing.Id != invalid.Id` assertion. Note: target file is in chunk B test scope but not currently in the diff — modify pre-existing test. Source: auditor.
- [x] [Review][Patch] AC9 fall-back-to-defaults test asserts only "still working" `[DriftAnalyzerConfigOptionsTests.cs:69-75]` — strengthen to assert the bad-option configuration diagnostic AND the documented default value applies (e.g., `MaxDiagnostics=DriftConstants.DefaultMaxDiagnostics`). Source: auditor.
- [x] [Review][Patch] AC9 invalid-option diagnostic does not pin HFC ID `[DriftAnalyzerConfigOptionsTests.cs:41-43]` — substring-only match. Pin to the catalog ID for configuration diagnostics. Source: auditor.

#### Patch — Cleanup (Low)

- [x] [Review][Patch] AC2 surface-label match passes on incidental `column` substring `[DriftClassifierProjectionPropertyTests.cs:59-61]` — `Any`-match against `{DataGrid, detail, MCP, filter, column}`. Tighten: require ≥2 distinct labels OR pin to expected label set per test case. Source: auditor.
- [x] [Review][Patch] AC6 BoundedContext rename does not assert severity is Warning `[DriftClassifierBoundedContextTests.cs:46-58]` — AC6 inherits AC2/AC5 default-Warning severity contract; a regression to Hidden would pass. Source: auditor.
- [x] [Review][Patch] `RunWithOption` does not set `HfcDriftDetectionEnabled=true` `[DriftAnalyzerConfigOptionsTests.cs:106-114]` — config validation fires before the gate today, but a future Enabled-gate refactor moving option validation behind the gate would silently break the test. Source: auditor.
- [x] [Review][Patch] `Result_IsDeterministicForIdenticalInputs` compares via `f.ToString()` `[DriftComparisonServiceTests.cs:59]` — if `DriftDiagnosticFact` lacks `ToString` override, comparison is trivial. Compare structurally on Id+Message+Properties. Source: edge.

#### Deferred

- [x] [Review][Defer] Order-dependence test concatenation `Id+"|"+Message` could mask non-Id/Message diff `[DriftBaselineTrustFailureTests.cs:136-138]` — pre-existing pattern; reinforced but not introduced by this diff. Source: blind.
- [x] [Review][Defer] `baselinePath.ShouldNotContain(":")` over-eager — would reject paths with legitimate colons `[DriftDiagnosticContractTests.cs:93]`. Edge case, low priority. Source: edge.
- [x] [Review][Defer] `StripUtf8Bom` boundary at text length 1-2 — unreachable in practice (1-byte BOM-only baseline). Already adjacent to deferred-list candidates. Source: edge.
- [x] [Review][Defer] `maxBaselineBytesOverride` vs `maxBytes` parameter naming inconsistency across `DriftBaselineTrustFailureTests.cs:142,171` and `DriftDiagnosticPrecedenceTests.cs:441,446` — pure cosmetic. Source: blind.

#### Dismissed

- [Dismissed] `StringComparison.Ordinal` for fixture filename equality (repo-controlled fixtures, casing fixed at write time).
- [Dismissed] Inline `System.Globalization.CultureInfo.InvariantCulture` long-form (style nit, no behavioral risk).
- [Dismissed] Filename-based feature flag observation (restating P5 production design, not a test issue).
- [Dismissed] Named-argument refactor in `DriftComparisonServiceTests.cs:317-335` (defensive readability, no risk).

