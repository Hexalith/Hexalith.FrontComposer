---
title: 'Repair CI and Quality failures after PR 101'
type: 'bugfix'
created: '2026-08-29'
status: 'done'
baseline_commit: '0d2fdc8ae633f503d8be90da254d4ae7e19ea5a5'
review_loop_iteration: 0
context: []
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** Push runs 33237411829 and 33237411614 fail before their substantive validation completes: package validation assumes a cached 4.1.1 baseline, the merged gitlinks contradict the Story 11.24 approved runtime tuple and select an incompatible xUnit/MTP catalog, and Windows cannot check out the newly preserved long evidence paths.

**Approach:** Restore the approved Builds/EventStore gitlinks, make the release packer restore validation baselines deterministically before no-build packing, and enable Git long-path handling only on the Windows accessibility checkout. Add regression tests around each locally enforceable invariant.

## Boundaries & Constraints

**Always:** Preserve the user's existing DW-1137 specification edit; retain package validation against published baseline 4.1.1; keep the eight-package inventory and Release/NuGet dependency mode; keep the Story 11.24 evidence byte-identical; initialize or move only root-declared submodules; use the existing VSTest/coverlet command contract selected by approved Builds commit `a8a50859fa2f27f511a9470dfe1e3ae54d0ebc1a`.

**Ask First:** Stop if either approved submodule commit is unavailable, dependency policy rejects the approved tuple, or a passing repair would require changing preserved evidence bytes, weakening a gate, changing the approved runtime identity, or modifying submodule contents.

**Never:** Do not disable package/API compatibility validation, rely on NuGet cache warmth, skip tests, rename or reseal the evidence tree, migrate the repository to Microsoft.Testing.Platform, initialize nested submodules, edit files inside `references/`, or include the unrelated DW-1137 work.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|---------------|----------------------------|----------------|
| Cold package cache | Baseline 4.1.1 is absent before synthetic CI packing | Validation-aware solution restore runs before all eight `--no-build` packs | Restore failure exits nonzero before deleting existing package outputs |
| Approved runtime identity | Root gitlinks select the post-approval latest commits | Builds and EventStore resolve exactly to `a8a50859…` and `bb94d93e…`; catalog remains EventStore 3.91.1/xUnit 3.2.2 | Governance and dependency-policy checks fail closed on drift |
| Windows evidence checkout | Runner path plus SHA-heavy evidence names exceeds legacy MAX_PATH | Checkout receives step-scoped `core.longpaths=true` and completes | Later a11y artifact failures remain downstream failures, not masked successes |

</frozen-after-approval>

## Code Map

- `references/Hexalith.Builds` and `references/Hexalith.EventStore` -- root gitlinks; `CiGovernanceTests.EventStoreRuntimeIdentityPinsOwnerApprovedTupleAndTruthfulDriftEvidence` requires Builds `a8a50859…`, EventStore `bb94d93e…`, and version 3.91.1. Do not edit submodule content.
- `scripts/pack-release-packages.py:99-177` -- `pack_commands()` uses validation properties with `--no-build`; `main()` must run the equivalent validation-aware restore before cleanup and packing.
- `eng/release_prepublish.py:185-207` -- existing golden restore pattern using `release_properties(version)`; reuse its invariant rather than inventing another property set.
- `tests/eng/test_pack_release_packages.py:35-130,279-391` -- command-plan and fail-before-output-mutation coverage for the packer.
- `.github/workflows/quality.yml:456-475` -- Windows `accessibility-visual` checkout that currently lacks `core.longpaths`; keep configuration scoped to that action.
- `tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs:252-289` -- pins the accessibility job and its scoped Git/submodule safety contract; extend it to guard long-path checkout.
- `_bmad-output/implementation-artifacts/evidence/frontcomposer-story-11-24/**` -- read-only captured evidence whose two acceptance paths triggered MAX_PATH; never rename or rewrite it.

## File List

- `.github/workflows/quality.yml`
- `_bmad-output/implementation-artifacts/spec-actions-33237411829-33237411614-fix-cicd.md`
- `references/Hexalith.Builds`
- `references/Hexalith.EventStore`
- `scripts/pack-release-packages.py`
- `tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs`
- `tests/eng/test_pack_release_packages.py`

## Commit Scope Dispositions

- `171bfce89ba042f3cdd03dd182808d7c95389df6` | `shared` | Concurrent user-owned deferred-work sweep and DW-1137 implementation landed while this CI repair workflow was active.

## Documented Unrelated Changes

- `_bmad-output/contracts/analyzer-policy-exception-ledger-v1.json` - Concurrent user-owned DW-1137 identifier-inventory state; the CI governance edit adds no underscore identifier.
- `_bmad-output/implementation-artifacts/deferred-work.md` - Concurrent user-owned deferred-work sweep.
- `_bmad-output/implementation-artifacts/spec-dw-1137-try-set-property-value-compile-time-switch-refactor.md` - Pre-existing and concurrent user-owned DW-1137 work.
- `docs/diagnostics/HFC1016.md` - Concurrent user-owned DW-1137 work.
- `docs/diagnostics/diagnostic-registry.json` - Concurrent user-owned DW-1137 work.
- `docs/validation/producer-fingerprints.json` - Concurrent user-owned DW-1137 work.
- `src/Hexalith.FrontComposer.SourceTools/AnalyzerReleases.Unshipped.md` - Concurrent user-owned DW-1137 work.
- `src/Hexalith.FrontComposer.SourceTools/Diagnostics/DiagnosticDescriptors.cs` - Concurrent user-owned DW-1137 work.
- `src/Hexalith.FrontComposer.SourceTools/Emitters/CommandRendererEmitter.cs` - Concurrent user-owned DW-1137 work.
- `src/Hexalith.FrontComposer.SourceTools/Parsing/AttributeParser.cs` - Concurrent user-owned DW-1137 work.
- `src/Hexalith.FrontComposer.SourceTools/Parsing/CommandParser.cs` - Concurrent user-owned DW-1137 work.
- `src/Hexalith.FrontComposer.SourceTools/Parsing/DomainModel.cs` - Concurrent user-owned DW-1137 work.
- `src/Hexalith.FrontComposer.SourceTools/Transforms/CommandRendererModel.cs` - Concurrent user-owned DW-1137 work.
- `src/Hexalith.FrontComposer.SourceTools/Transforms/CommandRendererTransform.cs` - Concurrent user-owned DW-1137 work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/CommandFormEmitterTests.cs` - Concurrent user-owned DW-1137 work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/CommandRendererEmitterTests.Renderer_FiveFields_FullPageBoundarySnapshot.verified.txt` - Concurrent user-owned DW-1137 snapshot work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/CommandRendererEmitterTests.Renderer_FourFields_CompactInlineBoundarySnapshot.verified.txt` - Concurrent user-owned DW-1137 snapshot work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/CommandRendererEmitterTests.Renderer_OneField_InlinePopoverSnapshot.verified.txt` - Concurrent user-owned DW-1137 snapshot work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/CommandRendererEmitterTests.Renderer_OneField_WithIconAttributeSnapshot.verified.txt` - Concurrent user-owned DW-1137 snapshot work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/CommandRendererEmitterTests.Renderer_OneField_WithoutIconUsesDefaultSnapshot.verified.txt` - Concurrent user-owned DW-1137 snapshot work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/CommandRendererEmitterTests.Renderer_TwoFields_CompactInlineSnapshot.verified.txt` - Concurrent user-owned DW-1137 snapshot work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/CommandRendererEmitterTests.Renderer_ZeroFields_InlineSnapshot.verified.txt` - Concurrent user-owned DW-1137 snapshot work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Emitters/CommandRendererEmitterTests.cs` - Concurrent user-owned DW-1137 work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Incremental/DomainModelCacheEqualityTests.cs` - Concurrent user-owned DW-1137 work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Integration/GeneratorDriverTests.cs` - Concurrent user-owned DW-1137 work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Parsing/CommandParserTests.cs` - Concurrent user-owned DW-1137 work.
- `tests/Hexalith.FrontComposer.SourceTools.Tests/Parsing/TestFixtures/CommandTestSources.cs` - Concurrent user-owned DW-1137 work.

## Tasks & Acceptance

**Execution:**
- [x] `references/Hexalith.Builds`, `references/Hexalith.EventStore` -- restore the owner-approved gitlinks without touching submodule files.
- [x] `scripts/pack-release-packages.py`, `tests/eng/test_pack_release_packages.py` -- add a planned validation-aware restore before output cleanup/packing and cover command properties, ordering, and restore-failure preservation.
- [x] `.github/workflows/quality.yml`, `tests/Hexalith.FrontComposer.Shell.Tests/Governance/CiGovernanceTests.cs` -- inject action-scoped Git config for Windows long paths and pin that behavior without permitting global mutation.

**Acceptance Criteria:**
- Given a cold baseline cache, when the CI packer runs, then restore receives the same version and compatibility properties as every pack before package outputs are mutated.
- Given the Story 11.24 approved tuple, when governance and semantic dependency checks inspect HEAD, then both gitlinks and the selected EventStore 3.91.1 catalog match exactly.
- Given the Windows accessibility job, when checkout creates the preserved evidence tree, then Git long paths are enabled only for checkout and all substantive browser steps remain blocking.
- Given focused and Release validation, when the repair is tested, then packer tests, evidence tests, dependency policy, governance tests, solution build, and synthetic package packing pass without changing the user's DW-1137 file.

## Spec Change Log

## Review Triage Log

Review pass 1 (2026-09-07). Layers: blind-hunter, edge-case-hunter, verification-gap.
Diff reviewed: commit `66c5a17104a9d58a4e6933705709d688854bc48a` restricted to the four
implementation paths. `baseline_commit` is 172 commits behind `main`, so a
`baseline..HEAD` diff would have swept in unrelated work; the DW-1137 paths already listed
under Documented Unrelated Changes were excluded.

| # | Finding (layer) | Verdict | Evidence | Route |
|---|-----------------|---------|----------|-------|
| 1 | Restore's validation properties are pinned only through the positional slice `restore[4:-2]`, which goes vacuous if `release_properties(version)` is dropped (verification-gap; also blind-hunter, edge-case) | medium | Confirmed. `restore` is 12 elements, so `[4:-2]` selects the six properties today; deleting the splat makes the slice empty and the loop body never runs, while `restore[:2]`, `restore[2]` and `-p:Configuration=Release` all still hold. The exact cold-cache regression this change exists to prevent would ship green. | patch |
| 2 | `check=True` and `cwd=REPO_ROOT` on the restore call are never observed by the tests named for them (verification-gap; also blind-hunter x2, and the unbound `--plan` restoreCommand) | medium | Confirmed. `observe_run(command, **_kwargs)` discarded every kwarg and asserted only `observed_commands[0][:2]`; `test_restore_failure_precedes_package_output_cleanup` mocks a mock that raises unconditionally, proving only that a raising callable propagates. A reviewer removed `check=True` and all 27 tests still passed. Without it a failed restore deletes existing `*.nupkg`/`*.snupkg` before erroring at pack time. | patch |
| 3 | Restore failure is reported as `Package packing failed with exit code N` (blind-hunter) | low | Confirmed. `__main__` catches `CalledProcessError` and prints only the message; the failing argv is never shown, so the cold-cache restore failure this change targets is indistinguishable from a pack failure in the CI log. | patch |
| 4 | Block comment claims the validation-aware restore is "above" the package-output mutation, but the call sits below the comment (blind-hunter) | low | Confirmed at `scripts/pack-release-packages.py`: only policy/SemVer/inventory validation precede the comment; `subprocess.run(restore, ...)` follows it. | patch |
| 5 | The per-step `continue-on-error` loop in `CiGovernanceTests` is vacuous, subsumed by the job-wide guard (blind-hunter, edge-case "claim falsified", verification-gap other) | false | Refuted twice. (a) At `66c5a171` no job-wide guard existed — `a11yJob.ShouldNotContain("continue-on-error:")` was introduced later by `2ee20c9f`, so the loop added real coverage when written. (b) Even today the loop is not zero-coverage: `ExtractNamedStep` hard-fails if any pinned step is renamed or removed, which the job-wide string check does not do. Later work also added a fifth name to the loop. | rejected |
| 6 | The `a11yJob.Replace(checkoutStep, "").ShouldNotContain("core.longpaths")` guard blocks the natural fix of extending long paths to `Initialize build submodules` (edge-case, blind-hunter, verification-gap other) | false | Refuted. The spec's own acceptance criterion is that long paths are enabled *only* for checkout; the assertion enforces exactly that contract. The submodule step deliberately skips EventStore to stay under MAX_PATH (its own comment says so) and Builds paths measure ~134 chars, so no live failure exists. A guard that fails closed on a state never shown reachable is correct behavior, not a defect. | rejected |
| 7 | No test asserts `runs-on: windows-latest` for `accessibility-visual`, so the env block could become dead config (blind-hunter, edge-case) | low | Real: no governance test pins the runner, and `accessibility-visual` is the only `windows-latest` job in `.github/workflows/`. Harm is dead configuration only, contingent on someone moving the sole Windows job; the fix adds a new guard rather than correcting anything. | rejected |
| 8 | `GIT_CONFIG_*` keys moved under `with:` instead of `env:` would still satisfy the pin (edge-case) | low | Real but unreachable in ordinary work: `checkoutStep` spans the whole step, so a misplacement would pass. Requires a deliberate refactor of a five-line block, and the fix adds an index-ordering assertion. | rejected |
| 9 | A `GIT_CONFIG_KEY_1` pair added while `GIT_CONFIG_COUNT` stays `1` would leave a dead config (edge-case) | low | Same shape as #8 — hypothetical future edit, fix adds a new assertion rather than correcting present code. | rejected |
| 10 | `continue-on-error` written as an expression or `True` would evade the per-step pin (edge-case) | false | Refuted: the job-wide `a11yJob.ShouldNotContain("continue-on-error:")` matches the key alone, so every value form is already forbidden anywhere in the job. | rejected |
| 11 | No existence check on `SOLUTION_PATH` before restoring (edge-case) | low | A missing solution produces an MSBuild error and a non-zero exit; the fix adds a guard for a state never shown reachable. | rejected |
| 12 | A packable inventory project absent from the slnx Release restore graph would restore silently and fail at pack (edge-case) | low | The Release configuration is the package-boundary set that already had to build `-warnaserror` before packing, and `release_prepublish.phase_build` restores the same solution; a missing project fails loudly at pack. Fix adds a cross-check for an undemonstrated state. | rejected |
| 13 | No timeout on the restore subprocess (edge-case) | low | Pre-existing pattern: no `subprocess.run` in this script carries a timeout, and the job carries `timeout-minutes`. Not caused by this change. | rejected |
| 14 | `schemaVersion` stayed `"1.0"` when `restoreCommand` was added to the plan payload (edge-case) | low | The packer plan has no consumer outside `tests/eng/test_pack_release_packages.py`; an additive key under a stable version is conventional and breaks nothing. | rejected |
| 15 | The restore duplicates the one `release_prepublish.phase_build` already runs, with no `--no-restore` escape hatch (blind-hunter) | low | Confirmed as duplication (`eng/release_prepublish.py:198-200` runs the same command minus `/m:1 /nr:false`), but it is load-bearing: the shared-CI lane reaches the packer without that restore, which is the whole point of the change. Extracting a shared helper adds surface for a developer-only drift risk. | rejected |
| 16 | The Gate 1 restore in `quality.yml` carries a different property set and is not in lockstep with `restore_command` (blind-hunter) | low | Different lanes with different jobs: Gate 1 warms validation for the build, the packer resolves baselines for `--no-build` packing. `PackageInventory_IsExplicitLockstepAndReviewable` pins each separately. No divergence harm named. | rejected |
| 17 | Whole-solution restore imports unrelated restore failures (e.g. NU1102) into the packing step (blind-hunter) | low | The Release configuration is already the narrow package-boundary set, not the full Debug source graph, and `phase_build` restores that same solution immediately before packing on the release path. Blast radius is unchanged. | rejected |
| 18 | No guard caps tracked path length, so the next content-addressed evidence tree re-breaks the Windows checkout (blind-hunter) | medium | Real and confirmed: longest tracked path is 212 chars, ~3 over MAX_PATH once prefixed with the runner workspace. This is a new preventive guard, not a defect in the change, which fixed the symptom exactly as specified. | defer |
| 19 | `--plan` mode is not asserted to skip the restore side effect (edge-case) | low | The plan branch returns before the restore call; a regression would be caught by the ordering test's command count. Fix adds a new test for an undemonstrated state. | rejected |

**Applied patches:** entries 1-4, all in `scripts/pack-release-packages.py` and `tests/eng/test_pack_release_packages.py`. Mutation-verified: dropping `release_properties(version)` from `restore_command`, dropping `check=True`, and dropping `cwd=REPO_ROOT` each now fail the suite (all three passed silently before). 27/27 packer tests and 40/40 packer + prepublish tests green on the restored tree.

**Deferred:** entry 18, appended to `deferred-work.md`.

## Design Notes

Re-pinning the already approved runtime tuple is intentionally narrower than migrating every workflow, release script, reporter, trait filter, and coverage extension to MTP. It simultaneously restores the governance contract and removes the xUnit 4/MTP 2.3.3 regression introduced by the later Builds pointer.

## Verification

**Commands:**
- `python3 -m unittest tests/eng/test_pack_release_packages.py tests/eng/test_release_prepublish.py` -- 39/39 package lifecycle tests passed.
- `python3 -m unittest tests/eng/test_eventstore_runtime_evidence.py` -- 40/40 preserved-evidence tests passed.
- `python3 eng/dependency_graph.py validate --commit HEAD` -- post-commit verification command; the approved tuple passed semantic policy through an ephemeral Git tree because the validator reads committed trees only.
- `dotnet build Hexalith.FrontComposer.slnx --configuration Release` -- succeeded with 0 warnings and 0 errors.
- `DiffEngine_Disabled=true dotnet test Hexalith.FrontComposer.slnx --configuration Release --no-build --filter "Category=Governance"` -- 224/227 passed. Two failures are commit-bound because current `HEAD` still exposes the old gitlinks (`InfrastructureGovernanceTests.CentralPackageVersions_WhenCatalogIsCentralized_AreInheritedFromPinnedBuilds` and `CiGovernanceTests.EventStoreRuntimeIdentityPinsOwnerApprovedTupleAndTruthfulDriftEvidence`); the third is the separately documented concurrent DW-1137 identifier-inventory drift.
- `env NUGET_PACKAGES="$(mktemp -d --tmpdir frontcomposer-nuget-cold.XXXXXX)" python3 scripts/pack-release-packages.py "$(mktemp -d --tmpdir frontcomposer-pack-cold.XXXXXX)" 0.0.0-ci-test` -- a genuinely empty package cache restored the 4.1.1 baselines and all eight synthetic package packs passed.
- `git diff --check` -- intended changes have no whitespace errors; final status still identifies the pre-existing DW-1137 edit separately.

**Manual checks:**
- After commit and push, new Windows accessibility and workflow reruns remain required for end-to-end proof.
